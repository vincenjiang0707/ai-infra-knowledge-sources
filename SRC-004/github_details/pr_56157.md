# [PR #56157] [PCP][DCP] Enable PCP+DCP on sparse-MLA models

source: https://github.com/vllm-project/vllm/pull/56157
state: closed | updated: 2026-09-23T02:20:20Z
labels: ready, ci/build, deepseek, nvidia, mrv2

## 正文

## Purpose

Enable DCP to run alongside PCP on sparse-MLA models, so a deployment can shard the KV cache for decode and still split prefill query work across ranks.

This change:

- gives the sparse indexer a DCP-gathered prefill path: this rank's KV shard is all-gathered across the DCP group
- gives the FlashMLA-sparse backend the matching prefill path, with a rank-major gathered KV workspace and a PCP-invariant chunk plan every rank derives identically without communicating;
- replicates prefills too short to fill all `2*PCP` chunks when DCP is on, which keeps the per-region row count identical on every rank — the invariant the gathered layout rests on;
- adds a MoE all-reduce fast path for steps whose rows are all PCP-replicated, replacing an unconditional all-gather + reduce-scatter that duplicated every decode row `PCP` times;
- relaxes the PCP CUDA graph restriction for PCP+DCP to everything up to `FULL_AND_PIECEWISE`, keeping only `FULL` over mixed batches rejected;

fixes https://github.com/vllm-project/vllm/issues/53573
Additionally
- fixes a pre-existing kernel/reference divergence: the top-k index kernel applied the block-table bound to prefill-workspace rows, while `_triton_convert_reference_impl` has always specified that only `-1` is invalid there. The existing test missed it because `token_indices[6, 150:160]` is an empty slice on a 128-column tensor;

## Test Plan

- NVIDIA GB200 x4
- GLM-5.3, NVFP4 weights
- TP=1, PCP=4, DCP=4

## Test Result

GSM8K, 1319 questions, TP1+PCP4+DCP4, `FULL_AND_PIECEWISE`. Seven runs across the

| Run | Accuracy | Invalid |
|---|---|---|
| TP4 | 0.830 | 0.000 |
| PCP4DCP4| 0.832 | 0.000 |


## Benchmarks

Prefill, 32768 in / 1 out:

| Layout | TTFT ms | P99 TTFT ms | vs tp4 |
|---|---|---|---|
| tp4 | 5512.1 | 8141.1 | 1.00x |
| pcp4 | 3278.1 | 4814.0 | 1.68x |
| pcp4dcp4 | 3326.8 | 4888.7 | 1.66x |

Decode, 1 in / 1024 out:

| Layout | TPOT ms | out tok/s | vs tp4 |
|---|---|---|---|
| tp4 | 18.6 | 3412.7 | 1.00x |
| dcp4 | 28.4 | 2236.8 | 0.66x |
| pcp4dcp4, PIECEWISE (this PR) | 86.7 | 735.9 | 0.22x |
| pcp4dcp4, with FULL decode graphs and moe deduplication   https://github.com/PatrykSaffer/vllm/compare/patryk/pcp-dcp-sparse-mla...patryk/pcp-dcp-full-cg-no-duplication | 32.6 | 1944.1 | 0.57x |



Why pcp4dcp4 decode is slower than dcp4 decode:
CudaCommunicator.__init__ enables custom all-reduce, torch symm-mem and FlashInfer fused all-reduce for tp groups only, so that per-layer all-reduce runs on NCCL ring.
followup is to enable it, sth like https://github.com/vllm-project/vllm/pull/56157/changes/590e9b7e185df741e5aabbecfed351dd3470c04f

## 评论 (16)

### mergify[bot] · 2026-09-09

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @PatrykSaffer.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


### mergify[bot] · 2026-09-11

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @PatrykSaffer.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


### foraxe · 2026-09-11

Hi @PatrykSaffer maybe we can improve those? 
1. seq_lens_cpu_upper_bound is now carrying a new semantic: under PCP+DCP it becomes the whole global-request extent on every local row. That field is generic and consumed outside sparse MLA too, so I’d prefer either a stronger contract/assertion or a dedicated field rather than silently changing its meaning. It is used across CP/ubatch/model-state paths.
2. _split_pcp_dcp_prefill_chunks() assumes the first PCP row is always a full chunk and any short tail is last:
                  query_lens = num_rows * first_row_query_len
That works with current DualChunkSwap, but it is a hidden coupling to PCP partition policy. I’d at least add an assertion/test around this invariant; otherwise a future PCP layout change can reintroduce divergent chunk plans.

### PatrykSaffer · 2026-09-11

> @PatrykSaffer I think we can pretty simplify this code more, make it less invasive, will try to be more thorough tmrw but heres some initial ideas: [PatrykSaffer#1](https://github.com/PatrykSaffer/vllm/pull/1)
> 
> The main principals here is:
> 
> 1. minimize changes to core files, i.e.: `vllm/v1/worker/gpu/model_runner.py`, `vllm/v1/worker/gpu/cudagraph_utils.py`
> 2. remove new global state (ideally we dont have too much of this), removed `_current_pcp_schedule`
> 3. avoid excessive new datastructures/dataclasses
> 4. try to unify with existing PCP/DCP paths as much as possible

Thanks for review!
LGTM,
for the `Make the gathered prefill a mode of the separate prefill/decode path` I intentionally put it separately to avoid if/else branching, but it's not that bad now, can probably refactored if needed in the future.

### PatrykSaffer · 2026-09-11

> Hi @PatrykSaffer maybe we can improve those?
> 
> 1. seq_lens_cpu_upper_bound is now carrying a new semantic: under PCP+DCP it becomes the whole global-request extent on every local row. That field is generic and consumed outside sparse MLA too, so I’d prefer either a stronger contract/assertion or a dedicated field rather than silently changing its meaning. It is used across CP/ubatch/model-state paths.
> 2. _split_pcp_dcp_prefill_chunks() assumes the first PCP row is always a full chunk and any short tail is last:
>    query_lens = num_rows * first_row_query_len
>    That works with current DualChunkSwap, but it is a hidden coupling to PCP partition policy. I’d at least add an assertion/test around this invariant; otherwise a future PCP layout change can reintroduce divergent chunk plans.

Thanks for review!
added asserts and test.

### github-actions[bot] · 2026-09-11

✅ @PatrykSaffer, CI is now available for this PR.

- `/ci run` starts upstream CI; `/amd-ci run` starts AMD CI only.
- `/ci retry` retries failed jobs in the CI build for the current PR head. If the current head has no CI build, it starts a new CI build for the current head containing only jobs that failed in the latest earlier CI build for this PR.
- `/amd-ci retry` retries failed jobs in AMD CI for the current PR head. Use `/amd-ci run` when the current head has no AMD CI build.
- `/ci cancel` cancels scheduled or running CI builds for this PR branch; `/amd-ci cancel` does the same for AMD CI only.

<!-- vllm-ci-authorized -->

### mergify[bot] · 2026-09-12

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @PatrykSaffer.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


### LucasWilkinson · 2026-09-12

/ci run

### github-actions[bot] · 2026-09-12

✅ Triggered [Buildkite CI #88543](https://buildkite.com/vllm/ci/builds/88543) for commit `f829cab0c032`.

### LucasWilkinson · 2026-09-12

/ci run

### github-actions[bot] · 2026-09-12

✅ Triggered [Buildkite CI #88553](https://buildkite.com/vllm/ci/builds/88553) for commit `81050300cb21`.

### LucasWilkinson · 2026-09-12

/ci run

### github-actions[bot] · 2026-09-12

✅ Triggered [Buildkite CI #88558](https://buildkite.com/vllm/ci/builds/88558) for commit `6830dd62a90a`.

### LucasWilkinson · 2026-09-12

/ci run

### github-actions[bot] · 2026-09-12

✅ Triggered [Buildkite CI #88566](https://buildkite.com/vllm/ci/builds/88566) for commit `6e93ff1f51b8`.

### xuV2 · 2026-09-23

Hi！ Can vLLM 0.30.0 use PCP with Qwen3.6 or Qwen3.8?

## Review (6)

### claude[bot] · 2026-09-09 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.

### LucasWilkinson · 2026-09-11 · COMMENTED

@PatrykSaffer I think we can pretty simplify this code more, make it less invasive, will try to be more thorough tmrw but heres some initial ideas: https://github.com/PatrykSaffer/vllm/pull/1

The main principals here is: 

1) minimize changes to core files, i.e.: `vllm/v1/worker/gpu/model_runner.py`, `vllm/v1/worker/gpu/cudagraph_utils.py`
2) remove new global state (ideally we dont have too much of this), removed `_current_pcp_schedule`
3) avoid excessive new datastructures/dataclasses 
4) try to unify with existing PCP/DCP paths as much as possible

### GirasoleY · 2026-09-11 · COMMENTED

Change overall looks good :)

### GirasoleY · 2026-09-11 · COMMENTED

(no text)

### LucasWilkinson · 2026-09-12 · COMMENTED

LGTM once @GirasoleY comments are resolved :+1:

### LucasWilkinson · 2026-09-12 · APPROVED

(no text)
