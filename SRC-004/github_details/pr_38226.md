# [PR #38226] [Bugfix] Respect max_output_tokens in Harmony tool-call loop

source: https://github.com/vllm-project/vllm/pull/38226
state: closed | updated: 2026-09-23T01:45:11Z
labels: bug, frontend, needs-rebase, stale, tool-calling, gpt-oss

## 正文

## Purpose

Fix the Harmony tool-call loop in `serving.py` to respect `max_output_tokens`. Currently the loop uses `max_model_len - len(token_ids)` to set `sampling_params.max_tokens`, completely ignoring the user's `max_output_tokens` constraint. This allows subsequent tool-call turns to generate far more tokens than intended (e.g., 130K instead of 100).

The `ParsableContext` path on line 697 already correctly calls `get_max_tokens()` — the `HarmonyContext` path was simply missed.

## Test Plan

```bash
python -m pytest tests/entrypoints/openai/responses/test_harmony_max_tokens_tool_loop.py -v
```

## Test Result

All 3 tests pass, verifying that:
1. `max_output_tokens=100` is respected (not 130K)
2. Without explicit limit, falls back to context window
3. `max_output_tokens` exceeding context window is capped correctly

## 评论 (4)

### dubin555 · 2026-04-12

Good catch, thanks for the thorough review! You're absolutely right — the previous commit added the `request` parameter to `HarmonyContext.__init__` but missed the two critical pieces in `serving.py`:

1. Passing `request=request` when constructing `HarmonyContext` and `StreamingHarmonyContext`
2. Replacing the naive `max_model_len - len(token_ids)` with the shared `get_max_tokens()` helper in the Harmony branch

Fixed in the latest push — now the Harmony tool-call loop uses the same `get_max_tokens()` path as `ParsableContext`, properly respecting `max_output_tokens`.

### mergify[bot] · 2026-05-23

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @dubin555.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### github-actions[bot] · 2026-08-22

This pull request has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days. Leave a comment if you feel this pull request should remain open. Thank you!

### github-actions[bot] · 2026-09-23

This pull request has been automatically closed due to inactivity. Please feel free to reopen if you intend to continue working on it. Thank you!

## Review (3)

### claude[bot] · 2026-03-26 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.

### gemini-code-assist[bot] · 2026-03-26 · COMMENTED

## Code Review

This pull request introduces regression tests and a fix to ensure `max_output_tokens` is correctly applied within Harmony tool-call loops, addressing a bug where it was previously ignored. The fix involves using the `get_max_tokens` utility function for `sampling_params.max_tokens` calculation. However, a critical issue was identified: accessing `context.request.max_output_tokens` will cause an `AttributeError` because `HarmonyContext` and `StreamingHarmonyContext` lack a `request` attribute. This requires updating these context classes to store the `request` object.

### dubin555 · 2026-03-26 · COMMENTED

(no text)
