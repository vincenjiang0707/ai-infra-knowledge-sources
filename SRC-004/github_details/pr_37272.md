# [PR #37272] fix(distributed): resolve inference failure in cpu_worker

source: https://github.com/vllm-project/vllm/pull/37272
state: closed | updated: 2026-09-23T01:46:38Z
labels: needs-rebase, stale, v1, cpu

## 正文

## Description

Fixes #37255

When running distributed inference with multiple nodes, the CPU worker fails with bIndexError: list index out of rangeb when accessing .

## Root Cause

When  is not None, the code slices  to a subset based on the data parallel rank and world size. However, it then tries to index into this sliced list using  (global rank), which can be out of bounds for the sliced list.

For example, with 2 nodes and world_size=2:
- Global ranks are 0, 1, 2, 3
- For local_dp_rank=0, the slice gives indices 0-1
- For local_dp_rank=1, the slice gives indices 2-3, but the sliced list only has length 2
- When rank=2 tries to access index 2 on a list of length 2, it fails

## Fix

Calculate the local rank within the DP slice using  to correctly index into the sliced list.

## Testing

Verified the fix with a test script that simulates the distributed scenario described in the issue:
- Rank 0,1 on node 0 (local_dp_rank=0) correctly get indices 0,1
- Rank 2,3 on node 1 (local_dp_rank=1) correctly get indices 0,1 from the sliced list

This fix is minimal and only affects the code path when data_parallel_rank_local is not None (distributed inference scenarios).

## 评论 (5)

### Soulhealer95 · 2026-03-21

I'll preface this with saying that it's my first time looking at this code/working with it so I can't attest to the splice calculation. 

That said, I tried the fix on Mac where `omp_cpu_list` length was `1` and it results in same failure. Probably require a different case for length of 1 where no calculation is needed and simple assignment should suffice? 
something like:
```
diff --git a/vllm/v1/worker/cpu_worker.py b/vllm/v1/worker/cpu_worker.py
index a24553c5c..5a3991040 100644
--- a/vllm/v1/worker/cpu_worker.py
+++ b/vllm/v1/worker/cpu_worker.py
@@ -92,12 +92,18 @@ class CPUWorker(Worker):
         else:
             local_dp_rank = self.parallel_config.data_parallel_rank_local
             omp_cpuids_list = omp_cpuids.split("|")
-            if local_dp_rank is not None:
+            if len(omp_cpuids_list) == 1:
+                self.local_omp_cpuid = omp_cpuids_list[0]
+            elif local_dp_rank is not None:
```

### lgx0319 · 2026-04-15

Friendly bump -- this PR has been open for a while. Happy to make any changes if needed, or close it if no longer relevant.

### mergify[bot] · 2026-05-23

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @BillionClaw.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### github-actions[bot] · 2026-08-22

This pull request has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days. Leave a comment if you feel this pull request should remain open. Thank you!

### github-actions[bot] · 2026-09-23

This pull request has been automatically closed due to inactivity. Please feel free to reopen if you intend to continue working on it. Thank you!

## Review (1)

### gemini-code-assist[bot] · 2026-03-17 · COMMENTED

## Code Review

This pull request addresses an `IndexError` in the `CPUWorker` during distributed inference. The issue stemmed from using a global rank to index a sliced list of CPU IDs when data parallelism is enabled. The fix introduces correct logic to calculate a local index within the sliced list, resolving the out-of-bounds access. The change is correct, minimal, and effectively fixes the bug.
