# [PR #38008] [Core][Frontend] Add gradient computation feature with /v1/gradients API endpoint

source: https://github.com/vllm-project/vllm/pull/38008
state: closed | updated: 2026-09-23T01:45:50Z
labels: frontend, needs-rebase, stale, v1

## 正文

## Summary
- Adds a `/v1/gradients` API endpoint that computes gradients of loss and/or per-token log-probabilities w.r.t. input/output embeddings for a given prompt + target pair, enabling token-level attribution and interpretability analysis.
- Introduces `GradientParams`, `GradientRunner`, and full engine wiring (`EngineCoreRequest`, `AsyncLLM`, `EngineCore`, `InputProcessor`, `OutputProcessor`) so gradient requests flow through the standard vLLM serving stack.
- Gradient requests bypass the scheduler and execute via `collective_rpc` to avoid conflicts with the batched `@torch.inference_mode()` inference path.

## Architecture

```
POST /v1/gradients (GradientRequest: prompt + target text)
  → ServingGradient tokenizes both, builds GradientParams
  → AsyncLLM.compute_gradients() sends EngineCoreRequest
  → EngineCore bypasses scheduler, dispatches via collective_rpc
  → Worker.compute_gradients() → GradientRunner (torch.enable_grad())
  → Results flow back as GradientRequestOutput → GradientResponse JSON
```

## Key components
- **`vllm/gradient_params.py`** — `GradientParams` msgspec struct controlling what/how to compute
- **`vllm/entrypoints/gradient/`** — FastAPI router, Pydantic request/response protocol, serving handler
- **`vllm/v1/worker/gpu/gradient/gradient_runner.py`** — Core computation: forward pass with gradient tracking, per-token backward passes, GPU-side aggregation
- **`vllm/v1/engine/core.py`** — Scheduler bypass via `_handle_gradient_request` + `collective_rpc`
- **`vllm/outputs.py`** — `GradientOutput` and `GradientRequestOutput` data classes

## Test plan
- [ ] Unit test `GradientRunner` with a small model (forward + backward correctness)
- [ ] Integration test `/v1/gradients` endpoint with `pytest` against a running server
- [ ] Verify `gradient_of="loss"`, `"token_log_probs"`, and `"both"` modes
- [ ] Verify `aggregation="l2_norm"`, `"abs_sum"`, and `"none"` modes
- [ ] Verify `target_token_indices` subset selection
- [ ] Confirm normal inference is unaffected (gradient requests don't interfere with batched generation)

## Notes
- AI assistance was used (Claude). All changes were reviewed line-by-line.
- No existing PR addresses this feature (checked via `gh pr list --search`).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 评论 (14)

### github-actions[bot] · 2026-03-24

👋 Hi! Thank you for contributing to the vLLM project.

💬 Join our developer Slack at https://slack.vllm.ai to discuss your PR in #pr-reviews, coordinate on features in #feat- channels, or join special interest groups in #sig- channels.

Just a reminder: PRs would not trigger full CI run by default.

Once the PR is approved and ready to go, your PR reviewer(s) can run CI to test the changes comprehensively before merging.

To run CI, PR reviewers can either: Add `ready` label to the PR or enable auto-merge.

If you have any questions, please reach out to us on Slack at https://slack.vllm.ai.

🚀

### DarkLight1337 · 2026-03-24

Do you have any real use cases for it? Why not simply use Transformers?

### gereblye · 2026-03-25

I want to calculate gradients for reference attribution in an enterprise software which uses vLLM and 1. I want to calculate the gradients with the same model that generated the answer; 2. I don't want to run a model on any local machine

### DarkLight1337 · 2026-03-25

vLLM is built for performance, and I think extracting gradients from it would really degrade that (e.g. because of IPC overhead). To avoid impacting the main service, it would be better to run the gradient extraction separately.

Also IMO, this feature is too niche to justify the intrusive changes. But feel free to maintain it in your own fork!

cc @WoosukKwon @robertgshaw2-redhat 

### gereblye · 2026-03-25

Thanks for the feedback.
Made some performance improvements.

Interest in interpretability is rising, so this could mean this feature will soon be not so niche after all.
<img width="943" height="333" alt="image" src="https://github.com/user-attachments/assets/a74c31be-c4b4-4cde-800c-d92a8e634f9d" />


### mergify[bot] · 2026-03-25

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @gereblye.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### mergify[bot] · 2026-03-25

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @gereblye.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### gereblye · 2026-03-30

What do the other reviewers think @WoosukKwon @njhill @aarnphm @chaunceyjiang @russellb ?

### mergify[bot] · 2026-03-31

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @gereblye.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### gereblye · 2026-04-02

Ping  @WoosukKwon @njhill @aarnphm @chaunceyjiang @russellb 

### russellb · 2026-04-13

> What do the other reviewers think @WoosukKwon @njhill @aarnphm @chaunceyjiang @russellb ?

I'm supportive of @DarkLight1337 's review comments. The changes are indeed very intrusive, so the bar has to be pretty high for an addition like this.

### mergify[bot] · 2026-05-23

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @gereblye.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### github-actions[bot] · 2026-08-22

This pull request has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days. Leave a comment if you feel this pull request should remain open. Thank you!

### github-actions[bot] · 2026-09-23

This pull request has been automatically closed due to inactivity. Please feel free to reopen if you intend to continue working on it. Thank you!

## Review (1)

### gemini-code-assist[bot] · 2026-03-24 · COMMENTED

## Code Review

This pull request introduces a significant new feature for computing gradients via a `/v1/gradients` API endpoint, which is a valuable addition for model interpretability. The implementation is well-structured, adding new modules for the gradient-specific logic and integrating them into the engine by bypassing the scheduler for these specialized requests. I've found one critical issue related to error handling that could cause server-side errors if a gradient computation fails. My feedback includes a specific code suggestion to address this.
