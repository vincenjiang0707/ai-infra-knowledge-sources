# [Issue #56868] [Bug]: GLM-5.3-Flash long-decode degeneration after accumulated reasoning decode

source: https://github.com/vllm-project/vllm/issues/56868
state: open | updated: 2026-09-22T13:17:14Z
labels: bug, tool-calling, quantization, glm

## 正文

### Your current environment

Hardware: 1x NVIDIA B300, ~267 GiB visible VRAM
Runtime: vLLM OpenAI-compatible server
vLLM: 0.28.1rc1.dev580+g385dce36b
FlashInfer: 0.6.18
Transformers: 5.16.1
Torch: 2.13.0+cu130
CUDA: 13.0

Model: private W4A16 quantized GLM-5.3-Flash checkpoint
TP: 1
Context length: ~372k

Reasoning parser: glm45
Tool-call parser: glm47
Native tools enabled

reasoning_effort=max
clear_thinking=true

MTP/EAGLE: disabled for the tests below
Prefix cache: tested on and off; issue happens in both cases

Workload: private long-running multi-agent / sub-agent coding workload with native tool calls.
I cannot post raw prompts or tool outputs publicly.

### 🐛 Describe the bug

### Purpose

I am running GLM-5.3-Flash in a long-running multi-agent / sub-agent coding workload with native tools and `reasoning_effort=max`.

The model works well for many turns, then after a long session it can suddenly collapse inside `reasoning`.

### Bug

The collapse is repetitive low-entropy output such as:

```text
= = = = = = = = ...
```

or:

```text
.0.0.0.0.0.0.0.0...
```

I logged raw SSE before the stream reaches the client. The bad output is already present in raw vLLM SSE, for example many deltas like:

```json
{"delta":{"reasoning":" ="}}
```

So this does not look like a client-side stream reconstruction issue.

When it happens, no tool call is emitted. The model stays stuck in `reasoning`.

### Already checked

- MTP on/off: issue happens in both cases.
- Prefix cache on/off: issue happens in both cases.
- Sampling/repetition settings: issue still happens.
- H200 and B300: similar issue, so not B300-only.
- SGLang: similar degeneration after 15-20 min.
- Lower reasoning or aggressive windowing can delay it, but they are not acceptable fixes for this workload.

Also tested/backported:

- Normalize assistant `content:null` + `tool_calls` to `content:""` before chat templating.
- PR #56635 / commit `b150783`.

The issue still happens.

### Reasoning replay

The client was originally replaying old assistant reasoning into later tool turns. I added a proxy that removes old `reasoning_content`, `reasoning`, and `thinking` before forwarding to vLLM.

In one failing session, the proxy removed ~471k chars of old reasoning and the forwarded request had zero historical `reasoning_content`.

This helped a lot, but the late-session collapse still came back.

### Replay tests

I replayed captured forwarded requests directly against vLLM, without the client.

1. Cold replay of the exact failing request: healthy, ended with `finish_reason=tool_calls`.

2. Five parallel cold replays of the same request: all healthy, all ended with `finish_reason=tool_calls`.

3. Sequential replay of historical requests: after 76 successful requests, request 77 collapsed into `.1.1.1...` then `.0.0.0...`.

4. Cold replay of request 77 only: healthy.

5. Prefill-only warmup: replayed the first 76 historical requests with `max_tokens=1`, then request 77 unchanged. Result: request 77 was healthy.

Key difference:

| Test | Prior prompt tokens | Prior decode/reasoning tokens | request 77 |
|---|---:|---:|---|
| Cold request 77 | 0 | 0 | healthy |
| 76 prefill-only requests, then request 77 | ~2.76M | ~76 | healthy |
| 76 full requests, then request 77 | ~2.76M | ~200k | collapsed |

So it looks more related to accumulated decode/reasoning than to prefill volume alone.

### Question

Could this be a correctness issue in the GLM-5.3 `glm5next` decode path, for example KDA/recurrent state lifecycle, slot reuse, hybrid KV/KDA mapping, CUDA graph replay, or numerical drift during long decode?

Is there a recommended branch or patch set for GLM-5.3-Flash long agentic workloads on single-GPU Blackwell/B300?

### Privacy

The workload is private and proprietary, so I cannot post raw prompts, raw tool outputs, or raw `forwarded.json` publicly.

## 评论 (22)

### gaby · 2026-09-15

@gde-cmyk Are you using Tensor Parallel or Data Parallel? One or more GPUs ?

### gde-cmyk · 2026-09-15

@gaby I'm on a single B300, TP=1, no data parallelism, MTP disabled

I also tested/backported #56635 but I can still reproduce the long-decode collapse.

### Recursive-Logic-Core · 2026-09-15

@gde-cmyk - Your replay isolation table cleanly rules out token-volume prefill saturation and pins the failure mode directly to accumulated decode steps.

This collapse into low-entropy attractors (`= = =` or `.0.0...`) across extended multi-turn sessions is characteristic of recurrent state saturation in hybrid architectures (KDA / linear attention layers). Unlike pure transformer KV caches that remain strictly static once written, recurrent/gated delta states accumulate numerical drift and activation variance over long decode horizons. 

While stripping `reasoning_content` prevents prompt-level attention pollution, continuing a monolithic sequence across 76 turns still drags the recurrent hidden state through an unbounded trajectory.

Instead of waiting for an upstream kernel-level clamping patch, the architectural defense at the orchestrator layer is explicit turn boundary isolation:

1. **State Snapshotting**: At regular intervals (e.g., every 20–30 turns), extract verified task state, tool outputs, and active constraints into a structured baseline.
2. **Sequence Eviction / Cold Reset**: Terminate the logical session and instantiate a fresh sequence context, completely draining the recurrent memory slot.
3. **Baseline Injection**: Re-seed the new sequence with the structured state snapshot at turn 0.

Your Test 1 vs. Test 3 result (cold replay of Request 77 succeeds while sequential replay fails) proves that the prompt itself is fully solvable — the failure is purely the residual state carried across the marathon session. State-decoupling between reasoning bursts prevents the recurrent layers from drifting into that degenerate low-entropy sink.

### gde-cmyk · 2026-09-15

I reproduced another case consistent with this being sequence specific two sub agents entered repetition loops while another sub agent on the same vllm engine kept working normally and a fresh chat also answered correctly during the failure

I’ll try the snapshot/reset mitigation separately

### gaby · 2026-09-16

@gde-cmyk Anyway to reproduce this with curl ?

### GiGaFotress · 2026-09-16

I have the same issue. Using GLM-5.3-Flash-W4A16 (https://huggingface.co/dealignai/GLM-5.3-Flash-CYBERSECURITY-W4A16), it degenerates during multi-agent work or long-horizon coding tasks. When I notice it starting to repeat, I stop the conversation; after letting it sit idle for 1 hour, entering "continue" recovers the conversation.

### gaby · 2026-09-18

@gde-cmyk Any data or way of replicating this that you can share?

We notice the issue goes away if you switch reasoning effort. Still can't pin-point the root issue.

### gaby · 2026-09-18

@gde-cmyk @GiGaFotress .

Try the Sep 17-18 nightly with the same flags: `vllm/vllm-openai:nightly-0bfc7a15d095fe83ecc82b50561a93c177fece2d` to pin it, or `vllm/vllm-openai:nightly`. 

Also can you try with this `VLLM_USE_V2_MODEL_RUNNER=0`

### gde-cmyk · 2026-09-18

@gaby 

Tested both

nightly-0bfc7a15... with the same flags still degenerates in long sessions (====, repetition, word-salad) after ~16 min

VLLM_USE_V2_MODEL_RUNNER=0 on that nightly never reached ready and was stuck >30 min at cuda graph memory profiling

On my older non-nightly baseline VLLM_USE_V2_MODEL_RUNNER=0 starts but later enginecore crashes with cuda error: an illegal memory access was encountered

So nightly doesnt fix the long-decode issue on my setup and V1 also has stability issues here.

I also tested on a few unrelated/simple projects and the same thing happens once the session gets long enough so it doesnt seem tied to one specific prompt/project

### xiaobao520123 · 2026-09-19

@gde-cmyk @gaby Same issue happens on `1x8xH100` for `GLM-5.3-Flash`. Yesterday I had Claude Code try harness new project and around 340K context, immediately after generated some JSON, the chain of thought collapsed. Resuming the conversation would get correct answer but shortly the degeneration again.

Another case where the 'word salad' happened was doing 'needle-in-the-haystack' test, with 512K as its context. Often the model fell into reasoning degeneration with very low-entropy thought and the whole inference collapsed. Hey, you can try this test yourself though.

More details:
Hardware: `1x8xH100`
vLLM commit: `eb42686a30cddf325ffeff7b3bd5e3a7298c2c00`
Model: [GLM-5.3-Flash](https://huggingface.co/zai-org/GLM-5.3-Flash/)
Tool parser: `glm47`
Reasoning parser: `glm45`
Tensor parallelism: `8`
Expert parallelism: `enabled`
Spec decoding: `mtp-5`
Prefix caching: `enabled`
No PD disagg, no DEP, no CP.

### gaby · 2026-09-19

@xiaobao520123 Yeah, there's lots of report and no solution yet.

Related
- https://github.com/vllm-project/vllm/issues/56583 
- https://github.com/vllm-project/vllm/issues/56605

Could be related to this (I tested with H100 and B200): 
- https://github.com/vllm-project/vllm/issues/54359

We need a way of replicating this to ease debugging.

### xiaobao520123 · 2026-09-19

@gaby, @Recursive-Logic-Core has provided very great insight in the low-entropy sink on KDA hybrids. I'll also try looking into best practice on implementing KDA and reducing its precision drift.

### xiaobao520123 · 2026-09-20

@gaby, new update: GLM official has admitted there's issue when the model tries to do reasoning. See https://github.com/zai-org/GLM-5/issues/146#issuecomment-5664237055. And they recently posted a new blog telling how they solved numerical accuracy issue through RSI (Recursive Self-improvement): https://z.ai/blog/glm-built-its-inference-infrastructure#:~:text=Looking%20back%20at,testable%20engineering%20hypotheses.

### gaby · 2026-09-20

@xiaobao520123 That's a different issue. The issue we are seeing here the model generates completely random content unrelated to the prompt or generates corrupted output.

This is probably a flashinfer/vllm issue.

### gaby · 2026-09-21

@gde-cmyk Can you try the latest nightly. I wonder if this fix was the issue https://github.com/vllm-project/vllm/pull/57477 

### leongreiner · 2026-09-21

Adding a data point from our own deployment (GLM-5.3-Flash, 8xH100, vLLM `0.29.1rc1.dev201+g8be5205ab`, `--tool-call-parser glm47 --reasoning-parser glm47`, prefix caching on, `--max-model-len 1048576`).

Replayed real Claude Code agentic session bodies (grown to target length, no recall/completion task — the model must continue with a tool call) through our bridge, 30 trials per size:

| length | degenerate | mode |
|---|---|---|
| 63,000 tokens | 23-30% | mostly a short one-sentence reply then stop, no tool call |
| 97,000 tokens | 7-20% | same, plus one corrupted tool-call argument (`<arg_key>`/`<arg_value>` markup leaking into a JSON key) |

Notably **63K degenerates worse than 97K** in our data — consistent with this being tied to accumulated per-session state rather than a length cliff.

We also specifically tested whether #56635 (parser-boundary fix, merged into the commit we're running) resolves this — it does not. Same rate, same corruption signature, before and after.

Happy to share our replay harness/transcripts if useful.

### gaby · 2026-09-21

@leongreiner Please share a reply script here. The Hopper GPUs have other bugs affecting their usage with GLM-5.3-Flash from what I have seen here on GitHub. I'm using Blackwell (Rented B200 from vast.ai).

### gaby · 2026-09-21

@leongreiner Regarding the corrupted tool-call, I've noticed that too, check this potential fix https://github.com/zai-org/GLM-5/issues/158

### gaby · 2026-09-21

@leongreiner PR https://github.com/vllm-project/vllm/pull/57477 fixed a kv cache corruption. It is several hundred commits ahead of the one you tested.

### leongreiner · 2026-09-21

@gaby thanks for both pointers.

Tested #57477 (`db1bfdd4`) and then went further and tried current `main` too (`dev480`, 4 more GLM-5.3-Flash kpool/indexer fixes past it). Same result both times: identical stalls, identical leaked-markup tool-call corruption, unchanged. Four pins tested now, ~450 commits apart, six targeted fixes between them - no change in the failure signature.

On zai-org/GLM-5#158 - good catch, the `<arg_key>/<arg_value>` tags are an exact match for what's leaking into our corrupted calls. Dug into vLLM's code though, and there's already a guard for string-vs-dict `arguments` in `chat_utils.py` that predates everything we tested and seems to apply on our request path regardless of endpoint. So probably not a simple missing-fix situation - but its fallback (silently emptying malformed arguments) matches one of our two corruption shapes closely enough that we're not done looking at it.

We're on H100 (Hopper), so can't speak to Blackwell - but the corruption reads as textual/template-shaped rather than numeric, if that's useful signal.

Repro script: https://gist.github.com/leongreiner/5f47843e0e4c596a69125588ef5c981c - standalone, stdlib only, point it at any OpenAI-compatible endpoint. It uses a synthetic transcript rather than our real captured session, so it'll run a bit lower than our internal degeneracy rates at a given length - the failure mode reproduces reliably, the exact rate less so. 97K+ hits more consistently than 63K here; use 30 trials for a real CI.

### gde-cmyk · 2026-09-21

@gaby 

I tested the latest nightly with #57477 included. The degeneration is still happening

So the issue is still present on the latest nightly with #57477

### gaby · 2026-09-22

I'm out of ideas. The latest release v0.30.0 claims to officially support GLM-5.3-Flash, but it probably has the same issue.

https://github.com/vllm-project/vllm/releases/tag/v0.30.0
