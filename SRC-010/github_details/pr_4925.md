# [PR #4925] fix: initialize MLA KV-B after online FP8 quantization

source: https://github.com/InternLM/lmdeploy/pull/4925
state: closed | updated: 2026-09-07T14:43:48Z
labels: Bug:P0

## 正文

## Summary

- load MLA `kv_b_proj` through the standard quant-aware linear path
- materialize the absorbed `kc`/`vc` BMM weights after linear weight processing completes
- reuse the two-phase post-load lifecycle for initial loading and online weight updates

## Motivation

When online FP8 is applied to a BF16 checkpoint, the previous special KV-B loader treated `kv_b_proj.weight` as serialized FP8 and waited for a checkpoint `weight_scale_inv`. Online quantization generates that scale instead, so the expected checkpoint scale never arrived and the derived `kc`/`vc` weights could remain uninitialized.

Routing KV-B through the standard linear loader lets it create the effective FP8 weight and scales. A second post-load pass then materializes the BF16 MLA BMM weights from that effective representation, matching the lifecycle used by vLLM.

## Validation

- existing focused PyTorch tests: 15 passed
- pre-commit hooks on all changed files: passed

## Assistance

Assisted with Codex + GPT-5.6-Sol xHigh, reviewed manually


## 评论 (0)

## Review (8)

### copilot-pull-request-reviewer[bot] · 2026-09-02 · COMMENTED

### 🟡 Changes recommended

A newly added generic `get_unquantized_weight()` fallback allocates a full identity matrix and can cause OOM for large layers unless it is made chunked/bounded.

*Once you've addressed the issues Copilot identified, you can request another Copilot review.*

<details>
<summary>Pull request overview</summary>

This PR fixes MLA KV-B initialization when applying online FP8 quantization by routing `kv_b_proj` through the standard quant-aware linear loading path, then performing a second post-load pass to materialize the dependent MLA BMM weights (`kc`/`vc`) from the effective KV-B representation.

**Changes:**
- Added a unified `process_weights_after_loading()` lifecycle (update pass + dependent post-load hook pass) and applied it to both initial loads and online weight updates.
- Introduced `get_unquantized_weight()` on linear modules to recover an effective (dequantized) local weight for absorption into MLA BMM weights.
- Updated DeepSeek v2/v3.2 attention modules to load `kv_b_proj` normally and derive `kc`/`vc` after weights are finalized.
</details>

<details>
<summary>File summaries</summary>

| File | Description |
| ---- | ----------- |
| lmdeploy/pytorch/weight_loader/model_weight_loader.py | Adds a two-pass post-load lifecycle and uses it after loading weights. |
| lmdeploy/pytorch/nn/linear/default.py | Adds a direct `get_unquantized_weight()` implementation for unquantized linear weights. |
| lmdeploy/pytorch/nn/linear/blocked_fp8.py | Adds dequantization logic in `get_unquantized_weight()` for blocked-FP8 weights. |
| lmdeploy/pytorch/nn/linear/base.py | Adds a generic fallback `get_unquantized_weight()` for linear modules. |
| lmdeploy/pytorch/models/deepseek_v32.py | Routes `kv_b_proj` through standard linear construction for v3.2 attention. |
| lmdeploy/pytorch/models/deepseek_v2.py | Adds `kv_b_proj` and a post-load hook to materialize `kc`/`vc`; removes special KV-B loading path. |
| lmdeploy/pytorch/models/deepseek_mtp.py | Removes special KV-B loading path to rely on standard loading + post-load processing. |
| lmdeploy/pytorch/engine/model_agent/agent.py | Reuses the unified post-load lifecycle for online distributed weight updates. |
</details>

<details>
<summary>Review details</summary>

- **Files reviewed:** 8/8 changed files
- **Comments generated:** 1
- **Review effort level:** Lite
</details>

---

💡 <a href="/InternLM/lmdeploy/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

### CUHKSZzxy · 2026-09-02 · COMMENTED

(no text)

### grimoire · 2026-09-02 · COMMENTED

(no text)

### grimoire · 2026-09-02 · COMMENTED

(no text)

### CUHKSZzxy · 2026-09-02 · COMMENTED

(no text)

### grimoire · 2026-09-02 · COMMENTED

(no text)

### CUHKSZzxy · 2026-09-02 · COMMENTED

(no text)

### grimoire · 2026-09-02 · APPROVED

(no text)
