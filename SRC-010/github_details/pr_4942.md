# [PR #4942] fix(serve): wait for XML tool name before streaming arguments

source: https://github.com/InternLM/lmdeploy/pull/4942
state: closed | updated: 2026-09-11T03:17:57Z
labels: 

## 正文

## Motivation

For the XML tool protocol (`qwen3coder` and friends), the streaming parser can emit
argument deltas before the function name has been parsed out of the incoming text.

This is harmless for the OpenAI-compatible endpoint, but it breaks the Anthropic
adapter: a `tool_use` content block must be opened with a non-empty `id` and `name`.
When an argument-only delta arrives first, the adapter opens a `content_block_start`
whose identity fields are empty, which Anthropic-compatible clients reject.

The failure is easy to hit with malformed or truncated XML, e.g. a `<tool_call>`
that carries `<parameter=...>` but never produces a well-formed function header:

```text
</think><tool_call><parameter=query>nvd zabbix</parameter>
```

## Modification

- `XmlToolParser.decode_tool_incremental`: keep parsed arguments in the incremental state
  until the function name has been emitted. Argument deltas are still emitted normally
  once the name delta has gone out, so well-formed streams are unaffected.
- Add regression tests:
  - `test_qwen3_5_parser.py`: incomplete XML function headers must not produce
    argument deltas ahead of the name.
  - `test_endpoints.py`: a malformed XML tool call must not open a `tool_use` block
    with empty identity, and the response is reported as `parse_error`.

## BC-breaking (Optional)

No. Well-formed tool calls produce the same delta sequence as before; only the
ordering of the first argument delta relative to the name delta changes, and only
for streams that previously produced an invalid Anthropic block.

## Checklist

1. Pre-commit or other linting tools are used to fix the potential lint issues.
2. The modification is covered by complete unit tests.
3. The documentation has been modified accordingly, like docstring or example tutorials.


## 评论 (1)

### lvhan028 · 2026-09-07

https://github.com/InternLM/lmdeploy/pull/4802 is handling the parsers issues.

## Review (1)

### copilot-pull-request-reviewer[bot] · 2026-09-07 · COMMENTED

### 🟢 Approval recommended

The parser change is narrowly scoped, aligns with Anthropic protocol requirements, and is covered by targeted regression tests for both the parser and the streaming endpoint behavior.

<details>
<summary>Pull request overview</summary>

This PR fixes an edge case in XML-style tool-call streaming (e.g., `qwen3coder`) where argument deltas could be emitted before the function name is known, which can cause the Anthropic adapter to open an invalid `tool_use` block (empty `id`/`name`). The change delays emitting streamed argument JSON until after the tool name delta has been emitted, and adds regression tests to ensure malformed/incomplete XML fails closed and is surfaced as `parse_error` rather than producing invalid Anthropic SSE events.

**Changes:**
- Update `XmlToolParser.decode_tool_incremental()` to withhold argument deltas until after the function-name delta is emitted.
- Add Qwen3Coder incremental parsing regression tests to ensure arguments never stream ahead of tool identity.
- Add Anthropic streaming regression test to ensure malformed XML tool calls do not open `tool_use` blocks with empty identity and instead yield `parse_error`.
</details>

<details>
<summary>File summaries</summary>

| File | Description |
| ---- | ----------- |
| `lmdeploy/serve/parsers/tool_parser/xml_tool_parser.py` | Gate streamed argument JSON emission on `_name_emitted` to prevent identity-less tool blocks. |
| `tests/test_lmdeploy/serve/parsers/test_qwen3_5_parser.py` | Regression tests for incremental XML parsing ordering and malformed header handling. |
| `tests/test_lmdeploy/serve/anthropic/test_endpoints.py` | Regression test ensuring malformed XML doesn’t produce invalid Anthropic `tool_use` blocks and is reported as `parse_error`. |
</details>

<details>
<summary>Review details</summary>

- **Files reviewed:** 3/3 changed files
- **Comments generated:** 0
- **Review effort level:** Lite
</details>

---

💡 <a href="/InternLM/lmdeploy/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>
