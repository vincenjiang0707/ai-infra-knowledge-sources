# [Issue #25937] Misc. bug: Memory not released after `llama_model_free` if no prompt was run (Metal)

source: https://github.com/ggml-org/llama.cpp/issues/25937
state: closed | updated: 2026-09-22T23:36:10Z
labels: bug-unconfirmed

## 正文

### Name and Version

```
% build/bin/llama-cli --version
version: 10073 (91d2fc387)
built with AppleClang 21.0.0.21000101 for Darwin arm64
```

### Operating systems

Mac

### Which llama.cpp modules do you know to be affected?

libllama (core library)

### Command line

```shell

```

### Problem description & steps to reproduce

On macOS with Metal, if no prompt was ever run, calling `llama_model_free` does not unwire the wired memory. Memory pressure on my Mac stays high.

<img width="590" height="102" alt="Image" src="https://github.com/user-attachments/assets/35f3fc79-deea-42c5-8c90-777d98c09338" />

Running any prompt before `llama_model_free` solves the problem. Wired memory goes down almost immediately after `llama_model_free`.

<img width="594" height="98" alt="Image" src="https://github.com/user-attachments/assets/e2474875-ddd2-4699-8bb9-9b952eeaa262" />

**Reproduction:**

```c
#include <unistd.h>
#include <llama.h>

int main() {
    llama_backend_init();

    struct llama_model* model = llama_model_load_from_file("gemma-4-26B_q4_0-it.gguf", llama_model_default_params());
    llama_model_free(model);

    llama_backend_free();

    sleep(20);

    return 0;
}
```

Then run `top -l 1 | grep PhysMem` during the 20 seconds of sleep and after the program exits.

During the `sleep(20)`, the output is:

```
PhysMem: 44G used (16G wired, 888M compressor), 2843M unused.
```

After the program exits, the output is:

```
PhysMem: 44G used (3000M wired, 888M compressor), 2912M unused.
```

13GB of memory seem to only be unwired after the program exits.

**Debugging:**

When running with `GGML_METAL_NO_RESIDENCY=1`, this problem is not visible. However, that's not good for performance.

I added some logging and found that [`endResidency`](https://github.com/ggml-org/llama.cpp/blob/91d2fc387529940230555abd297a8b5e99737d3f/ggml/src/ggml-metal/ggml-metal-device.m#L1484) was called properly on the residency set in my tests. `device.currentAllocatedSize` went down afterward. Wired memory, however, did not go down.

With the help of Fable 5, I found that doing **any GPU work** in the process before `llama_model_free` causes the system to unwire the memory (even GPU work that is unrelated to the residency set).

<details><summary>Fable-generated patch that does dummy GPU work and causes the memory to be unwired</summary>
<p>

```diff
diff --git a/ggml/src/ggml-metal/ggml-metal-device.m b/ggml/src/ggml-metal/ggml-metal-device.m
index 80e47f2c2..fbe52831b 100644
--- a/ggml/src/ggml-metal/ggml-metal-device.m
+++ b/ggml/src/ggml-metal/ggml-metal-device.m
@@ -869,6 +869,31 @@ ggml_metal_device_t ggml_metal_device_init(int device) {
                 dev->rsets = nil;
             }
 
+            // submit a dummy command buffer with non-empty work so that the OS starts tracking the
+            // process for wired memory accounting. without this, memory wired via residency sets
+            // whose buffers are never referenced by any GPU work stays wired after the buffers are
+            // freed, until the process exits
+            if (dev->props.use_residency_sets && dev->mtl_queue != nil) {
+                @autoreleasepool {
+                    id<MTLBuffer> buf = [dev->mtl_device newBufferWithLength:16 options:MTLResourceStorageModeShared];
+
+                    id<MTLCommandBuffer> cmd_buf = [dev->mtl_queue commandBufferWithUnretainedReferences];
+
+                    {
+                        id<MTLBlitCommandEncoder> encoder = [cmd_buf blitCommandEncoder];
+
+                        [encoder fillBuffer:buf range:NSMakeRange(0, 16) value:0];
+
+                        [encoder endEncoding];
+                    }
+
+                    [cmd_buf commit];
+                    [cmd_buf waitUntilCompleted];
+
+                    [buf release];
+                }
+            }
+
             // print MTL GPU family:
             GGML_LOG_INFO("%s: GPU name:   %s (%s)\n", __func__, dev->props.name, dev->props.desc);
 
```

</p>
</details>

(This patch is the only AI-generated part of this issue. The rest was hand-written by me.)

If maintainers have an idea for how to solve this properly, I'm happy to work on the implementation of a fix (hand-written, of course).

### First Bad Commit

_No response_

### Relevant log output

_No response_

## 评论 (3)

### nikwen · 2026-07-23

I've been digging into this further. Should have a PR up with a fix within 24 hours. I'll also post more details on the bug.

### nikwen · 2026-07-24

**Quick summary:** This is a Metal bug that I've reported to Apple. I can reproduce the issue outside of `llama.cpp`.

Feedback report: [FB23959296](https://feedbackassistant.apple.com/feedback/23959296) (not public)  
Forum post: https://developer.apple.com/forums/thread/839089 (public)

**Bug**

If a process creates a residency set, calls `requestResidency`, `endResidency`, and then releases the residency set without ever having done any GPU operations, the memory from the residency set is not freed.

**Workaround**

If the application runs any GPU operation (even an operation not involving the residency set) at any point in its lifecycle (before/while/after creating/releasing the residency set), the memory is freed properly.

**Reproduction**

1. Store the `repro.m` and `workaround.m` files from below
2. Run the following commands (`repro.m` demonstrates the bug; `workaround.m` demonstrates the workaround):

```
$ clang -framework Foundation -framework Metal -o repro repro.m
$ ./repro 
Footprint at start:                 0.00 GB
Footprint after buffer allocation:  4.30 GB
Footprint 5s after teardown:        4.30 GB
$ clang -framework Foundation -framework Metal -o workaround workaround.m
$ ./workaround 
Footprint at start:                 0.00 GB
Footprint after buffer allocation:  4.37 GB
Footprint 5s after teardown:        0.01 GB
```

We can see that the memory is not released in the `repro.m` case.

**Files**

<details><summary>repro.m</summary>
<p>

```objectivec
// Build:  clang -framework Foundation -framework Metal -o repro repro.m

#import <Metal/Metal.h>
#include <mach/mach.h>

// Returns the physical memory footprint of the process.
static double footprint_gb(void) {
    task_vm_info_data_t info;
    mach_msg_type_number_t n = TASK_VM_INFO_COUNT;
    task_info(mach_task_self(), TASK_VM_INFO, (task_info_t)&info, &n);
    return (double)info.phys_footprint / 1e9;
}

int main(int argc, char ** argv) {
    printf("Footprint at start:                %5.2f GB\n", footprint_gb());

    @autoreleasepool {
        id<MTLDevice> dev = MTLCreateSystemDefaultDevice();

        // Allocate ~4GB of memory.
        const size_t size = 4ULL << 30;
        id<MTLBuffer> buf = [dev newBufferWithLength:size options:MTLResourceStorageModeShared];
        memset(buf.contents, 0xab, size); // fault the pages in

        printf("Footprint after buffer allocation: %5.2f GB\n", footprint_gb());

        MTLResidencySetDescriptor * desc = [[MTLResidencySetDescriptor alloc] init];
        id<MTLResidencySet> rset = [dev newResidencySetWithDescriptor:desc error:nil];
        [desc release];
        [rset addAllocation:buf];
        [rset commit];
        [rset requestResidency];

        [rset endResidency];
        [rset removeAllAllocations];
        [rset commit];
        [rset release];
        [buf release];

        [dev release];
    }

    sleep(5);
    printf("Footprint 5s after teardown:       %5.2f GB\n", footprint_gb());

    return 0;
}
```

</p>
</details> 

<details><summary>workaround.m</summary>
<p>

```objectivec
// Build:  clang -framework Foundation -framework Metal -o workaround workaround.m

#import <Metal/Metal.h>
#include <mach/mach.h>

// Returns the physical memory footprint of the process.
static double footprint_gb(void) {
    task_vm_info_data_t info;
    mach_msg_type_number_t n = TASK_VM_INFO_COUNT;
    task_info(mach_task_self(), TASK_VM_INFO, (task_info_t)&info, &n);
    return (double)info.phys_footprint / 1e9;
}

// Performing any work on the GPU ensures the memory from the residency set will be released.
static void do_dummy_work(id<MTLDevice> dev, id<MTLCommandQueue> queue) {
    @autoreleasepool {
        id<MTLBuffer> tmp = [dev newBufferWithLength:1 options:MTLResourceStorageModeShared];
        id<MTLCommandBuffer> cb = [queue commandBuffer];
        id<MTLBlitCommandEncoder> enc = [cb blitCommandEncoder];
        [enc fillBuffer:tmp range:NSMakeRange(0, 1) value:0];
        [enc endEncoding];
        [cb commit];
        [tmp release];
    }
}

int main(int argc, char ** argv) {
    printf("Footprint at start:                %5.2f GB\n", footprint_gb());

    @autoreleasepool {
        id<MTLDevice> dev = MTLCreateSystemDefaultDevice();
        id<MTLCommandQueue> queue = [dev newCommandQueue];

        // Workaround that ensures the memory will be released.
        // It also works if we call this after the residency set release or at any point in between.
        do_dummy_work(dev, queue);

        // Allocate ~4GB of memory.
        const size_t size = 4ULL << 30;
        id<MTLBuffer> buf = [dev newBufferWithLength:size options:MTLResourceStorageModeShared];
        memset(buf.contents, 0xab, size); // fault the pages in

        printf("Footprint after buffer allocation: %5.2f GB\n", footprint_gb());

        MTLResidencySetDescriptor * desc = [[MTLResidencySetDescriptor alloc] init];
        id<MTLResidencySet> rset = [dev newResidencySetWithDescriptor:desc error:nil];
        [desc release];
        [rset addAllocation:buf];
        [rset commit];
        [rset requestResidency];

        [rset endResidency];
        [rset removeAllAllocations];
        [rset commit];
        [rset release];
        [buf release];

        [queue release];
        [dev release];
    }

    sleep(5);
    printf("Footprint 5s after teardown:       %5.2f GB\n", footprint_gb());

    return 0;
}
```

</p>
</details> 

**My environment**

XCode: 26.6 (17F113)  
Clang: 21.0.0 (clang-2100.1.1.101, arm64-apple-darwin25.5.0)  
macOS: 26.5.2 (25F84)

**Working around the issue in `llama.cpp`**

The easiest way to work around the issue is to schedule a tiny bit of GPU work.

There is almost no performance cost to it and it solves the issue:

<img width="601" height="101" alt="Image" src="https://github.com/user-attachments/assets/ed248b50-ced7-4247-9369-8d3572676964" />

PR with that fix: https://github.com/ggml-org/llama.cpp/pull/26082

### nikwen · 2026-09-22

I was curious if macOS 27 had fixed the underlying Metal bug. It has not.
