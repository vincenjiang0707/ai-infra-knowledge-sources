# [Issue #39479] [torch.compile] config hashing refactor follow-ups

source: https://github.com/vllm-project/vllm/issues/39479
state: open | updated: 2026-09-22T09:56:16Z
labels: help wanted, good first issue, feature request

## 正文

### 🚀 The feature, motivation and pitch

### MOTIVATION.

[PR #26468](https://github.com/vllm-project/vllm/pull/26468) moved vLLM config hashing toward an opt-out model so new compilation-relevant config fields are less likely to be accidentally omitted from torch.compile cache keys. 

This issue tracks the follow-ups and to-dos.

## TODOs
1. Refactor all locations of `compute_hash` to `compile_factors`.  `compute_hash` isn’t the right name because real computation of the hash is actually done by `utils.hash_factors`.
2. before calling `normalize value` in `get_compile_factors`, it needs to check if .compile_factors exists on the subobject. This allows us to avoid handling `PassConfig` specially as in the case for compilation.py 
4. Uniform use of `normalize_value` & `hash_factors` across configs with a compute_hash function.
5. address this comment: [comment here](https://github.com/vllm-project/vllm/pull/26468#discussion_r2545031784)
6. Create a shared helper instead of inlining both `_compute_code_hash` and `compilation_config_hash_factors`
7. Refactor all locations of `compute_hash` to `compile_factors`
8. Follow up on docstrings not being up-to date in the opt-out configs
9. typing cleanups (e.g., return {} where configs don’t affect graphs; use Path for traced files; consistent DP hash validation.

always print hashed configs into cache_key_factors.json

## CC List.
@ProExpertProg @zou3519 @hmellor 

### Alternatives

_No response_

### Additional context

_No response_

### Before submitting a new issue...

- [x] Make sure you already searched for relevant issues, and asked the chatbot living at the bottom right corner of the [documentation page](https://docs.vllm.ai/en/latest/), which can answer lots of frequently asked questions.

## 评论 (27)

### Alexey-Savelyev-0 · 2026-04-16

Hi @ProExpertProg, I'm new to vLLM and would like to tackle this issue. May I be assigned this? 

### monkeypunkey21 · 2026-04-16

Hey @ProExpertProg, I can take todos 5, 7 and 8.

### ProExpertProg · 2026-04-16

Feel free to collaborate and we can combine/compare your approaches

### WorldExplored · 2026-04-16

@ProExpertProg I'm working on 1 through 4.

### kasha01 · 2026-04-21

Regarding point No. 5
> Create a shared helper instead of inlining both _compute_code_hash and compilation_config_hash_factors

I noticed `_compute_code_hash` is only used in `vllm/compilation/decorators.py` [here](https://github.com/vllm-project/vllm/blob/8256833fe6f90e508b0264120757c5ea999a044d/vllm/compilation/decorators.py#L262).
Isn't it better to keep them in `vllm/compilation/caching.py` and if future development reveals these functions are needed outside the compilation module, then we can reconsider moving its location into a shared helper?

2nd question, I couldn't find `compilation_config_hash_factors` anywhere in the source code. Was the function renamed or deleted?

### ghost · 2026-04-22

> 2nd question, I couldn't find `compilation_config_hash_factors` anywhere in the source code. Was the function renamed or deleted?

@kasha01 you can refer to this pr to find related/old code: https://github.com/vllm-project/vllm/pull/29117 
all of these to-dos came from 29117. I don't have the bandwidth to verify but feel free to see if your q's are answered here. thanks.

### zzl-7 · 2026-04-25

@kasha01 Are you stilling working on 5.) , if i am not mistaken i think compilation config hash might be related to backends.py , i did a quick draft feel free to reference 
https://github.com/vllm-project/vllm/pull/40890


### VPC-byte · 2026-04-27

I would like to help with a narrow follow-up slice that does not overlap the active config-hashing refactor PRs: CPU-only tests and metadata/debuggability hardening around the cache key factors written for torch.compile. If that scope is useful, I can start with a small failing test around hash-factor serialization/metadata rather than changing runtime compile behavior first.

### kasha01 · 2026-04-29

> > 2nd question, I couldn't find `compilation_config_hash_factors` anywhere in the source code. Was the function renamed or deleted?
> 
> [@kasha01](https://github.com/kasha01) you can refer to this pr to find related/old code: [#29117](https://github.com/vllm-project/vllm/pull/29117) all of these to-dos came from 29117. I don't have the bandwidth to verify but feel free to see if your q's are answered here. thanks.

Looks like `compilation_config_hash_factors` was renamed to `aot_compile_hash_factors` per [PR #25205](https://github.com/vllm-project/vllm/pull/25205/changes).

@zzl-7 I think you are correct, the compilation config hash seems to be related to backends.py. I also landed on this closed [PR 29057](https://github.com/vllm-project/vllm/pull/29057) which seems was attempting to do the same.

@zzl-7 are you going to publish your draft PR to address the refactoring for (5)?

### mukund1985 · 2026-04-30

Hi @WorldExplored — any update on items 1/2/4? Happy to wait if you have a branch in flight. Either way, I've scoped a standalone PR covering **TODO 9** (typing cleanups: `LoadConfig.compute_hash` → `hash_factors({})`, `-> str` annotation on `ParallelConfig.compute_hash`, `traced_files: set[Path]`) plus the `cache_key_factors.json` expansion to dump per-sub-config hashes for easier cache-miss debugging — deliberately non-overlapping with the rename and with PR #40890. Opening it now; @ProExpertProg please flag if the sequencing is wrong.

### WorldExplored · 2026-04-30

> Hi [@WorldExplored](https://github.com/WorldExplored) — any update on items 1/2/4? Happy to wait if you have a branch in flight. Either way, I've scoped a standalone PR covering **TODO 9** (typing cleanups: `LoadConfig.compute_hash` → `hash_factors({})`, `-> str` annotation on `ParallelConfig.compute_hash`, `traced_files: set[Path]`) plus the `cache_key_factors.json` expansion to dump per-sub-config hashes for easier cache-miss debugging — deliberately non-overlapping with the rename and with PR [#40890](https://github.com/vllm-project/vllm/pull/40890). Opening it now; [@ProExpertProg](https://github.com/ProExpertProg) please flag if the sequencing is wrong.


Yes, my current pr, https://github.com/vllm-project/vllm/pull/40246/ address currently 1-4 and 6. It is ready for review, so feel free to work on other parts of this issue! 


### mukund1985 · 2026-05-01

Thanks for the update @WorldExplored — took a look at #40246, no overlap with my PR #41381 which covers TODO 9 and the cache_key_factors debug expansion. Good luck with the review!

### zzl-7 · 2026-05-01

> are you going to publish your draft PR to address the refactoring for (5)?

@kasha01  i want to be mindful not to step on your work if you are driving this, feel free to reference the draft to address 5, or if you want to split 5.) I can clean up and publish.

### SonicSaurav · 2026-05-01

Incase someone is not working on this can i take this pr it will be my first commit towards vllm 


### kasha01 · 2026-05-04

> > are you going to publish your draft PR to address the refactoring for (5)?
> 
> [@kasha01](https://github.com/kasha01) i want to be mindful not to step on your work if you are driving this, feel free to reference the draft to address 5, or if you want to split 5.) I can clean up and publish.

Thanks @zzl-7 for the mindful thinking. You did the hard work of the PR, feel free to publish and I can review.

### 02ez · 2026-05-26

I can take a small scoped follow-up here if it’s still useful and not already covered by the recent linked commits.

Proposed scope:
- TODO 7: update stale docstrings in the opt-out config hashing path
- TODO 8: typing cleanup only in the same touched files, if maintainers want that bundled

I’ll avoid behavior changes and won’t touch the broader `compute_hash` to `compile_factors` refactor unless requested.

Before I open a PR: are TODO 7 or TODO 8 still unclaimed after the recent commits, or should I wait for those changes to land first?

### weeta-code · 2026-06-19

Hey @02ez if possible I'd love to take one of those todos off your hands if you haven't gotten a chance to finish up on them!

### 02ez · 2026-06-21

> Hey [@02ez](https://github.com/02ez) if possible I'd love to take one of those todos off your hands if you haven't gotten a chance to finish up on them!

Hey @weeta-code, go ahead! I hadn’t heard anything back on whether these were still needed/unclaimed, so I haven’t started on them.

### renjingxiao · 2026-06-22

Hi @weeta-code , are you working on todo 7 and 8, or one of them? I'd like to pick up whichever is still available.

### Arraddhyyad · 2026-06-25

Hi! I'd like to pick up TODO 7 (docstring updates) if it's still available — I see the conversation between @weeta-code and @renjingxiao but no one has confirmed starting yet. Happy to coordinate if needed!

### NongFuShangQuan · 2026-06-25

Hi @ProExpertProg, I'm interested in this torch.compile config hashing refactor. I have some experience with PyTorch and vLLM inference. Would it be possible for me to work on part of it or collaborate with others? Happy to discuss approaches first.

### twb1235 · 2026-08-20

Exclude model field from ModelConfig.compute_hash()?
Current behavior:
ModelConfig.compute_hash() includes the model field (HF name or local path). This causes cache misses when the same model is loaded from different paths:

```
vllm serve /data1/Qwen3-8B --tensor-parallel-size 1

vllm serve /data2/Qwen3-8B --tensor-parallel-size 1
```
Both instances compute different config_hash solely because self.model differs, so Instance B cannot reuse Instance A's compiled cache.

Question:
Can we add "model" to ignored_factors in ModelConfig.compute_hash() (model.py:375)?

### SomeshBhandarkar · 2026-09-02

@twb1235 Thanks for flagging this — nice catch. I'd like to look into this if that's alright.

My understanding: the model field (HF name or local path) doesn't affect the actual compiled graph, only the model's architecture/config does — which should already be captured by other fields in the hash. If that's right, excluding model from ModelConfig.compute_hash() via ignored_factors should fix the cache-miss-on-path-change issue.

I'll dig into model.py:375 and see if this holds up — will follow up with a PR if so. Also let me know if I'm missing something about why model might need to stay in the hash cuz I'm kinda new to this!

### yashraj4 · 2026-09-06

Hi @ProExpertProg, I’m interested in helping with the remaining config-hashing follow-ups. I see that #40246, #40890 and #46714 already cover several TODOs, and there has also been recent work around typing/cache-key factors. Is there a specific TODO from #39479 that is still unclaimed and useful to work on? I’m particularly interested in the hashing/normalization or test coverage side.

### justinsortland · 2026-09-07

Hi @ProExpertProg ! I noticed the comment above after posting. I’m specifically interested in the remaining code-hashing cleanup from TODO 5. There still seems to be duplicated logic between the compilation backend and the helpers in `compilation/caching.py`.

If that particular piece isn’t already being worked on, I’d be happy to take it and add some focused tests while preserving the existing cache behavior.


### Manya1609 · 2026-09-09

Hi @ProExpertProg !
 I’m a first-time vLLM contributor and I’m interested in TODO 7. I inspected the current hashing implementation and noticed that the compute_hash() docstrings in CacheConfig and ModelConfig still instruct contributors to add new compilation-relevant fields to the factors list. Both implementations now use get_hash_factors() with an ignored_factors set, so fields are included by default and only non-compilation fields need to be explicitly excluded.  
Is updating these two docstrings the intended scope of TODO 7, and is this item still available?

### akshatgit07 · 2026-09-22

Hi! I’m a first-time vLLM contributor interested in learning the `torch.compile` configuration and cache-key path. I see several contributors are already assigned, so I don’t want to duplicate work. Is there an unclaimed beginner-sized follow-up—perhaps the outdated docstrings or a contained typing cleanup—that I could take? I’m happy to start with a narrowly scoped change and add/update tests as requested.
