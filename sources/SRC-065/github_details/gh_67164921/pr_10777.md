# [PR #10777] [Backend] Recognise reversed mul operands in CombineBroadcastMulReducePattern

source: https://github.com/triton-lang/triton/pull/10777
state: closed | updated: 2026-09-22T21:08:20Z
labels: 

## 正文

CombineBroadcastMulReducePattern folds a broadcast-mul-reduce form into `tt.dot`. After #10545 generalized this combine, the pattern no longer requires explicit `tt.expand_dims` ops: it identifies dot-like operands from broadcasted shapes such as `(M, K, 1) -> (M, K, N)` and `(1, K, N) -> (M, K, N)`.

Since `arith.mulf` is commutative, the two broadcast operands may appear in either order. However, the matcher still assumed that mul operand 0 was the axis-2 broadcast and mul operand 1 was the axis-0 broadcast.

For example, this order is matched:

```mlir
%lhs_b = tt.broadcast %lhs : tensor<32x16x1xf32> -> tensor<32x16x32xf32>
%rhs_b = tt.broadcast %rhs : tensor<1x16x32xf32> -> tensor<32x16x32xf32>
%mul = arith.mulf %lhs_b, %rhs_b : tensor<32x16x32xf32>
%out = "tt.reduce"(%mul) <{axis = 1 : i32}> ...
```

But the equivalent reversed order is missed:

```mlir
%mul = arith.mulf %rhs_b, %lhs_b : tensor<32x16x32xf32>
```

This patch identifies the dot operands from the broadcast axes instead of from the `arith.mulf` operand order. The axis-2 source is still used as the `tt.dot` lhs and the axis-0 source as rhs, so both multiplication orders produce the same `tt.dot`.

This is similar to #10734 in that it removes an unnecessary ordering assumption when matching operands of a commutative operation.

Added 1 lit test in `test/Triton/combine.mlir`, covering the swapped `arith.mulf` operand order in the generalized broadcast-reduce dot combine pattern.

## 评论 (2)

### ThomasRaoux · 2026-07-02

do you have a practical use case for this? This pattern is there for historic reason and in general we expect user to directly use dot operation. 
I don't think trying to fix any kind of suboptimal written case is the right strategy

### Chennesxu · 2026-07-03

> do you have a practical use case for this? This pattern is there for historic reason and in general we expect user to directly use dot operation. I don't think trying to fix any kind of suboptimal written case is the right strategy

To be honest, I don't have a concrete production workload that depends on the reversed form.

The source-level case that can trigger this is when the existing broadcast-mul-reduce spelling is written with the multiplication operands reversed, e.g.

```python
x_b = tl.expand_dims(x, 2)  # [M, K] -> [M, K, 1]
y_b = tl.expand_dims(y, 0)  # [K, N] -> [1, K, N]
out = tl.sum(y_b * x_b, axis=1)
```

This is mathematically the same as the already-supported `tl.sum(x_b * y_b, axis=1)` form, and I verified that the initial TTIR preserves that source operand order, producing an `arith.mulf` with the axis-0 broadcast as operand 0 and the axis-2 broadcast as operand 1.

But I understand the concern that this is a historical pattern and users should normally write `tl.dot` directly. If you don't think this is worth extending without a concrete user/codegen case, I am fine closing this PR.
