# [PR #9684] docs: add FP4 rhs_k_pack constraint for sm120 in dot_scaled

source: https://github.com/triton-lang/triton/pull/9684
state: closed | updated: 2026-09-22T21:14:01Z
labels: 

## 正文

## Summary

Add documentation for `rhs_k_pack` parameter in `dot_scaled` to clarify the FP4 constraint on sm120 architecture.

## Details

As reported in #9678 and confirmed by @masahi, FP4 formats (e2m1, used by MXFP4/NVFP4) require `rhs_k_pack=True` (the default) on sm120 (Blackwell desktop GPUs like GB202/GB203). Using `rhs_k_pack=False` with FP4 inputs on sm120 causes a compilation assertion failure in `MMAv2.cpp`:

```
Assertion `repA[2] == repB[1]' failed.
```

On sm100 (data-center Blackwell GPUs like GB100/GB200), `rhs_k_pack=False` is supported for FP4.

This information was not documented, leading to confusing compilation errors for users on sm120 hardware.

Closes #9678


## 评论 (7)

### masahi · 2026-03-11

I realized that we can just skip lowering to sm120 native scaled dot when `lhs_k_pack` or `rhs_k_pack` is False. See https://github.com/triton-lang/triton/blob/main/lib/Dialect/TritonGPU/Transforms/AccelerateMatmul.cpp#L682-L701. We can just add one more skip there. That way, it will be lowered to `dequantize -> fp16 MMA`. So we don't have to say that "K pack is required".



### liuyun7345 · 2026-03-11

Thanks for the great suggestion! I've updated the PR to skip the sm120 native scaled dot path when lhs_k_pack=False or rhs_k_pack=False for FP4, letting it fall back to dequantize -> fp16 MMA instead. Removed the doc-only changes accordingly.

### masahi · 2026-03-11

C++ change looks good, please add a lit test

### liuyun7345 · 2026-03-11

Added two lit tests: one for rhs_k_pack=false and one for lhs_k_pack=false, both verifying the fallback from native MMAv2 to decomposed dot.

### masahi · 2026-03-11

@liuyun7345 please run the pre-commit

### liuyun7345 · 2026-03-12

Done, ran pre-commit and fixed the clang-format issue.

### peterbell10 · 2026-09-22

This was fixed in #10726

## Review (2)

### masahi · 2026-03-10 · COMMENTED

(no text)

### liuyun7345 · 2026-03-11 · COMMENTED

(no text)
