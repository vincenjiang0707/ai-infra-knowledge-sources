# [PR #10602] [Hopper] Fix if-result data partition slicing

source: https://github.com/triton-lang/triton/pull/10602
state: closed | updated: 2026-09-22T22:28:36Z
labels: 

## 正文

# New contributor declaration
- [x] I am not making a trivial change, such as fixing a typo in a comment.

- [x] I have written a PR description following these
  [rules](https://cbea.ms/git-commit/#why-not-how).

- [x] I have run `pre-commit run --from-ref origin/main --to-ref HEAD`.

- Select one of the following.
  - [x] I have added tests.
    - `/test` for `lit` tests
    - `/unittest` for C++ tests
    - `/python/test` for end-to-end tests
  - [ ] This PR does not need a test because `FILL THIS IN`.

- Select one of the following.
  - [ ] I have not added any `lit` tests.
  - [x] The `lit` tests I have added follow these [best practices](https://mlir.llvm.org/getting_started/TestingGuide/#filecheck-best-practices),
    including the "tests should be minimal" section. (Usually running Python code
    and using the instructions it generates is not minimal.)

## Why

Fixes #9559.

`getBackwardSliceToPartition` returns `true` when a value can be sliced and `false` when partitioning should fail. The `scf.if` result path handled the else yield with `if (!getBackwardSliceToPartition(...)) return false;`, but the then yield used the opposite polarity. That made a successful then-yield backward slice abort the whole data partition.

This matters when a warp-specialized value flows through an `scf.if` before reaching a partitioned consumer such as `ttng.warp_group_dot`: the then and else yield operands both have to be followed backward before the result can be sliced.

## What changed

- Make the then-yield recursive check use the same success polarity as the else-yield path.
- Add a `ws_data_partition.mlir` regression where a `ttg.local_alloc` result is yielded through both arms of an `scf.if` and then consumed by `ttng.warp_group_dot`.

There was an earlier closed PR for the one-line code change (#9794). This version adds a focused lit regression and avoids carrying unrelated metadata.

## Testing

- `pre-commit run --from-ref upstream/main --to-ref HEAD`
- `pre-commit run --files third_party/nvidia/hopper/lib/Transforms/WarpSpecialization/WSDataPartition.cpp test/Hopper/WarpSpecialization/ws_data_partition.mlir`
- `git diff --check upstream/main..HEAD`

I could not run the lit invocation locally because this Windows checkout does not have `triton-opt` / `FileCheck` binaries available. The added test is intended for the existing CI command:

```bash
triton-opt test/Hopper/WarpSpecialization/ws_data_partition.mlir -split-input-file --nvgpu-test-ws-data-partition=num-warp-groups=3 | FileCheck test/Hopper/WarpSpecialization/ws_data_partition.mlir
```


## 评论 (8)

### he-yufeng · 2026-06-15

CI showed the new lit case being reduced to an empty function on every integration runner. The polarity fix itself is correct; the extra `scf.if` task-ID propagation I had added was not part of that fix and changed partitioning behavior beyond the issue.

I removed that propagation and kept the patch focused on the confirmed missing negation plus the regression test. The branch is also rebased onto current `main`.

Validation:

```text
pre-commit run --files third_party/nvidia/hopper/lib/Transforms/WarpSpecialization/WSDataPartition.cpp test/Hopper/WarpSpecialization/ws_data_partition.mlir
git diff --check upstream/main..HEAD
```

Both pass locally. This Windows checkout has no local `triton-opt` build, so the focused lit command is being revalidated by the integration workflow.

Current head: `5cd622a62`


### manman-ren · 2026-06-15

BTW we have similar fixes in https://github.com/facebookexperimental/triton/blob/main/third_party/nvidia/hopper/lib/Transforms/WarpSpecialization/WSDataPartition.cpp#L395. We are trying to unify/upstream Meta's autoWS with OAI/NVidia folks targeting both Hopper and Blackwell.

### he-yufeng · 2026-06-16

This is approved but it looks like CI never ran. Is there something I should do to trigger the checks, or is it good to merge as is?

### he-yufeng · 2026-06-24

Rebased onto main to refresh this.

The red integration checks above are from a much earlier push of this branch (an older revision of the test), not the current commit. What's left now is just the one-line `!` so the then-yield backward slice propagates an unpartitionable result the same way the else-yield already does, instead of falling through and later tripping the `llvm_unreachable` at WSDataPartition.cpp:775 during slicing. Plus a focused lit test for the if-result case.

The hardware integration jobs don't seem to re-run on fork pushes, so they'd need a re-trigger to reflect the current commit.


### he-yufeng · 2026-08-03

@manman-ren re-verified today against current main (WSDataPartition.cpp:323): the inverted condition is still there. `getBackwardSliceToPartition(thenYieldArg, ...)` returning true makes the whole partition fail, so if-results get partitioned exactly when the backward slice does not apply, and skipped when it does. The one-line flip in this PR plus the two lit tests still hold. Since you approved it back in June, is there anything left on my side before it can land?


### he-yufeng · 2026-09-13

This has been approved since June and re-verified still-applies on 08-03 against the same WSDataPartition.cpp:323 condition (rebased 06-24, so the head is recent-ish). The red integration checks on the page are from a stale run before the rebase. Anything left on my side before this can land, or is it just waiting in the merge queue?


### he-yufeng · 2026-09-20

Re-verified against current main (73126ea71). The pass has moved to `third_party/nvidia/hopper/lib/Transforms/WarpSpecialization/WSDataPartition.cpp`, and the inverted condition is still there at line 323: the then-branch still bails out when `getBackwardSliceToPartition` succeeds, while the else-branch right below bails when it fails. The rebased diff still applies to the new path.

This has been approved since June. @manman-ren anything else you need from me to land it?


### peterbell10 · 2026-09-22

Closing in favor of #9800

## Review (2)

### manman-ren · 2026-06-15 · COMMENTED

LGTM. Thanks for the fix!


### manman-ren · 2026-06-15 · APPROVED

(no text)
