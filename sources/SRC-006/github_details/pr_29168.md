# [PR #29168] [AMD] Enable unified-KV HiSparse on ROCm for DeepSeek-V4

source: https://github.com/sgl-project/sglang/pull/29168
state: open | updated: 2026-09-23T07:06:39Z
labels: amd, deepseek, run-ci, memory-pool

## 正文

<!-- Thank you for your contribution! Please follow these guidelines to enhance your pull request. If anything is unclear, submit your PR and reach out to maintainers for assistance. Join our Slack community at https://slack.sglang.io to discuss further. -->

## Motivation

<!-- Describe the purpose and goals of this pull request. -->
On ROCm, HiSparse currently runs only on the **separate-KV** DeepSeek-V4 path (`SGLANG_HACK_FLASHMLA_BACKEND=triton`, packed FP8 KV layout), enabled by #26639. The **unified-KV** backend (`SGLANG_HACK_FLASHMLA_BACKEND=unified_kv_triton`) — where compressed C4 KV lives in the unified pool's `rows[swa_pages:]` and the HiSparse hot buffer is a bf16 view into that region backed by a linear host cold pool — was blocked by a temporary startup guard. 
This PR enables unified-KV + HiSparse on ROCm end-to-end, and adds JIT/unit/eval coverage.

## Modifications

<!-- Detail the changes made in this pull request. -->
- Enable unified-KV HiSparse on ROCm (`hisparse_coordinator.py`, `deepseek_v4_memory_pool.py`, `compressor_v2.py`, `arg_groups/hisparse_hook.py`, `srt/mem_cache/allocator/hisparse.py`): remove the temporary guard that blocked unified_kv_triton + HiSparse, wire the unified C4 device pool / linear host cold pool, and fix the compressed-C4 remap to account for the pre-applied swa_pages offset.
- Enable dsv4 paged HiSparse JIT kernel tests on ROCm (`test/registered/kernels/ops/kvcache/test_hisparse.py`).
- Honor the C4 shrink factor on the unified path (`deepseek_v4_memory_pool.py`, `mem_cache/allocator/hisparse.py`): upstream `pool_configurator` already folds `host_to_device_ratio = S` into `bytes_per_full_token` as a `1/(4S)` C4 term and  raises `max_total_num_tokens` accordingly, but the unified pool ignored it. This sizes the on-device C4 region to the shrunk budget via `c4_compress_pages`, and stops `available_size()` / `full_available_size()` from clamping capacity to the  device C4 pool — on the unified path the cold remainder is host-resident, so the bound is the logical full-token pool. Net effect: freed VRAM actually reaches `full_token`. Separate-KV / CUDA behaviour is unchanged.
- Unit tests for the unified-KV HiSparse pool (`test/registered/unit/mem_cache/test_hisparse_unified_pool.py`): host-mirror geometry, view aliasing of the unified compressed region, c4-only layer views, alloc/free mapping, oversubscribe and that c4_compress_pages actually shrinks the device C4 region while the c128 layers are untouched.
- Eval tests (`.../mi35x/..._unified_eval_mi35x.py` and `..._unified_dp_eval_mi35x.py`): GSM8K accuracy + a ~19k-token long-context swap-path retrieval test, on TP8 and on TP8+DP-attention.

**Highlight — the NVIDIA CUDA forward path is unchanged.** All modifications are confined to ROCm-only branches: `_is_hip` guards and HiSparse/unified-KV-gated code. (The JIT test change only removes `skipif(is_hip())` markers, so those tests now also run on ROCm; CUDA behavior is unaffected.)

To enable this feature, you can pass e.g. 
```
    --enable-hisparse \
    --hisparse-config '{"top_k": 1024, "device_buffer_size": 2048, "host_to_device_ratio": 4}'
```
in the server launch configs.

## Accuracy Tests

<!-- If this pull request affects model outputs (e.g., changes to the kernel or model forward code), provide accuracy test results. -->

GSM8K few-shot accuracy alone does **not** validate the host↔device swap path. With `device_buffer_size=2048` (compressed-token budget) and GSM8K prompts of ~1k–1.7k tokens (~250–425 compressed after the 4x C4 ratio), every request stays on the fast path (`host_len <= device_buffer_size` → preloaded to the device hot buffer), so decode is all device-resident hits and the **cold→hot swap-in (miss-copy) kernel is never exercised**. GSM8K therefore validates: sparse top-k selection numerics, unified-KV pool wiring/aliasing, the compressor write path, and HIP-graph decode stability — but **not** swap correctness.

**Note:** a nonzero #cpu token in the decode log during GSM8K is the authoritative host backup of compressed C4 (every request is backed up to host), not decode-time swap-in. The swap-in (miss-copy) path is gated per-request by `host_len > device_buffer_size`; GSM8K's per-request footprint (~250–425 compressed) stays below `device_buffer_size=2048`, so it runs entirely on the preloaded hot path.

Swap correctness is established by, in increasing scope:

1. **JIT kernel tests** (`test/registered/kernels/ops/kvcache/test_hisparse.py`): `test_dsv4_swap_in_reads_paged_host_layout`, `*_miss_copy_layout`, and the LRU/miss cases check the swap-in kernel byte-for-byte against a reference.
2. **End-to-end long-context swap test** (`test_b_long_context_swap`): a ~19k token prompt (~4750 compressed > 2048) forces a host cold pool and a swap-in on every decode step; a passcode buried in the long context can only be retrieved if cold→hot copies land correctly. This is the authoritative end-to-end swap oracle.

**Accuracy of record:** The numbers reported here **(TP8 0.953 / TP8 DP8 0.949)** were reproduced locally on `deepseek-ai/DeepSeek-V4-Pro`.

## Tests reproduction guidance
### JIT kernel (11 passed)
```
HIP_VISIBLE_DEVICES=0 python -m pytest test/registered/kernels/ops/kvcache/test_hisparse.py -v
```
These now also run on ROCm (the `skipif(is_hip())` markers were removed). The swap-in kernel cases (`test_dsv4_swap_in_reads_paged_host_layout`, `*_miss_copy_layout`, LRU/miss) compare the host→device cold-copy byte-for-byte against a reference, so a pass proves the swap-in kernel is numerically correct.
### unit test (6 passed)
```
HIP_VISIBLE_DEVICES=0 python -m pytest test/registered/unit/mem_cache/test_hisparse_unified_pool.py -v
```
Validates the unified-KV pool: host-mirror geometry, the bf16 view aliasing into `rows[swa_pages:]`, C4-only layer views, alloc/free index mapping, and oversubscribe handling.
### e2e test (2 passed each)
```
python -m pytest -s -v test/registered/amd/accuracy/mi35x/test_deepseek_v4_pro_fp4_hisparse_unified_eval_mi35x.py
python -m pytest -s -v test/registered/amd/accuracy/mi35x/test_deepseek_v4_pro_fp4_hisparse_unified_dp_eval_mi35x.py
```
Each file runs two methods. **What proves the feature is live:**
**(a) Server startup** — the unified-KV HiSparse host cold pool is allocated, and the config is applied:
```text
... enable_hisparse=True, hisparse_config='{"top_k": 1024, "device_buffer_size": 2048, "host_to_device_ratio": 2}'
... Allocating 31.35 GB host memory for V4 paged pool 'dsv4_hisparse_c4' ...
```
The `dsv4_hisparse_c4` host pool only exists on the HiSparse path; its presence with `unified_kv_triton` confirms the unified hot/cold wiring is taken.
**(b) `test_a_gsm8k`** — accuracy / sparse-selection guard. Expect the method to **PASS** with GSM8K accuracy ≈ 0.95 (assertion gate `> 0.91`).
**(c) `test_b_long_context_swap`** — the swap-path guard, and the **authoritative evidence that host↔device swap works**. A ~19k-token prompt with a passcode buried at the top is sent; expect the method to **PASS** with the passcode echoed back:
```text
long_context_swap completion='7G-ZULU-4419'
```
The corresponding server decode line is the smoking gun — a single long request whose compressed C4 footprint far exceeds `device_buffer_size=2048`:
```text
Decode batch, #running-req: 1,, ..., #gpu token: 2049, gpu token usage: 0.00, #cpu token: 9566, cpu token usage: 0.01, cuda graph: True, ...
```
- `#gpu token: 2049` ≈ `device_buffer_size` (2048) → the device hot buffer is capped at the configured budget.
- `#cpu token: 9566` (> 0) → the remaining cold C4 tokens live in the host pool and are streamed in on every decode step.
- The passcode can only be retrieved if those cold→hot swap-ins land correctly, so a PASS proves end-to-end swap correctness (the DP file shows the same behavior under `--dp 8 --enable-dp-attention`).

## Speed Tests and Profiling

<!-- If this pull request impacts inference speed, provide benchmarking and profiling results. -->
This is a **capacity** feature, not a latency one, and it is worth being explicit about the trade rather than reporting a throughput number that misses the point.
`host_to_device_ratio = S` shrinks the GPU-resident C4 region to `1/S` of the budget and mirrors the remainder on the host. Upstream `_get_bytes_per_full_token` already carries the resulting `1/(4S)` C4 term, so the freed VRAM shows up
directly as a larger `max_total_num_tokens`. #32368 measures 12.90M → 17.56M (+36%) on the same 1P1D topology.
The cost is decode-step latency: the swap-in runs on the main stream between the indexer and attention of every C4 layer. It is latency/occupancy-bound, not bandwidth-bound — `LaunchKernel` uses `grid = batch size`, so a per-rank decode batch of ~20 leaves ~9% occupancy on 256 CUs, which is why #32368's `device_buffer_size` sweep (2048 → 16384, ~8x fewer bytes moved) made decode monotonically *worse*. The levers are multi-CTA sharding (#31341) and
side-stream overlap (#28523), neither of which is in this PR's scope.
When to turn this on: HiSparse trades decode latency for token capacity, so it is a loss whenever the KV working set of your concurrent requests already fits in VRAM — you pay the swap-in cost and get nothing back. It pays off only past
the point where the non-HiSparse configuration can no longer admit the load, at which point the comparison is not "slower" but "runs at all".

## CI coverage

PR-CI:
- `test/registered/kernels/ops/kvcache/test_hisparse.py` (HiSparse JIT kernels; HIP skip removed so it now runs on ROCm)
- `test/registered/unit/mem_cache/test_hisparse_unified_pool.py` (unified-KV C4 device pool)
These give per-PR coverage of the unified-KV + HiSparse pool/kernel paths.

Nightly e2e (8-GPU TP8 / TP8 DP8, `nightly=True`):
- `nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified`
- `nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified-dp`

These two suites are registered but **not yet wired into any workflow**, so they do not run on PR or on the nightly schedule yet. Following the precedent of the unified-KV PR #27380, the PR-CI/nightly wiring is tracked in the AMD CI coverage issue #27521 (cc @bingxche @yctseng0211). I will add them to the issue list then.

## Checklist

- [✔] Format your code according to the [Format code with pre-commit](https://docs.sglang.io/developer_guide/contribution_guide.html#format-code-with-pre-commit).
- [✔] Add unit tests according to the [Run and add unit tests](https://docs.sglang.io/developer_guide/contribution_guide.html#run-and-add-unit-tests).
- [ ] Update documentation according to [Write documentations](https://docs.sglang.io/developer_guide/contribution_guide.html#write-documentations).
- [✔] Provide accuracy and speed benchmark results according to [Test the accuracy](https://docs.sglang.io/developer_guide/contribution_guide.html#test-the-accuracy) and [Benchmark the speed](https://docs.sglang.io/developer_guide/contribution_guide.html#benchmark-the-speed).
- [✔] Follow the SGLang code style [guidance](https://docs.sglang.io/developer_guide/contribution_guide.html#code-style-guidance).

## Review and Merge Process

1. Ping Merge Oncalls to start the process. See the [PR Merge Process](https://github.com/sgl-project/sglang/blob/main/.github/MAINTAINER.md#pull-request-merge-process).
2. Get approvals from [CODEOWNERS](https://github.com/sgl-project/sglang/blob/main/.github/CODEOWNERS) and other reviewers.
3. Trigger CI tests with [comments](https://docs.sglang.io/developer_guide/contribution_guide.html#how-to-trigger-ci-tests) or contact authorized users to do so.
   - Common commands include `/tag-and-rerun-ci`, `/tag-run-ci-label`, `/rerun-failed-ci`
4. After green CI and required approvals, ask Merge Oncalls or people with Write permission to merge the PR.



































































































<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:x: [Run #35813712809](https://github.com/sgl-project/sglang/actions/runs/35813712809)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #35813712476](https://github.com/sgl-project/sglang/actions/runs/35813712476)<!-- slot:pr-test-extra:end -->
Latest PR Test (AMD ROCm 10): <!-- slot:pr-test-amd:start -->:hourglass_flowing_sand: [Run #35813712792](https://github.com/sgl-project/sglang/actions/runs/35813712792)<!-- slot:pr-test-amd:end -->
<!-- pr-states:end -->

## 评论 (26)

### amd-danli103 · 2026-06-26

> ## Code Review
> This pull request implements unified-KV HiSparse support on ROCm for DeepSeek-V4, allowing the compressed C4 KV cache to reside directly within the unified pool's rows. Key updates include removing the temporary ROCm guard, introducing `HiSparseUnifiedC4DevicePool` to alias the unified compressed region, updating the swap-in logic to use the linear MLA path, and preventing runtime reallocations of DSA decode metadata buffers. The review feedback highlights a critical indexing bug in `compressor_v2.py` where unmapped slots (indicated by `-1`) could corrupt the SWA ring, recommends raising a `RuntimeError` to guard against post-capture buffer reallocations in `dsa_backend.py`, and suggests initializing inherited `KVCache` attributes in `HiSparseUnifiedC4DevicePool` to prevent potential `AttributeError`s.
> 
> Important
> 
> The [consumer version of Gemini Code Assist on GitHub](https://developers.google.com/gemini-code-assist/docs/review-repo-code) is being sunset. Starting **June 18, 2026**, new organization installations will be blocked, and all code review activity will officially cease on **July 17, 2026**. For more details on the timeline and next steps, please review the [Help Documentation](https://developers.google.com/gemini-code-assist/docs/deprecations/consumer-code-review).

Removed "preventing runtime reallocations of DSA decode metadata buffers" - this potential bug fix from this PR. Rationale: that fix is orthogonal to this feature. It will be submitted as a separate PR.

### gemini-code-assist[bot] · 2026-06-26

> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!

### amd-danli103 · 2026-06-26

hi @1am9trash or @HaiShaw , could u pls help to trigger the CI test? Thank you!

### amd-danli103 · 2026-06-29

> Verified all 19 tests pass locally: JIT 10/10, unit 5/5, TP8 e2e 2/2 (GSM8K 0.952), TP8+DP8 e2e 2/2 (GSM8K 0.950). Long-context swap path confirmed working.
> 
> All runtime changes are safely gated behind _is_hip / unified_hisparse so no impact on the NVIDIA path.
> 
> Gemini findings reviewed: the -1 index concern in compressor_v2.py is a false positive (c4_out_loc defaults to 0 via tl.where, not -1). The skipped super().**init** in HiSparseUnifiedC4DevicePool is intentional and safe as the uninitialized KVCache attributes are never accessed on this pool.
> 
> @amd-danli103 @HaiShaw

Hi @clintg6 , thank you so much for the thorough verification and the approval! The results aligned with my local results.

### amd-danli103 · 2026-06-29

@amd-bot ci-status

### amd-bot · 2026-06-29

> @amd-danli103

## CI Status for PR #29168

**Merge verdict:** 🚫 **Do not merge on CI signal — PR CI never ran.** All 20 "failures" are the `pr-gate` job rejecting the PR for a **missing `run-ci` label** (PR currently carries only `deepseek`), plus the `*-finish` aggregators failing on the cascade. Every actual test job was **skipped**, so none of this PR's changed code was exercised. The PR is also reported `mergeable: false` (needs a rebase/conflict resolution against `main`).

> [!CAUTION]
> **This PR's changed code is not exercised by any PR-CI test — every test job was skipped because the gate failed.** The `pr-gate` job across all 10 workflows exits with `Missing required label 'run-ci'`, which fast-fails the gate and skips all downstream stages. A green-looking signal is impossible and the red Xs are *not* code failures. **Action required:** add the `run-ci` label (and `run-ci-extra` if the Extra suites are wanted), resolve the merge conflict, and re-run before judging CI.

Changed files: `hisparse_hook.py` (+4/-16), `compressor_v2.py` (+20/-4), `hisparse_coordinator.py` (+41/-13), `deepseek_v4_memory_pool.py` (+95/-0), `test_hisparse.py` (+0/-2), and 3 new AMD/unit tests (`test_hisparse_unified_pool.py`, `test_deepseek_v4_pro_fp4_hisparse_unified_eval_mi35x.py`, `test_deepseek_v4_pro_fp4_hisparse_unified_dp_eval_mi35x.py`).

**Executed CI failure attribution:** AMD: 0 executed failures · Others: 0 executed failures — **all failures are the gate-block cascade, not executed tests.** No test job ran in any workflow.

### Root cause (single, shared across all workflows)

| Workflow | Failing jobs | Test File | Test Function | Error | Related? | Why |
|---|---|---|---|---|---|---|
| [PR #29168 (base)](https://github.com/sgl-project/sglang/actions/runs/28214387616) | [call-gate / pr-gate](https://github.com/sgl-project/sglang/actions/runs/28214387616/job/83582247832), pr-test-finish | N/A | N/A | `Missing required label 'run-ci'` → exit 1 | 🟢 | Gate config check, not code. [Log](https://github.com/sgl-project/sglang/actions/runs/28214387616/job/83582247832#step:6:82) |
| [PR Test (AMD)](https://github.com/sgl-project/sglang/actions/runs/28214387634) | [call-gate / pr-gate](https://github.com/sgl-project/sglang/actions/runs/28214387634/job/83582211916), pr-test-amd-finish | N/A | N/A | same | 🟢 | same gate block |
| [PR Test ROCm 7.2 (AMD)](https://github.com/sgl-project/sglang/actions/runs/28214387631) | pr-gate, pr-test-amd-rocm720-finish | N/A | N/A | same | 🟢 | same gate block |
| [PR Test Extra (AMD)](https://github.com/sgl-project/sglang/actions/runs/28217233557) | pr-gate, pr-test-amd-extra-finish | N/A | N/A | same | 🟢 | same gate block |
| [PR Test Extra](https://github.com/sgl-project/sglang/actions/runs/28217233529) | pr-gate, pr-test-extra-finish | N/A | N/A | same | 🟢 | same gate block |
| PR Test (NPU / Xeon / MUSA / Arm64 / XPU) | pr-gate (+musa-finish) | N/A | N/A | same | 🟢 | same gate block |

Every downstream stage (`stage-a/b/c-*`, `base-a/b/c-*`, `extra-*`, kernel/unit/multimodal tests) shows `skipped` — confirmed by job listings. The `*-finish` jobs fail only because they detect `call-gate: failure`.

### Coverage gap — what is currently unverified
This PR adds a new memory pool (`deepseek_v4_memory_pool.py`) and HiSparse coordinator/compressor changes, plus three new tests intended to cover them:
- `test/registered/unit/mem_cache/test_hisparse_unified_pool.py` — unit coverage for the new pool
- `test/registered/amd/accuracy/mi35x/test_deepseek_v4_pro_fp4_hisparse_unified_eval_mi35x.py` and the `_dp_eval_` variant — AMD MI35x accuracy

**None of these ran.** Until the gate passes, there is zero signal on the changed code.

### What to do before merge
1. **Add the `run-ci` label** to PR #29168 (the only label present is `deepseek`). Add `run-ci-extra` too if the Extra suites should run. See the [CI trigger docs](https://docs.sglang.io/developer_guide/contribution_guide.html#how-to-trigger-ci-tests).
2. **Resolve the merge conflict** — GitHub reports `mergeable: false`; rebase/merge `main` so CI runs against a clean tree.
3. After re-running, confirm the new AMD MI35x accuracy + unit pool tests actually execute and pass — those are the suites that verify this PR's value. The non-AMD backends (NPU/Xeon/MUSA/Arm64/XPU) showing red here are also purely the gate cascade and can be ignored once the label is added.
---
*Generated by amd-bot using Claude Code CLI*


### amd-danli103 · 2026-06-29

/tag-and-rerun-ci

### amd-danli103 · 2026-06-30

@amd-bot ci-status

### amd-bot · 2026-06-30

> @amd-danli103

## CI Status for PR #29168

**Merge verdict:** No executed CI failure is attributable to this PR — every red job is an unrelated pre-existing/flaky/infra failure (FP-comparator flake, perf thresholds, a pytest-missing build env, a stale test signature). The new code's **unit + JIT tests passed on AMD** (the target backend). **BUT** PR CI did **not** verify the actual feature: the two new end-to-end accuracy evals are `nightly=True` (8-GPU MI35x) and never ran, and NVIDIA `base-b` was fast-fail-truncated. Green here does **not** prove unified-KV HiSparse works on ROCm — run the nightly suites before merging on the basis of correctness.

> [!CAUTION]
> This PR's headline value — DeepSeek-V4 Pro FP4 HiSparse unified-KV accuracy on ROCm — is exercised **only** by the two new tests registered `nightly=True` (`nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified` / `…-dp`), which do **not** run on PR CI. The unit test (`test_hisparse_unified_pool.py`) and JIT test (`jit/test_hisparse.py`) ran and passed on AMD, but they only cover the memory-pool/compressor at unit level. **Author must run the nightly MI35x 8-GPU eval suites to verify end-to-end accuracy before trusting a green PR run.**

> [!WARNING]
> NVIDIA PR CI is **incomplete**: in run [28346854737](https://github.com/sgl-project/sglang/actions/runs/28346854737), `base-b-test-1-gpu-small (1)` hit an unrelated flaky failure and fast-failed, **skipping 14 downstream `base-b` shards**. The pool unit test is also registered to NVIDIA `base-b-1gpu-small` and was likely in a skipped shard there (it did pass on AMD). Re-run NVIDIA `base-b` (or use `bypass-fastfail` sparingly) if NVIDIA signal is needed.

Changed files: `deepseek_v4_memory_pool.py` (+95), `hisparse_coordinator.py`, `compressor_v2.py`, `hisparse_hook.py`, +2 nightly MI35x eval tests (+466), `test_hisparse_unified_pool.py` (+246), `jit/test_hisparse.py`.

**Executed CI failure attribution:** AMD: 4 executed failures (0 related) · Others: 4 executed failures (0 related) · plus 2 fast-fail/gate cascades. NVIDIA `base-b`: 1 real failure + 14 skipped (cascade).

### AMD Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [stage-b-1gpu-small (8)](https://github.com/sgl-project/sglang/actions/runs/28346854794/job/83992600626) | `test/registered/utils/test_type_based_dispatcher.py` | `test_type_dispatcher_e2e_performance` | `TypeError: Missing required argument 'input_embeds'` | 🟢 | Pre-existing test out of sync with `TokenizedGenerateReqInput` ctor; unrelated to hisparse. A fix branch `amd_fix_test_type_based_dispatcher` already exists. |
| [stage-b-1gpu-small (11)](https://github.com/sgl-project/sglang/actions/runs/28346854794/job/83992600638) | `test/registered/vlm/test_vision_chunked_prefill.py` | (server requests) | `JSONDecodeError: Expecting value: line 1 column 1` | 🟢 | VLM server returned empty body — infra/flake, no hisparse code path. |
| [stage-b-1gpu-small-mi35x](https://github.com/sgl-project/sglang/actions/runs/28346854794/job/83992600450) | `test/registered/quant/test_quark_mxfp4.py` | (load_weights check) | `ValueError: Should have found memory increase in load_weights` | 🟢 | Quark MXFP4 quant test; unrelated to DeepSeek-V4 KV pool. |
| [stage-b-2gpu-large (0)](https://github.com/sgl-project/sglang/actions/runs/28346854794/job/83992600483) | `test/registered/perf/test_bench_serving_2gpu.py` | `test_pp_offline_throughput_default_decode` | `AssertionError: 5463.5 not greater than 6700` | 🟢 | Perf-throughput threshold (Llama-3.1-8B PP); flake/regression unrelated to PR. |

`wait-for-stage-b-amd` and `pr-test-amd-finish` are aggregator/gate jobs that went red because of the four rows above — not independent failures.

### Other Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [base-b-1gpu-small (1)](https://github.com/sgl-project/sglang/actions/runs/28346854737/job/83981398707) | `test/registered/unit/utils/test_weight_checker_comparator.py` | `test_chunked_result_matches_unchunked` | `AssertionError: mean_abs_err 3.13791624e-05 != 3.13791606e-05` | 🟢 | Flaky FP comparator (~1.8e-13 diff). **Root of the 14-shard NVIDIA `base-b` cascade.** Not hisparse. |
| [stage-b-1gpu-xpu](https://github.com/sgl-project/sglang/actions/runs/28346854668/job/83982863329) | `test/registered/xpu/test_triton_attention_backend.py` | (bench serving) | `ClientPayloadError / TransferEncodingError 400` | 🟢 | XPU server connection drop — infra/flake, different backend. |
| [stage-b-test-4-npu-a3](https://github.com/sgl-project/sglang/actions/runs/28346854700/job/83980632198) | `test/registered/ascend/basic_function/quant/test_npu_w4a4_quantization.py` | (throughput) | `AssertionError: 917.29 not >= 1000` | 🟢 | Ascend NPU perf threshold; unrelated. |
| [build-test (Arm64)](https://github.com/sgl-project/sglang/actions/runs/28346854679/job/83980641204) | `test/registered/cpu/test_activation.py` | N/A | `ModuleNotFoundError: No module named 'pytest'` | 🟢 | Arm64 build env missing pytest — infra, not PR code. |

`PR Test Extra` and `PR Test Extra (AMD)` failed only at `call-gate / pr-gate` (exit 1) — gate cascades from the above, not independent test failures. The NPU `multimodal-gen-test-2-npu-a3` job also exited 1 with no PR-related signal.

### Details / what to do before merge
- **Coverage (do this):** run the two new nightly suites on 8-GPU MI35x — `nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified` and `…-unified-dp` — to actually validate accuracy of unified-KV HiSparse. PR-CI green does not cover them.
- **Unit coverage that DID run (good):** `test_hisparse_unified_pool.py` passed on AMD ([shard 13, job 83992600619](https://github.com/sgl-project/sglang/actions/runs/28346854794/job/83992600619)); `jit/test_hisparse.py` passed on AMD ([shard 4, job 83992600617](https://github.com/sgl-project/sglang/actions/runs/28346854794/job/83992600617)).
- **NVIDIA completeness:** if NVIDIA signal matters, re-run `base-b` after the flaky `test_weight_checker_comparator` clears, since 14 shards were skipped.
- **No action needed** on any of the 8 red executed jobs — all are unrelated to this PR.
---
*Generated by amd-bot using Claude Code CLI*


### amd-danli103 · 2026-06-30

hi @HaiShaw , I think this PR is ready for the next step of the merge process — a quick status recap:

**PR-CI is green for this PR.** After resolving the merge conflict and re-triggering, the two PR-CI tests that cover this change both ran and passed on AMD (the target backend):
- `test/registered/unit/mem_cache/test_hisparse_unified_pool.py` — PASSED (AMD stage-b shard 13)
- `test/registered/jit/test_hisparse.py` — PASSED (AMD stage-b shard 4)

**All red jobs are unrelated to this PR.** Per CI-status analysis, there are 0 executed failures attributable to this PR, and amd-bot's verdict is "No action needed on any of the red executed jobs."

**Notes on coverage (already called out in the PR description):**
- The end-to-end accuracy evals (`..._unified_eval_mi35x.py` / `..._unified_dp_eval_mi35x.py`) are registered `nightly=True` and not yet wired into a workflow (will be tracked in #27521). 
- End-to-end correctness is backed by local runs (JIT 10/10, unit 5/5, TP8 e2e 2/2 GSM8K 0.952, TP8+DP8 e2e 2/2 GSM8K 0.950) and independently reproduced + approved by @clintg6.


### amd-danli103 · 2026-07-08

@amd-bot ci-status

### amd-bot · 2026-07-08

> @amd-danli103

## CI Status for PR #29168

**Merge verdict:** ❌ **Do not merge yet — PR CI is incomplete AND this PR's changed code was never exercised.** Every AMD/NVIDIA test that covers this PR's new ROCm code was *skipped* (not run): the AMD `wait-for-stage-a-amd` gate timed out on runner-queue backlog, and the NVIDIA base-b stage was fast-fail-skipped by an unrelated `test_custom_all_reduce.py` failure. **Zero of the ~14 executed red jobs are related to this PR** — they are all pre-existing failures on non-ROCm backends (Intel XPU/Xeon, Arm64 CPU build, Ascend NPU, NVIDIA all-reduce). But "0 related failures" here does **not** mean safe: the AMD signal this PR exists to produce is entirely absent.

> [!CAUTION]
> **This PR's changed ROCm code is not exercised by any PR-CI test that actually ran.** The two non-nightly tests that cover it both run in AMD **stage-b-1gpu-small**, which was **skipped**:
> - `test/registered/unit/mem_cache/test_hisparse_unified_pool.py` — AMD `stage-b-1gpu-small` (skipped); CUDA `base-b-1gpu-small` (fast-fail-skipped, landed on an aborted shard — and only exercises the non-HIP fallback anyway).
> - `test/registered/jit/test_hisparse.py` — AMD `stage-b-1gpu-small` (skipped).
> - The two `mi35x` eval tests are `nightly=True` → **never run on PR CI**.
>
> Green would not have verified this PR, and CI isn't even green. Before merge, the author must get AMD **stage-b** to actually run (re-run / update branch once MI325 runners free up) so `test_hisparse_unified_pool` + `test_hisparse` execute, and should run the two nightly `mi35x` eval suites out-of-band.

Changed files: `hisparse_coordinator.py` (+41/-13), `deepseek_v4_memory_pool.py` (+107/-1), `compressor_v2.py` (+20/-4), `hisparse.py` (+21/-8), `hisparse_hook.py` (+4/-16), 3 new tests, `test_hisparse.py` (jit).

**Executed CI failure attribution:** AMD: 2 failures (0 related — both infra/cascade) · Others: ~12 failures (0 related — all pre-existing on non-ROCm backends). No PR-caused executed failure found; the real problem is the coverage gap above.

### AMD Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [wait-for-stage-a-amd](https://github.com/sgl-project/sglang/actions/runs/28916631462/job/85784982324) | N/A | N/A | `Timeout waiting for stage-a-amd jobs` (240×60s) | 🟢 | Infra: `stage-a-test-1-gpu-small-amd` didn't get an MI325 runner until 08:48, **after** the gate expired at 08:07 — it then passed. Runner-queue backlog, not the PR. |
| [pr-test-amd-finish](https://github.com/sgl-project/sglang/actions/runs/28916631462/job/85829475306) | N/A | N/A | `wait-for-stage-a-amd: failure` | 🟢 | Pure cascade from the wait-gate timeout above. |

*(PR Test Extra (AMD) — [call-gate / pr-gate](https://github.com/sgl-project/sglang/actions/runs/28916631380/job/85784922050) failed and all `extra-a-*` jobs skipped: this is the standard label-gate behavior, not a test failure.)*

### Other Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [jit-kernel-multigpu-unit-test (NVIDIA)](https://github.com/sgl-project/sglang/actions/runs/28916631436/job/85785023373) | `test/registered/jit/test_custom_all_reduce.py` | N/A | `returned exit code 1` | 🟢 | Custom all-reduce multi-GPU kernel; PR touches none of it. This + `wait-for-base-b` triggered the NVIDIA fast-fail that skipped all base-b/base-c tests. |
| [stage-b-test-1-gpu-xpu](https://github.com/sgl-project/sglang/actions/runs/28916631337/job/85788309101) | `test/registered/xpu/test_intel_xpu_backend.py` | `test_mla_decode_attention_backend` | `RuntimeError: Failed to parse benchmark output` / `KeyError: 'sglang'` | 🟢 | Intel XPU backend; unrelated to ROCm. |
| [build-test (xeon-gnr)](https://github.com/sgl-project/sglang/actions/runs/28916631355/job/85785003846) | `test/registered/cpu/test_intel_amx_attention_backend_b.py` | `test_latency_fp8_moe_model` | `Failed to parse benchmark output` / `KeyError: 'sglang'` | 🟢 | Intel AMX CPU path; same infra flake signature as XPU. |
| [build-test (Arm64)](https://github.com/sgl-project/sglang/actions/runs/28916631372/job/85785525263) | N/A (build) | N/A | `Failed to build sglang-kernel-cpu` — `norm.cpp:391 error` | 🟢 | CPU C++ kernel build; PR only edits Python ROCm files. |
| [stage-b-test-2-npu-a2](https://github.com/sgl-project/sglang/actions/runs/28916631338/job/85784983678) | `test/registered/ascend/.../test_npu_mla_fia_w8a8int8.py` | `test_a_gsm8k` | `Server process exited with code -9` | 🟢 | Ascend NPU (+ several sibling NPU failures); unrelated backend. |

### Details / what to do before merge
- **The only blocker is coverage, not any red X.** All executed failures are pre-existing on backends this PR does not touch (NVIDIA CUDA forward path is explicitly unchanged; changes are `_is_hip`-guarded).
- **Get AMD stage-b to actually run.** The wait-gate timed out purely because MI325 runners were saturated (stage-a queued 4h, ran only after the gate died). Re-run "PR Test (AMD)" or push an update once runners are free so `test_hisparse_unified_pool.py` and `test_hisparse.py` execute in `stage-b-1gpu-small-amd`. If this is high-priority and unrelated early failures keep cascading, the `bypass-fastfail` label will let the AMD pipeline reach stage-b despite them (use sparingly — it consumes more CI).
- **Run the nightly `mi35x` eval suites out-of-band** (`nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified` and `...-unified-dp`) — they never run on PR CI, and they are the GSM8K + long-context correctness check for the feature.
- The unrelated NVIDIA `test_custom_all_reduce.py` and Intel `KeyError: 'sglang'` benchmark-parse failures look like current main-branch/infra issues — worth flagging to the maintainers, but not this PR's responsibility.
---
*Generated by amd-bot using Claude Code CLI*


### amd-danli103 · 2026-08-05

@amd-bot ci-status

### amd-bot · 2026-08-05

> @amd-danli103

## CI Status for PR #29168

**Merge verdict:** The primary vendor for this AMD/ROCm PR — **[PR Test (AMD)](https://github.com/sgl-project/sglang/actions/runs/30794524486)** — is fully GREEN (38/38 executed jobs passed, 0 failures). **Zero executed failures are caused by this PR.** All red X's are unrelated: an NVIDIA `uv`-lock infra flake, three NPU-backend accuracy/perf tests, and two "Extra" workflows that are gated off. **However, the feature's true end-to-end validation is nightly-only and did NOT run here** — see caution below. NVIDIA `PR Test` signal is also incomplete (install stage flaked). The PR's unit-level pool/kernel wiring *was* exercised on AMD and passed.

> [!WARNING]
> **Green PR CI does not verify this PR's core feature end-to-end.** The two e2e serving-accuracy tests this PR adds — `test_deepseek_v4_pro_fp4_hisparse_unified_eval_mi35x.py` and `..._dp_eval_mi35x.py` — are registered `nightly=True` (suites `nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified[-dp]`, 8× MI355X). They do **not** run on PR CI. What *did* run and pass on AMD: the unit test `test/registered/unit/mem_cache/test_hisparse_unified_pool.py` (`stage-b-test-1-gpu-small-amd`, incl. HIP-specific alloc/view cases) and the kernel test `test/registered/kernels/ops/kvcache/test_hisparse.py`. **Before merge, run the two nightly mi35x suites** (or trigger the nightly-AMD workflow) to validate GSM8K accuracy of unified-KV HiSparse serving DeepSeek-V4 on ROCm.

> [!CAUTION]
> **NVIDIA `PR Test` is incomplete.** `base-a-test-1-gpu-small (0)` failed at the `install_sglang` step (uv cache-lock timeout, before any test ran), so `wait-for-base-a` failed and downstream `jit-kernel-b200-test` was fast-fail skipped — those NVIDIA paths are **not tested**. Rerun the branch to get full NVIDIA signal.

Changed files: `deepseek_v4_memory_pool.py` (+107/-1), `hisparse_coordinator.py` (+41/-13), `hisparse.py` (+21/-8), `compressor_v2.py` (+20/-4), `hisparse_hook.py` (+4/-16), 3 new tests, 1 test edit — all ROCm/CUDA HiSparse + DeepSeek-V4 paths; no NPU/workflow files touched.

**Executed CI failure attribution:** AMD: 0 failures · Others (NVIDIA + NPU): 6 executed failures (0 related). Two "Extra" workflows failed at gating, not counted as executed failures.

### Other Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [base-a-test-1-gpu-small (0)](https://github.com/sgl-project/sglang/actions/runs/30794524457/job/91625132925) | N/A (install) | N/A | `uv: Timeout (300s) acquiring lock on sdists cache` | 🟢 | Runner `uv` cache-lock flake during `install_sglang`, before any test; no code path involved |
| [wait-for-base-a](https://github.com/sgl-project/sglang/actions/runs/30794524457/job/91625132492) | N/A | N/A | wait job failed (base-a failed) | 🟢 | Cascade from base-a install flake |
| [call-jit-kernel-tests / jit-kernel-b200-test](https://github.com/sgl-project/sglang/actions/runs/30794524457/job/91625132951) | N/A | N/A | `Fast-fail: root cause wait-for-base-a, base-a-test-1-gpu-small` | 🟢 | Fast-fail cascade, not executed |
| [stage-b-test-4-npu-a3 (0)](https://github.com/sgl-project/sglang/actions/runs/30794524238/job/91625203836) | `test/registered/npu/basic_function/dp_attn/test_npu_dp_attention.py` | `TestDPAttentionMixedChunk` | `AssertionError: 0.34 not >= 0.343035` (GSM8K, retried) | 🟢 | NPU DP-attention accuracy, marginally under threshold; PR touches no NPU/dp-attn code |
| [multimodal-gen-test-2-npu-a3](https://github.com/sgl-project/sglang/actions/runs/30794524238/job/91625203845) | `sglang/multimodal_gen/test/server/ascend/test_server_2_npu.py` | `TestDiffusionServerTwoNpu::test_diffusion_generation[wan2_2_t2v_14b_w8a8_2npu]` | `Consistency check failed` | 🟢 | NPU diffusion server; unrelated to HiSparse |
| [single-node-poc (qwen3_5_397b_w4a8…)](https://github.com/sgl-project/sglang/actions/runs/30794524238/job/91872639640) | `test/registered/npu/...` (qwen3 poc) | perf gate | `AssertionError: 54.47 not <= 51.0` | 🟢 | NPU qwen3 latency threshold; unrelated |

**Gated (not executed, not counted):**
- [PR Test Extra (AMD) → call-gate / pr-gate](https://github.com/sgl-project/sglang/actions/runs/30794524300/job/91624984463): `Missing required label 'run-ci-extra'` — expected; PR carries only `deepseek`,`run-ci`. Not a failure.
- [PR Test Extra → check-changes](https://github.com/sgl-project/sglang/actions/runs/30794524255/job/91624984415): `RuntimeError: load_run_timeouts: inline suite 'base-a-test-cpu' needs exactly one 'Run test' step with timeout-minutes in .github/workflows/pr-test-extra.yml` — pre-existing CI-config bug in `pr-test-extra.yml`, which this PR does not touch.

### Details / what to do before merge
- **Run the nightly mi35x suites** (`nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified` and `...-unified-dp`) to validate the feature's serving accuracy — PR CI only covered the unit/kernel layer. This is the one action that actually verifies the PR.
- **Rerun the branch** (or update it) to clear the NVIDIA `base-a` `uv`-lock flake and get complete NVIDIA signal; the current NVIDIA red is a runner issue, not a code issue.
- **NPU / Extra reds need no action from this PR** — NPU failures are unrelated backend accuracy/perf thresholds; the Extra workflows are gated off / hit a pre-existing `pr-test-extra.yml` config bug.
---
*Generated by amd-bot using Claude Code CLI*


### amd-danli103 · 2026-08-05

## CI Status update: CI is complete, AMD is fully green, and the nightly gap is closed

Recording where this stands, and closing both follow-ups it raised.

### 1. AMD PR-CI — green

`PR Test (AMD)` passed 38/38 executed jobs with 0 failures, including everything that actually exercises this PR:
- all 14 `stage-b-test-1-gpu-small-amd` shards (runs `test_hisparse_unified_pool.py`)
- `jit-kernel-unit-test-amd` (runs `test_hisparse.py`)
- `stage-c-test-large-8-gpu-amd-mi35x` × 3, `wait-for-stage-b-amd`, `pr-test-amd-finish`
This closes the blocker from the Jul 8 verdict, where `stage-b-1gpu-small-amd` timed out on the runner queue and the two tests covering this PR never ran.

### 2. Nightly e2e — re-run **locally** on the rebased tree
The bot correctly notes that the two e2e suites this PR adds are `nightly=True` and do not run on PR CI and had not beem added into Nightly CI tests. I re-ran both out-of-band **locally** on 8× MI355X (gfx950) at the current branch head `7c9e37ea1`, on top of `main` @ `21d930aae`, with `deepseek-ai/DeepSeek-V4-Pro`:

| variant | GSM8K | invalid | `test_b_long_context_swap` | wall |
|---|---|---|---|---|
| unified-KV, TP8 | **0.949** | 0.000 | passed | 2 passed / 32:02 |
| unified-KV, TP8 + DP8 dp-attention | **0.950** | 0.000 | passed | 2 passed / 28:05 |

Both exited 0. These supersede the numbers previously in the description (0.953 / 0.949), which were measured before the rebase; the small movement is GSM8K sampling noise — `random_seed` is generated per run — and both remain far
above the 0.91 gate.

Runtime evidence that the feature is live rather than silently falling back. The device-resident C4 region is exactly half the logical budget at `host_to_device_ratio=2`, and the host mirror carries the full logical budget:

```
HiSparse c4 host-to-device ratio = 2
c4_size=2025408  c4_logical_size=4050816
Allocating 124.44 GB host memory for V4 paged pool 'dsv4_hisparse_c4'
  (layers=30, pages=4050816, item_bytes=1024, layout=layer_first)
```
And the swap path on a single 19,456-token request — the hot buffer stays at the configured budget while cold C4 streams in from host on every decode step:
```
Decode batch, #running-req: 1, #full token: 19456, #swa token: 11520,
  #gpu token: 2357, #cpu token: 4812, cuda graph: True
```
The buried passcode is retrieved correctly in both variants, which is only possible if the cold→hot copies land correctly.
Resulting capacity, for the Speed Tests section: `max_total_num_tokens` is 16,203,264 on TP8 and 14,163,968 on TP8+DP8 at `bytes_per_full_token = 8803.84`.

### 3. Remaining red X's — none attributable to this PR
12 of 171 checks are red, but I think none of them are caused by this PR.

### amd-danli103 · 2026-08-05

Furthermore, I noticed two open PRs build directly on the pool/coordinator wiring introduced here:
- #32368 — DSV4 unified-KV HiSparse PD over MoRI.
- #31722 — Enable MTP/spec decoding with HiSparse on DSV4 unified-KV.

@HaiShaw could you pls assign a code-owner for this PR when you have a moment? @clintg6 approved and independently reproduced, more CODEOWNER approval is what this PR currently needs to move forward. 

For the CI status, a brief result for you is that:
- PR-CI has completed and `PR Test (AMD)` is fully green (38/38 executed jobs, including the stage-b shards that run this PR's tests); the remaining red X's are unrelated NPU and NVIDIA-install flakes, as @amd-bot's latest ci-status
confirms.
- The two e2e suites here are `nightly=True` and never run on PR CI, so I re-ran both locally on 8× MI355X after the rebase: GSM8K 0.949 (TP8) / 0.950 (TP8+DP8), `test_b_long_context_swap` passed in both.

On the point you care about most, the CUDA forward path is unchanged — every edit sits behind `_is_hip` / HiSparse / unified-KV gates, and the test change only drops `skipif(is_hip())`. 

That's the latest update. Thank you so much!

### amd-bot · 2026-08-05

> @amd-danli103 requested a review

## Claude Code Review

> PR #29168 — Reviewed at 2026-08-05 10:51 UTC

I now have a thorough understanding of the PR. Writing the review.

## Summary

This PR enables **unified-KV HiSparse on ROCm** for DeepSeek-V4, completing the counterpart to the already-supported separate-KV path (#26639). It removes a temporary startup guard and wires the compressed C4 KV — which in unified-KV mode lives inside `DeepSeekV4UnifiedKVPool.rows[swa_pages:]` (bf16, `head_dim`-wide) — into the existing HiSparse hot/cold machinery. The centerpiece is a new `HiSparseUnifiedC4DevicePool` that **aliases views into the unified buffer instead of allocating its own device buffer**, so the unified pool stays a single PD-friendly contiguous allocation. It also honors the `host_to_device_ratio` C4 shrink factor by sizing the on-device C4 region to the shrunk budget (`c4_compress_pages=c4_size`) and by dropping the device-C4 capacity clamp on the unified path (cold C4 is host-resident). Adds unit tests, enables the dsv4 JIT swap-in kernel tests on ROCm, and adds two nightly e2e eval suites.

The CUDA/NVIDIA forward path is genuinely untouched — every change is behind `_is_hip` + `unified_hisparse`/`_is_unified_hisparse` guards. I verified this claim holds.

## Code Quality

**1. `_forward_unified_hip` does NOT contain the HiSparse remap — latent gap under `SGLANG_OPT_USE_JIT_NORM=0` (Medium)**

The new HiSparse remap (`compressor_v2.py:241-255`) lives in the `else` branch of `forward_unified`. That branch is only reached on ROCm when `SGLANG_OPT_USE_JIT_NORM` is `True` — which is the default (`environ.py:1214`), so the common path is correct. But if a user sets `SGLANG_OPT_USE_JIT_NORM=0`, ROCm takes `_forward_unified_hip` (`compressor_v2.py:222`), which has no `unified_hisparse` branch and writes compressed C4 via `set_extra_key_buffer` (line 408) using the raw `_get_out_loc` — i.e. it would write to the wrong rows / bypass the hot-buffer remap entirely, silently. Since this PR advertises unified-KV HiSparse as an end-to-end ROCm feature, the two forward paths now diverge in correctness. Recommend either handling `unified_hisparse` in `_forward_unified_hip` too, or failing fast when `enable_hisparse and unified_kv and not SGLANG_OPT_USE_JIT_NORM`.

**2. `available_size()` / `full_available_size()` clamp removal (Medium — verify, not clearly a bug)**

`allocator/hisparse.py:366-391` drops the `hisparse_attn_allocator.available_size() * compress_ratio` clamp for the unified path and returns only the logical pool size. This is coherent with the design — decode uses `alloc_logical_only` (line 399) which never allocates device C4 pages, and the device hot region is bounded per-request by `device_buffer_size` via the swap trigger — so the logical pool is the real bound. Worth a reviewer confirming there is no batch-level path that can drive concurrently device-resident C4 rows past the shrunk `c4_size` region without the swap machinery evicting; the unit test covers single-alloc oversubscribe but not this batch interaction.

**3. `out_loc` dtype divergence between branches (Low)**

`full_to_hisparse_device_index_mapping` is `int64` (`allocator/hisparse.py:320`), so the hisparse branch yields an `int64` `out_loc` (`_translate_loc_to_hisparse_device` deliberately does *not* cast, unlike the public `translate_loc_to_hisparse_device` which `.to(int32)`s) plus `unified_swa_pages`. The non-hisparse branch's `core_metadata.unified.c4_out_loc` is typically `int32`. Both feed `compress_norm_rope_store`. The e2e tests presumably pass, so the kernel tolerates int64, but the silent dtype divergence between the two arms of the same `if` is a foot-gun — consider casting to match.

**4. `HiSparseUnifiedC4DevicePool.__init__` skips the parent constructor (Low)**

Intentional and documented (must not allocate a second buffer), but it manually reproduces a subset of base attributes. The inherited byte-packed write accessors (`set_key_buffer*`) reference base state that this path doesn't fully set up; the docstring says they're "not used," but nothing enforces it. Consider overriding them to `raise NotImplementedError` in this subclass so accidental use fails loudly rather than corrupting memory.

**5. Empty C4-layer stage (Low/edge)** — `self.size = self.kv_buffer[0].shape[0] if self.kv_buffer else 0` handles an empty `c4_local_layer_ids` gracefully to `size=0`, but a PP stage with HiSparse enabled and zero C4 layers would then produce an empty `data_ptrs`. Likely unreachable (HiSparse implies C4 layers), but no assertion guards it.

## Suggestions

- Address finding #1: either extend `_forward_unified_hip` to honor `unified_hisparse`, or add a fast-fail in `validate_hisparse` for the `unified_kv + hisparse + SGLANG_OPT_USE_JIT_NORM=0` combination (the guard you removed was exactly this kind of "fail fast with a clear message").
- Cast the hisparse `out_loc` to match the non-hisparse branch dtype (#3).
- Add `NotImplementedError` overrides for the unused inherited write accessors on `HiSparseUnifiedC4DevicePool` (#4).
- The comments and PR writeup are excellent and genuinely aid review — keep that standard.

## Testing

- **Unit tests** (`test_hisparse_unified_pool.py`, 6 cases) are well-targeted at derived properties — view aliasing into `rows[swa_pages:]`, host-mirror geometry, `c4_compress_pages` actually shrinking the device region while c128 layers are untouched, page_size==1 alloc/free mapping, and oversubscribe-returns-None. These guard real regressions, not tautologies.
- **JIT tests**: removing `skipif(is_hip())` on the two dsv4 swap-in cases now exercises the swap-in kernel byte-for-byte on ROCm — good per-PR coverage of the kernel correctness.
- **Coverage gap to flag to reviewers**: the authoritative swap oracle — `test_b_long_context_swap` (~19k-token cold→hot retrieval) — is in the two `nightly=True` eval suites that, per the PR body, are **registered but not yet wired into any workflow** (tracked in #27521). So **PR CI does not run the end-to-end swap path**; green PR CI verifies pool wiring + kernel numerics only. The author validated the e2e path locally (TP8 0.953 / TP8-DP8 0.949). Reviewers should not read a green PR run as e2e-verified, and the nightly wiring should be followed up before this is relied on in production.
- **CI status**: the 5 currently-failing checks are all **NPU** (`pr-test-npu-finish`, `single-node-poc` npu, `multimodal-gen-test-2-npu-a3`, `stage-b-test-4-npu-a3`) — unrelated to this ROCm-only PR; no AMD/ROCm check is failing.

## Overall

**Comment (lean approve, pending clarification).** The design is sound, well-documented, and correctly scoped to ROCm with the CUDA path provably untouched; sizing math (`c4_size = full_token//(4·c4_shrink_factor)` → `c4_compress_pages`) checks out and test coverage of the pool internals is strong. The one item I'd want addressed before merge is finding #1 — the `SGLANG_OPT_USE_JIT_NORM=0` path silently bypasses the HiSparse remap on ROCm, which reintroduces exactly the kind of foot-gun the removed guard prevented. Findings #2 (verify batch-level device-C4 bound) and #3 (out_loc dtype) are worth a look but likely fine given local e2e results.

---
*Generated by amd-bot using Claude Code CLI*


### amd-danli103 · 2026-08-21

Status update @Duyi-Wang @AMD-yanfeiwang 
1.Rebased onto latest main and solve conflict.
2.Re-verified everything on MI355X after the rebase: JIT kernel + unit tests pass, and both e2e suites pass 2/2 — GSM8K **0.954** (TP8) and **0.957** (TP8+DP8), and `test_b_long_context_swap` retrieves the passcode in both. 
3.The only rebase-induced code change is a 5-line `is_dsv4_paged_layout` predicate that keeps unified-KV off upstream's new `copy_cache_planned_mla` paged path (#34329). This is an minor change.

### amd-danli103 · 2026-08-22

/rerun-failed-ci

### amd-danli103 · 2026-08-25

@amd-bot ci-status

### amd-bot · 2026-08-25

> @amd-danli103

## CI Status for PR #29168

**Merge verdict:** **Not ready to merge.** Two independent gaps, neither caused by this PR's code: (1) **NVIDIA PR CI never ran** — it is blocked by the repo-wide "Rebase Required" maintenance gate ([#21065](https://github.com/sgl-project/sglang/issues/21065), required base `3e096629cf98`); the PR must rebase onto latest `main`. (2) The feature's **end-to-end accuracy/eval tests are `nightly=True`**, so they did **not** run on PR CI. The PR's HiSparse **unit + kernel tests did run on AMD and passed.** All executed AMD/NPU/CPU failures are unrelated (MoE torch.compile, perf flake, attention head-validation, disagg fabric infra, HF gated-repo auth).

> [!CAUTION]
> **NVIDIA PR CI is incomplete.** `wait-for-base-b` + `base-b-test-1-gpu-small (2)/(4)` failed on the maintenance-mode rebase gate; every other `base-b` job fast-fail cascaded and all `base-c` jobs were skipped. No NVIDIA test actually executed. **Rebase onto latest `main` and re-run** before merge.

> [!CAUTION]
> **The feature's accuracy is not verified by PR CI.** `test_deepseek_v4_pro_fp4_hisparse_unified_dp_eval_mi35x.py` and `test_deepseek_v4_pro_fp4_hisparse_unified_eval_mi35x.py` are registered `nightly=True` → they never run on PR CI. Green PR CI only proves the **unit** (`test_hisparse_unified_pool.py` ✅) and **kernel** (`test_hisparse.py` ✅) layers. Run the two `nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified[-dp]` suites on mi35x before trusting the unified-KV path end-to-end.

Changed files: `hisparse_coordinator.py` (+48/-14), `deepseek_v4_memory_pool.py` (+107/-1), `compressor_v2.py` (+18/-4), `mem_cache/allocator/hisparse.py` (+21/-8), `arg_groups/hisparse_hook.py` (+4/-16), 3 new tests + 1 modified test.

**Executed CI failure attribution:** AMD: 4 executed failures (0 related) · NPU: 2 (0 related) · CPU: 1 (0 related) · NVIDIA: 0 executed (all 14 blocked/cascaded by rebase gate — not counted).

### AMD Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [stage-b-1gpu-small (0)](https://github.com/sgl-project/sglang/actions/runs/32474067909/job/97043961695) | `test/registered/moe/test_torch_compile_moe.py` | (collection, 0 ran) | `ValueError: Incorrect number of arguments … expected 'USE_PDL'` | 🟢 | Triton MoE fused kernel signature mismatch; PR touches only HiSparse/DeepSeek-V4 KV pool, not MoE torch.compile |
| [stage-b-1gpu-large (1)](https://github.com/sgl-project/sglang/actions/runs/32474067909/job/97043961538) | `test/registered/perf/test_bench_serving_1gpu_part2.py` | perf gate | `52.93 not less than 48` (throughput threshold) | 🟢 | General serving perf benchmark; does not enable HiSparse/unified-KV |
| [stage-b-1gpu-small mi35x](https://github.com/sgl-project/sglang/actions/runs/32474067909/job/97043961518) | `test/registered/attention/test_verify_shared_kv.py` | `test_rejects_multiple_local_kv_heads` | `AssertionError: True is not false` | 🟢 | Attention KV-head validation guard; PR does not modify attention head-count validation |
| [stage-b-8gpu-mi35x-disagg](https://github.com/sgl-project/sglang/actions/runs/32474067909/job/97043961520) | `test/registered/amd/disaggregation/test_disaggregation_basic.py` | (server bringup) | UCX `ib_device.c` — `failed to get interface index for tw-eth*` | 🟢 | RDMA/fabric infra on the mi35x 8-GPU runner, not code |

(`wait-for-stage-b-amd-rocm720` [97043961536](https://github.com/sgl-project/sglang/actions/runs/32474067909/job/97043961536) is the cascade aggregator for shard 0 — same root cause as row 1, not an independent failure.)

### Other Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [base-c-test-perf-16-npu-a3](https://github.com/sgl-project/sglang/actions/runs/32474067794/job/97043947106) | N/A | N/A | `Executing the custom container implementation failed … contact your self hosted runner administrator` | 🟢 | NPU runner/container infra failure; PR is ROCm-only |
| [multimodal-gen-test-1-npu-a3](https://github.com/sgl-project/sglang/actions/runs/32474067794/job/97043946165) | N/A | N/A | NPU setup failure | 🟢 | NPU path, unrelated to ROCm HiSparse |
| [build-test (xeon-gnr, base-b-test-cpu)](https://github.com/sgl-project/sglang/actions/runs/32474067324/job/97043948173) | N/A | N/A | `GatedRepoError: 401` for `meta-llama/Llama-3.1-8B-Instruct` | 🟢 | HF gated-repo auth / token infra issue |

### Details / what to do before merge

1. **Rebase onto latest `main`** (required base `3e096629cf98`, per maintenance issue [#21065](https://github.com/sgl-project/sglang/issues/21065)) and push, so NVIDIA `pr-test` (`base-b`/`base-c`) and the rest actually execute. Until then NVIDIA required checks cannot go green.
2. **Verify the feature end-to-end**: PR CI proved only unit + kernel level (both ✅ on AMD — [unit](https://github.com/sgl-project/sglang/actions/runs/32474067909/job/97043961672) `test_hisparse_unified_pool.py`, [kernel](https://github.com/sgl-project/sglang/actions/runs/32474067909/job/97043961678) `test_hisparse.py`). Trigger the two `nightly=True` mi35x DeepSeek-V4 unified-KV HiSparse eval suites to confirm accuracy before merge.
3. The 4 AMD + 3 other executed failures are all pre-existing/infra (🟢) and not blockers attributable to this PR — but do confirm they are green on the post-rebase re-run so they don't mask a real regression.
---
*Generated by amd-bot using Claude Code CLI*


### amd-danli103 · 2026-08-25

/rerun-failed-ci

### amd-danli103 · 2026-08-26

@amd-bot ci-status

### amd-bot · 2026-08-26

> @amd-danli103

## CI Status for PR #29168

**Merge verdict:** ⚠️ **PR CI is incomplete — do not read "mostly green" as "verified."** Zero of the executed failures are related to this PR's changed code (all failing tests run non-DSV4 / non-HiSparse default paths, while every source change in this PR is DeepSeek-V4 / HiSparse–specific). The unit + kernel tests that *do* exercise the changed memory-pool/allocator code passed. **But the AMD `wait-for-stage-b-amd` gate failed, so all `stage-c-*-amd` jobs — including `stage-c-dsv4-pro-fp4-amd-mi35x` and `stage-c-dsv4-flash-fp4-fp8-amd-mi35x` — were fast-fail *skipped*, and this PR's two end-to-end accuracy tests are `nightly=True` so they never run on PR CI.** The core end-to-end behavior this PR enables is therefore **not verified** by this run.

> [!CAUTION]
> **PR CI is incomplete AND the PR's headline behavior is not exercised.**
> - **Fast-fail skip:** [`wait-for-stage-b-amd`](https://github.com/sgl-project/sglang/actions/runs/32802308668/job/97828528303) failed → `stage-c-dsv4-pro-fp4-amd-mi35x`, `stage-c-dsv4-flash-fp4-fp8-amd-mi35x`, `stage-c-test-large-8-gpu-amd*`, `stage-c-test-4-gpu-amd`, and `call-pr-test-amd-extra` were **skipped (not tested)**.
> - **Nightly-only e2e:** the two new accuracy tests `test_deepseek_v4_pro_fp4_hisparse_unified_eval_mi35x.py` and `..._dp_eval_mi35x.py` are registered `nightly=True` → they **do not run on PR CI at all**. Green here does not verify unified-KV HiSparse DSV4 accuracy.
> - **What did run & pass** (covers the changed `deepseek_v4_memory_pool.py` / `allocator/hisparse.py`): new unit test `test_hisparse_unified_pool.py` ✅ (AMD [shard 12](https://github.com/sgl-project/sglang/actions/runs/32802308668/job/97828531517) + CUDA `base-b` green), modified kernel test `test_hisparse.py` ✅ (AMD [shard 8](https://github.com/sgl-project/sglang/actions/runs/32802308668/job/97828530443)).
> - **Before merge:** re-run the branch so `stage-c` AMD completes (or add `bypass-fastfail` for a full-signal run — use sparingly), and trigger the nightly `deepseek-v4-pro-hisparse-unified[-dp]` suites (or run them manually on mi35x) to actually validate the accuracy path.

Changed files: `deepseek_v4_memory_pool.py` (+107/-1), `hisparse_coordinator.py` (+48/-14), `allocator/hisparse.py` (+21/-8), `dsv4/compressor_v2.py` (+18/-4), `arg_groups/hisparse_hook.py` (+4/-16), +2 new nightly accuracy tests, +1 new unit test, kernel `test_hisparse.py` modified.

**Executed CI failure attribution:** AMD: 4 failures (0 related) · Others: 3 failures (0 related). All completed — no jobs pending. Aggregator/gate jobs (`*-finish`, `pr-gate`, `wait-for-stage-b-amd`) are fast-fail cascades, not independent failures.

### AMD Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [stage-b-1gpu-small (0)](https://github.com/sgl-project/sglang/actions/runs/32802308668/job/97828530498) | `test/registered/moe/test_torch_compile_moe.py` | server-launch (exit 5) | `TypeError: launcher() missing 1 required positional argument: '_grid_2'` → server exit -9 | 🟢 | Triton/torch.compile MoE launcher path; PR touches no MoE/compile code |
| [stage-b-1gpu-small-mi35x](https://github.com/sgl-project/sglang/actions/runs/32802308668/job/97828528448) | `test/registered/attention/test_verify_shared_kv.py` | `retry`-wrapped subtest | `AssertionError: True is not false` (retry exceeded) | 🟢 | File has zero `hisparse`/`dsv4` refs; tests Qwen3.5/Kimi shared-KV, unrelated path |
| [stage-b-large-8gpu-mi35x-disagg](https://github.com/sgl-project/sglang/actions/runs/32802308668/job/97828528379) | `test/registered/amd/disaggregation/test_disaggregation_basic.py` | `retry`-wrapped subtest | `RuntimeError: Internal Server Error` | 🟢 | Runs `Qwen/Qwen3-8B`, not DeepSeek-V4; generic disagg server flake |
| [stage-b-1gpu-large (1)](https://github.com/sgl-project/sglang/actions/runs/32802308668/job/97828528346) | `test/registered/perf/test_bench_serving_1gpu_part2.py` | `test_score_api_latency_throughput` | `AssertionError: 52.65 not less than 48` | 🟢 | Perf threshold flake; no HiSparse/DSV4 code involved |

### Other Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [base-c-test-perf-8-npu-a3](https://github.com/sgl-project/sglang/actions/runs/32802308544/job/97828538923) | `test/registered/npu/performance/minimax_m2_5/test_npu_minimax_m2_5_w8a8_4p_in64k_out1k_prefix90_50ms.py` | perf subtest | `ConnectionRefused :20366` (server never came up) | 🟢 | NPU backend; PR is ROCm-only |
| [base-c-test-perf-4-npu-a3](https://github.com/sgl-project/sglang/actions/runs/32802308544/job/97828538842) | NPU minimax perf (same family) | perf subtest | server/connection failure | 🟢 | NPU backend; unrelated to ROCm HiSparse |
| [build-test (xeon-spr)](https://github.com/sgl-project/sglang/actions/runs/32802308375/job/97828511086) | `test/registered/cpu/test_intel_amx_attention_backend_b.py` | bench subtest | `ValueError: ...no GPU memory for KV cache under --mem-fraction-static=0.1` → parse fail | 🟢 | Intel AMX CPU backend; unrelated to ROCm HiSparse |

### Details / what to do before merge
- **No action needed on the 7 executed failures** — all are on unrelated backends (NPU, CPU) or unrelated ROCm default paths (torch.compile MoE, Qwen shared-KV, Qwen3-8B disagg, perf threshold). None touch the DSV4/HiSparse code this PR changes. Several look like pre-existing flakes/infra (connection-refused, perf threshold, torch.compile launcher).
- **The real gap is coverage, not red X's.** To actually verify this PR before merge:
  1. Re-run/update the branch so the AMD `stage-c` jobs complete instead of being fast-fail skipped (or apply `bypass-fastfail` for one full-signal run — it costs more CI, use sparingly). This restores `stage-c-dsv4-*-amd-mi35x` signal.
  2. Trigger the nightly suites `nightly-amd-8-gpu-mi35x-deepseek-v4-pro-hisparse-unified` and `...-unified-dp` (the PR's two new accuracy tests), or run them manually on an mi35x 8-GPU box — these are the only tests that validate the unified-KV HiSparse DSV4 end-to-end behavior and they are excluded from PR CI by design.
---
*Generated by amd-bot using Claude Code CLI*


### amd-danli103 · 2026-08-27

/rerun-failed-ci

### amd-danli103 · 2026-09-21

/rerun-failed-ci

## Review (3)

### gemini-code-assist[bot] · 2026-06-24 · COMMENTED

## Code Review

This pull request implements unified-KV HiSparse support on ROCm for DeepSeek-V4, allowing the compressed C4 KV cache to reside directly within the unified pool's rows. Key updates include removing the temporary ROCm guard, introducing `HiSparseUnifiedC4DevicePool` to alias the unified compressed region, updating the swap-in logic to use the linear MLA path, and preventing runtime reallocations of DSA decode metadata buffers. The review feedback highlights a critical indexing bug in `compressor_v2.py` where unmapped slots (indicated by `-1`) could corrupt the SWA ring, recommends raising a `RuntimeError` to guard against post-capture buffer reallocations in `dsa_backend.py`, and suggests initializing inherited `KVCache` attributes in `HiSparseUnifiedC4DevicePool` to prevent potential `AttributeError`s.

> [!IMPORTANT]
> The [consumer version of Gemini Code Assist on GitHub](https://developers.google.com/gemini-code-assist/docs/review-repo-code) is being sunset. Starting **June 18, 2026**, new organization installations will be blocked, and all code review activity will officially cease on **July 17, 2026**.
> For more details on the timeline and next steps, please review the [Help Documentation](https://developers.google.com/gemini-code-assist/docs/deprecations/consumer-code-review).

### amd-danli103 · 2026-06-25 · COMMENTED

(no text)

### clintg6 · 2026-06-26 · APPROVED

Verified all 19 tests pass locally: JIT 10/10, unit 5/5, TP8 e2e 2/2 (GSM8K 0.952), TP8+DP8 e2e 2/2 (GSM8K 0.950). Long-context swap path confirmed working.

All runtime changes are safely gated behind _is_hip / unified_hisparse so no impact on the NVIDIA path.

Gemini findings reviewed: the -1 index concern in compressor_v2.py is a false positive (c4_out_loc defaults to 0 via tl.where, not -1). The skipped super().__init__ in HiSparseUnifiedC4DevicePool is intentional and safe as the uninitialized KVCache attributes are never accessed on this pool.

@amd-danli103 @HaiShaw
