# [Issue #36830] [Bug] GLM-5.3-Flash cannot use FP8 KV cache: `index_kpool > 1` excludes `flashmla_kv`, and no CUDA DSA backend supports bf16-query x fp8-KV

source: https://github.com/sgl-project/sglang/issues/36830
state: open | updated: 2026-09-23T08:52:43Z
labels: 

## 正文


## Summary

On 8xH20 (SM90), `--kv-cache-dtype fp8_e4m3` cannot be used with **GLM-5.3-Flash**, while it works fine
with **GLM-5.2** on the *same hardware, same cluster, same image* (in production for months).

The difference is one model-config field: **GLM-5.3-Flash sets `index_kpool: 4`; GLM-5.2 does not set it
(defaults to 1)**. That single value excludes `flashmla_kv` — the backend GLM-5.2 uses for FP8 KV — and the
three replacement backends (`fa3` / `tilelang` / `trtllm`) have no bf16-query x fp8-KV path on CUDA.

Net effect: GLM-5.3-Flash is forced onto bf16 KV and loses **~1.75x-2.0x** of KV cache capacity.

## Positive control: GLM-5.2 with FP8 KV works on the same hardware

Production node, running continuously for months:

```
--model-path <path>/GLM-5.2-W4AFP8
--quantization w4afp8
--kv-cache-dtype fp8_e4m3
```
Engine-resolved:
```
kv_cache_dtype='fp8_e4m3'
dsa_prefill_backend='flashmla_kv'
dsa_decode_backend='flashmla_kv'
```
```
KV Cache is allocated. dtype: torch.float8_e4m3fn, #tokens: 958208
```

So FP8 KV itself is healthy on SM90. The problem is specific to GLM-5.3-Flash.

## Root cause chain

**1. The only relevant config difference**

```
GLM-5.3-Flash : "index_kpool": 4   "index_topk": 2048  "index_n_heads": 32  "index_head_dim": 128
GLM-5.2       : (absent)           "index_topk": 2048  "index_n_heads": 32  "index_head_dim": 128
```
```python
# srt/configs/model_config.py:280
def get_dsa_index_kpool(config) -> int:
    return getattr(config, "index_kpool", 1)
```

**2. The guard excludes `flashmla_kv`**

```python
# srt/layers/attention/dsa_backend.py  _check_kpool_tail_backend
if (topk_indices is None
    or self.dsa_index_kpool <= 1                      # GLM-5.2 exits here
    or dsa_impl in ("fa3", "tilelang", "trtllm")):    # GLM-5.3 is limited to these
    return
raise NotImplementedError(
    "index_kpool > 1 appends tail tokens to topk_indices and is "
    "currently only supported by the FA3/TileLang/TRTLLM DSA {phase} backend.")
```

**3. The existing escape hatch only covers the bf16 path**

```python
# _resolve_kpool_tail_backend
if (topk_indices is None or self.dsa_index_kpool <= 1
    or dsa_impl != "flashmla_sparse"):     # does not cover flashmla_kv
    return dsa_impl
if self.device_sm_major == 9: return "fa3"
```

This is why the **bf16** configuration works: the engine auto-selects `flashmla_sparse` + `fa3`, and the
resolver hands the KPool tail to `fa3`. FP8 requires `flashmla_kv`, which this branch does not handle.

**4. None of the three allowed backends can consume FP8 KV on CUDA**

```python
# dsa_backend.py:594-601 - the only mixed q/kv dtype path
if (self.dsa_prefill_impl == "aiter" or self.dsa_decode_impl == "aiter") \
   and model_runner.kv_cache_dtype == fp8_dtype:
        self._ensure_aiter_dsa_decode_metadata_buffer(
            q_dtype=torch.bfloat16, kv_dtype=fp8_dtype)
```

It exists only for `aiter` (ROCm). `flashmla_sparse_q8` is native FP8 but **prefill-only** (its own error
message: *"flashmla_sparse_q8 is a prefill-only backend. For FP8, use --dsa-prefill-backend
flashmla_sparse_q8 together with --dsa-decode-backend flashmla_kv"*) - and `flashmla_kv` is exactly what
the KPool guard rejects.

## Reproduction

8xH20, image `lmsysorg/sglang:glm-5.3-flash`, two instances of `--tp-size 4 --expert-parallel-size 4`
(GPU 0-3 and 4-7), `--kv-cache-dtype fp8_e4m3`, `--context-length 200000`:

| # | prefill / decode backend | MTP | Failure |
|---|---|---|---|
| 1 | `flashmla_sparse_q8` / `flashmla_kv` | on | `NotImplementedError: index_kpool > 1 ...` (decode cuda-graph capture) |
| 2 | `flashmla_sparse_q8` / `fa3` | on | `RuntimeError: query and key must have the same dtype` (target-verify graph) |
| 3 | as #2 + `--speculative-attention-mode decode` | on | same dtype error |
| 4 | `flashmla_sparse_q8` / `fa3` | **off** | **same dtype error** |

Case #4 matters: **the failure is not specific to speculative decoding.**

The KV pool itself allocates fine - the blocker is on the consumer side:

| config | KV tokens / instance |
|---|---|
| bf16 + MTP (the only working one) | 2,280,000 |
| fp8_e4m3 + MTP | 3,993,472 (1.75x) |
| fp8_e4m3, no MTP | 4,610,560 (2.02x) |

Launch command for case #2:
```bash
python3 -m sglang.launch_server \
  --model-path <path>/GLM-5.3-Flash --trust-remote-code \
  --tp-size 4 --expert-parallel-size 4 \
  --quantization fp8 --disable-shared-experts-fusion \
  --moe-runner-backend deep_gemm \
  --kv-cache-dtype fp8_e4m3 \
  --dsa-prefill-backend flashmla_sparse_q8 --dsa-decode-backend fa3 \
  --speculative-algorithm NEXTN --speculative-num-steps 3 \
  --speculative-eagle-topk 1 --speculative-num-draft-tokens 4 \
  --context-length 200000 --mem-fraction-static 0.85
```

## A config-level workaround does not exist

`--json-model-override-args '{"index_kpool":1}'` is accepted, but leaves the engine in an inconsistent
half-KPool state:

| attempt | assertion |
|---|---|
| kpool=1 + `flashmla_kv` + MTP | `AssertionError: DSA kpool target_verify requires kpool_write_plan` |
| kpool=1 + `flashmla_kv`, no MTP | `AssertionError: kpool_decode_update_index_cache called when kpool compress is disabled` |

KPool has a dedicated indexer (`srt/layers/attention/dsa/dsa_indexer_kpool.py`) reading three fields
(`index_kpool`, `index_kpool_always_select_tail`, `index_kpool_compress`); overriding one is not enough.
We deliberately did **not** override the rest: KPool changes which tokens sparse attention selects, and the
indexer weights were exported for `kpool=4`, so such a run would silently produce degraded output.

**Consequently we could not verify whether KPool is the *only* blocker for FP8 KV on this model** - that
needs a code change, not a config.

## Suggested fixes (independent)

1. **Teach `flashmla_kv` to handle the KPool tail.** GLM-5.2 already proves `flashmla_kv` + FP8 KV is sound;
   only the tail handling is missing. This looks like the smaller change.
2. **Give the fa3 DSA path a bf16-query x fp8-KV mode**, mirroring what `aiter` already does.

(Extending `_resolve_kpool_tail_backend` to also cover `flashmla_kv` is *not* a third option on its own -
the tail would land on `fa3`, which still faces fp8 KV and hits the same dtype error. It depends on #2.)

## Environment

- 8x NVIDIA H20 (SM90), CUDA, driver/runtime per `lmsysorg/sglang:glm-5.3-flash`
- sglang `0.0.0.dev1+g033446bb05` (image `lmsysorg/sglang:glm-5.3-flash`)
- Models: `ZhipuAI/GLM-5.3-Flash` (native FP8, e4m3) and GLM-5.2-W4AFP8
- Related: #36802 (same model, `--enable-dp-attention` incompatibility)


## 评论 (6)

### beastllama · 2026-08-28

Confirming this wall from different hardware, with a measured backend matrix — it is not SM90-specific. On 2× DGX Spark (GB10, sm_121), GLM-5.3-Flash (LibertAIDAI NVFP4 weight-only quant), SGLang `refs/pull/36507/head`:

| route | result |
|---|---|
| tilelang DSA + `--kv-cache-dtype fp8_e4m3` | hard `ValueError` at arg resolution: `_check_tilelang_dsa_fp8_kv` — "on CUDA they require a bfloat16 KV cache" (ROCm/HIP only) |
| trtllm DSA + fp8 KV | `TllmGenFmhaRunner ... Unsupported arch` — no sm_121 kernels |
| flashmla_kv | excluded at `index_kpool>1` as you found |

So on consumer Blackwell there is currently **no** fp8-KV path for this model at all; bf16 KV costs us roughly half the KV capacity and is a large share of the decode gap vs the patched-vLLM route on identical hardware (which reaches fp8 KV only via a hand-patched CTA tile cap for GB10's 101 KB dynamic smem — see tonyd2wild's recipe).

Possibly useful for a tilelang CUDA fp8-KV path: we mapped the DSA tilelang smem envelope on GB10 exhaustively — `block_I=64` tiles need ≥103.4 KB in any stage/thread shape (169,984 B at stock 64/2/256; 104,448 at 64/1/256; 103,424 at 64/1/128) vs the 101,376 B ceiling, while `block_I=32, num_stages=1, threads=128` fits with no measurable perf cost vs stock on this chip. Data + method: https://github.com/beastllama/GLM-5.3-Flash-DFlash2-SGLang-2x-DGX-Spark

We're attempting a tilelang fp8-KV load path for CUDA (arch-conditional tiles) and will report back either way.


### beastllama · 2026-08-28

Following up on our earlier comment — we said we'd attempt a tilelang fp8-KV path for CUDA and report back. **It works on sm_121.**

Finding: the blocker was plumbing, not kernels. A complete raw-fp8 sparse kernel (`sparse_mla_fwd_decode_partial_fp8`, generic TileLang, no HIP intrinsics) already ships in-tree; it's HIP-gated in three plumbing sites: the dispatch in `tilelang_sparse_fwd`, the pool-layout selection in `kv_cache_configurator.py` (CUDA always builds the scaled 528 B/token layout tilelang can't parse; HIP+tilelang gets the raw 512 B layout), and the raw fused-quant write in `memory_pool.py`. The gate's "CUDA requires bfloat16" is true only of the kernels CUDA happens to dispatch to today.

Port = relax the gate (CUDA + both-backends tilelang + SM89+, hard error on mixed backends), route the raw layout on CUDA for that combination, and key the raw-write branch on layout rather than platform (incidentally more correct on HIP too). GLM-5.3-Flash is the easy case: `kv_lora_rank=512, qk_rope_head_dim=0` → no rope in cache, raw fp8 is exactly half of bf16 (512 vs 1024 B/token on the 11 full-attn layers).

Validation on 2× DGX Spark (GB10, sm_121), GLM-5.3-Flash NVFP4, DFlash2 drafter pinned bf16:
- kernel-level: one-hot exact case rel_err 0.000000; negative control (scrambled indices) fails at 91%; fp8 GEMMs lower and run on sm_121
- boot-gate negative control (mixed backends + fp8) dies with the new ValueError as required
- decode: 29.2 tok/s code (n=5 median) vs 29.8 same-image bf16 — parity
- temp-0 outputs: 4/5 exact vs bf16 across a 5-prompt set
- 32k-depth recall probe: pass (checking the ROCm context-degradation report, #36390)
- TTFT@16k warm: 6.6 s vs 7.9 s bf16 — ~17% faster prefill
- open item: the raw layout shifts pool accounting and our first boot resolved max_running_requests lower than requested — config retune in progress, doesn't look structural

Patch set is 5 files; preparing an upstream PR. Happy to share anything sooner — files and validation protocol are staged in our recipe repo.


### Hansen523 · 2026-08-30

@beastllama This is excellent — thank you for following through, and for the negative controls. The one-hot `rel_err 0.000000` with a scrambled-index control failing at 91% is exactly the evidence that makes the result trustworthy.

**We'd like to take you up on the offer to share early, and can validate on SM90.** Your gate is `CUDA + both-backends tilelang + SM89+`, so H20 (sm_90) should be in scope, but as far as I know nobody has run this path on Hopper yet — your validation is sm_121. We have the hardware idle and a clean baseline to diff against, so we can close that gap rather than leave it as an assumption in the PR.

### What we can offer

**Hardware:** 8x NVIDIA H20-3e (sm_90, 140 GB each), single node, currently free. This is the same cluster the original report came from, and it also runs GLM-5.2-W4AFP8 with `flashmla_kv` + `fp8_e4m3` in production — so we have a working FP8-KV positive control on the *same* silicon to compare against.

**Baseline to diff against:** we now have GLM-5.3-Flash serving correctly on this node with bf16 KV, verified end to end:

```bash
--tp-size 8 --data-parallel-size 8 --enable-dp-attention --expert-parallel-size 8 \
--quantization fp8 --disable-shared-experts-fusion --moe-runner-backend deep_gemm \
--kv-cache-dtype bfloat16 --mm-enable-dp-encoder
```

(on this branch's head, i.e. including #36884 and #36885 — without those, dp8 silently produces wrong output, see #36802). Text and image requests both correct, stable across repeats and 8-way concurrency. So any output divergence we see with your patch is attributable to the fp8-KV path rather than to a shaky starting point.

**What we'd run**, mirroring your protocol so the numbers are comparable:

1. Your kernel-level one-hot check and the scrambled-index negative control, on sm_90
2. The boot-gate negative control (mixed backends + fp8 must raise)
3. `temperature=0` output parity vs the bf16 baseline over a fixed prompt set
4. Decode tok/s and TTFT at a couple of context depths, bf16 vs fp8 same image
5. The allocation numbers — `KV Cache is allocated. dtype/#tokens` plus `max_total_num_tokens` and `max_running_requests` — which speaks directly to your open item about the raw layout shifting pool accounting. On our node bf16 currently resolves to ~3.26M tokens/rank at dp8, so the raw-layout delta should be easy to read off.

We're also happy to report back on anything sm_90-specific that shows up in your three plumbing sites, since Hopper has neither MNNVL nor the sm_121 tensor-memory paths.

### One thing that may be relevant to your open item

You mention the raw layout shifting pool accounting and `max_running_requests` resolving lower than requested. On this model that value is also capped by the mamba state cache, independently of KV: the engine logs `max_running_requests is capped to N by the mamba state cache (max_mamba_cache_size=M, R state slots per request)`, and we measured the per-slot state scaling exactly with `attn_tp_size` (conv 1.166 → 2.329 → 4.695 MB and ssm 33.24 → 66.54 → 133.4 MB at dp2/dp4/dp8). If your retune is chasing a `max_running_requests` that came out lower than requested, it may be worth checking whether the mamba cap or the KV pool is the binding constraint before attributing it to the raw layout — `--mamba-ssm-dtype bfloat16` roughly doubles that pool with byte-identical output at `temperature=0`, which makes it easy to tell the two apart.

Whatever is easiest for you — the five files, a branch we can fetch, or a link to the staged recipe repo — we'll run it and post the sm_90 numbers here.


### Hansen523 · 2026-08-30

## Solved on SM90 — validated PR #36904 on 8x H20

Recording the resolution here so this issue is self-contained.

@beastllama's **#36904** fixes this, and we have now run it on Hopper. The premise of my original report — that no CUDA DSA backend can consume fp8 KV for this model — was too strong. As they put it, the gate's "CUDA requires bfloat16" was only true of the kernels CUDA happened to dispatch to; `sparse_mla_fwd_decode_partial_fp8` already ships in-tree and is generic TileLang, just unreachable behind three HIP-gated plumbing sites plus a pool layout (scaled 528 B/token) the kernel cannot parse.

The root-cause chain in the original report still holds — `index_kpool: 4` does exclude `flashmla_kv`, and that is why GLM-5.2 is fine on the same hardware while GLM-5.3-Flash is not. What was wrong was the conclusion drawn from it.

### SM90 result

8x NVIDIA H20-3e, `ZhipuAI/GLM-5.3-Flash` FP8 weights, dp8. Strict A/B, everything identical except `--kv-cache-dtype`:

| | bf16 | fp8_e4m3 |
|---|---|---|
| `KV Cache is allocated` dtype | `torch.bfloat16` | `torch.float8_e4m3fn` |
| `#tokens` | 2,742,144 | **4,922,240** |
| `KV size` | 32.48 GB | **32.47 GB** |
| `max_mamba_cache_size` | 211 | 211 |

**1.795x the tokens in the same 32.5 GB.** Short of the 2x the raw layout implies, because only the 11 full-attention layers carry the halved KV — the 34 KDA layers are unaffected, and the DSA indexer cells appear not to halve either. The mamba pool is identical on both sides, so the gain is entirely KV.

Output parity at `temperature=0`, final answers after `</think>`: **3/3 identical to bf16** (`北京`, `5`, `9.8`). Completion lengths differ on 2 of 3 — fp8 shifts the reasoning trajectory slightly and converges on the same answer. This is spot-checking, not an accuracy suite.

The gate admits sm_90 (`overrides.py`: SM89+ and both DSA backends tilelang) and logs the expected enablement line.

Full numbers and method: https://github.com/sgl-project/sglang/pull/36904#issuecomment-5466584906

### One caveat for anyone reproducing this today

PR #36904 branches from `xinyuan/glm-5.3-flash-support` at 08-28 14:56, which **predates #36884 and #36885**. Testing it as-is on a `tp_size == attn_dp_size == moe_ep_size` layout will show those two defects — silently wrong output — and it is easy to misattribute them to the fp8-KV change. Merging the PR onto that branch's current head first is a clean merge with no conflicts, and gives a trustworthy baseline.

### Status

Leaving this open until #36904 lands, since the fix is not merged yet and this issue is the reference for the problem. Nothing further needed from our side; happy to re-run on Hopper against any revision of the PR.

Thanks @beastllama for turning this around so quickly, and for the negative controls — the scrambled-index case failing at 91% is what made the kernel-level result worth trusting.


### ormandj · 2026-09-08

Opened #38430 with companion [flashinfer-ai/flashinfer#5022](https://github.com/flashinfer-ai/flashinfer/pull/5022) for a native FlashInfer GLM NoPE FP8-KV path on SM120, including KPool tails. It uses scaled FP8 rows: 512 FP8 values plus four FP32 scales in 528 bytes, compared with the native backend's legacy 656-byte padded rows. This preserves the values and scales; it is separate from #36904's raw TileLang layout.

Author tests passed 17 CPU cases plus 17 subtests and 6 GPU adapter cases on RTX PRO 6000 Blackwell (SM120). The 6 adapter cases and 64 FlashInfer masked-entry cases passed Compute Sanitizer with zero errors. The PR bodies include test commands, geometry and limitations. #37625 remains the separate exact top-k selection dependency.

This does not add SM90 support or establish a fix for the original H20 reproduction. #36904's earlier H20 validation remains relevant, but that PR closed without merging. Keeping this issue open is appropriate; #37813 records the backend and hardware scopes.


### Yihua-Liu-1997 · 2026-09-23

**Status note plus SM90 data: the NoPE `k_dim=0` kernel blocker is fixed on main; the #36904 plumbing is still the remaining gap on CUDA.**

Two parts — a status correction for anyone landing here, and some numbers I don't think are in the thread yet.

### 1. The NoPE tail-GEMM blocker is fixed on main

`sparse_mla_fwd_decode_partial_fp8` used to emit its tail (rope) GEMM unconditionally, so a NoPE model (`qk_rope_head_dim = 0`, hence `d_tail == 0`) hit a `k_dim=0` GEMM and died at compile time:

```
ValueError: Unsupported k_dim 0
  (tilelang/cuda/intrinsics/macro/mma_macro_generator.py:181, via gemm_infer_layout)
```

@marknx reported this on #36904 (2026-08-30) and PR #40301 was opened for it (2026-09-19). It is now fixed on `main` by **#38547** (merged 2026-09-22), which guards the tail alloc / copy / gather / GEMM behind `has_tail` in both the fp8 and the bf16 partial kernels, allows `d_v=256`, and adds `test_tilelang_dsa_zero_rope.py`. #40301 looks superseded by that merge.

One note for anyone reproducing on a pip release: #38547 landed after **v0.5.20** (2026-09-18, currently the newest release), so release users still need it backported. We hit exactly this on 0.5.20 and applied the same four guards by hand before anything would boot.

### 2. What is still missing on main

Read off `main` today:

- `_check_dsa_backend_constraints` (`python/sglang/srt/arg_groups/overrides.py`) still raises for CUDA + tilelang + `fp8_e4m3`.
- `tilelang_sparse_fwd` still selects the fp8 partial kernel only under `if _is_hip`.

So the kernel-side blocker is gone and the #36904 plumbing (gate, dispatch, pool layout, plus whatever `memory_pool.py` needs now) is the remaining gap on CUDA. We have not tested that plumbing against current `main` — this is a reading of the source, not a claim that #36904 still applies cleanly.

### 3. SM90 numbers, on top of the ones already posted here

@Hansen523 already posted 8×H20-3e validation in this thread (1.795× tokens at identical footprint, 3/3 temp-0 probes). What follows is an independent port on a different H20 SKU and a different parallelism layout, so please read it as a second data point rather than a re-report:

- pip release **0.5.20**, with #36904 hand-ported (3 of the 5 sites had drifted since the PR base) plus the #38547 tail guards
- `ZhipuAI/GLM-5.3-Flash` native FP8 (310 GB), single node **8×H20-96G**, TP8+EP8, MTP EAGLE 5-1-6, ctx 1M

Everything identical except `--kv-cache-dtype`:

| | bf16 KV | fp8_e4m3 KV |
|---|---|---|
| `max_total_num_tokens` | 1,483,648 | **2,663,168 (1.80×)** |
| temp=0 known-answer probes | 3/3 | 3/3, identical answers |
| 2k-in/2k-out, out tok/s @ c16/32/64 | 1092 / 1700 / 2289 | 1079 / 1698 / 2283 |
| 50k-in/2k-out @ c32 | **0/96, OOM crash** | **96/96 OK**, 163 out tok/s, TTFT p50 3.9 s |
| 50k-in/2k-out @ c48 | pool too small to run | 144/144 OK, TTFT p50 11.2 s |

The 1.80× reproducing Hansen523's 1.795× on a different H20 SKU and a TP8+EP8+MTP layout is a useful consistency check. The parts I believe are new: short-context throughput is at parity out to c64 (within ~1%), and the 50k-input case is a capability difference rather than a capacity one — that arm cannot run at all with bf16 KV on this node.

Caveats: this is the raw-fp8 layout from #36904 (native fp8 attention), single runs rather than a warmed steady state, and the probe set is spot-checking, not an accuracy suite. These numbers are also **not** comparable with #39349's — that draft takes a different approach (dequantizing the 528 B group-scaled rows into the bf16 consumer) and measures at 64K/128K input, whereas our parity measurement is at 2k input and our bf16 arm OOMs before it can give us a long-context TPOT baseline to compare against.

Happy to post the exact 0.5.20 diff, or to re-run anything on this node against a revision of whichever CUDA approach maintainers prefer.

