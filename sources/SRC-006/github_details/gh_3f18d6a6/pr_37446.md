# [PR #37446] [Fix] Load FP8 KV-cache scales for Qwen3.5

source: https://github.com/sgl-project/sglang/pull/37446
state: open | updated: 2026-09-25T07:08:56Z
labels: run-ci

## 正文

## Motivation

Fixes #37379.

When `--kv-cache-dtype fp8_e4m3` is used with
`--quantization-param-path`, Qwen3.5 failed during startup because its model
classes did not expose `load_kv_cache_scales`. The architecture mixes full
attention and GatedDeltaNet layers, so the implementation must only apply the
loaded factors to full-attention layers. Pipeline-parallel ranks also contain
placeholder layers for stages they do not own.

## What changed

- Added `Qwen3_5ForCausalLM.load_kv_cache_scales`.
- Skip GatedDeltaNet and pipeline-placeholder layers when applying factors.
- Populate `k_scale`, `v_scale`, `k_scale_float`, and `v_scale_float` for the
  Triton/FlashInfer paths.
- Added delegation through both Qwen3.5 conditional-generation wrappers.
- Added CPU unit coverage for skipping non-attention layers and wrapper
  delegation.

No open or historical PR references #37379 or this Qwen3.5 KV-scale-loading
fix (checked immediately before submission).

## Validation

- SGLang pre-commit checks for both changed files passed (with the repository's
  protected-branch hook intentionally skipped on the feature branch).
- Python AST/bytecode compilation passed for the changed module and test.
- `git diff --check` passed.
- The isolated unit test could not be executed locally because this Apple
  Silicon environment does not have SGLang's CUDA-only dependencies (`orjson`
  and Triton); CI has the complete runtime.
- Full GPU FP8 validation requires the model and hardware described in the
  issue.

AI assistance was used for repository scouting and implementation. The human
submitter reviewed the complete diff and validation before opening this PR.

<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:white_check_mark: [Run #36072795762](https://github.com/sgl-project/sglang/actions/runs/36072795762)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #36072795780](https://github.com/sgl-project/sglang/actions/runs/36072795780)<!-- slot:pr-test-extra:end -->
Latest PR Test (AMD ROCm 10): <!-- slot:pr-test-amd:start -->:x: [Run #36072795985](https://github.com/sgl-project/sglang/actions/runs/36072795985)<!-- slot:pr-test-amd:end -->
<!-- pr-states:end -->

## 评论 (4)

### xesdiny · 2026-09-02

Hi @saichowdary007, I'm the issue author (#37379). The overall approach is correct — I had a nearly identical implementation in a local patch and validated it on L20 x8 / TP=8 with `Qwen3_5MoeForConditionalGeneration`.

A few notes:

**1. P1: `k_scale`/`v_scale` should be tensors, not floats**

`kv_cache_scales_loader` yields Python `float` values. The AITER backend calls `.reshape(1)` on `layer.k_scale` and `layer.v_scale`, so those two must remain tensors:

```python
layer.attn.k_scale = torch.tensor(scaling_factor)
layer.attn.v_scale = torch.tensor(scaling_factor)
layer.attn.k_scale_float = float(scaling_factor)
layer.attn.v_scale_float = float(scaling_factor)
```

(The `_float` fields are used by the Triton path and should stay as scalars.)

**2. `Qwen3_5ForConditionalGeneration` delegation is correct and needed**

Good catch adding it — that class was also missing in my local fix.

**3. Validation**

My local patch (same logic, duck-typed layer guard) on NVIDIA L20 x8: server starts without the `RuntimeError`, FP8 KV cache scales load correctly for all 12 attention layers, SSM layers silently skipped. Happy to re-test once the tensor fix is applied.

### saichowdary007 · 2026-09-03

Thanks @xesdiny — fixed in commit `610d2c2b60`. `k_scale` and `v_scale` now stay as `torch.tensor(scaling_factor)` for AITER/FlashMLA, while `k_scale_float` and `v_scale_float` remain Python floats for Triton/FlashInfer. I also resolved the branch conflict against current `main` and updated the regression assertions. Local `py_compile`, Ruff checks, and whitespace checks pass; the full CUDA/L20 validation is still unavailable in this Apple Silicon environment. Please re-test the updated commit on L20 x8 when convenient.

### xesdiny · 2026-09-03

@saichowdary007 
Re-tested commit `610d2c2b60` on L20×8 (8× NVIDIA L20 48 GB, SGLang v0.5.16, TP=8).

- Server startup: ✅ no errors during `load_kv_cache_scales`
- KV FP8 scale loading: ✅ all attention layers received scales without error
- Inference: ✅ outputs correct, `finish_reason: stop`

The `torch.tensor(scaling_factor)` fix for `k_scale`/`v_scale` is confirmed correct on this hardware. LGTM from the L20 side.

### xesdiny · 2026-09-14

@ispobock @xiezhq-hermann @Fridge003 — CI is blocked at `pr-gate` (needs maintainer approval to run). The fix has been validated on L20×8 / TP=8 / SGLang v0.5.16 (see #37379). Could someone trigger CI and take a look?

## Review (1)

### chatgpt-codex-connector[bot] · 2026-09-01 · COMMENTED


### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `45b6dafda8`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team has set up Codex to review pull requests in this repo](https://chatgpt.com/codex/cloud/settings/general). Reviews are triggered when you
- Open a pull request for review
- Mark a draft as ready
- Comment "@codex review".

If Codex has suggestions, it will comment; otherwise it will react with 👍.




Codex can also answer questions or update the PR. Try commenting "@codex address that feedback".
            
</details>
