# [PR #41230] [MLX] Reject output logprobs without sampling

source: https://github.com/sgl-project/sglang/pull/41230
state: open | updated: 2026-09-25T07:35:34Z
labels: documentation, apple-silicon

## 正文

## Summary

On the MLX backend, `return_logprob=True` without `--mlx-enable-sampling` produced empty logprob arrays. `/v1/score` then returned an opaque 500 after the score result processor found no output logprobs.

The scheduler now rejects this unsupported request before admission with `Output logprobs on the MLX backend require --mlx-enable-sampling.` Non-MLX requests and MLX requests with sampling enabled keep their existing paths. The Apple Metal guide lists the flag beside the other MLX serving parameters and describes its logprob requirement.

## Validation

- `python -m pytest test/registered/unit/managers/test_scheduler_uno_request_validation.py test/registered/unit/managers/test_scheduler_sampling_mask_validation.py -q`: 3 passed, 2 subtests passed.
- The regression constructs a scheduler request with MLX enabled, sampling disabled, and `return_logprob=True`; it verifies the request abort message and queue boundary.
- Repository pre-commit checks passed for AST validation, import ordering, Ruff, formatting, codespell, CI registry validation, and related hooks.
- `git diff --check` passed.
- An Apple Metal server run remains pending; the local validation covers the request boundary before backend execution.

Fixes #41211.

<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:x: [Run #36108357064](https://github.com/sgl-project/sglang/actions/runs/36108357064)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #36108356818](https://github.com/sgl-project/sglang/actions/runs/36108356818)<!-- slot:pr-test-extra:end -->
Latest PR Test (AMD ROCm 10): <!-- slot:pr-test-amd:start -->:x: [Run #36108356886](https://github.com/sgl-project/sglang/actions/runs/36108356886)<!-- slot:pr-test-amd:end -->
<!-- pr-states:end -->

## 评论 (1)

### ZenAlexa · 2026-09-25

The implementation and CPU regression are ready. The repository gate requires the `run-ci` label before the MLX workflow can start; the fork account cannot add repository labels. Please add `run-ci` when convenient so the staged MLX checks can run.


## Review (1)

### copilot-pull-request-reviewer[bot] · 2026-09-25 · COMMENTED

Copilot was unable to review this pull request because the user who requested the review has reached their quota limit.
