# [Issue #21443] [Feature][MPS] Better memory management for Apple Silicon Macs

source: https://github.com/sgl-project/sglang/issues/21443
state: open | updated: 2026-09-25T03:13:51Z
labels: 

## 正文

### Checklist

- [ ] If this is not a feature request but a general question, please start a discussion at https://github.com/sgl-project/sglang/discussions. Otherwise, it will be closed.
- [ ] Please use English. Otherwise, it will be closed.

### Motivation

## Problem

On Apple Silicon Macs, GPU and CPU share Unified Memory Architecture (UMA). Several places in SGLang use `psutil.virtual_memory().total` or `.available` to determine how much memory is available for model loading and KV cache allocation on the MPS device:

- **`python/sglang/srt/utils/common.py` – `get_available_gpu_memory()`** (line 604–605):
  ```python
  elif device == "mps":
      free_gpu_memory = psutil.virtual_memory().available
  ```
- **`python/sglang/_mps_stub.py` – `get_device_properties()`** (line 131–132):
  ```python
  _cached_props = _MPSDeviceProperties(
      total_memory=psutil.virtual_memory().total,
  )
  ```
- **`python/sglang/multimodal_gen/runtime/platforms/mps.py` – `MpsPlatform.get_available_gpu_memory()`** (line 86):
  ```python
  free_memory = psutil.virtual_memory().available
  ```

However, **Metal cannot use all of system RAM**. Apple exposes a per-device limit via [`MTLDevice.recommendedMaxWorkingSetSize`](https://developer.apple.com/documentation/metal/mtldevice/2369280-recommendedmaxworkingsetsize), which is typically **~65–75% of total RAM** depending on the chip and macOS version. Allocating beyond this limit causes the system to start paging GPU-resident memory, leading to:

- Severe performance degradation (GPU stalls waiting for page-ins)
- System instability
- **Machine reboots** (I have personally experienced this)

This was also flagged in a code review: *"What is our OOM behavior here? (I don't want you to OOM your Mac haha, but we should have a guardrail around this)"*

## Expected Behavior

Memory reporting for MPS should be capped to Metal's recommended max working set size, not the full system RAM. This ensures SGLang never tries to allocate more memory than the GPU subsystem can safely handle.

## Reference Implementation

The [vllm-metal plugin](https://github.com/vllm-project/vllm-metal) already handles this correctly using MLX's API:

```python
# vllm-metal/vllm_metal/utils.py
device_info = mx.metal.device_info()
max_wired = int(device_info.get("max_recommended_working_set_size", 0))
```

It also sets the wired memory limit to prevent paging:

```python
# vllm-metal/vllm_metal/utils.py
mx.set_wired_limit(max_wired)
```

And uses this value to cap prefix cache and KV cache budgets:

```python
# vllm-metal/vllm_metal/v1/model_runner.py
device_info = mx.metal.device_info()
total = int(device_info.get("max_recommended_working_set_size", 0))
max_bytes = int(total * fraction)
```

### Related resources

_No response_

## 评论 (5)

### karanb192 · 2026-03-27

Hi @yeahdongcn, I'd like to pick this up!

**Plan:**
- Cap memory reporting to `mx.metal.device_info()["max_recommended_working_set_size"]` in all three callsites:
  1. `get_available_gpu_memory()` in `common.py`
  2. `get_device_properties()` in `_mps_stub.py`
  3. `MpsPlatform.get_available_gpu_memory()` in `mps.py`
- Add `mx.set_wired_limit()` to prevent paging
- Following the [vllm-metal reference implementation](https://github.com/vllm-project/vllm-metal)

**Testing:** M3 Pro (18GB) — can verify the fix prevents OOM locally.

Will have a PR up shortly.

### github-actions[bot] · 2026-05-27

This issue has been automatically closed due to inactivity. Please feel free to reopen it if needed.

### yeahdongcn · 2026-05-30

Synced with @karanb192, he will get this fixed soon.

### github-actions[bot] · 2026-07-30

This issue has been automatically closed due to inactivity. Please feel free to reopen it if needed.

### luke-edward · 2026-09-25

One more data point for this issue, on the MLX runtime rather than torch-MPS: the buffer cache is what grows.

MLX's default cache limit equals its memory limit (15.2 GB of 16 GB on my M2), and `MlxModelRunner` then sets the wired limit to `max_recommended_working_set_size` (10.67 GB). On `main` @ 6103a8c, serving Qwen2.5-0.5B-Instruct under 30 s of 1.5 req/s Poisson load, system wired memory grows from 4.4 GB to 8.9 GB, and available memory bottoms out at 1.35 GB. With `SGLANG_MLX_CACHE_LIMIT_GB=1` (the existing, opt-in knob) it grows to 6.4 GB instead, available memory stays above 3.5 GB, and TTFT/TPOT are unchanged (two interleaved repeats each; full tables in the PR).

#41209 makes that cap the default (10% of the recommended working set, at least 1 GiB; still overridable) and documents the env var, which the Apple Metal guide did not mention. It is the small static version of what vllm-metal does with `mx.set_cache_limit` after its profiling pass. @yeahdongcn, if you would rather have the measured version or different constants, say so and I will adjust.

