# [PR #2254] Adding FI_MORE to the Gin Proxy Path

source: https://github.com/NVIDIA/nccl/pull/2254
state: closed | updated: 2026-08-26T18:47:42Z
labels: 

## 正文

## Description

Plugins in the RMA/GIN Host Proxy path would benefit from being able to batch sends at the network level. This change bumps the RMA version and adds in logic for passing `optFlags` from the proxy through NCCL into the plugin, so the plugin can hold off on ringing the NIC doorbell when it knows more ops are coming. i.e. In Libfabric, an `FI_MORE` flag can be set to let the NIC know that more data is coming and to hold off on ringing the doorbell meaning a batch of ops rings one doorbell instead of one per op.

The proxy sets the hint by looking checking if the next slot on the GFD queue is already filled. If so then, more ops are in flight. It falls back to an aggregate bit the kernel can stamp in the GFD, which covers the terminal op of a batch where look-ahead can't see the next op yet.

Note the `FI_MORE` handling itself lives in the plugin (e.g. aws-ofi-nccl), not in this change — NCCL just plumbs the optFlags hint down to it.


## Changes & Impact

- Bumping up the RMA version from v14 -> v15 to add support for passing optFlags to the plugin. Older v14 plugins don't get the param and behave as before.
- Carved out an aggregation bit in the GFD header for aggregating writes via FI_MORE (guarded by a static_assert on the GFD version).
- Added look-ahead logic in the proxy to set the flag when it sees more data coming on the GFD queue.

## 评论 (9)

### kgioioso · 2026-06-25

Thinking a bit more, I see what you're trying to do with the lookahead. So I think we need:
aggregate = (userSetAggregateFlag || nextGfdAvailable) && !isLastInBatch

### hershys-aws · 2026-06-26

> Thinking a bit more, I see what you're trying to do with the lookahead. So I think we need: aggregate = (userSetAggregateFlag || nextGfdAvailable) && !isLastInBatch

Thanks for the prompt review. I have changed the logic so that aggregate will be set as long as there are ops coming in and it's not the last op in the batch. I bumped the GFD version and placed a runtime check. I kept the `static_assert` there just as a sanity check for the next person who may change the header. Let me know if anything else is needed. Thanks for your time!

### kgioioso · 2026-06-26

/mirror dev

### kgioioso · 2026-06-26

I've mirror this to our internal repo. 

@hershys-aws , for completeness, can you post perf numbers? How much improvement is EFA seeing from this change?

You or @bhasunit can also send it to me over slack if preferred.

### kgioioso · 2026-06-26

@hershys-aws I discovered another issue. If the next GFD is not ready, but aggregate is set, we will early-return, and the ops will be left hanging until we re-enter Progress. This could make setting aggregate _worse_ than not setting it, which is an issue. 

At this point, I think this design needs a bit more thought. I'm tentatively thinking we should not use the aggregate flag from the device code, and instead only send aggregate to the plugin if the next GFD is already ready (and it's not the last in the batch). But I need to think a bit more. Let me know if you have ideas or feedback.

### hershys-aws · 2026-06-26

> @hershys-aws I discovered another issue. If the next GFD is not ready, but aggregate is set, we will early-return, and the ops will be left hanging until we re-enter Progress. This could make setting aggregate _worse_ than not setting it, which is an issue.
> 
> At this point, I think this design needs a bit more thought. I'm tentatively thinking we should not use the aggregate flag from the device code, and instead only send aggregate to the plugin if the next GFD is already ready (and it's not the last in the batch). But I need to think a bit more. Let me know if you have ideas or feedback.

Sorry about that, I didn't see this in the testing when running on our hardware. I talked to @bhasunit  and we decided the simplest thing at this point would be to go with the approach you have mentioned. My next push will only pass the flag to the plugin if we see the next GFD ready. Going to do this in `proxyGinProcessGfd`:

```
    // BEFORE
    bool aggregate = (extractAggregate(gfd) || isGfdAvailable(hostGpuCtx, targetRank)) && !isLastInBatch;
    // AFTER
    bool aggregate = isGfdAvailable(hostGpuCtx, targetRank) && !isLastInBatch;
```

and stripping out the plumbing which was placed in `src/include/nccl_device/gin/proxy/gin_proxy.h` and `src/include/nccl_device/gin/proxy/gin_proxy_device_host_common.h`. 

### kgioioso · 2026-07-06

/mirror dev

### kgioioso · 2026-07-07


> Sorry about that, I didn't see this in the testing when running on our hardware. 

Just to clarify, this was a hypothetical regression, not a measured regression. But I think it was important enough to warrant a change.

> I talked to @bhasunit and we decided the simplest thing at this point would be to go with the approach you have mentioned. 

This looks good. Is there a perf difference between the current approach and actually looking at the device-side aggregate requests? If so, we can re-visit in the next release. But I'm hoping this approach is just as good

### kgioioso · 2026-07-08

@bhasunit @hershys-aws This was merged. I changed the name of the opt flag in this [commit](https://github.com/NVIDIA/nccl/commit/f6be6cfa070074baed121b76db7e2bb188633a25).

## Review (1)

### kgioioso · 2026-06-25 · COMMENTED

Thanks for the PR. I like the overall idea. 

I'm a bit concerned with the interaction between aggregate requests and proxy progress loop. In particular, if the proxy progress loop moves on to another iteration (`ncclGinProxyProgress` returns), I believe we should set the aggregate flag to false, possible overriding the user's request. 

Also, I think we should consider tweaking the batch logic to only process >1 GFD if aggregate is set.

So, I'm thinking `ncclGinProxyProgress` should look something like this:
```
if (int p = 0; p < pollBatch; p+) {
  bool isAggregate;
  if (!proxyGinPollGfd) break;
  bool isLastInBatch = (p == pollBatch - 1);
  // overrides the user's aggregate flag if isLastInBatch is true
  // Updates isAggregate with the user's aggregate flag
  proxyGinProcessGfd(..., &isAggregate, isLastInBatch);
  
 if (!isAggregate) break;
}
```
Basically, aggregate decides whether we should process more GFDs. The pollBatch is used to enforce fair-sharing across devComms/connections.

What do you think?
