# [PR #1427] refactor: move run_agent_loop to evalscope.api.agent as public API

source: https://github.com/modelscope/evalscope/pull/1427
state: closed | updated: 2026-09-10T07:43:28Z
labels: 

## 正文

## Summary

Move `run_agent_loop` from internal module `evalscope/api/benchmark/adapters/_agent_loop_runner.py` to `evalscope/api/agent/runner.py` as a public API.

## Motivation

The underscore-prefixed `_agent_loop_runner.py` was being imported by `evalscope/agent/runner.py` (a cross-package consumer), violating the Python convention that `_`-prefixed modules are package-internal implementation details.

## Changes

- **Move** `_agent_loop_runner.py` → `evalscope/api/agent/runner.py` (semantically belongs with `AgentLoop`, `AgentContext`, etc.)
- **Export** `run_agent_loop` from `evalscope.api.agent.__init__`
- **Update imports** in both consumers:
  - `evalscope/api/benchmark/adapters/agent_loop_adapter.py`
  - `evalscope/agent/runner.py`
- **Delete** the old internal module

## Testing

All related tests pass with no regression:
- `tests/agent/test_agent_loop.py` — 8 passed
- `tests/agent/external/test_agent_loop_external.py` — 2 passed
- `tests/agent/test_t2_environment.py` — 34 passed, 9 skipped
- `tests/benchmark/test_agent_loop.py` — 3 passed (1 pre-existing failure unrelated to this change)

## 评论 (0)

## Review (2)

### gemini-code-assist[bot] · 2026-06-23 · COMMENTED

## Code Review

This pull request refactors the import path of `run_agent_loop` by moving it to the `evalscope.api.agent` package and exposing it in its `__init__.py`. It also updates imports in `evalscope/api/agent/runner.py` to use direct absolute imports to avoid circular dependencies. The feedback suggests using relative imports instead of absolute imports for sibling modules in `runner.py` to maintain consistency with other modules in the package.

> [!IMPORTANT]
> The [consumer version of Gemini Code Assist on GitHub](https://developers.google.com/gemini-code-assist/docs/review-repo-code) is being sunset. Starting **June 18, 2026**, new organization installations will be blocked, and all code review activity will officially cease on **July 17, 2026**.
> For more details on the timeline and next steps, please review the [Help Documentation](https://developers.google.com/gemini-code-assist/docs/deprecations/consumer-code-review).

### copilot-pull-request-reviewer[bot] · 2026-06-23 · COMMENTED

## Pull request overview

This PR promotes `run_agent_loop` to a public API under `evalscope.api.agent`, replacing prior imports from an underscore-prefixed internal module and aligning ownership with the rest of the AgentLoop API surface.

**Changes:**
- Expose `run_agent_loop` via `evalscope.api.agent.runner` and re-export it from `evalscope.api.agent`.
- Update internal and cross-package consumers to import `run_agent_loop` from the public API.
- Refactor `evalscope.api.agent.runner` imports to avoid importing via `evalscope.api.agent` (package `__init__`) and reduce circular-import risk.

### Reviewed changes

Copilot reviewed 4 out of 4 changed files in this pull request and generated no comments.

| File | Description |
| ---- | ----------- |
| evalscope/api/benchmark/adapters/agent_loop_adapter.py | Switch to importing `run_agent_loop` from the public `evalscope.api.agent` surface. |
| evalscope/api/agent/runner.py | Hosts the shared `run_agent_loop` helper and adjusts imports to avoid circular dependencies. |
| evalscope/api/agent/__init__.py | Re-export `run_agent_loop` as part of the public Agent API (`__all__`). |
| evalscope/agent/runner.py | Update consumer to import `run_agent_loop` from `evalscope.api.agent` instead of an internal module. |



<details>
<summary>Comments suppressed due to low confidence (1)</summary>

**evalscope/api/agent/runner.py:22**
* Within the `evalscope.api.agent` package, other modules consistently use relative imports for intra-package dependencies (e.g., `evalscope/api/agent/loop.py:24-29`, `tool_executor.py:12`, `strategy.py:28`). `runner.py` is the only module using absolute `evalscope.api.agent.*` imports, which is inconsistent and can make refactors harder (module renames/relocations require more global updates). Consider switching these to relative imports while still avoiding importing from `evalscope.api.agent` (the package `__init__`) to prevent circular imports.
</details>


