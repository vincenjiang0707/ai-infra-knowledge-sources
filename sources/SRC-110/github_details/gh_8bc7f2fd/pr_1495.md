# [PR #1495] feat(perf): unified --dataset-args with fixed-length input (#1483)

source: https://github.com/modelscope/evalscope/pull/1495
state: closed | updated: 2026-09-10T07:43:20Z
labels: 

## 正文

## Summary

Closes #1483. Adds a unified, typed `--dataset-args` mechanism for the `perf` module and implements fixed-length input construction for real-text datasets on top of it.

## What's new

**1. Typed per-dataset args framework**
- Each dataset plugin declares an `args_schema` (Pydantic, `extra='forbid'`). `--dataset-args` (JSON) is validated against the selected dataset's schema **at plugin construction** — unknown keys fail fast.
- New module `evalscope/perf/plugin/datasets/dataset_args.py`: `BaseDatasetArgs` / `TextLengthArgs` / `TextDatasetArgs` / `MultiTurnDatasetArgs`.

**2. Fixed-length input for real datasets (#1483)**
- `--dataset-args '{"target_input_len": N, "input_len_mode": "cap"|"drop"}'` actively truncates real-text prompts to an exact token length (requires `--tokenizer-path`).
- `cap` (default): truncate over-length, keep shorter as-is. `drop`: truncate over-length, drop shorter ones (every prompt exactly `N`).
- Applies to `openqa` / `longalpaca` / `line_by_line` / single-turn ShareGPT. Truncation primitive is shared in `datasets/utils.py` (also reused by `swe_smith`).
- This is distinct from `--max/min-prompt-length` (which only **filters**, never mutates); `min==max` filtering cannot produce fixed-length inputs from real data.

**3. Backward compatibility**
- `--multi-turn-args` is deprecated and automatically folded into `--dataset-args` (on key conflict, `--dataset-args` wins; `num_workers` still promoted to top-level `--num-workers`). Existing scripts keep working.

**4. Architecture**
- The config layer (`Arguments`) holds `dataset_args` as a raw dict only and has **no dependency on the plugin layer** — the plugin resolves it via its own `args_schema`, eliminating the import cycle (no scattered lazy imports).

## Scope notes
- `input_len_mode` intentionally supports only `cap`/`drop` (no padding — padding real prompts with synthetic tokens would defeat the realism the feature is for).
- Output-side `ignore_eos` was already supported via `--extra-args '{"ignore_eos": true}'`; not re-added.
- `#1489` (workload_trace) is out of scope; the framework leaves an `args_schema` extension point for it.

## Testing
- Unit: `tests/perf/test_dataset_args.py` (schema selection, fail-fast validation, `--multi-turn-args` folding), `tests/perf/test_target_input_len.py` (cap/drop, tokenizer requirement).
- Regression: existing perf tests (`test_request_generation.py`, `test_line_by_line_body.py`) pass; CI smoke `test_ci_lite` passes.
- e2e (real): `longalpaca` + Qwen2.5 tokenizer truncated to exactly 512 tokens; `swe_smith` live construction from ModelScope; **real API** run against DashScope `qwen-turbo` confirmed server-side Input Tokens ≈ 524 (512 + chat-template overhead) with 100% success.

## Docs
- Updated `docs/{zh,en}/user_guides/stress_test/parameters.md` and `multi_turn.md`.

## 评论 (0)

## Review (2)

### gemini-code-assist[bot] · 2026-07-16 · COMMENTED

## Code Review

This pull request introduces a unified `--dataset-args` parameter to replace the deprecated `--multi-turn-args` and support per-dataset configurations, such as truncating real-text datasets to a fixed input length. The changes include Pydantic schemas for validation, backward-compatibility folding logic, and updated documentation and tests. The review feedback highlights a critical bug where `num_workers` is not popped from `dataset_args` during promotion (which would trigger a `ValidationError` due to forbidden extra fields), suggests performance optimizations in the text-fitting helpers to avoid redundant decoding, and recommends adding a test case to cover the promotion and popping of `num_workers`.

> [!IMPORTANT]
> The [consumer version of Gemini Code Assist on GitHub](https://developers.google.com/gemini-code-assist/docs/review-repo-code) is being sunset. Starting **June 18, 2026**, new organization installations will be blocked, and all code review activity will officially cease on **July 17, 2026**.
> For more details on the timeline and next steps, please review the [Help Documentation](https://developers.google.com/gemini-code-assist/docs/deprecations/consumer-code-review).

### copilot-pull-request-reviewer[bot] · 2026-07-16 · COMMENTED

## Pull request overview

This PR introduces a unified, typed `--dataset-args` mechanism for the `perf` module and adds fixed-length input truncation behavior for real-text datasets, while maintaining backward compatibility with the deprecated `--multi-turn-args`.

**Changes:**
- Added per-dataset Pydantic `args_schema` validation (fail-fast `extra='forbid'`) and folded legacy `--multi-turn-args` into `dataset_args`.
- Implemented fixed-length input fitting (`target_input_len` + `input_len_mode=cap|drop`) via shared dataset utilities and integrated it into real-text dataset plugins.
- Added unit/integration tests and updated English/Chinese documentation for the new CLI flag and deprecation.

### Reviewed changes

Copilot reviewed 16 out of 16 changed files in this pull request and generated 3 comments.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| tests/perf/test_target_input_len.py | Tests for `truncate_text_to_token_len` / `fit_text_to_token_len` and prompt preparation integration. |
| tests/perf/test_dataset_args.py | Tests schema selection, fail-fast validation, JSON parsing, and legacy `--multi-turn-args` folding. |
| evalscope/perf/plugin/datasets/utils.py | Adds shared truncation/fitting helpers for fixed-length input behavior. |
| evalscope/perf/plugin/datasets/swe_smith.py | Reuses shared truncation helper; adopts typed `MultiTurnDatasetArgs`; refactors turn conversion. |
| evalscope/perf/plugin/datasets/share_gpt.py | Applies `prepare_prompt` to single-turn ShareGPT (fixed-length fit / filtering). |
| evalscope/perf/plugin/datasets/openqa.py | Applies `prepare_prompt` to OpenQA prompts; declares text dataset schema. |
| evalscope/perf/plugin/datasets/multi_turn.py | Declares multi-turn dataset schema for multi-turn plugins. |
| evalscope/perf/plugin/datasets/longalpaca.py | Applies `prepare_prompt` and declares text dataset schema. |
| evalscope/perf/plugin/datasets/line_by_line.py | Applies `prepare_prompt` for plain-text lines; declares text dataset schema. |
| evalscope/perf/plugin/datasets/dataset_args.py | New typed dataset-args schema module (`BaseDatasetArgs`, `TextDatasetArgs`, `MultiTurnDatasetArgs`). |
| evalscope/perf/plugin/datasets/base.py | Validates `dataset_args` at plugin construction; adds `prepare_prompt` helper and tokenizer requirement check. |
| evalscope/perf/arguments.py | Adds `dataset_args` field + CLI flag; folds legacy args and promotes deprecated `num_workers`. |
| docs/zh/user_guides/stress_test/parameters.md | Documents `--dataset-args`, fixed-length input behavior, and deprecation notes (ZH). |
| docs/zh/user_guides/stress_test/multi_turn.md | Updates multi-turn docs to use `--dataset-args` and notes deprecation (ZH). |
| docs/en/user_guides/stress_test/parameters.md | Documents `--dataset-args`, fixed-length input behavior, and deprecation notes (EN). |
| docs/en/user_guides/stress_test/multi_turn.md | Updates multi-turn docs to use `--dataset-args` and notes deprecation (EN). |
</details>







