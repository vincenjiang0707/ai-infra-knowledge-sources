# [PR #1442] fix(perf): absolute-time rate scheduling for multi-turn + close event loop

source: https://github.com/modelscope/evalscope/pull/1442
state: closed | updated: 2026-09-10T07:43:26Z
labels: 

## 正文

## Summary

Two targeted fixes for the perf benchmark module:

### 1. Multi-turn rate limiting: absolute-time scheduling

**Problem**: `MultiTurnStrategy._worker` used relative-time pacing (`await asyncio.sleep(interval)`) which accumulates drift from event-loop jitter — the effective send rate decays monotonically over time (observed ~20-30% below target for long runs).

**Fix**: Replace with absolute-time scheduling (same approach already used by `OpenLoopStrategy` and `ClosedLoopStrategy`):
- Each worker maintains a `_next_dispatch_at` timestamp anchor
- Advances the anchor by a sampled Poisson interval before each turn
- Sleeps until the target time; if already past it, dispatches immediately (self-corrects drift)

### 2. Event loop resource leak

**Problem**: `run_one_benchmark` creates a new `asyncio.new_event_loop()` per sweep iteration but never calls `loop.close()`, leaking file descriptors and thread resources across multi-run sweeps.

**Fix**: Wrap `loop.run_until_complete()` in `try/finally` to ensure `loop.close()` is always called.

## Testing

- All 36 existing perf unit tests pass (trace_metrics, workload_timeline, openai_api_parse, trie_replay)
- Mock integration test verifies 12 multi-turn turns dispatched at 10 rps complete within expected wall-clock time (0.643s actual vs 0.8s expected max)
- All pre-commit hooks pass (flake8, isort, yapf)

## 评论 (0)

## Review (2)

### gemini-code-assist[bot] · 2026-06-29 · COMMENTED

## Code Review

This pull request introduces absolute-time scheduling to the multi-turn benchmark strategy to prevent drift from event-loop jitter, and ensures that the event loop is closed properly in `evalscope/perf/main.py` using a `try...finally` block. A critical issue was identified in the multi-turn rate-limiting logic: because the worker awaits API responses, `_next_dispatch_at` can fall far behind the current time, causing the worker to skip sleeping entirely. A suggestion was provided to bound `_next_dispatch_at` using `max(_next_dispatch_at, time.perf_counter())` to reset the anchor when blocked by API latency.

> [!IMPORTANT]
> The [consumer version of Gemini Code Assist on GitHub](https://developers.google.com/gemini-code-assist/docs/review-repo-code) is being sunset. Starting **June 18, 2026**, new organization installations will be blocked, and all code review activity will officially cease on **July 17, 2026**.
> For more details on the timeline and next steps, please review the [Help Documentation](https://developers.google.com/gemini-code-assist/docs/deprecations/consumer-code-review).

### copilot-pull-request-reviewer[bot] · 2026-06-29 · COMMENTED

## Pull request overview

This PR targets the perf benchmark runtime by improving multi-turn request pacing accuracy under `--rate` and ensuring per-sweep event loops are properly cleaned up to avoid resource accumulation.

**Changes:**
- Switch `MultiTurnStrategy` rate limiting from relative sleeps to absolute-time scheduling to prevent long-run drift.
- Ensure the event loop created in `run_one_benchmark` is closed via `try/finally` to reduce resource leaks across multi-run sweeps.

### Reviewed changes

Copilot reviewed 2 out of 2 changed files in this pull request and generated 2 comments.

| File | Description |
| ---- | ----------- |
| evalscope/perf/main.py | Wrap benchmark execution in `try/finally` and close the per-run asyncio loop. |
| evalscope/perf/core/strategies/multi_turn.py | Implement absolute-time rate scheduling for multi-turn dispatch to reduce event-loop jitter drift. |





