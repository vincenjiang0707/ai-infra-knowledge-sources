# [PR #3112] Restore offloaded tensors outside inference mode

source: https://github.com/ModelCloud/GPTQModel/pull/3112
state: closed | updated: 2026-09-25T00:37:22Z
labels: 

## 正文

## Summary

undo_offload_to_disk can restore inference tensors that cannot be saved for backward. Construct replacement tensors and detach hooks outside inference mode, including when the caller enables inference mode.

## What Changed

- Create replacement parameters and buffers outside inference mode.
- Detach Accelerate hooks and restore tied weights in the same safe context.

## Tests

- [x] Added targeted regression tests and ran them locally.
- [x] Verified the defect against the unchanged base.

```bash
python -m pytest -o addopts= -o log_cli=false -q tests/test_offload_autograd.py tests/test_offload_files.py
```

Focused result: 59 passed, 1 skipped. Runtime: Python 3.13, PyTorch 2.11,
Transformers 5.16.1, Accelerate 1.14.0. Full model quantization suite was not run.

## Review Requirements

- [x] I personally reviewed every file in this diff.
- [x] I checked that the code matches existing project structure, APIs, and conventions.
- [x] I avoided unnecessary monkeypatching and used the project's normal extension points where possible.

## Notes

Regressions cover preserved values and dtypes, parameters and buffers, tied weights, version counters, and backward. No change to offload storage ownership or dtype-conversion policy.


## 评论 (1)

### Qubitium · 2026-09-25

Pushed `0ac7f391`: hook detachment and weight tying still run outside inference mode, with gradient recording disabled so custom `tie_weights()` methods can update restored trainable parameters in place. Added a regression test; 62 focused offload tests pass and Ruff is clean.

