# [PR #15242] build: compile NVTX ranges into runtime wheels via the header-only nvtx crate

source: https://github.com/ai-dynamo/dynamo/pull/15242
state: open | updated: 2026-09-24T02:52:55Z
labels: size/M, build, container

## 正文

## Summary

Make the runtime's NVTX ranges shippable.

### Compile NVTX ranges into runtime wheels

The Rust runtime annotates the frontend hot path (`preprocess.*`, `route.*`, `transport.*`, `detokenize`) behind the `nvtx` Cargo feature and the `DYN_ENABLE_RUST_NVTX` runtime gate. The feature was absent from the wheel builds, so in every published wheel the macros expand to nothing and the runtime gate has no effect. It could not simply be enabled: the ranges went through `cudarc::nvtx`, whose build links `libnvToolsExt.so`, the NVTX v2 shared library that CUDA 12.9 removed. The CUDA 13 base images ship only the `nvtx3/*.h` headers, so a wheel built with the feature either fails to link or fails to import.

- `lib/runtime/src/nvtx.rs` now delegates to the `nvtx` crate, which compiles the header-only NVTX v3 shim into the binary. No shared library at build or run time; Nsight attaches through its injection library at launch as before. The macros and the `DYN_ENABLE_RUST_NVTX` gate are unchanged. The crate is already a workspace dependency through kvbm.
- `nvtx` is added to the maturin feature lists in the wheel builder. With the feature compiled in and the gate unset, each range site costs one relaxed atomic load.
- `dynamo.common.utils.nvtx_utils` degrades to its existing no-op path with a warning when `DYN_NVTX=1` is set but the `nvtx` Python package is absent, instead of failing at import. Profiling presets set that variable on every launch, and not every serving image ships the package.

## Validation

- `cargo check` and `cargo clippy -D warnings` on `dynamo-runtime` with and without the `nvtx` feature; `cargo check -p dynamo-llm --features nvtx`; `cargo fmt --check`.
- A runtime wheel built with the feature on a CUDA 13 aarch64 SGLang image: `ldd` on `_core.abi3.so` shows no `libnvToolsExt` dependency, the NVTX symbols are present, `from dynamo.runtime import DistributedRuntime` and the `dynamo.frontend` and `dynamo.sglang` entry points work.
- Serving with that wheel under `DYN_ENABLE_RUST_NVTX=1`: the frontend and workers log "NVTX annotations enabled", and the frontend's Nsight report contains the `preprocess.*`, `route.*`, `transport.*`, and `detokenize` ranges.
- `nvtx_utils` import guard checked by importing the module with the package made unavailable.
- `pre-commit` passes on every changed file.

## Known limitations

- The CI multi-arch wheel builds are the remaining check for the x86 and CUDA 12 variants.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Profiling now continues without NVTX when the optional profiling library is unavailable, with a warning logged.
* **Improvements**
  * NVTX profiling is enabled in runtime builds, and thread naming uses the updated NVTX integration.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->

## 评论 (1)

### coderabbitai[bot] · 2026-09-23

<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- review_stack_entry_start -->

<a href="https://app.coderabbit.ai/change-stack/ai-dynamo/dynamo/pull/15242"><img src="https://storage.googleapis.com/coderabbit_public_assets/review-stack-in-coderabbit-ui-dark.svg?v=2" alt="Review in Change Stack →" width="220" height="32"></a>

Navigate logical layers of code changes, visualize relationships, and explore their blast radius.

<!-- review_stack_entry_end -->
<!-- walkthrough_start -->

## Walkthrough

The Rust runtime now uses the `nvtx` crate for NVTX calls, and runtime wheel builds enable the `nvtx` feature. When Python cannot import `nvtx`, it logs a warning and uses the existing no-op path.

### Changes

**NVTX integration**

|Layer / File(s)|Summary|
|---|---|
|**Rust NVTX backend** <br> `lib/runtime/Cargo.toml`, `lib/runtime/src/nvtx.rs`|The runtime’s `nvtx` feature now uses the optional `nvtx` crate. Range operations and thread naming use its macros. The documentation removes the `libnvToolsExt.so` runtime requirement.|
|**Runtime wheel feature enablement** <br> `container/templates/wheel_builder.Dockerfile`|The SGLang, media-FFmpeg, and standard runtime wheel builds enable the `nvtx` feature.|
|**Python NVTX import fallback** <br> `components/src/dynamo/common/utils/nvtx_utils.py`|If importing `nvtx` fails while `DYN_NVTX` is enabled, the code logs a warning, disables NVTX, and uses the existing no-op path.|

<!-- change_assessment_start -->
**Priority:** ➖ Normal

**Estimated code review effort:** 2 (Simple) | ~10 minutes

<!-- change_assessment_commit:"790c6aa3532699c6e3d990537d15ac36fba4348a" -->

<!-- change_assessment_end -->

<!-- walkthrough_end -->
<!-- final_review_risk_start -->
**Merge Risk:** _🔵 Low_ · up to `790c6`
<!-- final_review_risk_coverage:{"sourceCommitId":"790c6aa3532699c6e3d990537d15ac36fba4348a","coveredCommitId":"790c6aa3532699c6e3d990537d15ac36fba4348a","kind":"reviewed"} -->

A broken NVTX installation can silently disable requested profiling. The issue is localized and has a straightforward fix; it warrants owner awareness or follow-up before merge.
<!-- final_review_risk_end -->
<!-- pre_merge_checks_walkthrough_start -->

<details>
<summary>🚥 Pre-merge checks | ✅ 4 | ❌ 1</summary>

### ❌ Failed checks (1 warning)

|     Check name    | Status     | Explanation                                                                                                                                                                                               | Resolution                                                                                                                                                                                                                  |
| :---------------: | :--------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Description check | ⚠️ Warning | The description provides detailed scope, implementation details, validation results, and known limitations. It does not follow the required template because it omits the required Related Issues sectio… | Add the required Related Issues section and select either a real issue reference or the confirmed no-related-issue option. Add a Where should the reviewer start? section that names the primary files or areas for review. |

<details>
<summary>✅ Passed checks (4 passed)</summary>

|         Check name         | Status   | Explanation                                                                                                                                                                                               |
| :------------------------: | :------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|         Title check        | ✅ Passed | The title clearly identifies the main change: compiling NVTX ranges into runtime wheels with the header-only nvtx crate.                                                                                  |
|     Docstring Coverage     | ✅ Passed | Docstring coverage is 80.00% which is sufficient. The required threshold is 80.00%. Docstring coverage is scoped to functions touched by this diff. Analyzed 5 functions across 2 files. (2 skipped: 2 u… |
|     Linked Issues check    | ✅ Passed | Check skipped because no linked issues were found for this pull request.                                                                                                                                  |
| Out of Scope Changes check | ✅ Passed | Check skipped because no linked issues were found for this pull request.                                                                                                                                  |

</details>

<details>
<summary>Full details: Description check</summary>

**Explanation**

The description provides detailed scope, implementation details, validation results, and known limitations. It does not follow the required template because it omits the required Related Issues section and does not identify where the reviewer should start.

</details>

</details>

<!-- pre_merge_checks_walkthrough_end -->

- [ ] <!-- {"checkboxId":"585bb3f6-faf5-4dbf-96d2-74e382adf19a"} --> Fix all pre-merge checks with AI
<!-- finishing_touch_checkbox_start -->

<details>
<summary>✨ Finishing Touches 💡 1</summary>

<!-- finishing_touch_suggestion:fix_ci -->
<details open>
<summary>🛠️ Fix failing CI checks 💡</summary>

- [ ] <!-- {"checkboxId": "9f0d24fb-b419-4f01-baf0-8b26b6424f34", "radioGroupId": "fix-ci-output-choice-group-unknown_comment_id"} --> Commit to this branch
- [ ] <!-- {"checkboxId": "6d21cfe8-ec3f-40e2-9222-b8318b64d3b0", "radioGroupId": "fix-ci-output-choice-group-unknown_comment_id"} --> Create a new PR

</details>

</details>

<!-- finishing_touch_checkbox_end -->
<!-- tips_start -->

---




<sub>Comment `@coderabbitai help` to get the list of available commands.</sub>

<!-- tips_end -->
