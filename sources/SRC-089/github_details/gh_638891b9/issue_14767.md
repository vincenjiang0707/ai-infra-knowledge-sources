# [Issue #14767] DEP: Make PowerAgent pod readiness reflect cap enforcement

source: https://github.com/ai-dynamo/dynamo/issues/14767
state: open | updated: 2026-09-23T16:09:08Z
labels: dynamo-deploy, deployment::k8s, needs-triage, operator, dep:under-review, k8s

## 正文

Baselined on `main` at [`81fa669fcb`](https://github.com/ai-dynamo/dynamo/commit/81fa669fcb456054b3aee6557e6e80ced6783a2e). Every behavioral claim and code reference below was read from that tree. This DEP has **no prerequisite** — the PowerAgent DaemonSet, its chart, `POWER_ANNOTATION_KEY`, the reconcile loop, and the actuator contract it modifies are all already on `main`.

## Summary

Make a PowerAgent pod's Kubernetes readiness mean that the agent is enforcing power caps.

Today a PowerAgent pod has no readiness probe, so it is Ready from the moment its container starts
and stays Ready while every reconcile cycle fails. This DEP adds three things and nothing else
(§1–§4 below):

1. the actuator reports whether its writes actually succeeded, and `reconcile_once()` folds that
   into a **whole-cycle enforcement boolean**;
2. a fixed `GET /readyz` on container port `8081`, serving that state; and
3. a `readinessProbe` on the PowerAgent DaemonSet.

No operator changes, no CRD or status changes, no Planner changes, no chart changes beyond the
probe and port. The claim published is narrow and self-reported:

> The agent's last reconcile cycle completed every enforcement action it was required to take with
> no reported failure, and completed recently.

A GPU with no running workload is intentionally skipped, so the claim covers the actions the cycle
was required to take, not the live state of every GPU on the node.

## Motivation

PowerAgent's failure handling is designed around *not* dropping caps: a failed write, an
unconfirmed GPU identity, a failed PID discovery, a failed release, a failed pod list, and a failed
device-count refresh all skip work and continue, preserving last-known-good state rather than
exiting. That is the right runtime behavior and this DEP does not change it. The cost is that none
of it is observable as *state*: the only external signal is `apply_failures_total`, a counter, which
says a failure happened — not whether the agent is currently enforcing anything.

The gap is wider than a metrics inconvenience. `apply_cap()` returns the intended post-clamp value
even when the NVML/DCGM write fails, and its sole caller
([`power_agent.py#L1648`](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1641-L1648))
discards the return value. So the loop never learns the outcome of its own writes; there is no
in-process notion of "this cycle enforced" to expose. This DEP creates that notion, then exposes it.

Today, therefore, `kubectl get pods` shows `1/1 Running` for an agent that has enforced nothing
since startup, and `kubectl rollout status` reports success for a rollout of non-functional agents.
Pod readiness is the standard place for "this instance is doing its job" and is already plumbed
everywhere; populating it truthfully is the smallest change that closes the gap.

## Current code fit

- `run()` has no per-cycle result and no watchdog, and sleeps `RECONCILE_INTERVAL_S` (15s) *after*
  each cycle returns — so the cycle period is the interval plus the cycle's own duration.
- `reconcile_once()` returns `None`. A failed pod list aborts the cycle early by design; otherwise
  every GPU is reconciled inside its own `try`.
- A GPU with no running processes returns early from `_reconcile_gpu` before any attribution or
  write, so idle GPUs are untouched.
- Actuator-init and GPU-discovery failures already abort in `PowerAgent.__init__`, producing
  `CrashLoopBackOff`.
- The image already installs `prometheus-client` and runs Python 3.12. The metrics server can be
  disabled with `--prometheus-port=0`, so readiness must not be served from it.
- The chart's DaemonSet has no probes of any kind today.

## Proposal

### 1. Return a whole-cycle enforcement boolean

`reconcile_once()` returns `True` iff every enforcement action the cycle was required to take
completed, and the inputs it depended on were obtained. Every path that currently swallows a failure
sets it false: a failed cap write, a cap skipped on unconfirmed GPU identity or mid-write
re-enumeration, a failed PID discovery, a failed release on a disowned GPU, a failed pod list, a
failed device-count refresh.

**No enforcement failure may be silently absorbed.** One boolean is the whole contract because
nothing branches on *which* class; attribution stays in logs and `apply_failures_total`. Two things
are outside it. The persistence-only retries in `_flush_pending_retirements` and
`_flush_pending_acquisitions` are excluded: each retries a durable record whose hardware write and
in-memory ownership already succeeded, so live enforcement is correct and only a *restarted* agent
would notice. And a cgroup read that yields no pod UID is indistinguishable from a non-Kubernetes
process, so it is not a failure the cycle can observe at all (see Risks).

**A cycle short-circuited by `_shutdown` returns the result accumulated so far**, at both the fast
path at the top of `reconcile_once` and the mid-loop break. It is not special-cased to `True`. The
GPUs the short-circuit skipped were not actions the cycle was required to take, so a shutdown that
interrupts an otherwise clean cycle still returns `True` and does not flip a pod NotReady during its
own termination grace period. But a shutdown landing after an earlier GPU already failed must not
discard that failure: an unconditional `True` would publish a fresh successful timestamp at the exact
moment `_shutdown_cleanup` starts restoring default caps, so `/readyz` would claim enforcement while
enforcement is being torn down. Returning the fold costs nothing and needs no special case.

**`apply_cap()` returning normally is not success.** Both actuators catch `NVMLError`/`DCGMError`,
increment `apply_failures_total`, and return the intended post-clamp int anyway — the documented
contract is explicitly "regardless of whether the underlying write succeeded." This cannot be fixed
at the call site by inspecting the return value; it requires changing the actuator's result contract
so the write outcome reaches the caller. The same holds for `restore_default()`, which already
returns `False` for an intentional skip that leaves a cap live.

Idle GPUs are never written and contribute nothing either way. A failing cycle must not exit the
process: a NotReady agent that keeps reconciling can recover, while a restart drops in-memory GPU
ownership.

### 2. Readiness rule

Readiness is one monotonic timestamp, `_last_good_cycle`, initialized to `0.0` before the server
thread starts and assigned once per cycle thereafter: the current `time.monotonic()` when the cycle
succeeded, `0.0` when it failed or raised. Ready iff it is non-zero and its age is under:

```python
STALE_AFTER_S = 3 * RECONCILE_INTERVAL_S   # 45s at the default interval
```

One value carries the whole state — `0.0` covers both "no cycle has ever succeeded" and "the last
one failed", and the age covers "the last one hung". Because it is a single assignment computed
after the `try`, a reader can never see a torn mix, an exception cannot leave a stale *successful*
timestamp in place, and no lock is required.

The staleness bound catches a hung cycle, which nothing else can: `run()` has no watchdog, NVML and
DCGM calls are unbounded, and a blocked cycle keeps serving whatever the last one published — the
probe's `failureThreshold` cannot see that, because nothing is failing, it is simply not advancing.
The bound restarts nothing.

The multiplier of 3 is what makes that safe. Since `run()` sleeps *after* each cycle, the timestamp
refreshes every `RECONCILE_INTERVAL_S` plus the cycle's own duration, so a bound of
`3 × RECONCILE_INTERVAL_S` tolerates a healthy cycle lasting up to `2 × RECONCILE_INTERVAL_S` —
about 30s at the default. A cycle that exceeds that exposes a `503` for the overshoot, and a node
whose cycles routinely run long enough for three consecutive polls to land inside that window —
through a throttled pod LIST or a slow DCGM reconnect on a many-GPU host — will go NotReady with
nothing failing, and wants a longer `RECONCILE_INTERVAL_S` rather than a looser bound. A one-off
cycle that overruns by a few seconds only shows a brief `503`.

**No agent-side failure counter.** `failureThreshold: 3` is the sole debounce. It counts *polls*,
not cycles, and
`run()` sleeps after each cycle, so a failed result stays published for `RECONCILE_INTERVAL_S` plus
the next cycle's duration. Three consecutive failing polls at `periodSeconds: 10` span 20s, and the
first of them lands 0–10s after the `503` window opens, so the pod goes NotReady 20–30s into that
window. At a 15s interval a single failed cycle therefore takes the pod NotReady once the following
cycle takes roughly 5–15s, depending on probe phase. The honest contract is that a brief failure is
**usually** absorbed, not guaranteed to be; it depends on elapsed time and probe phase. Accepted
rather than fixed — a counter would duplicate a debounce Kubernetes already provides, and the cost is
a bounded spurious NotReady that self-heals on the next successful cycle plus one poll.

Note that the `STALE_AFTER_S` bound and `failureThreshold` compose rather than overlap: the bound
decides when `/readyz` starts returning `503`, and the threshold decides how long a `503` must
persist before the pod is marked NotReady. For a hung cycle the two add — 45s of staleness plus
20–30s of failing polls — so the pod is NotReady roughly 65–75s after the last successful cycle.

### 3. `GET /readyz` on port `8081`

A `wsgiref.simple_server` WSGI app on a daemon thread, at fixed port `8081` and path `/readyz`.
`200` when §2 holds, `503` otherwise, with the age in the body so a `503` is diagnosable without log
access. It takes a `handler_class` whose `log_message` is a no-op — `WSGIRequestHandler` inherits
`BaseHTTPRequestHandler`'s, which would write a line to stderr on every poll, roughly 8,600 a day
per node at `periodSeconds: 10`.

Started in `run()` **before** its `try`, so a bind failure propagates and the agent never starts.
Placing it inside would run the full `_shutdown_cleanup` restore sweep on the way out of a failure
that never enforced anything. `CrashLoopBackOff` with a bind error is strictly better than a
`Running`/`NotReady` process with no path to recovery.

Fixed rather than configurable, and deliberately not on the Prometheus server, which
`--prometheus-port=0` can disable — readiness must not be optional.

### 4. `readinessProbe` on the DaemonSet

```yaml
readinessProbe:
  httpGet:
    path: /readyz
    port: 8081
  initialDelaySeconds: 15
  periodSeconds: 10
  failureThreshold: 3
```

**No liveness probe.** Defining an irrecoverably stuck agent wrongly restarts agents about to
recover, and a restart is strictly worse than a NotReady agent that keeps reconciling.

**This couples agent health to DaemonSet rollout progress**, the one behavioral change for existing
installs. An already-NotReady agent consumes the rollout budget under `rollingUpdate.maxUnavailable:
1` and can stall a `helm upgrade` that previously completed. That is correct — an upgrade rolling
over a node whose agent is not enforcing should not be reported as progress — but it must be in the
release notes. The update strategy itself is unchanged.

## Deployment and failure behavior

The kubelet does not probe for the first `initialDelaySeconds: 15`; the delay does not add to the
first cycle, so a pod whose first cycle has already succeeded goes Ready on the first probe.
Existing workers are never killed, evicted, or gated by this, and nothing outside the pod's own
readiness reads the new state.

- **Transient write failure:** usually absorbed by the probe debounce and visible in
  `apply_failures_total`; see §2 for when it is not.
- **Sustained failure, or an unexpected exception:** the timestamp resets to `0.0`, so `/readyz`
  returns `503` from the next poll and the pod is NotReady 20–30s later, after three failing polls.
  The loop keeps running and recovers without a restart.
- **Hung cycle:** the 45s bound trips, then three failing polls mark the pod NotReady — roughly
  65–75s after the last successful cycle. A healthy cycle running longer than ~30s exposes a `503`
  for the overshoot and only reaches NotReady if the overshoot covers three consecutive polls; see
  §2.
- **Pod terminating:** a cycle short-circuited by `_shutdown` returns whatever it had accumulated, so
  a clean cycle keeps readiness steady through the grace period while a cycle that had already failed
  still reports that failure.
- **Actuator-init or GPU-discovery failure:** unchanged — fatal in `__init__`, not modeled by the
  probe.
- **Port `8081` already bound:** fatal at startup.
- **`--prometheus-port=0`:** `/readyz` is unaffected.

## Explicit non-goals

This proposal does not:

- prove that a requested cap was applied to a particular pod or GPU;
- prevent a worker from starting before its local PowerAgent reconciliation;
- terminate or evict workers when PowerAgent fails;
- add a liveness probe, restart-policy change, or update-strategy change;
- publish per-node Leases, digests, GPU UUID sets, or per-pod enforcement records;
- change any operator, CRD, DGD status, or Planner behavior; or
- assert anything about nodes that have no PowerAgent pod at all.

## Alternatives considered

- **Leave readiness implicit and alert on `apply_failures_total`.** A counter cannot express
  "currently not enforcing", cannot be consumed by Kubernetes, requires every operator to build the
  same alert, and cannot see the failure classes that never reach it.

## Requirements

- **Loop contract.** `reconcile_once()` returns one boolean covering failed writes, identity skips,
  PID discovery, releases, pod list, and device-count refresh, with no
  class silently absorbed and both persistence-only retry queues excluded. The per-GPU fold must not
  short-circuit, and a `_shutdown` short-circuit returns the fold so far rather than an unconditional
  `True`. A failing cycle never exits the process.
- **Write contract.** `apply_cap()` and `restore_default()` report whether the write took, and the
  loop consumes that. No post-write readback. GPUs the cycle did not write are not asserted about.
- **Readiness state.** One monotonic timestamp, assigned once per cycle after the `try`; `0.0` on
  failure or exception. Ready iff non-zero and younger than `3 * RECONCILE_INTERVAL_S`.
- **Endpoint.** Fixed `GET /readyz` on `8081`, independent of the Prometheus server, `200`/`503`,
  age in the body, no per-request access logging, bind failure fatal.
- **Chart.** `readinessProbe` with `failureThreshold: 3` as the sole debounce, `periodSeconds: 10`,
  `initialDelaySeconds: 15`; container port added; no liveness probe; update strategy unchanged.

## Implementation plan

1. Change the apply contract so the write outcome reaches the loop. On the NVML path this means
   plumbing it out of module-level `_apply_cap`, which today catches `NVMLError`, increments
   `apply_failures_total`, and returns `None`; `NvmlActuator.apply_cap` only re-derives the
   post-clamp value from constraints. Do the same for `DcgmActuator.apply_cap` and
   `restore_default()`.
2. Return the whole-cycle boolean from `reconcile_once()`, folding per-GPU results without
   short-circuiting and leaving both persistence-only retry queues out.
3. Track `_last_good_cycle` in `run()` and serve it from a `wsgiref` app on `/readyz:8081`.
4. Add the probe and container port to the DaemonSet.
5. Note the rollout-coupling change in the chart README and release notes.

## Test strategy

- `/readyz` is `503` before the first successful cycle, `200` immediately after, and `503` once the
  staleness bound elapses with no completed cycle.
- Each swallowed failure path drives the boolean false; each queued persistence-only retry leaves it
  true.
- An `apply_cap()` whose underlying NVML/DCGM write raised drives the boolean false, even though the
  call returns a plausible int — the regression test for "clean return is not success".
- A `restore_default()` returning `False` for an intentional skip fails the cycle.
- A failure on GPU 0 marks the cycle false **and** GPUs 1…N are still reconciled. A pod-list failure
  still aborts before the loop and marks it false.
- An idle GPU is never written and does not affect the boolean.
- A GPU falling back to `safe_default_watts` on a missing or invalid annotation still yields a
  successful cycle once that default is written.
- An unexpected exception from `reconcile_once()` resets the timestamp, flips `/readyz` to `503` on
  the next poll rather than waiting out the staleness bound, and does not terminate the process.
- A cycle short-circuited by `_shutdown`, at either the fast path or the mid-loop break, returns the
  accumulated result: `True` when nothing had failed, and `False` when a GPU had already failed
  before the short-circuit — the regression test for "shutdown does not launder a failure".
- A healthy cycle lasting longer than `2 × RECONCILE_INTERVAL_S` makes `/readyz` return `503` —
  asserted so the budget in §2 is not silently changed by a future interval or bound edit.
- Stepping the wall clock does not extend or expire readiness.
- `/readyz` is served with `--prometheus-port=0`; binding an occupied `8081` is fatal; probe traffic
  emits no per-request log line.
- Helm: the DaemonSet renders the probe and port, and no liveness probe.

Deliberately **not** asserted: that one failing cycle cannot make the pod NotReady. Per §2 that is
not a property this design has, and a test claiming it would encode a false guarantee.

## Risks and limitations

- Readiness is the agent reporting its own write outcomes, not independent proof, and describes the
  *previous* cycle: a new worker may start before its cap is applied.
- An out-of-band override is corrected, not reported: every active GPU is rewritten every cycle.
- Readiness tracks the cap the agent *resolved*, not the one the user asked for.
  `_resolve_cap_for_gpu` falls back to `safe_default_watts` on a missing, unparseable, or contested
  annotation, and `apply_cap` clamps to the SKU's settable range — so a node enforcing safe defaults,
  or clamping a 5000W request to 700W, reports Ready. That is the right readiness answer; catching an
  unachievable request is admission's job.
- Detection latency is roughly 20–30s for a reported failure and 65–75s for a hung cycle, and the
  debounce is timing-dependent in both directions (§2), so readiness transitions are not a precise
  function of failure count. A node failing intermittently but never on consecutive cycles may retain
  readiness throughout; alert on `apply_failures_total`.
- The probe couples agent health to DaemonSet rollout progress and can stall a `helm upgrade`.
- Readiness asserts live enforcement, not durable recoverability: an agent whose persistence writes
  are failing stays Ready, risking the ownership record on an ungraceful exit.
- A node-wide `/proc` or permission problem would make cgroup attribution fail silently, since
  `_extract_pod_uid_from_cgroup` cannot distinguish an unreadable cgroup from a non-Kubernetes
  process. Left as-is: the container runs `privileged`, `runAsUser: 0`, `hostPID: true`, so this is
  not a failure mode this deployment has, and splitting the errno cases would add a flapping surface
  on every short-lived GPU process.

## Deferred

Correlating this readiness with DGD worker placement — detecting a power-annotated worker that
landed on a node with no operational PowerAgent, and feeding that into DGD convergence and Planner
scaling — is deferred to a follow-up DEP. This DEP is a prerequisite for it and is independently
useful without it.

## Code references

Pinned to `main` at `81fa669fcb456054b3aee6557e6e80ced6783a2e`.

**PowerAgent** — [`apply_cap` failure contract](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/actuator.py#L142-L178) · [`restore_default` skip contract](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/actuator.py#L182-L194) · [caller discards the return value](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1641-L1648) · [`_resolve_cap_for_gpu` safe-default fallback](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1167-L1195) · [`reconcile_once` returns `None` today](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1424) · [idle GPU returns early](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1596-L1597) · [pod-list failure aborts the cycle](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1452-L1469) · persistence-only retries ([acquisitions](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L339-L348), [retirements](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L582-L591)) · [shutdown short-circuits](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1441-L1443) · [module-level `_apply_cap` swallows `NVMLError`](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L501-L518) · [sleep after cycle](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1667-L1673) · [fatal init](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/power-agent/power_agent.py#L1296-L1315)

**Chart** — [DaemonSet, no probes today](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/helm/charts/power-agent/templates/daemonset.yaml#L94-L121) · [update strategy](https://github.com/ai-dynamo/dynamo/blob/81fa669fcb456054b3aee6557e6e80ced6783a2e/deploy/helm/charts/power-agent/values.yaml#L219-L222)



## 评论 (1)

### kaim-eng · 2026-09-23

# Design: PowerAgent readiness reflects cap enforcement

Implementation design for [DEP #14767](https://github.com/ai-dynamo/dynamo/issues/14767).
Read against the working tree at `main` (`6562e7d04b`); the DEP is baselined at `81fa669fcb`,
so line numbers below are the **current** tree, not the DEP's.

Audience: the engineer(s) implementing this. The DEP owns the *why* and the accepted
trade-offs; this document owns the *what to change*, file by file, and the decisions the
DEP left to implementation.

---

## 0. Scope

Four changes, in dependency order:

| # | Area | Files |
|---|------|-------|
| 1 | Write outcome reaches the loop | `deploy/power-agent/actuator.py` |
| 2 | Whole-cycle enforcement boolean | `deploy/power-agent/power_agent.py` |
| 3 | `_last_good_cycle` + `GET /readyz:8081` | `deploy/power-agent/power_agent.py` |
| 4 | `readinessProbe` + container port | `deploy/helm/charts/power-agent/templates/daemonset.yaml`, `Chart.yaml` |

Out of scope, per the DEP: no liveness probe, no operator/CRD/DGD-status/Planner change, no
update-strategy change, no post-write readback, no per-pod enforcement records.

### Drift between the DEP text and the current tree

Two points where an implementer following the DEP literally would write the wrong code:

- **The reconcile release path calls `restore_default_by_uuid`, not `restore_default`.**
  The DEP's Write-contract requirement names `restore_default()`. In the current tree
  `_release_managed_gpu` ([`power_agent.py:610`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L610))
  routes every runtime release through `actuator.restore_default_by_uuid(...)`
  ([`actuator.py:197`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L197)), which already returns
  `Optional[bool]` with the exact semantics the DEP wants (`False` = not conclusively
  restored, cap may still be live). **So `restore_default`/`restore_default_by_uuid` need no
  signature change at all.** The work on the release side is entirely in
  `_release_managed_gpu`, which currently returns `None` across nine explicit `return`s plus
  an implicit fall-through — ten distinct outcomes, tabulated in §2.1.
- **`NvmlActuator.restore_default` ([`actuator.py:448`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L448))
  does not catch NVML errors** — it raises. That is fine and unchanged; the caller's `try`
  classifies it.

---

## 1. Write outcome reaches the loop

### 1.1 The result type

Add to `actuator.py`, above the `Actuator` Protocol:

```python
class CapWriteResult(NamedTuple):
    """Outcome of a single cap write.

    `effective_w` keeps the existing contract exactly: the post-clamp value the
    agent INTENDED to apply, `max(min_w, min(watts, max_w))`, returned whether or
    not the underlying NVML/DCGM write landed. `ok` is the new information — False
    when the write raised, was skipped on an identity mismatch, or was otherwise
    not applied.
    """
    effective_w: int
    ok: bool
```

`apply_cap` returns `CapWriteResult` instead of `int` on the Protocol
([`actuator.py:142`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L142)) and both implementations.

A `NamedTuple` specifically, so every existing wattage consumer must be edited to read
`.effective_w` rather than silently continuing to work against a value that now means
something else. `NamedTuple` is not currently imported in `actuator.py`
([line 43](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L43)) — add it.

### 1.2 `NvmlActuator.apply_cap` ([`actuator.py:399`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L399))

The failure is swallowed one level down, in module-level
`power_agent._apply_cap` ([`power_agent.py:501`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L501)),
which catches `pynvml.NVMLError`, increments `apply_failures_total`, and returns `None`.

- Change `_apply_cap` to return `bool` — `True` after the `nvmlDeviceSetPowerManagementLimit`
  call and the ownership/metric bookkeeping complete, `False` in the `except NVMLError`
  branch. Keep the `except` as-is otherwise: still log, still increment
  `apply_failures_total`, still do not re-raise.
- `NvmlActuator.apply_cap` captures `ok = power_agent._apply_cap(...)` and wraps **both**
  returns of the post-clamp re-derivation block ([`actuator.py:437-446`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L437)):
  line 440 `return max(min_w, min(watts, max_w))` and line 446 `return watts` on the
  constraints-read `NVMLError`. Both become `CapWriteResult(<same value>, ok)`. What must stay
  **unchanged** is the block's *logic* — a pure constraints read with no second
  `_clamp_to_constraints` call, or the clamp warning and `cap_clamped_total` double-fire (that
  regression is already covered by `tests/test_apply_cap.py`). Note that the `NVMLError`
  branch is a failure of the *value re-derivation*, not of the write: `ok` still reflects
  what `_apply_cap` reported and must not be forced to `False` here.

`NvmlActuator.apply_cap` also has two **raising** exits that return no result at all: the
`RuntimeError` when `self._metrics is None` ([`actuator.py:424`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L424))
and `nvmlDeviceGetHandleByIndex` at line 433, which sits outside any `try`. `DcgmActuator.apply_cap`
can likewise raise `RuntimeError` from `_coerce_power_limit_watts` by deliberate design. These are
not `CapWriteResult` cases — they propagate to `reconcile_once`'s per-GPU `except`, which §2.3
folds to `False`. Called out here so a reviewer can see they are covered rather than missed.

`_apply_cap` is module-level and called from tests directly; changing `-> None` to `-> bool`
is source-compatible for anything that ignores the return.

### 1.3 `DcgmActuator.apply_cap` ([`actuator.py:1204`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L1204))

**Five exits today, not three**, and only one of them performed the Set. Every early return
below leaves the GPU unwritten, so every one is `ok=False`:

| Exit | Returns today | New `ok` |
|---|---|---|
| Constraints read raised `dcgm_structs.DCGMError` (~line 1291) — no clamp, no Set | `watts`, unclamped | `False` |
| Entry-time identity was unreadable, so `expected_uuid is None` (~line 1306) | `effective_w` | `False` |
| `constraints_uuid != expected_uuid` (~line 1312) — SKU range came from a different physical GPU, so `effective_w` is clamped against the wrong limits; no Set | `effective_w` | `False` |
| `_GpuIdentityMismatch` from `_apply_cap_inner` | `effective_w` | `False` |
| `dcgm_structs.DCGMError` from `_apply_cap_inner` | `effective_w` | `False` |
| Normal return of `self._apply_cap_inner(...)` | written value | `True` |

The first and third are easy to miss: they sit above the `try` that wraps `_apply_cap_inner`,
they already increment `apply_failures_total`, and they return a plausible-looking wattage.
Keep each return's *value* exactly as it is today — including the unclamped `watts` on the
constraints-read failure, which mirrors `NvmlActuator.apply_cap`'s fallback — and change only
the `ok` flag.

`_apply_cap_inner` ([`actuator.py:1380`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/actuator.py#L1380)) keeps its
`-> int` signature and its raise-on-failure behaviour — the restore/orphan/SIGTERM callers
depend on that and are not in scope. Only the outer `apply_cap` wrapper gains the boolean.
Do **not** widen the `except dcgm_structs.DCGMError` clause while you are in there; the
narrowness is deliberate (an `AttributeError` from the pydcgm bindings must still surface).

### 1.4 Call sites to update

`apply_cap`'s consumers fall into **three classes**, and each needs a different edit.

**Class A — the production call site.** `_reconcile_gpu`
([`power_agent.py:1648`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L1648)). Reads `result.ok`; see §2.2.

**Class B — test doubles that must now PRODUCE a `CapWriteResult`.** Most actuator fixtures
are a bare `MagicMock()`: `tests/test_reconcile_wiring.py` (~20 of them),
`tests/test_annotation_scope.py:235`, `tests/test_multi_dgd_topology.py:133`. A `MagicMock`'s
`apply_cap` returns a `MagicMock`, so once `_reconcile_gpu` ends in `return result.ok`,
**`reconcile_once()` returns a truthy `MagicMock`, not `True`.** A new `assertIs(..., True)`
fails; a new `assertTrue(...)` passes while asserting nothing. **This is the only class that
fails silently**, which is why it needs the discipline stated as a rule: every double
standing in for an `Actuator` must set `actuator.apply_cap.return_value =
CapWriteResult(effective_w, ok)` rather than being left bare.

**Class C — consumers that must now READ `.effective_w`.** These break loudly, but only if
someone runs them, and the real-hardware one is not part of the default suite:

| Consumer | What it does today | Edit |
|---|---|---|
| [`tests/test_actuator_protocol.py:238`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/tests/test_actuator_protocol.py#L238) and its clamp siblings | `self.assertEqual(result, 300)` | `result.effective_w`; add `assertTrue(result.ok)` |
| [`tests/test_dcgm_actuator.py:1200`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/tests/test_dcgm_actuator.py#L1200) and siblings | `self.assertEqual(result, 300)` | same |
| [`tests/e2e_actuator_parity.py:194`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/tests/e2e_actuator_parity.py#L194) | stores the raw return as `apply_cap_returned` | store `.effective_w` |
| [`tests/e2e_actuator_parity.py:278`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/tests/e2e_actuator_parity.py#L278) | `abs(nvml[...] - dcgm[...]) > tolerance_w` | unchanged once the stored value is an int again |
| [`tests/test_parity_harness.py:38,52`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/tests/test_parity_harness.py#L38) | carries `apply_cap_returned` through the comparison harness under test | keep it an int, matching the script |

`e2e_actuator_parity.py` is the one to be careful with: it is a **real-hardware parity
script**, not a double. It calls the live actuator and subtracts the NVML and DCGM returns, so
handing it a `CapWriteResult` raises `TypeError: unsupported operand type(s)` at line 278 — on
a GPU node, during a manual parity run, long after CI went green. Fix it in the same PR.

**Migration checklist.** Grep `apply_cap` (not `.apply_cap(`) across `tests/` — it appears in
ten files. Then, per file: does it *produce* a return (Class B: give it a `CapWriteResult`) or
*consume* one (Class C: read `.effective_w`)? Suites that call `reconcile_once` without
touching `apply_cap` (`test_reconcile_failsafe.py`, `test_shutdown.py`,
`test_pending_persistence.py`, `test_multi_pod_policy.py`) are unaffected today, but any new
assertion there on the cycle boolean puts them in Class B.

---

## 2. Whole-cycle enforcement boolean

### 2.1 `_release_managed_gpu` → `bool`

Change the signature to `-> bool`, where `True` means "nothing this call was required to do
was left undone". Every existing `return` gets a value:

| Exit in [`_release_managed_gpu`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L610) | Result | Reason |
|---|---|---|
| `actuator.get_uuid()` raised | `False` | Release was required and did not happen |
| `expected_uuid != uuid` (re-enumeration between snapshot and check) | `False` | Release deferred; a cap may still be stranded |
| Not in `_managed_gpu_indices` and UUID not in `_previously_managed` | `True` | Not ours — no action was required |
| `managed_uuid_for_idx` raised | `False` | Fail-closed deferral of a required release |
| Occupant branch: `restore_default_by_uuid` returned `False` | `False` | Cap not conclusively released |
| Occupant branch: returned `True`/`None`, `_commit_release` ran | `True` | Done |
| Stale-projection skip (occupant not managed by us) | `False` | **See note below** |
| `restore_default_by_uuid` raised | `False` | Release failed |
| Main branch: returned `False` | `False` | Cap may still be live |
| Main branch: `True`/`None`, `_commit_release` ran | `True` | Done |

**The stale-projection skip is the one judgment call in this table, and it is `False`.** The
GPU loop only moves forward, so a re-enumeration that lands the displaced managed GPU on an
already-passed index leaves it unassessed this cycle; `True` would publish a fresh successful
timestamp for a cycle that skipped a managed GPU. When the displaced GPU lands on a
not-yet-reached index this is a false negative, which the next cycle clears.

Note the branch's precondition when writing its test (§5): the occupant of the skipped index
must be a GPU we have **never** capped. An occupant already in `_previously_managed` takes the
UUID-addressed release at [`power_agent.py:728`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L728)
instead, and an index that is neither in `_managed_gpu_indices` nor carrying a managed UUID
returns at the "not ours" guard above.

**Add an explicit `return True` at the success fall-through**
([`power_agent.py:821`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L821), after `_commit_release`),
which today falls off the end of the function implicitly. Same for `_reconcile_gpu` after the
`apply_cap` call at line 1648.

### 2.2 `_reconcile_gpu` → `bool`

| Exit in [`_reconcile_gpu`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L1507) | Result |
|---|---|
| `get_uuid` raised (identity anchor unreadable) | `False` |
| `list_running_pids` raised `_GpuIdentityMismatch` | `False` |
| `not pids` (idle GPU) | `True` — never written, contributes nothing |
| `not pod_annotations` | whatever `_release_managed_gpu` returned |
| Reached `apply_cap` | `result.ok` — or `False` via `reconcile_once`'s per-GPU `except` if it raised (§1.2) |

`_resolve_cap_for_gpu`'s fallback to `safe_default_watts` on a missing/unparseable/contested
annotation is **not** a failure: once that resolved cap is written the cycle is successful.
The DEP is explicit that readiness tracks the cap the agent resolved, not the one the user
asked for.

### 2.3 `reconcile_once` → `bool`

```python
def reconcile_once(self) -> bool:
    ok = True
    if _shutdown.is_set():
        ...
        return ok                      # fast path: returns the (empty) fold, not a literal

    _flush_pending_retirements()       # persistence-only: excluded from the fold
    _flush_pending_acquisitions()      # persistence-only: excluded from the fold

    pods = self._list_pods_on_node()
    if pods is None:
        ...
        return False                   # was: bare return

    uid_to_annotation = self._build_uid_to_annotation(pods)

    try:
        self.device_count = self._actuator.device_count()
    except Exception as e:
        ...
        ok = False                     # was: warn and continue on the stale count

    for gpu_idx in range(self.device_count):
        if _shutdown.is_set():
            ...
            break                      # falls through to `return ok` — the fold so far
        try:
            ok = self._reconcile_gpu(gpu_idx, uid_to_annotation) and ok
        except Exception as e:
            logger.error("Reconcile failed for GPU %d: %s", gpu_idx, e)
            ok = False

    return ok
```

Three things the implementation must get right, each with a test in §5:

- **`ok = self._reconcile_gpu(...) and ok`, not `ok = ok and self._reconcile_gpu(...)`.**
  The second form short-circuits: once a GPU fails, no later GPU is reconciled. The fold must
  never short-circuit — GPUs 1…N are still reconciled after GPU 0 fails.
- **Both `_shutdown` exits return the accumulated fold**, never a literal `True`. An
  unconditional `True` would publish a fresh successful timestamp at the moment
  `_shutdown_cleanup` begins restoring default caps, so `/readyz` would claim enforcement
  while enforcement was being torn down.
- **Both persistence-only flushes stay out of the fold.** Each retries a durable record whose
  hardware write and in-memory ownership already succeeded; live enforcement is correct and
  only a *restarted* agent would notice. Their failures already log and are visible in the
  `_pending_*` set sizes.

A cgroup read that yields no pod UID is not observable as a failure — it is indistinguishable
from a non-Kubernetes process — and is deliberately not folded in. The DEP accepts this in
Risks; the container runs `privileged` / `runAsUser: 0` / `hostPID: true`, so a node-wide
`/proc` failure is not a mode this deployment has.

---

## 3. Readiness state and `/readyz`

### 3.1 Where the code goes

**In `power_agent.py`, not a new module.** A fourth file forces every dev ConfigMap recipe to
change (`dev-pod.yaml` header plus `NOTES.txt` ×2) and hard-breaks existing dev pods with
`ModuleNotFoundError`; the chart carries that scar twice already, for `actuator.py` (v1.1.0)
and `managed_state.py` (v1.3.0), for ~50 lines of gain.

Note that `power_agent.py` is module `__main__` under the entrypoint while `actuator.py`
reaches it via `import power_agent` — two distinct module objects. Keep `_last_good_cycle`
an **instance attribute on `PowerAgent`**, not a module global, so the dual-module split
cannot produce two copies. Nothing in `actuator.py` needs to read it.

### 3.2 State

```python
STALE_AFTER_S = 3 * RECONCILE_INTERVAL_S   # 45s at the default 15s interval
READYZ_PORT = 8081
```

`power_agent.py` currently imports `argparse, json, logging, os, re, signal, threading`
([lines 36-43](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/power_agent.py#L36)) — **add `import time` and the
`wsgiref.simple_server` / `wsgiref.handlers` imports.** Neither is present today.

`PowerAgent.__init__` sets `self._last_good_cycle: float = 0.0`. `run()` assigns it exactly
once per cycle, **after** the `try`, from a local:

```python
while not _shutdown.is_set():
    try:
        cycle_ok = self.reconcile_once()
    except Exception as e:
        logger.exception("Unexpected error in reconcile loop: %s", e)
        cycle_ok = False
    self._last_good_cycle = time.monotonic() if cycle_ok else 0.0
    _shutdown.wait(timeout=RECONCILE_INTERVAL_S)
```

One float, one assignment. A reader on the server thread cannot see a torn mix, an exception
cannot leave a stale *successful* timestamp in place, and no lock is needed (a float
attribute read/write is a single bytecode under the GIL build this image ships — CPython
3.12, `library/python:3.12-slim-bookworm`).

`0.0` is unambiguous as "no cycle has ever succeeded or the last one failed":
`time.monotonic()` is `CLOCK_MONOTONIC`, seconds since boot, and cannot realistically be
exactly `0.0` at the moment a cycle completes.

Readiness predicate:

```python
def _readiness(self) -> tuple[bool, Optional[float]]:
    """Returns (ready, age_seconds). `None` age means no cycle has ever succeeded."""
    last = self._last_good_cycle
    if last == 0.0:
        return False, None
    age = time.monotonic() - last
    return age < STALE_AFTER_S, age
```


### 3.3 The server

`wsgiref.simple_server.make_server("", READYZ_PORT, app, handler_class=_QuietWSGIHandler)`
on a daemon thread.

- `_QuietWSGIHandler(WSGIRequestHandler)` overrides `log_message` to a no-op.
  `WSGIRequestHandler` inherits `BaseHTTPRequestHandler.log_message`, which writes a line to
  stderr per request — ~8,600 lines/node/day at `periodSeconds: 10`.
- Exact response contract — §5 asserts on the body, so this is not left to taste:

  ```python
  def _readyz_app(self, environ, start_response):
      if environ["PATH_INFO"] != "/readyz":
          start_response("404 Not Found", [("Content-Type", "text/plain")])
          return [b""]
      ready, age = self._readiness()
      age_s = "none" if age is None else f"{age:.1f}"
      body = f"{'ok' if ready else 'not-ready'} last_good_cycle_age_s={age_s}\n"
      status = "200 OK" if ready else "503 Service Unavailable"
      start_response(status, [("Content-Type", "text/plain; charset=utf-8")])
      return [body.encode("utf-8")]
  ```

  The age in the body is what makes a `503` diagnosable with `kubectl exec ... curl` and no
  log access.
- **Started in `run()` before its `try`.** A bind failure must propagate out of `run()`
  uncaught so the process exits and the pod enters `CrashLoopBackOff`. Starting it inside
  the `try` would send a never-enforcing agent through the full `_shutdown_cleanup` restore
  sweep on the way out.
- Fixed port and path — deliberately **not** on the Prometheus server, which
  `--prometheus-port=0` disables. Readiness must not be optional. No new CLI flag, no new
  chart value.
- Daemon thread, so it does not hold the process open during shutdown. Readiness keeps
  serving through the termination grace period, which is what §2's shutdown-fold rule is for.

The pod does not use `hostNetwork`, so `8081` is pod-local and cannot collide with anything
on the node.

---

## 4. Chart

In [`daemonset.yaml`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/helm/charts/power-agent/templates/daemonset.yaml#L118), extend
the existing `ports:` block and add the probe to the same container:

```yaml
          ports:
            - name: metrics
              containerPort: {{ .Values.agent.prometheusPort }}
              protocol: TCP
            - name: readyz
              containerPort: 8081
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: readyz
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
```

- **Hard-coded, not templated through `values.yaml`.** The port is fixed in the agent with
  no flag; a chart value that could disagree with it is a footgun, and a configurable
  `failureThreshold` would let an operator dial the debounce until readiness means nothing.
- **No liveness probe**, no `startupProbe`, `updateStrategy` untouched.
- `dev-pod.yaml` gets no probe. It is a single-pod dev iteration surface; the server still
  binds and is reachable by `kubectl port-forward`.

### 4.1 Version bump

**Bump `version: 1.3.1` → `1.4.0` and `appVersion: "1.2.0"` → `"1.3.0"`.** The `/readyz`
server is new runtime code in the image, so the rule stated in the
[`Chart.yaml` version comment](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/helm/charts/power-agent/Chart.yaml#L43) applies. Record
there and in NOTES.txt that **chart ≥1.4.0 requires image ≥v1.3.0** — an older image has no
`/readyz`, so every pod is permanently NotReady and the rollout stalls at the first node.

One line in the `values.yaml` comment at
[line 50](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/helm/charts/power-agent/values.yaml#L50), next to `prometheusPort`:
`# Must not be 8081 — the readiness server binds that port and a collision is fatal at startup.`
No render-time validator: §3.3 already decided a failed bind should be fatal and visible, and
a sixth `validate*` helper plus its helm suite is not worth an operator's self-inflicted typo.
### 4.2 The rollout-coupling note

The one behavioural change for existing installs, and DEP implementation-plan item 5. The DEP
says "the chart README" — **there is no `README.md` under `deploy/helm/charts/power-agent/`**,
so it goes in NOTES.txt and the `daemonset.updateStrategy` comment block in `values.yaml`
(noted here so a reviewer does not flag it as a missed DEP item). Release notes are GitHub
Releases on `ai-dynamo/dynamo`, not a repo file — add the same text to the release body.

Paste-ready:

> Power Agent pods now carry a readiness probe tied to cap enforcement. An agent that is not
> enforcing goes NotReady, which consumes the DaemonSet rollout budget under
> `rollingUpdate.maxUnavailable: 1` and can stall a `helm upgrade` that previously completed.
> This is intended — an upgrade rolling over a node whose agent is not enforcing should not be
> reported as progress. The update strategy itself is unchanged. Chart ≥1.4.0 requires agent
> image ≥v1.3.0.

---

## 5. Test plan

Python tests live in `deploy/power-agent/tests/` (`pytest.ini` sets `pythonpath=.`) and run
in the Docker **test stage** at [`Dockerfile:140`](https://github.com/ai-dynamo/dynamo/blob/6562e7d04b04ad177459f8a5b720718de71e1a6a/deploy/power-agent/Dockerfile#L140) — that
stage is `FROM runtime AS test` and is not referenced by `--target runtime`, so a failure here
breaks the CI `--target test` build, not the pushed image. Helm tests are `helm unittest` under
`deploy/helm/charts/power-agent/tests/`.

Before writing any of these: work §1.4's three-class migration checklist first. Assertions on
the cycle boolean are meaningless against a bare `MagicMock` actuator, and the Class C
consumers — including the real-hardware `e2e_actuator_parity.py` — must be migrated to
`.effective_w` in the same PR, not left to fail on the next manual parity run.

**New file `tests/test_readiness.py`** — endpoint and timestamp:

- `503` before the first successful cycle; `200` immediately after one; `503` once
  `STALE_AFTER_S` elapses with no completed cycle.
- An unexpected exception from `reconcile_once` resets the timestamp to `0.0`, so `/readyz`
  is `503` on the *next* poll rather than after the staleness bound, and the process does
  not terminate.
- A healthy cycle lasting longer than `2 * RECONCILE_INTERVAL_S` yields `503` — asserted so
  the §2 budget cannot be silently changed by a future interval or bound edit.
- Stepping the wall clock does not extend or expire readiness (monkeypatch `time.time`,
  not `time.monotonic`).
- `/readyz` serves with `--prometheus-port=0`.
- Binding an already-occupied `8081` is fatal at startup: pre-bind the wildcard `("", 8081)`
  — not loopback, or the collision is kernel-dependent — and assert the exception escapes
  `run()` and `_shutdown_cleanup` did **not** run.
- Probe traffic emits no per-request log line (capture stderr across N requests).

**Extend `tests/test_reconcile_failsafe.py`** — the cycle fold:

- Each swallowed failure path drives the boolean false: failed cap write, unconfirmed GPU
  identity, `_GpuIdentityMismatch` on the PID snapshot, failed `managed_uuid_for_idx`,
  `restore_default_by_uuid` returning `False` or raising, failed pod list, failed
  device-count refresh.
- A failure on GPU 0 marks the cycle false **and** GPUs 1…N are still reconciled — the
  no-short-circuit regression test.
- A pod-list failure still aborts before the GPU loop and marks the cycle false.
- An idle GPU is never written and does not affect the boolean.
- A GPU falling back to `safe_default_watts` on a missing or invalid annotation still yields
  a successful cycle once that default is written.
- The stale-projection skip returns `False`. Unit-test `_release_managed_gpu` directly: the
  index is in `_managed_gpu_indices`, `managed_uuid_for_idx` returns a different UUID than the
  occupant, and the occupant's UUID is in **neither** managed set — otherwise the call takes
  the UUID-addressed release at line 728 and the test asserts nothing about the skip. A
  full re-enumeration-ordering fixture is not worth building for a constant return.

**Extend `tests/test_apply_cap.py` / `tests/test_dcgm_actuator.py`** — the write contract:

- An `apply_cap` whose underlying NVML write raised returns `ok=False` *and* a plausible
  `effective_w` — the regression test for "a clean return is not success".
- All five DCGM non-write exits from §1.3 return `ok=False`, each asserted separately:
  constraints-read `DCGMError` (and that it still returns the unclamped `watts`),
  unreadable entry identity, `constraints_uuid != expected_uuid`, `_GpuIdentityMismatch`
  from `_apply_cap_inner`, and `DCGMError` from `_apply_cap_inner`. The first and third are
  assert in each that no `dcgmConfigSet` was issued, so the test pins "no Set happened" rather
  than just the flag.

**Extend `tests/test_pending_persistence.py`**:

- A queued persistence-only retry (acquisition or retirement) leaves the cycle boolean
  `True`.

**Extend `tests/test_shutdown.py`**:

- **Fast path** (`_shutdown` already set on entry): returns `True`. Only `True` is reachable
  here — no GPU has been touched, so the fold is still at its initial value. The DEP's
  "`True` *and* `False`" phrasing conflates the two short-circuits; do not write an
  unsatisfiable assertion.
- **Mid-loop break**: returns the accumulated result — `True` when nothing had failed before
  the break, `False` when a GPU had already failed. This is the actual regression test for
  "shutdown does not launder a failure", and the `False` half is reachable only here.

**New `deploy/helm/charts/power-agent/tests/readiness_probe_test.yaml`** (helm unittest —
note this `tests/` is the chart's, not `deploy/power-agent/tests/` used above; the
`*_test.yaml` suffix matches the five existing suites):

- The DaemonSet renders the probe with the DEP's `initialDelaySeconds` / `periodSeconds` /
  `failureThreshold`, renders the `readyz` container port, and renders **no** `livenessProbe`.
  (The probe's `port` is the name `readyz`, not the DEP's literal `8081` — see §4.)

**Deliberately not asserted**, per the DEP: that one failing cycle cannot make the pod
NotReady. That is not a property this design has — `failureThreshold: 3` counts polls, not
cycles — and a test claiming it would encode a false guarantee.

---

## 6. Suggested PR split

Reviewable in two, or one if the reviewer prefers:

1. **`fix(power-agent): report cap-write outcomes to the reconcile loop`** — §1 and §2. No
   observable behaviour change on its own: the boolean is computed and discarded. This is
   the half that needs careful review, and it is where every regression risk lives.
2. **`feat(power-agent): gate pod readiness on cap enforcement`** — §3 and §4, plus the
   chart version bump and release note.

Conventional Commit titles, `Summary` + `Validation` in each description, `git commit -s`.
Review routing is the operator area
(`@ai-dynamo/dynamo-operator-codeowners`); confirm with
`python .github/codeowners/who_owns.py --codeowners CODEOWNERS --changed`.

---

## 7. Decisions this document made that the DEP left open

Flagging these explicitly so the reviewer can overrule them rather than discover them:

1. `CapWriteResult` NamedTuple as the new `apply_cap` return type (§1.1).
2. The stale-projection skip in `_release_managed_gpu` counts as **failure** (§2.1), rather
   than adding per-cycle reconciled-UUID bookkeeping to distinguish the safe sub-case.
3. The readiness server lives in `power_agent.py` rather than a new module (§3.1).
4. Probe values are hard-coded in the template, not exposed as chart values (§4).
5. Chart bump to `1.4.0` **and** `appVersion` to `"1.3.0"`, with a documented chart ≥1.4.0 →
   image ≥v1.3.0 floor (§4.1).

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)

