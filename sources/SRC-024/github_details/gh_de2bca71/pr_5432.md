# [PR #5432] [Triton/Gluon] [Config] [GFX950] Unified Attention configs tuning

source: https://github.com/ROCm/aiter/pull/5432
state: open | updated: 2026-09-24T14:06:23Z
labels: Triton/Gluon, Config

## 正文

Unified Attention 2d config tuning on GFX950.

Get speedup geomean 1.237x overall, 1.380x on the 12 prefill rows

Bench data tested with `bench_unified_attention.py`
  |  row |    B | HQ/HK | N_CTX_Q | before |  after | speedup |
  | ---: | ---: | ----: | ------: | -----: | -----: | ------: |
  |    0 |    1 |    16 |       1 |   3.15 |   3.15 |   1.00× |
  |    1 |    1 |    16 |    1024 |  57.98 | 102.25 |   1.76× |
  |    2 |    1 |    16 |    4096 | 253.63 | 341.71 |   1.35× |
  |    3 |    1 |    48 |       1 |   3.90 |   3.88 |   1.00× |
  |    4 |    1 |    48 |    1024 | 286.41 | 316.22 |   1.10× |
  |    5 |    1 |    48 |    4096 |  89.48 | 218.41 |   2.44× |
  |    6 |    4 |    16 |       1 |   2.99 |   2.95 |   0.99× |
  |    7 |    4 |    16 |    1024 |  81.05 | 169.74 |   2.09× |
  |    8 |    4 |    16 |    4096 | 476.96 | 553.67 |   1.16× |
  |    9 |    4 |    48 |       1 |   4.71 |   4.63 |   0.98× |
  |   10 |    4 |    48 |    1024 | 445.93 | 511.65 |   1.15× |
  |   11 |    4 |    48 |    4096 | 455.22 | 480.64 |   1.06× |
  |   12 |    8 |    16 |       1 |   2.94 |   2.93 |   1.00× |
  |   13 |    8 |    16 |    1024 | 213.48 | 400.35 |   1.88× |
  |   14 |    8 |    16 |    4096 | 456.08 | 517.39 |   1.13× |
  |   15 |    8 |    48 |       1 |   4.34 |   4.37 |   1.01× |
  |   16 |    8 |    48 |    1024 | 446.61 | 495.90 |   1.11× |
  |   17 |    8 |    48 |    4096 | 508.26 | 542.39 |   1.07× |


cc: @Dewei-Wang-sh 

## 评论 (1)

### github-actions[bot] · 2026-09-11

### 🏷️ CI Guide

**Runs automatically on every PR:**
- ✅ Pre-checks (submodule verification, code formatting)
- ✅ Aiter op tests (gfx942 + gfx950)
- ✅ Triton tests on MI35X (only when `aiter/ops/triton/**` or related paths are changed)

**Extended tests (opt-in via labels):**

| Label | Tests |
|-------|-------|
| `ci:gfx1250-ffm-triton` | Run the five-shard gfx1250 FFM Triton test suite |
| `ci:triton-300x` | Run an additional Triton test job on MI300X in PRs; main branch always runs both MI35X and MI300X |
| `multigpu` | Aiter multi-GPU tests on the 8-GPU runner |
| `ci:sglang` | SGLang integration tests: DeepSeek-R1-MXFP4 accuracy, Qwen 3.5 accuracy |
| `ci:atom` | ATOM benchmark: DeepSeek-R1-0528, GPT-OSS-120B |
| `ci:atom_full` | ATOM accuracy suite for PR and main models from ATOM `models_accuracy.json` |
| `ci:vllm` | vLLM benchmark: GPT-OSS-120B, DeepSeek-R1-0528, Kimi-K2.5 |
| `ci:all` | All standard extended tests (excludes `ci:atom_full`) |

> Only add `ci:atom_full` for FlyDSL or Triton upgrades.
> Add labels via the sidebar or `gh pr edit 5432 --add-label <label>`

**PR title tags & labels:**
Component tags (`[Triton/Gluon]`, `[HIP]`, `[CK]`, `[ASM]`, ...) are added to the PR title and as PR labels automatically from the changed files and re-synced on every push — change-type tags like `[fix]`/`[Perf]`, op tags like `[MLA]`, and human labels (`ci:*`) are left untouched. Add the `no-auto-title` label to opt this PR out.

## Review (3)

### copilot-pull-request-reviewer[bot] · 2026-09-11 · COMMENTED

### 🔵 Needs a closer look

Moderate issues remain around 2D coverage, shuffled-case testing, and short-prefill validation.

<details>
<summary>Pull request overview</summary>

Tunes GFX950 Unified Attention configuration selection for improved prefill performance.

**Changes:**
- Adds query-length-aware `attn_3d` tuning.
- Adds a `Q_GEQ_2` specialization and related attention branches.
</details>

<details>
<summary>File summaries</summary>

| File | Summary |
|---|---|
| `aiter/ops/triton/configs/gfx950/triton/attention/unified_attention/DEFAULT.json` | Adds GFX950 attention configuration tuning; coverage and validation gaps remain. |
</details>

<details>
<summary>Review details</summary>

### Suppressed comments (3)

**aiter/ops/triton/configs/gfx950/triton/attention/unified_attention/DEFAULT.json:12**
* [inferred] The new axis and `Q_GEQ_2` entry are added under `attn_3d`, while the PR description says this is 2D tuning. On gfx950, `use_2d_kernel()` routes sufficiently large prefill workloads to `attn_2d` before this table is consulted, so the added entries cannot tune the claimed 2D path (and the large prefill benchmark rows will continue using the existing `attn_2d` entries). **Author must** either add the tuning to the `attn_2d` table with the required tile parameters or update the PR scope and benchmark claim to 3D.
```
      "D",
      "Q"
```
**aiter/ops/triton/configs/gfx950/triton/attention/unified_attention/DEFAULT.json:150**
* [verified] This new `D_LEQ_128.Q_GEQ_256.SHUF` branch is reachable for gfx950 Triton 2D prefill, but `test_unified_attention.py` explicitly skips every shuffled case when the Gluon 2D path is unavailable, and `bench_unified_attention.py` never enables `shuffled_kv_cache`. A bad tile/staging choice here would therefore pass the current suite and benchmark unnoticed. **Author must** add a gfx950 Triton test or a focused config/dispatch test that exercises this branch.
```
    "D_LEQ_128.Q_GEQ_256.SHUF": {
      "BLOCK_M": 128,
      "num_warps": 4,
      "num_stages": 1,
      "waves_per_eu": 2,
      "TILE_SIZE_MIN": 64,
      "TILE_SIZE_MAX": 64
```
**aiter/ops/triton/configs/gfx950/triton/attention/unified_attention/DEFAULT.json:197**
* [verified] `Q_GEQ_2` routes every 3D call with `max_seqlen_q >= 2` to `BLOCK_M=128`, but the supplied benchmark only covers Q=1024/4096. For a concrete short-prefill case (D=128, HQ=HK=16, Q=2, K=8192), the 3D path uses `BLOCK_Q=128`, so 126 of 128 query rows are masked; the previous `any` entry used `BLOCK_M=16`. This broad threshold can regress Q=2..255 latency. **Author must** benchmark the short-prefill range or restrict this entry to a measured range.
```
    "Q_GEQ_2": {
      "BLOCK_M": 128,
      "num_warps": 4,
      "num_stages": 2,
      "waves_per_eu": 1
```

- **Files reviewed:** 1/1 changed files
- **Comments generated:** 0
- **Review effort level:** Lite
</details>

---

💡 <a href="/ROCm/aiter/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

### copilot-pull-request-reviewer[bot] · 2026-09-11 · COMMENTED

### 🔵 Needs a closer look

Moderate issues remain in short-query behavior and unmeasured 3D dispatch coverage.

<details>
<summary>Review details</summary>

### Suppressed comments (2)

**aiter/ops/triton/configs/gfx950/triton/attention/unified_attention/DEFAULT.json:197**
* [verified] `Q_GEQ_2` now selects `BLOCK_M=128` for every non-decode query length >=2, whereas the existing fallback uses `BLOCK_M=16`. For example, `(B=1, HQ=HK=16, max_seqlen_q=2, max_seqlen_k=8192, D=128)` remains on the 3D path (`num_2d_prgms=16` is below the dispatch target), and the 3D kernel masks 126 of its 128 query rows while still doing the tile work. This regresses short prefill/partial-decode requests that are not represented by the benchmark. **Author must** add a small-Q tier or provide tuning evidence for this range.
```
    "Q_GEQ_2": {
      "BLOCK_M": 128,
      "num_warps": 4,
      "num_stages": 2,
      "waves_per_eu": 1
```
**aiter/ops/triton/configs/gfx950/triton/attention/unified_attention/DEFAULT.json:197**
* [verified] This hunk also changes the 3D dispatch: with `attn_3d` now keyed by `Q`, this `Q_GEQ_2` leaf is selected for every gfx950 3D call with `max_seqlen_q >= 2`, changing `BLOCK_M`/warps from the `any` leaf. The PR description and benchmark table cover 2D tuning and provide no 3D Q>=2 evidence, so this is an unmeasured runtime change outside the stated scope. **Author must** split the 3D tuning into a follow-up or add explicit gfx950 3D Q>=2 benchmark/correctness coverage and document it.
```
    "Q_GEQ_2": {
      "BLOCK_M": 128,
      "num_warps": 4,
      "num_stages": 2,
      "waves_per_eu": 1
```

- **Files reviewed:** 1/1 changed files
- **Comments generated:** 0 new
- **Review effort level:** Lite
</details>

### copilot-pull-request-reviewer[bot] · 2026-09-14 · COMMENTED

### 🔵 Needs a closer look

Add GFX950 3D test coverage for `max_seqlen_k > 512` and `max_seqlen_q > 1`, including Q=2 and Q=256 boundaries.

<details>
<summary>Review details</summary>

### Suppressed comments (1)

**aiter/ops/triton/configs/gfx950/triton/attention/unified_attention/DEFAULT.json:197**
* This new `attn_3d` branch is not exercised by the current attention tests: the dedicated 3D parametrization only uses `max_query_len == 1`, while the other parametrizations have `max_seqlen_k <= 512` and are routed to the 2D kernel. Please add a gfx950 3D case with `max_seqlen_k > 512` and `max_seqlen_q > 1` (ideally covering the Q=2 and Q=256 boundaries) so this config is compiled and correctness-checked.
```
    "Q_GEQ_2": {
      "BLOCK_M": 128,
      "num_warps": 4,
      "num_stages": 2,
      "waves_per_eu": 1
```

- **Files reviewed:** 1/1 changed files
- **Comments generated:** 0 new
- **Review effort level:** Lite
</details>
