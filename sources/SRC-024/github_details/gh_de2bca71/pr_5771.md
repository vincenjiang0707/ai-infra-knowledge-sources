# [PR #5771] [Triton/Gluon] Assert SPLIT_UNMASKED_LOOP is not used with sliding window

source: https://github.com/ROCm/aiter/pull/5771
state: open | updated: 2026-09-24T20:30:11Z
labels: Triton/Gluon

## 正文

Per #5741.

## 评论 (6)

### github-actions[bot] · 2026-09-22

### 🏷️ CI Guide

**Runs automatically on every PR:**
- ✅ Pre-checks (submodule verification, code formatting)
- ✅ Aiter op tests (gfx942 + gfx950)
- ✅ Triton tests on MI35X (only when `aiter/ops/triton/**` or related paths are changed)

**Extended tests (opt-in via labels):**

| Label | Tests |
|-------|-------|
| `ci:gfx1250-ffm-triton` | Run the five-shard gfx1250 FFM Triton test suite |
| `ci:triton-300x` | Run an additional Triton test job on MI300X in PRs; main branch always runs both MI35X and MI300X |
| `multigpu` | Aiter multi-GPU tests on the 8-GPU runner |
| `ci:sglang` | SGLang integration tests: DeepSeek-R1-MXFP4 accuracy, Qwen 3.5 accuracy |
| `ci:atom` | ATOM benchmark: DeepSeek-R1-0528, GPT-OSS-120B |
| `ci:atom_full` | ATOM accuracy suite for PR and main models from ATOM `models_accuracy.json` |
| `ci:vllm` | vLLM benchmark: GPT-OSS-120B, DeepSeek-R1-0528, Kimi-K2.5 |
| `ci:all` | All standard extended tests (excludes `ci:atom_full`) |

> Only add `ci:atom_full` for FlyDSL or Triton upgrades.
> Add labels via the sidebar or `gh pr edit 5771 --add-label <label>`

**PR title tags & labels:**
Component tags (`[Triton/Gluon]`, `[HIP]`, `[CK]`, `[ASM]`, ...) are added to the PR title and as PR labels automatically from the changed files and re-synced on every push — change-type tags like `[fix]`/`[Perf]`, op tags like `[MLA]`, and human labels (`ci:*`) are left untouched. Add the `no-auto-title` label to opt this PR out.

### amd-xavierwang · 2026-09-22

@cagrikymk @vorapolsiloai Initial proposal created.

### cagrikymk · 2026-09-22

Btw, can we also extend the tests to larger head size in this PR? Please keep the test runtime in mind. This was the main issue why the bug went unnoticed when the split logic got introduced.

### amd-xavierwang · 2026-09-22

> Btw, can we also extend the tests to larger head size in this PR? Please keep the test runtime in mind. This was the main issue why the bug went unnoticed when the split logic got introduced.

Do you mean for [decode/test_triton_unified_attn_3d](https://github.com/ROCm/aiter/blob/52ffe895e64acc993a14b6af04a588fc5a448d56/op_tests/triton_tests/attention/test_unified_attention.py#L369)? I think #4761 only changed attn_2d/prefill and it covers head_size to 512 with sliding_window=256 option. Only `test_triton_unified_attn_3d` doesn't have large head_size nor sliding-window: https://github.com/ROCm/aiter/blob/52ffe895e64acc993a14b6af04a588fc5a448d56/op_tests/triton_tests/attention/test_unified_attention.py#L379 https://github.com/ROCm/aiter/blob/52ffe895e64acc993a14b6af04a588fc5a448d56/op_tests/triton_tests/attention/test_unified_attention.py#L380

### cagrikymk · 2026-09-23

```bash
> > Btw, can we also extend the tests to larger head size in this PR? Please keep the test runtime in mind. This was the main issue why the bug went unnoticed when the split logic got introduced.
> 
> Do you mean for [decode/test_triton_unified_attn_3d](https://github.com/ROCm/aiter/blob/52ffe895e64acc993a14b6af04a588fc5a448d56/op_tests/triton_tests/attention/test_unified_attention.py#L369)? I think #4761 only changed attn_2d/prefill and it covers head_size to 512 with sliding_window=256 option. Only `test_triton_unified_attn_3d` doesn't have large head_size nor sliding-window:
> 
> https://github.com/ROCm/aiter/blob/52ffe895e64acc993a14b6af04a588fc5a448d56/op_tests/triton_tests/attention/test_unified_attention.py#L379
> 
> https://github.com/ROCm/aiter/blob/52ffe895e64acc993a14b6af04a588fc5a448d56/op_tests/triton_tests/attention/test_unified_attention.py#L380
```

Oh I missed that.I thought [this ](https://github.com/ROCm/aiter/issues/5741) was pointing at existing bug that gets triggered with current configs so I wanted to extend the tests to trigger it in CI, if it is the case.

I assume we have no config that executes the split path, so we are good.

### amd-xavierwang · 2026-09-24

Comment fixed! so far no bugs in existing configs and this assertion will catch future bugs.

## Review (2)

### copilot-pull-request-reviewer[bot] · 2026-09-22 · COMMENTED

<!-- ccr-overview-v2 -->

## Copilot review overview

### 🔵 Needs a closer look

Add a regression test forcing the incompatible configuration and verifying compilation fails.

**Review effort:** Lite  
**Findings:** None

<details>
<summary><strong>What changed in this PR</strong></summary>

Adds a Triton compile-time guard preventing `SPLIT_UNMASKED_LOOP` from being used with sliding-window attention.

**Changes:**
- Adds a static assertion rejecting the unsupported configuration.
- Provides a clear compilation failure message.

| File | Summary |
|---|---|
| `aiter/​ops/​triton/​_triton_kernels/​attention/​unified_attention.py` | Adds the sliding-window compatibility guard; a focused regression test is still requested. |
</details>

---

💡 <a href="/ROCm/aiter/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

### cagrikymk · 2026-09-23 · COMMENTED

Nitpick: Can we address this? I think we dont have to be that verbose here.

Besides that LGTM!
