source: https://docs.openvino.ai/2025/api/genai_api/_autosummary/openvino_genai.SchedulerConfig.html
lastmod: 

# openvino_genai.SchedulerConfig[#](https://docs.openvino.ai#openvino-genai-schedulerconfig)

-
*class*openvino_genai.SchedulerConfig[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig) Bases:

`pybind11_object`

SchedulerConfig to construct ContinuousBatchingPipeline

Parameters: max_num_batched_tokens: a maximum number of tokens to batch (in contrast to max_batch_size which combines

independent sequences, we consider total amount of tokens in a batch).

num_kv_blocks: total number of KV blocks available to scheduler logic. cache_size: total size of KV cache in GB. block_size: block size for KV cache. dynamic_split_fuse: whether to split prompt / generate to different scheduling phases.

vLLM-like settings: max_num_seqs: max number of scheduled sequences (you can think of it as “max batch size”). enable_prefix_caching: Enable caching of KV-blocks.

When turned on all previously calculated KV-caches are kept in memory for future usages. KV-caches can be overridden if KV-cache limit is reached, but blocks are not released. This results in more RAM usage, maximum RAM usage is determined by cache_size or num_kv_blocks parameters. When turned off only KV-cache required for batch calculation is kept in memory and when a sequence has finished generation its cache is released.

use_cache_eviction: Whether to use cache eviction during generation. cache_eviction_config Cache eviction configuration struct. use_sparse_attention Whether to use sparse attention during prefill. sparse_attention_config Sparse attention configuration struct.

-
__init__(
*self:*) None[openvino_genai.py_openvino_genai.SchedulerConfig](https://docs.openvino.ai#openvino_genai.SchedulerConfig)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__init__)

Methods

(name, /)`__delattr__`

Implement delattr(self, name).

()`__dir__`

Default dir() implementation.

(value, /)`__eq__`

Return self==value.

(format_spec, /)`__format__`

Default object formatter.

(value, /)`__ge__`

Return self>=value.

(name, /)`__getattribute__`

Return getattr(self, name).

Helper for pickle.

(value, /)`__gt__`

Return self>value.

()`__hash__`

Return hash(self).

(self)`__init__`

This method is called when a class is subclassed.

(value, /)`__le__`

Return self<=value.

(value, /)`__lt__`

Return self<value.

(value, /)`__ne__`

Return self!=value.

(**kwargs)`__new__`

Helper for pickle.

(protocol, /)`__reduce_ex__`

Helper for pickle.

()`__repr__`

Return repr(self).

(name, value, /)`__setattr__`

Implement setattr(self, name, value).

Size of object in memory, in bytes.

()`__str__`

Return str(self).

Abstract classes can override this to customize issubclass().

(self)`to_string`

Attributes

-
__annotations__
*= {}*[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__annotations__)

-
__class__
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__class__) alias of

`pybind11_type`


-
__delattr__(
*name*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__delattr__) Implement delattr(self, name).


-
__dir__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__dir__) Default dir() implementation.


-
__eq__(
*value*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__eq__) Return self==value.


-
__format__(
*format_spec*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__format__) Default object formatter.

Return str(self) if format_spec is empty. Raise TypeError otherwise.


-
__ge__(
*value*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__ge__) Return self>=value.


-
__getattribute__(
*name*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__getattribute__) Return getattr(self, name).


-
__getstate__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__getstate__) Helper for pickle.


-
__gt__(
*value*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__gt__) Return self>value.


-
__hash__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__hash__) Return hash(self).


-
__init__(
*self:*) None[openvino_genai.py_openvino_genai.SchedulerConfig](https://docs.openvino.ai#openvino_genai.SchedulerConfig)[#](https://docs.openvino.ai#id0)

-
__init_subclass__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__init_subclass__) This method is called when a class is subclassed.

The default implementation does nothing. It may be overridden to extend subclasses.


-
__le__(
*value*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__le__) Return self<=value.


-
__lt__(
*value*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__lt__) Return self<value.


-
__ne__(
*value*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__ne__) Return self!=value.


-
__new__(
***kwargs*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__new__)

-
__reduce__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__reduce__) Helper for pickle.


-
__reduce_ex__(
*protocol*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__reduce_ex__) Helper for pickle.


-
__repr__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__repr__) Return repr(self).


-
__setattr__(
*name*,*value*,*/*)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__setattr__) Implement setattr(self, name, value).


-
__sizeof__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__sizeof__) Size of object in memory, in bytes.


-
__str__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__str__) Return str(self).


-
__subclasshook__()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.__subclasshook__) Abstract classes can override this to customize issubclass().

This is invoked early on by abc.ABCMeta.__subclasscheck__(). It should return True, False or NotImplemented. If it returns NotImplemented, the normal algorithm is used. Otherwise, it overrides the normal algorithm (and the outcome is cached).


-
_pybind11_conduit_v1_()
[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig._pybind11_conduit_v1_)

-
*property*cache_eviction_config[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.cache_eviction_config)

-
*property*cache_size[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.cache_size)

-
*property*dynamic_split_fuse[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.dynamic_split_fuse)

-
*property*enable_prefix_caching[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.enable_prefix_caching)

-
*property*max_num_batched_tokens[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.max_num_batched_tokens)

-
*property*max_num_seqs[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.max_num_seqs)

-
*property*num_kv_blocks[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.num_kv_blocks)

-
*property*sparse_attention_config[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.sparse_attention_config)

-
to_string(
*self:*) str[openvino_genai.py_openvino_genai.SchedulerConfig](https://docs.openvino.ai#openvino_genai.SchedulerConfig)[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.to_string)

-
*property*use_cache_eviction[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.use_cache_eviction)

-
*property*use_sparse_attention[#](https://docs.openvino.ai#openvino_genai.SchedulerConfig.use_sparse_attention)

-
__init__(