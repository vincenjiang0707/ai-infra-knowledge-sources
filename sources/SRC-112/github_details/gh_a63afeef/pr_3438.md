# [PR #3438] [Klaud Cold] Update qwen3.5-fp8-b200-sglang (+mtp) SGLang image to nightly-dev-cu13-20260925-8ca82118

source: https://github.com/SemiAnalysisAI/InferenceX/pull/3438
state: open | updated: 2026-09-25T18:27:18Z
labels: full-sweep-enabled

## 正文

## Summary
Update SGLang image from nightly-dev-cu13-20260918-20518d85 to nightly-dev-cu13-20260925-8ca82118 (latest nightly) for the Qwen3.5 8k1k recipes.

Recipes touched: `qwen3.5-fp8-b200-sglang`, `qwen3.5-fp8-b200-sglang-mtp`

Changes:
- `configs/nvidia-master.yaml`: image bump for both keys.
- `srt-slurm-recipes/qwen3.5/sglang/b200-fp8{,-mtp}/8k1k.yaml`: `container` bump.
- `perf-changelog.yaml`: new entry.

## Test plan
- [ ] full-sweep-enabled sweep passes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 评论 (1)

### github-actions[bot] · 2026-09-25

<!-- recipe-reminder -->
Thanks for the contribution!

- **Review:** If this PR changes files owned by someone other than a repository admin or `@SemiAnalysisAI/core`, ask one eligible [CODEOWNER](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/CODEOWNERS) to complete the latest [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md) before contacting a core maintainer on Slack. Follow the template exactly, including `As a PR reviewer and CODEOWNER, I have reviewed this and have`, so sign-off verification triggers.
- **PR verification:** Sweeps only run on labeled PRs. Add `full-sweep-fail-fast` (strongly recommended); use `full-sweep-enabled` only when matrix jobs should continue after a failure.
- **After merging:** PR authors must ensure all GitHub Actions jobs pass. Transient failures often pass on rerun; see [how to rerun failed jobs](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs#re-running-failed-jobs-in-a-workflow).

<details>
<summary>中文</summary>

感谢你的贡献！

- **审阅：**如果 PR 修改的文件归属于仓库管理员及 `@SemiAnalysisAI/core` 之外的 CODEOWNER，请先联系一位有资格的 [CODEOWNER](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/CODEOWNERS) 填写最新的 [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md)，再通过 Slack 联系核心维护者。必须严格遵循模板，并保留 `As a PR reviewer and CODEOWNER, I have reviewed this and have`，才能触发签核验证。
- **PR 验证：**扫描仅在带有标签的 PR 上运行。强烈建议添加 `full-sweep-fail-fast`；仅当需要矩阵任务在失败后继续运行时才使用 `full-sweep-enabled`。
- **合并后：**PR 作者必须确保所有 GitHub Actions 任务通过。临时性失败通常可以通过重新运行恢复；参见[重新运行失败任务的说明](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs#re-running-failed-jobs-in-a-workflow)。

</details>

## Review (1)

### claude[bot] · 2026-09-25 · COMMENTED

I reviewed this PR and did not find any code bugs: the image bump is applied consistently, with the same new tag used for `model.container` in both `b200-fp8/8k1k.yaml` and `b200-fp8-mtp/8k1k.yaml` and for the matching `image` field of both `qwen3.5-fp8-b200-sglang` and `qwen3.5-fp8-b200-sglang-mtp` keys in configs/nvidia-master.yaml, and a changelog entry is appended (append-only, tail-only). A human should still look before merge: the new perf-changelog.yaml entry's `pr-link` is the literal string `PRLINK_PLACEHOLDER`, which is not one of the placeholders (`PR_LINK_PLACEHOLDERS = {"XXX", ".../pull/XXX"}`) that `infx/workflows/validate_perf_changelog.py` / `prepare_perf_changelog_merge.py` recognize, so the reuse-merge automation documented in docs/ci-procedures.md (which "canonicalizes appended `XXX` links") would reject this entry until it is changed to `XXX` or the real PR link.


<details>
<summary>Extended reasoning...</summary>

Four-file config-only diff: two srt-slurm recipe YAMLs, configs/nvidia-master.yaml, and perf-changelog.yaml, all for a routine SGLang nightly image bump with no security-sensitive surface. Verified container/image tags are bumped identically and kept equal per the multi-node convention, and the changelog append preserves prior bytes. The one concrete issue found by re-deriving the merge-tooling logic (not just repeating the summary's note) is that `PRLINK_PLACEHOLDER` is outside the recognized placeholder set, which decided defer over approve since it affects the documented merge path rather than being a pure style nit.

</details>

This review covers commit cb57c17c7563dcf43b23606658994995ae948cdd, which is no longer the latest commit on this pull request; later commits are not covered by it.

<!-- bhrv:1014f9dd6d1b1438 -->
