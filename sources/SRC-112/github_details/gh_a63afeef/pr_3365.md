# [PR #3365] [AMD][Qwen3.5] Bump MI355X fixed-seq SGLang images to v0.5.20-rocm720-mi35x-20260924

source: https://github.com/SemiAnalysisAI/InferenceX/pull/3365
state: closed | updated: 2026-09-25T18:31:01Z
labels: full-sweep-fail-fast

## 正文

https://inferencex.semianalysis.com/inference?unofficialRun=36026478679
https://inferencex.semianalysis.com/evaluation?unofficialRun=36026478679

## Summary

- Bump `qwen3.5-fp4-mi355x-sglang` and `qwen3.5-fp4-mi355x-sglang-mtp` from `lmsysorg/sglang-rocm:v0.5.19-rocm720-mi35x-20260913` to `lmsysorg/sglang-rocm:v0.5.20-rocm720-mi35x-20260924` (Docker Hub tag HTTP 200, digest `sha256:baadaba198e23c46c1dd651b5edefae8000bd5430943bd10b246b563905b3ecf`).
- Search spaces, model (`amd/Qwen3.5-397B-A17B-MXFP4-AttnFP8-V2`), and recipes are unchanged.
- AgentX (`qwen3.5-fp4-mi355x-sglang-agentic-mtp`) is not in this PR.

## Details

Fixed-seq 8k/1k MI355X SGLang (non-MTP and MTP) still used the 20260913 image on `origin/main`. This PR only moves those two arms to the 20260924 daily. Previous unofficial-run links for the 20260922 pin no longer apply; a new sweep should run from `full-sweep-fail-fast`.

## AI model disclosure

Cursor Grok 4.6 prepared the image bump, changelog, and this PR text.


## 评论 (8)

### github-actions[bot] · 2026-09-22

<!-- recipe-reminder -->
Thanks for the contribution!

- **Review:** If this PR changes files owned by someone other than a repository admin or `@SemiAnalysisAI/core`, ask one eligible [CODEOWNER](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/CODEOWNERS) to complete the latest [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md) before contacting a core maintainer on Slack. Follow the template exactly, including `As a PR reviewer and CODEOWNER, I have reviewed this and have`, so sign-off verification triggers.
- **PR verification:** Sweeps only run on labeled PRs. Add `full-sweep-fail-fast` (strongly recommended); use `full-sweep-enabled` only when matrix jobs should continue after a failure.
- **After merging:** PR authors must ensure all GitHub Actions jobs pass. Transient failures often pass on rerun; see [how to rerun failed jobs](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs#re-running-failed-jobs-in-a-workflow).

<details>
<summary>中文</summary>

感谢你的贡献！

- **审阅：**如果 PR 修改的文件归属于仓库管理员及 `@SemiAnalysisAI/core` 之外的 CODEOWNER，请先联系一位有资格的 [CODEOWNER](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/CODEOWNERS) 填写最新的 [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md)，再通过 Slack 联系核心维护者。必须严格遵循模板，并保留 `As a PR reviewer and CODEOWNER, I have reviewed this and have`，才能触发签核验证。
- **PR 验证：**扫描仅在带有标签的 PR 上运行。强烈建议添加 `full-sweep-fail-fast`；仅当需要矩阵任务在失败后继续运行时才使用 `full-sweep-enabled`。
- **合并后：**PR 作者必须确保所有 GitHub Actions 任务通过。临时性失败通常可以通过重新运行恢复；参见[重新运行失败任务的说明](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs#re-running-failed-jobs-in-a-workflow)。

</details>

### github-actions[bot] · 2026-09-22

<!-- inferencex-unofficial-run-visualizer -->

View unofficial run (performance): https://inferencex.semianalysis.com/inference?unofficialRun=36026478679

View unofficial run (accuracy): https://inferencex.semianalysis.com/evaluation?unofficialRun=36026478679

### yichiche · 2026-09-22

<img width="542" height="344" alt="image" src="https://github.com/user-attachments/assets/6e606a18-231d-41da-b8b9-ddfc112cd0de" />

### 1am9trash · 2026-09-23

/reuse-sweep-run 35743948727

### github-actions[bot] · 2026-09-23

<!-- codeowner-signoff-verify signoff=pullrequestreview-5285944616 -->
## ❌❌❌ **REJECTED** ❌❌❌

@1am9trash — one blocker, unchanged from the previous verdict: this PR bumps the image under the MTP config `qwen3.5-fp4-mi355x-sglang-mtp`, so the sign-off's additional detail section must record the draft head's shipped and effective precision (checklist item 6 / Check 13). It still contains only the recipe link. Independent inspection shows the draft is served as shipped, so the fix is documentation only: edit the existing checklist review to add the draft-precision record and re-trigger verification. Assessed at the pinned tip `b0bc091a` ("Fix format"); the PR head has not moved.

❌ Check 13 (Draft runs as shipped): FAIL — Draft precision could not be verified from the sign-off: the additional detail section identifies no draft head, stored precision, pinned-image default handling, or effective serving precision for `qwen3.5-fp4-mi355x-sglang-mtp` (image-only bumps are in scope). Independently verified facts the reviewer can record: served checkpoint `amd/Qwen3.5-397B-A17B-MXFP4-AttnFP8-V2` (rev `e17e5f0e`) embeds a 1-layer MTP head (`mtp_num_hidden_layers: 1`) whose 1,553 `mtp.*` tensors are all stored BF16 in `model-00005-of-00005.safetensors` with no scale tensors; all 1,546 MTP linear modules (`mtp.fc`, q/k/v/o proj, `mlp.gate`, shared expert, all 512 experts) are in the Quark `exclude` list; SGLang v0.5.20 `qwen3_5_mtp.py::_mtp_quant_config` returns `None` for a Quark config with `mtp.*` excludes (draft built unquantized) and `quark.py::get_quant_method` returns `UnquantizedLinearMethod` for excluded layers; the [run's MTP server_args](https://github.com/SemiAnalysisAI/InferenceX/actions/runs/35743948727) show `quantization: None`, `speculative_draft_model_path: None`, `speculative_draft_model_quantization: None`, `speculative_draft_kv_cache_dtype: None` (draft KV inherits the target `fp8_e4m3`, allowed), `dtype: auto`; `qwen3.5_fp4_mi355x_mtp.sh` sets no draft-related env or flags. Effective draft precision = BF16 = baseline.

<details>
<summary>Passed and not applicable checks</summary>

✅ Check 0 (CODEOWNER): PASS — `@1am9trash` is a named owner of `configs/amd-master.yaml`; `perf-changelog.yaml` falls to the catch-all, covered by any listed CODEOWNER.

✅ Check 1 (Passing sweep in PR): PASS — in-PR commit `e78a79ee` carries [run 35743948727](https://github.com/SemiAnalysisAI/InferenceX/actions/runs/35743948727) with all 19 `single-node 8k1k /` and 4 `eval /` check-runs `success` (none skipped); `7bd28d19` and `b0bc091a` only have skip runs.

✅ Check 2 (Evals pass): PASS — `eval_results_all/agg_eval_all.json`: gsm8k em_strict 0.9659–0.9697 on 4 configs (TP2 non-MTP c64/c256, TP2 MTP c64/c128), all above the `infx/evals/thresholds.yaml` qwen3.5 bar of 0.94; run used `lmsysorg/sglang-rocm:v0.5.20-rocm720-mi35x-20260922`, matching both changed entries.

✅ Check 3 (Recipe linked/merged/complete): PASS — published [SGLang cookbook Qwen3.5](https://docs.sglang.io/cookbook/autoregressive/Qwen/Qwen3.5) MI355X MXFP4 section matches major args: model `amd/Qwen3.5-397B-A17B-MXFP4-AttnFP8-V2`, TP 2/4, `--attention-backend aiter`, `--kv-cache-dtype fp8_e4m3`, `--page-size 16`, MTP steps 3 / topk 1 / draft tokens 4 (`EAGLE` in the script is the target of the cookbook's `NEXTN` alias per `spec_registry._RESERVED_ALIASES`). Informational only: `ROCM_QUICK_REDUCE_QUANTIZATION=INT8` (higher precision than the cookbook's INT4 all-reduce), `--disable-radix-cache`, `--max-running-requests $CONC`, newer image tag.

✅ Check 4 (Reuse command): PASS — `/reuse-sweep-run 35743948727` posted as a whole-line comment by `1am9trash` (COLLABORATOR) on 2026-09-23.

✅ Check 5 (Latest checklist template): PASS — all 17 current `docs/PR_REVIEW_CHECKLIST.md` items present and checked, including the Pareto-coverage item.

✅ Check 6 (Upstream image / engine-first): PASS — both entries are `framework: sglang` with `lmsysorg/sglang-rocm:v0.5.20-rocm720-mi35x-20260922` (tag active on Docker Hub, pushed 2026-09-22, digest `7163e9dc…`); engine-first ordering N/A for an SGLang submission.

✅ Check 7 (No deprecated models/scenarios): PASS — `qwen3.5` fp4 Single-turn 8k1k is active in `MODELS.md` on 2026-09-23 (only bf16, 1k1k, 1k8k are retired for this model).

✅ Check 8 (No architecture hacks): PASS — image-only diff; effective scripts have no `--hf-overrides`/`--json-model-override-args` (server_args show `json_model_override_args: '{}'`).

✅ Check 9 (Spec-decode via chat template): PASS — `qwen3.5_fp4_mi355x_mtp.sh` runs the client with `--use-chat-template`.

✅ Check 10 (No engine patches): PASS — no `.patch`, `git apply`, `sed -i`, heredoc rewrites, or engine wheel installs in the diff or the two effective benchmark scripts.

➖ Check 11 (Agentic spec-decode golden AL): N/A — fixed-seq-len configs only; no agentic changes (`qwen3.5-fp4-mi355x-sglang-agentic-mtp` untouched).

➖ Check 12 (Append-only): N/A — the new `perf-changelog.yaml` entry has no `append-only: true`.

✅ Check 14 (Pareto coverage): PASS — from `results_bmk/agg_bmk.json` of [run 35743948727](https://github.com/SemiAnalysisAI/InferenceX/actions/runs/35743948727) attempt 1 (source `e78a79ee`, image `v0.5.20-rocm720-mi35x-20260922`, 8k1k, `median_e2el` vs `tput_per_gpu`), `infx.workflows.pareto_coverage` gives non-MTP curve 8/5 frontier points (10 measured, TP2 c4–c256 + TP4 c4–c16) and MTP curve 7/5 (9 measured, TP2 c4–c128 + TP4 c4–c16); 0 invalid points.

</details>

Assessed commit: `b0bc091a84c6ad404cb46a4e4b4a3c8797134983`.


### github-actions[bot] · 2026-09-24

<!-- codeowner-signoff-verify signoff=pullrequestreview-5298331036 -->
## ✅✅✅ **Verdict: PASS** ✅✅✅

<details>
<summary>Passed and not applicable checks</summary>

✅ Check 0 (CODEOWNER): PASS — `@1am9trash` is a named owner of `configs/amd-master.yaml` in `.github/CODEOWNERS`; `perf-changelog.yaml` falls to the `*` catch-all, which any listed CODEOWNER covers.

✅ Check 1 (Passing sweep on in-PR commit): PASS — in-PR commit `e78a79ee` carries [run 35743948727](https://github.com/SemiAnalysisAI/InferenceX/actions/runs/35743948727) with all 19 `single-node 8k1k /` and 4 `eval /` check-runs `success` (none skipped or failed); PR head `4875b23f` matches the pinned SHA.

✅ Check 2 (Evals pass): PASS — `eval_results_all/agg_eval_all.json` from run 35743948727: GSM8K em_strict 0.9659 / 0.9689 (non-MTP TP2 c64 / c256) and 0.9666 / 0.9697 (MTP TP2 c64 / c128), n_eff 1319 each, all above the `infx/evals/thresholds.yaml` qwen3.5 bar of 0.94; eval server logs ran `lmsysorg/sglang-rocm:v0.5.20-rocm720-mi35x-20260922` with `amd/Qwen3.5-397B-A17B-MXFP4-AttnFP8-V2`, matching both changed entries.

✅ Check 3 (Recipe linked, merged, complete): PASS — the additional detail section links the published [SGLang cookbook Qwen3.5 page](https://docs.sglang.io/cookbook/autoregressive/Qwen/Qwen3.5), whose MI355X MXFP4 section matches every major arg in `qwen3.5_fp4_mi355x.sh` / `qwen3.5_fp4_mi355x_mtp.sh`: same checkpoint, TP2 (TP4 for low concurrency), `--attention-backend aiter`, `--kv-cache-dtype fp8_e4m3`, `--page-size 16`, AITER env vars, and MTP `--speculative-algorithm EAGLE --speculative-num-steps 3 --speculative-eagle-topk 1 --speculative-num-draft-tokens 4`. Informational only: `ROCM_QUICK_REDUCE_QUANTIZATION=INT8` (cookbook shows INT4; the PR uses higher precision), `--disable-radix-cache`, `--max-running-requests $CONC`, and the newer image tag.

✅ Check 4 (Reuse command): PASS — `@1am9trash` (COLLABORATOR) posted `/reuse-sweep-run 35743948727` as a whole-line comment on 2026-09-23.

✅ Check 5 (Latest checklist template): PASS — all 17 current template items (16 top-level plus the recipe sub-item) are present and checked, none unchecked; the only later template edit (#3403, 2026-09-24T01:45Z, after the 00:31Z sign-off) reworded item 6 without adding an item.

✅ Check 6 (Upstream image / engine-first ordering): PASS — both changed entries are `framework: sglang`, `runner: mi355x`, `image: lmsysorg/sglang-rocm:v0.5.20-rocm720-mi35x-20260922` (upstream lmsysorg org; tag active on Docker Hub, pushed 2026-09-22, digest `7163e9dc…`); no non-vLLM/SGLang framework is added, so ordering does not apply.

✅ Check 7 (No deprecated models/scenarios): PASS — `MODELS.md` lists `qwen3.5` Single-turn 8k1k fp4 as active on 2026-09-24; only bf16 recipes and 1k1k/1k8k are retired for this model.

✅ Check 8 (No architecture hacks): PASS — image-only diff; the effective scripts have no `--hf-overrides`/`--json-model-override-args`/config edits, and the run's server args show `json_model_override_args: '{}'`.

✅ Check 9 (Spec-decode uses chat template): PASS — `qwen3.5_fp4_mi355x_mtp.sh` passes `--use-chat-template` to `run_benchmark_serving`.

✅ Check 10 (No engine patches): PASS — the diff, both benchmark scripts, and `runners/launch_mi355x-amds.sh` contain no `.patch`/`git apply`/`sed -i`/heredoc rewrites or engine wheel installs; the pinned image runs as shipped.

➖ Check 11 (Agentic golden AL): N/A — fixed-seq-len configs only; `qwen3.5-fp4-mi355x-sglang-agentic-mtp` is not touched.

➖ Check 12 (Append-only): N/A — the new `perf-changelog.yaml` entry does not set `append-only: true`.

✅ Check 13 (Draft runs as shipped): PASS — checkpoint `amd/Qwen3.5-397B-A17B-MXFP4-AttnFP8-V2` (rev `e17e5f0e`) stores all 1553 `mtp.*` tensors as BF16 in `model-00005-of-00005.safetensors` with zero scale tensors, and lists all 1546 MTP linear/expert modules in the Quark `exclude` list; SGLang v0.5.20 `qwen3_5_mtp.py::_mtp_quant_config` returns `None` for a Quark config with `mtp.*` excludes (draft built unquantized) and `quark.py::get_quant_method` returns `UnquantizedLinearMethod` for excluded layers; run server args show `speculative_draft_model_path: None`, `speculative_draft_model_quantization: None`, `speculative_draft_kv_cache_dtype: None` (draft KV inherits target `fp8_e4m3`, allowed), `dtype: auto`, and `Qwen3_5ForCausalLMMTP` loads 7.91 GB/rank at TP2 (consistent with BF16). `SGLANG_NVFP4_CKPT_FP8_NEXTN_MOE` is unset in the scripts, launcher, and workflows, defaults to `False` in v0.5.20 `environ.py`, and is only read by the DeepSeek override path. Effective draft = BF16 = shipped baseline, matching the sign-off's evidence.

✅ Check 14 (Pareto coverage): PASS — from `results_bmk/agg_bmk.json` of [run 35743948727](https://github.com/SemiAnalysisAI/InferenceX/actions/runs/35743948727) attempt 1 (source `e78a79ee`, image `lmsysorg/sglang-rocm:v0.5.20-rocm720-mi35x-20260922`, 8k1k, `median_e2el` vs `tput_per_gpu`, `upper_right` per pinned `metric-registry.ts`, all 19 cells `passed`, 0 invalid), `infx.workflows.pareto_coverage` reports qwen3.5/8k1k/MI355X-SGLang/fp4 non-MTP: 8/5 frontier points (10 measured, TP2 c4–c256 + TP4 c4–c16) and MTP: 7/5 frontier points (9 measured, TP2 c4–c128 + TP4 c4–c16); no admin exception needed.

</details>

Assessed commit: `4875b23f4d4d09fbf066d9e65cb52a3164cbb2e0`.


### yichiche · 2026-09-24

<img width="533" height="314" alt="image" src="https://github.com/user-attachments/assets/4bae3c27-61a5-4344-b4ba-f3cca92c6fab" />

Result updated, @1am9trash can you help approve again? More than appreciate.

### functionstackx · 2026-09-25

supersecded by this https://github.com/SemiAnalysisAI/InferenceX/pull/3439

## Review (5)

### claude[bot] · 2026-09-22 · COMMENTED

(no text)

### 1am9trash · 2026-09-23 · APPROVED

As a PR reviewer and CODEOWNER, I have reviewed this and have:
- [x] Verified that as of the moment of typing this, this is the latest version of [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md)
- [x] Verified that the general code quality meets the InferenceX standard and does not make the code quality any worse.
- [x] Verified that this PR has passed PR validation. Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/inference?unofficialRun=35743948727
- [x] Verified that this PR passes evals.  Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/evaluation?unofficialRun=35743948727
- [x] Verified that speculative decoding PRs uses chat templates to align the AL distribution to real world
- [x] Verified that every draft model and draft head is served as it ships: the draft that ships with the served checkpoint, at its stored precision, through the pinned upstream image's default handling, with the shipped and effective draft precision recorded in the additional detail section. No submission-side quantization, dtype override, checkpoint substitution, or patch may lower draft precision below that default, regardless of eval results or AL. See [Draft-model precision](https://github.com/SemiAnalysisAI/InferenceX/blob/main/CONTRIBUTING.md#draft-model-precision) for what counts as the default and the MLPerf comparison.
- [x] For agentic workloads: verified that speculative-decoding configs (EAGLE / MTP / draft models) run with simulated synthetic acceptance, with the acceptance-length value taken from the committed golden AL curve in [golden_al_distribution/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/golden_al_distribution) for that model, thinking mode, and draft length. A submission may choose any supported draft length, but it may not substitute a different acceptance target.
- [x] Verified against the current [MODELS.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/MODELS.md) that this PR does not submit a deprecated model, scenario, or model-scenario combination.
- [x] Verified that the model architecture isn't changed with benchmark hacks like using --hf-overrides to skipping indexer for every x layers on models that don't natively support this. As a general rule, we won't accept optimizations that reduces the number of model architecture FLOPs. Anything that makes that same computation run faster is fair game; target/verifier FLOPs at lower precisions is fine, given that the config passes private evals, but this does not permit lowering draft-model or draft-head precision below what ships. As an general north star princple, we should only use optimizations which is used in production by customers that care about accuracy
- [x] If an company claims that they support vLLM/SGLang as first class LLM inference engines on their hardware, I have verified that the respective vLLM submission made using upstream https://hub.docker.com/u/vllm docker repo, upstream SGLang https://hub.docker.com/u/lmsysorg docker repo. The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet as supported by vLLM/SGLang community maintainers
- [x] If an company claims that they support vLLM/SGLang as first class upstream in-tree LLM inference engines on their hardware, I have have verified that the respective vLLM/SGLang submission has been made before additional frameworks (TRT-LLM, ATOM, etc.). The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet.
- [x] Verified that every single-node vLLM/SGLang recipe in this PR is documented in the official [vLLM recipes](https://recipes.vllm.ai/) and/or the [SGLang cookbook](https://docs.sglang.io/cookbook/intro):
  - [x] I linked the corresponding upstream PR in the [vLLM recipe repo](https://github.com/vllm-project/recipes) or [SGLang repo](https://github.com/sgl-project/sglang/tree/main/docs_new) and verified that it is **MERGED** before this InferenceX PR merges. An opened, draft, or closed-without-merge upstream PR does not satisfy this requirement. If the matching recipe was already published, I linked the published recipe/cookbook page in the additional detail section below.
- [x] Verified that this PR does not patch the inference engine or serving stack — the pinned image must run as shipped. This covers .patch files / git apply / patch, inline patches embedded in benchmark scripts (e.g. a python3/sed heredoc that rewrites installed engine sources before serving), in-place edits of site-packages, monkey-patching, overwriting container files, and installing forked/rebuilt engine wheels on top of the pinned image. The only exception is a patch covered by a filled-out waiver at [docs/waiver/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/docs/waiver)`<PR_NUMBER>.md` — named after the PR that introduces the patch and filed in that same PR, stating what is patched, why the unmodified upstream image cannot run this benchmark, the upstream PR/issue link, and the removal plan — which I have linked below in the additional detail section.
- [x] If this PR uses `append-only: true`, verified that it only adds generated points or recipe variants inside a selected existing config/scenario and existing same-image visual curve: every previously generated point remains present with the same recipe, no prior point is removed or rerun, and every benchmark-affecting change in the complete diff can affect only the corresponding newly appended points (never an existing point), regardless of which file contains it.
- [x] If any of the above criteria cannot reasonably be satisfied, I have provided additional reasoning below.
- [x] Reported measured throughput/E2EL Pareto counts and evidence per affected curve (≥5 points strongly recommended). Below 5 or unverifiable: tag a core maintainer for review; recorded admin bypass required before merge. N/A if no curves are affected. [Details](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/codeowner-signoff-verify-prompt.md#check-14--pareto-coverage-recommendation-with-admin-exception).

### Additional detail section:
- Recipe: https://docs.sglang.io/cookbook/autoregressive/Qwen/Qwen3.5

Signed: @1am9trash 

### 1am9trash · 2026-09-23 · APPROVED

As a PR reviewer and CODEOWNER, I have reviewed this and have:
- [x] Verified that as of the moment of typing this, this is the latest version of [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md)
- [x] Verified that the general code quality meets the InferenceX standard and does not make the code quality any worse.
- [x] Verified that this PR has passed PR validation. Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/inference?unofficialRun=35743948727
- [x] Verified that this PR passes evals.  Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/evaluation?unofficialRun=35743948727
- [x] Verified that speculative decoding PRs uses chat templates to align the AL distribution to real world
- [x] Verified that every draft model and draft head is served as it ships: the draft that ships with the served checkpoint, at its stored precision, through the pinned upstream image's default handling, with the shipped and effective draft precision recorded in the additional detail section. No submission-side quantization, dtype override, checkpoint substitution, or patch may lower draft precision below that default, regardless of eval results or AL. See [Draft-model precision](https://github.com/SemiAnalysisAI/InferenceX/blob/main/CONTRIBUTING.md#draft-model-precision) for what counts as the default and the MLPerf comparison.
- [x] For agentic workloads: verified that speculative-decoding configs (EAGLE / MTP / draft models) run with simulated synthetic acceptance, with the acceptance-length value taken from the committed golden AL curve in [golden_al_distribution/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/golden_al_distribution) for that model, thinking mode, and draft length. A submission may choose any supported draft length, but it may not substitute a different acceptance target.
- [x] Verified against the current [MODELS.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/MODELS.md) that this PR does not submit a deprecated model, scenario, or model-scenario combination.
- [x] Verified that the model architecture isn't changed with benchmark hacks like using --hf-overrides to skipping indexer for every x layers on models that don't natively support this. As a general rule, we won't accept optimizations that reduces the number of model architecture FLOPs. Anything that makes that same computation run faster is fair game; target/verifier FLOPs at lower precisions is fine, given that the config passes private evals, but this does not permit lowering draft-model or draft-head precision below what ships. As an general north star princple, we should only use optimizations which is used in production by customers that care about accuracy
- [x] If an company claims that they support vLLM/SGLang as first class LLM inference engines on their hardware, I have verified that the respective vLLM submission made using upstream https://hub.docker.com/u/vllm docker repo, upstream SGLang https://hub.docker.com/u/lmsysorg docker repo. The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet as supported by vLLM/SGLang community maintainers
- [x] If an company claims that they support vLLM/SGLang as first class upstream in-tree LLM inference engines on their hardware, I have have verified that the respective vLLM/SGLang submission has been made before additional frameworks (TRT-LLM, ATOM, etc.). The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet.
- [x] Verified that every single-node vLLM/SGLang recipe in this PR is documented in the official [vLLM recipes](https://recipes.vllm.ai/) and/or the [SGLang cookbook](https://docs.sglang.io/cookbook/intro):
  - [x] I linked the corresponding upstream PR in the [vLLM recipe repo](https://github.com/vllm-project/recipes) or [SGLang repo](https://github.com/sgl-project/sglang/tree/main/docs_new) and verified that it is **MERGED** before this InferenceX PR merges. An opened, draft, or closed-without-merge upstream PR does not satisfy this requirement. If the matching recipe was already published, I linked the published recipe/cookbook page in the additional detail section below.
- [x] Verified that this PR does not patch the inference engine or serving stack — the pinned image must run as shipped. This covers .patch files / git apply / patch, inline patches embedded in benchmark scripts (e.g. a python3/sed heredoc that rewrites installed engine sources before serving), in-place edits of site-packages, monkey-patching, overwriting container files, and installing forked/rebuilt engine wheels on top of the pinned image. The only exception is a patch covered by a filled-out waiver at [docs/waiver/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/docs/waiver)`<PR_NUMBER>.md` — named after the PR that introduces the patch and filed in that same PR, stating what is patched, why the unmodified upstream image cannot run this benchmark, the upstream PR/issue link, and the removal plan — which I have linked below in the additional detail section.
- [x] If this PR uses `append-only: true`, verified that it only adds generated points or recipe variants inside a selected existing config/scenario and existing same-image visual curve: every previously generated point remains present with the same recipe, no prior point is removed or rerun, and every benchmark-affecting change in the complete diff can affect only the corresponding newly appended points (never an existing point), regardless of which file contains it.
- [x] If any of the above criteria cannot reasonably be satisfied, I have provided additional reasoning below.
- [x] Reported measured throughput/E2EL Pareto counts and evidence per affected curve (≥5 points strongly recommended). Below 5 or unverifiable: tag a core maintainer for review; recorded admin bypass required before merge. N/A if no curves are affected. [Details](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/codeowner-signoff-verify-prompt.md#check-14--pareto-coverage-recommendation-with-admin-exception).

### Additional detail section:
- Recipe: https://docs.sglang.io/cookbook/autoregressive/Qwen/Qwen3.5

Signed: @1am9trash 

### 1am9trash · 2026-09-24 · APPROVED

As a PR reviewer and CODEOWNER, I have reviewed this and have:
- [x] Verified that as of the moment of typing this, this is the latest version of [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md)
- [x] Verified that the general code quality meets the InferenceX standard and does not make the code quality any worse.
- [x] Verified that this PR has passed PR validation. Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/inference?unofficialRun=35743948727
- [x] Verified that this PR passes evals.  Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/evaluation?unofficialRun=35743948727
- [x] Verified that speculative decoding PRs uses chat templates to align the AL distribution to real world
- [x] Verified that every draft model and draft head is served as it ships: the draft that ships with the served checkpoint, at its stored precision, through the pinned upstream image's default handling, with the shipped and effective draft precision recorded in the additional detail section. No submission-side quantization, dtype override, checkpoint substitution, or patch may lower draft precision below that default, regardless of eval results or AL. See [Draft-model precision](https://github.com/SemiAnalysisAI/InferenceX/blob/main/CONTRIBUTING.md#draft-model-precision) for what counts as the default and the MLPerf comparison.
- [x] For agentic workloads: verified that speculative-decoding configs (EAGLE / MTP / draft models) run with simulated synthetic acceptance, with the acceptance-length value taken from the committed golden AL curve in [golden_al_distribution/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/golden_al_distribution) for that model, thinking mode, and draft length. A submission may choose any supported draft length, but it may not substitute a different acceptance target.
- [x] Verified against the current [MODELS.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/MODELS.md) that this PR does not submit a deprecated model, scenario, or model-scenario combination.
- [x] Verified that the model architecture isn't changed with benchmark hacks like using --hf-overrides to skipping indexer for every x layers on models that don't natively support this. As a general rule, we won't accept optimizations that reduces the number of model architecture FLOPs. Anything that makes that same computation run faster is fair game; target/verifier FLOPs at lower precisions is fine, given that the config passes private evals, but this does not permit lowering draft-model or draft-head precision below what ships. As an general north star princple, we should only use optimizations which is used in production by customers that care about accuracy
- [x] If an company claims that they support vLLM/SGLang as first class LLM inference engines on their hardware, I have verified that the respective vLLM submission made using upstream https://hub.docker.com/u/vllm docker repo, upstream SGLang https://hub.docker.com/u/lmsysorg docker repo. The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet as supported by vLLM/SGLang community maintainers
- [x] If an company claims that they support vLLM/SGLang as first class upstream in-tree LLM inference engines on their hardware, I have have verified that the respective vLLM/SGLang submission has been made before additional frameworks (TRT-LLM, ATOM, etc.). The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet.
- [x] Verified that every single-node vLLM/SGLang recipe in this PR is documented in the official [vLLM recipes](https://recipes.vllm.ai/) and/or the [SGLang cookbook](https://docs.sglang.io/cookbook/intro):
  - [x] I linked the corresponding upstream PR in the [vLLM recipe repo](https://github.com/vllm-project/recipes) or [SGLang repo](https://github.com/sgl-project/sglang/tree/main/docs_new) and verified that it is **MERGED** before this InferenceX PR merges. An opened, draft, or closed-without-merge upstream PR does not satisfy this requirement. If the matching recipe was already published, I linked the published recipe/cookbook page in the additional detail section below.
- [x] Verified that this PR does not patch the inference engine or serving stack — the pinned image must run as shipped. This covers .patch files / git apply / patch, inline patches embedded in benchmark scripts (e.g. a python3/sed heredoc that rewrites installed engine sources before serving), in-place edits of site-packages, monkey-patching, overwriting container files, and installing forked/rebuilt engine wheels on top of the pinned image. The only exception is a patch covered by a filled-out waiver at [docs/waiver/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/docs/waiver)`<PR_NUMBER>.md` — named after the PR that introduces the patch and filed in that same PR, stating what is patched, why the unmodified upstream image cannot run this benchmark, the upstream PR/issue link, and the removal plan — which I have linked below in the additional detail section.
- [x] If this PR uses `append-only: true`, verified that it only adds generated points or recipe variants inside a selected existing config/scenario and existing same-image visual curve: every previously generated point remains present with the same recipe, no prior point is removed or rerun, and every benchmark-affecting change in the complete diff can affect only the corresponding newly appended points (never an existing point), regardless of which file contains it.
- [x] If any of the above criteria cannot reasonably be satisfied, I have provided additional reasoning below.
- [x] Reported measured throughput/E2EL Pareto counts and evidence per affected curve (≥5 points strongly recommended). Below 5 or unverifiable: tag a core maintainer for review; recorded admin bypass required before merge. N/A if no curves are affected. [Details](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/codeowner-signoff-verify-prompt.md#check-14--pareto-coverage-recommendation-with-admin-exception).

### Additional detail section:
- Recipe: https://docs.sglang.io/cookbook/autoregressive/Qwen/Qwen3.5
- Draft precision (qwen3.5-fp4-mi355x-sglang-mtp): in-checkpoint 1-layer MTP on amd/Qwen3.5-397B-A17B-MXFP4-AttnFP8-V2; mtp.* stored BF16, Quark-excluded, SGLang v0.5.20 builds draft unquantized; no draft path/quant/dtype override. Effective = BF16 = baseline.

Signed: @1am9trash 

### 1am9trash · 2026-09-24 · APPROVED

As a PR reviewer and CODEOWNER, I have reviewed this and have:
- [x] Verified that as of the moment of typing this, this is the latest version of [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md)
- [x] Verified that the general code quality meets the InferenceX standard and does not make the code quality any worse.
- [x] Verified that this PR has passed PR validation. Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/inference?unofficialRun=35743948727
- [x] Verified that this PR passes evals.  Please link to GitHub Action workflow that shows this. Link: https://inferencex.semianalysis.com/evaluation?unofficialRun=35743948727
- [x] Verified that speculative decoding PRs uses chat templates to align the AL distribution to real world
- [x] Verified that every draft model and draft head is served as it ships: the draft that ships with the served checkpoint, at its stored precision, through the pinned upstream image's default handling, with the shipped and effective draft precision recorded in the additional detail section. No submission-side quantization, dtype override, checkpoint substitution, or patch may lower draft precision below that default, regardless of eval results or AL. See [Draft-model precision](https://github.com/SemiAnalysisAI/InferenceX/blob/main/CONTRIBUTING.md#draft-model-precision) for what counts as the default and the MLPerf comparison.
- [x] For agentic workloads: verified that speculative-decoding configs (EAGLE / MTP / draft models) run with simulated synthetic acceptance, with the acceptance-length value taken from the committed golden AL curve in [golden_al_distribution/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/golden_al_distribution) for that model, thinking mode, and draft length. A submission may choose any supported draft length, but it may not substitute a different acceptance target.
- [x] Verified against the current [MODELS.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/MODELS.md) that this PR does not submit a deprecated model, scenario, or model-scenario combination.
- [x] Verified that the model architecture isn't changed with benchmark hacks like using --hf-overrides to skipping indexer for every x layers on models that don't natively support this. As a general rule, we won't accept optimizations that reduces the number of model architecture FLOPs. Anything that makes that same computation run faster is fair game; target/verifier FLOPs at lower precisions is fine, given that the config passes private evals, but this does not permit lowering draft-model or draft-head precision below what ships. As an general north star princple, we should only use optimizations which is used in production by customers that care about accuracy
- [x] If an company claims that they support vLLM/SGLang as first class LLM inference engines on their hardware, I have verified that the respective vLLM submission made using upstream https://hub.docker.com/u/vllm docker repo, upstream SGLang https://hub.docker.com/u/lmsysorg docker repo. The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet as supported by vLLM/SGLang community maintainers
- [x] If an company claims that they support vLLM/SGLang as first class upstream in-tree LLM inference engines on their hardware, I have have verified that the respective vLLM/SGLang submission has been made before additional frameworks (TRT-LLM, ATOM, etc.). The only exceptions are for new hardware, such as MI455X UALoE72, Vera Rubin NVL72, Rubin NVL8, etc., and for new model architectures where there is an actual reason why vLLM/SGLang does not fundamentally support them yet.
- [x] Verified that every single-node vLLM/SGLang recipe in this PR is documented in the official [vLLM recipes](https://recipes.vllm.ai/) and/or the [SGLang cookbook](https://docs.sglang.io/cookbook/intro):
  - [x] I linked the corresponding upstream PR in the [vLLM recipe repo](https://github.com/vllm-project/recipes) or [SGLang repo](https://github.com/sgl-project/sglang/tree/main/docs_new) and verified that it is **MERGED** before this InferenceX PR merges. An opened, draft, or closed-without-merge upstream PR does not satisfy this requirement. If the matching recipe was already published, I linked the published recipe/cookbook page in the additional detail section below.
- [x] Verified that this PR does not patch the inference engine or serving stack — the pinned image must run as shipped. This covers .patch files / git apply / patch, inline patches embedded in benchmark scripts (e.g. a python3/sed heredoc that rewrites installed engine sources before serving), in-place edits of site-packages, monkey-patching, overwriting container files, and installing forked/rebuilt engine wheels on top of the pinned image. The only exception is a patch covered by a filled-out waiver at [docs/waiver/](https://github.com/SemiAnalysisAI/InferenceX/tree/main/docs/waiver)`<PR_NUMBER>.md` — named after the PR that introduces the patch and filed in that same PR, stating what is patched, why the unmodified upstream image cannot run this benchmark, the upstream PR/issue link, and the removal plan — which I have linked below in the additional detail section.
- [x] If this PR uses `append-only: true`, verified that it only adds generated points or recipe variants inside a selected existing config/scenario and existing same-image visual curve: every previously generated point remains present with the same recipe, no prior point is removed or rerun, and every benchmark-affecting change in the complete diff can affect only the corresponding newly appended points (never an existing point), regardless of which file contains it.
- [x] If any of the above criteria cannot reasonably be satisfied, I have provided additional reasoning below.
- [x] Reported measured throughput/E2EL Pareto counts and evidence per affected curve (≥5 points strongly recommended). Below 5 or unverifiable: tag a core maintainer for review; recorded admin bypass required before merge. N/A if no curves are affected. [Details](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/codeowner-signoff-verify-prompt.md#check-14--pareto-coverage-recommendation-with-admin-exception).

### Additional detail section:
- Recipe: https://docs.sglang.io/cookbook/autoregressive/Qwen/Qwen3.5
- Draft precision (qwen3.5-fp4-mi355x-sglang-mtp): in-checkpoint 1-layer MTP on amd/Qwen3.5-397B-A17B-MXFP4-AttnFP8-V2; mtp.* stored BF16, Quark-excluded, SGLang v0.5.20 builds draft unquantized; no draft path/quant/dtype override. Effective = BF16 = baseline.

Signed: @1am9trash 
