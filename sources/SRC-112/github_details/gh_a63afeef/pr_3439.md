# [PR #3439] [Klaud Cold] Update qwen3.5-fp4-mi355x-sglang (+mtp) SGLang ROCm image to v0.5.20-rocm720-mi35x-20260924

source: https://github.com/SemiAnalysisAI/InferenceX/pull/3439
state: open | updated: 2026-09-25T18:19:25Z
labels: full-sweep-enabled

## 正文

## Summary
Update SGLang ROCm image from v0.5.19-rocm720-mi35x-20260913 to v0.5.20-rocm720-mi35x-20260924 (latest nightly) for the Qwen3.5 8k1k recipes.

Recipes touched: `qwen3.5-fp4-mi355x-sglang`, `qwen3.5-fp4-mi355x-sglang-mtp`

Changes:
- `configs/amd-master.yaml`: image bump for both keys.
- `srt-slurm-recipes/qwen3.5/sglang/mi355x-fp4{,-mtp}/8k1k.yaml`: `container` bump.
- `perf-changelog.yaml`: new entry.

## Test plan
- [ ] full-sweep-enabled sweep passes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 评论 (1)

### github-actions[bot] · 2026-09-25

<!-- recipe-reminder -->
Thanks for the contribution!

- **Review:** If this PR changes files owned by someone other than a repository admin or `@SemiAnalysisAI/core`, ask one eligible [CODEOWNER](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/CODEOWNERS) to complete the latest [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md) before contacting a core maintainer on Slack. Follow the template exactly, including `As a PR reviewer and CODEOWNER, I have reviewed this and have`, so sign-off verification triggers.
- **PR verification:** Sweeps only run on labeled PRs. Add `full-sweep-fail-fast` (strongly recommended); use `full-sweep-enabled` only when matrix jobs should continue after a failure.
- **After merging:** PR authors must ensure all GitHub Actions jobs pass. Transient failures often pass on rerun; see [how to rerun failed jobs](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs#re-running-failed-jobs-in-a-workflow).

<details>
<summary>中文</summary>

感谢你的贡献！

- **审阅：**如果 PR 修改的文件归属于仓库管理员及 `@SemiAnalysisAI/core` 之外的 CODEOWNER，请先联系一位有资格的 [CODEOWNER](https://github.com/SemiAnalysisAI/InferenceX/blob/main/.github/CODEOWNERS) 填写最新的 [PR_REVIEW_CHECKLIST.md](https://github.com/SemiAnalysisAI/InferenceX/blob/main/docs/PR_REVIEW_CHECKLIST.md)，再通过 Slack 联系核心维护者。必须严格遵循模板，并保留 `As a PR reviewer and CODEOWNER, I have reviewed this and have`，才能触发签核验证。
- **PR 验证：**扫描仅在带有标签的 PR 上运行。强烈建议添加 `full-sweep-fail-fast`；仅当需要矩阵任务在失败后继续运行时才使用 `full-sweep-enabled`。
- **合并后：**PR 作者必须确保所有 GitHub Actions 任务通过。临时性失败通常可以通过重新运行恢复；参见[重新运行失败任务的说明](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs#re-running-failed-jobs-in-a-workflow)。

</details>

## Review (1)

### claude[bot] · 2026-09-25 · COMMENTED

**Nothing blocking.** The comments below are optional suggestions. There is no need to push a fix for them before merging.

**Additional findings** (outside the current diff — GitHub can't attach inline comments there):

- 🟡 `perf-changelog.yaml` — The new perf-changelog entry ships with pr-link set to the literal placeholder "PRLINK_PLACEHOLDER" instead of a real PR URL, unlike every prior entry in the file (e.g. line 8909 has a real github.com/.../pull/3423 link). Anyone auditing image bumps via perf-changelog.yaml loses the ability to trace this entry back to its PR. Fix: replace pr-link with the actual PR URL before merge; this file is append-only and byte-sensitive per AGENTS.md so the placeholder must be corrected now, not amended later.

  <details><summary>Why this was flagged</summary>

  perf-changelog.yaml is append-only per AGENTS.md:124 ("preserve all existing bytes... append only at the tail"), so once merged this entry's pr-link at line 8919 cannot be edited in place without violating that invariant. Every other entry in the file (e.g. line 8912's pr-link) points to a real GitHub PR URL, used for auditing which PR caused a given image/config change. With pr-link left as "PRLINK_PLACEHOLDER", tooling or humans scanning perf-changelog.yaml for the source PR of the qwen3.5-fp4-mi355x-sglang(-mtp) v0.5.20 bump get a dead placeholder instead of a working link.

  **Verification:** nit. The placeholder is genuinely present. `git diff` shows the appended perf-changelog.yaml entry ends with `pr-link: PRLINK_PLACEHOLDER` (perf-changelog.yaml:8918; candidate cited 8919, which is the trailing blank line — the placeholder is one line up), whereas the immediately preceding entry at perf-changelog.yaml:8911 and every other entry carry a real URL (e.g.… | nit. The new perf-changelog…

  </details>

<!-- bhrv:6cee5f96513262a4 -->
