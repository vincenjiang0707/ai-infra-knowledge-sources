# [PR #3410] chore: quiet srt-slurm setup output

source: https://github.com/SemiAnalysisAI/InferenceX/pull/3410
state: closed | updated: 2026-09-25T05:25:16Z
labels: 

## 正文

Quiet login-side Git and uv setup. Save make setup output to srt-setup.log, upload it with server logs, and print it on failure. Do not pass the login virtual-environment marker to Slurm; keep orchestrator and benchmark logs live.

[MI300X TP8, concurrency 4 validation](https://github.com/SemiAnalysisAI/InferenceX/actions/runs/36062741702/job/107845453466): login setup output is quiet and the compute-node VIRTUAL_ENV warning is gone. Compute-node package listings remain because upstream srt-slurm invokes uv sync/run without --quiet. Cancellation requested after inspecting startup.

Shell syntax and local success/failure log handling passed. No dependency or caching changes.

## 评论 (0)

## Review (1)

### claude[bot] · 2026-09-24 · COMMENTED

Looks good, straightforward log-verbosity change.

Reviewed the six pool launchers' `uv venv`/`uv pip install` calls and slurm_utils.sh's git init/fetch/checkout/clone calls — `--quiet` only suppresses informational/progress output, not errors, so failure detection (`|| return 1`, `command -v srtctl` checks) is unaffected. Checked the new `echo "Using srt-slurm revision ..."` line — placed after the existing SHA verification, purely additive logging. Confirmed this is log-noise suppression only (no dependency/behavior change), so no perf-changelog.yaml entry is warranted.


<details>
<summary>Extended reasoning...</summary>

Seven Bash launcher scripts under runners/ gain --quiet flags on uv/git setup commands plus one new informational echo; no logic, config precedence, or dependency behavior changes. No auth/injection/data-exposure surface is touched — this is CI log noise reduction only. The change is small, mechanical, and consistent across files, with no outstanding review objections in the timeline, so it merits approval without further human review.

</details>

<!-- bhrv:d08ebaa8ba5a35f7 -->
