source: https://docs.nvidia.com/deeplearning/nccl/user-guide/docs/nccl4py/communicator/lifecycle.html

# Creation and Lifecycle Methods[](https://docs.nvidia.com#creation-and-lifecycle-methods)

Methods on [ Communicator](https://docs.nvidia.com/class.html#nccl.core.Communicator) for creation, splitting, growing, and
teardown.

## Construction[](https://docs.nvidia.com#construction)

-
*classmethod*Communicator.init(*nranks: int*,*rank: int*,*unique_id:*,[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)| Sequence[[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)]*config:*)[NCCLConfig](https://docs.nvidia.com/configuration.html#nccl.core.NCCLConfig)| None = None[Communicator](https://docs.nvidia.com/class.html#nccl.core.Communicator)[](https://docs.nvidia.com#nccl.core.Communicator.init) Initializes a new NCCL communicator.

Creates a communicator that connects multiple ranks. This is a collective operation: all ranks must call this method with the same

`nranks`

and`unique_id`

but with different`rank`

values.- Parameters:
**nranks**– Total number of ranks in the communicator.**rank**– This rank (must be between 0 and`nranks - 1`

).**unique_id**– Unique identifier(s) shared by all ranks. A sequence may be passed to use.`ncclCommInitRankScalable()`

**config**– NCCL configuration options. Defaults to`None`

.

- Returns:
A new Communicator instance.

- Raises:
– If**NcclInvalid**`unique_id`

has an invalid type.


-
*classmethod*Communicator.init_all(*devices: int | Sequence[int] | None = None*) list[[Communicator](https://docs.nvidia.com/class.html#nccl.core.Communicator)][](https://docs.nvidia.com#nccl.core.Communicator.init_all) Initializes multiple NCCL communicators for single-process multi-GPU operations.

Creates an array of NCCL communicators, one for each device, within a single process. This is optimized for single-machine scenarios where all GPUs are controlled by the same process. Unlike

, which requires multi-process coordination (e.g. via MPI),`init()`

handles all coordination internally.`init_all()`

Each communicator is bound to its corresponding device and has its rank equal to its index in the returned list. The current device context is preserved by the underlying NCCL API. All communicators must be manually destroyed via

on each one.`destroy()`

- Parameters:
**devices**– Specifies which devices to initialize.`None`

(the default) initializes all visible CUDA devices. An int creates communicators for devices`[0, 1, ..., devices - 1]`

. A sequence of ints uses the explicit device IDs. If the resulting device list is empty (`devices=0`

, an empty sequence, or no visible devices), returns an empty list without calling into NCCL.- Returns:
List of initialized communicators, one per device. Rank

`i`

uses`devices[i]`

(or device`i`

when`devices`

is an int).- Raises:
**TypeError**– If`devices`

is not an int, sequence of ints, or`None`

.


-
Communicator.initialize(
*nranks: int*,*rank: int*,*unique_id:*,[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)| Sequence[[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)]*config:*) None[NCCLConfig](https://docs.nvidia.com/configuration.html#nccl.core.NCCLConfig)| None = None[](https://docs.nvidia.com#nccl.core.Communicator.initialize) Initializes this communicator in-place.

Instance-method counterpart of the

classmethod. Allows creating a null communicator first (via`init()`

`Communicator()`

) and initializing it later. This is a collective operation; all ranks must call this method.- Parameters:
**nranks**– Total number of ranks in the communicator.**rank**– This rank (must be between 0 and`nranks - 1`

).**unique_id**– Unique identifier(s) shared by all ranks.**config**– NCCL configuration options. Defaults to`None`

.

- Raises:
– If**NcclInvalid**`unique_id`

has an invalid type or this communicator is already initialized.


## Bootstrap identifier[](https://docs.nvidia.com#bootstrap-identifier)

A [ UniqueId](https://docs.nvidia.com#nccl.core.UniqueId) is generated by one rank (typically rank 0) and
broadcast to all participating ranks; all ranks then pass it to

[.](https://docs.nvidia.com#nccl.core.Communicator.init)

`Communicator.init()`

-
*class*nccl.core.UniqueId(*_internal: _nccl_bindings.UniqueId | None = None*)[](https://docs.nvidia.com#nccl.core.UniqueId) Bases:

`object`

NCCL unique identifier for communicator initialization.

A UniqueId is used to coordinate communicator initialization across multiple ranks. All ranks must use the same UniqueId to form a communicator. Typically one rank generates the UniqueId via

and broadcasts it to all other ranks. Three serialization paths are supported:`get_unique_id()`

**Bytes**:`bytes(uid)`

(or) on the producer,`as_bytes`

on receivers. The bytes of unique ID can be transmitted through any byte-oriented channel – a TCP socket, a shared filesystem, etc.`from_bytes()`

**NumPy**:returns an in-place view of the underlying buffer, suitable for NumPy-aware buffer transports such as`as_ndarray`

`mpi4py.MPI.Comm.Bcast`

(uppercase`B`

).**Pickle**: instances are picklable directly, so higher level object broadcast helpers like`mpi4py.MPI.Comm.bcast`

(lowercase`b`

) work out of the box.

-
*static*from_bytes(*b: bytes | bytearray | memoryview*)[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)[](https://docs.nvidia.com#nccl.core.UniqueId.from_bytes) Reconstructs a UniqueId from a bytes-like buffer.


-
*property*as_ndarray*: numpy.ndarray*[](https://docs.nvidia.com#nccl.core.UniqueId.as_ndarray) NumPy array view of the unique ID data.


-
*property*as_bytes*: bytes*[](https://docs.nvidia.com#nccl.core.UniqueId.as_bytes) Bytes representation of the unique ID, suitable for serialization or broadcast.



-
nccl.core.get_unique_id(
*empty: bool = False*)[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)[](https://docs.nvidia.com#nccl.core.get_unique_id) Generates a new NCCL unique identifier for communicator initialization.

Should be called by one rank (typically rank 0); the resulting

must then be broadcast (e.g. via MPI) to all other ranks.`UniqueId`

- Parameters:
**empty**– If True, return an emptywithout calling NCCL. Useful when the bytes will be filled in later via`UniqueId`

. Defaults to False.`UniqueId.from_bytes()`

- Returns:
A new

to be shared across ranks.`UniqueId`



## Splitting and growing[](https://docs.nvidia.com#splitting-and-growing)

-
Communicator.split(
*color: int | None = None*,*key: int = 0*,*config:*)[NCCLConfig](https://docs.nvidia.com/configuration.html#nccl.core.NCCLConfig)| None = None[Communicator](https://docs.nvidia.com/class.html#nccl.core.Communicator)[](https://docs.nvidia.com#nccl.core.Communicator.split) Splits this communicator into sub-communicators based on color values.

Ranks that pass the same

`color`

value will be part of the same group. If`color`

is`None`

, the rank will not be part of any group and receives a null communicator (ainstance with`Communicator`

`ptr=0`

). The`key`

value determines rank ordering; smaller`key`

means smaller rank in the new communicator. If keys are equal, the rank in the original communicator determines ordering.This is a collective operation: all ranks in the communicator must call this method, even ranks that pass

`color=None`

. There must be no outstanding NCCL operations on the communicator to avoid deadlock.- Parameters:
**color**– Non-negative color value for grouping ranks. Pass`None`

to exclude this rank from all groups. Defaults to`None`

.**key**– Ordering key within the color group. Defaults to 0.**config**– Configuration for the new communicator. If`None`

, inherits the parent’s configuration. Defaults to`None`

.

- Returns:
New sub-communicator, or a null communicator if

`color`

is`None`

.- Raises:
– If the communicator is not initialized.**NcclInvalid**

See also


-
Communicator.shrink(
*exclude_ranks: Sequence[int] | None = None*,*config:*,[NCCLConfig](https://docs.nvidia.com/configuration.html#nccl.core.NCCLConfig)| None = None*flag:*)[CommShrinkFlag](https://docs.nvidia.com#nccl.core.CommShrinkFlag)= CommShrinkFlag.DEFAULT[Communicator](https://docs.nvidia.com/class.html#nccl.core.Communicator)[](https://docs.nvidia.com#nccl.core.Communicator.shrink) Creates a new communicator by removing specified ranks from this one.

Ranks listed in

`exclude_ranks`

are excluded from the new communicator; the remaining ranks are renumbered to a contiguous`[0, n)`

range.This is a collective operation. All non-excluded ranks must call this method; excluded ranks must NOT call it. With

there must be no outstanding NCCL operations to avoid deadlock; combine with`DEFAULT`

`config.shrink_share=True`

to reuse parent communicator resources. Withoutstanding operations are automatically aborted and no resources are shared with the parent.`ABORT`

- Parameters:
**exclude_ranks**– Ranks to exclude from the new communicator. Defaults to`None`

(no exclusions).**config**– Configuration for the new communicator. If`None`

, inherits the parent’s configuration. Defaults to`None`

.**flag**– Shrink behavior. Usefor normal operation or`DEFAULT`

after errors. Defaults to`ABORT`

.`DEFAULT`


- Returns:
New communicator without the excluded ranks.

- Raises:
– If the communicator is not initialized.**NcclInvalid**

See also


-
Communicator.get_unique_id()
[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)[](https://docs.nvidia.com#nccl.core.Communicator.get_unique_id) Returns a per-communicator unique ID for use with

.`grow()`

Generates a unique identifier bound to this communicator that can be shared with new ranks joining via

. This is distinct from the global`grow()`

used for initial communicator creation. Only one existing rank (the grow root) should call this method.`get_unique_id()`

A new UID cannot be generated while a previous UID is unconsumed; each UID can be used only once and the user must wait for the corresponding grow operation to complete before calling again.

- Returns:
for grow operations.`UniqueId`

- Raises:
– If the communicator is not initialized.**NcclInvalid**


-
Communicator.grow(
*nranks: int*,*unique_id:*,[UniqueId](https://docs.nvidia.com#nccl.core.UniqueId)| None = None*rank: int | None = None*,*config:*)[NCCLConfig](https://docs.nvidia.com/configuration.html#nccl.core.NCCLConfig)| None = None[Communicator](https://docs.nvidia.com/class.html#nccl.core.Communicator)[](https://docs.nvidia.com#nccl.core.Communicator.grow) Grows the communicator by adding new ranks.

Creates a

**new**communicator that includes both existing ranks from this communicator and new ranks joining the group. The original communicator is unchanged. There are two roles in grow:Existing ranks: ranks already in an existing communicator.

New ranks: ranks joining the existing communicator.


`grow()`

is a collective operation. All ranks (existing and new) must call this method. Usage by role:Existing rank:

`new_comm = existing_comm.grow(nranks)`

New rank:

`new_comm = Communicator().grow(nranks, uid, rank=assigned_rank)`


The UID is consumed upon successful grow and cannot be reused. Exactly one UID should be generated before each grow.

- Parameters:
**nranks**– Total number of ranks in the new communicator (existing plus new). All roles must pass the same value.**unique_id**– Unique identifier from. New ranks must pass the`get_unique_id()`

. Existing ranks may leave this as`UniqueId`

`None`

because NCCL does not use this parameter for existing ranks. Defaults to`None`

.**rank**– This rank’s ID in the new communicator. New ranks must pass their assigned rank, which must be`>=`

the parent communicator size. Existing ranks must pass`None`

. Defaults to`None`

.**config**– Configuration for the new communicator. Defaults to`None`

.

- Returns:
New

containing all ranks.`Communicator`

- Raises:
– If a new rank is given an initialized communicator, or an existing rank is given a null communicator.**NcclInvalid**


## Teardown[](https://docs.nvidia.com#teardown)

-
Communicator.destroy() None
[](https://docs.nvidia.com#nccl.core.Communicator.destroy) Destroys the communicator and frees local resources.

If

has not been called explicitly,`finalize()`

will call it internally. If`destroy()`

is called explicitly, users must ensure the communicator state becomes`finalize()`

`ncclSuccess`

before calling. The communicator should not be accessed after`destroy()`

returns.`destroy()`

All resources (registered buffers, windows, custom operators) owned by this communicator are automatically closed before destruction. This is an intra-node collective call: all ranks on the same node must call it to avoid hanging. The recommended pattern is

followed by`finalize()`

.`destroy()`

Errors during cleanup are suppressed for safety.

See also


-
Communicator.abort() None
[](https://docs.nvidia.com#nccl.core.Communicator.abort) Aborts the communicator and frees resources, terminating in-flight operations.

Should be called when an unrecoverable error occurs. Unlike

, this immediately aborts uncompleted operations. All active ranks must call this function in order to abort the NCCL communicator successfully.`destroy()`

All resources (registered buffers, windows, custom operators) owned by this communicator are automatically closed before aborting. Errors during cleanup are suppressed for safety. For more details, see the Fault Tolerance section in the NCCL documentation.

See also


-
Communicator.finalize() None
[](https://docs.nvidia.com#nccl.core.Communicator.finalize) Finalizes the communicator, flushing uncompleted operations and network resources.

Typically called before

to ensure all operations complete. This is a collective operation that must be called by all ranks. When one thread manages multiple host-local ranks (multiple GPUs per thread), calls to`destroy()`

must be issued within`finalize()`

so that all local ranks can enter finalization together; otherwise they will hang.`group()`

For nonblocking communicators this is itself nonblocking: success sets the communicator state to

`ncclInProgress`

to indicate finalization is in progress. Once all NCCL operations complete, the communicator transitions to`ncclSuccess`

. Users can query the state with.`get_async_error()`

See also


## Pause and resume[](https://docs.nvidia.com#pause-and-resume)

-
Communicator.revoke(
*flags: int = 0*) None[](https://docs.nvidia.com#nccl.core.Communicator.revoke) Revokes the communicator.

Stops all in-flight operations and marks the communicator state as

`ncclInProgress`

. The state transitions to`ncclSuccess`

when the communicator becomes quiescent, after which management operations (,`destroy()`

,`split()`

) can proceed safely.`shrink()`

Calling

after`finalize()`

is invalid. Resource sharing via split-share / shrink-share is disabled while revoked.`revoke()`

- Parameters:
**flags**– Reserved for future use. Currently must be 0.- Raises:
– If the communicator is not initialized.**NcclInvalid**


-
Communicator.suspend(
*flags:*) None[CommSuspendFlag](https://docs.nvidia.com#nccl.core.CommSuspendFlag)= CommSuspendFlag.MEM[](https://docs.nvidia.com#nccl.core.Communicator.suspend) Suspends communicator operations to free resources.

The communicator cannot be used for communication while suspended. Call

to restore it.`resume()`

- Parameters:
**flags**– Suspend flags controlling what resources to release.releases dynamic GPU memory allocations.`MEM`

- Raises:
– If the communicator is not initialized.**NcclInvalid**


-
Communicator.resume() None
[](https://docs.nvidia.com#nccl.core.Communicator.resume) Resumes all previously suspended communicator resources.

Restores a communicator that was suspended with

so that it can be used for communication again.`suspend()`

- Raises:
– If the communicator is not initialized.**NcclInvalid**


## Flag enums[](https://docs.nvidia.com#flag-enums)

### CommShrinkFlag[](https://docs.nvidia.com#commshrinkflag)

-
*class*nccl.core.CommShrinkFlag(*value*,*names=<not given>*,**values*,*module=None*,*qualname=None*,*type=None*,*start=1*,*boundary=None*)[](https://docs.nvidia.com#nccl.core.CommShrinkFlag) Bases:

`IntEnum`

Behavior flag for

.`Communicator.shrink()`

-
DEFAULT
*= 0*[](https://docs.nvidia.com#nccl.core.CommShrinkFlag.DEFAULT) Shrink the parent communicator normally; outstanding NCCL operations must already be quiesced.


-
ABORT
*= 1*[](https://docs.nvidia.com#nccl.core.CommShrinkFlag.ABORT) First terminate ongoing parent operations, then shrink. No resources are shared with the parent.


-
DEFAULT

### CommSuspendFlag[](https://docs.nvidia.com#commsuspendflag)

-
*class*nccl.core.CommSuspendFlag(*value*,*names=<not given>*,**values*,*module=None*,*qualname=None*,*type=None*,*start=1*,*boundary=None*)[](https://docs.nvidia.com#nccl.core.CommSuspendFlag) Bases:

`IntFlag`

Behavior flag for

.`Communicator.suspend()`

-
MEM
*= 1*[](https://docs.nvidia.com#nccl.core.CommSuspendFlag.MEM) Suspend memory by releasing dynamic GPU memory allocations held by the communicator.


-
MEM