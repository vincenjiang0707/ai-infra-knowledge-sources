{"number": 2581, "title": "fix(pd-disaggregation): use kv_consumer for TPU v6 decode", "state": "open", "created_at": "2026-09-25T02:53:05Z", "updated_at": "2026-09-25T03:30:19Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "vLLM defines KV roles as overlapping sets, so kv_both sets is_kv_producer=True. \r\nTPUConnector collapses the roles into a single is_producer flag and returns early in get_num_new_matched_tokens when it is set, never consulting is_kv_consumer. With kv_both the decode engine reports zero loadable tokens on every request: prefill stages KV blocks that are never pulled and expire with \"KV transfer timeout. \r\n\r\n\r\nMeasured on TPU v6e (Qwen3-30B-A3B), kv_both -> kv_consumer:\r\n  decode reqs_to_load:          0 -> 12\r\n  decode insert_kv_chunks:      0 -> 3 per 3 reqs\r\n  prefill KV transfer timeouts: every request -> 0\r\n  avg transfer size:            0 -> 420-810 MB", "url": "https://github.com/llm-d/llm-d/pull/2581"}
{"number": 1598, "title": "ack-gateway support for alibaba cloud", "state": "closed", "created_at": "2026-05-25T08:40:47Z", "updated_at": "2026-09-25T01:19:39Z", "closed_at": "2026-09-25T01:19:38Z", "labels": ["lifecycle/rotten"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Manifests for using `ack-gateway` out of the box with alibaba-cloud\r\n\r\nSome of this is optional such as the backend traffic policy but I think its a strong addition since we can relay on envoyCRDs anyways in this case\r\n\r\ncc @shmuelk @ahg-g ", "url": "https://github.com/llm-d/llm-d/pull/1598"}
{"number": 2336, "title": "feat(agentic-serving): add eval-containers benchmark + nightly for the guide", "state": "open", "created_at": "2026-08-20T07:32:02Z", "updated_at": "2026-09-25T01:19:23Z", "closed_at": null, "labels": ["lifecycle/stale"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Adds the agentic `eval-containers` benchmark to the **agentic-serving guide**, in\ntwo pieces:\n\n1. **A benchmark template** —\n   `guides/agentic-serving/benchmark-templates/agentic-serving-eval-containers.yaml`,\n   a third sibling beside the two existing `inference-perf` templates, plus the\n   README section documenting it. Makes the eval a documented, runnable benchmark\n   of the guide today, by hand, against any model the guide serves.\n2. **A nightly** — `nightly-e2e-eval-containers-ibm-acc-gpu-vllm-x.yaml`, which\n   `uses:` llm-d-infra's `reusable-ci-nightly-benchmark.yaml` and passes the eval\n   as the **workload** (`harness: eval-containers`), the same way\n   `inference-perf` is the workload for the perf nightlies. Runs at `0 2 * * *`,\n   clear of the 03:00 slot the other eight OCP nightlies share.\n\n### Why the eval complements inference-perf rather than duplicating it\n\nThe guide's existing templates report throughput, TTFT, and ITL — how fast the\nstack serves a synthetic agentic load. This one drives a **real coding agent**\n(aider-polyglot) and reports **reward**: the fraction of tasks actually solved.\nThe guide's own Benchmarking section already named that as a gap — *\"Replaying\ncomplete agent dependency graphs with tool timing and sub-agent fan-out remains a\nseparate evaluation mode.\"*\n\nThe two modes catch different failures. A broken tool-call parser, reasoning\ntraces leaking into content, or truncation mid-solution each leave TTFT and ITL\nperfectly healthy while the agent silently fails every task. Reward sees them: a\nuniform 0.0 across tasks points at a transport or parser fault, a spread reflects\ngenuine task difficulty.\n\nUnlike its siblings the template takes `${MODEL}` rather than pinning a handle,\nbecause the harness resolves the served model from the deployment itself\n(`EVAL_MODEL=\"${LLMDBENCH_DEPLOY_CURRENT_MODEL:?}\"`). One template therefore\ncovers Nemotron-3-Ultra, GLM-5.2, Qwen3-Coder-480B, and smaller models where a\ncheap repeatable signal matters more than frontier scale.\n\n### Revised from the dispatch approach (thanks @maugustosilva)\n\nThe first version of this PR used a cross-repo `workflow_dispatch` into\nllm-d-benchmark, on the grounds that the shared engine couldn't drive the eval.\nThat's no longer true, and the review was right to push back:\n\n- `--cluster-config` and `-j` were missing from the engine. **llm-d-infra#244\n  has since merged** and adds both (`cluster_config`, `tasks`).\n- `eval-containers` is already a declared value of the engine's `harness` input,\n  and `workload/harnesses/eval-containers-llm-d-benchmark.sh` is in\n  llm-d-benchmark on `main`.\n\nWith those in place a separate dispatch path buys nothing, so this revision\n**deletes** `EVAL_DISPATCH_TOKEN`, the `github-script` dispatch, and the\nrun-URL polling — about 85 lines. A reusable-workflow call needs no PAT, and the\nrun now shows up as a normal job in this repo instead of a link to another one.\n\n### The anyuid SCC is a prerequisite, not a CI step\n\nMy earlier argument was that the eval needs the **anyuid SCC** bound before\nstandup (the eval image and vLLM both run as root), and that putting an\nOpenShift-specific SCC grant into the engine all nightlies share was too wide a\nblast radius. The second half stands; the first half was wrong about where the\ngrant belongs.\n\n`config/cluster-configs/examples/openshift-pokprod.yaml` already supplies the\n`serviceAccount` and `runAsUser` to the cluster-generic scenario, and its own\nheader documents the binding as a **one-time-per-namespace prerequisite**:\n\n```\noc create serviceaccount eval-runner -n llm-d-nightly-eval-containers-ibm-gpu\noc adm policy add-scc-to-user anyuid -z eval-runner -n llm-d-nightly-eval-containers-ibm-gpu\n```\n\nThat's cluster state, not per-run work. It's recorded in the workflow header,\nand the shared engine carries no OpenShift privilege logic.\n\n### On the nightly's model\n\n@maugustosilva — agreed on the direction, and this now uses the mechanism you\ndescribed, with the eval as the guide's workload.\n\nThe nightly serves **Qwen/Qwen3.6-27B on a single H100**, via\n`config/scenarios/examples/eval-containers-aider-polyglot-gpu.yaml` — already on\nllm-d-benchmark `main`, so there's no cross-repo dependency here. That model is\ndeliberate rather than a stand-in: a nightly wants a fast, cheap, every-night\nsignal, and this configuration scores non-trivially on aider-polyglot, so a score\nregression is meaningful. The guide's three documented deployments are 64-GPU\nconfigurations — right as reference deployments, but a nightly on that footprint\nis a capacity event, not a regression test.\n\nWorth stating plainly, since my first version of this PR got it wrong: nothing in\nthe guide scopes it by model size. There is no `gpu.product` selector or H200\nconstraint in the manifests, and the guide's substance — the routing and\nscheduling profile in `router/agentic-serving-gpu.values.yaml` (prefix-cache\naffinity, token-load scoring, prefill/decode filters) — binds to model servers by\nlabel:\n\n```yaml\n  modelServers:\n    matchLabels:\n      llm-d.ai/guide: \"agentic-serving-gpu\"\n```\n\n`llm-d.ai/guide` and `llm-d.ai/model` are independent labels, so the guide's\nidentity is decoupled from the model by design.\n\nThe nightly drives the eval through the shared engine via `scenario_dir: examples`\nagainst the eval's own scenario, which is what makes it self-contained: the\nscenario is on llm-d-benchmark `main`, so nothing else has to land first. If this\nguide later migrates to the `guide.yaml` method, the same job becomes\n`scenario_dir: guides` + `standup_scenario: agentic-serving` by changing two\ninputs, and the served model then comes from the guide's own manifests via the\nengine's `--model` auto-detection.\n\n### Cluster selection is by workflow name\n\nWorth flagging for reviewers, since it's non-obvious: the engine picks the\ncluster by grepping `github.workflow` for a provider substring\n(`reusable-ci-nightly-benchmark.yaml:289-303`). `\"OCP\"` in the title selects the\nself-hosted pok-prod runner pool and `KUBECONFIG_DATA_OCP`. So the\n`(OCP GPU)` suffix and the `-ibm-` filename segment are load-bearing.\n\n### Testing\n\n- `actionlint` clean, and the full `pre-commit` suite passes on the new file\n  (including `sync-nightly-matrix --check` — `eval-containers` is a workload,\n  not one of the ten release-gating guides in `matrix_common.GUIDES`, so it's\n  correctly absent from the matrix table).\n- Every value in the `with:` block diffed against the engine's declared inputs.\n  `scenario_dir: examples` and `workload: aider-polyglot.yaml` both resolve\n  (`config/scenarios/examples/eval-containers-aider-polyglot-gpu.yaml`,\n  `workload/profiles/eval-containers/aider-polyglot.yaml.in`).\n- `decode_pods` is deliberately **not** exposed: the engine only applies it to\n  kustomize `patch-*.yaml` manifests (step gated\n  `if: env.LLMDBENCH_CICD_METHOD == 'kustomize'`), and this scenario stands up\n  via `modelservice`, so the input would silently do nothing. Replica count\n  lives in the scenario.\n- Unlike the siblings, every input referenced in `with:` is declared or literal,\n  so `actionlint` reports no `property \"...\" is not defined` warnings. The rest\n  of the family has 535 of these; I didn't touch those files.\n\nNot exercised: a live nightly, which needs the one-time SCC binding above on\npok-prod.\n\n### Note on the DCO bot comment\n\nThe \"Unsigned commits detected!\" comment above is stale — it fired at 07:32:10\nagainst an earlier push, 11 minutes before the signed commit landed. Both\ncommits on this branch are SSH-signed and report `verified: true` /\n`reason: valid` from the commits API.\n", "url": "https://github.com/llm-d/llm-d/pull/2336"}
{"number": 2347, "title": "docs(operations): add OrcaRouter as an external LLM provider in the LiteLLM guide", "state": "open", "created_at": "2026-08-21T06:21:59Z", "updated_at": "2026-09-25T01:19:22Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nAdd a named **OrcaRouter** external LLM provider to the LiteLLM \"Serve External APIs\" guide, mirroring the existing Google Gemini external provider pattern.\n\n[OrcaRouter](https://www.orcarouter.ai) is an OpenAI-compatible gateway that smart-routes `orcarouter/auto` to an appropriate upstream model. It also runs gateway-level, zero-trust security for AI agents on the same endpoint — screening every prompt/response and governing every tool call on a default-deny basis, with no application code changes.\n\n## What changed\n\n- `docs/operations/serve-external-apis/litellm.md`\n  - **Step 1 (Secrets)**: added `ORCAROUTER_API_KEY` export and `litellm-env` secret literal.\n  - **Step 3 (`model_list`)**: added a named `orcarouter-auto` entry:\n    ```yaml\n    - model_name: orcarouter-auto\n      litellm_params:\n        model: openai/orcarouter/auto\n        api_base: https://api.orcarouter.ai/v1\n        api_key: os.environ/ORCAROUTER_API_KEY\n    ```\n  - **Step 4 (Verify)**: updated `/v1/models`, `/key/generate`, the inference example (new subsection C), and `/key/info` to cover `orcarouter-auto`.\n- `docs/operations/serve-external-apis/README.md`\n  - Added OrcaRouter to the section intro and the architecture diagram.\n\n## Validation\n\n- `markdownlint` (repo `.markdownlint.json`) — clean.\n- `check-yaml` / `check-json` / `trailing-whitespace` / EOF / mixed-line-ending (pre-commit hooks) — clean.\n- YAML `model_list` block parses with all four entries intact.\n- Live connectivity: `POST https://api.orcarouter.ai/v1/chat/completions` with model `orcarouter/auto` returned **HTTP 200** (`ORCA-LIVE-OK`, finish `stop`).\n\nNo code or cluster behavior is changed; this is a docs-only addition.\n\nDisclosure: I'm an engineer on the OrcaRouter team.\n", "url": "https://github.com/llm-d/llm-d/pull/2347"}
{"number": 2363, "title": "docs(observability): add WVA dashboards and alerts to llm-d", "state": "open", "created_at": "2026-08-24T09:10:04Z", "updated_at": "2026-09-25T01:19:20Z", "closed_at": null, "labels": ["lifecycle/stale"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## What this PR does\n\nMoves the Workload-Variant-Autoscaler's user-facing observability assets into `llm-d/llm-d`, per the [observability-integration proposal](https://github.com/llm-d/llm-d/blob/main/proposals/observability-integration.md) and #1983. This is the llm-d half of [wva#1466](https://github.com/llm-d/llm-d-workload-variant-autoscaler/issues/1466); the cleanup PR in the component repo follows once this lands, as that issue's acceptance criteria require.\n\nIt mirrors the layout the EPP migration established rather than inventing one — dashboards alongside the existing seven, the `PrometheusRule` as a sibling of `epp-alerting-rules.yaml`.\n\n**Added:**\n\n| File | Source in wva |\n|---|---|\n| `guides/recipes/observability/grafana/dashboards/llm-d-wva-operational.json` | `deploy/grafana/operational-dashboard.json` (16 panels) |\n| `guides/recipes/observability/grafana/dashboards/llm-d-wva-benchmark-scaleup.json` | `deploy/grafana/benchmark-dashboard.json` (5 panels) |\n| `guides/recipes/observability/alerts/wva-alerting-rules.yaml` | `config/components/prometheus-alerts/prometheusrule.yaml` (5 alerts) |\n\n## The dashboards are not copied verbatim\n\nThis is the part worth reviewing closely. The two dashboards did not use this repo's datasource convention, and copying them across unchanged would have produced dashboards that load but don't bind:\n\n- The operational dashboard bound every panel to a `$datasource_uid` template variable — a name that exists only in the wva repo.\n- The benchmark dashboard had **no datasource variable at all** and no `uid` on its datasource references, relying on whatever Grafana had set as default.\n\nEvery dashboard already here uses `${DS_PROMETHEUS}` with a standard `datasource`-type template variable. Both files were retargeted — **47 datasource references in total** — and given that variable, so they behave like their neighbours. `load-llm-d-dashboards.sh` globs `*.json`, so no registration was needed.\n\nI left the panel content, queries, and dashboard `uid`s untouched; the `uid`s are stable so existing links keep working.\n\n## Alerts\n\nRenamed to `wva-alerting-rules` and relabelled `app: wva-metrics`, matching `epp-alerting-rules.yaml`. The component repo used `release: kube-prometheus-stack`, which is a kube-prometheus-stack discovery convention rather than this repo's.\n\nWorth noting: the `ruleSelector` caveat already documented in `alerting.md` Step 1 applies here identically — in central mode `ruleSelector: {}` picks up any rule, and in scoped mode neither this file nor the EPP one carries the `monitoring-ns` label. I have not changed that behaviour, only matched the existing precedent.\n\n## Docs\n\n- `alerting.md` — intro now covers both components, plus a WVA section with apply/verify steps and a reference table for the five `wva.rules` alerts. These fire on autoscaler health rather than request-path health, which the section calls out.\n- `metrics.md` — the expected `kubectl get configmaps` output now lists the two new dashboards, so the sample stays accurate.\n\n## What is deliberately not here\n\nThe ServiceMonitor and the `docs/user-guide/monitoring.md` runbook. I asked on [wva#1466](https://github.com/llm-d/llm-d-workload-variant-autoscaler/issues/1466) whether the scrape manifest belongs under `guides/workload-autoscaling/.../monitoring/` or in the shared recipe directory — the issue says \"or equivalent recipe path\" and I would rather match @gyliu513's intent than guess and have it moved twice. Happy to add both here once that's settled, or in a follow-up.\n\n## Testing\n\nBoth dashboards parse as valid JSON and retain their panel counts; no `$datasource_uid` references remain. The `PrometheusRule` retains all five alerts across the single `wva.rules` group.\n\nI have not applied these to a live cluster — I don't have a Grafana/Prometheus stack with WVA running to hand, so the datasource rebinding is verified by structure rather than by rendering. Worth a look from someone who can load them.\n\n---\n\n*Disclosure: prepared with AI assistance. The asset inventory, the datasource-convention mismatch, and the alert reference were checked against both repositories before writing.*\n", "url": "https://github.com/llm-d/llm-d/pull/2363"}
{"number": 2388, "title": "proposal: selective tracing for llm-d", "state": "open", "created_at": "2026-08-27T02:17:35Z", "updated_at": "2026-09-25T01:19:18Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Adds a design proposal for **selective tracing**: a second, orthogonal control on top of the existing sampling-ratio knob (`OTEL_TRACES_SAMPLER_ARG`) from [Distributed Tracing for llm-d](https://github.com/llm-d/llm-d/blob/main/proposals/distributed-tracing.md). Where sampling decides *which requests* get traced, this proposal decides *which components* actually record and export spans for a request that's already been sampled in. That original work is implemented and documented in [Distributed Tracing](https://github.com/llm-d/llm-d/blob/main/docs/operations/observability/tracing.md).\r\n\r\n/cc @ahg-g @robertgshaw2-redhat @chcost ", "url": "https://github.com/llm-d/llm-d/pull/2388"}
{"number": 2408, "title": "feat(guides): add encoder-cache affinity to e-disaggregation vLLM profiles", "state": "open", "created_at": "2026-08-31T12:47:13Z", "updated_at": "2026-09-25T01:19:15Z", "closed_at": null, "labels": ["lifecycle/stale"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nAdds the `mm-embeddings-cache-producer` and `mm-embeddings-cache-scorer` plugins to both vLLM e-disaggregation router profiles (E/PD and E/P/D), so repeated multimodal media routes to the encode worker that already holds the embeddings.\n\nFixes #1952\n\n## Changes\n\n- `router/vllm/e-pd-disaggregation.values.yaml` and `router/vllm/e-p-d-disaggregation.values.yaml`: add `mm-embeddings-cache-producer` and `mm-embeddings-cache-scorer`, with the scorer referenced in the `encode` scheduling profile at weight 4 (matching the plugin README example). `cacheSizeInMBPerServer` is left unset (plugin default 4096 MiB) with a comment explaining the relationship to the vLLM `num_ec_blocks` this guide configures.\n- `README.md`: new \"Encoder-Cache Affinity\" section covering what the plugins do, the three EPP metrics (`encoder_cache_queries_total`, `encoder_cache_hits_total`, `encoder_cache_hit_ratio`), and how to tune `cacheSizeInMBPerServer` relative to `num_ec_blocks`.\n\n## Notes\n\n- The affinity signal only benefits repeated-media workloads; the README states this.\n- Benchmarking the guide with and without the plugins is tracked separately and will follow.\n- End-to-end encoder-cache affinity in E/PD depends on the router-side fix in llm-d/llm-d-router#2627 (without it the scorer scores 0). This guide change is the config/docs half.", "url": "https://github.com/llm-d/llm-d/pull/2408"}
{"number": 2470, "title": "Fix: Align AWS GPT-OSS-120B prefill and decode at TP2", "state": "open", "created_at": "2026-09-10T06:49:04Z", "updated_at": "2026-09-25T00:29:14Z", "closed_at": null, "labels": ["needs-rebase"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "\r\nFollow-up to #2442.\r\n\r\nThe AWS SGLang prefill overlay duplicated shared settings from `base/patch-prefill.yaml`, including the SGLang arguments and GPU resources. Those overrides left AWS prefill at TP=4 and 4 GPUs after the base configuration moved to TP=2.\r\n\r\nRemove the duplicated AWS overrides so prefill inherits the shared base configuration, including TP=2 and 2 GPU requests/limits.\r\n\r\n\r\n", "url": "https://github.com/llm-d/llm-d/pull/2470"}
{"number": 2271, "title": "add LMetric routing policy", "state": "open", "created_at": "2026-08-13T05:43:30Z", "updated_at": "2026-09-25T00:27:43Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Add LMetric https://arxiv.org/html/2603.15202v1#S5.SS1 as an optional policy for precise-prefix-cache-routing and add a router values overlay that replaces the default scoring profile with lmetric-scorer.\r\n\r\nAlso update the XPU example to use Qwen3-32B with FP8 quantization and TP=2 so it matches the guide's 32B baseline model.\r\n\r\nDepends on https://github.com/llm-d/llm-d-router/pull/2351.", "url": "https://github.com/llm-d/llm-d/pull/2271"}
{"number": 2580, "title": "docs(wide-ep): add observability and troubleshooting section", "state": "open", "created_at": "2026-09-24T23:45:32Z", "updated_at": "2026-09-24T23:45:32Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Part of #1982 (Phase 3, wide expert parallelism).\n\nAdds an Observability & Troubleshooting section to the wide expert parallelism guide, following the P/D, optimized-baseline and tiered prefix cache sections.\n\n- `guides/wide-ep/README.md`: new step 4 with the signals specific to this path (per DP rank load and KV cache, requests scheduled per rank port, prefill and decode balance, NIXL transfer health, DisaggregatedSet rollout gating) and four common failure modes.\n- `docs/operations/observability/promql.md`: adds a Wide Expert Parallelism query table.\n- `docs/well-lit-paths/foundations/wide-expert-parallelism.md`: adds an Observability paragraph that links to the guide section.\n\nThe queries rely on two labels this guide's own monitoring overlay produces. Prometheus Operator sets `endpoint` to the scraped port name, so each DP rank (`rank0` to `rank7`) is separable, and it sets `job` to `<namespace>/<PodMonitor name>`, so the prefill and decode roles separate without matching on pod names. DeepEP dispatch and combine expose no Prometheus metrics, so the section points at the startup logs for those failures instead.\n\nDocs only. Checked that the new links and anchors resolve.", "url": "https://github.com/llm-d/llm-d/pull/2580"}
{"number": 2493, "title": " guides/wide-ep-lws: add AMD/ROCm MoRI wide-EP 2P2D recipe for DeepSeek-V3", "state": "open", "created_at": "2026-09-14T19:46:45Z", "updated_at": "2026-09-24T23:27:15Z", "closed_at": null, "labels": ["needs-rebase"], "comments_count": null, "is_pr": true, "merged_at": null, "body": " Generic wide-ep guides for running an inference with wide expert parallelism using leader-worker deployment.\r\n DP=EP=16, TP=1 over two pods per role; MoRI-EP for the expert all2all and\r\n MoRI-IO WRITE mode for KV.", "url": "https://github.com/llm-d/llm-d/pull/2493"}
{"number": 2579, "title": "calibration: add costModel fit for the p2p-source-producer", "state": "open", "created_at": "2026-09-24T22:41:34Z", "updated_at": "2026-09-24T22:46:41Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fits prefill, transfer, transferFixedMs, sourceWaitMs and requeueMs from the minCachedTokenDelta crossover table plus a loaded fixed-delta run (inference-perf per-request output joined to the EPP p2p decision log), and warns when the loaded run cannot pin down the fixed costs.", "url": "https://github.com/llm-d/llm-d/pull/2579"}
{"number": 2516, "title": "[Do not marge] Dev Image", "state": "open", "created_at": "2026-09-17T03:01:32Z", "updated_at": "2026-09-24T22:13:48Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "", "url": "https://github.com/llm-d/llm-d/pull/2516"}
{"number": 2339, "title": "CI - strip out llm-d cuda images + deepep v2", "state": "closed", "created_at": "2026-08-20T17:11:24Z", "updated_at": "2026-09-24T20:30:32Z", "closed_at": "2026-09-24T20:30:32Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T20:30:32Z", "body": "Addresses part of: https://github.com/llm-d/llm-d/issues/2338. This currently just represents stripping out the cuda images. I need to make the guide changes to use Deepep HTv2\r\n", "url": "https://github.com/llm-d/llm-d/pull/2339"}
{"number": 2302, "title": "[SGLang] Add Support multimodal optimized baseline", "state": "open", "created_at": "2026-08-15T00:06:38Z", "updated_at": "2026-09-24T20:13:54Z", "closed_at": null, "labels": ["needs-rebase"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "**Description**\r\nAdd support for multimodal optimized baseline with benchmarks. With llm-d, we see better TTFT and higher throughput compared to a k8s loadbalancer service.\r\n\r\n**Validations**\r\nRan the llm-d-benchmark multimodal optimized baseline benchmark for images (without the video part) and added the results to this PR. peakPrefillThroughput and affinityThreshold are set differently for SGLang based on the calibration and benchmarks. \r\n\r\n**Related issues:**\r\n\r\n https://github.com/llm-d/llm-d/issues/2210  #2174", "url": "https://github.com/llm-d/llm-d/pull/2302"}
{"number": 2349, "title": "Guides definition", "state": "closed", "created_at": "2026-08-21T20:17:19Z", "updated_at": "2026-09-24T20:11:04Z", "closed_at": "2026-09-24T20:11:04Z", "labels": ["lifecycle/stale"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T20:11:04Z", "body": "**Status:** draft for discussion\r\n**Scope:** everything under `guides/`\r\n**Audience:** guide authors, guide owners, the leading team, and users deciding how much to trust a guide", "url": "https://github.com/llm-d/llm-d/pull/2349"}
{"number": 2366, "title": "Slack notification workflow", "state": "closed", "created_at": "2026-08-24T17:13:09Z", "updated_at": "2026-09-24T20:09:38Z", "closed_at": "2026-09-24T20:09:38Z", "labels": ["lifecycle/rotten"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T20:09:38Z", "body": "## What\r\n\r\nPosts the result of every **scheduled** nightly to the Slack channel of the SIG that owns it, from within the repo instead of via the GitHub app for Slack.\r\n\r\nCloses #1548.\r\n\r\n## Why\r\n\r\nThe GitHub app for Slack (`/github subscribe`) hit two limits we could not work around: it only supports **one workflow subscription per channel** (so `#sig-router`, which owns 16 nightlies, could never be covered), and it **also fired on manual `workflow_dispatch` runs**.\r\n\r\n## How\r\n\r\n```\r\n.github/slack-channels.yaml          <- source of truth (keyed by workflow FILE name)\r\n        |  scripts/sync-slack-channels.py --fix  (generates the `workflows:` list)\r\n        v\r\n.github/workflows/notify-slack-nightly.yaml   <- GENERATED list, do not hand-edit\r\n        |  at runtime\r\n        v\r\n.github/scripts/notify-slack-nightly.py       <- routes on workflow_run.path\r\n```\r\n\r\nTwo identifiers, deliberately: the **trigger** must use display names because `workflow_run` matches on nothing else, which makes it the part that breaks *silently* — rename a nightly and notifications just stop, indistinguishable from a quiet night. Generating that list turns the rename red in the same PR. **Runtime routing** uses the workflow *file* name, which is stable, so a rename can only stop notifications, never misroute them.\r\n\r\nA nightly in neither `channels` nor `skip` fails the new `sync-slack-channels` pre-commit hook — the only place the mistake can be caught, since an unrouted nightly is also absent from the trigger and therefore never fires at all.\r\n\r\n**Routing**: 40 nightlies across 7 channels, 4 explicitly skipped with a reason (= all 44 `nightly-*.yaml`). `#sig-router` 16, `#sig-kv-disaggregation` 7, `#llm-d-intel` 6, `#sig-pd-disaggregation` 5, `#sig-autoscaling` 3, `#sglang-collab` 2, `#fast-model-actuation` 1. Note SGLang and Intel XPU route by engine/accelerator rather than by guide, because that is who acts on a failure. Run `python scripts/sync-slack-channels.py --audit` for the full table.\r\n\r\n## Decisions worth a look in review\r\n\r\n- **Only `schedule` runs notify** — the whole point of the change. The filter must live in the job's `if:`; `workflow_run` cannot filter on the upstream run's own event.\r\n- **Re-runs of a scheduled run DO notify**, marked `(attempt N)`: when a SIG re-runs a nightly that failed on a transient cluster error, that result is the current truth about the guide.\r\n- **`cancelled` / `skipped` do not notify.** Every nightly sets `cancel-in-progress`, so dispatching one mid-flight cancels the previous run — not news for a channel.\r\n- **The guide's result comes from the `nightly` jobs, not the run conclusion.** `update-badge` runs with `if: always()`, so a green test whose badge push failed surfaces as a failed *run*; reporting that verbatim would tell a channel its guide is broken when it is not. That case gets its own `test passed, update-badge failed` message.\r\n- **Unrouted at runtime → `#llm-d-ci-alerts` + warning, not a failed job.** A red notifier is a second alert channel nobody watches.\r\n- Slack action **pinned by SHA**; `permissions` limited to `contents: read` + `actions: read`, which matters because a `workflow_run` workflow gets secrets and a privileged token even when the upstream run had neither.\r\n\r\n## Files\r\n\r\nNew: `.github/slack-channels.yaml`, `scripts/sync-slack-channels.py` (`--check`/`--fix`/`--audit`, modelled on `sync-nightly-matrix.py` and reusing `matrix_common.py`), `.github/workflows/notify-slack-nightly.yaml`, `.github/scripts/notify-slack-nightly.py`.\r\n\r\nModified: `.pre-commit-config.yaml` (new hook), `.github/workflows/README.md` (new \"Slack notifications\" section + a third step in \"Adding a new guide\").\r\n\r\nNo nightly workflow is touched, and nothing is needed in `llm-d-infra`.\r\n\r\n## Testing\r\n\r\nValidated against real runs (success, failure, cancelled), including `32211600170`, which renders identically to what the GitHub app posted for it:\r\n\r\n```bash\r\nGH_TOKEN=$(gh auth token) python .github/scripts/notify-slack-nightly.py \\\r\n    --repo llm-d/llm-d --run-id <run-id> --dry-run\r\n```\r\nA real test can be seen in `#llm-d-ci-alerts` where the app has posted a filing workflow using:\r\n```\r\n  GH_TOKEN=$(gh auth token) .venv/bin/python .github/scripts/notify-slack-nightly.py \\\r\n    --repo llm-d/llm-d --run-id 32737554939 --channel '#llm-d-ci-alerts' --json \\\r\n  | curl -s -X POST https://slack.com/api/chat.postMessage \\\r\n      -H \"Authorization: Bearer $SLACK_BOT_TOKEN\" \\\r\n      -H 'Content-type: application/json; charset=utf-8' \\\r\n      --data-binary @- | jq '{ok, error, channel}'\r\n```\r\nPlus 25 unit checks over the pure functions, all five guardrail failure modes verified to fail actionably and be fixed by `--fix`.\r\n\r\n## Required before this takes effect\r\n\r\nNeeds Slack + repo admin; full checklist with an app manifest is in `.github/workflows/README.md`.\r\n\r\n- [x] Create and install the Slack app with the **`chat:write`** bot scope\r\n- [x] Create `#llm-d-ci-alerts` (fallback channel)\r\n- [x] `/invite @llm-d-ci` in all 8 channels — skipping this is the likely first-deploy failure (`not_in_channel`)\r\n- [x] Add the `SLACK_BOT_TOKEN` repository secret\r\n\r\n## Follow-ups\r\n\r\nThe 3 multimodal-serving nightlies need a SIG owner and a channel; they sit in `skip` until then. `nightly-build-image` is unrouted by design — happy to point it at a CI channel if reviewers prefer.\r\n\r\n@llm-d/release-crew ", "url": "https://github.com/llm-d/llm-d/pull/2366"}
{"number": 2385, "title": "[Helper] Creates a Helper File for llm-d-planner", "state": "open", "created_at": "2026-08-26T14:57:11Z", "updated_at": "2026-09-24T20:08:53Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "# Summary\r\n\r\n- Creates initial documentation for `llm-d-planner` for interested users to begin _planning_ workloads for their desired use case using `llm-d`\r\n\r\n- Provides an overview, general installation, quick start, and capabilities that `llm-d-planner` will bring to the `llm-d` ecosystem", "url": "https://github.com/llm-d/llm-d/pull/2385"}
{"number": 2410, "title": "fix: align modelexpress-p2p PROVIDER_NAME default with optimized-baseline", "state": "open", "created_at": "2026-08-31T16:17:48Z", "updated_at": "2026-09-24T20:05:37Z", "closed_at": null, "labels": ["needs-rebase"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n`guides/modelexpress-p2p/guide.yaml` defaulted `PROVIDER_NAME` to `gke`, while `guides/optimized-baseline/guide.yaml` defaults it to `none` (per #2260). Both guides pass this value to the same `--set provider.name=` flag on the router gateway install, so the two guides documented conflicting defaults for the same knob.\n\nThe `gke` default was actively wrong for this guide's own default path: `modelexpress-p2p`'s `INFRA_PROVIDER` defaults to `coreweave`, not `gke`, so a user following the guide's defaults would apply GKE-only router resources on a non-GKE cluster, which fail to apply.\n\n## Changes\n\n- `guides/modelexpress-p2p/guide.yaml`: `PROVIDER_NAME` default changed from `gke` to `none`, matching `optimized-baseline`.\n- `guides/modelexpress-p2p/README.md`: re-rendered via `scripts/guide.py render` to reflect the new default, and added the same `[!IMPORTANT]` tradeoff note that `optimized-baseline` has, explaining that the `none` default renders no provider-specific resources (e.g. no `HealthCheckPolicy` on GKE) and that users should override `PROVIDER_NAME` to match their deployed gateway.\n\nFixes #2330\n\n## Test plan\n\n- [x] `python scripts/guide.py check guides/modelexpress-p2p` passes\n- [x] `python scripts/guide.py render guides/modelexpress-p2p --check` confirms README is up to date with guide.yaml\n- [x] `python -m pytest scripts/tests/` passes (27 passed)", "url": "https://github.com/llm-d/llm-d/pull/2410"}
{"number": 2474, "title": "feat(observability): wire metric-to-trace navigation for EPP exemplars", "state": "open", "created_at": "2026-09-11T08:01:05Z", "updated_at": "2026-09-24T20:03:10Z", "closed_at": null, "labels": ["observability"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Closes #2462.\r\n\r\nThe EPP attaches trace IDs to `llm_d_epp_request_duration_seconds` as Prometheus exemplars (llm-d/llm-d-router#2774), but nothing in the recipe made them usable. Three things were missing, and none of them error when absent - the exemplar just quietly isn't there:\r\n\r\n- `enableFeatures: [exemplar-storage]` - without it Prometheus parses the exemplar off the scrape and drops it\r\n- a Jaeger datasource in Grafana - the tracing script installs Jaeger, the prometheus script only registered Prometheus, so the exemplar had nowhere to point\r\n- `exemplarTraceIdDestinations` on the Prometheus datasource - without it `trace_id` renders as plain text instead of a link\r\n\r\nThe values file is written twice, once per mode, so all three go into both branches. The TLS branch already wrote its own `jsonData`; both settings are now built into a single block, because two `jsonData` keys under one datasource is valid YAML in which one silently wins.\r\n\r\nJaeger is optional and its namespace is chosen at install time, so the link is wired only when a `jaeger-collector` service is found, with `TRACING_NAMESPACE` to override.\r\n\r\nThe only dashboard change is `\"exemplar\": true` on the P99 query of the existing latency panel. All three quantiles read the same buckets, so enabling them all would draw the same points three times.\r\n\r\n### Verifying\r\n\r\n`scripts/test-prometheus-exemplar-config.sh` runs the real installer against stubbed `kubectl`/`helm` - no cluster needed - and asserts on the values file helm would actually receive. 43 assertions over six configurations: no tracing installed, central mode, central + TLS, individual mode, explicit `TRACING_NAMESPACE`, and a `TRACING_NAMESPACE` holding no Jaeger.\r\n", "url": "https://github.com/llm-d/llm-d/pull/2474"}
{"number": 2482, "title": "fix(scripts): lint scripts on RUN continuation lines", "state": "closed", "created_at": "2026-09-12T11:31:46Z", "updated_at": "2026-09-24T20:02:18Z", "closed_at": "2026-09-24T20:02:18Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #2481\n\n`find_script_runs` only scanned the line starting with `RUN`, so scripts invoked after `RUN --mount=... \\` were never checked — 10 of 12 in `Dockerfile.cuda`. Follow-up noted in #2416.\n\n- Join `\\` continuations (as `DockerfileParser.parse` already does) and report each script once per `RUN`.\n- Drop the `GDRCOPY_PREFIX` header line from `build-gdrcopy.sh`; the script never reads it and no Dockerfile declares it. Surfaced by the fixed check.\n- Regression test in `scripts/tests/test_lint_dockerfile_envvars.py`.\n\n`python3 -m pytest scripts/tests/`: 38 passed, 1 skipped; the new test fails on `main`.\n\nRunning the linter over all Dockerfiles still reports `UCCL_DEVICE` until #2416 lands (Optional-block false positive fixed there). Happy to rebase once it merges.\n\nRelated: #2445 also edits `find_script_runs` (inline `VAR=x /script.sh` assignments) but still scans line by line, so it does not cover this. The two changes are independent; whichever lands second needs a trivial rebase.\n", "url": "https://github.com/llm-d/llm-d/pull/2482"}
{"number": 2485, "title": "docs(tiered-prefix-cache): add observability and troubleshooting section", "state": "closed", "created_at": "2026-09-13T07:01:51Z", "updated_at": "2026-09-24T20:01:37Z", "closed_at": "2026-09-24T20:01:37Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T20:01:37Z", "body": "Part of #1982 (Phase 2, tiered prefix cache).\n\nAdds an Observability & Troubleshooting section to the tiered prefix cache guide, following the P/D and optimized-baseline sections.\n\n- `guides/tiered-prefix-cache/README.md`: new step 4 with the key signals for this path (offload tier hit rate, store/load volume and speed, allocation failures, per-tier EPP prefix index, TTFT) and four common failure modes. The verification step now uses `vllm:kv_offload_store_bytes_total` instead of the deprecated `vllm:kv_offload_total_bytes`.\n- `docs/operations/observability/metrics.md`: adds the vLLM `OffloadingConnector` and SGLang HiCache metrics, which were not documented.\n- `docs/operations/observability/promql.md`: adds a Tiered Prefix Cache query table.\n- `docs/well-lit-paths/foundations/tiered-prefix-cache.md`: adds an Observability paragraph that links to the guide section.\n\nMetric names are checked against source at the versions this guide deploys: vLLM v0.26.0 (`offloading/metrics.py`, `kv_offload/cpu/spec.py`), SGLang v0.5.16 (`observability/metrics_collector.py`, exported with the `sglang_` prefix per sgl-project/sglang#15758), LMCache `observability.py`, and llm-d-router `approximateprefix/indexer.go` (the index size gauge is summed across pods).\n\nNot covered here: the TPU and MooncakeStore paths, and the per-tier `vllm:kv_offload_tiering_*` read/write counters, which v0.26.0 does not have.\n\nDocs only. Checked that the new relative links and anchors resolve.", "url": "https://github.com/llm-d/llm-d/pull/2485"}
{"number": 2487, "title": "feat(observability): add GKE TPU dashboard", "state": "closed", "created_at": "2026-09-14T08:19:39Z", "updated_at": "2026-09-24T20:00:47Z", "closed_at": "2026-09-24T20:00:47Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T20:00:47Z", "body": "## Summary\r\n\r\nThe TPU PodMonitor added in #2190 provides a scrape recipe, but llm-d has no dashboard for inspecting the collected hardware metrics. This PR adds a GKE TPU overview dashboard and documentation for enabling it and diagnosing missing data.\r\n\r\n- Show exporter scrape health, TensorCore utilization, HBM bandwidth utilization, and runtime duty-cycle and memory gauges when available.\r\n- Reuse the shared dashboard loader and `${DS_PROMETHEUS}` convention. The dashboard selects a single exporter job using an exact match and filters hardware metrics with `make=\"cloud-tpu\"`. It preserves per-instance and per-accelerator series instead of aggregating them. Panels show `No data` when a metric is unavailable.\r\n- Document metric names, units, label scope, availability checks, and compatibility limits in the TPU recipe and observability reference.\r\n\r\nThe metric names and labels are based on the GKE device-plugin metrics consumed by [Ray's TPU metrics parser](https://github.com/ray-project/ray/blob/be4889928d3a396d7a1476d1bac3288dac72f9ab/python/ray/dashboard/modules/reporter/reporter_agent.py). The utilization metrics and their `make=\"cloud-tpu\"` labels also appear in a [GKE v6e sample shared in a Ray issue](https://github.com/ray-project/ray/issues/57829).\r\n\r\nICI, PCIe, VPU, and kube-state-metrics topology recording rules remain follow-up work.\r\n\r\nRelates to #2157\r\n\r\n## Validation\r\n\r\n- `make verify` passed.\r\n- Markdown lint, JSON parsing, local documentation links, and `git diff --check` passed.\r\n- Local Prometheus 3.5.0 `promtool` checks passed: 34 dashboard query cases and six label-isolation cases using synthetic data. Coverage includes missing metrics, scrape failures, multiple instances, other component jobs, and foreign/missing `make` labels.\r\n- As a negative control, replacing the exact job matcher with a regex matcher caused the label-isolation checks to fail.\r\n\r\nThe query checks were run locally; this PR adds no test tooling or CI dependencies. No live GKE/TPU deployment validation was available. The synthetic checks validate query behavior, not hardware metric availability.\r\n", "url": "https://github.com/llm-d/llm-d/pull/2487"}
{"number": 2505, "title": "Hardware support: Add Support for BR166M ", "state": "open", "created_at": "2026-09-16T07:57:36Z", "updated_at": "2026-09-24T19:56:40Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "I've added the support for BR166M in both optimized-baseline and pd-disaggregation\r\n\r\nhere's the pd-disaggregation launched by this PR with TP=4\r\n<img width=\"1788\" height=\"1406\" alt=\"c69ef1c2-b0d7-4a7f-a15c-af932c82d9d6\" src=\"https://github.com/user-attachments/assets/dc764113-4872-49ae-af22-fcf7768496ea\" />\r\n\r\nthe deployments:\r\n<img width=\"2330\" height=\"316\" alt=\"4fd8e63b-c6a0-4964-87c7-92a0edb04a9b\" src=\"https://github.com/user-attachments/assets/806169d4-1cc2-4049-b348-3a96e0b09f54\" />\r\n\r\n", "url": "https://github.com/llm-d/llm-d/pull/2505"}
{"number": 2519, "title": "docs: add AI infrastructure learning resource", "state": "open", "created_at": "2026-09-17T09:43:18Z", "updated_at": "2026-09-24T19:56:23Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## What does this PR do?\n\nAdds an introductory AI infrastructure learning resource to the Getting Started page.\n\nThe page now includes an embedded video covering GPUs, vLLM, and llm-d, along with a direct YouTube link for users who prefer to watch on YouTube.\n\n## Why is this change needed?\n\nThe Getting Started page is the natural entry point for users who are new to llm-d and distributed LLM inference. Providing an introductory learning resource helps readers understand the surrounding AI infrastructure concepts before moving on to deployment and Well-Lit Paths.\n\nThe video provides background on GPUs, vLLM, and llm-d, making the documentation more accessible to newcomers.\n\n## How was this tested?\n\n- [x] Manual testing performed (`npm run serve`) on the `llm-d.github.io` website repo with synced docs\n\n## Checklist\n\n- [x] Commits are signed off (`git commit -s`) per [DCO](../DCO)\n- [x] Documentation updated", "url": "https://github.com/llm-d/llm-d/pull/2519"}
{"number": 2520, "title": "[Do not merge] Dev image build", "state": "open", "created_at": "2026-09-17T12:06:29Z", "updated_at": "2026-09-24T19:55:10Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "", "url": "https://github.com/llm-d/llm-d/pull/2520"}
{"number": 2553, "title": "Add COMPONENTS.md: core and ecosystem component roles", "state": "closed", "created_at": "2026-09-22T15:53:58Z", "updated_at": "2026-09-24T19:54:22Z", "closed_at": "2026-09-24T19:54:22Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T19:54:22Z", "body": "## What\r\n\r\nAdds `COMPONENTS.md`, defining two classes of component the project maintains:\r\n\r\n- **Core** - on the inference request path; a failure or regression there\r\n  degrades or breaks live inference. Deliberately small, carries production\r\n  reliability expectations.\r\n- **Ecosystem** - maintained and first-class, but not needed to serve a\r\n  request (planning, benchmarking, analysis, simulation, adjacent services).\r\n\r\nThe role is orthogonal to graduation: a component moves from\r\n`llm-d-incubation` into the main `llm-d` org and takes on one of the two roles\r\nat that point. An ecosystem component can later be promoted to core if it moves\r\nonto the request path and meets the higher bar.\r\n\r\n## Why\r\n\r\nToday \"in the main org\" is a single undifferentiated signal with no written\r\ngraduation criteria, so every graduation becomes an argument. This gives\r\ncommitted-but-off-path components (starting with `llm-d-planner`) a real home\r\nwithout implying request-path guarantees, while keeping the core set small and\r\nhigh-bar to protect the request path.\r\n\r\n## Notes\r\n\r\n- Graduation criteria are grounded in the existing principles in `PROJECT.md`\r\n- Includes a starter roster. The roster is the source of truth and changes by\r\n  pull request.", "url": "https://github.com/llm-d/llm-d/pull/2553"}
{"number": 2530, "title": "fix(makefile): restore the leading tab on the uninstall-docker completion message", "state": "open", "created_at": "2026-09-18T05:45:49Z", "updated_at": "2026-09-24T19:54:08Z", "closed_at": null, "labels": ["needs-code-signing"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`Makefile:213` starts at column zero, so make does not read it as part of the `uninstall-docker` recipe.\n\nThe line contains a colon, in `Remove alias if set:`, so make reads it as a rule rather than reporting a missing separator. The file parses with no warning.\n\n## Two consequences\n\nThe recipe ends at line 212, so the completion message never prints:\n\n```\n$ make -n uninstall-docker | tail -1\ndocker stop llm-d-container && docker rm llm-d-container\n```\n\n`install-docker` at `:205`, `uninstall-k8s` at `:243` and `uninstall-openshift` at `:272` all print theirs. This is the only one that does not.\n\nAnd make defines a target out of the text:\n\n```\n$ make -p -n | grep '^@echo'\n@echo: unalias llm-d\"\n```\n\n## After\n\n```\n$ make -n uninstall-docker | tail -1\necho \"docker uninstallation complete. Remove alias if set: unalias llm-d\"\n\n$ make -p -n | grep -c '^@echo'\n0\n```\n\n## The repository already states the rule\n\n`.editorconfig`, added in #2103:\n\n```\n# Makefiles require tabs\n[Makefile]\nindent_style = tab\n```\n\n## Scope\n\nOne character. A scan of the whole file for a column-zero line inside a recipe block returns line 213 and nothing else. The only other hits were `:302` to `:311`, the space-indented prerequisite continuation under `check-tools`, which is correct as written.\n\n`make uninstall-docker` was run with `-n` only. Running it for real stops and removes a container.\n\n## Duplicate search\n\n`gh issue list --state all` for `Makefile tab` and for `uninstall-docker`, and `gh pr list --state all` for `Makefile`, return nothing covering this.\n\nOpen PR #2339 edits this same line, to swap `$(PROJECT_NAME)` for `$(IMAGE_REGISTRY_REPO)`, and keeps the column-zero start. It is `CONFLICTING` today. If it lands first the tab still needs adding, and if this lands first that PR needs a one-line rebase.\n", "url": "https://github.com/llm-d/llm-d/pull/2530"}
{"number": 2529, "title": "fix(smoke-test): keep the real /v1/models status in the summary table", "state": "open", "created_at": "2026-09-18T05:13:08Z", "updated_at": "2026-09-24T19:53:30Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`check_models` discards the status it has just reported. `helpers/smoke-test/healthcheck.sh:184` fails with the real code, and `:185` overwrites it with `000`, which is the sentinel `curl` produces when nothing answered at all.\n\nSo a reachable endpoint returning 503 reads as unreachable in the summary table, while the failure list directly below it shows the true code. Both come from the same variable in the same run:\n\n```\n║  /health          HTTP 503               ║\n║  /v1/models       HTTP 000   count=-     ║\n║  /v1/chat/completions HTTP 503   8     ms   ║\n...\nFailures:\n  - /v1/models returned HTTP 503\n  - /v1/chat/completions returned HTTP 503\n```\n\n`/health` and `/v1/chat/completions` both report 503 correctly. `/v1/models` is the outlier.\n\nEvery other exit from this code keeps the status: `check_health` at `:171`, and `check_models` itself at `:194` and `:210`. This is the only path that loses it.\n\n## The change\n\nReporting `${http_code}` covers both cases, because `curl` already yields `000` when the connection fails. Verified against a 503 server and against a port nothing listens on:\n\n```\n=== 503 server ===\n║  /v1/models       HTTP 503   count=-     ║\n=== nothing listening ===\n║  /v1/models       HTTP 000   count=-     ║\n```\n\nThe sentinel survives where it is real, and the true code survives where there is one.\n\n## Test\n\n`tests/test_healthcheck_failure.py` already stands up a `ThreadingHTTPServer` answering 503 to everything, but asserted only the exit code, the `UNHEALTHY` banner and the word `failed`. It now also asserts the row.\n\n```\n$ python -m pytest helpers/smoke-test/tests/\n1 passed\n\n$ shellcheck -x --severity=warning helpers/smoke-test/healthcheck.sh\n(exit 0)\n```\n\nRevert the script, keep the test, and it fails on the new assertion.\n\n## Not checked\n\n`report_json` at `:373` reads the same `MODELS_RESULT` and is affected identically, but this run did not exercise `-o json`.\n\n## Duplicate search\n\n`gh issue list --state all` and `gh pr list --state all` over `healthcheck.sh`, `smoke-test healthcheck` and `v1/models HTTP 000` return only #2401, which fixed the subshell counter accounting in this same file and merged on 2026-08-30, and #767, the original addition. Nothing covers this.\n", "url": "https://github.com/llm-d/llm-d/pull/2529"}
{"number": 2531, "title": "Hardware Support: add Moore Threads MTT S5000 support", "state": "open", "created_at": "2026-09-18T06:53:54Z", "updated_at": "2026-09-24T19:52:18Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "# Add Moore Threads MTT S5000 vLLM and SGLang support\r\n\r\n## Summary\r\n\r\nAdd community Well Lit Path support for Moore Threads MTT S5000 GPUs.\r\n\r\nThis PR adds both vLLM and SGLang modelserver paths:\r\n\r\n- optimized-baseline colocated deployments;\r\n- P/D disaggregation deployments;\r\n- Moore Threads modelserver image components;\r\n- accelerator documentation and router calibration matrix entries.\r\n\r\nThe Kubernetes device resource used by these overlays is `mthreads.com/gpu`.\r\n\r\n## Changes\r\n\r\n### Common Moore Threads integration\r\n\r\n- Add Moore Threads MTT S5000 to the accelerator support documentation.\r\n- Add Moore Threads paths to the optimized-baseline and P/D disaggregation\r\n  guides.\r\n- Add Moore Threads image components under\r\n  `guides/recipes/modelserver/components/images/`.\r\n- Add Moore Threads entries to the router calibration matrix.\r\n\r\n### vLLM\r\n\r\nAdd `guides/optimized-baseline/modelserver/mthreads/vllm`:\r\n\r\n- serves `Qwen/Qwen3-32B`;\r\n- uses one vLLM Deployment;\r\n- uses tensor parallelism `TP=8`;\r\n- requests eight `mthreads.com/gpu` devices;\r\n- pulls the model from Hugging Face through the `llm-d-hf-token` Secret.\r\n\r\nAdd `guides/pd-disaggregation/modelserver/mthreads/vllm`:\r\n\r\n- single-node compatibility topology on one 8-GPU MTT S5000 host;\r\n- Prefill: `TP=4`, four `mthreads.com/gpu` devices;\r\n- Decode: `TP=4`, four `mthreads.com/gpu` devices;\r\n- uses vLLM `MooncakeConnector`;\r\n- uses Mooncake TCP with MUSA transport;\r\n- sets `kv_role=kv_producer` on Prefill and `kv_role=kv_consumer` on Decode;\r\n- sets `kv_load_failure_policy=fail` so KV transfer failures are visible.\r\n\r\nThe vLLM image component points to:\r\n\r\n```text\r\nregistry.mthreads.com/mcconline/inference/vllm:v0.28.0-ph1-5.2.0-torch2.11.0.post1-20260915\r\n```\r\n\r\n### SGLang\r\n\r\nAdd `guides/optimized-baseline/modelserver/mthreads/sglang`:\r\n\r\n- serves `DeepSeek-V4-Flash-0731-FP8-mt`;\r\n- uses one SGLang Deployment;\r\n- uses `TP=8` and `EP=8`;\r\n- requests eight `mthreads.com/gpu` devices;\r\n- uses the Moore Threads `mthreads` RuntimeClass;\r\n- mounts `/data` for the local model path\r\n  `/data/models/DeepSeek-V4-Flash-0731-FP8-mt`;\r\n- carries the MUSA/SGLang environment required by this vendor image.\r\n\r\nAdd `guides/pd-disaggregation/modelserver/mthreads/sglang/base`:\r\n\r\n- two-node topology, one 8-GPU Prefill pod plus one 8-GPU Decode pod;\r\n- Prefill and Decode each request eight `mthreads.com/gpu` devices;\r\n- Prefill and Decode each request `rdma/ib`;\r\n- uses SGLang `--disaggregation-transfer-backend mooncake`;\r\n- uses Mooncake bootstrap port `8998`;\r\n- requires 200/400 Gb IB devices visible inside the pod;\r\n- keeps the overlay non-privileged and uses `IPC_LOCK` plus Unconfined seccomp.\r\n\r\nThe SGLang image component points to:\r\n\r\n```text\r\nregistry.mthreads.com/devtech/sglang-dsv4:1.0\r\n```", "url": "https://github.com/llm-d/llm-d/pull/2531"}
{"number": 2537, "title": "feat(wide-ep-lws): add Qwen3-235B Intel XPU deployment guide", "state": "open", "created_at": "2026-09-20T08:50:37Z", "updated_at": "2026-09-24T19:51:46Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Add an Intel XPU deployment for aggregated `Qwen/Qwen3-235B-A22B` serving using\r\nvLLM, in the WideEP guide with TP8 per node and EP16 spanning 16 XPUs across two nodes.\r\n\r\n## Topology\r\n\r\nThis configuration deploys Qwen3-235B with **EP16 across two nodes**:\r\n\r\n| Setting | Value |\r\n|---|---:|\r\n| Nodes / LWS pods | 2 |\r\n| XPUs per node | 8 |\r\n| Total XPUs | 16 |\r\n| Tensor parallel size | 8 |\r\n| Data parallel size | 2 |\r\n| Expert parallel size | 16 |\r\n| RDMA NICs per node | 1 |\r\n\r\nEach node runs one vLLM instance using TP8 across its eight local XPUs.\r\nThe two instances form DP2 across the two nodes. With expert parallelism\r\nenabled, the model's experts are distributed across all 16 XPU ranks,\r\nforming an **EP16 cross-node topology**.\r\n\r\n```text\r\n                       llm-d Router / EPP\r\n                               |\r\n                        Service port 8200\r\n                               |\r\n              +----------------+----------------+\r\n              |                                 |\r\n          Node 0 / LWS leader              Node 1 / LWS worker\r\n          DP rank 0                        DP rank 1\r\n          TP8: XPU 0-7                     TP8: XPU 8-15\r\n          1 RDMA NIC                       1 RDMA NIC\r\n              |                                 |\r\n              +------ EP16 over XCCL/OFI -------+\r\n                    allgather_reducescatter\r\n```\r\n\r\nIntel XPUs and RDMA network devices are allocated through Kubernetes\r\nDRA using the gpu.intel.com and dranet-rdma DeviceClasses.\r\n\r\nChanges\r\n- Add the Qwen3-235B LeaderWorkerSet model server manifests.\r\n- Configure vLLM with TP=8, DP=2, FP8 quantization, and expert\r\nparallelism.\r\n- Configure XCCL over OFI verbs for cross-node communication.\r\n- Add DRA claims for eight Intel XPUs and one RDMA NIC per pod.\r\n- Add an aggregated-serving EPP configuration targeting port 8200.\r\n- Document the commands required to deploy the Qwen3-235B model and\r\nits router configuration.\r\n- Assign stable qwen-0 and qwen-1 hostnames to avoid ambiguous\r\nhostname-prefix matching during oneCCL local-rank discovery.", "url": "https://github.com/llm-d/llm-d/pull/2537"}
{"number": 2551, "title": "fix: resolve typos linter false positives for AIMD and opt-in", "state": "closed", "created_at": "2026-09-22T10:51:36Z", "updated_at": "2026-09-24T19:50:02Z", "closed_at": "2026-09-24T19:50:02Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T19:50:01Z", "body": "fixes: #2479 \r\n- Add `aimd` to `_typos.toml` as a valid domain term (Additive Increase Multiplicative Decrease)\r\n- Rename `optin` step to `opt-in` in [kueue-rebalancing/guide.yaml](https://github.com/llm-d/llm-d/blob/main/guides/workload-autoscaling/kueue-rebalancing/guide.yaml) and [kueue-rebalancing/README.md](https://github.com/llm-d/llm-d/blob/main/guides/workload-autoscaling/kueue-rebalancing/README.md)", "url": "https://github.com/llm-d/llm-d/pull/2551"}
{"number": 2544, "title": "fix(build): remove invalid buildx command prefix", "state": "open", "created_at": "2026-09-21T07:57:18Z", "updated_at": "2026-09-24T19:48:39Z", "closed_at": null, "labels": ["Image builds"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "The Docker branch of the buildah-build recipe runs as one continued shell block, so the leading '-' before docker buildx create was passed to the shell as a command name instead of acting as a Make error-ignore prefix. The existing || true hid that error, and docker buildx use image-builder then failed in a clean environment because the builder was never created.\r\n\r\nRemove the invalid '-' while retaining || true, allowing the buildx create command to run and preserving the existing tolerance for an already-existing builder.\r\n\r\nTests:\r\n- make -n buildah-build BUILDER=docker\r\n- git diff --check\r\n- Verified the generated recipe contains docker buildx create followed by docker buildx use without the invalid command prefix.", "url": "https://github.com/llm-d/llm-d/pull/2544"}
{"number": 2563, "title": "fix(wide-ep): make Intel XPU decode manifest actually cross-node wide-EP", "state": "open", "created_at": "2026-09-23T05:24:37Z", "updated_at": "2026-09-24T19:45:06Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Problem\n\n`decode.yaml` shipped with `leaderWorkerTemplate.size: 1` and only a `workerTemplate` (no `leaderTemplate`). Every \"decode\" pod was a fully independent TP+EP-only replica — no pod ever shared a DP/EP group with another pod. Despite the guide's name, expert parallelism was never actually organized across pods/nodes on the Intel XPU path.\n\n## Fix\n\n* `decode.yaml`: split into `leaderTemplate` (HTTP API + routing-proxy sidecar, DP-supervisor rank 0) and `workerTemplate` (`--headless`, exec-based probes). `leaderWorkerTemplate.size` bumped `1 -> 2` (smallest config that exercises real cross-pod EP). `vllm serve` args rewritten to the DP-supervisor pattern. Added `podAntiAffinity` (leader/workers on distinct hosts, scoped to this guide) and `rolloutStrategy` (avoids surge-based rollout deadlocks on capacity-constrained clusters).\n* `prefill.yaml`: added the same `podAntiAffinity`/`rolloutStrategy`. `size` stays `1` — prefill scales via independent replicas, not a cross-node EP group; its own wide-EP is unvalidated and documented as such inline.\n* `kustomization.yaml`: added a labels-transformer path for the new `leaderTemplate`.\n* `router/xpu.values.yaml`: EPP endpoint selection now also requires `leaderworkerset.sigs.k8s.io/worker-index: \"0\"`, since headless decode workers share every other label with the leader but don't serve HTTP on port 8000.\n* `README.md`: updated the Intel XPU config table and added notes on the fix and current EPLB status.\n\n## Validation\n\nValidated on a real 4-node/16-XPU Intel cluster: EP world size > 1 actually spans pods, correct expert split, cross-node P/D traffic (NIXL), and light concurrent request handling.\n\nEPLB (expert-parallel load balancing) was not enabled as part of this fix; it's left as follow-up work for Intel XPU.\n\n## Known gaps\n\n* EPLB unsupported/unvalidated on Intel XPU (follow-up work).\n* Not tested behind a Gateway/InferencePool front-end (direct Service routing only).\n* Only tested with the small `DeepSeek-V2-Lite-Chat` model, light concurrency only.\n* `guides/wide-ep/monitoring` still assumes GPU-style `rank0..rank7` ports; pre-existing gap, not touched here.\n* Prefill's own cross-node wide-EP (vs. replica scale-out) is unvalidated and out of scope.\n", "url": "https://github.com/llm-d/llm-d/pull/2563"}
{"number": 2564, "title": "chore(router): align guide with latest EPP code", "state": "open", "created_at": "2026-09-23T13:19:29Z", "updated_at": "2026-09-24T19:43:16Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "# Notes\r\n- bump API to  use v1 even v1alpha1 is still acceptable\r\n- move staturationDetelctor under FlowControl\r\n- update flags name from --vllm-port to --model-server-port, --connector to --kv-connevtor, and --secure-proxy to --secur-serving\r\n- update docs for datalayer discovery and cross-replica fileds\r\n- rename coordinator TLS from serer.secure_coorindator to serer.secure_serving\r\n\r\nall ref to https://github.com/llm-d/llm-d-router/commits/main/ ", "url": "https://github.com/llm-d/llm-d/pull/2564"}
{"number": 2566, "title": "Enable cron schedule for multimodal tests", "state": "closed", "created_at": "2026-09-23T18:15:14Z", "updated_at": "2026-09-24T19:42:31Z", "closed_at": "2026-09-24T19:42:30Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T19:42:30Z", "body": "", "url": "https://github.com/llm-d/llm-d/pull/2566"}
{"number": 2572, "title": "P2p hot prefix guide", "state": "open", "created_at": "2026-09-23T22:37:32Z", "updated_at": "2026-09-24T19:40:17Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## p2p-kv-cache-sharing: token-aware placement for document Q&A, and self-describing CPU-tier events\r\n\r\n### Why\r\n\r\nThe guide's document Q&A scenario recommends load-aware + P2P because it beats affinity there. Token-aware placement was never measured. On the guide's own document Q&A profile it beats both existing arms. Separately, the model-server manifest omits `self_describing_kv_events`, which can keep CPU-tier blocks out of the precise index under churn.\r\n\r\n### What changes\r\n\r\n- **Model server:** `self_describing_kv_events: true` in the offloading connector config. Without it, CPU-tier store events carry no tokens or parent hash, and the router can attribute a CPU-tier block only through the mapping learned from the same block's GPU-tier store. Under churn the GPU removal can arrive first, and the CPU copy a peer would pull never enters the index.\r\n- **New EPP arms:** `benchmarking/epp-tokenaware-p2p.yaml` and `benchmarking/epp-tokenaware.yaml`, token-aware placement (in-flight uncached tokens on the precise index) with and without the pull.\r\n- **Report:** `benchmark-results/gpt-oss-120b-docqa-token-aware.md`.\r\n- **Guide text:**\r\n  - a token-aware placement bullet and two configuration table rows;\r\n  - token-aware + P2P recommended for session-pinned workloads;\r\n  - the calibration result over TCP;\r\n  - the self-describing events requirement in Best Practices.\r\n\r\nThe shipped default stays affinity + P2P, because token-aware placement has not been measured on the uniform pool, where affinity leads.\r\n\r\n### Results\r\n\r\nAll runs use the guide's document Q&A profile (llm-d-benchmark#1656 at the pinned commit) through the llm-d-benchmark `v0.7.0` harness, scaled from 16 to 8 pods (96 conversations, 64 in flight). The fleet is 8 x gpt-oss-120b TP=1 on H200, pulls over TCP. Every run starts from empty caches.\r\n\r\n| Placement | Pull | Served req/s | TTFT p50 / p90 ms | TPOT p90 ms | Request latency p50 s |\r\n| --- | --- | --- | --- | --- | --- |\r\n| token-aware | yes | **6.03** | 609 / **7,191** | **33.2** | 5.3 |\r\n| token-aware | no | 5.02 | 1,758 / 7,221 | 45.8 | 8.7 |\r\n| load-aware | yes | 4.26 | 2,993 / 8,882 | 51.2 | 9.0 |\r\n| load-aware | no | 2.75 | 3,555 / 8,840 | 97.4 | 13.9 |\r\n| affinity (default) | yes | 2.75 | 528 / 14,054 | 139.4 | 5.0 |\r\n| affinity (default) | no | 2.66 | 637 / 14,624 | 110.5 | 5.7 |\r\n\r\n**Token-aware placement, with and without the pull, four paired runs each** (chart attached): served throughput +15%, TTFT p50 -26%, TPOT p90 -15%, request latency p50 -26%, TTFT p90 -2%. TTFT p90 is set by each conversation's cold 48K-token first turn, which no pull can serve.\r\n\r\n### Follow-up\r\n\r\nA cost-model decision for the pull (llm-d-router `p2p-source-producer` `costModel`, crediting the prefill a pull saves on a loaded pod) matched the fixed-delta rule on TTFT p90 across four paired runs. It made about 30% fewer pulls. It is not referenced here until that router change lands.\r\n\r\n### Test plan\r\n\r\n- [x] `pre-commit run` on all changed files (markdownlint, yamllint, check-yaml)\r\n- [x] `kubectl kustomize` renders both model-server overlays with the new connector setting\r\n- [x] Token-aware arms deployed and measured with the guide's llm-d-benchmark profile (report tables)\r\n\r\n<img width=\"3200\" height=\"832\" alt=\"image\" src=\"https://github.com/user-attachments/assets/c956ed31-bcc5-4285-80b5-3c67a4b051e5\" />\r\n", "url": "https://github.com/llm-d/llm-d/pull/2572"}
{"number": 2578, "title": "Bump guides to LWS v0.11.0", "state": "open", "created_at": "2026-09-24T18:29:46Z", "updated_at": "2026-09-24T18:29:46Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "LWS v0.11.0 is out. This points the guides at it:\n\n- pd-disaggregation DS README: chart `0.11.0`\n- wide-ep README and the four guide kustomization comments: requires `v0.11.0`\n- multi-node doc: `0.9.0` -> `0.11.0`, and adds `--set enableDisaggregatedSet=true`, which the DS-based guides need\n- CI CRD install script (kustomize dry-run): `v0.11.0`\n\nAlso fixes the wide-ep README's description of `enableDisaggregatedSet`: the chart always installs the DS CRD and controller, and the flag adds the validating webhook and DS RBAC. That webhook is where v0.11.0's new DS name-length validation runs.\n", "url": "https://github.com/llm-d/llm-d/pull/2578"}
{"number": 2574, "title": "feat(snapshot): add SGLang wrapper and launcher for Pod Snapshotting [UNTESTED]", "state": "open", "created_at": "2026-09-24T09:30:45Z", "updated_at": "2026-09-24T18:07:57Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "", "url": "https://github.com/llm-d/llm-d/pull/2574"}
{"number": 2577, "title": "refactor(autoscaling): consolidate keda-epp queue and saturation guides", "state": "closed", "created_at": "2026-09-24T15:39:31Z", "updated_at": "2026-09-24T17:34:56Z", "closed_at": "2026-09-24T17:34:56Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T17:34:56Z", "body": "**What type of PR is this?**\r\n\r\n/kind cleanup\r\n\r\n**What this PR does / why we need it**:\r\n\r\nThis is a pure refactor: it consolidates the two overlapping KEDA + EPP autoscaling guides into one and changes no deployed behavior on the default path.\r\n\r\nPreviously `keda-epp-queue` and `keda-epp-saturation` were two near-duplicate guides that differed only in which EPP metric drives scaling. This PR merges them into a single `guides/workload-autoscaling/keda-epp` guide where the scaling signal (queue vs saturation) is a kustomize Component axis with queue as the default, and platform (generic Kubernetes vs OpenShift) is the other axis.\r\n\r\n- The new `keda-epp` guide is built from a trigger-agnostic base plus `trigger-queue`, `trigger-saturation`, and `ocp-auth` kustomize Components, composed into four leaves: `k8s-queue`, `k8s-saturation`, `ocp-queue`, `ocp-saturation`.\r\n- The queue leaves render byte-identical to the old `keda-epp-queue` overlays, so the default path is provably unchanged; the saturation leaves differ only by the shared-base reconciliation and a single consolidated OCP `TriggerAuthentication` name.\r\n- The old `keda-epp-queue` and `keda-epp-saturation` directories are reduced to redirect stubs, and every inbound reference is repointed: the workload-autoscaling index, the autoscaling architecture doc, token-aware, kueue-rebalancing, multi-inference-pool, recipes/autoscaling, and fast-model-actuation-keda.\r\n- `fast-model-actuation-keda` is repointed to the new `keda-epp` `k8s-queue` leaf and its build stays byte-identical.\r\n\r\nVerified locally: kustomize build across all guide overlays, `guide.py render --check` and schema check, and markdownlint plus yamllint on all changed and new files. The queue and fast-model-actuation-keda builds were confirmed byte-identical to the pre-refactor output, and the saturation build diff matches only the enumerated reconciliation.\r\n\r\n**Which issue(s) this PR fixes**:\r\n\r\nPart of #2565 (guide-consolidation epic; this is PR 2 of the series, not a full fix)\r\n\r\n**Release note**:\r\n\r\n```release-note\r\nNONE\r\n```\r\n", "url": "https://github.com/llm-d/llm-d/pull/2577"}
{"number": 2576, "title": "Delete WVA nightly E2E CI", "state": "closed", "created_at": "2026-09-24T15:06:00Z", "updated_at": "2026-09-24T15:29:59Z", "closed_at": "2026-09-24T15:29:59Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T15:29:59Z", "body": "## Summary\nWVA (Workload Variant Autoscaler) is deprecated as of `v0.9.0` with no further releases, and its nightly regression lanes already had scheduling disabled in-repo (`WVA is deprecated`). This PR finishes that by deleting the WVA nightly CI outright:\n\n- Deletes the 3 `nightly-e2e-workload-autoscaling-{amd,cks,ibm}-...yaml` lanes and their 3 `consolidate-status-workload-autoscaling-{amd,cks,ibm}-...yaml` counterparts\n- Deletes `guides/workload-autoscaling/scripts/nightly-deploy-{amd,cks,ocp}.sh` and their unit tests (`scripts/tests/test_nightly_deploy_{amd,cks}.py`)\n- Removes the \"Workload Autoscaling (WVA)\" row from `scripts/matrix_common.py` and regenerates `release/README.md` via `scripts/sync-nightly-matrix.py --fix`\n- Drops the stale WVA status badge from `guides/workload-autoscaling/README.md`\n- Removes a doc reference in `guides/workload-autoscaling/wva/README.md` that pointed at the deleted `nightly-deploy-amd.sh`\n- Removes the stale `/test-nightly wva-*` example from `slash-test-nightly.yaml`\n\n**Not touched:** the WVA guide/docs themselves, and the separate KEDA + EPP Queue autoscaling nightly (`nightly-e2e-workload-autoscaling-keda-epp-ibm-...yaml` and its deploy script/badge/matrix row) — that path uses a different autoscaler and explicitly has no WVA controller involved.\n\n## Test plan\n- [x] `python scripts/sync-nightly-matrix.py --check` passes (matrix in sync)\n- [x] Swept the repo for remaining references to the deleted workflow/script filenames (excluding the kept `-keda-epp-` lane) — none found\n- [x] `pytest scripts/tests/` — no import/collection errors from the removed test files\n- [ ] CI (kustomize dry-run, guides check) green on this PR", "url": "https://github.com/llm-d/llm-d/pull/2576"}
{"number": 2374, "title": "Add deployment guide for Flagos-cuda using vLLM", "state": "closed", "created_at": "2026-08-25T10:53:05Z", "updated_at": "2026-09-24T15:22:28Z", "closed_at": "2026-09-24T15:22:28Z", "labels": ["lifecycle/rotten"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Depends on #1624 for the image(ghcr.io/llm-d/llm-d-flagos-cuda) \r\n\r\nThis PR adds a deployment guide for the flagos-cuda variant. \r\n\r\nSummary of changes:\r\n1. guides/optimized-baseline/modelserver/flagos-cuda/vllm/kustomization.yaml\r\n    - Kustomize overlay modeled after the existing GPU variant\r\n    - Points to the ghcr.io/llm-d/llm-d-flagos-cuda image\r\n    - Adds labels: accelerator-variant: flagos-cuda, accelerator-vendor: nvidia\r\n\r\n2. guides/optimized-baseline/modelserver/flagos-cuda/vllm/patch-vllm.yaml\r\n    - Deployment patch for the decode pods\r\n    - 2 replicas (reduced from 8 since this is a new variant being validated)\r\n    - Same model/resources as GPU variant: Qwen3-32B, TP=2, 2x nvidia.com/gpu\r\n    - Adds VLLM_PLUGINS=fl env var — the one key difference that enables FlagOS\r\n\r\n3. guides/optimized-baseline/README.md \r\n    - Added FlagOS row to the Supported Hardware Backends table\r\n    - Added flagos-cuda to the ACCELERATOR_TYPE options list\r\n\r\nOnce PR #1624 merges (providing the image), users can deploy FlagOS CUDA with:\r\n  ```\r\nexport ACCELERATOR_TYPE=flagos-cuda\r\n  export MODEL_SERVER=vllm\r\n  kubectl apply -n ${NAMESPACE} -k ${REPO_ROOT}/guides/${GUIDE_NAME}/modelserver/${ACCELERATOR_TYPE}/${MODEL_SERVER}/\r\n```", "url": "https://github.com/llm-d/llm-d/pull/2374"}
{"number": 2571, "title": "fix(autoscaling): use plain HTTP for keda-epp generic-k8s prometheus scaler", "state": "closed", "created_at": "2026-09-23T19:56:44Z", "updated_at": "2026-09-24T12:52:02Z", "closed_at": "2026-09-24T12:52:02Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T12:52:02Z", "body": "**What type of PR is this?**\r\n\r\n/kind bug\r\n\r\n**What this PR does / why we need it**:\r\n\r\nThe generic-Kubernetes KEDA autoscaling path in both keda-epp guides was broken. KEDA's prometheus scaler only applies a `TriggerAuthentication` CA when the trigger also sets `authModes` (its `GetAuthConfigs` returns early without it), so the shipped \"CA-only\" trigger silently fell back to the system trust store and failed serving-cert verification with `x509: certificate signed by unknown authority`. This path was never end-to-end tested against a real TLS Prometheus.\r\n\r\nThis PR reworks the generic-k8s path to plain in-cluster HTTP and preserves the (working) OpenShift path exactly:\r\n\r\n- keda-epp-saturation and keda-epp-queue `k8s` overlays now target the bundled kube-prometheus-stack over plain in-cluster HTTP (`http://...:9090`) with no client auth, no `TriggerAuthentication`, and no CA secret.\r\n- Removed the base `TriggerAuthentication` (deleted the saturation `base/triggerauthentication.yaml`; removed the embedded TA block from the queue `base/scaledobject.yaml`) and dropped the `deploy.prometheus_auth` step and the CA-secret cleanup from both `guide.yaml` files.\r\n- OpenShift is unchanged: each guide now carries its own `ocp/triggerauthentication.yaml` (`authModes: bearer` + metrics-reader `service-ca.crt`), and the `ocp` overlay patches `serverAddress` to Thanos Querier and attaches it. `kubectl kustomize .../ocp` is byte-identical to `main` for both guides.\r\n- Refreshed the README and `guide.yaml` prose, re-rendered both guides (`guide.py render --check` is clean), and added an `> [!IMPORTANT]` note documenting the KEDA CA/`authModes` caveat so future readers do not re-introduce a CA-only trigger.\r\n\r\nVerified end to end on a kind cluster for both guides against a plain-HTTP Prometheus: the ScaledObject reaches `Ready=True`/`Active=True` with no `x509` errors, the generated HPA reads both external metrics over threshold, and the target Deployment performs a real 1-to-N scale-out.\r\n\r\n**Which issue(s) this PR fixes**:\r\n\r\nRefs #2565\r\n\r\n**Release note**:\r\n\r\n```release-note\r\nThe generic-Kubernetes KEDA + EPP autoscaling guides (keda-epp-saturation, keda-epp-queue) now connect to the bundled Prometheus over plain in-cluster HTTP. The previous CA-only TriggerAuthentication never verified TLS on KEDA's prometheus scaler (it requires authModes to load the CA). The OpenShift path is unchanged and still uses bearer auth over HTTPS to Thanos Querier.\r\n```\r\n", "url": "https://github.com/llm-d/llm-d/pull/2571"}
{"number": 2552, "title": "Agentic api guide", "state": "open", "created_at": "2026-09-22T15:05:11Z", "updated_at": "2026-09-24T05:54:48Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "**(Stacked ontop of https://github.com/llm-d/llm-d/pull/2555 and https://github.com/llm-d/llm-d/pull/2557, see latest commit for diff)**\r\n\r\nAdd a new guide (`guides/agentic-api/`) for deploying `vllm/agentic-api:v0.8.0`\r\nwith PostgreSQL state persistence in front of an `llm-d` wide-ep deployment\r\n(`zai-org/GLM-5.3-Flash`).\r\nKey additions:\r\n- Add `guides/agentic-api/README.md` documenting architecture topologies,\r\n  prerequisites (`guides/wide-ep/README.aggregated.md`), and step-by-step\r\n  deployment instructions for both Standalone Router Mode and GKE Gateway Mode.\r\n- Add `guides/agentic-api/manifests/postgres.yaml` and\r\n  `guides/agentic-api/manifests/agentic-api-standalone.yaml` for deploying\r\n  `agentic-api` as a Kubernetes Service in front of `wide-ep-epp` in\r\n  standalone mode (`--llm-api-base http://wide-ep-epp.llm-d-wide-ep.svc.cluster.local:80`/).\r\n- Add `guides/agentic-api/manifests/gateway/gateway-routes.yaml` and\r\n  `guides/agentic-api/manifests/gateway/agentic-api-gateway.yaml` for GKE\r\n  Gateway Mode (`gke-l7-regional-external-managed`), routing external\r\n  `/v1/responses`, `/v1/conversations`, and `/v1/messages` traffic to\r\n  `Service/agentic-api` while routing upstream requests from `agentic-api`\r\n  (`Host: epp.gateway.internal` via `hostAliases` and `HTTPRoute`\r\n  `spec.hostnames`) directly to `InferencePool/wide-ep` to prevent request\r\n  looping.\r\n- Add `guides/agentic-api/verify.py` to verify model discovery, PostgreSQL-backed\r\n  stateful HTTP `/v1/responses` continuations (`previous_response_id`), HTTP\r\n  webhook delivery with stateful `function_call_output` continuation, and\r\n  WebSocket (`WS /v1/responses`) streaming.", "url": "https://github.com/llm-d/llm-d/pull/2552"}
{"number": 2324, "title": "bump to vLLM v0.27.1", "state": "closed", "created_at": "2026-08-19T15:15:58Z", "updated_at": "2026-09-24T01:19:49Z", "closed_at": "2026-09-24T01:19:49Z", "labels": ["lifecycle/rotten"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Tests #2318 ", "url": "https://github.com/llm-d/llm-d/pull/2324"}
{"number": 2361, "title": "fix(workload-autoscaling): capture WVA controller diagnostics before CKS teardown", "state": "open", "created_at": "2026-08-23T22:53:01Z", "updated_at": "2026-09-24T01:19:28Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Motivation:\nThe nightly CKS workload-autoscaling lane\n(workload-autoscaling-cks-acc-gpu-vllm-x) has failed for at least seven\nconsecutive nights waiting for wva-controller-manager to become\nAvailable. The root cause is unknown because the controller pod is\nforce-deleted during teardown before its state can be inspected —\nkubectl describe output and pod logs never make it into the run output.\n\nApproach:\nWrap the `kubectl wait deployment/wva-controller-manager ... --for=condition=Available`\nstep in nightly-deploy-cks.sh so that on failure, before the script exits\n(and before the calling workflow tears the namespace down), it captures\nand prints to stderr:\n  - `kubectl describe` of the Deployment\n  - `kubectl describe`, `kubectl logs --all-containers`, and\n    `kubectl logs --all-containers --previous` (to catch a crashed prior\n    container instance, e.g. CrashLoopBackOff) for every pod matching the\n    wva-controller-manager- name prefix\nThis mirrors the existing diagnostics block later in the same script\n(the KEDA fallback check), so it matches this file's own conventions.\nnightly-deploy-ocp.sh's `kubectl wait` step does not have an equivalent\nfailure-diagnostics wrapper today — that parity gap is left for a\nfollow-up, since this PR only targets the CKS nightly lane the issue is\nabout. The script still exits 1 on failure, preserving the original CI\nfailure semantics.\n\nThis does not fix the underlying reason the controller never becomes\nAvailable — that root cause is still unconfirmed and requires\ninspecting a live CKS run. This change implements next step #1 from\nthe issue report (\"Capture kubectl describe deploy/wva-controller-manager\nplus the controller pod's events and logs before teardown runs\") so the\nnext nightly failure leaves enough evidence in the run log to diagnose\nthe real problem.\n\nValidation:\nNo live CKS/GPU cluster is available in this environment, so the\nunderlying Available-timeout failure itself could not be reproduced or\nconfirmed fixed (it isn't meant to be fixed by this change). What was\nrun:\n  - `bash -n guides/workload-autoscaling/scripts/nightly-deploy-cks.sh` — passed.\n  - `shellcheck -x --severity=warning guides/workload-autoscaling/scripts/nightly-deploy-cks.sh` — zero warnings.\n    (Note: this repo's .pre-commit-config.yaml only runs the shellcheck\n    hook against docker/scripts/*.sh, so this script is not gated by\n    that hook; ran shellcheck manually as an extra check.)\n  - Extracted the new diagnostic block into an isolated harness with a\n    stubbed `kubectl` simulating the exact failure mode from the issue\n    (kubectl wait returns 1, one matching pod\n    wva-controller-manager-6bfcf4cfb5-qxvkw as in the issue's log\n    excerpt). Confirmed: describe/logs/logs --previous output print in\n    the correct order for the deployment and the matching pod, the\n    pod-name grep matches correctly and is empty-safe under pipefail,\n    and the script still exits 1, preserving the original failure path\n    for the caller.\n\nReport: https://github.com/llm-d/llm-d/issues/2124\nSigned-off-by: Pujitha Paladugu <10557236+pujitha24@users.noreply.github.com>\nAssisted-by: claude-sonnet-5 (via Claude Code)\n\nFixes #2124\n", "url": "https://github.com/llm-d/llm-d/pull/2361"}
{"number": 2573, "title": "docs(guides): runtime AI inventory guide (k8s-aibom)", "state": "open", "created_at": "2026-09-23T23:19:47Z", "updated_at": "2026-09-23T23:28:59Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Adds a short Operational Excellence guide for runtime AI inventory:\ndeploying k8s-aibom alongside any well-lit path so platform and\nsecurity teams get an auditable CycloneDX 1.6 ML-BOM of what is\nactually serving. llm-d and vLLM are natively detected runtimes, so\nthis is documentation of a working combination — no code changes on\neither side.\n\nThe controller is deliberately compatible with this repo's philosophy:\nunprivileged, API-server-only observation, no pod-spec changes,\nnamespace-opt-in so nothing is recorded without an explicit operator\ndecision. It ships digest-pinned and attested, and is a qualified\ncomponent in NVIDIA AI Cluster Runtime.\n\nMarkdown-only guide, so it is added to .ci-dry-run-guide-exclusions\nfollowing the rl/batch-serving precedent. Happy to reshape to fit\nguide conventions (env.sh integration, placement) per maintainer\nguidance.\n", "url": "https://github.com/llm-d/llm-d/pull/2573"}
{"number": 2513, "title": "fix(ci,guides): use GAIE_URL in fast-model-actuation-keda", "state": "closed", "created_at": "2026-09-16T21:24:26Z", "updated_at": "2026-09-23T22:48:22Z", "closed_at": "2026-09-17T07:31:20Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T07:31:20Z", "body": "Part of the fixes for nightly E2E `guide-error` failures tracked in https://github.com/llm-d/llm-d/issues/2511.\r\n\r\n### Summary of Changes\r\n1. **Fix GAIE manifest URL in `fast-model-actuation-keda`**:\r\n   - Replace hardcoded `releases/download/${GAIE_VERSION}` with `${GAIE_URL}` in `guides/fast-model-actuation-keda/README.md` and `guide.yaml` so `GAIE_VERSION=latest` resolves to `/releases/latest/download` instead of returning HTTP 404.", "url": "https://github.com/llm-d/llm-d/pull/2513"}
{"number": 2570, "title": "guides(wide-ep-lws): add SGLang GLM-5.2 recipe using the Rust frontend and /generate", "state": "open", "created_at": "2026-09-23T19:49:33Z", "updated_at": "2026-09-23T19:52:57Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Adds an SGLang recipe for GLM-5.2-FP8 under `guides/wide-ep-lws/modelserver/gpu/sglang-glm-5.2/` using the SGLang Rust frontend (one HTTP listener per DP rank, node-local ports) and the native `/generate` path, plus the EPP override `router/sglang-glm-5.2.values.yaml` (`sglanghttp-parser`, prefix-cache and active-request scoring). DisaggregatedSet with prefill DEP8 and decode DEP16 across 2 nodes, DeepEP normal/low_latency, NIXL KV transfer, EAGLE MTP. GKE overlay (DRA + DRANET).\n\nBrought up once end to end on GKE A3 Ultra (3 Spot H200 nodes, `gke-p1w1d1w2`). GKE is the only provider overlay for now and there are no benchmark numbers yet.\n\nPoints for reviewers, all documented in the README Limitations:\n\n- The stock SGLang nightly ships NVSHMEM 3.4.5, whose IBGDA transport fails on RoCE; `image/Dockerfile` rebuilds the IBGDA plugin with the existing `patches/nvshmem_zero_ibv_ah_attr_v3.4.5-0.patch`.\n- The Rust `/health` answers 503 on every listener except global DP rank 0, so probes use TCP plus a one-token `/generate`.\n- The DisaggregatedSet name must stay at or under 33 characters because LWS derives a pod label from it; the existing vLLM GLM-5.2 name (35 characters) likely hits the same limit.\n\nPart of #2554, tracked in #2198.\n", "url": "https://github.com/llm-d/llm-d/pull/2570"}
{"number": 2568, "title": "fix(no-kubernetes-deployment): enable EPP health checking for Envoy", "state": "open", "created_at": "2026-09-23T19:05:09Z", "updated_at": "2026-09-23T19:18:58Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #2567\r\n\r\n## What\r\n\r\nThe no-Kubernetes guide's `envoy.yaml` health-checks the EPP with `grpc_health_check` on the ext-proc port (`9002`). The EPP only serves `grpc.health.v1.Health` on that port when started with `--health-checking=true`, and neither EPP command in the guide passed it. Every check failed, Envoy held the EPP at `/failed_active_hc`, and requests reached it only through Envoy's panic routing.\r\n\r\nThis PR adds `--health-checking=true` to both EPP commands (container and from-source), which matches the router chart in priority-routing mode, where it passes the flag and fails the render if it's set to false (`routerlib/templates/_helpers.tpl`: *\"priorityRouting requires EPP's gRPC health service on port 9002\"*).\r\n\r\n## Why it matters\r\n\r\nWith one EPP, completions succeed either way. That's why this went unnoticed. But:\r\n\r\n- the health check the guide says it carries over from the standalone preset has never passed;\r\n- a second EPP added for failover gets no health-based failover, because every EPP looks equally unhealthy and Envoy load-balances across all of them in panic mode.\r\n\r\n| Envoy stat (`ext_proc` cluster) | main | this PR |\r\n|---|---|---|\r\n| `health_check.success` / `failure` | 0 / 6 | 1 / 0 |\r\n| `lb_healthy_panic` | 1 | 0 |\r\n| `health_flags` | `/failed_active_hc` | `healthy` |\r\n| completion via Envoy `:8081` | 200 | 200 |\r\n\r\nProbing the EPP directly on main: port `9002` returns `UNIMPLEMENTED unknown service grpc.health.v1.Health`, and port `9003` returns `SERVING`.\r\n\r\n## Changes\r\n\r\n- `README.md`: `--health-checking=true` in both EPP commands; a paragraph on what it does and how it differs from `--grpc-health-port`; a `health_flags` check in **Verification**; the `failed_active_hc` symptom in **Troubleshooting**.\r\n- `router/envoy/envoy.yaml`: a comment above `health_checks:` stating the requirement, since the guide tells users to copy this file to hosts on its own.\r\n\r\nI considered dropping `health_checks` from `envoy.yaml` instead, but decided against it. The guide says it mirrors the standalone preset's health checks, and the health check is what makes a multi-EPP setup fail over.\r\n\r\n## Testing\r\n\r\nRan the full guide on one host with Docker and 2 GPUs (RTX 3080 + RTX 3090). Two vLLM `v0.19.1` workers served Qwen3-0.6B, one per GPU, both listed in `endpoints.yaml`, behind EPP `main` and Envoy `distroless-v1.33.2`.\r\n\r\n- [x] Before/after Envoy stats as in the table above; completions return 200 in both cases\r\n- [x] `--health-checking` is present in the `v0.10.0` and `main` EPP images (`--help`)\r\n- [x] Routing is unaffected: requests that share a prefix stay on one worker (90% prefix-cache hit rate), and unique prompts spread across workers\r\n- [x] `envoy --mode validate` passes on the edited `envoy.yaml`\r\n- [x] `prek` hooks pass on both files. markdownlint reports 6 MD060 table errors at README lines 29–31, which already exist on `main` and are untouched here\r\n\r\n## Related\r\n\r\nThe router chart's default standalone preset renders the same unconditional `health_checks` block but passes `--health-checking=true` only under priority routing, so it may show the same thing in Kubernetes. I haven't reproduced that, so I'm raising it as a question in llm-d-router rather than touching it here.\r\n", "url": "https://github.com/llm-d/llm-d/pull/2568"}
{"number": 2558, "title": "fix(pd-disaggregation): drop the ROCM_ATTN backend pin from the AMD overlay", "state": "closed", "created_at": "2026-09-22T19:03:54Z", "updated_at": "2026-09-23T16:39:05Z", "closed_at": "2026-09-23T16:39:05Z", "labels": ["hold", "amd"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T16:39:04Z", "body": "The AMD NIXL P/D nightly has failed since 8/18, when the guide moved to `llm-d-rocm:v0.9.0`. Its vLLM 0.26.0 base rejects the `ROCM_ATTN` attention backend whenever a KV connector is configured, causing every prefill and decode pod to exit at startup:\r\n\r\n```\r\nValueError: Selected backend AttentionBackendEnum.ROCM_ATTN is not valid for this configuration. Reason: ['KV connector not supported']\r\n```\r\n\r\nThe overlay pinned the backend for heterogeneous TP transfer, but now vLLM sets the KV cache layout that the NIXL connector needs on its own and only auto-selects backends that pass the KV connector check, so the pin is removed from prefill and decode.\r\n\r\nThe nightly itself can verify this working now. Previous failure traceback from the [9/22 run](https://github.com/llm-d/llm-d/actions/runs/35682014366).\r\n\r\nContext: #2523\r\n\r\n", "url": "https://github.com/llm-d/llm-d/pull/2558"}
{"number": 2533, "title": "pd-disaggregation: fold the TPU guide into the main guide, cache TPU weights on the node, pin TPU7x to vllm-tpu v0.26.0", "state": "closed", "created_at": "2026-09-18T16:11:15Z", "updated_at": "2026-09-23T15:17:51Z", "closed_at": "2026-09-23T15:17:50Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T15:17:50Z", "body": "## Summary\r\n\r\nFolds the separate TPU page of the P/D disaggregation guide into the main guide, and fixes two problems found while validating the TPU7x path end to end.\r\n\r\nThree commits, each independently revertable:\r\n\r\n1. **Fold the TPU guide into the main guide.** `README.tpu.md` was last synced with the shared content on 2026-06-12 and had drifted (dead benchmark anchor, retired `run_only.sh` flow, an env block that sourced `env.sh` before `REPO_ROOT` was set, an unflagged inheritance of the H200-calibrated `peakPrefillThroughput`). It is replaced by a `#### TPU` section next to `#### GPU`, with the dynamic sub-slicing content reduced to a pointer at `docs/infrastructure/providers/gke/dynamic-slicing/`. `README.tpu.md` and the orphaned `benchmark-templates/tpu.yaml` are deleted. The model-server overlays stay where they are; the nightly TPU workflow selects them by `accelerator_type`, and the other CI jobs discover work by path, so no CI change is needed.\r\n2. **Cache model weights on the node for the TPU overlays.** Adds the node-local HuggingFace cache (`hostPath /var/cache/huggingface`, `HF_HOME`) already used by the GKE GPU overlays to the TPU base patches and the dynamic-slice manifests. Without it every container restart re-downloaded the 406 GB TPU7x model into the writable layer, which triggered ephemeral-storage eviction on nodes with other data on the boot disk. The boot-disk requirement is documented.\r\n3. **Pin the TPU7x overlays to `vllm-tpu:v0.26.0`.** In `v0.27.0` through `v0.29.0` the vLLM scheduler reads `connector._kv_transfer_config` (`requires_kv_delivery`), but the bundled `TPUConnectorHMA` skips the base-class initializer, so EngineCore fails at startup with `AttributeError: 'TPUConnectorHMA' object has no attribute '_kv_transfer_config'`. Fixed upstream by vllm-project/tpu-inference#3566 (2026-09-12); no `vllm-tpu` release contains it yet. The shared `tpu-vllm/release` component stays at `v0.27.0` because tiered-prefix-cache's TPU multi-host offload (#2313) needs it; a new `tpu-vllm/release-v0.26.0` component pins only the two HMA overlays. The v6e overlay uses the non-HMA `TPUConnector` and is unaffected. This commit is reverted once a release includes the fix.\r\n\r\n## Validation\r\n\r\n* Deployed `modelserver/tpu/v7/vllm` from this branch on GKE TPU7x (`2x2x1`, 1 prefill + 1 decode, standalone router). `/v1/completions` and `/v1/chat/completions` succeed through the EPP; the prefill engine logs the KV send and the decode engine the KV pull for every request; decode reports 100% external prefix-cache hits (574/574 prompt tokens over three requests).\r\n* Every `kustomization.yaml` under `guides/pd-disaggregation` builds; `tpu/v6/vllm` renders `v0.27.0`, both TPU7x overlays render `v0.26.0`.\r\n* `make verify`, markdownlint (no new findings beyond the existing `<h4>`-in-`<summary>` pattern), yamllint, and the router `helm template` pass.\r\n* Internal links and anchors in the changed Markdown resolve.\r\n\r\n## Note\r\n\r\nThe regression in commit 3 was not caught by CI because the nightly TPU lane only runs `tpu/v6`. A TPU7x lane for the HMA path would close that gap after we can get capacity for nightly e2e test.\r\n\r\ncc: @achandrasekar @ahg-g \r\n", "url": "https://github.com/llm-d/llm-d/pull/2533"}
{"number": 2540, "title": "docs: add a model loading and startup acceleration guide", "state": "closed", "created_at": "2026-09-21T02:56:16Z", "updated_at": "2026-09-23T12:51:15Z", "closed_at": "2026-09-23T12:31:47Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T12:31:47Z", "body": "## Summary\n\nPartially addresses #2216, following the requests for [a unified guide covering P2P, PVC](https://github.com/llm-d/llm-d/issues/2216#issuecomment-5270178177), and [MatrixHub](https://github.com/matrixhub-ai/matrixhub) and [keeping startup optimization separate from the routing-focused Optimized Baseline](https://github.com/llm-d/llm-d/issues/2216#issuecomment-5342664659).\n\nAdd a Model Loading and Startup Acceleration guide covering model sources, persistent caches, JIT cache reuse, ModelExpress, Pod snapshots, startup measurement, and troubleshooting. Link it from the operations navigation and Optimized Baseline. Consolidate caching and startup troubleshooting guidance from the GKE provider documentation into this guide, while retaining the GKE-specific Google Cloud Storage integration section.\n", "url": "https://github.com/llm-d/llm-d/pull/2540"}
{"number": 2490, "title": "docs(autoscaling): deprecate WVA and pin its image to release-0.9", "state": "closed", "created_at": "2026-09-14T15:54:39Z", "updated_at": "2026-09-23T12:26:16Z", "closed_at": "2026-09-23T12:26:16Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T12:26:16Z", "body": "## What\n\nWVA is deprecated and gets no further releases — `v0.9.0` is the final version (upstream also carries a `wva-engine-eol` tag). This pins the WVA guide assets to that release and marks the path deprecated.\n\n### Image pin\n\nThe controller overlays already tracked `?ref=release-0.9`, but the image tag came from whatever the upstream overlay happened to set. Both platform overlays now pin it explicitly:\n\n```yaml\nimages:\n  - name: ghcr.io/llm-d/llm-d-workload-variant-autoscaler\n    newTag: v0.9.0\n```\n\n`kustomize build` on `wva/controller/platform/{k8s,ocp}` renders `…llm-d-workload-variant-autoscaler:v0.9.0`.\n\n### Deprecation notices\n\n- **`guides/workload-autoscaling/README.md`** — warning at the top pointing new deployments at the KEDA + EPP paths, naming Saturation-based Autoscaling as the closest replacement for WVA's saturation signal. `KEDA + WVA Metrics (Legacy)` → `(Deprecated)`.\n- **`wva/README.md`** — replaces the stale VariantAutoscaling warning (it said the CRD \"will be removed in 0.9.0\", already past) with the deprecation notice, keeping the CRD fact as context.\n- **`replica-rebalancing/README.md`** — deprecation folded into the existing experimental-feature blockquote, since the rebalancer ships inside WVA.\n\n### Workflows\n\nThe three WVA nightly lanes each built the controller **from `main`** and passed it via `WVA_TAG`, which the deploy scripts turn into an `images:` override — that would have defeated the pin. For `nightly-e2e-workload-autoscaling-{ibm-acc-gpu,cks-acc-gpu,amd-acc-rocm}-vllm-x.yaml`:\n\n- Removed the `build-wva-image` job; `nightly` no longer `needs:` it.\n- `WVA_TAG` now comes only from the optional `image_tag` dispatch input, so the overlay pin applies by default.\n- `caller_ref` default `main` → `release-0.9`, so the e2e suite matches the image.\n- Schedules disabled on cks (07:30 UTC) and amd (13:00 UTC); ibm's was already off and its reason is updated. All three keep `workflow_dispatch`.\n\n`nightly-e2e-workload-autoscaling-keda-epp-ibm-acc-gpu-vllm-x.yaml` is EPP-only and untouched.\n\n## Testing\n\n- `kustomize build` on both platform overlays renders `v0.9.0`.\n- `make verify-guide-readmes` passes.\n- `markdownlint-cli2` error count on the three READMEs unchanged from base (all pre-existing).\n- All three workflows parse and no longer contain `build-wva-image`; no dangling references remain.\n\n## Notes for reviewers\n\n- `permissions: packages: write` was left in place on the three workflows — now unused for pushing, but reusable workflows inherit permissions and I did not want to risk an image pull.\n- The three `consolidate-status-workload-autoscaling-{amd,cks,ibm}` badge workflows and the WVA badge in the guide README still exist. With schedules off those badges will go stale, so retiring them may be worth a follow-up.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)", "url": "https://github.com/llm-d/llm-d/pull/2490"}
{"number": 2528, "title": "fix(pd-disaggregation): add shared HF cache to vLLM CoreWeave overlay", "state": "closed", "created_at": "2026-09-18T02:42:58Z", "updated_at": "2026-09-23T08:53:06Z", "closed_at": "2026-09-23T08:53:06Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T08:53:06Z", "body": "Motivation:\nThe vLLM CoreWeave overlay was missing the shared HuggingFace cache and\nXet-disable settings that the GKE overlay for this guide already has,\nleaving CoreWeave pods without a shared model cache across restarts and\nexposed to Xet CDN 429s when multiple pods pull the model at once.\n\nApproach:\nAdd JSON6902 patches to\nguides/pd-disaggregation/modelserver/gpu/vllm/coreweave/kustomization.yaml,\nmatching this file's existing `op: add` patch style, for both the decode\nand prefill Deployments:\n- HF_HOME, HF_HUB_DOWNLOAD_TIMEOUT, and HF_HUB_DISABLE_XET env vars\n- a volumeMount at /root/.cache/huggingface\n- a node-local hostPath volume (huggingface-cache) backed by\n  /var/cache/huggingface\n\nReport: https://github.com/llm-d/llm-d/issues/2523\nSigned-off-by: Pujitha Paladugu <10557236+pujitha24@users.noreply.github.com>\nAssisted-by: claude-sonnet-5 (via Claude Code)", "url": "https://github.com/llm-d/llm-d/pull/2528"}
{"number": 2500, "title": "fix(sglang): align P/D KV-page size with EPP chunking and sibling guides", "state": "closed", "created_at": "2026-09-15T17:53:03Z", "updated_at": "2026-09-23T08:03:51Z", "closed_at": "2026-09-23T08:03:51Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T08:03:51Z", "body": "## What this changes\n\nAdds `--page-size=64` to SGLang prefill and decode deployments in `guides/pd-disaggregation/`.\n\nSGLang defaults to `page_size=1` (token-level KV allocation). In this guide, the llm-d EPP router configures `approx-prefix-cache-producer`, which tracks prefix caching in 64-token chunks. Aligning SGLang's KV-page size to 64 prevents conservative under-counting of prefix hits by EPP, and aligns this guide with the other SGLang guides in the repository (`tiered-prefix-cache`, `precise-prefix-cache-routing`).\n\nAlso updates `aws/patch-prefill.yaml` to repeat the flag, because that patch restates the full `args` list and strategic-merge replaces lists.\n\n## Testing\n\nVerified manifest rendering across all overlays:\n\n```bash\nfor o in base aws coreweave gke; do\n  kubectl kustomize guides/pd-disaggregation/modelserver/gpu/sglang/$o | grep -E -- '--page-size=64'\ndone\n```\n\nAll overlays render `--page-size=64` on both prefill and decode deployments.\n", "url": "https://github.com/llm-d/llm-d/pull/2500"}
{"number": 2556, "title": "Update multimodal guides to use leaf name for ci/cd testing.", "state": "closed", "created_at": "2026-09-22T18:21:26Z", "updated_at": "2026-09-23T07:59:04Z", "closed_at": "2026-09-23T07:59:04Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T07:59:04Z", "body": "CI/CD was failing because of multimodal nested directory. llm-d-benchmark and llm-d-infra have been updated to now use leaf name of guide. \r\n\r\nThis change updates multimodal guide to use the leaf name for testing. ", "url": "https://github.com/llm-d/llm-d/pull/2556"}
{"number": 2486, "title": "fix(ci): default harness_resources to 2/8Gi for Intel XPU nightly workflows", "state": "closed", "created_at": "2026-09-14T01:37:23Z", "updated_at": "2026-09-23T02:23:51Z", "closed_at": "2026-09-23T02:23:51Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Problem\n3 Intel XPU nightly workflows have been failing every run since 2026-09-09:\n- `nightly-e2e-optimized-baseline-intel-acc-xpu-vllm-x`\n- `nightly-e2e-pd-disaggregation-intel-acc-xpu-vllm-x`\n- `nightly-e2e-precise-prefix-cache-routing-intel-acc-xpu-vllm-x`\n\n```\nFailed to deploy pod 'inference-perf-xxx': The Pod \"inference-perf-xxx\" is invalid:\nspec.containers[0].resources.requests: Invalid value: \"64Gi\": must be less than or equal to memory limit of 8Gi\n```\n\n## Cause\n- These workflows send `harness_resources=32/64Gi` to `llm-d-infra`'s reusable workflow, which only overrides the harness pod's memory **request** (`--set harness.resources.memory=64Gi`).\n- Since `llm-d-benchmark#1882` (merged 2026-09-08), the memory **limit** comes from a separate field, `harness.resources.memoryLimit`, hard-coded to `8Gi` in the `intel-xpu` overlay used by these 3 scenarios.\n- Result: `requests.memory=64Gi > limits.memory=8Gi` → Kubernetes rejects the pod → no harness pod → run fails.\n\n## Fix\nDefault `harness_resources` to `2/8Gi` for these 3 workflows, matching the intel-xpu overlay's `memoryLimit`. This isn't an arbitrary downgrade — it's the value the overlay was originally sized for (single-node Kind/B60, harness sharing the node with the serving stack), and it ran successfully for weeks before `harness_resources` was introduced (see 8/18–8/27 run history, all green).\n\nOther (non-XPU) nightly workflows are untouched — their overlays use the global default `memoryLimit=128Gi`, so `32/64Gi` doesn't conflict there. `predicted-latency-routing` / `wide-ep-lws` / `tiered-prefix-cache` on Intel XPU are also untouched — they don't set `accelerator.profile: auto`, so this overlay/bug doesn't apply to them.\n\n## Scope\nStopgap only. Filing a follow-up issue on `llm-d/llm-d-infra` to also override `memoryLimit` alongside `memory`, so this can't recur for other accelerator overlays.\n", "url": "https://github.com/llm-d/llm-d/pull/2486"}
{"number": 2372, "title": " Fix stale forward-looking version claims in WVA guides for 0.9.0", "state": "open", "created_at": "2026-08-25T09:35:48Z", "updated_at": "2026-09-23T01:18:54Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #2357\r\n\r\n`guides/workload-autoscaling/wva/README.md` and\r\n`docs/architecture/advanced/autoscaling/hpa-wva.md` described the 0.9.0 release in future tense even though they ship inside the 0.9.0 tag itself:\r\n\r\n- \"The VariantAutoscaling CRD will be removed in 0.9.0\",  but the CRD was already removed as of 0.9.0 (confirmed against\r\n  llm-d-workload-variant-autoscaler release-0.9, which no longer ships\r\n  a CRD manifest).\r\n- \"Saturation engine v2 will be the default in the next release (0.9.0), but for now it must be enabled manually\", but v2 is\r\n  already the default as of 0.9.0 (confirmed via the shipped saturation ConfigMap, which already includes the `analyzers:`\r\n  section that selects v2).\r\n  \r\nA reader on the v0.9.0 tag had no way to tell from the docs whether these \"future\" events had already happened.", "url": "https://github.com/llm-d/llm-d/pull/2372"}
{"number": 2562, "title": "Set UCX_IB_ROCE_REACHABILITY_MODE=all in the wide-ep GKE overlay", "state": "open", "created_at": "2026-09-22T22:56:13Z", "updated_at": "2026-09-22T23:28:02Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Two things found while running wide-ep as a DisaggregatedSet on a GKE A4 (B200) cluster with GKE managed DRANET.\r\n\r\n- GKE puts each of the eight RDMA NICs on its own subnet. UCX's default reachability check rejects the remote RoCE v2 GID across subnets and falls back to the link-local GID, so the NIXL transfer fails. `UCX_IB_ROCE_REACHABILITY_MODE=all` fixes prevents this. The pd-disaggregation GKE overlays already has this so adding it to the wide-ep GKE overlay too.\r\n- Hybrid Mamba models (GLM-5.3-Flash) need `VLLM_SSM_CONV_STATE_LAYOUT=DS` or the NIXL connector refuses to start. No wide-ep manifest serves such a model today, so this is a README note only.\r\n\r\nTested that prefill to decode transfers over RDMA succeed in both DisaggSet ordinal and hash group identity modes.\r\n", "url": "https://github.com/llm-d/llm-d/pull/2562"}
{"number": 2557, "title": "Aggregated wide-ep GLM 5.3 flash guide with gke overlay", "state": "open", "created_at": "2026-09-22T18:22:59Z", "updated_at": "2026-09-22T22:38:05Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "**(Stacked ontop of https://github.com/llm-d/llm-d/pull/2555, see latest commit for diff)**\r\n\r\nAdd an aggregated (non-disaggregated) wide expert parallelism modelserver\r\noverlay (`guides/wide-ep/modelserver/gpu/vllm-glm-5.3-flash-aggregated/`)\r\nfor serving `zai-org/GLM-5.3-Flash` (`Glm5NextForConditionalGeneration`,\r\n320B total / 18B active parameters, 288 routed experts) across 2 x 8-GPU\r\nnodes (16 x NVIDIA B200 GPUs).\r\n\r\nKey details:\r\n- Configure `LeaderWorkerSet` (`size: 2`) with `DP=16, EP=16, TP=1`\r\n  (18 routed experts per GPU) using vLLM's DP Supervisor on port 8008\r\n  (`--data-parallel-multi-port-external-lb`, ports 8000-8007) and\r\n  `--reasoning-parser glm47` / `--tool-call-parser glm47`.\r\n- Use the `gpu-vllm/nightly` component (`v0.29.1rc1+`) required for the\r\n  `glm5_next` hybrid KDA linear attention + sparse NoPE MLA architecture\r\n  and TileLang mHC / DSA kernels.\r\n- Set `TRITON_LIBCUDA_PATH=/usr/local/nvidia/lib64` and `LD_LIBRARY_PATH`\r\n  so Triton/FLA kernels locate GKE's mounted `libcuda.so.1` in Ubuntu-based\r\n  upstream `vllm-openai` images.\r\n- Use `--all2all-backend allgather_reducescatter` over GKE DRANET RoCEv2\r\n  RDMA NICs (`gke-rdma-nic-template`, `mrdma.google.com` `gpu0rdma0`-`gpu7rdma0`)\r\n  to avoid the unpatched NVSHMEM `ibv_create_ah` issue on GKE COS.\r\n- Document `GLM-5.3-Flash` deployment and architecture sizing in\r\n  `guides/wide-ep/README.aggregated.md` and `gke/README.md`.", "url": "https://github.com/llm-d/llm-d/pull/2557"}
{"number": 2560, "title": "Update multimodal guides to use leaf name for ci/cd testing", "state": "closed", "created_at": "2026-09-22T21:07:13Z", "updated_at": "2026-09-22T22:00:01Z", "closed_at": "2026-09-22T22:00:01Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## What does this PR do?\n\nRe-submission of #2556 (authored by @rlakhtakia — commit authorship and sign-off preserved via cherry-pick) with the merge conflict against `main` resolved. That PR's head branch is the fork's `main`, which we don't have push access to update.\n\nOriginal description from #2556:\n\n> CI/CD was failing because of multimodal nested directory. llm-d-benchmark and llm-d-infra have been updated to now use leaf name of guide. This change updates multimodal guide to use the leaf name for testing.\n\n## What changed relative to #2556\n\nOnly the conflict resolution: `guides/multimodal-serving/e-disaggregation/modelserver/gpu/vllm/e-pd/base/patch-encode.yaml` — both #2556 and #2559 (merged) deleted the `image: REPLACE_MODEL_SERVER_IMAGE` line; kept `main`'s version of the hunk (deletion plus the explanatory comment). Everything else is the original commit as-is, including its whitespace/EOF lint fixes.\n\n## How was this tested?\n\n- [ ] Unit tests added/updated\n- [ ] Integration/e2e tests added/updated\n- [x] Manual testing performed\n\nAfter the cherry-pick, `kubectl kustomize` renders of `multimodal-serving/e-disaggregation/modelserver/gpu/vllm/{e-pd,e-p-d}/{base,gke}` contain no leftover `REPLACE_MODEL_SERVER_IMAGE`, and the aggregation overlays render with the new `llm-d.ai/guide: aggregation` labels.\n\n## Checklist\n\n- [x] Commits are signed off (`git commit -s`) per DCO\n- [x] Code follows project contributing guidelines\n- [ ] Tests pass locally (`make test`)\n- [ ] Documentation updated (if applicable)\n\n## Related Issues\n\nSupersedes #2556. Related: llm-d/llm-d#2511, llm-d/llm-d-infra#261, llm-d/llm-d-benchmark#1935, #2559.", "url": "https://github.com/llm-d/llm-d/pull/2560"}
{"number": 2561, "title": "Update multimodal guides to use leaf name for ci/cd testing.- #2556", "state": "closed", "created_at": "2026-09-22T21:08:27Z", "updated_at": "2026-09-22T21:23:05Z", "closed_at": "2026-09-22T21:23:05Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T21:23:05Z", "body": "This is https://github.com/llm-d/llm-d/pull/2556 which resolves conflicts", "url": "https://github.com/llm-d/llm-d/pull/2561"}
{"number": 2559, "title": "fix(e-disaggregation): drop placeholder image from e-pd encode patch", "state": "closed", "created_at": "2026-09-22T19:22:33Z", "updated_at": "2026-09-22T21:01:33Z", "closed_at": "2026-09-22T21:01:32Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T21:01:32Z", "body": "## What does this PR do?\n\nRemoves the explicit `image: REPLACE_MODEL_SERVER_IMAGE` line from `guides/multimodal-serving/e-disaggregation/modelserver/gpu/vllm/e-pd/base/patch-encode.yaml`.\n\n## Why is this change needed?\n\nThe GKE e-disaggregation nightly fails with encode pods stuck in `InvalidImageName`:\n\n```\nspec.containers[modelserver]: Failed to apply default image tag \"REPLACE_MODEL_SERVER_IMAGE\": couldn't parse image name ...\n```\n\nRoot cause is kustomize transformer ordering. In `e-pd/base/kustomization.yaml`, the encode skeleton comes from `recipes/modelserver/base/single-host/e-pd` and is filled in by `patch-encode.yaml`. Kustomize applies **component** transformers before the kustomization's own **patches**, so the `images/gpu-vllm/ec-connector` component substitutes the placeholder first, and the explicit `image:` field in `patch-encode.yaml` then overwrites it back to `REPLACE_MODEL_SERVER_IMAGE`. Decode is unaffected because `patch-decode.yaml` carries no `image:` field, and e-p-d is unaffected because it ships encode as a full resource (substituted by the component, never re-patched).\n\nWith the image line removed, the encode container keeps the component-substituted image.\n\n## How was this tested?\n\n- [ ] Unit tests added/updated\n- [ ] Integration/e2e tests added/updated\n- [x] Manual testing performed\n\n`kubectl kustomize` on all four overlays before/after:\n\n| Overlay | Before | After |\n| --- | --- | --- |\n| `e-pd/base`, `e-pd/gke` | encode = `REPLACE_MODEL_SERVER_IMAGE` | encode = `ghcr.io/llm-d/vllm-openai:ec-cpu-connector-support` |\n| `e-p-d/base`, `e-p-d/gke` | unchanged | unchanged |\n\nAlso verified the rendered encode Deployment still carries the full patched spec (command/args, ports `modelserver`+`ec-nixl`, `nvidia.com/gpu: 2`, probes, volumes).\n\n## Checklist\n\n- [x] Commits are signed off (`git commit -s`) per DCO\n- [x] Code follows project contributing guidelines\n- [x] Linters pass locally (rendered with `kubectl kustomize`)\n- [ ] Documentation updated (if applicable)\n\n## Related Issues\n\nNightly failure in `nightly-e2e-multimodal-serving-e-disaggregation-gke-acc-gpu-vllm-x.yaml` (run 34521873302); follow-up to the nested-guide-path fixes in llm-d/llm-d#2556, llm-d/llm-d-infra#261, llm-d/llm-d-benchmark#1935.", "url": "https://github.com/llm-d/llm-d/pull/2559"}
{"number": 1715, "title": "[Docs] Replace deprecated `kv_both` for NIXLConnector and with explicit P/D Roles", "state": "closed", "created_at": "2026-06-08T13:39:36Z", "updated_at": "2026-09-22T19:21:33Z", "closed_at": "2026-09-22T19:21:32Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T19:21:32Z", "body": "We're deprecating `kv_both` in vLLM for the NixlConnector in favor of explicit roles https://github.com/vllm-project/vllm/issues/43807 so startup vllm serve commands for P and D will be asymmetric (they already are when configuring things like DeepEP but anyway..).\r\nThe TL;DR: this allows vLLM to have finer control at config time, that is prior to handshaking/pod discovery, to handle some important edge-cases more cleanly. \r\n\r\nThis PR simply changes the docs to recommend setting roles.\r\nMind that at the time of writing `kv_both` isn't yet fully deprecated, so nothing is happening in the current release when setting kv_both.\r\n", "url": "https://github.com/llm-d/llm-d/pull/1715"}
{"number": 2555, "title": "feat(wide-ep): add aggregated (non-P/D) 2-node 16-GPU guide and GKE overlay", "state": "open", "created_at": "2026-09-22T18:17:14Z", "updated_at": "2026-09-22T18:17:14Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Add an aggregated (unified prefill + decode) Wide Expert Parallelism deployment variant for DeepSeek-R1-0528 (`DP=16, EP=16, TP=1`) that runs on 2 x 8-GPU nodes (16 B200/H200 GPUs total) without P/D disaggregation:\r\n\r\n- Add `guides/wide-ep/modelserver/gpu/vllm-deepseek-r1-0528-aggregated/base` with a single 2-worker `LeaderWorkerSet` serving all 16 DP ranks directly on ports 8000-8007 (`deepep_low_latency`) without NIXL KV transfer or routing sidecar.\r\n- Add `guides/wide-ep/modelserver/gpu/vllm-deepseek-r1-0528-aggregated/gke` overlay using GKE DRANET (`mrdma.google.com`) `ResourceClaimTemplate` for the 8 RDMA NICs alongside `nvidia.com/gpu` device plugin allocation.\r\n- Add `guides/wide-ep/router/wide-ep-aggregated.values.yaml` to route across ports 8000-8007 using a single default EPP scheduling profile.\r\n- Document deployment steps and prerequisites in `guides/wide-ep/README.aggregated.md` and link from `guides/wide-ep/README.md`.", "url": "https://github.com/llm-d/llm-d/pull/2555"}
{"number": 2547, "title": "fix(ci): unpin SGLang tiered cache nightly cluster", "state": "closed", "created_at": "2026-09-21T23:07:54Z", "updated_at": "2026-09-22T17:47:14Z", "closed_at": "2026-09-22T17:47:13Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T17:47:13Z", "body": "## What\r\n\r\nDrop `cluster_name` and `cluster_zone` from the SGLang tiered-prefix-cache\r\nnightly lane so it uses the llm-d-infra default, `llm-d-e2e-us-east5`. This is a follow-up PR for https://github.com/llm-d/llm-d/pull/2480\r\n\r\n## Why\r\n\r\nThe lane was pinned to `llm-d-e2e-us-south1-2`, which no longer has any GPU\r\nnodes. Its `a3u-dra-pool` is gone and only `default-pool` and `pool-1` remain,\r\nso llm-d-benchmark cannot resolve the accelerator for the `decode` section and\r\nevery run aborts during the pre-standup teardown, about two minutes in:\r\n\r\n```\r\nRuntimeError: Could not auto-detect the following cluster resources:\r\ndecode.acceleratorType.labelValue\r\n  cluster_resource_resolver.py:244 in resolve_all\r\n```\r\n\r\nBecause nothing is deployed at that point, the run also logs a misleading\r\n`namespaces \"...\" not found` while collecting debug information.\r\n\r\nThe pin was never needed. This lane serves Qwen3-32B at TP=2 and requests\r\n`nvidia.com/gpu` directly, with no DRA or RDMA, so it belongs on the reserved\r\nH100 cluster alongside the optimized-baseline SGLang lane and both vLLM\r\ntiered-prefix-cache lanes, none of which set `cluster_name`. `us-east5` has a\r\n16-node `h100-pool` and already carries the vLLM tiered lane at 4 replicas x 2\r\nGPUs, so it has room for this lane's 2 x 2.\r\n\r\n## Testing\r\n\r\nConfirmed the lane passes only `cluster_namespace`, matching the other H100\r\nlanes, and that yamllint is clean:\r\n\r\n```shell\r\nyq '.jobs.nightly.with | keys' \\\r\n  .github/workflows/nightly-e2e-tiered-prefix-cache-gke-cpu-gpu-sglang-native.yaml\r\npre-commit run --files \\\r\n  .github/workflows/nightly-e2e-tiered-prefix-cache-gke-cpu-gpu-sglang-native.yaml\r\n```\r\n\r\nA scheduled or dispatched run is needed to confirm standup on the new cluster.\r\nI do not have permission to trigger `/test-nightly`, so a guide owner running it\r\non this PR would help.\r\n", "url": "https://github.com/llm-d/llm-d/pull/2547"}
{"number": 1624, "title": "[hardware-support] Build and publish flagos images for cuda and NPU ascend", "state": "closed", "created_at": "2026-05-28T09:07:03Z", "updated_at": "2026-09-22T14:40:49Z", "closed_at": "2026-09-22T14:40:48Z", "labels": ["hardware-support", "lifecycle/rotten"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "vllm-plugin-FL is a plugin for the [vLLM](https://github.com/vllm-project/vllm) inference/serving framework, built on FlagOS's unified multi-chip backend — including the unified operator library [FlagGems](https://github.com/flagos-ai/FlagGems) and the unified communication library [FlagCX](https://github.com/flagos-ai/FlagCX). It extends vLLM's capabilities and performance across diverse hardware environments. Without changing vLLM's original interfaces or usage patterns, the same command can run model inference/serving on different chips.\r\n\r\nFor more information see: https://github.com/flagos-ai/vllm-plugin-FL", "url": "https://github.com/llm-d/llm-d/pull/1624"}
{"number": 2542, "title": "fix(smoke-test): rebuild payload for chat API fallback", "state": "closed", "created_at": "2026-09-21T07:35:05Z", "updated_at": "2026-09-22T12:37:27Z", "closed_at": "2026-09-22T12:37:27Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T12:37:27Z", "body": "When /v1/completions returned a non-200 response, healthcheck retried /v1/chat/completions with the original completions payload. Chat-only services rejected the prompt field and returned HTTP 400, so healthy deployments were reported as unhealthy.\r\n\r\nBuild the payload from the selected endpoint before each inference request, and add a regression test for a completions 404 followed by a chat request that accepts only messages.\r\n\r\nTests:\r\n- bash -n helpers/smoke-test/healthcheck.sh\r\n- pytest -q helpers/smoke-test/tests/test_healthcheck_failure.py\r\n- PATH=/usr/bin:/bin /opt/anaconda3/bin/pytest -q helpers/smoke-test/tests/test_healthcheck_failure.py", "url": "https://github.com/llm-d/llm-d/pull/2542"}
{"number": 2549, "title": "Re-enabling automation for workflow", "state": "closed", "created_at": "2026-09-22T02:43:38Z", "updated_at": "2026-09-22T12:30:41Z", "closed_at": "2026-09-22T12:30:41Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T12:30:41Z", "body": "", "url": "https://github.com/llm-d/llm-d/pull/2549"}
{"number": 2456, "title": "fix: normalize abbreviated vLLM versions", "state": "closed", "created_at": "2026-09-07T10:02:37Z", "updated_at": "2026-09-22T09:54:38Z", "closed_at": "2026-09-08T16:07:01Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T16:07:00Z", "body": "An abbreviated release such as `0.24rc1` becomes `(0, 24)`, which compares below the `(0, 24, 0)` metric-version threshold, while `0.24.0rc1` selects the new metric family. Normalize two-component release strings with a trailing zero and preserve the existing dotted development-version handling.\n\nValidation: `python -m pytest -q .github/scripts/nightly-e2e-verification/test_verify_helpers.py`: 52 passed.\nChanged-file pre-commit checks passed.\n", "url": "https://github.com/llm-d/llm-d/pull/2456"}
{"number": 2455, "title": "fix(flow-control): validate tuning wizard inputs", "state": "closed", "created_at": "2026-09-07T10:02:33Z", "updated_at": "2026-09-22T09:54:38Z", "closed_at": "2026-09-08T20:51:48Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T20:51:48Z", "body": "The tuning wizard accepts nonfinite and physically invalid inputs: infinite throughput raises a traceback, while out-of-range efficiency or correlation can produce misleading capacity recommendations. Validate finite values and parameter bounds before running the model, returning a clear CLI error. Regression tests cover invalid inputs and normal recommendation output.\n\nValidation: `python -m pytest -q scripts/tests`: 33 passed.\nChanged-file pre-commit checks passed.\n", "url": "https://github.com/llm-d/llm-d/pull/2455"}
{"number": 2452, "title": "fix(wva): keep CKS nightly patch out of source tree", "state": "closed", "created_at": "2026-09-07T10:02:25Z", "updated_at": "2026-09-22T09:54:37Z", "closed_at": "2026-09-08T15:54:25Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T15:54:25Z", "body": "The CKS nightly deploy helper edits the tracked optimized-baseline patch in place and appends the Triton cache volume and mount again on reruns. Render a temporary overlay under OUTPUT_DIR using the existing WVA model-server patch, then apply that overlay. The regression simulates failure and rerun, verifies source checksums remain unchanged, and builds the generated overlay with kustomize.\n\nValidation: `python -m pytest -q scripts/tests`: 29 passed.\nChanged-file pre-commit checks passed.\n", "url": "https://github.com/llm-d/llm-d/pull/2452"}
{"number": 2451, "title": "docs: fix ModelExpress base image component path", "state": "closed", "created_at": "2026-09-07T10:02:22Z", "updated_at": "2026-09-22T09:54:37Z", "closed_at": "2026-09-11T16:33:09Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-11T16:33:09Z", "body": "The ModelExpress build command reads `gpu-vllm/kustomization.yaml`, but the shared release image component lives under `gpu-vllm/release/`. Update the command so the base image lookup resolves to an existing component. The regression checks that the documented component exists and contains a complete model-server image replacement.\n\nValidation: `python -m pytest -q scripts/tests`: 29 passed.\nChanged-file pre-commit checks passed.\n", "url": "https://github.com/llm-d/llm-d/pull/2451"}
{"number": 2450, "title": "fix: handle malformed metrics summaries", "state": "closed", "created_at": "2026-09-07T10:02:19Z", "updated_at": "2026-09-22T09:54:37Z", "closed_at": "2026-09-08T16:10:59Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T16:10:59Z", "body": "Nightly verification can crash when a metrics summary contains valid JSON with an unexpected object shape or a nonnumeric metric value. Validate the summary containers before using them and report a failed check for nonnumeric or nonfinite values. Regression tests cover malformed summary objects and aggregated/per-pod metric values.\n\nValidation: `python -m pytest -q .github/scripts/nightly-e2e-verification/test_verify_helpers.py`: 57 passed.\nChanged-file pre-commit checks passed.\n", "url": "https://github.com/llm-d/llm-d/pull/2450"}
{"number": 2404, "title": "fix(interactive-pod): scope gateway discovery to namespace", "state": "closed", "created_at": "2026-08-30T07:56:58Z", "updated_at": "2026-09-22T09:54:37Z", "closed_at": "2026-09-08T15:44:16Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T15:44:16Z", "body": "`prepare-inference.sh` uses `NAMESPACE` to construct the service endpoint but omits it from the Gateway and Service queries. Discovery fails when the configured namespace differs from the current kubectl context.\n\nPass `-n \"${NAMESPACE}\"` to both queries. The shell regression test rejects unnamespaced queries and checks the discovered endpoint.\n\n## Reproduction\n\nWith `NAMESPACE=target-ns` and kubectl requiring an explicit namespace, the unmodified `main` branch reports:\n\n```text\nexit=1\nError, could not find the Gateway\n```\n\nAfter this change:\n\n```text\nexit=0\nGATEWAY_SERVICE_ENDPOINT=http://inference-gateway.target-ns.svc.cluster.local\n```\n\n## Validation\n\n```bash\nbash helpers/interactive-pod/build/tests/test-namespace-queries.sh\nbash -n helpers/interactive-pod/build/prepare-inference.sh \\\n  helpers/interactive-pod/build/tests/test-namespace-queries.sh\nshellcheck helpers/interactive-pod/build/prepare-inference.sh \\\n  helpers/interactive-pod/build/tests/test-namespace-queries.sh\ngit diff --check upstream/main...HEAD\n```\n\nAll commands passed. This change was also merge-tested with #2400; both interactive-pod regression tests pass on the combined tree.\n", "url": "https://github.com/llm-d/llm-d/pull/2404"}
{"number": 2403, "title": "fix(observability): start P/D traffic statistics at zero", "state": "closed", "created_at": "2026-08-30T07:56:11Z", "updated_at": "2026-09-22T09:54:37Z", "closed_at": "2026-08-31T19:18:57Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-08-31T19:18:56Z", "body": "## Summary\n\nInitialize the P/D traffic generator statistics files as empty counters so an idle run reports zero requests.\n\n## Root Cause\n\nThe statistics are calculated by counting lines in the counter files. The initial `echo \"0\"` writes one line to each file, so the generator reports one total, one successful request, and one failed request before any request has been sent.\n\n## Changes\n\n- Truncate the total, success, and failure counter files without writing an initial marker line.\n- Add a regression test covering the initial statistics state.\n\n## Reproduction\n\nOn the unmodified `main` branch, before sending any requests:\n\n```text\n1 1 1\n```\n\nAfter this change:\n\n```text\n0 0 0\n```\n\n## Validation\n\n```bash\nPYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider -q \\\n  scripts/tests/test_pd_traffic_stats.py\nbash -n guides/recipes/observability/generate-traffic-pd.sh\nshellcheck --severity=error -x \\\n  guides/recipes/observability/generate-traffic-pd.sh\ngit diff --check upstream/main...HEAD\n```\n\nAll commands passed. The regression test reports `1 passed`.\n\n## Scope\n\nThis only changes counter initialization. Per-request counter updates and final statistics aggregation are unchanged.\n", "url": "https://github.com/llm-d/llm-d/pull/2403"}
{"number": 2400, "title": "fix(interactive-pod): unquote discovered model name", "state": "closed", "created_at": "2026-08-30T07:40:17Z", "updated_at": "2026-09-22T09:54:37Z", "closed_at": "2026-09-08T15:41:49Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T15:41:49Z", "body": "`prepare-inference.sh` reads the served model ID with `jq` without raw output, leaving literal quotes in `MODEL_NAME`. The value passed to guidellm therefore differs from the served model name.\n\nUse `jq -r` to extract the raw ID, retaining the existing error for a missing or empty ID. A shell regression test covers automatic discovery.\n\n## Reproduction\n\nOn the unmodified `main` branch, the mock `/v1/models` response produces a shell value containing literal quotes:\n\n```text\n<\"served-model\">\n```\n\nAfter this change, the same response produces:\n\n```text\n<served-model>\n```\n\n## Validation\n\n```bash\nbash helpers/interactive-pod/build/tests/test-model-name.sh\nbash -n helpers/interactive-pod/build/prepare-inference.sh \\\n  helpers/interactive-pod/build/tests/test-model-name.sh\nshellcheck helpers/interactive-pod/build/prepare-inference.sh \\\n  helpers/interactive-pod/build/tests/test-model-name.sh\ngit diff --check upstream/main...HEAD\n```\n\nAll commands passed. The regression test fails against the unmodified `main` implementation and passes with this change.\n", "url": "https://github.com/llm-d/llm-d/pull/2400"}
{"number": 2448, "title": "docs: document GKE gateway guide prerequisites", "state": "closed", "created_at": "2026-09-07T10:02:13Z", "updated_at": "2026-09-22T09:54:36Z", "closed_at": "2026-09-11T11:56:45Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-11T11:56:45Z", "body": "The GKE gateway guide uses `GUIDE_NAME`, `MODEL_NAME`, and `NAMESPACE` in its commands without introducing them in the prerequisites. Document that they come from the selected well-lit path guide, before sourcing the repository environment file.\n\nValidation: Checked every variable use against the prerequisites; changed-file pre-commit checks passed.\nChanged-file pre-commit checks passed.\n", "url": "https://github.com/llm-d/llm-d/pull/2448"}
{"number": 2446, "title": "fix: inherit variables between Dockerfile stages", "state": "closed", "created_at": "2026-09-07T10:02:07Z", "updated_at": "2026-09-22T09:54:36Z", "closed_at": "2026-09-08T20:55:23Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T20:55:23Z", "body": "When a Dockerfile uses `FROM base AS build`, ARG and ENV variables declared in `base` are available in `build`. The linter currently starts the child stage with empty variable sets and reports inherited script requirements as missing. Copy the base-stage declarations into the child stage and add a regression for both ARG and ENV inheritance.\n\nValidation: `python -m pytest -q scripts/tests`: 29 passed.\nChanged-file pre-commit checks passed.\n", "url": "https://github.com/llm-d/llm-d/pull/2446"}
{"number": 2406, "title": "fix(guides): report unhashable YAML mapping keys", "state": "closed", "created_at": "2026-08-30T07:58:21Z", "updated_at": "2026-09-22T09:54:36Z", "closed_at": "2026-09-08T16:18:18Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T16:18:18Z", "body": "A sequence used as a YAML mapping key makes `_no_duplicate_keys()` raise an uncaught `TypeError` during dictionary membership checking.\n\nConvert that error to a `ConstructorError` with the source location so `parse_guide_yaml()` returns a normal validation finding. A regression test covers sequence mapping keys.\n\n## Reproduction\n\nOn the unmodified `main` branch:\n\n```text\nTypeError: unhashable type: 'list'\n```\n\nAfter this change:\n\n```text\nline 1: unhashable mapping key; YAML mapping keys must be hashable\n```\n\nThe parser returns `(None, finding)` instead of raising an uncaught exception.\n\n## Validation\n\n```bash\nPYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider -q \\\n  scripts/tests/test_guide.py\nPYTHONDONTWRITEBYTECODE=1 python scripts/guide.py check guides/*/\nPYTHONDONTWRITEBYTECODE=1 python scripts/guide.py render guides/*/ --check\ngit diff --check upstream/main...HEAD\n```\n\nAll commands passed: `28 passed`, all four guides validated, and all generated READMEs are up to date. This change was also merge-tested with #2405; the combined guide test suite reports `29 passed`.\n", "url": "https://github.com/llm-d/llm-d/pull/2406"}
{"number": 2405, "title": "fix(guides): validate environment variable names", "state": "closed", "created_at": "2026-08-30T07:57:44Z", "updated_at": "2026-09-22T09:54:36Z", "closed_at": "2026-09-08T15:36:13Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T15:36:13Z", "body": "Guide validation accepts invalid `env.static` names such as `BAD-NAME`, which later render as malformed shell assignments.\n\nRequire shell identifiers matching `[A-Za-z_][A-Za-z0-9_]*`, report invalid names as findings, and exclude them from the declared-variable set. Regression coverage includes invalid hyphenated names and valid lowercase names.\n\n## Reproduction\n\nOn the unmodified `main` branch:\n\n```text\nfindings.ok() == True\nexport BAD-NAME=value\n```\n\nAfter this change:\n\n```text\nfindings.ok() == False\nenv.static.BAD-NAME: variable name must be a shell identifier matching [A-Za-z_][A-Za-z0-9_]*\n```\n\n## Validation\n\n```bash\nPYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider -q \\\n  scripts/tests/test_guide.py\nPYTHONDONTWRITEBYTECODE=1 python scripts/guide.py check guides/*/\nPYTHONDONTWRITEBYTECODE=1 python scripts/guide.py render guides/*/ --check\ngit diff --check upstream/main...HEAD\n```\n\nAll commands passed: `28 passed`, all four guides validated, and all generated READMEs are up to date.\n", "url": "https://github.com/llm-d/llm-d/pull/2405"}
{"number": 2402, "title": "fix(nightly-e2e-verification): reject empty benchmark workspaces", "state": "closed", "created_at": "2026-08-30T07:55:45Z", "updated_at": "2026-09-22T09:54:36Z", "closed_at": "2026-08-31T15:35:03Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-08-31T15:35:03Z", "body": "## Summary\n\nReject an empty `LLMDBENCH_WORKSPACE` before searching for benchmark results.\n\n## Root Cause\n\n`Path(\"\")` resolves to the current working directory. If `LLMDBENCH_WORKSPACE` was unset and the current directory happened to contain matching `run_metadata.yaml` files, `find_results_dirs()` could select unrelated results instead of reporting that the workspace was missing.\n\n## Changes\n\n- Validate that `workspace` is a non-empty string in `find_results_dirs()`.\n- Return `None` with an explicit error instead of recursively scanning the current directory.\n- Strengthen the regression test by placing matching metadata in a temporary current directory.\n\n## Reproduction\n\nOn the unmodified `main` branch, an empty workspace can search the current directory and return unrelated results:\n\n```text\nresult=[PosixPath('user-1/results/exp')]\n```\n\nAfter this change:\n\n```text\nLLMDBENCH_WORKSPACE is empty; refusing to search the current directory\nresult=None\n```\n\n## Validation\n\n```bash\nPYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider -q \\\n  .github/scripts/nightly-e2e-verification/test_verify_helpers.py\ngit diff --check upstream/main...HEAD\n```\n\nAll commands passed. The test suite reports `51 passed`.\n\n## Scope\n\nThis only changes handling of missing or blank workspace input. Valid workspace paths and result selection behavior are unchanged.\n", "url": "https://github.com/llm-d/llm-d/pull/2402"}
{"number": 2401, "title": "fix(smoke-test): preserve healthcheck failure accounting", "state": "closed", "created_at": "2026-08-30T07:55:14Z", "updated_at": "2026-09-22T09:54:36Z", "closed_at": "2026-09-08T16:33:28Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T16:33:28Z", "body": "Healthcheck functions update the pass, fail, and warning counters, but command substitution runs those functions in subshells and discards the updates. Failing endpoints can consequently produce a HEALTHY report and exit code zero.\n\nRun the checks in the current shell and store their results in a result variable. A regression test uses a local HTTP server returning 503.\n\n## Reproduction\n\nWith an endpoint returning `503` for `/health`, `/v1/models`, and inference requests, the unmodified `main` branch reports:\n\n```text\nexit=0\nStatus: HEALTHY\nChecks: 0 passed, 0 failed, 0 warned\n```\n\nAfter this change:\n\n```text\nexit=1\nStatus: UNHEALTHY\nChecks: 0 passed, 2 failed, 1 warned\n```\n\n## Validation\n\n```bash\nPYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider -q \\\n  helpers/smoke-test/tests/test_healthcheck_failure.py\nbash -n helpers/smoke-test/healthcheck.sh\nshellcheck helpers/smoke-test/healthcheck.sh\ngit diff --check upstream/main...HEAD\n```\n\nAll commands passed. The regression test reports `1 passed`.\n", "url": "https://github.com/llm-d/llm-d/pull/2401"}
{"number": 2438, "title": "fix(ci): wait for flow-control request bursts", "state": "closed", "created_at": "2026-09-04T10:06:03Z", "updated_at": "2026-09-22T09:54:35Z", "closed_at": "2026-09-08T16:17:48Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T16:17:48Z", "body": "The flow-control validator launches background request bursts inside command substitutions, so their PIDs cannot reliably be waited on by the parent shell. The final metrics scrape can run before requests finish and report misleading flow-control configuration failures.\n\nStart each band in the parent shell and capture its PID with `$!`. Wait for every band, including when an earlier band fails. Before evaluating metrics, check each band's process exit status and require the expected number of HTTP 2xx responses. Failed or incomplete bursts print their logs and stop validation before configuration assertions. These diagnostics run in the existing nightly validator.\n\nValidation:\n- Six temporary local shell scenarios cover concurrent success, failures in the premium and standard processes, HTTP 500, HTTP 302, and missing responses.\n- Every scenario waits for all three bands before cleanup; request failures stop before flow-control metric assertions.\n- `bash -n`, `shellcheck`, applicable changed-file pre-commit hooks, and `git diff --check` pass.\n\nLocal validation used temporary fixtures outside the repository. No live Kubernetes E2E run was performed.\n", "url": "https://github.com/llm-d/llm-d/pull/2438"}
{"number": 2437, "title": "fix(ci): reject HTTP errors in gateway smoke tests", "state": "closed", "created_at": "2026-09-04T10:06:02Z", "updated_at": "2026-09-22T09:54:35Z", "closed_at": "2026-09-08T16:05:27Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T16:05:27Z", "body": "The gateway smoke validator currently reports success when inference endpoints return HTTP errors with JSON bodies. For example, twenty HTTP 400 responses can still produce `All 10 iterations succeeded` because curl normally exits zero for HTTP errors.\n\nAdd `--fail-with-body` to chat and completion requests, retain the response body, and print the HTTP status. Failure messages distinguish HTTP 4xx/5xx, curl/kubectl execution errors, and responses containing no JSON object. The existing nightly smoke validator exercises these diagnostics.\n\nValidation:\n- Local HTTP server and real curl: HTTP 200 completes all ten iterations; independent HTTP 400/500 errors on both endpoints fail and retain response-body diagnostics and pod cleanup.\n- Direct shell checks cover HTTP error, execution error, missing JSON object, and success diagnostics.\n- `bash -n`, `shellcheck -x --severity=warning`, applicable changed-file pre-commit hooks, and `git diff --check` pass.\n\nLocal validation used a temporary kubectl adapter outside the repository. No live Kubernetes E2E run was performed.\n", "url": "https://github.com/llm-d/llm-d/pull/2437"}
{"number": 2436, "title": "fix(ci): separate GKE native offloading badge", "state": "closed", "created_at": "2026-09-04T10:05:44Z", "updated_at": "2026-09-22T09:54:35Z", "closed_at": "2026-09-08T15:41:01Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-08T15:41:01Z", "body": "The GKE GPU native-offloading and LMCache nightlies write to the same badge file, so whichever finishes last sets the status shown for both tests.\n\nUse `tiered-prefix-cache-gke-cpu-native` for the native workflow and update its link in the release testing matrix.\n\nVerified that the two workflows produce distinct badge URLs and that the README matches the generated matrix. Pre-commit still reports Markdown table formatting and undeclared workflow inputs; these also fail on unmodified main.\n", "url": "https://github.com/llm-d/llm-d/pull/2436"}
{"number": 2447, "title": "fix(multimodal): avoid duplicate USER environment", "state": "open", "created_at": "2026-09-07T10:02:10Z", "updated_at": "2026-09-22T09:34:56Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "The E/P/D encode deployment includes `USER=llm-d` twice. Remove the duplicate while retaining the existing workaround value.\n\nFollowing review, the PR contains only the three-line removal in `encode-deployment.yaml`; the additional regression test file was removed as requested.\n\nValidation: both the base and GKE overlays were rendered with `kustomize build` after the review update. All rendered container environment names are unique, and the encode container retains exactly one `USER=llm-d` entry.\n", "url": "https://github.com/llm-d/llm-d/pull/2447"}
{"number": 2548, "title": "fix(p2p): enable experimental router plugins", "state": "closed", "created_at": "2026-09-22T02:30:45Z", "updated_at": "2026-09-22T07:28:51Z", "closed_at": "2026-09-22T07:28:50Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T07:28:50Z", "body": "## What this PR does\n\nEnable `router.epp.flags.allow-experimental-plugins` for the P2P KV-cache-sharing guide. Its `p2p-source-producer` plugin is Alpha, so the EPP exits during startup validation without this opt-in.\n\nThis reproduces with both standalone and gateway Charts on v0.10.0 and v0.11.0-rc.1; it is not a new RC regression.\n\n## Validation\n\n- Reproduced the startup failure in a local Kind cluster with both Chart modes and versions. Adding only this flag made both EPP replicas Ready with zero restarts in each combination.\n- `pre-commit run --files guides/p2p-kv-cache-sharing/router/p2p-kv-cache-sharing.values.yaml`\n- `make verify`\n", "url": "https://github.com/llm-d/llm-d/pull/2548"}
{"number": 2332, "title": "fix(guides): unify PROVIDER_NAME default to none", "state": "open", "created_at": "2026-08-19T19:43:02Z", "updated_at": "2026-09-22T01:18:15Z", "closed_at": null, "labels": ["lifecycle/stale"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`guides/optimized-baseline` and `guides/modelexpress-p2p` documented different defaults for `PROVIDER_NAME`, the value passed to `--set provider.name=` in the router gateway install. PR #2260 changed the optimized-baseline README default to `none` inside the rendered `guide:env.static` block. Both `guide.yaml` files, the render source of truth, still said `gke`, so `scripts/guide.py render` would have reverted the README.\r\n\r\nBoth guides default `PROVIDER_NAME` to `none`, matching the router chart's default for `provider.name`. GKE users must set `PROVIDER_NAME=gke`: with `none`, the chart omits the `HealthCheckPolicy`, the load balancer health check fails against vLLM, and requests return 503. Each `guide.yaml` carries a comment above the variable stating the requirement, and each README states it in prose above the env block.\r\n\r\nRelated fixes in the same files:\r\n\r\n- optimized-baseline `guide.yaml` declares `ROUTER_CHART_VERSION`. The variable existed only in the README's rendered block, so `guide.py render` deleted it and `render --check` failed on `main`.\r\n- `guides/recipes/router/README.md` documented `provider.name=<gke|istio|none>`. The placeholder lists all four accepted values, and a paragraph describes what each value renders.\r\n\r\nSeven other guide READMEs export `PROVIDER_NAME=gke` without a warning; a follow-up issue should track that sweep.\r\n\r\n**Verification:**\r\n\r\n- `scripts/guide.py check` and `scripts/guide.py render --check` pass for both guides.\r\n- The scheduled GKE nightlies for both guides use `gateway_class: epponly` (standalone mode), which does not read `PROVIDER_NAME`, so the default change does not affect CI.\r\n\r\nFixes #2330\r\n", "url": "https://github.com/llm-d/llm-d/pull/2332"}
{"number": 2512, "title": "Rename the wide-ep-lws guide to wide-ep", "state": "closed", "created_at": "2026-09-16T21:02:03Z", "updated_at": "2026-09-21T18:57:05Z", "closed_at": "2026-09-21T18:56:26Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T18:56:26Z", "body": "## What\r\n\r\nRenames the `wide-ep-lws` guide to `wide-ep`. The guide deploys a DisaggregatedSet, so the lws substring is a leftover from when LeaderWorkerSets were the base.\r\n\r\nPure rename, 124 files, no functional change:\r\n\r\n- `guides/wide-ep-lws/` -> `guides/wide-ep/`\r\n- DS objects: `wide-ep-lws-nvidia-gpu-vllm*` -> `wide-ep-nvidia-gpu-vllm*`\r\n- `llm-d.ai/guide` label, `wide-ep-epp` service, `router/wide-ep.values.yaml`\r\n- The 10 nightly and consolidate-status workflows: filenames, `standup_scenario`, concurrency groups, namespaces, bucket paths, badge names\r\n- All cross-references (agentic-serving, precise-prefix-cache-routing, docs, CONTRIBUTING)\r\n\r\nEvery kustomization in the guide (49) renders byte-identical to before modulo the name substitution, and the agentic-serving composition still builds.\r\n\r\nKnown side effects:\r\n\r\n- Inbound links to `guides/wide-ep-lws/` (blog posts, external docs) break.\r\n- The regression bucket path moves to `llm-d-benchmarks/regressions/wide-ep`, so nightly comparison history restarts under the new path.\r\n", "url": "https://github.com/llm-d/llm-d/pull/2512"}
{"number": 2480, "title": "feat(sglang): add tiered prefix cache GKE nightly lane", "state": "closed", "created_at": "2026-09-12T05:38:30Z", "updated_at": "2026-09-21T17:31:01Z", "closed_at": "2026-09-21T17:31:01Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T17:31:01Z", "body": "## Summary\n\nAdd the nightly E2E workflow and status consolidation job for SGLang tiered prefix cache CPU offloading on GKE GPU, closing the remaining automated-testing gap for SGLang alongside `optimized-baseline` and `pd-disaggregation`.\n\nThe lane resolves to `guides/tiered-prefix-cache/modelserver/gpu/sglang/native/cpu/gke` (`offloading_target: cpu`, `connector: native`). L3 / filesystem offloading is **not** added to CI here.\n\n## Overlay fixes required for CI execution\n\nTwo defects in the `cpu` overlay prevented `llm-d-infra` from standing it up:\n\n- **Model auto-detection.** `llm-d-infra` derives the served model by stripping the `--model-path=` prefix and discarding anything still shaped like a flag. Bare option values on their own list items (`write_through`, `kernel`) survive that filter, so detection returned 3 lines instead of 1 and the result was passed on as `--model`. Normalizing to `--flag=value` restores single-line detection.\n- **`decode_pods` override.** Renaming `base/patch-sglang.yaml` to `base/patch-decode.yaml` lets `llm-d-infra`'s glob (`patch-{vllm,decode}.yaml`) apply the functional-gate override. Without it the lane would stand up the guide's benchmark topology of 8 replicas at TP=2.\n\n## Memory sizing\n\nRaise decode container memory `limits` from `400Gi` to `512Gi` and `requests` from `200Gi` to `400Gi`. `--hicache-size` is **per TP rank**, so `200` at TP=2 pins ~372.5 GiB of host memory; together with the `20Gi` memory-backed `/dev/shm` volume charged to the same cgroup, a `400Gi` limit left no room for Python and CUDA runtime allocations.\n\n## Scope note: the `fs` overlay\n\nThe `fs` overlay is **not exercised by this lane**. It is fixed here because it carries the identical model-detection defect (5 bare values rather than 3) and would fail the moment CI is pointed at it. That change is limited to the argument form plus one wrong field name in a comment — no behavioural change.\n\n## Workflow scheduling and isolation\n\nRuns at `18:30 UTC` daily, 3 hours after P/D SGLang on the same cluster, with status consolidation at `20:30 UTC`. Dedicated namespace, Helm release (`llmdbenchcicdr-gke-sglang-tiered`), and runner workspace directory (`/tmp/llmdbenchcicdk-gke-sglang-tiered`) so runs do not collide with sibling GKE lanes.\n\n## Testing\n\nReplayed `llm-d-infra`'s model detection pipeline against all four `sglang` overlays — each now returns exactly one line (`Qwen/Qwen3-32B`), where `cpu/gke` previously returned 3 and `fs/gke` 5:\n\n```bash\nkubectl kustomize <overlay> \\\n  | yq .spec.template.spec.containers[0].args \\\n  | sed 's^--model-path=^^g' \\\n  | grep -Ev \"\\- \\--|\\- '|\\---|\\- \\||    \\--|.*exec vllm|'|null|#|\\\"|find|START_RANK\" \\\n  | sed -e 's^ ^^g' -e 's|^-||g' -e 's^\\\\^^g' -e '/^$/d' | uniq\n```\n\nRendering, lint, and matrix sync:\n\n```bash\nkubectl kustomize guides/tiered-prefix-cache/modelserver/gpu/sglang/native/cpu/gke\nkubectl kustomize guides/tiered-prefix-cache/modelserver/gpu/sglang/native/fs/gke\npre-commit run --files \\\n  .github/workflows/nightly-e2e-tiered-prefix-cache-gke-cpu-gpu-sglang-native.yaml \\\n  .github/workflows/consolidate-status-tiered-prefix-cache-gke-cpu-gpu-sglang-native.yaml \\\n  guides/tiered-prefix-cache/modelserver/gpu/sglang/base/kustomization.yaml \\\n  guides/tiered-prefix-cache/modelserver/gpu/sglang/base/patch-decode.yaml \\\n  guides/tiered-prefix-cache/modelserver/gpu/sglang/native/cpu/base/patch-sglang.yaml \\\n  guides/tiered-prefix-cache/modelserver/gpu/sglang/native/fs/base/patch-sglang.yaml\ntouch .release-team && python3 scripts/sync-nightly-matrix.py --check && rm -f .release-team\n```\n\n> [!NOTE]\n> `scripts/sync-nightly-matrix.py --check` also fails on a clean `main` checkout — the `wide-ep-lws` AMD MoRIIO workflow was added without regenerating `release/README.md`. That drift is pre-existing and deliberately left out of this PR; `release/README.md` here changes only the row for this lane.\n", "url": "https://github.com/llm-d/llm-d/pull/2480"}
{"number": 2545, "title": "fix(interactive-pod): respect namespace for gateway lookup", "state": "closed", "created_at": "2026-09-21T08:02:42Z", "updated_at": "2026-09-21T17:25:48Z", "closed_at": "2026-09-21T17:25:48Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T17:25:48Z", "body": "run-guide-llm.sh queried the Gateway without a namespace. When the interactive pod ran outside the default namespace, kubectl could not find the Gateway, and the benchmark then proceeded with an empty address instead of reporting the cause.\r\n\r\nPass the injected NAMESPACE to kubectl, enable strict shell error handling, and fail explicitly when the Gateway has no assigned address.\r\n\r\nTests:\r\n- bash -n helpers/interactive-pod/build/run-guide-llm.sh\r\n- git diff --check\r\n- Fake kubectl/curl/guidellm regression checks for a non-default namespace, an empty Gateway address, and a missing NAMESPACE.", "url": "https://github.com/llm-d/llm-d/pull/2545"}
{"number": 2510, "title": "Added recipe for inference cost", "state": "closed", "created_at": "2026-09-16T14:24:12Z", "updated_at": "2026-09-21T17:12:09Z", "closed_at": "2026-09-21T17:12:09Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T17:12:09Z", "body": "This PR is the same as https://github.com/llm-d/llm-d/pull/1926 except that it doesn't update the existing guides to support cost.\r\n\r\nThis PR implements an llm-d guide to deploy inference cost tracking capabilities for llm-d.\r\nImplements the first phase of [this proposal](https://github.com/simanadler/llm-d/blob/5c5bd7c5981ea155a9059787a63ec0ec728d409e/docs/proposals/inference-costs.md)\r\n\r\nOpenCost is used to provide the cost metrics and APIs, and the guide describes how to deploy these capabilities in a cluster to get the costs associated with the models run via llm-d.\r\n\r\nThe related https://github.com/opencost/opencost/pull/3845 has been merged by the OpenCost maintainers.\r\n\r\n NO changes are made to files in other guides to align model name labels for accurate cost tracking.", "url": "https://github.com/llm-d/llm-d/pull/2510"}
{"number": 2541, "title": "fix(workload-autoscaling): avoid mutating AMD model patch", "state": "closed", "created_at": "2026-09-21T06:54:07Z", "updated_at": "2026-09-21T17:08:35Z", "closed_at": "2026-09-21T17:08:35Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T17:08:35Z", "body": "Bug: The AMD nightly deployment used yq -i to change the tracked AMD model-server patch from 8 replicas to 2. A failed or repeated run left the worktree modified and caused later deployments to inherit the nightly-only replica count.\r\n\r\nFix: Apply the replica override in the temporary Kustomize overlay generated under OUTPUT_DIR. The tracked patch remains unchanged. Add a regression test that checks the source patch hash and the generated overlay.\r\n\r\nTest: bash -n guides/workload-autoscaling/scripts/nightly-deploy-amd.sh; UV_CACHE_DIR=/tmp/llmd-uv-cache uv run --with 'pyyaml==6.*' --with pytest python -m pytest scripts/tests/test_nightly_deploy_amd.py -q; git diff --check", "url": "https://github.com/llm-d/llm-d/pull/2541"}
{"number": 2543, "title": "fix(guides): make calibration script executable", "state": "closed", "created_at": "2026-09-21T07:43:28Z", "updated_at": "2026-09-21T17:07:18Z", "closed_at": "2026-09-21T17:07:18Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T17:07:18Z", "body": "The keda-epp-token-aware guide invokes calibrate.sh directly, but the script was tracked with mode 100644. Guide execution therefore failed with Permission denied and exit code 126 before calibration could run.\r\n\r\nMark calibrate.sh as executable with mode 100755 so the guide can invoke it directly without changing the script contents.\r\n\r\nTests:\r\n- bash -n guides/recipes/router/calibration/calibrate.sh\r\n- test -x guides/recipes/router/calibration/calibrate.sh\r\n- git diff --check", "url": "https://github.com/llm-d/llm-d/pull/2543"}
{"number": 2538, "title": "docs(observability): fix EPP tracing values key", "state": "closed", "created_at": "2026-09-20T10:02:57Z", "updated_at": "2026-09-20T19:30:38Z", "closed_at": "2026-09-20T19:30:38Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T19:30:37Z", "body": "## What this PR does\n\nUpdates the distributed tracing guide to use the current llm-d Router Helm\nvalues key, `router.tracing`, instead of the legacy\n`inferenceExtension.tracing` namespace.\n\nThe Router Helm values were migrated from `inferenceExtension` to `router` in\n[llm-d-router#1262](https://github.com/llm-d/llm-d-router/pull/1262).\n[llm-d-router#1302](https://github.com/llm-d/llm-d-router/pull/1302) then added\nstrict validation that rejects the legacy top-level `inferenceExtension` key.\nAs a result, following the existing tracing guide causes Helm rendering to fail\nbefore deployment.\n\nThis change updates both the configuration summary and the EPP tracing example\nto use `router.tracing`.\n\n## Validation\n\n- Confirmed the legacy `inferenceExtension.tracing.enabled=true` value fails\n  against the current Router chart with:\n\n  ```text\n  Top-level 'inferenceExtension' is deprecated. Please migrate your values to\n  'router.epp' and other 'router.*' fields.\n  ```\n\n- Rendered the current Router chart with `router.tracing.enabled=true` and\n  confirmed the EPP Deployment contains:\n  - `--tracing=true`\n  - `OTEL_EXPORTER_OTLP_ENDPOINT`\n  - `OTEL_TRACES_SAMPLER_ARG`\n- `git diff --check` passes.\n\n## Related changes\n\n- llm-d/llm-d-router#1262\n- llm-d/llm-d-router#1302\n", "url": "https://github.com/llm-d/llm-d/pull/2538"}
{"number": 2539, "title": "docs(autoscaling): fix EPP image pin value", "state": "closed", "created_at": "2026-09-20T11:26:16Z", "updated_at": "2026-09-20T19:30:13Z", "closed_at": "2026-09-20T19:30:13Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T19:30:13Z", "body": "## What this PR does\n\nUpdates the token-aware autoscaling guide to use `router.epp.image.tag` when pinning the EPP image. The previously documented `ROUTER_EPP_VERSION` environment variable is not consumed by the Helm install commands.\n\n## Validation\n\n- Confirmed only `router.epp.image.tag` changes the rendered EPP image tag.\n- Guide README render check passes.\n\n## Related changes\n\n- llm-d/llm-d#2466", "url": "https://github.com/llm-d/llm-d/pull/2539"}
{"number": 2525, "title": "docs(guides): rename gke-pod-snapshots guide to pod-snapshot", "state": "closed", "created_at": "2026-09-17T21:08:32Z", "updated_at": "2026-09-20T19:26:17Z", "closed_at": "2026-09-20T19:26:17Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T19:26:17Z", "body": "This is a followup to #2489. Replace every instance of `gke-pod-snapshot` with `pod-snapshot` so the well lit path is not presented as GKE only.\r\n\r\n- guides/gke-pod-snapshots/ -> guides/pod-snapshot/\r\n- router/gke-pod-snapshots.values.yaml -> router/pod-snapshot.values.yaml\r\n- guide.yaml name, GUIDE_NAME, Helm release, and EPP service name\r\n- NAMESPACE: llm-d-gke-pod-snapshots -> llm-d-pod-snapshot\r\n- the llm-d.ai/guide label in kustomization.yaml, podsnapshotpolicy.yaml, and the router values\r\n- namePrefix, storageConfigName, and the Workload Identity service account\r\n\r\nLeft unchanged:\r\n\r\nSNAPSHOT_PROVIDER=gke_gvisor, which is a backend identifier matched in docker/scripts/snapshot/providers.py rather than guide branding, and the modelserver/gpu/vllm/gke/ overlay directory.\r\n\r\nThe Workload Identity principal embeds both the namespace and the KSA name (.../subject/ns/<NAMESPACE>/sa/<KSA>), so the IAM binding in the prerequisites moved with them. Verified that the ServiceAccount Kustomize renders matches the KSA in the gcloud add-iam-policy-binding command; a mismatch there surfaces only at checkpoint time as a GCS 403.", "url": "https://github.com/llm-d/llm-d/pull/2525"}
{"number": 2443, "title": "feat(guides): serve encode disaggregation on the vLLM nightly image", "state": "open", "created_at": "2026-09-06T09:34:35Z", "updated_at": "2026-09-20T09:24:11Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\n\r\nMove the Coordinator Disaggregation guide and the multimodal Encode Disaggregation\r\nguide (E/PD and E/P/D) from pinned development vLLM images to the upstream vLLM\r\nnightly image. Configure the\r\n[ECCPU Connector](https://docs.vllm.ai/en/latest/features/ec_cpu_connector/) in its\r\nP2P NIXL mode, with the V2 model runner that it requires. In the Coordinator guide,\r\nreplace the Coordinator's CPU render sidecar with a Service that fronts the prefill\r\nmodel servers. Add CoreWeave overlays for E/PD and E/P/D. Make the vLLM model\r\nservers and the Coordinator pull their moving image tags (`:nightly`, `:main`) on\r\nevery start.\r\n\r\nThe nightly pin is temporary: we expect to move to the vLLM 0.29.0 release image\r\nonce it ships, since encoder-cache transfer is not in any released version yet.\r\n\r\n## Changes\r\n\r\n### Both guides: vLLM nightly image and ECCPU Connector\r\n\r\n- **Image:** the `gpu-vllm/nightly` component (`docker.io/vllm/vllm-openai:nightly`)\r\n  replaces `ghcr.io/revit13/vllm-openai:61e53561a` (Coordinator guide) and the\r\n  `gpu-vllm/ec-connector` component (`ghcr.io/llm-d/vllm-openai:ec-cpu-connector-support`,\r\n  Encode Disaggregation guide). The component already adds the `USER=llm-d`\r\n  workaround for vllm-project/vllm#44548, so the inline copies are removed.\r\n- **`imagePullPolicy: Always`:** the `gpu-vllm/nightly` component now sets it on the\r\n  vLLM container. `:nightly` moves every day, and with `IfNotPresent` a node that\r\n  cached an older nightly runs a different vLLM build. In a live E/PD test, one encode\r\n  pod ran vLLM 0.28.1 from a cached image while the other pods ran 0.29.1. The EC\r\n  connector compatibility check includes the vLLM version.\r\n- **`VLLM_USE_V2_MODEL_RUNNER=1`:** set only on instances that have\r\n  `--ec-transfer-config`, because `ECCPUConnector` raises `ValueError` without the V2\r\n  model runner:\r\n  - Coordinator guide: encode, prefill\r\n  - E/P/D: encode, prefill\r\n  - E/PD: encode, decode\r\n- **EC config:** P2P NIXL mode (`\"ec_enable_nixl\": true`). `ec_cpu_bytes`, which is\r\n  required and has no default, replaces `num_ec_blocks`. The sizes keep the earlier\r\n  capacity (Qwen3-VL-32B uses 40,960 bytes per encoder token):\r\n\r\n  | Instances | Before | `ec_cpu_bytes` | Blocks | `/dev/shm` |\r\n  |---|---|---|---|---|\r\n  | E/P/D encode, E/P/D prefill, E/PD encode | `num_ec_blocks: 1000000` | 40 GiB | 1,048,576 | 90Gi |\r\n  | E/PD decode | default (100,000) | 4 GiB | 104,857 | 20Gi |\r\n  | Coordinator encode, prefill | default (100,000) | 4 GiB | 104,857 | 8Gi |\r\n\r\n- **Docs:** each `--ec-transfer-config` has a comment that links to\r\n  https://docs.vllm.ai/en/latest/features/ec_cpu_connector/.\r\n\r\n### Coordinator Disaggregation guide\r\n\r\n- **`--enable-scale-out` on all model servers:** `vllm serve` serves `/render`,\r\n  `/derender` and `/inference/v1/generate` only with this flag\r\n  (vllm-project/vllm#55176 replaced `VLLM_ENABLE_SCALE_OUT_ENDPOINTS` with it).\r\n  Prefill needs it for render. All roles need it because the Coordinator sends\r\n  `/inference/v1/generate` to every phase for `/inference/v1/generate`, Responses\r\n  and Messages requests.\r\n- **Render Service (`render/`, new):** render (`/v1/*/render`) is CPU-only work in\r\n  the vLLM API server, so any vLLM instance that serves the model can handle it.\r\n  Before this change, all render calls went to one `vllm launch render` sidecar in\r\n  the Coordinator pod. llm-d/llm-d-router#2701 (fixes llm-d/llm-d-router#2696) adds\r\n  render balancing across discovered endpoints, but only for the EPP\r\n  `token-producer`. The Coordinator's `render` step still takes one `address`. So\r\n  this guide spreads the render load with a Service: `coord-disaggregation-render`\r\n  fronts the prefill pods on their `modelserver` port, and render capacity grows\r\n  with the prefill replicas. The render step address is\r\n  `http://coord-disaggregation-render.${NAMESPACE}.svc:8000`. The `.svc` suffix\r\n  matches `NO_PROXY` in `multimedia-downloader/patch-coordinator-ca.yaml`, so render\r\n  calls do not go through the media proxy.\r\n  - Prefill only, not all vLLM pods: render on decode slows token streaming.\r\n    Encode sets no `--max-model-len`, so render there accepts prompts that prefill\r\n    and decode reject. Prefill returns one token per request, so its API server has\r\n    spare CPU.\r\n  - The `vllm-render` sidecar (`vllm-openai-cpu:v0.26.0`) and its volumes are\r\n    removed. Render now runs on the same vLLM version as the model servers, and no\r\n    extra copy of the model is downloaded.\r\n- **Coordinator image:** `ghcr.io/llm-d/llm-d-router-coordinator:main` now uses\r\n  `imagePullPolicy: Always`, because `:main` moves with every merge to llm-d-router\r\n  main.\r\n- **README:** step 2 applies `render/`, Cleanup deletes it, and the image note\r\n  describes the nightly image and the EC settings.\r\n\r\n### Encode Disaggregation guide\r\n\r\n- **CoreWeave overlays (new):** `e-pd/coreweave` and `e-p-d/coreweave` add\r\n  `rdma/ib: \"1\"` to each role. `rdma/ib` is removed from the E/P/D base decode and\r\n  prefill patches, so the base and GKE overlays no longer request it. The README\r\n  lists `coreweave` as a provider.\r\n- **README:** describes the nightly image, the V2 model runner and the P2P NIXL\r\n  settings. The GKE bullet no longer claims DRA and DRANet (RoCE), because the GKE\r\n  overlays do not configure RDMA yet.\r\n\r\n## Defaults\r\n\r\n- `--enable-scale-out`: off in `vllm serve` when not set. Set only in the\r\n  Coordinator guide. The multimodal guides do not use the scale-out endpoints and\r\n  keep the default. vLLM builds from before 2026-09-14 do not know the flag.\r\n- `VLLM_USE_V2_MODEL_RUNNER`: not set on decode in the Coordinator guide and in\r\n  E/P/D. vLLM then uses V2 by default (vllm-project/vllm#53183) and falls back to V1\r\n  when a feature needs it.\r\n- `ec_cpu_bytes`: required, with no default. vLLM does not check it against the\r\n  `/dev/shm` size, so each value is below its pod's `/dev/shm` limit.\r\n- `imagePullPolicy`: Kubernetes uses `IfNotPresent` when it is not set and the tag is\r\n  not `:latest`. It is now `Always` on the vLLM container in every guide that uses\r\n  `gpu-vllm/nightly` (E/PD decode and wide-EP GLM-5.2 already set it) and on the\r\n  Coordinator.\r\n- Render step `address`: no default in the Coordinator. It is now the Service URL.\r\n\r\n## Testing\r\n\r\n- [x] `kustomize build` passes for the Coordinator model servers (`base`,\r\n  `coreweave`, `gke`), `coordinator/` and `render/`, and for E/PD and E/P/D\r\n  (`base`, `coreweave`, `gke`). All 38 kustomizations that use `gpu-vllm/nightly`\r\n  build, and the only change there is `imagePullPolicy: Always`.\r\n- [x] Checked the built manifests. Every Deployment uses\r\n  `docker.io/vllm/vllm-openai:nightly`. No env var name is duplicated. The V2 runner\r\n  is set only on EC instances. The render Service selects only prefill pods. The\r\n  render address host matches `NO_PROXY`.\r\n- [x] Deployed E/P/D on CoreWeave and sent an image request. The encode pod logged\r\n  `EC save`, `serving NIXL READ request` and `NIXL READ completed`, and prefill\r\n  answered. A text-only request skipped encode.\r\n- [x] Deployed E/PD on CoreWeave and sent an image request. The encode pod logged\r\n  `NIXL READ completed`, and decode answered.\r\n- [x] Deployed the Coordinator guide on CoreWeave (3 EPPs) and sent a text and an\r\n  image request through the Gateway. Render went through `coord-disaggregation-render`\r\n  to a prefill pod (`/v1/chat/completions/render` 200, 83 tokens, 1 image), and the\r\n  encode pod logged `NIXL READ completed`. Both requests returned 200.\r\n\r\n## Notes\r\n\r\n- The Service balances connections, not requests. For more render capacity, add\r\n  prefill replicas or CPU. llm-d/llm-d-router#2701 balances render per request, but\r\n  only in the EPP. If the Coordinator's `render` step gets a similar option, it can\r\n  replace this Service.\r\n- The `ec_cpu_bytes` sizes depend on the model. For another model, calculate\r\n  `out_hidden_size * (1 + deepstack layers) * dtype bytes` per token.\r\n- GKE RDMA: the E/PD, E/P/D and Coordinator GKE overlays do not configure RDMA yet,\r\n  so the NIXL KV-cache and encoder-cache transfers use TCP on GKE. We will open a\r\n  follow-up issue to add the `gke-rdma` DRA component (one GPU+NIC claim per TP rank),\r\n  as in the P/D Disaggregation guide, once it can be tested on a GKE A3/A4 cluster\r\n  with DRA and DRANet.\r\n", "url": "https://github.com/llm-d/llm-d/pull/2443"}
