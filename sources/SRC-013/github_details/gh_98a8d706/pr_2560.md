# [PR #2560] Update multimodal guides to use leaf name for ci/cd testing

source: https://github.com/llm-d/llm-d/pull/2560
state: closed | updated: 2026-09-22T22:00:01Z
labels: 

## 正文

## What does this PR do?

Re-submission of #2556 (authored by @rlakhtakia — commit authorship and sign-off preserved via cherry-pick) with the merge conflict against `main` resolved. That PR's head branch is the fork's `main`, which we don't have push access to update.

Original description from #2556:

> CI/CD was failing because of multimodal nested directory. llm-d-benchmark and llm-d-infra have been updated to now use leaf name of guide. This change updates multimodal guide to use the leaf name for testing.

## What changed relative to #2556

Only the conflict resolution: `guides/multimodal-serving/e-disaggregation/modelserver/gpu/vllm/e-pd/base/patch-encode.yaml` — both #2556 and #2559 (merged) deleted the `image: REPLACE_MODEL_SERVER_IMAGE` line; kept `main`'s version of the hunk (deletion plus the explanatory comment). Everything else is the original commit as-is, including its whitespace/EOF lint fixes.

## How was this tested?

- [ ] Unit tests added/updated
- [ ] Integration/e2e tests added/updated
- [x] Manual testing performed

After the cherry-pick, `kubectl kustomize` renders of `multimodal-serving/e-disaggregation/modelserver/gpu/vllm/{e-pd,e-p-d}/{base,gke}` contain no leftover `REPLACE_MODEL_SERVER_IMAGE`, and the aggregation overlays render with the new `llm-d.ai/guide: aggregation` labels.

## Checklist

- [x] Commits are signed off (`git commit -s`) per DCO
- [x] Code follows project contributing guidelines
- [ ] Tests pass locally (`make test`)
- [ ] Documentation updated (if applicable)

## Related Issues

Supersedes #2556. Related: llm-d/llm-d#2511, llm-d/llm-d-infra#261, llm-d/llm-d-benchmark#1935, #2559.

## 评论 (0)
