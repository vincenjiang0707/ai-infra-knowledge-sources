# [PR #2700] [Docs] Add Simplified Chinese catalogs and a navbar language switcher

source: https://github.com/vllm-project/aibrix/pull/2700
state: open | updated: 2026-09-11T23:32:42Z
labels: kind/documentation, area/website, area/kv-cache, area/batch

## 正文

## Pull Request Description
Add Sphinx gettext catalogs for Simplified Chinese and a navbar language switcher so the docs site can be read in Chinese while keeping the English RST as the source of truth.

- Enable Sphinx i18n in `docs/source/conf.py` and ignore compiled `.mo` files.
- Translate existing docs into `docs/source/locale/zh_CN`.
- Add an English / 中文 switcher in the header (local `/html/` vs `/zh-cn/`, Read the Docs `/en/` vs `/zh-cn/`).
- Document local workflow: `make update-po` after RST changes, `make html-zh` to preview Chinese HTML.

Online Chinese pages at `https://aibrix.readthedocs.io/zh-cn/latest/` still need a Read the Docs translation project (same repo, language Chinese Simplified) linked from the main project's Translations settings.

## Related Issues
Resolves: #2698

## Test plan
- [x] `cd docs && make html` builds English HTML
- [x] `cd docs && make html-zh` builds Chinese HTML
- [x] Language switcher jumps between English and Chinese on local preview
- [ ] Maintainer: import a Read the Docs Chinese project and attach it as a translation

## 评论 (5)

### FAUST-BENCHOU · 2026-09-10

<img width="2560" height="1660" alt="image" src="https://github.com/user-attachments/assets/8e044365-3adc-4db4-aed8-b575089a8eed" />


### cursor[bot] · 2026-09-10

## Review summary (maintainer publish pass)

Reviewed #2700 against #2698 and the automated review comments, then pushed a follow-up commit onto this branch (`e9c47cce`).

### Looks good
- Sphinx i18n wiring (`locale_dirs`, `gettext_compact`, `exclude_patterns`, `html-zh` / `update-po`, `jieba` + `sphinx-intl`) matches the intended workflow.
- zh_CN catalogs are complete at publish time (~4264 entries, 0 empty / 0 fuzzy in a spot-check).
- Docs Build / DCO were already green on the original commit.

### Addressed before publish
1. **Language switcher path rewrite** – prefer RTD language prefixes and `/build/<lang>/` so paths like `/home/html/...` are not rewritten incorrectly.
2. **`file://` previews** – avoid `origin === "null"` broken redirects.
3. **`conf.py` language normalization** – normalize zh and default empty → `en`, but do not force every non-zh language to English.
4. **RTD 中文 option gating** – hide Chinese on hosted English builds until `AIBRIX_DOCS_SHOW_ZH=1` after the RTD translation project is linked (avoids production 404s to `/zh-cn/`).

### Remaining maintainer follow-up (not blocking merge of catalogs/tooling)
- Create/link the Read the Docs **Chinese Simplified** translation project for this repo.
- Set `AIBRIX_DOCS_SHOW_ZH=1` on the main RTD project once `/zh-cn/` is live.

### cursor[bot] · 2026-09-10

## Publish update

I reviewed this PR and pushed hardening commit `e9c47cce` onto this fork branch (switcher path/`file://` fixes, language normalization, RTD Chinese option gating).

To finish publish cleanly I also rebased onto current `main`, but GitHub refused a force-push back to this fork because the rebase would update workflow files without App `workflows` permission. Separately, the new AIBrix Bot `triage` job is failing here because `pull_request.base.sha` is still pinned to `c2e1e28d` (before `#2696`), so checkout does not include `.github/scripts/aibrix-bot.py`.

**Published maintainer PR (rebased + review fixes):** will link in a follow-up once opened.

Please prefer that PR for merge; this one can be closed afterward with credit retained for @FAUST-BENCHOU.

### cursor[bot] · 2026-09-10

Published maintainer PR with the rebased + review-hardened changes: https://github.com/vllm-project/aibrix/pull/2702

### cursor[bot] · 2026-09-11

## Review

Looks good to merge the catalogs and switcher tooling. This matches #2698: Sphinx gettext for Simplified Chinese, English RST as source of truth, and a navbar language switcher. Prior review nits are addressed in `e9c47cce`, `main` is merged (`ccfc7658`), and CI is green (Docs Build, DCO, triage, RTD preview).

**Looks good**
- i18n wiring (`locale_dirs`, `gettext_compact = False`, `exclude_patterns`, `jieba` + `sphinx-intl`, `make update-po` / `make html-zh`, ignore `*.mo`) matches the intended workflow.
- Catalogs are complete in this snapshot: 48 `.po` files, 4216 entries, 0 empty / 0 fuzzy. Sampled pages preserve Sphinx roles/links and read like real technical Chinese.
- Switcher hardening: prefer RTD prefixes and `/build/<lang>/`; preserve `file://`; do not coerce every non-zh Sphinx language to English; gate 中文 on hosted English RTD until `AIBRIX_DOCS_SHOW_ZH=1`.

**Follow-ups (not merge blockers)** — also left as inline comments:
1. Docs CI still only builds English HTML, so a broken `msgstr` would not fail the PR.
2. The generic `/html|en|zh-cn/` rewrite fallback is still present after the RTD/`/build/` checks.
3. On production English, the switcher is a single-option dropdown until the RTD translation project is linked.
4. Catalog drift vs RST is still a manual `make update-po` process.

Hosted Chinese still needs a maintainer step: create/link the RTD **Chinese Simplified** translation project, then set `AIBRIX_DOCS_SHOW_ZH=1` on the main project. Until then, gating the navbar is the right call.

## Review (7)

### gemini-code-assist[bot] · 2026-09-10 · COMMENTED

## Code Review

This pull request introduces internationalization support for the documentation, specifically adding Chinese translations (zh_CN) and a language switcher component. It updates the build configuration, adds translation files, and introduces CSS/JS for the language switcher. The review feedback highlights three main areas for improvement: making the URL rewriting logic in the language switcher more robust against accidental path replacements, handling the `file://` protocol gracefully when previewing documentation locally, and ensuring the Sphinx configuration hook does not inadvertently override non-English/non-Chinese languages.

### chatgpt-codex-connector[bot] · 2026-09-10 · COMMENTED


### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `8251676e3e`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team has set up Codex to review pull requests in this repo](https://chatgpt.com/codex/cloud/settings/general). Reviews are triggered when you
- Open a pull request for review
- Mark a draft as ready
- Comment "@codex review".

If Codex has suggestions, it will comment; otherwise it will react with 👍.




Codex can also answer questions or update the PR. Try commenting "@codex address that feedback".
            
</details>

### copilot-pull-request-reviewer[bot] · 2026-09-10 · COMMENTED

Copilot wasn't able to review this pull request because it exceeds the maximum number of lines (20,000). Try reducing the number of changed lines and requesting a review from Copilot again.

### cursor[bot] · 2026-09-11 · COMMENTED

(no text)

### cursor[bot] · 2026-09-11 · COMMENTED

(no text)

### cursor[bot] · 2026-09-11 · COMMENTED

(no text)

### cursor[bot] · 2026-09-11 · COMMENTED

(no text)
