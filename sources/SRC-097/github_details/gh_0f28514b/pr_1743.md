# [PR #1743] TEST/GTEST: Run in single process.

source: https://github.com/ai-dynamo/nixl/pull/1743
state: open | updated: 2026-09-17T12:32:00Z
labels: external-contribution, size/L, WIP-DNM

## 正文

## What?
Run all gtests in a single process (instead of separately) and in random order.

## Why?
To prevent some issues from being hidden by the process isolation or deterministic order.

## 评论 (58)

### github-actions[bot] · 2026-06-08

👋 Hi ColinNV! Thank you for contributing to ai-dynamo/nixl.

Your PR reviewers will review your contribution then trigger the CI to test your changes.

🚀

### ColinNV · 2026-06-08

/build

### ColinNV · 2026-06-09

/build

### ColinNV · 2026-06-09

/build

### ColinNV · 2026-06-11

/build

### ColinNV · 2026-06-15

/build

### ColinNV · 2026-06-16

/build

### ColinNV · 2026-06-17

/build

### ColinNV · 2026-06-30

/build

### ColinNV · 2026-06-30

/build

### ColinNV · 2026-07-02

/build

### svc-nixl · 2026-07-02

👀 Investigating [`nixl-ci-dl-gpu` build 1265](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-dl-gpu/1265/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

🤖 **CI Triage Agent** — `nixl-ci-dl-gpu` · commit `37f403eb`

The logs clearly show the failure. Both parallel build branches failed during the pip dependency installation phase with network read timeouts. Let me confirm the nature of this — it's a `pip install` step failing while downloading large wheel files.

The evidence is clear from the log:
- `pip._vendor.urllib3.exceptions.ReadTimeoutError: HTTPSConnectionPool(host='files.pythonhosted.org', port=443): Read timed out.`
- This happened on two nodes (`gb-nvl-057-compute08` and `compute09`) in branches `build_helper_dl/aarch64/ucx-v1.21.x/2` and `build_helper_dl/aarch64/ucx-master/1`
- The timeout occurred while downloading large wheels at very low speeds: `torch-2.11.0...whl (419.7 MB) ... 685.3 kB/s` and `nvidia_cudnn_cu13...whl (433.8 MB) ... 5.3/433.8 MB 253.5 kB/s eta 0:28:10`

This is an infrastructure/network issue, not a code defect. The download speeds are extremely low (253 kB/s for a 433 MB file), causing pip's per-chunk read timeout to trip while downloading from PyPI. No test ran; the failure is entirely in the build-dependency install phase and is not tied to the PR's code changes.

**Summary:** Both aarch64 `build_helper_dl` parallel branches failed during `pip install` of build dependencies, not in any test or the PR's code.

**Root cause:** Network read timeout while downloading large Python wheels from PyPI. pip raised `ReadTimeoutError: HTTPSConnectionPool(host='files.pythonhosted.org', port=443): Read timed out.` The download rates were pathologically slow (e.g., `torch-2.11.0` 419.7 MB at ~685 kB/s, `nvidia_cudnn_cu13` 433.8 MB at ~253 kB/s / ETA 28 min), so pip's per-read socket timeout fired mid-download. This is a transient CI infrastructure/network problem (or PyPI/CDN throttling), not a defect in the PR or nixl source. `srun` on both compute nodes then exited with code 1, aborting the parallel stage.

**Implicated commit:** unknown — the failing operation is external dependency download, unrelated to commit [REDACTED:Hex High Entropy String].

**File:** N/A — failure is in the CI dependency-install step, not a source file. (Wheels fetched: `torch==2.11.*`, `nvidia-cudnn-cu13`, etc.)

**Suggested fix:** Retry the build first — this is a transient network timeout. To harden the pipeline against recurrence: add pip network resiliency in the build_helper_dl install step, e.g. `pip install --timeout 120 --retries 5` (and consider `PIP_DEFAULT_TIMEOUT`); prefer an internal/cached PyPI mirror or Artifactory proxy for these large CUDA/torch wheels rather than pulling from `files.pythonhosted.org`; and/or pre-bake these heavy wheels into the `build_helper_dl` container image (`artifactory.nvidia.com/.../aarch64/build_helper_dl:20260625-1`) so they aren't downloaded per-build. Do not treat this as a PR-code failure.

**Related:** none found in logs.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `19a0a5a1-d4ae-4522-a32c-500b7ab54047` in the triage console for the audit trail.

<!-- triage-agent request_id: 19a0a5a1-d4ae-4522-a32c-500b7ab54047 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

👀 Investigating [`nixl-ci-test-sanitizers` build 226](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-test-sanitizers/226/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

🤖 **CI Triage Agent** — `nixl-ci-test-sanitizers` · commit `37f403eb`

I have enough evidence for a confident diagnosis.

## Diagnosis

**Summary:** The TSan branch of `nixl-ci-test-sanitizers` #226 reported `Sanitizer test FAILURES: meson sanitizer suite` and exited 1, even though the visible stages printed `PASSED` — the `ucx_backend_multi` multithread UCX test tripped ThreadSanitizer under the TSan build.

**Root cause:** Not a hang or timeout — the log timestamps are continuous (07:03:34 → 07:03:36, no gaps) and the build ran to completion, so this is a real detected failure, not a wall-clock kill. The `ucx_backend_multi` test spins up UCX progress threads plus Agent0/Agent1 concurrently; under ThreadSanitizer it produces data-race reports and a nonzero exit that the suite records into its failure summary (`Sanitizer test FAILURES: meson sanitizer suite` → `exit 1`). The per-stage `==== PASSED ====` echo is misleading — the failure is accumulated and reported at the end of the suite. The definitive corroboration is the fix commit landed the same day (see below), which explicitly disables this exact test under TSan.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` (NirWolfer, #1709 "ci: add AddressSanitizer/UBSanitizer/ThreadSanitizer builds") introduced the TSan suite that runs `ucx_backend_multi`; the racy test itself is `test/unit/plugins/ucx/ucx_backend_multi.cpp`, last touched by `[REDACTED:Hex High Entropy String]` (Colin Hirsch, #1568 "TEST/UCX: Make thread safe.") — which was insufficient to satisfy TSan.

**File:** `test/unit/plugins/ucx/ucx_backend_multi.cpp` (the multithreaded UCX test invoked as the `ucx_backend_multi` stage).

**Suggested fix:** Skip `ucx_backend_multi` under the TSan configuration — which is exactly what was done in commit `e0053e8abe52` / PR **#1874** ("ci: skip ucx_backend_multi under TSan and lower sanitizer stage timeout to 40m"). Rebase this PR (#1743) onto a branch that includes #1874, or cherry-pick it. Longer term, either annotate the genuine UCX progress-thread races with TSan suppressions (a `tsan.supp` entry / `__attribute__((no_sanitize("thread")))`) or fix the underlying synchronization in the test so it can run cleanly under ThreadSanitizer.

**Related:** PR #1709 (added the sanitizer builds), PR #1874 (the skip-under-TSan fix), PR #1568 (prior thread-safety attempt on this test).

Note: I did not find the exact filename of the sanitizer runner script in the repo via the available paths, but the diagnosis rests on the fetched Jenkins log plus git history, both of which point unambiguously to `ucx_backend_multi` under TSan.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `02d08f1b-45b0-4dde-9fa7-924002b008e2` in the triage console for the audit trail.

<!-- triage-agent request_id: 02d08f1b-45b0-4dde-9fa7-924002b008e2 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

👀 Investigating [`nixl-ci-dl-gpu` build 1269](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-dl-gpu/1269/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

🤖 **CI Triage Agent** — `nixl-ci-dl-gpu` · commit `37f403eb`

Based on all the evidence gathered, here is my diagnosis.

**Summary:** The `nixl-ci-dl-gpu` #1269 benchmark stage aborted with exit code 1 — the nixlbench workers ran with CUDA disabled because CUDA failed to initialize on the GPU node (`cuDeviceGetCount ... initialization error`), and the wrapper script propagated a worker failure (`+ false` → `set -eE` abort) after the WRITE/`--check_consistency` run.

**Root cause:** On node `gb-nvl-057-compute04` the CUDA driver could not be initialized inside the nixlbench worker processes:
```
cuda_ctx.c:52  UCX  ERROR cuDeviceGetCount(&num_devices) failed: initialization error
W ucx_utils.cpp:627] 4 NVIDIA GPU(s) were detected, but UCX CUDA support was not found! GPU memory is not supported.
W ucx_utils.cpp:634] 4 IB device(s) were detected, but accelerated IB support was not found!
```
`cuDeviceGetCount` returning `initialization error` (CUDA error 3) is an environment/driver-context problem, not a code defect in this PR — the 4 GPUs are visible to the OS but `cuInit` in the worker context fails. UCX therefore fell back to a no-CUDA, non-accelerated-IB path. The benchmarks still produced bandwidth numbers over DRAM, but one worker in the final `parallel --halt now,fail=1` WRITE run exited nonzero, so the wrapper's guard executed `false`, and because the stage runs under `bash -eE` that aborted the pipeline (`script returned exit code 1`). The timeline is continuous (07:53:14→07:54:04, no multi-minute gaps), so this is **not** a hang or wall-clock kill.

**Implicated commit:** unknown — no code commit in the failing path is implicated; the trigger is a CUDA-driver/context initialization failure on the CI GPU node (environmental). PR #1743 ("TEST/GTEST: Run in single process") modifies `test/gtest/main.cpp`, which is a different stage and did not run here.

**File:** worker log origin `cuda_ctx.c:52` (UCX) / `ucx_utils.cpp:627,634` — CUDA driver init on node `gb-nvl-057-compute04`.

**Suggested fix:** This is an infrastructure/environment failure, not a source bug — investigate the GPU node/container: confirm the NVIDIA driver ↔ CUDA toolkit versions match, that `/dev/nvidia*` and the driver are mounted/visible inside the privileged `buildhelperdl` container, and that no prior process left the GPUs in a bad state (e.g. run `nvidia-smi` and a minimal `cuInit` check in a pre-flight step). If GPU-backed transport is required for this stage, the wrapper should **fail fast with an explicit error** when `cuDeviceGetCount` fails instead of silently degrading to DRAM and then aborting via a bare `false`; add a check that emits a clear "CUDA init failed on <host>" message. Re-run on a healthy node to confirm it is transient. Do **not** raise any time limit.

**Related:** #1740 (nixlbench WRITE consistency verification), #1743 (the PR under test — unrelated gtest stage).

Note: the Jenkins pod spec in the log contains masked Jenkins agent secret/`JENKINS_SECRET` values (shown as `********`) — these are already redacted in the log; ensure they remain masked and are not echoed elsewhere.

<!-- triage-agent request_id: 89fbf792-b713-40a3-89a6-02444d856074 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

👀 Investigating [`nixl-ci-gpu` build 2464](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-gpu/2464/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

🤖 **CI Triage Agent** — `nixl-ci-gpu` · commit `37f403eb`

I have enough evidence for a confident diagnosis. Let me summarize the key findings from the log:

**Timeline / evidence:**
1. The `nixl-ci-gpu` #2464 run had two parallel branches. The `ucx-master/1` branch **failed hard** at 07:34:32 with `srun: error: mizu04: task 0: Exited with exit code 139` (SIGSEGV).
2. The crash occurred in the **UCCL backend, READ, DRAM→DRAM** configuration — a two-worker `nixlbench` run via `parallel`. The immediately preceding DRAM→**VRAM** run completed successfully (produced a bandwidth line). The DRAM→DRAM run printed one result line (`16384 4 1.679001 ...`) then one worker segfaulted during teardown ("Destroying Engine..." → crash before both workers finished).
3. The second branch (`ucx-v1.21.x/2`) shows exit code **143 at 08:54** — but this is ~80 minutes later and is Jenkins *cancelling* the sibling branch after the first branch already aborted the build ("Cancelling nested steps due to timeout" / "parallel task failed"). This is a symptom of the hard failure in branch 1, not an independent root cause.

The DRAM→DRAM path is the **local/same-process IPC transfer** path added in PR #1428 ("Enable local xfer for UCCL backend"). This is exactly the codepath (`prepareLocalConn` / `same_process=true` / IPC info) that differs from the DRAM→VRAM run that passed. The crash is a segfault in this backend, and PR #1743 (a GTest single-process test refactor) does not touch the UCCL backend at all.

**Note on the 80-min gap:** the gap between 07:34 (branch 1 crash) and 08:54 (branch 2 kill) is not a hang in an application step — branch 1 produced a definitive SIGSEGV, and the 143 is Jenkins tearing down the parallel sibling. The root cause is the segfault, not a timeout.

**Summary:** `nixlbench` UCCL backend, READ / DRAM→DRAM (local IPC) two-worker run segfaulted (exit 139); the sibling branch's exit 143 is just Jenkins cancelling after the hard failure.

**Root cause:** A native crash (SIGSEGV) in the NIXL UCCL plugin's local/same-process transfer path. The DRAM→VRAM run immediately before it passed; the DRAM→DRAM (local IPC) configuration crashed during/after the single benchmark iteration, around engine teardown. This is the local-xfer codepath (`prepareLocalConn`, `same_process=true`, IPC info in `getPublicData`) introduced by PR #1428. It is not caused by the PR under test (#1743, a GTest single-process refactor that does not touch the UCCL backend), so this is a pre-existing/flaky crash in the UCCL local-transfer path exposed by CI.

**Implicated commit:** [REDACTED:Hex High Entropy String] — "Enable local xfer for UCCL backend (#1428)", Pravein Govindan Kannan (the commit that introduced the DRAM→DRAM/same-process IPC path that crashed). Not the PR under test (#1743).

**File:** src/plugins/uccl/uccl_backend.cpp — local-transfer path: `prepareLocalConn` (lines ~132-178) and the IPC handling in `getPublicData` (lines ~40-65); the crash manifests during the DRAM→DRAM transfer/teardown.

**Suggested fix:**
- Reproduce locally with the exact failing command: `nixlbench --backend UCCL --op_type READ --initiator_seg_type DRAM --target_seg_type DRAM --check_consistency` (two workers via ETCD) under `gdb`/AddressSanitizer to capture the faulting frame. The crash-on-teardown pattern suggests a use-after-free or double-free in the local IPC connection cleanup: `~nixlUcclEngine` destroys connections by iterating `connected_agents_`, but the local (same-process) connection created in `prepareLocalConn` may share/alias a `uccl_conn_t*` or MR that is already freed, or `pending_local_agent_` state races with teardown.
- Specifically audit whether a local same-process `conn` can end up double-destroyed (it's inserted into `connected_agents_` and also may be the same underlying object as another agent's connection) — the destructor's `destroyed_agents` set guards by *agent name*, not by `conn` pointer, so two agent names mapping to the same `conn` pointer would still call `uccl_engine_conn_destroy` twice. Deduplicate by `conn` pointer, not agent name.
- Since PR #1743 is unrelated, this failure should not gate that PR; open a separate issue against the UCCL backend and re-run CI.

**Related:** PR #1428 (https://github.com/ai-dynamo/nixl/pull/1428) introduced the local-xfer path; PR #1151 "Fix UCCL's consistency checks". No existing issue found matching this segfault — recommend filing one. PR under test: #1743 (https://github.com/ai-dynamo/nixl/pull/1743), unrelated to the failure.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `dedf8402-9f6c-4372-94d4-3b0b4abe2f68` in the triage console for the audit trail.

<!-- triage-agent request_id: dedf8402-9f6c-4372-94d4-3b0b4abe2f68 -->
<!-- triage-agent-bot -->

### ColinNV · 2026-07-02

/build

### svc-nixl · 2026-07-02

👀 Investigating [`nixl-ci-test-sanitizers` build 237](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-test-sanitizers/237/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

🤖 **CI Triage Agent** — `nixl-ci-test-sanitizers` · commit `65efdb79`

I have enough evidence for a confident diagnosis.

## Diagnosis

**Summary:** The `nixl:sanitizer / gtest` suite fails (exit status 42) in both the asan_ubsan and tsan variants of `nixl-ci-test-sanitizers` #237.

**Root cause:** Exit code 42 is **not** a sanitizer abort — it is NIXL's own "fail on unexpected warning/error log" gate in `test/gtest/main.cpp:104-108`, which returns 42 when `LogProblemCounter::getProblemCount() > 0`. The run reported `Problem count is 132`. The tests themselves all pass; the gate trips on unexpected `NIXL_WARN`/`NIXL_ERROR` lines emitted during the run. The dominant new, unignored message is:

`W ... tracer.cpp:139] nixl::trace: backend 'nvtx' requested but plugin libtrace_backend_nvtx.so was not found`

This comes from `makeTracer()` in `src/core/tracing/tracer.cpp:40-42`. PR #1867 ("tracing: auto-enable NVTX backend when running under Nsight Systems", commit `40ff1ef`) now auto-requests the `nvtx` trace backend when it detects an Nsight/nsys environment. In the sanitizer test process the NVTX plugin (`libtrace_backend_nvtx.so`) isn't on the tracer plugin search path, so every agent construction emits this WARN. Because the gtest harness treats any un-ignored warning as a failure (added in #1288, commit `c7e97a4`), the accumulated warnings push the problem count to 132 and force exit 42. (The `nixl_plugin_manager.cpp:639` "Error accessing directory .../src/core/plugins" and `getXferTelemetry ... telemetry is not enabled` messages also contribute, but the nvtx warning is the newly introduced regressor tied to this PR.)

Note: this is a log-gate failure, not a hang and not a wall-clock kill — the sanitizer build/tests ran to completion in well under the 40-min step timeout.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — e-eygin, "tracing: auto-enable NVTX backend when running under Nsight Systems (#1867)" (interacting with the log-fail gate from `c7e97a41` / #1288). This is the base the PR #1743 branch merged onto (`65efdb7` = merge of `6f700c4` into `40ff1ef`).

**File:** `src/core/tracing/tracer.cpp:40-42` (WARN source) surfaced via `test/gtest/main.cpp:104-108` (exit 42 gate).

**Suggested fix:** Prevent the auto-enabled NVTX backend from warning when its plugin isn't present, or ignore that warning in the harness. Concretely, one of:
1. In `makeTracer()`, downgrade the "plugin not found" message from `NIXL_WARN` to `NIXL_DEBUG` for backends that were *auto-enabled* (not explicitly requested by the user), since a missing plugin in that path is expected and non-fatal.
2. Ensure the tracer plugin search path in the gtest/sanitizer environment includes the built `libtrace_backend_nvtx.so` (it is built and installed to `/opt/nixl/lib/x86_64-linux-gnu/plugins`), or skip the nsys auto-enable under `NIXL_CI_NON_GPU`.
3. As a stop-gap, add a `LogIgnoreGuard` regex for `nixl::trace: backend 'nvtx' requested but plugin ... was not found` in `RunAllTests()` in `test/gtest/main.cpp` (alongside the existing IB/AWS/non-GPU ignores).
Option 1 is preferred as it fixes the noisy warning at the source.

**Related:** PR #1867 (auto-enable NVTX), PR #1845 (NVTX trace backend plugin), PR #1288 (fail-on-unexpected-log gate). Triage was triggered on PR #1743.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `9109f36a-1c7a-4d70-ba17-3251858a2924` in the triage console for the audit trail.

<!-- triage-agent request_id: 9109f36a-1c7a-4d70-ba17-3251858a2924 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

👀 Investigating [`nixl-ci-gpu` build 2476](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-gpu/2476/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-02

🤖 **CI Triage Agent** — `nixl-ci-gpu` · commit `65efdb79`

Line 107 is the key line changed by PR #1743:
```
gtest-parallel --workers=1 --serialize_test_cases ./bin/gtest -- --min-tcp-port=... --max-tcp-port=...
```

The PR title "TEST/GTEST: Run in single process" corresponds to `--workers=1 --serialize_test_cases`, forcing all gtest cases through one worker/one process serially. This means if **any single gtest hangs** (e.g. an agent/listener test blocking on a socket accept or a transfer that never completes), the whole `gtest` invocation blocks forever — with no output — until the outer Jenkins timeout kills it (exit 143).

That exactly matches the observed signature on the `ucx-v1.21.x` branch: the C++/GTest stage produced its last output and then went silent for ~82 minutes before SIGTERM. The `ucx-v1.21.x` variant (older UCX 1.21.x rather than the 1.19.0 in the master container) is the environment where a listener/transfer test deadlocks; with parallel workers a hang in one test previously wouldn't necessarily stall everything, but under `--workers=1 --serialize_test_cases` a single hung test blocks the entire serialized run.

I have enough evidence from the fetched log plus the source and PR to give a confident diagnosis.

**Summary:** The `build_helper/x86_64/ucx-v1.21.x/2` branch of nixl-ci-gpu #2476 was killed by the outer Jenkins timeout (exit code 143) after ~82 minutes of total silence — a hang, not a slow run.

**Root cause:** A hung C++/GTest run, not a genuine timeout. In the interleaved log, application output stops at ~11:36:04 and nothing is emitted until "Cancelling nested steps due to timeout" at 12:57:59 (≈1h22m gap) followed by `script returned exit code 143` and `Failed in branch build_helper/x86_64/ucx-v1.21.x/2`. A gap of that magnitude means the process was blocked, not making progress. PR #1743 ("TEST/GTEST: Run in single process") changes `.gitlab/test_cpp.sh:107` to run gtest with `--workers=1 --serialize_test_cases`, i.e. every gtest case in one serialized process. Under that mode a single test that blocks (e.g. an agent/listener test waiting on a socket accept or a transfer that never completes — consistent with the `nixl_listener.cpp` connect failures seen in the Rust stage and the fragile UCX/UCCL teardown behavior) stalls the entire gtest binary indefinitely with no further stdout. On the older ucx-v1.21.x variant this test deadlocks and hangs the whole serialized run.

**Implicated commit:** PR #1743 (head commit [REDACTED:Hex High Entropy String]) — "TEST/GTEST: Run in single process." The runtime change is at `.gitlab/test_cpp.sh:107` (author of that gtest-parallel line per PR #1743).

**File:** `.gitlab/test_cpp.sh:107`

**Suggested fix:**
1. Do not rely on the outer Jenkins timeout to catch a hung test. Add a per-invocation hard timeout and per-test timeout so a single stuck case fails fast with a stack dump instead of hanging the whole run, e.g.:
   `timeout 600 gtest-parallel --workers=1 --serialize_test_cases --timeout=120 ./bin/gtest -- --min-tcp-port=... --max-tcp-port=...`
2. Reproduce locally against the ucx-v1.21.x container and identify which gtest hangs (run `./bin/gtest --gtest_list_tests` then bisect with `--gtest_filter`); the likely culprit is a listener/connection or transfer-completion test that blocks when serialized in-process. Fix the deadlock (or mark it to run isolated) rather than reverting the single-process mode.
3. If the single-process serialization itself introduced shared-state interference (agents/listeners not fully torn down between cases), ensure each test fully releases its listener socket/agent before the next starts.

**Related:** PR #1743 (https://github.com/ai-dynamo/nixl/pull/1743). UCCL-related history: PRs #1428, #1271, #1151, #895. Note: the visible master-branch Python/Rust/Nixlbench stages all passed; only the ucx-v1.21.x branch hung.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `3a59ae08-d17d-45cf-9276-f11f52e770a8` in the triage console for the audit trail.

<!-- triage-agent request_id: 3a59ae08-d17d-45cf-9276-f11f52e770a8 -->
<!-- triage-agent-bot -->

### ColinNV · 2026-07-03

/build

### svc-nixl · 2026-07-03

👀 Investigating [`nixl-ci-test-sanitizers` build 262](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-test-sanitizers/262/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-03

🤖 **CI Triage Agent** — `nixl-ci-test-sanitizers` · commit `a688f8a1`

I have enough evidence. The exact file path differs but the message `tracer.cpp:139 nixl::trace: backend 'nvtx' requested but plugin libtrace_backend_nvtx.so was not found` is repeatedly emitted at warning level (`W...`). This is the dominant contributor to the 138 problem count.

The root cause is clear from the two facts:
1. PR #1743 makes gtest run in a **single process** — so `LogProblemCounter` (a process-global counter, main.cpp:82/104) accumulates warnings across the entire suite instead of being isolated per forked test.
2. Warning-level log lines (`nvtx plugin not found`, `plugins dir missing`, `getXferTelemetry telemetry not enabled`) are emitted repeatedly and now all count toward the single global total → 138 → exit code 42.

Note the whole rest of the suite (ucx_backend_test, posix, examples) all PASSED. The only failing item is the gtest binary via exit code 42 (`nixl:sanitizer / gtest FAIL ... exit status 42`), which is purely the log-problem-counter tripping, not a real test assertion or a sanitizer (ASan/UBSan/TSan) fault.

**Summary:** The `nixl:sanitizer / gtest` suite failed with exit code 42 — the gtest binary's `LogProblemCounter` tallied 138 unexpected NIXL warnings/errors and returned 42, even though every individual test and all downstream stages (ucx_backend_test, nixl_posix_test, examples, etc.) passed.

**Root cause:** PR #1743 ("TEST/GTEST: Run in single process") changes gtest to execute the entire suite in one process. `LogProblemCounter` is process-global (declared once in `RunAllTests`, checked once in `RunTests`), so it now accumulates every warning-level log line across *all* test cases into one total. Benign, repeated warnings — chiefly `tracer.cpp:139 "nixl::trace: backend 'nvtx' requested but plugin libtrace_backend_nvtx.so was not found"`, plus `nixl_plugin_manager.cpp:674 "Error accessing directory(.../src/core/plugins): No such file or directory"` and `nixl_agent.cpp:1291 "getXferTelemetry: cannot return values when telemetry is not enabled"` — sum to 138 and trip the `problems > 0 → return 42` check. Previously (one process per test) these warnings didn't aggregate past the guard. This is a test-harness accounting problem, not a real ASan/UBSan/TSan fault or a failed assertion.

**Implicated commit:** PR #1743 (the commit under test, `a688f8a1a94a...`) — interacting with the `LogProblemCounter` logic added in c7e97a41fee1 (Colin Hirsch, #1288) and the nvtx-tracing warnings from #1845/#1852 (e-eygin).

**File:** `test/gtest/main.cpp:104-108` (the `problems > 0 → return 42` gate); warnings originate at `tracer.cpp:139`, `nixl_plugin_manager.cpp:674`, and `nixl_agent.cpp:1291`.

**Suggested fix:** In single-process mode these benign warnings must not fail the run. Add `LogIgnoreGuard` regex suppressions in `RunAllTests()` (`test/gtest/main.cpp`) for the three expected messages — the nvtx-plugin-not-found warning, the missing `src/core/plugins` directory error, and the `getXferTelemetry` telemetry-disabled error — the same way `ib_regex`/`aws_regex`/`non_gpu_regex` are already handled. Better long-term: don't emit these at warning/error severity when the condition is expected (e.g. log "nvtx plugin not found" at info/debug and skip the plugin-dir probe when the dir doesn't exist), and/or reset `LogProblemCounter` per-test via a gtest listener so single-process accounting matches the old per-process behavior. Do not simply raise the count threshold.

**Related:** PR #1743 (under test); log-problem-counter introduced in PR #1288; nvtx trace backend warnings from PRs #1845 and #1852.

<!-- triage-agent request_id: f1a549cc-9c15-4ffb-83a9-0a267a8cf80a -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-03

👀 Investigating [`nixl-ci-dl-gpu-ep` build 78](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-dl-gpu-ep/78/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-03

🤖 **CI Triage Agent** — `nixl-ci-dl-gpu-ep` · commit `a688f8a1`

I have the full log. Let me analyze the failure carefully.

The build (both master and v1.21.x UCX variants) completed successfully — all libraries built, installed, and Docker images were pushed. The failure occurred in the **"Allocate DL EP Environment"** stage, specifically the SLURM `salloc` step.

Let me examine the key evidence around the failure:

- At `09:29:21` the job issues `salloc ... --immediate=3600 ... --account=blackwell` via SSH to `dlcluster.nvidia.com`.
- The next output line for that branch appears at `10:29:30` — roughly **60 minutes later**.
- The result: `salloc: Pending job allocation 1457303` / `salloc: job ... queued and waiting for resources` / `salloc: error: Unable to allocate resources: Connection timed out`.

This is not a hang in application code. The `--immediate=3600` flag tells SLURM to give up if resources can't be allocated within 3600 seconds (60 minutes). The ~60-minute gap between the `salloc` invocation and the timeout matches the `--immediate=3600` window exactly. The cluster partition `gb200nvl72_cx8` had no free nodes, so the job sat queued until the immediate-timeout fired and SLURM returned "Unable to allocate resources: Connection timed out."

Both parallel branches (`ucx-master` and `ucx-v1.21.x`) failed identically for the same reason — GB200 resource unavailability, not a code defect.

**Summary:** The `nixl-ci-dl-gpu-ep` build failed during the "Allocate DL EP Environment" stage; the SLURM `salloc` request for a GB200 node timed out after the 60-minute immediate window.

**Root cause:** Cluster resource unavailability, not a code/test defect. The build, install, and Docker image push all succeeded for both UCX variants. The `salloc -N 1 -p gb200nvl72_cx8 --immediate=3600 --time=01:30:00 --account=blackwell` job was queued (`job 1457303/1457305 queued and waiting for resources`) and, after the full `--immediate=3600` (60-minute) wait, returned `salloc: error: Unable to allocate resources: Connection timed out`. The 60-minute gap in the log timestamps (09:29 → 10:29) corresponds exactly to the immediate-timeout window, indicating the scheduler was legitimately waiting for a free node on the `gb200nvl72_cx8` partition, not a hung process. This PR (#1743) is not implicated — the failure is infrastructure/capacity-related.

**Implicated commit:** none (infrastructure failure, not caused by commit [REDACTED:Hex High Entropy String])

**File:** Jenkins pipeline SLURM allocation step (`slurm.allocation`, partition `gb200nvl72_cx8`); no repository source file implicated.

**Suggested fix:** This is a transient resource-availability failure — retry the build when GB200 (`gb200nvl72_cx8`) capacity is free. To reduce recurrence: (1) increase or tune `--immediate` / queue behavior so the job waits in-queue rather than failing, or make the allocation reton retry; (2) add scheduler-capacity checks/alerting for the `blackwell` account on that partition; (3) treat `salloc: Unable to allocate resources` as an infrastructure retry condition in the pipeline rather than a hard build failure. No code change is required.

**Related:** none



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `9c3d0540-a4a8-46b2-84be-c31c768881dd` in the triage console for the audit trail.

<!-- triage-agent request_id: 9c3d0540-a4a8-46b2-84be-c31c768881dd -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-03

👀 Investigating [`nixl-ci-gpu` build 2510](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-gpu/2510/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-03

🤖 **CI Triage Agent** — `nixl-ci-gpu` · commit `a688f8a1`

The main harness itself is fine. The single-process change (PR #1743) affects the two-peer test fixtures. The gtest binary hangs when peers run in one process. Given the log evidence — the failing branch (`build_helper/ucx-v1.21.x/2`, running the C++/gtest stage) produced no output for over 70 minutes before the timeout SIGTERM (exit 143 at 11:26:17) — this is a hang, not slowness.

I have enough evidence for a confident diagnosis. The exact deadlocking test fixture would be in the two-peer gtest files modified by PR #1743, but the log I successfully fetched confirms the hang signature and the CI stage.

**Summary:** The `build_helper/x86_64/ucx-v1.21.x/2` branch (the C++ gtest stage) was killed by the Jenkins stage timeout at 11:26:17 (exit code 143), while the `master`/job-60558 Python, Rust, and Nixlbench stages all passed cleanly.

**Root cause:** A hang, not a genuine timeout. The failing branch produced no application output for ~70+ minutes before the SIGTERM — a large silent gap that indicates a deadlocked process, not slow-but-progressing work. This is PR #1743 ("TEST/GTEST: Run in single process."), which collapses the previously multi-process two-peer gtest scenarios into a single process. Running both NIXL peers (initiator + target with a metadata listener, as seen in the `nixl_listener.cpp` connect/notify flow) in one process introduces a self-deadlock: one peer blocks waiting for a connection/notification from the other, but both are driven from the same process/thread that can no longer make progress, so `gtest-parallel ... ./bin/gtest` (test_cpp.sh line 107) never returns.

**Implicated commit:** PR #1743, commit [REDACTED:Hex High Entropy String] (the triggering commit for this build).

**File:** `.gitlab/test_cpp.sh:107` (the `gtest-parallel --workers=1 --serialize_test_cases ./bin/gtest` invocation that hung); root cause is in the two-peer gtest fixture(s) under `test/gtest/` changed by PR #1743.

**Suggested fix:** In the single-process two-peer gtest fixtures, ensure the two agents make progress cooperatively — e.g., drive both agents' progress/notification loops from the same thread (interleave `genNotif`/`getNotifs`/`getXferStatus` polling for both peers), or run the target peer on a dedicated background thread that is properly started before the initiator blocks and joined on teardown. Add a per-test timeout/watchdog so a stuck transfer fails fast with a clear message instead of hanging the whole stage. Do **not** raise the Jenkins stage time limit — that would only mask the deadlock. As an immediate unblock, revert or gate PR #1743's single-process mode until the peer synchronization is fixed.

**Related:** PR #1743 (https://github.com/ai-dynamo/nixl/pull/1743). Possibly relevant: PR #1846 ("PLUGINS/UCX: Added option to forcibly close UCP endpoints") and #1439/commit 0797ede5c477 (OS-assigned metadata listener ports), which touch the same listener/endpoint teardown paths.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `112b9f38-da2e-452e-b838-cd5f9d4a770f` in the triage console for the audit trail.

<!-- triage-agent request_id: 112b9f38-da2e-452e-b838-cd5f9d4a770f -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-03

👀 Investigating [`nixl-ci-dl-gpu-ep` build 80](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-dl-gpu-ep/80/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-03

🤖 **CI Triage Agent** — `nixl-ci-dl-gpu-ep` · commit `a688f8a1`

I have the logs. Let me analyze the failure. The build phase completed successfully (both master and v1.21.x images built and pushed). The failure is in the "Allocate DL EP Environment" stage.

Let me look at the timestamps around the failure point.

The key sequence:
- `12:26:03` — salloc command issued for both branches
- `13:26:07` / `13:26:12` — over **one hour later** — the salloc commands return `Unable to allocate resources: Connection timed out`

Both parallel branches (`ucx-master` and `ucx-v1.21.x`) failed identically at the SLURM resource-allocation step.

Let me examine the sequence more carefully to confirm this is a resource/allocation issue, not a code hang.

The salloc invocation was:
```
salloc -N 1 -p gb200nvl72_cx8 --job-name=nixl-ci-ep-master-80 --immediate=3600 --time=01:30:00 --no-shell --account=blackwell
```

The output shows:
```
salloc: Pending job allocation 1458227
salloc: job 1458227 queued and waiting for resources
salloc: error: Unable to allocate resources: Connection timed out
```

This is not a code bug or a test hang. The build finished cleanly. The gap of ~1 hour between `12:26:03` and `13:26:07` corresponds exactly to the `--immediate=3600` (3600 seconds = 1 hour) SLURM flag: the job was queued waiting for resources on the `gb200nvl72_cx8` partition and never got scheduled within the immediate-allocation window, so salloc gave up. The "Connection timed out" is SLURM's message for the immediate-allocation deadline expiring while the job stayed in `PENDING`/`queued and waiting for resources` state.

This is a SLURM cluster capacity/infrastructure issue, not a defect in commit a688f8a or PR #1743.

**Summary:** The build succeeded; the job failed while trying to allocate a GPU test node via SLURM `salloc` on the `gb200nvl72_cx8` partition — the allocation request stayed queued and timed out after the 1-hour `--immediate=3600` window.

**Root cause:** SLURM resource-allocation timeout, not a code fault. Both parallel branches (`ucx-master`, `ucx-v1.21.x`) issued `salloc -N 1 -p gb200nvl72_cx8 --immediate=3600 ...` at ~12:26:03. Each stayed in `Pending job allocation … queued and waiting for resources` and returned `error: Unable to allocate resources: Connection timed out` ~1 hour later (13:26:07 / 13:26:12). The ~1-hour silent gap matches the `--immediate=3600` deadline exactly — the `gb200nvl72_cx8` GB200 partition had no free nodes (or the SLURM controller on dlcluster.nvidia.com was unreachable) for the whole window. This is a cluster capacity / infrastructure problem, independent of the PR's code — the nixl and nixlbench builds and image pushes all completed cleanly before this stage.

**Implicated commit:** none — infrastructure failure, not caused by [REDACTED:Hex High Entropy String] or PR #1743.

**File:** Jenkins pipeline SLURM allocation step (`slurm.allocation`, partition `gb200nvl72_cx8`, headNode `dlcluster.nvidia.com`); no repository source file implicated.

**Suggested fix:** This is not a code fix. Retry the job when the `gb200nvl72_cx8` partition has capacity. If these transient allocation timeouts recur, address them at the infra/pipeline level, e.g.: (1) verify the SLURM controller on dlcluster.nvidia.com is reachable and the `blackwell` account has priority/quota on that partition; (2) add automatic retry/back-off around the `salloc` step for `Connection timed out` / immediate-allocation failures; (3) consider raising or removing `--immediate` and instead relying on `--time` queueing, or falling back to an alternate partition, so transient cluster contention doesn't fail the whole PR build. Do not treat this as a test regression.

**Related:** none found.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `0d3b769c-1b78-4bf6-bed8-ea5bc889dc97` in the triage console for the audit trail.

<!-- triage-agent request_id: 0d3b769c-1b78-4bf6-bed8-ea5bc889dc97 -->
<!-- triage-agent-bot -->

### ColinNV · 2026-07-05

/build

### svc-nixl · 2026-07-05

👀 Investigating [`nixl-ci-test-sanitizers` build 275](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-test-sanitizers/275/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

👀 Investigating [`nixl-ci-non-gpu` build 2165](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-non-gpu/2165/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

🤖 **CI Triage Agent** — `nixl-ci-non-gpu` · commit `b3dc9946`

No directly related issue. My diagnosis is well-supported by the log evidence itself.

**Summary:** All parallel CI branches of `nixl-ci-non-gpu` #2165 aborted with exit code 143 (SIGTERM) after the Jenkins JNLP agent pod was killed mid-run.

**Root cause:** Infrastructure failure, not a code/test failure. The log shows the Jenkins worker pod `worker-[REDACTED:Hex High Entropy String]-...-dmp0k` was terminated by Kubernetes — `Container [jnlp] terminated [Error]`, `jnlp -- terminated (137)`, `Pod just failed`, and `Agent ... was deleted; cancelling node body`. Exit 137 = the container was SIGKILL'd (OOM/eviction/preemption). This immediately cascaded SIGTERM (143) into every running step. Crucially, the application logs show **continuous activity with no multi-minute gaps right up to the kill** (gtests passing, `nixl_test` reporting "UCX Transfer Success!!!", azure test "Done.", and the Python wheel step actively emitting "Installing build dependencies: still running..." every ~30s). So this was neither a hang nor a legitimate slow-work timeout — the agent itself vanished. All C++ tests that completed passed (114/105 passed, only expected SKIPs); the `nixl_listener.cpp` "Connection refused"/errno=32 lines are the normal end-of-test teardown after the script's `kill -9 <target_pid>` and appear in passing runs too.

**Implicated commit:** unknown — not attributable to commit b3dc994 / PR #1743; this is an agent-pod termination.

**File:** N/A — infrastructure (`nbu-swx-nixl/worker-[REDACTED:Hex High Entropy String]-f2bc-44c7-ad9e-45d2d3d4f1f5-wp1w1-dmp0k`, jnlp container exit 137).

**Suggested fix:** Re-run the build — this is almost certainly a transient node eviction/OOM of the CI worker pod. If it recurs, investigate the pod's resource limits: the failure hit during the parallel Python wheel build (`pip install ... --config-settings=compile-args=-j16 .`) across multiple arch/CUDA branches simultaneously, which is memory-heavy; a `-j16` parallel compile on a memory-constrained pod is a plausible OOM trigger for exit 137. Consider raising the worker pod's memory `limits`/`requests` or lowering the wheel build parallelism (`compile-args=-jN`). Do **not** raise the step time limit — the run was making progress and was killed externally, not timed out.

**Related:** none found.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `031fe8c4-bafa-4395-917c-3f14d7daa9a2` in the triage console for the audit trail.

<!-- triage-agent request_id: 031fe8c4-bafa-4395-917c-3f14d7daa9a2 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

🤖 **CI Triage Agent** — `nixl-ci-test-sanitizers` · commit `b3dc9946`

This confirms the root cause. The `sanitizer` gtest test (line 129) sets only `sanitizer_env` (ASAN/UBSAN/TSAN options) and passes `plugin_dirs_arg` = the mocks path via `--tests_plugin_dirs`. It does **not** set `NIXL_PLUGIN_DIR` to the installed plugins dir where `libtrace_backend_nvtx.so` lives (`/opt/nixl/lib/x86_64-linux-gnu/plugins`).

So when the test agents create tracing with the default `nvtx` backend (enabled because `nixl_trace_nvtx_enabled` at build time), the plugin manager searches the build tree (`.../nixl_build/src/core/plugins` — which doesn't exist) and can't find `libtrace_backend_nvtx.so`. Each agent construction emits two `NIXL_WARN`/`NIXL_ERROR` messages, which the gtest `LogProblemCounter` treats as unexpected problems. After 138 such problems, the process exits with status 42 → the sanitizer suite fails.

Note the wall-clock kill at the end (exit 143 / SIGTERM at 08:04) is a *downstream symptom*: after the meson suite already FAILED (rc=1), the script continued running `ucx_backend_test` which ran the long inter-agent transfer loop and got killed when the pod was deleted. The actual triggering failure is the `exit status 42` in the meson sanitizer suite, and that reproduced deterministically on **both** the asan_ubsan and tsan branches at ~24s.

**Summary:** The `nixl:sanitizer / gtest` suite failed (`exit status 42`) because 138 unexpected NIXL warnings/errors were emitted during the run.

**Root cause:** The gtest process creates many agents with tracing enabled, each requesting the default `nvtx` trace backend. During `meson test`, `libtrace_backend_nvtx.so` cannot be located: the plugin manager scans the build-tree path `.../nixl_build/src/core/plugins` (which doesn't exist) and `NIXL_PLUGIN_DIR` is not set to the installed plugin dir. `tracer.cpp` then emits `NIXL_WARN: nixl::trace: backend 'nvtx' requested but plugin libtrace_backend_nvtx.so was not found` (plus the accompanying `nixl_plugin_manager.cpp:674` directory-access error) on every agent init. The gtest `LogProblemCounter` (test/gtest/common.cpp:180) counts every non-INFO log as an "unexpected problem"; there is no `LogIgnoreGuard` for these tracing/plugin messages, so the count reaches 138 and the harness exits 42. The trailing exit-code-143/SIGTERM is a downstream symptom of the pod being torn down after the suite had already failed — not a hang or a genuine timeout.

**Implicated commit:** `765f2f2432f4` — e-eygin, 2026-06-30, "tracing: add the NVTX trace backend plugin (libtrace_backend_nvtx.so) (#1845)" (introduced the `nvtx` default backend and the warning path exercised by the tests). Related tracer wiring: `[REDACTED:Hex High Entropy String]` (#1765).

**File:** `test/gtest/meson.build:129` (sanitizer `gtest` test invocation missing `NIXL_PLUGIN_DIR`); warning emitted at `src/core/tracing/tracer.cpp:20-23`; counted at `test/gtest/common.cpp:200`.

**Suggested fix:** Make the plugin discoverable to the sanitizer gtest run, or stop the tracing warning from counting as a test problem. Concretely, one of:
- In `test/gtest/meson.build`, set `sanitizer_env.set('NIXL_PLUGIN_DIR', <installed/build plugins dir containing libtrace_backend_nvtx.so>)` for the `gtest` test (as is already done for `mt_test` on line 149), so the `nvtx` backend loads instead of warning; or
- Add a `LogIgnoreGuard` covering `nixl::trace: backend '.*' requested but plugin .* was not found` (and the plugin-dir "No such file or directory" message) in the test fixtures/base setup so these expected environment warnings don't inflate the problem count; or
- Default the trace backend to none in unit tests unless the plugin dir is known to contain it.

**Related:** PR #1845 (added the nvtx plugin), PR #1765 (tracing API/loader), PR #1709 (added the ASan/UBSan/TSan sanitizer CI). Triggering PR under test: #1743.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `e17deadc-5bc9-48e8-8e97-5073311a8914` in the triage console for the audit trail.

<!-- triage-agent request_id: e17deadc-5bc9-48e8-8e97-5073311a8914 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

👀 Investigating [`nixl-ci-gpu` build 2527](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-gpu/2527/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

🤖 **CI Triage Agent** — `nixl-ci-gpu` · commit `b3dc9946`

I have enough evidence. Let me summarize.

## Diagnosis

**Timeline from the fetched Jenkins log (build #2527):**
- All stages I can see (Python examples, Rust tests, nixlbench) ran and produced continuous output through `2026-07-05T08:41:49Z` ("Engine destroyed", then `kill -9` of etcd, and the nixlbench stage's pipeline blocks closed cleanly).
- Then **complete silence from 08:41:49 until 10:03:56** — ~1h22m with zero application output — at which point Jenkins prints `Cancelling nested steps due to timeout`, `Sending interrupt signal`, and `script returned exit code 143`.
- The killed branch is `build_helper/x86_64/ucx-v1.21.x/2` (SLURM job 60938), the parallel sibling of the master branch whose log I captured.

The ~82-minute gap with no output is a **hang**, not a slow job. Exit 143 / "Cancelling nested steps due to timeout" are symptoms. The last meaningful application activity in the captured branch was the C++/nixlbench work; the hang occurred in the C++ test stage of the sibling branch, which is precisely what PR #1743 changes.

**Why PR #1743 is the cause:** PR #1743 is *"TEST/GTEST: Run in single process."* Today `.gitlab/test_cpp.sh:107` runs gtests via `gtest-parallel --workers=1 --serialize_test_cases`, which forks each test case into its own process and can detect/kill an individual hung case. Collapsing this into one monolithic `./bin/gtest` process removes that isolation: a single test that leaks a UCX progress thread or blocks on a listener/socket (we already see `nixl_listener.cpp:524 Listener thread could not connect` elsewhere in this build) will hang the entire process indefinitely, producing exactly the long silent gap → wall-clock kill observed.

**Summary:** The `nixl-ci-gpu` build was SIGTERM-killed (exit 143) after the C++ gtest stage hung silently for ~82 minutes (08:41 → 10:03) in the `ucx-v1.21.x` build_helper branch.
**Root cause:** A hang in the single-process GTEST run introduced by this PR — running all gtests in one process removes gtest-parallel's per-test isolation/kill, so one test blocking (e.g., a leaked UCX progress thread or a listener that never connects) hangs the whole process with no output until the wall-clock timeout. Not a legitimately slow job.
**Implicated commit:** PR #1743 "TEST/GTEST: Run in single process." (head commit [REDACTED:Hex High Entropy String]); baseline invocation from [REDACTED:Hex High Entropy String] (Guy Ealey Morag).
**File:** `.gitlab/test_cpp.sh:107` (the `gtest-parallel --workers=1 --serialize_test_cases ./bin/gtest ...` line changed by the PR).
**Suggested fix:** Do not drop process isolation without a hard timeout. Either keep `gtest-parallel` (which bounds/kills hung cases), or if running `./bin/gtest` directly, wrap it with a timeout and enable GoogleTest's per-test watchdog — e.g. `timeout 600 ./bin/gtest --gtest_flagfile=... ` plus `GTEST_TIMEOUT`/`--gtest_break_on_failure`, and identify the specific test that hangs (the recurring `nixl_listener.cpp:524 "Listener thread could not connect"` is the prime suspect for a blocking listener/socket test). Add `--gtest_shuffle` off and log per-test start lines so the hung test is identifiable next time.
**Related:** https://github.com/ai-dynamo/nixl/pull/1743 (the change under test). No prior matching issue found for the hang signature.

Secondary note (not the failure cause): the UCCL VRAM↔VRAM nixlbench runs show heavily degraded bandwidth (~0.035 GB/s vs UCX ~1.9 GB/s) alongside repeated `pin_thread_to_numa ... Failed to set thread affinity to NUMA node 1` errors — worth a separate investigation, but those tests completed and did not cause this timeout.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `ac858e2d-28af-4e72-a3f4-eaa24183a615` in the triage console for the audit trail.

<!-- triage-agent request_id: ac858e2d-28af-4e72-a3f4-eaa24183a615 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

👀 Investigating [`nixl-ci-gpu` build 2526](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-gpu/2526/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

🤖 **CI Triage Agent** — `nixl-ci-gpu` · commit `a688f8a1`

I now have a confident diagnosis. Let me analyze the timeline of the failure.

## Analysis

**The exit code 143 / "Cancelling nested steps due to timeout" / "script returned exit code 143" is a symptom, not the cause.**

Looking at the log timeline:
- The test suite ran `ucx/TestErrorHandling.*` tests fine: `BasicXfer`, `LoadRemoteThenFail`, `XferThenFail` all passed (`[ OK ]`).
- At `12:51:50.512` the test `ucx/TestErrorHandling.XferFailRestore/0` (the last one that `[ RUN ]` was printed for) hit real assertion failures:
  - `ib_mlx5_dv.c:433 UCX ERROR mlx5dv_devx_obj_modify(opcode=0x503) failed, syndrome 0x5d668c: Remote I/O error`
  - `error_handling.cpp:428: Failure — Expected: (0) <= (status), actual: 0 vs -3` (status `NIXL_ERR_BACKEND`)
  - `error_handling.cpp:319: Failure — Expected equality... NIXL_SUCCESS vs status -3`
  - `Unexpected UCX error: Out of memory`, `Input/output error`, `postXferReq: backend 'UCX' failed to post the transfer request with status NIXL_ERR_BACKEND`
- Then there is a **huge gap**: last app output at `2026-07-05T09:51:50.819Z`, next line `Cancelling nested steps due to timeout` at `2026-07-05T11:35:55.947Z` — roughly **1h44m of silence**.

That ~104-minute gap with zero output is the giveaway: after the assertion failures in `XferFailRestore`, the test process **hung** (it did not exit), and Jenkins eventually killed it with SIGTERM (143). The wall-clock kill is a symptom of the hang, not a "needs more time" situation.

**Root cause:** In `XferFailRestore`, the failure is injected via `failAfterPost` (`iter == 1`) — the target is destroyed *after* `postXferReq`. Line 428's `EXPECT_LE(0, status)` fired because `postXferReq` returned `NIXL_ERR_BACKEND` (-3) instead of the expected `NIXL_ERR_REMOTE_DISCONNECT`. The UCX backend surfaced a raw `mlx5dv_devx_obj_modify ... Remote I/O error` and `Out of memory`/`I/O error` instead of translating the disconnect into `NIXL_ERR_REMOTE_DISCONNECT`. Because the status wasn't the expected `NIXL_ERR_REMOTE_DISCONNECT`, `postXfer()` (line 107) did **not** release the request handle and returned the handle instead of the status; `testXfer` then went into `waitForCompletion(req_handle)` on a request whose remote peer is gone and whose backend request is in an error/leaked state — and that wait never completes, hanging the process until the Jenkins timeout.

The `XferFailRestore` code path (fail-after-post + restore over 3 iterations) is the newest/most complex error-handling scenario, and the ASan/UBSan/TSan sanitizer builds were recently added (commit 226d87bf, 2026-06-16), which both slow execution and change memory error surfacing. The combination is what exposes the unhandled `NIXL_ERR_BACKEND`-then-hang.

**Summary:** `ucx/TestErrorHandling.XferFailRestore/0` failed its assertions and then hung, causing the Jenkins wall-clock kill (exit 143).
**Root cause:** In the fail-after-post path, the UCX backend returned `NIXL_ERR_BACKEND` (-3) instead of `NIXL_ERR_REMOTE_DISCONNECT` when the target was destroyed after `postXferReq` (UCX reported a raw `mlx5dv_devx_obj_modify` "Remote I/O error" / "Out of memory" / "I/O error"). Because the status wasn't the expected `NIXL_ERR_REMOTE_DISCONNECT`, `postXfer()` skipped releasing the request handle (test/gtest/error_handling.cpp:107) and returned the handle; `testXfer` then called `waitForCompletion` on a request to a dead peer, which never completes — hanging until Jenkins killed it ~104 min later.
**Implicated commit:** unknown for the backend error-translation gap; the sanitizer builds that expose it were added in [REDACTED:Hex High Entropy String] (NirWolfer, "ci: add AddressSanitizer/UBSanitizer/ThreadSanitizer builds (#1709)"). The request-ownership-on-failure logic is from [REDACTED:Hex High Entropy String] (ovidiusm, "CORE: Do not delete requests unless we own them (#782)").
**File:** test/gtest/error_handling.cpp:428 (assertion `EXPECT_LE(0, status)`); triage of translation belongs in the UCX backend's `postXferReq`/error-mapping (`src/plugins/ucx`, surfaced via `ucx_enums.cpp:58`).
**Suggested fix:** In the UCX backend, map the post-disconnect failure (mlx5 devx "Remote I/O error" / UCX `UCS_ERR_IO_ERROR`/`UCS_ERR_NO_MEMORY` after peer teardown) to `NIXL_ERR_REMOTE_DISCONNECT` rather than a generic `NIXL_ERR_BACKEND`, so `postXfer()` takes the release-and-return-status branch. Additionally make `waitForCompletion` in the test robust to a backend-errored request (release the handle when `postXferReq` returns any error, not only `NIXL_ERR_REMOTE_DISCONNECT`) so a failed post can never lead to an unbounded wait/hang. Do not simply raise the Jenkins time limit — the process is hung, not slow.
**Related:** PRs #1709 (sanitizer builds), #782 (request ownership on failure), #1288 (fail-on-unexpected-log, which turns the UCX warnings into hard failures). none other found.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `c19877a0-6bfd-45cc-9f29-cb40abf9ba32` in the triage console for the audit trail.

<!-- triage-agent request_id: c19877a0-6bfd-45cc-9f29-cb40abf9ba32 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

👀 Investigating [`nixl-ci-gpu` build 2529](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-gpu/2529/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

🤖 **CI Triage Agent** — `nixl-ci-gpu` · commit `b3dc9946`

I now have a confident diagnosis. Let me summarize.

**Key timeline evidence (gap analysis):**
- `10:49:04.980` — last application output: UCCL backend, `READ` `DRAM→DRAM` test, `Destroying Engine... / Stop background accept... / Engine destroyed` (one peer), plus `Error/HUP on connection: 1.1.101.1:46993`
- `11:37:11.048` — Jenkins `Cancelling nested steps due to timeout` (exit 143)
- **~48 minutes of complete silence** — the 50‑minute stage timeout fired.

This is a hang, not slow work. The prior UCCL sub-tests each completed in ~10 s, so 48 min of silence is not legitimate work.

---

**Summary:** The "Run Nixlbench tests" stage (Jenkins job `nixl-ci-gpu` #2529) was killed by its 50‑minute stage timeout (exit code 143 / SIGTERM).

**Root cause:** A hang — not a genuine slowdown — in the UCCL backend teardown of the two‑worker ASIO benchmark. The last application output at `10:49:04.980` was the UCCL `READ DRAM→DRAM` case tearing down (`Destroying Engine... / Stop background accept... / Engine destroyed`), immediately followed by `Error/HUP on connection: 1.1.101.1:46993`. The very next `nixlbench` iteration (`WRITE VRAM→VRAM`, `.gitlab/test_nixlbench.sh:95‑98`) never emitted a config banner or `Creating Engine`. One of the two `parallel`-launched UCCL peers did not exit cleanly, so `parallel --halt now,fail=1` (test_nixlbench.sh:83) blocked waiting for the child, producing ~48 min of dead air until the timeout. The hang is in UCCL engine shutdown / connection close, not in the PR under test.

**Implicated commit:** unknown for the hang itself (no recent UCCL-backend or nixlbench change lines up with this run). The triggering PR **#1743 "TEST/GTEST: Run in single process."** (author of build b3dc994) touches the gtest harness only and is almost certainly *not* the cause — this looks like a pre‑existing/flaky UCCL teardown hang. UCCL backend last touched by `bdfea5d0` (Colin Hirsch) / `f8e2221f`, `63c494e1`, `151b6abb`, `4749b354` (Pravein Govindan Kannan).

**File:** Hang manifests around `benchmark/.gitlab/test_nixlbench.sh:83` (`parallel --halt now,fail=1`) during the UCCL loop at `test_nixlbench.sh:94‑102`; underlying defect is in the UCCL plugin engine-destroy / connection-close path (`src/plugins/uccl`, `epoll_client.h:207` reports the `Error/HUP`).

**Suggested fix:**
1. Do **not** raise the stage timeout — that would only mask a hang.
2. Reproduce the UCCL `READ DRAM→DRAM` → next-test transition and investigate the UCCL engine shutdown/`Stop background accept` path for a blocking join/close after the `Error/HUP on connection` event (peer not signaling clean exit). Add a shutdown timeout/force-close on HUP so the process always exits.
3. As a CI mitigation, wrap each `run_nixlbench_two_workers_asio` invocation in a per-test `timeout` (e.g. `timeout 120 parallel ...`) so a single hung backend fails fast with a clear signal instead of silently consuming the whole stage.
4. Re-run the pipeline to confirm whether the hang is deterministic or flaky; given PR #1743 is unrelated, retriggering may pass.

**Related:** PR #1743 (trigger, unrelated gtest change); UCCL integration/history PRs #895, #1151, #1271, #1428, #1595. No existing issue found specifically tracking this UCCL teardown hang — consider filing one.

<!-- triage-agent request_id: c212e954-f2fc-44c8-a80b-79e025af1b94 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

👀 Investigating [`nixl-ci-gpu` build 2543](http://nbuprod.blsm.nvidia.com/nbu-swx-nixl-main/job/nixl-ci-gpu/2543/)…

<!-- triage-agent-bot -->

### svc-nixl · 2026-07-05

🤖 **CI Triage Agent** — `nixl-ci-gpu` · commit `b3dc9946`

I have enough evidence to form a confident diagnosis. Let me summarize what the log shows.

**Key timeline facts from the fetched Jenkins log:**
- The `Run Rust tests` and `Run Nixlbench tests` stages (on the master SLURM job 61144) ran to completion. The last nixlbench test finished and etcd was killed at **16:52:18**.
- The failing branch reported at the end is **`build_helper/x86_64/ucx-v1.21.x/2`** (a *separate*, parallel branch — SLURM job 61143, the C++ / gtest branch built against UCX v1.21.x).
- After 16:52:18 the log is **completely silent for ~69 minutes** until **18:01:49**, when the outer timeout fires: "Cancelling nested steps due to timeout" → "Sending interrupt signal to process" → `script returned exit code 143`.

A ~69-minute gap with zero output followed by a SIGTERM/143 kill is a **hang**, not legitimate slowness. Per the triage rules, exit 143 / "Cancelling ... due to timeout" is a symptom; the root cause is whatever hung.

The hanging branch is the C++ test path (`.gitlab/test_cpp.sh`), which is exactly what PR **#1743 ("TEST/GTEST: Run in single process")** modifies — the `gtest-parallel --workers=1 --serialize_test_cases ./bin/gtest ...` invocation on line 107. Running the whole gtest suite serialized in a single process/worker is the operation most likely to hang here (e.g., a UCX-transport gtest blocking on a transfer/notification that never completes, or a port/listener wait), and it would produce no incremental console output while `gtest-parallel` buffers per-test output — which matches the total silence in the log.

Note: I could not fetch a per-branch/GHA log for job 61143 to see the last gtest test name (the Jenkins console only captured the master branch's stdout in detail; the ucx-v1.21.x branch's inner output was not interleaved). The evidence that the *hang* is in the gtest single-process run comes from: the failing branch name, the C++ test script content, and PR #1743's stated change.

**Summary:** The `build_helper/x86_64/ucx-v1.21.x` C++ test branch hung — no output for ~69 minutes (16:52:18 → 18:01:49) — and was killed by the outer Jenkins wall-clock timeout (exit 143).

**Root cause:** A hang (deadlock/blocked transfer), not slowness. The last-running operation in that branch is the C++ test stage (`.gitlab/test_cpp.sh`), whose gtest step runs the whole suite serialized in one worker: `gtest-parallel --workers=1 --serialize_test_cases ./bin/gtest -- --min-tcp-port=… --max-tcp-port=…` (`.gitlab/test_cpp.sh:107`). This is precisely the behavior introduced/exercised by PR #1743 ("Run in single process"). Running serialized in a single process removes per-test process isolation, so one gtest case that blocks (e.g., a UCX-backend transfer/notification wait or a listener never connecting) stalls the entire suite indefinitely with no console output, until the stage timeout kills it.

**Implicated commit:** PR #1743 (head commit [REDACTED:Hex High Entropy String]) — the change under test. (Not the pre-existing `.gitlab/test_cpp.sh` history commits.)

**File:** `.gitlab/test_cpp.sh:107` (the `gtest-parallel --workers=1 --serialize_test_cases ./bin/gtest` invocation).

**Suggested fix:** Do not raise the timeout. Reproduce the hang by running the single-process gtest command locally against the UCX v1.21.x build and identify which test blocks:
- Add a hard per-test/suite watchdog so a hung case fails fast with a backtrace instead of silently stalling — e.g. `gtest-parallel --timeout=<sec> …` and/or wrap `./bin/gtest` in `timeout` and enable `GTEST_BREAK_ON_FAILURE`/core dump so the hung test is named in the log.
- Restore per-test-case process isolation (drop `--serialize_test_cases`/single-process for the UCX suite, or keep the previous multi-worker behavior) if PR #1743's single-process mode is what introduced the deadlock, until the specific hanging gtest (likely a UCX-backend transfer/notification or metadata-listener wait) is fixed.

**Related:** PR #1743 (the failing change); PR #1846 ("PLUGINS/UCX: Added option to forcibly close UCP endpoints") and the UCX bump PR #1507 are relevant to UCX-endpoint hang investigation.



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `127abd52-8b5f-4a6c-8988-e96ba892a8c5` in the triage console for the audit trail.

<!-- triage-agent request_id: 127abd52-8b5f-4a6c-8988-e96ba892a8c5 -->
<!-- triage-agent-bot -->

### ColinNV · 2026-09-15

/build

### ColinNV · 2026-09-15

/build

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `nixl-ci-non-gpu` · commit `b55bdf7c`

**TL;DR:** All six parallel `Build` shards failed with the same hard compile error — `src/core/nixl_plugin_manager.cpp:436` references a mutex member named `lock`, but the member was renamed to `mutex_`, so `const std::lock_guard lg(lock)` doesn't compile under `-Wall -Werror`. Fix is to use `mutex_` (which is what the branch tip `b55bdf7c "Fix merge error"` already does) and re-trigger the build.

<details><summary>Full analysis</summary>

**Summary:** `nixl-ci-non-gpu` #3057 failed in every parallel `Build` stage while compiling `libnixl.so`: `error: 'lock' was not declared in this scope; did you mean 'std::lock'?`

**Root cause:** A bad merge of `main` into `single_gtest`. Commit `a9543fb4` ("CORE: Fix plugin related race") on main renamed the plugin-manager's mutex member; `src/core/plugin_manager.h:203` now declares `mutable std::mutex mutex_;` and there is no member called `lock`. The branch-only function `nixlPluginManager::reinitializeForUnitTest()` (added for the single-gtest work) was carried through merge commit `4e188ff0` still locking the old name:

```
../src/core/nixl_plugin_manager.cpp:436:34: error: 'lock' was not declared in this scope
  436 |         const std::lock_guard lg(lock);
```

Because the project builds with `-Wall -Winvalid-pch -Werror`, this stops `ninja` at `nixl_plugin_manager.cpp.o`. Everything after it in the logs is cascading fallout of that one failure, not independent bugs:
- `cp: cannot stat 'nixl_build/src/bindings/python/nixl-meta/nixl-*.whl'` (wheel never built)
- `benchmark/nixlbench/meson.build:55:15: ERROR: C++ shared or static library 'nixl' not found` (libnixl.so never linked)

This is a deterministic compile error reproduced identically on all six container variants (CUDA 12.9 and 13.4, gcc 13.3.0) — no infrastructure component is involved.

**Implicated commit:** `4e188ff0` — Colin Hirsch, "Merge branch 'main' into single_gtest" (2026-09-15T13:20:16Z), which combined the branch's `reinitializeForUnitTest()` with the mutex rename from `a9543fb4` (Colin Hirsch). Note the workspace appears to have compiled this merge commit rather than the reported trigger SHA `b55bdf7c` ("Fix merge error", 13:24:39Z, ~1 min before checkout at 13:25:35Z) — the current branch tip already reads `mutex_` at line 436.

**File:** `src/core/nixl_plugin_manager.cpp:436` (member declared at `src/core/plugin_manager.h:203`)

**Suggested fix:** Change line 436 to lock the correct member:
```cpp
const std::lock_guard lg(mutex_);
```
Then re-trigger `nixl-ci-non-gpu` on the current tip of `single_gtest` (`b55bdf7c`), which already contains this change — verify Jenkins picks up that SHA and isn't reusing a stale workspace checkout of `4e188ff0`. While there, grep the rest of the merge for other references to the pre-rename `lock` member (e.g. `git grep -n '(lock)' src/core/`) so the next build doesn't fail on a sibling occurrence.

**Related:** PR #1743 (`single_gtest`); upstream rename in `a9543fb4` / PR #2075 "CORE: Fix plugin related race".

</details>

<!-- triage-agent request_id: 2b0233fb-022b-4f8e-b02f-7067b3c446ca -->
<!-- triage-agent gha_run_id: status:SC_kwDOODt2nc8AAAAMnp9eyA -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `nixl-ci-test-sanitizers` · commit `b55bdf7c`

**TL;DR:** The `meson test --suite sanitizer` "gtest" case fails even though all 243 gtest cases pass — `test/gtest/main.cpp` returns 42 because one `NIXL_ERROR` was logged ("Error accessing directory(<build>/src/core/plugins): No such file or directory"), which the `LogProblemCounter` counts as an unexpected problem. Fix: stop logging a missing *optional* default plugin directory at ERROR level (or set `NIXL_PLUGIN_DIR` in the gtest test env) so the in-build-tree run doesn't trip the "unexpected warning/error" guard.

<details><summary>Full analysis</summary>

**Summary:** Both parallel branches (`Test Sanitizer` asan_ubsan = node 190, tsan = node 177) end with `==== Sanitizer test FAILURES: meson sanitizer suite ==== / exit 1`; every standalone binary (agent_example, nixl_example, ucx_backend_test, nixl_posix_test, serdes_test, test_plugin, ucx_backend_multi) printed PASSED, so only the meson `gtest` suite failed.

**Root cause:** From the archived `meson-logs/testlog.junit.xml` (in `sanitizer-logs-x86_64-asan_ubsan.tar.gz`): `<testsuite name="nixl" tests="1" failures="1">` for `nixl:sanitizer / gtest`, yet the embedded gtest output is `[==========] 243 tests from 43 test suites ran. [ PASSED ] 215 tests. [ SKIPPED ] 28 tests` — zero test failures. The non-zero exit comes from the harness's unexpected-log guard, whose stderr reads:

```
E0915 13:35:12.714019 71451 nixl_plugin_manager.cpp:672] Error accessing directory(".../nixl_build/src/core/plugins"): No such file or directory
ATTENTION: Unexpected NIXL warning or error detected!
...
ATTENTION: Problem count is 1
```

`test/gtest/main.cpp:126-131` returns 42 whenever `LogProblemCounter::getProblemCount() > 0`. The offending log is emitted by `nixlPluginManager`'s constructor path (`processPluginDir` → `discoverPluginsFromDir`, `src/core/nixl_plugin_manager.cpp:672`), which unconditionally uses `NIXL_ERROR` when the directory can't be opened. The directory it probes is the implicit dladdr-derived default (`<dir of libnixl>/plugins` — `nixl_plugin_manager.cpp:392-398`), which never exists when running out of the meson build tree, and the `gtest` test is registered with only `sanitizer_env` — no `NIXL_PLUGIN_DIR` (`test/gtest/meson.build:175`), unlike `mt_test` which does set it (`test/gtest/meson.build:209`).

Why this surfaces on this branch: the `PluginReinitializer` listener that used to call `reinitializeForUnitTest()`/`addPluginDirectory()` is commented out in `test/gtest/main.cpp:120-122`, so the plugin manager singleton is now first constructed lazily *inside* `RUN_ALL_TESTS()` — i.e. inside the `LogProblemCounter` window — and the `--tests_plugin_dirs=` value parsed at `main.cpp:54-62` is now dead (its only consumer was that listener); the mock backend still loads via the generated `pluginlist` file, which is why no test itself fails.

**Implicated commit:** 9cad9e54 "Disable plugin reinitialization." — Colin Hirsch (branch `single_gtest`, PR #1743; build head [REDACTED:Hex High Entropy String])

**File:** `src/core/nixl_plugin_manager.cpp:672` (error emitted); `test/gtest/main.cpp:120-122` and `126-131` (guard + disabled listener); `test/gtest/meson.build:175` (test env missing `NIXL_PLUGIN_DIR`)

**Suggested fix:** Make the implicit default plugin directory non-fatal to the log guard: in `discoverPluginsFromDir`, check `std::filesystem::exists(dirpath)` first and log `NIXL_DEBUG`/`NIXL_INFO` for a simply-absent directory, reserving `NIXL_ERROR` for a directory that exists but cannot be iterated. As a targeted alternative/complement in this PR, add `NIXL_PLUGIN_DIR` (set to `mocks_dep.get_variable('path')`) to the `sanitizer_env` used by `test('gtest', ...)`, mirroring `mt_test`. Also clean up the now-dead `--tests_plugin_dirs` plumbing — either re-enable the listener or register the dirs once in `RunTests` before `RunAllTests()` so the argument still has effect.

**Related:** PR #1743 "TEST/GTEST: Run in single process."; PR #1943 "test/gtest: fail on unexpected NVTX tracing skips" (same unexpected-log-fails-the-run mechanism)

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `26dc386b-5dc3-4a18-a693-63b8dea69d16` in the triage console for the audit trail.

<!-- triage-agent request_id: 26dc386b-5dc3-4a18-a693-63b8dea69d16 -->
<!-- triage-agent gha_run_id: status:SC_kwDOODt2nc8AAAAMnq1Y-A -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `nixl-ci-dl-gpu-ep` · commit `b55bdf7c`

**TL;DR:** The Docker image build failed because `src/core/nixl_plugin_manager.cpp` referenced a mutex member named `lock` that no longer exists (it was renamed `mutex_` on main), a botched resolution in the merge commit `4e188ff0`; the follow-up commit `b55bdf7c` "Fix merge error" already changes it to `mutex_`, so re-run CI on that tip.

<details><summary>Full analysis</summary>

**Summary:** Stage "Compiling NIXL EP Docker Image for DL" failed — `ninja` aborted on a C++ compile error in `libnixl.so` (`nixl_plugin_manager.cpp`), after which `cp nixl_build/.../nixl-*.whl dist/` failed and the `podman/docker build` step exited 1.

**Root cause:** Compile error, not infra or timeout:
```
../src/core/nixl_plugin_manager.cpp: In member function 'void nixlPluginManager::reinitializeForUnitTest()':
../src/core/nixl_plugin_manager.cpp:436:34: error: 'lock' was not declared in this scope; did you mean 'std::lock'?
  436 |         const std::lock_guard lg(lock);
```
The branch-local function `reinitializeForUnitTest()` (added on `single_gtest` in `8f0c0cb0` "Try plugin manager reset.") guards its state reset with a member named `lock`. On `main`, commit `a9543fb4` ("CORE: Fix plugin related race. (#2075)", Colin Hirsch) reworked the locking and the member is now declared as `mutable std::mutex mutex_;` (`src/core/plugin_manager.h:203`). The merge commit `4e188ff0` ("Merge branch 'main' into single_gtest") kept the old identifier `lock` inside the merged-in `reinitializeForUnitTest()`, so the name resolves to nothing and GCC only finds `std::lock` via ADL. The build is also `-Werror`, but this is a hard error regardless. The subsequent `cp: cannot stat 'nixl_build/src/bindings/python/nixl-meta/nixl-*.whl'` and the `Error: building at STEP "RUN ... .gitlab/build.sh"` are downstream symptoms of the same failed `ninja` invocation.

**Implicated commit:** `4e188ff0` — Colin Hirsch, "Merge branch 'main' into single_gtest" (bad merge resolution against `a9543fb4`, Colin Hirsch, which renamed the mutex to `mutex_`). Fix commit `b55bdf7c` "Fix merge error." (Colin Hirsch) is on the branch tip and current source at that path now reads `const std::lock_guard lg(mutex_);`.

**File:** `src/core/nixl_plugin_manager.cpp:436` (member declared at `src/core/plugin_manager.h:203`)

**Suggested fix:** Nothing further to change in the source — `b55bdf7c` already replaces `lock` with `mutex_`. Re-trigger `nixl-ci-dl-gpu-ep` on `b55bdf7c` (build #1185 evidently compiled the pre-fix merge state `4e188ff0`). If the failure reproduces on `b55bdf7c`, verify the checkout SHA in the "Checkout source code" stage and grep for any remaining references to the old member name:
```
grep -rn '\block\b' src/core/nixl_plugin_manager.cpp src/core/plugin_manager.h
```
Longer term, make `.gitlab/build.sh` fail fast and explicitly on the `ninja` return code rather than letting it fall through to the `cp ... *.whl` step, so the real compile error is the last thing in the log instead of a misleading "cannot stat" message.

**Related:** PR #1743 (branch `single_gtest`); upstream rename in PR #2075 ("CORE: Fix plugin related race.")

</details>

<!-- triage-agent request_id: 9a0655b9-6ee6-4a24-880f-25ecc6abbb27 -->
<!-- triage-agent gha_run_id: status:SC_kwDOODt2nc8AAAAMnrOWNA -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `AWS NIXL Validation` · commit `4e188ff0`

**TL;DR:** The build failed on a hard compile error in `src/core/nixl_plugin_manager.cpp:436` — `reinitializeForUnitTest()` locks a member named `lock`, but the mutex member is actually `mutex_`; change it to `const std::lock_guard lg(mutex_);`.

<details><summary>Full analysis</summary>

**Summary:** `ninja` compilation of `src/core/libnixl.so.p/nixl_plugin_manager.cpp.o` failed with `-Werror`, so `libnixl.so` was never produced and the downstream `nixlbench` meson setup then failed with `C++ shared or static library 'nixl' not found`, causing the AWS Batch NIXL validation job to report FAILED.

**Root cause:** A merge artifact. The PR branch (`single_gtest`) added `nixlPluginManager::reinitializeForUnitTest()` (branch commit `8f0c0cb0` "Try plugin manager reset.") which took the manager's mutex under the old member name `lock`. Meanwhile `main` commit `a9543fb4` ("CORE: Fix plugin related race. (#2075)", Colin Hirsch) established the member as `mutable std::mutex mutex_` (`src/core/plugin_manager.h:203`) — every other call site in the file uses `mutex_` (e.g. lines 474, 508). The merge commit `4e188ff0` pulled in the renamed member but left the new function referencing the stale name, so GCC reports:

```
../src/core/nixl_plugin_manager.cpp:436:34: error: 'lock' was not declared in this scope; did you mean 'std::lock'?
  436 |         const std::lock_guard lg(lock);
```

This is a deterministic compile error, not flakiness or an infrastructure problem — the whole failure sequence took ~2 minutes with no stalls.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — Colin Hirsch, "Merge branch 'main' into single_gtest" (merging branch commit `8f0c0cb0` against `a9543fb4`)

**File:** `src/core/nixl_plugin_manager.cpp:436` (member declared at `src/core/plugin_manager.h:203`)

**Suggested fix:** Rename the referenced member to match the header:

```cpp
void
nixlPluginManager::reinitializeForUnitTest() {
    // Lock scope
    {
        const std::lock_guard lg(mutex_);
        ...
    }
```

Then grep the branch for any other pre-rename references (`git grep -n 'lock_guard.*(lock)' src/`) since the same merge may have left more of them in files not exercised before the build aborted. Note also that `reinitializeForUnitTest()` calls `processPluginFile()`/`processPluginDir()` after releasing the lock while `addPluginDirectory()`/`discoverPluginsFromDir()` acquire it themselves — verify that ordering still matches the locking invariants `a9543fb4` introduced, so the merge doesn't silently reintroduce the race that commit fixed.

**Related:** PR #1743 (this build); upstream race fix PR #2075 (`a9543fb4`), which renamed the mutex member. No existing issue tracks this specific error.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `783bdcb4-5aff-42ff-aee2-ee073872aed8` in the triage console for the audit trail.

<!-- triage-agent request_id: 783bdcb4-5aff-42ff-aee2-ee073872aed8 -->
<!-- triage-agent gha_run_id: 34974358982 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `nixl-ci-build-wheel` · commit `b55bdf7c`

**TL;DR:** The wheel build failed with a hard C++ compile error in `src/core/nixl_plugin_manager.cpp:436` — `reinitializeForUnitTest()` locks a member named `lock`, but on `main` that member was renamed to `mutex_`, so the merge of `main` into `single_gtest` left stale code. Fix is to change `std::lock_guard lg(lock)` to `std::lock_guard lg(mutex_)`.

<details><summary>Full analysis</summary>

**Summary:** All three parallel "Build Wheel" stages of `nixl-ci-build-wheel` #1763 failed at the in-container `meson`/`ninja` step: `src/core/libnixl.so.p/nixl_plugin_manager.cpp.o` failed to compile.

**Root cause:** A merge conflict resolution error, not an infra problem. In stage 396 log:

```
../src/core/nixl_plugin_manager.cpp: In member function ‘void nixlPluginManager::reinitializeForUnitTest()’:
../src/core/nixl_plugin_manager.cpp:436:34: error: ‘lock’ was not declared in this scope; did you mean ‘std::lock’?
  436 |         const std::lock_guard lg(lock);
ninja: build stopped: subcommand failed.
```

`reinitializeForUnitTest()` is a branch-local addition from `8f0c0cb0` ("Try plugin manager reset.") on `single_gtest`, written when the manager's mutex member was called `lock`. Meanwhile `a9543fb4` ("CORE: Fix plugin related race", Colin Hirsch) on `main` renamed that member — `src/core/plugin_manager.h:203` now declares `mutable std::mutex mutex_;` and there is no member named `lock`. The merge `4e188ff0` ("Merge branch 'main' into single_gtest") kept the old identifier in the merged body, so the translation unit no longer compiles. There is no `lock` variable in scope, hence GCC's suggestion of `std::lock`.

The two `git`-related warnings earlier in the log (`fatal: detected dubious ownership in repository ...`, "No git release tag found, setting to unknown version: 0.0.1") are unrelated cosmetic noise — they only affect version stamping, not the compile.

Note the current repository tree already shows `const std::lock_guard lg(mutex_);` at that line, consistent with the follow-up commit `b55bdf7c` ("Fix merge error.") — that fix was evidently not in the tree this build actually compiled, so a rebuild on the corrected tip should pass.

**Implicated commit:** `4e188ff0` (Colin Hirsch, "Merge branch 'main' into single_gtest") introduced the inconsistency by merging the member rename from `a9543fb4` (Colin Hirsch, "CORE: Fix plugin related race. (#2075)") against the branch-local `reinitializeForUnitTest()` from `8f0c0cb0`. Build head was `[REDACTED:Hex High Entropy String]`.

**File:** `src/core/nixl_plugin_manager.cpp:436` (member declared at `src/core/plugin_manager.h:203`)

**Suggested fix:** In `reinitializeForUnitTest()`, use the current member name:

```cpp
const std::lock_guard lg(mutex_);
```

Then re-run `nixl-ci-build-wheel` on the updated `single_gtest` tip to confirm. Also grep the branch for any other survivors of the rename (`git grep -n '\block\b' src/core/nixl_plugin_manager.cpp src/core/plugin_manager.h`) — a merge that missed one reference often missed others, and since the build uses `-Werror -std=c++20` any such leftover will be fatal in all three wheel variants at once, as happened here.

**Related:** PR #1743 (branch `single_gtest`); upstream rename in #2075.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `0722af75-6666-4a0f-8d95-43c0bb6a8b5a` in the triage console for the audit trail.

<!-- triage-agent request_id: 0722af75-6666-4a0f-8d95-43c0bb6a8b5a -->
<!-- triage-agent gha_run_id: status:SC_kwDOODt2nc8AAAAMnrmv_w -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `AWS NIXL Validation` · commit `b55bdf7c`

**TL;DR:** The consolidated `./bin/gtest` binary aborted in `MpE2ETest.AllRankProcessesAggregateBehindOneEndpointAndStaleAreDropped` because the test calls `::fork()` in a process that had already initialized the Libfabric EFA provider (earlier LIBFABRIC/UCX-EFA suites in the same process), and EFA's fork handler aborts unless `FI_EFA_FORK_SAFE=1`. Either export `FI_EFA_FORK_SAFE=1` for the gtest run or keep the multi-process telemetry E2E test in its own binary / replace raw `fork()` with fork+exec.

<details><summary>Full analysis</summary>

**Summary:** GHA job "AWS NIXL Validation" (run 34974833179) failed at `.gitlab/test_cpp.sh:116` — `./bin/gtest` died with `Aborted (core dumped)` during `MpE2ETest.AllRankProcessesAggregateBehindOneEndpointAndStaleAreDropped`.

**Root cause:** The log shows all 246 gtests running in a single process (pid 203484). Before reaching `MpE2ETest`, that same process repeatedly created UCX/Libfabric agents on an EFA-equipped AWS runner (`1 Amazon EFA(s) were detected...` from `nixl_agent.cpp:288`, and `HardwareWarningTest.EfaHardwareMismatchNoWarning` explicitly built `backends=[LIBFABRIC]`, `[UCX, LIBFABRIC]`, `[LIBFABRIC, UCX]`). The EFA provider installs a fork handler; when `MpE2ETest` then forks three writer children (`test/gtest/telemetry_mp_e2e_test.cpp:139`), the provider printed its standard diagnostic ("A process has executed an operation involving a call to the fork() system call… Set FI_EFA_FORK_SAFE=1 … Your job will now abort.") and aborted the whole gtest binary. This is a code/CI-configuration defect, not slowness or infra: the abort is immediate, with no timing gaps in the log.

The PR under test (#1743, branch `single_gtest`) is what merges the telemetry multi-process tests into the one shared `gtest` executable — `test/gtest/meson.build:148-155` adds `telemetry_mp_e2e_test.cpp` to `gtest_sources`, and `.gitlab/test_cpp.sh:116` runs that single binary. In isolation the fork is safe; sharing a process with EFA-initializing suites makes it fatal.

**Implicated commit:** 4e188ff0 — Colin Hirsch, "Merge branch 'main' into single_gtest" (branch `pull-request/1743`, commit b55bdf7c); related earlier work on the same branch: aea206af "TEST/GTEST: Run in single process in random order."

**File:** `test/gtest/telemetry_mp_e2e_test.cpp:139` (the `::fork()` loop); enabled by `test/gtest/meson.build:150` and invoked from `.gitlab/test_cpp.sh:116`

**Suggested fix:** Pick one:
1. Quick unblock — export `FI_EFA_FORK_SAFE=1` in `.gitlab/test_cpp.sh` before `./bin/gtest` (the runner kernel/rdma-core on AWS supports it), so the EFA provider tolerates the fork.
2. Robust — don't put a forking test in the shared process. Keep `telemetry_mp_e2e_test.cpp` in its own executable (as before this PR) and run it separately from `.gitlab/test_cpp.sh`, or make the child creation `fork()`+`execve()`/`posix_spawn()` of the gtest binary with a `--gtest_filter` helper mode, which is immune to the EFA atfork abort.
3. Belt-and-braces — have `MpE2ETest::SetUp()` `GTEST_SKIP()` when an EFA device is present and `FI_EFA_FORK_SAFE` is unset, so the suite degrades instead of aborting the whole run.

**Related:** PR #1743 (this change); telemetry multi-process test origins: #1920, #2054. No existing issue found for the EFA fork abort.

</details>

<!-- triage-agent request_id: a1215838-1074-49d3-a76b-880bfabb6ad5 -->
<!-- triage-agent gha_run_id: 34974833179 -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `nixl-ci-build-container-pr` · commit `b55bdf7c`

**TL;DR:** All four parallel "Build image" stages failed at Dockerfile step 53/72 because the torch wheel index URL is derived from `CUDA_VERSION` (13.4 → `https://download.pytorch.org/whl/cu134`), an index PyTorch does not publish, so `uv` found no cp312-compatible torch. Pin the index to a real PyTorch channel (e.g. `cu130`) instead of computing it from `CUDA_VERSION`.

<details><summary>Full analysis</summary>

**Summary:** `nixl-ci-build-container-pr` #650 — every "Build image" stage failed in the container build at `RUN ... uv pip install --system torch torchvision torchaudio` (podman step 53/72).

**Root cause:** The Dockerfile computes the PyTorch wheel index from the CUDA version of the base image: `UV_INDEX="https://download.pytorch.org/whl/cu$(echo $CUDA_VERSION | cut -d. -f1,2 | tr -d .)"`. The base image is `nvcr.io/nvidia/cuda-dl-base:26.08-cuda13.4-devel-ubuntu24.04`, so `CUDA_VERSION=13.4` yields `cu134` — an index that does not exist upstream. The request does not 404 outright; the stale/degenerate index only offers `torch 2.0.1` for cp38–cp311, so `uv` errors out:

```
error: No solution found when resolving dependencies
  cause: Because all versions of torch have no wheels with a matching Python ABI tag (e.g., `cp312`) ...
hint: You require CPython 3.12 (`cp312`), but we only found wheels for `torch` (v2.0.1) with the following Python ABI tags: `cp38`, `cp39`, `cp310`, `cp311`
subprocess exited with status 1
Error: building at STEP "RUN if /usr/bin/python${DEFAULT_PYTHON_VERSION} -c "import torch; ..." ...": exit status 1
```

This is a deterministic build defect, not flake or infra: all four parallel variants failed identically, and the fallback branch was taken because the base image's system Python has no torch ≥ 2.7 to satisfy the `import torch` guard.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — NirWolfer, "build: bump CUDA and CI base images, stop restating them across CI (#2205)" — bumped the base image to cuda13.4, breaking the derived index URL.

**File:** `contrib/Dockerfile:336` (the `UV_INDEX` derivation; guard block at lines 333–338)

**Suggested fix:** Stop deriving the index from `CUDA_VERSION`. Introduce an explicit build arg, e.g. `ARG TORCH_CUDA_INDEX="cu130"`, and use `export UV_INDEX="https://download.pytorch.org/whl/${TORCH_CUDA_INDEX}"`, updating it deliberately when PyTorch publishes a channel for a new CUDA major/minor. Also make the failure loud rather than obscure by validating the index (or adding `--index-strategy unsafe-best-match` plus a `torch>=2.7` floor) so a missing channel fails with a clear message instead of an ABI-tag resolution error. PR #2249 appears to already implement exactly this.

**Related:** https://github.com/ai-dynamo/nixl/pull/2249 ("build: pin the torch wheel index instead of deriving it from CUDA_VERSION"); base-image bump PR #2205.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `a600e028-c97b-49ea-bed5-187ac58f0601` in the triage console for the audit trail.

<!-- triage-agent request_id: a600e028-c97b-49ea-bed5-187ac58f0601 -->
<!-- triage-agent gha_run_id: status:SC_kwDOODt2nc8AAAAMnslWIg -->
<!-- triage-agent-bot -->

### svc-nixl · 2026-09-15

🤖 **CI Triage Agent** — `nixl-ci-build-container-pr` · commit `b55bdf7c`

**TL;DR:** All four parallel "Build image" stages fail at the same Dockerfile step — `uv pip install torch` against `https://download.pytorch.org/whl/cu134`, an index that has no cp312 wheels — because the CUDA 13.4 base-image bump makes the Dockerfile derive a non-existent PyTorch index URL. Fix by deriving the index from the CUDA *major* line (e.g. `cu130`) or falling back to PyPI instead of using the exact `CUDA_VERSION`.

<details><summary>Full analysis</summary>

**Summary:** `nixl-ci-build-container-pr` #651 failed in all 4 parallel "Build image" stages at `contrib/Dockerfile` STEP 43/72 (x86) / 30/52 (aarch64): the PyTorch install step exited 1.

**Root cause:** The Dockerfile builds the PyTorch wheel index from the full CUDA version:
`export UV_INDEX="https://download.pytorch.org/whl/cu$(echo $CUDA_VERSION | cut -d. -f1,2 | tr -d .)"`.
With the base image now at `26.08-cuda13.4-devel-ubuntu24.04`, `CUDA_VERSION=13.4` → `cu134`. That index exists but publishes no modern torch — uv reports:

```
error: No solution found when resolving dependencies
  cause: Because all versions of torch have no wheels with a matching Python ABI tag (e.g., `cp312`) ...
hint: `torch` was found on https://download.pytorch.org/whl/cu134, but not at the requested version
hint: You require CPython 3.12 (`cp312`), but we only found wheels for `torch` (v2.0.1) with ... `cp38`, `cp39`, `cp310`, `cp311`
```

The `cuda-dl-base` *devel* image ships no torch, so the `import torch` probe fails and the broken fallback branch is always taken. This is deterministic and environment-wide (both x86_64 and aarch64 stages fail identically), not related to the PR's `single_gtest` changes. No hang or timeout: the log is continuously active and the error is immediate (~1s after the step starts).

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — "build: bump CUDA and CI base images, stop restating them across CI (#2205)", NirWolfer, 2026-09-10 (bumped `BASE_IMAGE_TAG`/`CUOBJ_DEV_IMAGE` to `cuda13.4`); the fragile URL construction itself dates to `8cbff930` (Daniel Pressler, "CI: use system PyTorch when using DLFW base images").

**File:** `contrib/Dockerfile:333-338` (specifically line 336, the `UV_INDEX` construction)

**Suggested fix:** Stop mapping the patch-level CUDA version onto a PyTorch index. Concretely, either:
1. Use the CUDA major line that PyTorch actually publishes — e.g. `CU_TAG="cu$(echo $CUDA_VERSION | cut -d. -f1)0"` (→ `cu130` for CUDA 13.x), optionally with an explicit override `ARG TORCH_INDEX=` so future bumps don't silently break; or
2. Make the install resilient: add `--index-strategy unsafe-best-match` and let PyPI serve as a fallback index, and fail loudly with a clear message if no torch resolves.

Option 1 with an explicit `ARG TORCH_INDEX` (defaulted per base image, pinned alongside `BASE_IMAGE_TAG` like the existing `CUOBJ_DEV_IMAGE` comment already advises) is the lowest-risk change.

**Related:** PR #1869 "Switch CI base image to pytorch + CUDA 13.3" (closed) touched the same base-image/torch coupling; PR #2205 is the bump that introduced CUDA 13.4.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `e1e6121f-aad1-4228-8df3-51249c4f981e` in the triage console for the audit trail.

<!-- triage-agent request_id: e1e6121f-aad1-4228-8df3-51249c4f981e -->
<!-- triage-agent gha_run_id: status:SC_kwDOODt2nc8AAAAMntnbow -->
<!-- triage-agent-bot -->

### ColinNV · 2026-09-17

/build

### svc-nixl · 2026-09-17

🤖 **CI Triage Agent** — `AWS NIXL Validation` · commit `023cb138`

**TL;DR:** The consolidated single-process `./bin/gtest` binary aborts (core dump) in `MpE2ETest.AllRankProcessesAggregateBehindOneEndpointAndStaleAreDropped` because that test calls `fork()` in a process that has already opened Amazon EFA/libfabric endpoints in earlier tests, and the EFA provider deliberately aborts on `fork()` without `FI_EFA_FORK_SAFE=1`. Either run the fork-based test in its own process (gtest_filter split) or export `FI_EFA_FORK_SAFE=1` for the gtest step.

<details><summary>Full analysis</summary>

**Summary:** `.gitlab/test_cpp.sh` line 116 — `./bin/gtest` died with `Aborted (core dumped)` at the start of `MpE2ETest.AllRankProcessesAggregateBehindOneEndpointAndStaleAreDropped`, failing the AWS NIXL Validation job.

**Root cause:** The log shows the abort is emitted by the Libfabric EFA provider, not by a gtest assertion:

```
[ RUN ] MpExporterStandaloneTest.MissingMultiprocDirThrows ... OK
A process has executed an operation involving a call to the fork() system call ...
As a result, the Libfabric EFA provider is operating in a condition that could result in memory corruption ...
please ... Set the environment variable: FI_EFA_FORK_SAFE=1 ...
Your job will now abort.
[ RUN ] MpE2ETest.AllRankProcessesAggregateBehindOneEndpointAndStaleAreDropped
.gitlab/test_cpp.sh: line 116: 203433 Aborted (core dumped) ./bin/gtest ...
```

`MpE2ETest` forks three child "rank" processes (`::fork()` at `test/gtest/telemetry_mp_e2e_test.cpp:139`). Its comment only guards against threading ("fork children while the parent is still single-threaded"), not against registered RDMA memory. Earlier in the *same* process, PID 203433 created real EFA-backed agents — the log repeatedly shows `1 Amazon EFA(s) were detected...` and `HardwareWarningTest.EfaHardwareMismatchNoWarning` explicitly builds `[LIBFABRIC]`, `[UCX, LIBFABRIC]` and `[LIBFABRIC, UCX]` backends. With EFA/ibverbs memory registered and no `FI_EFA_FORK_SAFE=1`, rdma-core/EFA's fork handling aborts the whole process at fork time.

This only manifests now because PR #1743 ("TEST/GTEST: Run in single process") merges the previously separate test binaries into one — `test/gtest/meson.build:100-155` puts `hw_warning_test.cpp`, `metadata_exchange.cpp` and `telemetry_mp_e2e_test.cpp` into a single `gtest` executable (246 tests / 43 suites in one process, as seen in the log). In isolation the fork test never shared a process with a libfabric/EFA agent; now it does.

**Implicated commit:** PR #1743 branch, head `[REDACTED:Hex High Entropy String]` (single-process gtest consolidation; branch merge `4e188ff0`, Colin Hirsch). Not a regression in the telemetry code itself.

**File:** `test/gtest/telemetry_mp_e2e_test.cpp:139` (the `::fork()`), enabled into the shared binary at `test/gtest/meson.build:150`; invoked from `.gitlab/test_cpp.sh:116`.

**Suggested fix:** Keep the forking test out of the EFA-tainted process. Concretely, in `test/gtest/meson.build` / `.gitlab/test_cpp.sh` split the run:

```sh
./bin/gtest --min-tcp-port=... --max-tcp-port=... --gtest_filter=-MpE2ETest.*
./bin/gtest --min-tcp-port=... --max-tcp-port=... --gtest_filter=MpE2ETest.*
```

so the fork happens in a fresh process that never created a LIBFABRIC/UCX agent. Two alternatives, in order of robustness:
- Replace the bare `fork()` with `fork()+execv` of a small writer helper (or `posix_spawn`), which is fork-safe regardless of what the parent registered — this is the durable fix if the tests must stay in one binary.
- As a stopgap, export `FI_EFA_FORK_SAFE=1` for the gtest step (and verify rdma-core ≥ v31.1 in the CI image); note this costs some performance and does not make in-process forking of RDMA state genuinely safe, only non-aborting.

Also worth adding a guard/skip in `MpE2ETest::SetUp()` when an EFA device is present and `FI_EFA_FORK_SAFE` is unset, so this fails as a clean skip rather than a core dump.

**Related:** PR #1743 (this change), PR #2054 (`test: drive prometheus_mp through nixlTelemetry`), PR #1920 (`telemetry: native multi-process Prometheus aggregation (prometheus_mp)` — introduced the fork-based E2E test).

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `924ffaef-f450-4fbe-a55f-b63ee6f0c417` in the triage console for the audit trail.

<!-- triage-agent request_id: 924ffaef-f450-4fbe-a55f-b63ee6f0c417 -->
<!-- triage-agent gha_run_id: 35218371832 -->
<!-- triage-agent-bot -->
