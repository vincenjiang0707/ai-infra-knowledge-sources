# [Issue #18115] [Bug]: V1 MAX_UTILIZATION paused_requests are never processed in the PP executor loop

source: https://github.com/NVIDIA/TensorRT-LLM/issues/18115
state: open | updated: 2026-09-14T07:52:30Z
labels: Inference runtime

## 正文

### System Info

N/A — identified through source inspection of current `main` (while working on #17882 / PR #18113).

### Reproduction

Code inspection of `tensorrt_llm/_torch/pyexecutor/py_executor.py`:

- With `pp_size > 1`, the executor always selects `_executor_loop_pp`.
- `_executor_loop_pp` only handles the V2 recompute-pause flow (`_terminate_recompute_paused_requests` / `_pause_recompute_paused_requests`).
- V1 `scheduled_batch.paused_requests` — produced by the MAX_UTILIZATION capacity scheduler (both the Python `MaxUtilizationPolicy` and the C++ bind scheduler, which run with `two_step_lookahead` under PP) — are neither passed to `_terminate_requests` nor to `_pause_requests` anywhere in that loop. The single-rank loops (`_executor_loop`, `_executor_loop_overlap`) do both.

### Expected behavior

When the V1 MAX_UTILIZATION scheduler decides to pause a started request under pipeline parallelism, the request's resources should be freed and the request reset for replay (`req.pause(...)`), as in the non-PP loops.

### Actual behavior

Under PP, a "paused" request is simply not scheduled that iteration: its KV/resources are never released and its state is never reset, so the preemption the scheduler planned for never materializes. Under sustained memory pressure this can leave the scheduler unable to make room (the same requests get re-paused every iteration while still holding their blocks).

### Additional notes

Found while fixing #17882: PR #18113 preserves multimodal payloads for pause replay and threads a `for_pause` flag through the (PP-)termination path, but the PP loop currently has no V1 pause flow to call it from. Wiring `paused_requests` handling into `_executor_loop_pp` touches PP microbatch/inflight-id semantics for all request types, so it is kept out of that PR and tracked here instead.

## 评论 (3)

### Klein-HIT · 2026-08-27

I’d like to work on this. I’ll trace the PP microbatch/inflight timing, wire the V1 `scheduled_batch.paused_requests` path into `_executor_loop_pp`, and add a CPU mock regression for the pause lifecycle. I’ll keep the change independent of #18113 and coordinate its `for_pause=True` dependency / rebase if that PR lands first.

### Lipenghee · 2026-08-27

Could a TensorRT-LLM engineer confirm whether a fix scoped to #18115 is approved for a PR, as requested by CONTRIBUTING?

The local candidate drains outstanding PP microbatches before releasing V1 pause victims and keeps retirement/termination progress running while draining. On non-first ranks, it runs local scheduling before resource release, then retries capacity. It resets paused requests for replay after teardown, including the native decoding iteration used by variable-width beam search. The shared decoding-iteration reset also affects V2 replay; V2 scheduling and its unbounded pause budget are unchanged.

This stays separate from #18113's multimodal-payload retention change. If #18113 lands first, the pause teardown will need rebasing onto its `for_pause` interface.

Using source-built native bindings and the upstream CPU test mechanism, the same 560 regression nodes produced 208 failures before the fix and 560 passes afterward. The full CPU-marked executor suite also passes (1477 tests). These are CPU lifecycle tests with device/event/transport test doubles where needed, not multi-GPU PP inference or performance validation.

A supplemental, unmarked `test_bindings_ut.py::test_llm_request` remains blocked on this CPU-only host: its tensor getter calls `cudaPointerGetAttributes` and aborts with no CUDA device, before reaching `pause`. I am not counting this as passing, and have not changed or skipped the test.

No code has been pushed and no PR opened. The implementation, tests and automated review were assisted by OpenAI Codex. Please let me know if a different pause/drain policy or additional hardware validation is required before proceeding.


### Lipenghee · 2026-09-14

@eopXD, could you please confirm whether #18115 is approved for a PR? The local Codex-assisted candidate is limited to the V1 PP pause-victim lifecycle (drain outstanding PP work, free/reset the paused request, then replay) and remains independent of #18113. On source-built native bindings, the focused CPU regression is 186/186, the full executor suite is 1314/1314, related tests are 711 passed with 2 expected Mooncake skips, sampler is 11/11, and pre-commit is clean. No code has been pushed. If approved, I will rebase onto current main and submit the regression with a truthful AI-assistance disclosure.
