# [PR #4946] Migrate TurboMind to C++20

source: https://github.com/InternLM/lmdeploy/pull/4946
state: closed | updated: 2026-09-08T16:34:02Z
labels: improvement

## 正文

## Motivation

Consolidate TurboMind’s mixed language-standard settings so all owned host C++ and CUDA targets consistently compile as C++20, including Python bindings and communication wrappers.

## Modification

- Centralize C++20 requirements in CMake, remove conflicting overrides.
- Add `const std::string&` and `std::string&&` logger overloads for preformatted messages.
- Match the attention registry’s cost tuple types for C++20 comparisons.
- Apply `/Zc:twoPhase-` to NVCC’s MSVC host pass for CuTe compatibility.
- Fix existing test portability: link `test_quant` with `${CMAKE_DL_LIBS}` and use aligned allocation/deallocation supported by MSVC.
- Update build requirements and migration documentation.

## BC-breaking

TurboMind now requires CMake 3.25.2+, CUDA Toolkit 12.0+, and a CUDA-supported C++20 host compiler, such as GCC 10+ or Visual Studio 2022. The `CXX_STD` configuration option is removed.

## Validation

- Full Linux builds passed with CUDA 12.8 and 13.0, including Python bindings, communication targets, and existing tests.
- Native Windows compilation/linking passed with CUDA 12.8.1 and MSVC 19.38.
- Compile-command audits confirmed C++20 without conflicting standard flags.
- Logger tests: 12 cases / 115 assertions passed.
- Targeted memory tests: 12 cases / 671 assertions passed.
- Single-GPU `Qwen/Qwen3-8B` smoke generated 256 coherent, relevant tokens; generation reached the token limit during thinking.

Windows GPU runtime remains unvalidated because no Windows NVIDIA GPU host is available.

## Checklist

- [x] Formatting checks passed.
- [x] Existing tests exercised the affected logger and memory code.
- [x] Linux and Windows build configurations validated.
- [x] Documentation updated.

## 评论 (0)

## Review (3)

### copilot-pull-request-reviewer[bot] · 2026-09-08 · COMMENTED

### 🟡 Changes recommended

`src/turbomind/memory/test_memory.cc` introduces a C++ compilation issue (`std::align_val_t` initialization) and should use the matching nothrow aligned deallocation overload for correctness.

*Once you've addressed the issues Copilot identified, you can request another Copilot review.*

<details>
<summary>Pull request overview</summary>

This PR migrates TurboMind’s owned host C++ and CUDA code (including Python bindings and comm wrappers) to a consistent C++20 build, centralizing language-standard requirements in top-level CMake and removing per-target overrides to avoid conflicting flags.

**Changes:**
- Centralize C++20 (CXX/CUDA) requirements in root `CMakeLists.txt`, remove legacy `CXX_STD` and per-target standard properties, and add an MSVC/NVCC host-pass workaround (`/Zc:twoPhase-`).
- Extend TurboMind’s logger API with overloads that accept preformatted `std::string` messages (lvalue/rvalue, with/without source location).
- Fix/adjust portability and build details (attention registry tuple type alignment, `test_quant` link libs, build requirements, and installation docs).
</details>

<details>
<summary>File summaries</summary>

| File | Description |
| ---- | ----------- |
| CMakeLists.txt | Enforces CUDA>=12.0 and sets global C++20/CUDA20 standards; removes legacy standard configuration. |
| src/turbomind/python/CMakeLists.txt | Removes `_xgrammar` C++14 override so it follows the centralized C++20 configuration. |
| src/turbomind/comm/nccl/deepep/CMakeLists.txt | Removes per-target standard properties to avoid conflicting language-standard settings. |
| src/turbomind/core/logger.h | Adds `Log` overloads for preformatted `std::string` messages (copy/move) with optional source location. |
| src/turbomind/kernels/attention/registry.cu | Aligns `cost` tuple type to support C++20 tuple comparisons consistently. |
| src/turbomind/kernels/attention/CMakeLists.txt | Links `test_quant` against `${CMAKE_DL_LIBS}` for portability. |
| src/turbomind/memory/test_memory.cc | Switches aligned allocation to aligned `operator new/delete` for MSVC portability. |
| requirements/build.txt | Updates build requirement to `pybind11>=2.13.6`. |
| docs/en/get_started/installation.md | Updates source-build requirements to reflect C++20/CMake/CUDA minimums. |
| docs/zh_cn/get_started/installation.md | Same as English docs: updates source-build requirements for C++20/CMake/CUDA. |
| builder/windows/README.md | Updates Windows build requirements and documents the `/Zc:twoPhase-` workaround. |
</details>

<details>
<summary>Review details</summary>

### Suppressed comments (1)

**src/turbomind/memory/test_memory.cc:31**
* The region is allocated with the nothrow aligned `::operator new(size_t, std::align_val_t, const std::nothrow_t&)`, but freed with `::operator delete(void*, std::align_val_t)`. To ensure the matching deallocation function is used (and avoid UB on implementations where these differ), call the corresponding nothrow aligned delete overload.
```
        ::operator delete(ptr_, alignment_);
```

- **Files reviewed:** 11/11 changed files
- **Comments generated:** 1
- **Review effort level:** Lite
</details>

---

💡 <a href="/InternLM/lmdeploy/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

### irexyc · 2026-09-08 · APPROVED

(no text)

### lvhan028 · 2026-09-08 · APPROVED

(no text)
