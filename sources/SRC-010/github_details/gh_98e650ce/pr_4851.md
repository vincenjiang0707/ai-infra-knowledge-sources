# [PR #4851] fix(serve): emit signatures for Anthropic thinking blocks

source: https://github.com/InternLM/lmdeploy/pull/4851
state: closed | updated: 2026-09-11T03:17:57Z
labels: Bug:P1

## 正文

## Motivation

Anthropic-compatible responses currently omit the `signature` field from thinking blocks.

Clients may replay assistant thinking blocks in subsequent requests and expect each block to contain a signature. For streaming responses, the Anthropic protocol delivers the signature through a `signature_delta` event immediately before `content_block_stop`.

This PR adds a local opaque signature marker so that LMDeploy-generated thinking blocks can be replayed by Anthropic-compatible clients.

## Modification

- Add `signature` to non-streaming thinking blocks.
- Emit one `signature_delta` before closing each streaming thinking block.
- Keep `content_block_start` unchanged and only emit signatures for thinking blocks.
- Use the existing block-closing path so the event ordering is consistent when thinking is followed by text, tool use, or the end of generation.
- Add unit tests covering:
  - non-streaming thinking signatures;
  - streaming event ordering;
  - signed thinking block conversion in subsequent requests.

The streaming sequence is:

```text
content_block_start
thinking_delta
signature_delta
content_block_stop

## 评论 (0)

## Review (2)

### copilot-pull-request-reviewer[bot] · 2026-08-13 · COMMENTED

## Pull request overview

This PR updates LMDeploy’s Anthropic-compatible `/v1/messages` implementation to include a replayable `signature` for thinking blocks, matching Anthropic’s expectation that thinking blocks carry signatures (including via `signature_delta` in streaming).

**Changes:**
- Add a default `signature` field to non-streaming `thinking` blocks in the response schema.
- Emit a `signature_delta` event immediately before `content_block_stop` for each streaming thinking block.
- Expand unit tests to validate non-stream signatures, streaming event ordering, and signed thinking-block replay during adapter conversion.

### Reviewed changes

Copilot reviewed 4 out of 4 changed files in this pull request and generated no comments.

| File | Description |
| ---- | ----------- |
| tests/test_lmdeploy/serve/anthropic/test_endpoints.py | Updates endpoint tests to assert thinking block signatures in non-streaming responses and correct streaming `signature_delta` ordering. |
| tests/test_lmdeploy/serve/anthropic/test_adapter_conversion.py | Ensures assistant thinking blocks that include `signature` can be converted for OpenAI-compatible downstream usage. |
| lmdeploy/serve/anthropic/streaming.py | Adds `signature_delta` emission on thinking block close while preserving existing block start behavior and ordering. |
| lmdeploy/serve/anthropic/protocol.py | Introduces `LOCAL_THINKING_SIGNATURE` and adds `signature` to `MessageThinkingBlock` plus a `SignatureDelta` stream delta type. |







---

💡 <a href="/InternLM/lmdeploy/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

### lvhan028 · 2026-08-13 · APPROVED

(no text)
