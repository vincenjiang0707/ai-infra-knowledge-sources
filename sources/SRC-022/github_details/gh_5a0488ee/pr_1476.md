# [PR #1476] Adds API for non-caching-store and load

source: https://github.com/ROCm/rccl/pull/1476
state: closed | updated: 2026-01-22T01:47:32Z
labels: noCI

## 正文

RCCL provides "low latency" protocols for communication between agents, where the entire message consisting of data and flags is packed into a single L2 cache line. This is usually accomplished using atomic relaxed instructions in LLVM. But the 128-byte version of this protocol (LL-128) requires 128-bit load or store instructions that bypass the cache and are not broken up into multiple instructions. The nontemporal builtin is not always suitable for this use case.

The proposed approach is to provide a C++ function template that encapsulates an inline assembly call. This asm is intended to use the appropriate load/store parameters for each combination of data size and architecture.

## 评论 (7)

### ranapratap55 · 2025-01-02

created a new PR with both load and store. Added test cases for byte, 2-byte, 4, 8, 16.

closed the old pr #1289

### wenkaidu · 2025-01-02

@ranapratap55 can you clarify if the non-caching load and store can work over coarse grained device memory, which is the memory type used in your tests?

### ranapratap55 · 2025-01-08

> @ranapratap55 can you clarify if the non-caching load and store can work over coarse grained device memory, which is the memory type used in your tests?

yes, it works on coarse-grained memory. I have used the hipMalloc() which allocates coarse-grained memory. 

### wenkaidu · 2025-01-08

> > @ranapratap55 can you clarify if the non-caching load and store can work over coarse grained device memory, which is the memory type used in your tests?
> 
> yes, it works on coarse-grained memory. I have used the hipMalloc() which allocates coarse-grained memory.

Can you port and test https://github.com/ROCm/rccl/tree/develop/tools/p2p-latency-test to use the new API, and add the commit to this PR? This is more accurate testing for RCCL use cases. Please try changing memory from "uncached" to coarse-grained memory in these tests.

### ranapratap55 · 2025-02-03

```bash
> > > @ranapratap55 can you clarify if the non-caching load and store can work over coarse grained device memory, which is the memory type used in your tests?
> > 
> > 
> > yes, it works on coarse-grained memory. I have used the hipMalloc() which allocates coarse-grained memory.
> 
> Can you port and test https://github.com/ROCm/rccl/tree/develop/tools/p2p-latency-test to use the new API, and add the commit to this PR? This is more accurate testing for RCCL use cases. Please try changing memory from "uncached" to coarse-grained memory in these tests.
```

the new __non_caching_load template that we provided takes input arguments as 8, 16, 32, 64, and 128byte pointer. It will generate global_load_b128 asm when we provide 128b input.

But https://github.com/ROCm/rccl/tree/develop/tools/p2p-latency-test is using two 64b __builtin_nontemporal_loads() as below

[    i4.v[0] = __builtin_nontemporal_load(src->v);
    i4.v[1] = __builtin_nontemporal_load(src->v+1);](https://github.com/ROCm/rccl/blob/dc75209dd7ad35379f7738417db8a31261c1a281/tools/p2p-latency-test/ll_latency_test.cpp#L68C1-L69C52) 
    
  and converting them to one global_load_b128 which I am unaware of how to port using new template.

### wenkaidu · 2025-02-18

@ranapratap55 The test code you added appears to be used in kernel launches. CP firmware will flush/invalidate caches around kernel boundary which defeat the purpose of non-caching load/store. You will need to use persistent kernel to test this correctly, such as https://github.com/ROCm/rccl/tree/develop/tools/p2p-latency-test. In these tests, both load and store are issues from persistent kernels which do not launch and exit during the test.

### ammallya · 2026-01-22

Imported to ROCm/rocm-systems

## Review (1)

### mustafabar · 2025-01-15 · COMMENTED

(no text)
