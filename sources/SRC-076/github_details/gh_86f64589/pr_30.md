# [PR #30] Fuser

source: https://github.com/meta-pytorch/KernelAgent/pull/30
state: closed | updated: 2026-04-08T23:23:42Z
labels: CLA Signed

## 正文

(empty)

## 评论 (1)

### Jack-Khuu · 2025-11-02

Stamp to unblock (can land as-is); All comments/concerns can be addressed in post/parallel

---

For my understanding, on initial parse the major entry points are:
- Application UI: `fuser_ui`, `pipeline_ui`
- Main Entrypoint: `auto_agent`

Then the secondary entry points that do a subset of `auto_agent`:
- Full Pipeline w/o autoroute: `pipeline` 
- Pipeline processes: `subgraph_extractor`, `dispatch_kernel_agent`, `compose_end_to_end`
 -Engine: `orchestrator` (via `cli.py`, see code comment)

Does that sound right?

---

Highlevel thoughts:
 - Let’s move the UI (verbatim) up out of the Fuser path, it’s an Application. 
 - Is `auto_agent`, a core component or an application? To me it feels like an application. If you agree, let’s also move this out of Fuser
 - I like how you organized the sub-components into distinct files. Do we need to keep a CLI in each or should we keep the files succinct (maybe move the cli to scripts)? If it’s for testing, we can replace them with unit tests
 - We should take a look at some low hanging fruit with file grouping, Fuser is relatively stratifiable.
- I’m not terribly concerned about the mix of relative and absolute imports. Some package cleanliness will clear those right up


## Review (3)

### Jack-Khuu · 2025-11-02 · APPROVED

(no text)

### kaiming-cheng · 2025-11-03 · APPROVED

stamp to unblock landing

### sijiac · 2025-11-03 · APPROVED

(no text)
