# [PR #5007] [RayJob] Deprecate DELETE_RAYJOB_CR_AFTER_JOB_FINISHES env var and remove unused Configuration field

source: https://github.com/ray-project/kuberay/pull/5007
state: open | updated: 2026-09-24T16:40:39Z
labels: lgtm

## 正文

## Why are these changes needed?

`RayJobDeletionPolicy` reached beta in v1.6.0, so the older `DELETE_RAYJOB_CR_AFTER_JOB_FINISHES` env var is now redundant. This PR:

1. **Removes the unused `DeleteRayJobAfterJobFinishes` field** from the `Configuration` struct — it was assigned from the env var in `main.go` but never read (the RayJob controller reads the env var directly), so it was dead code.
2. **Deprecates the `DELETE_RAYJOB_CR_AFTER_JOB_FINISHES` env var** by logging a warning when it's used, pointing users to the `RayJobDeletionPolicy` API. The env var still works — it's only deprecated, not removed.

Notes for reviewers:
- **Warning placement:** I log the deprecation at the point of use in the RayJob controller, so it fires only when the env var is actually set to `true`. Happy to move it to a one-time warning at operator startup if you'd prefer.
- **Tests:** the existing `DELETE_RAYJOB_CR_AFTER_JOB_FINISHES` tests cover behavior that is **retained** (the env var still deletes the RayJob CR), so they're kept. The removed `Configuration` field had no dedicated test.

## Related issue number

Closes #4415

## Checks

- [ ] I've made sure the tests are passing.
- Testing Strategy
   - [x] Unit tests
   - [ ] Manual tests
   - [ ] This PR is not tested :(

## 评论 (4)

### omkar619-dev · 2026-08-06

@win5923 — following up on #4415  , this is the PR for it. All 32 checks are green and it merges cleanly.

One open call from the description: I log the deprecation warning at point of use in the RayJob controller (fires only when the env var is actually set), rather than once at operator startup. Happy either way — Let me know and I'll push the change. Otherwise I think this is ready for a look.

### omkar619-dev · 2026-08-20

Thanks @win5923, no worries at all. Added the deprecation note at values.yaml:271 — it points users to spec.deletionStrategy with a deletionRules / DeleteSelf entry, since that's the direct equivalent of deleting the RayJob CR (onSuccess/onFailure being deprecated themselves). Also rebased onto master. Let me know if you'd like the wording tightened.

### omkar619-dev · 2026-08-20

Applied, thanks @win5923  — DeletionStrategy is the right name, and it now matches the values.yaml note.

One thing on CI: the Build historyserver failure is TestShutdown_DrainsRetriedTasks, which is the known flake , This PR doesn't touch historyserver/ — pushing this suggestion could re-run it.

### omkar619-dev · 2026-09-24

@rueian @andrewsykim this has been approved since 20 Aug - would one of you be able to merge it?

The single red check is test-rayservice-incremental-upgrade-e2e-nightly-operator. This PR is +3/-4 and touches the RayJob controller, a dead Configuration field and a values.yaml comment, so there is no path from it into the RayService upgrade flow, and test-rayjob-e2e-nightly-operator - the job that does cover this change - passed. That job is also green on master right now, so I read it as environmental rather than a regression; it is the same one that flapped across pushes on https://github.com/ray-project/kuberay/pull/5181. Happy to rebase if a fresh run would help.

## Review (3)

### win5923 · 2026-08-20 · COMMENTED

Hi @omkar619-dev, sorry for lately review, could you add a comment indicating the env is DEPRECATED in values.yaml?

https://github.com/ray-project/kuberay/blob/feeaf72facab4881ba2b179248bb67315634e229/helm-chart/kuberay-operator/values.yaml#L271-L273

### win5923 · 2026-08-20 · APPROVED

Thank! cc @andrewsykim and @rueian to merge

### win5923 · 2026-08-20 · COMMENTED

(no text)
