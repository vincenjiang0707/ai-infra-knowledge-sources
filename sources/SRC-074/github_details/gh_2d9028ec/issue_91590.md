# [Issue #91590] Crashes of linalg.ldl_solve on different edge cases not coming from linalg.ldl_factor

source: https://github.com/pytorch/pytorch/issues/91590
state: closed | updated: 2026-09-25T02:20:04Z
labels: module: crash, triaged, module: linear algebra, module: edge cases

## 正文

## Double free on negative indices on CPU

```python
import torch
import numpy as np
arg_1_tensor = torch.neg(torch.rand([4, 5, 5], dtype=torch.float32))
arg_1 = arg_1_tensor.clone()
arg_2_tensor = torch.randint(-2048,1,[4, 5], dtype=torch.int32)
arg_2 = arg_2_tensor.clone()
arg_3_tensor = torch.rand([4, 5, 1], dtype=torch.float32)
arg_3 = arg_3_tensor.clone()
arg_4 = False
try:
  res = torch.linalg.ldl_solve(arg_1,arg_2,arg_3,hermitian=arg_4,)
except Exception as e:
  print("Error:"+str(e))
```

## Segfault on neg + CPU

```python
import torch
import numpy as np
arg_1_tensor = torch.neg(torch.rand([2, 2, 5, 5], dtype=torch.complex64))
arg_1 = arg_1_tensor.clone()
arg_2_tensor = torch.randint(-128,512,[2, 2, 5], dtype=torch.int32)
arg_2 = arg_2_tensor.clone()
arg_3_tensor = torch.rand([2, 2, 5, 1], dtype=torch.complex64)
arg_3 = arg_3_tensor.clone()
arg_4 = True
try:
  res = torch.linalg.ldl_solve(arg_1,arg_2,arg_3,hermitian=arg_4,)
except Exception as e:
  print("Error:"+str(e))
```

## 评论 (3)

### lezcano · 2023-01-03

the inputs to `linalg.ldl_solve` should come from `linalg.ldl_factor`. If you pass any other input to it you risk getting crashes and otherwise.
If you don't want to risk it and you want to use a safer API, I'd very much recommend you to simply use `linalg.solve`, whose derivative is much faster than that of this function.

### dmc1778 · 2023-01-03

> the inputs to `linalg.ldl_solve` should come from `linalg.ldl_factor`. If you pass any other input to it you risk getting crashes and otherwise. If you don't want to risk it and you want to use a safer API, I'd very much recommend you to simply use `linalg.solve`, whose derivative is much faster than that of this function.

Thanks. I am running a fuzzer on torch and the fuzzer find this bug. 

### malfet · 2026-09-25

Cannot reproduce the crash on the latest PyTorch release (torch **2.13.0**, CPU, macOS). I ran both reproducers in the report as written; each now fails validation cleanly instead of crashing (double free / segfault):

```
RuntimeError: Pivots given to ldl_solve must all satisfy |pivot| <= LD.size(-2). Did you properly pass the result of ldl_factor?
```

Closing as fixed; please reopen if you can still reproduce a crash on a current build.
