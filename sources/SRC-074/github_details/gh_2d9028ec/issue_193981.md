# [Issue #193981] assign the data in a torch.nn.parameter with different shape and different nelement doesn't work when backward()

source: https://github.com/pytorch/pytorch/issues/193981
state: closed | updated: 2026-09-24T16:00:43Z
labels: bot-triaged

## 正文

### 🐛 Describe the bug

https://github.com/YagaoDirac/Pytorch-extension-from-YagaoDirac-v2/blob/main/tutorial_for_beginners/a%20reshape%20bug.py

All info is in the code.
2.12

I plan to refresh the entire module object to do this reshape, since no deep copy will happen. The performance should be ok. Anyway, if you need any updating, let me know. Im also on x.com.
Good night from China.

### Versions

It shouldnt have anything with env. Or does it?

## 评论 (3)

### github-actions[bot] · 2026-08-18

> Thanks for the report! The reproduction is only available via an external link (a script hosted in another GitHub repo), which makes this harder for maintainers to triage quickly.
>
> Could you paste a minimal, self-contained repro directly in this issue (a short code block), along with the exact error/traceback you see when calling `backward()`? Also, `python -m torch.utils.collect_env` output would help confirm the version/environment.
>
> This will let us classify and route the issue properly.

### YagaoDirac · 2026-08-18

I tested it a bit more. It feels like some shape info inside the backend doesn't update when I assign the new torch.Tensor to the parameter member in the Module object. 
In the new code, I create a new object with the same data. The backend sees a new object and probably stores the new info. Probably it's when the new torch.nn.Parameter is created. New method works properly.

### janeyx99 · 2026-09-24

Setting the .data is not recommended API. `copy_` is the more sane API that is supported. Closing this issue as not planned.
