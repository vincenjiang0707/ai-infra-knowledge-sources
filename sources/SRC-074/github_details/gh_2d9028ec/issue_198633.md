# [Issue #198633] DISABLED test_sep (__main__.FilterTestCase)

source: https://github.com/pytorch/pytorch/issues/198633
state: open | updated: 2026-09-25T12:53:49Z
labels: module: flaky-tests, skipped, oncall: pt2, module: dynamo

## 正文

Platforms: dynamo

  This test was disabled because it is failing in CI. See [recent examples](https://hud.pytorch.org/flakytest?name=test_sep&suite=FilterTestCase&limit=100) and the most recent trunk [workflow logs](https://github.com/pytorch/pytorch/runs/107991765584).

  Over the past 6 hours, it has been determined flaky in 11 workflow(s) with 22 failures and 11 successes.

  **Debugging instructions (after clicking on the recent samples link):**
  DO NOT ASSUME THINGS ARE OKAY IF THE CI IS GREEN. We now shield flaky tests from developers so CI will thus be green but it will be harder to parse the logs.
  To find relevant log snippets:
  1. Click on the workflow logs linked above
  2. Click on the Test step of the job so that it is expanded. Otherwise, the grepping will not work.
  3. Grep for `test_sep`
  4. There should be several instances run (as flaky tests are rerun in CI) from which you can study the logs.
  
  
  Test file path: `cpython/v3_13/test_fnmatch.py`

  For all disabled tests (by GitHub issue), see https://hud.pytorch.org/disabled.

cc @chauhang @penguinwu @voznesenskym @EikanWang @jgong5 @Guobing-Chen @XiaobingSuper @zhuhaozhe @blzheng @wenzhe-nrv @jiayisunx @kadeng @amjames @jataylo @azahed98

## 评论 (1)

### pytorch-bot[bot] · 2026-09-25

<!-- validation-comment-start --><body>Hello there! From the DISABLED prefix in this issue title, it looks like you are attempting to disable a test in PyTorch CI. The information I have parsed is below:

* Test name: `test_sep (__main__.FilterTestCase)`
* Platforms for which to skip the test: dynamo
* Disabled by `pytorch-bot[bot]`

Within ~15 minutes, `test_sep (__main__.FilterTestCase)` will be disabled in PyTorch CI for these platforms: dynamo. Please verify that your test name looks correct, e.g., `test_cuda_assert_async (__main__.TestCuda)`.

To modify the platforms list, please include a line in the issue body, like below. The default action will disable the test for all platforms if no platforms list is specified. 
```
Platforms: case-insensitive, list, of, platforms
```
We currently support the following platforms: asan, dynamo, inductor, linux, mac, macos, rocm, slow, win, windows, xpu.


### How to re-enable a test
To re-enable the test globally, close the issue. To re-enable a test for only a subset of platforms, remove the platforms from the list in the issue body. This may take some time to propagate. To re-enable a test only for a PR, put `Fixes #198633` in the PR body and rerun the test jobs. Note that if a test is flaky, it maybe be difficult to tell if the test is still flaky on the PR.
</body><!-- validation-comment-end -->
