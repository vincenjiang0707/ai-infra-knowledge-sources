{"number": 1351, "title": "[BUG] illegal memory access when using topk_selector", "state": "open", "created_at": "2025-11-27T05:58:23Z", "updated_at": "2026-09-25T07:28:13Z", "closed_at": null, "labels": ["bug"], "comments_count": 5, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.6.post2+cuda.gitf0c721a4\n\n### System information\n\n3.12.3 (main, Aug 14 2025, 17:47:21) [GCC 13.3.0] linux\n0.1.6.post2+cuda.gitf0c721a4\n2.9.1+cu128\n\n### Problem description\n\nWhen I use `tl_topk()`, if the input data values ​​are close (for example, only the last 10 bits are different), an illegal memory access will occur.\n\n### Reproducible example code\n\nPlease using `examples/deepseek_v32/topk_selector.py`. And add the following code.\n\nThe Python snippets:\n\n```python\n\n# Returns float values that differ in the last 10 bits\ndef get_10bit_data(bs: int, seq_len: int) -> torch.Tensor:\n    torch.manual_seed(42)\n\n    top_22_bits_mask = 0xFFFFFC00\n    last_10_bits_mask = 0x000003FF\n    fixed_top_22_bits = 0x3F900000\n\n    # Generate random bits for the last 10 bits\n    random_bottom_bits = torch.randint(\n        0, 2**10, (bs, seq_len), dtype=torch.int32, device=\"cuda\"\n    )\n\n    # Combine: fixed top 22 bits with random last 10 bits\n    score_bits = (fixed_top_22_bits & top_22_bits_mask) | (\n        random_bottom_bits & last_10_bits_mask\n    )\n\n  # Convert back to float\n  score = score_bits.view(torch.float32)\n  return score\n\n\nbatch = 2\nseq_len = 32 * 1024\ntopk = 2048\ninput = get_10bit_data(batch, seq_len).cuda()\nstarts = torch.zeros(batch, dtype=torch.int32).cuda()\nends = torch.ones(batch, dtype=torch.int32).cuda() * seq_len\n\nindexes_ref = torch.topk(input, topk, dim=-1)[1] \nprint(f\"indexes_ref shape: {indexes_ref.shape}, indexes_ref: {indexes_ref}\") # good\n\nindexes = tl_topk(input, starts, ends, topk) \nprint(f\"indexes shape: {indexes.shape}, indexes: {indexes}\") # illegal memory access\n\n```\n\n\n\n\n### Traceback\n\n```pytb\nTraceback (most recent call last):\n  File \"/workspace_new/tilelang/examples/deepseek_v32/topk_selector.py\", line 284, in <module>\n    test_topk_selector()\n  File \"/workspace_new/tilelang/examples/deepseek_v32/topk_selector.py\", line 235, in test_topk_selector\n    print(f\"indexes shape: {indexes.shape}, indexes: {indexes}\")\n                                                     ^^^^^^^^^\n  File \"/workspace_new/tilelang/venv/lib/python3.12/site-packages/torch/_tensor.py\", line 1109, in __format__\n    return object.__format__(self, format_spec)\n           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n  File \"/workspace_new/tilelang/venv/lib/python3.12/site-packages/torch/_tensor.py\", line 568, in __repr__\n    return torch._tensor_str._str(self, tensor_contents=tensor_contents)\n           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n  File \"/workspace_new/tilelang/venv/lib/python3.12/site-packages/torch/_tensor_str.py\", line 722, in _str\n    return _str_intern(self, tensor_contents=tensor_contents)\n           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n  File \"/workspace_new/tilelang/venv/lib/python3.12/site-packages/torch/_tensor_str.py\", line 643, in _str_intern\n    tensor_str = _tensor_str(self, indent)\n                 ^^^^^^^^^^^^^^^^^^^^^^^^^\n  File \"/workspace_new/tilelang/venv/lib/python3.12/site-packages/torch/_tensor_str.py\", line 375, in _tensor_str\n    formatter = _Formatter(get_summarized_data(self) if summarize else self)\n                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n  File \"/workspace_new/tilelang/venv/lib/python3.12/site-packages/torch/_tensor_str.py\", line 142, in __init__\n    value_str = f\"{value}\"\n                  ^^^^^^^\n  File \"/workspace_new/tilelang/venv/lib/python3.12/site-packages/torch/_tensor.py\", line 1108, in __format__\n    return self.detach().item().__format__(format_spec)\n           ^^^^^^^^^^^^^^^^^^^^\ntorch.AcceleratorError: CUDA error: an illegal memory access was encountered\nSearch for `cudaErrorIllegalAddress' in https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__TYPES.html for more information.\nCUDA kernel errors might be asynchronously reported at some other API call, so the stacktrace below might be incorrect.\nFor debugging consider passing CUDA_LAUNCH_BLOCKING=1\nCompile with `TORCH_USE_CUDA_DSA` to enable device-side assertions.\n```\n\n### Expected behavior\n\n_No response_\n\n### Additional context\n\nPlease also pay attention to accuracy issues after the repair. Thank you.", "url": "https://github.com/tile-ai/tilelang/issues/1351"}
{"number": 3270, "title": "[BUG] reduce_max/min/absmax `nan_propagate=True` is silently ignored for float32/float64", "state": "closed", "created_at": "2026-09-23T06:15:56Z", "updated_at": "2026-09-24T09:03:45Z", "closed_at": "2026-09-24T09:03:45Z", "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n- [x] I have tried the latest version of TileLang.\n\n### What version of TileLang are you using?\n\nmain (227515d2)\n\n### System information\n\nLinux, CUDA release 13.1, PyTorch 2.11.0+cu130, NVIDIA H800 (sm_90). The behaviour is decided at lowering time by dtype, not by hardware.\n\n### Problem description\n\n`tilelang.cuda.language.reduce_max/reduce_min/reduce_absmax` accept a `nan_propagate` keyword (added in #2788 and the PR before it). For `float16`/`bfloat16` it lowers the reduction to `__hmax_nan`/`__hmin_nan` and NaNs propagate as documented. For `float32` and `float64` the flag is **silently dropped**: the lowering is identical to `nan_propagate=False`, the reduction still uses `fmaxf`/`fminf` (IEEE `maxNum`, returns the non-NaN operand), and the NaN is swallowed. There is no warning and no error — the user asked for NaN propagation, got `True` accepted, and received a finite result.\n\nWhere this happens:\n\n- `src/backend/common/op/reduce.h:276` (and `:327`, `:622`): `use_nan_op = op.nan_propagate && (dtype.is_float16() || dtype.is_bfloat16())` — every other dtype falls through to the plain op.\n- `src/tl_templates/cuda/reduce.h`: `MaxOpNan`/`MinOpNan` only specialise `half_t`/`bfloat16_t`; the generic overload is `tl::fast_max`, and `fast_max<float>` is `fmaxf` (`src/tl_templates/cuda/common.h:1334`), so even the \"NaN\" functor does not propagate for fp32.\n- The docstring in `tilelang/cuda/language/reduce_op.py` says the knob \"is meaningful for float16/bfloat16 only\", and the `ReduceOpNode` field comment says the same — but that is a documented silent no-op, and fp32 is the most common accumulator dtype for exactly the reductions (softmax `m_i`, absmax scaling) where NaN handling matters.\n\nMeasured on main with a 128-element row containing one NaN:\n\n| dtype | `nan_propagate=False` | `nan_propagate=True` | `torch.max` |\n|---|---|---|---|\n| float16 | 127.0 | **nan** | nan |\n| bfloat16 | 127.0 | **nan** | nan |\n| float32 | 127.0 | 127.0 | nan |\n| float64 | 127.0 | 127.0 | nan |\n\n### Reproducible example code\n\n```python\nimport torch, tilelang, tilelang.language as T\nfrom tilelang.cuda.language import reduce_max\n\ndef kernel(dtype, nan_propagate):\n    @T.prim_func\n    def main(A: T.Tensor((1, 128), dtype), B: T.Tensor((1,), dtype)):\n        with T.Kernel(1, threads=128):\n            A_local = T.alloc_fragment((1, 128), dtype)\n            B_local = T.alloc_fragment((1,), dtype)\n            T.copy(A, A_local)\n            reduce_max(A_local, B_local, dim=1, nan_propagate=nan_propagate)\n            T.copy(B_local, B)\n    return main\n\nfor dtype in (\"float16\", \"float32\"):\n    k = tilelang.compile(kernel(dtype, nan_propagate=True))\n    a = torch.arange(128, device=\"cuda\", dtype=torch.float32); a[5] = float(\"nan\")\n    a = a.to(getattr(torch, dtype)).reshape(1, 128)\n    b = torch.empty(1, device=\"cuda\", dtype=a.dtype)\n    k(a, b)\n    print(dtype, \"nan_propagate=True ->\", b.float().item())   # float16 -> nan, float32 -> 127.0\n```\n\n### Traceback\n\nNo traceback; the kernel compiles and runs, returning `127.0` for `float32`.\n\n### Expected behavior\n\nEither of these would be acceptable; the current silent acceptance is not:\n\n1. **Honour the flag for fp32/fp64.** CUDA has no `fmaxf` variant that propagates NaN, but PTX has `max.NaN.f32` / `min.NaN.f32` (sm_80+), and an `isnan`-guarded select works everywhere else. That would give `MaxOpNan`/`MinOpNan` real `float`/`double` overloads.\n2. **Reject it loudly.** If fp32/fp64 support is out of scope, `ReduceOpNode` should raise for `nan_propagate=True` on a dtype it cannot honour, the way it already raises for non-CUDA targets (`src/backend/common/op/reduce.h:754`).\n\n### Additional context\n\nFound while reviewing #3205 (`T.clamp` NaN propagation, #3024), which hits the same root cause from the other side: TileLang has no NaN-propagating `max`/`min` primitive for fp32, so every path that needs one (`clamp`, scalar `T.max`/`T.min`, fp32 reductions) either falls back to a select — which the vectorizer cannot vectorize because the condition varies per lane — or silently does not propagate. A single target-lowered NaN-propagating max/min intrinsic (PTX `max.NaN` on CUDA, select fallback elsewhere) would fix this issue, `nan_propagate` for fp32, and the vectorization cost in #3205 together. Related: #2882 (`T.max`/`T.min` constant folding is order-dependent on NaN).\n", "url": "https://github.com/tile-ai/tilelang/issues/3270"}
{"number": 3024, "title": "[BUG][Fuzzer][wrong-code] `T.clamp` silently drops `NaN` and returns the `min` bound instead of propagating `NaN`", "state": "closed", "created_at": "2026-08-17T03:16:46Z", "updated_at": "2026-09-24T05:44:23Z", "closed_at": "2026-09-24T05:44:23Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n- [x] I have tried the latest version of TileLang.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nNVIDIA L40S (sm_89), CUDA 13.0, PyTorch 2.13.0, Python 3.13. The behavior is architecture-independent — the wrong value comes from the CUDA `fminf`/`fmaxf` NaN semantics, which are identical on every NVIDIA target; only sm_89 was run.\n\n### Problem description\n\n`T.clamp(x, lo, hi)` on a `NaN` input silently returns `lo` (the lower bound) instead of `NaN`. The reference semantics `T.clamp` should match are **`torch.clamp` and `numpy.clip`, both of which propagate `NaN`** (verified this session) — these are the frameworks a TileLang kernel is written against and differentially tested against. The kernel compiles and runs to completion with no error, so the wrong value is silent.\n\nClamping is used in the shipped fp8-quantization examples (`T.clamp(y / y_s, fp8_min, fp8_max)` in `examples/cast/example_group_per_split_token_cast_to_fp8.py:48`, `example_per_token_cast_to_fp8.py:35`, and the DeepSeek act-quant examples), where a `NaN` in the input would be quantized to `fp8_min` rather than surfacing.\n\n`T.clamp` lowers to `min(max(x, lo), hi)`; the emitted CUDA is `min(max(A[i], -1.0f), 1.0f)`. `NaN` fed to CUDA `max`/`min` (`fmaxf`/`fminf`) returns the non-`NaN` operand, so `max(NaN, -1.0f)` gives `-1.0f` and the `NaN` is gone before the outer `min` runs.\n\nThe same `NaN`-suppression is visible in `T.max`/`T.min` on their own (not just via `T.clamp`) — see the neighbor note below.\n\nNot a regression: `clamp` has been `min(max(...))` since it was added.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nN = 4\n\n@T.prim_func\ndef main(A: T.Tensor((N,), \"float32\"), C: T.Tensor((N,), \"float32\")):\n    with T.Kernel(1, threads=N) as bx:\n        i = T.get_thread_binding()\n        C[i] = T.clamp(A[i], T.float32(-1.0), T.float32(1.0))\n\nkernel = tilelang.compile(main, out_idx=[1])\n\na   = torch.tensor([float('nan'), 2.0, 0.5, -5.0], dtype=torch.float32, device=\"cuda\")\ngot = kernel(a).cpu()\nref = torch.clamp(a.cpu(), -1.0, 1.0)                 # trusted oracle\nprint(\"T.clamp :\", got.tolist())                      # -> [-1.0, 1.0, 0.5, -1.0]  NaN -> -1.0 (min bound)\nprint(\"torch   :\", ref.tolist())                      # -> [nan, 1.0, 0.5, -1.0]   NaN preserved\nprint(\"NaN preserved:\", torch.isnan(got[0]).item())   # -> False  (should be True)\n\n# control: with no NaN present, T.clamp matches torch exactly\nb = torch.tensor([2.0, 0.5, -5.0, 0.0], dtype=torch.float32, device=\"cuda\")\nprint(\"finite ctrl OK:\", torch.equal(kernel(b).cpu(), torch.clamp(b.cpu(), -1.0, 1.0)))  # -> True\n```\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion; the result is silently wrong and deterministic.\n\n### Expected behavior\n\n`T.clamp(NaN, lo, hi)` returns `NaN`.\n\nThe reference semantics that matter most here are **`torch.clamp` and `numpy.clip`** — both propagate `NaN` (a `NaN` input yields a `NaN` output; verified this session on 0.1.13's PyTorch and NumPy). These are the libraries TileLang kernels are written against and validated against, so `T.clamp` diverging from them on `NaN` is the core defect. The finite-input control already matches `torch` exactly, so only a `NaN` operand disagrees.\n\nThis also aligns with the newer IEEE-754 direction: 754**-2019** `minimum`/`maximum` propagate `NaN` (whereas the older 754**-2008** `minNum`/`maxNum` — which is what CUDA's `fmaxf`/`fminf` follow — suppress it). So \"propagate\" is not a single universal rule for min/max, but it is what both dominant array libraries do and what the current IEEE revision moved to; the concrete ask is that `T.clamp` match `torch`/`numpy`. Whether that is achieved by making `T.clamp` `NaN`-transparent or by exposing an opt-in is the maintainers' call.\n\n### Additional context\n\n**Root cause.** `T.clamp` composes clamping out of `T.max`/`T.min`, whose CUDA lowering does not preserve `NaN`. `clamp` is defined as `T.min(T.max(dst, min_val), max_val)` at [`customize.py#L55-L56`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/customize.py#L55); on CUDA `T.max`/`T.min` on floats emit `max`/`min`, i.e. `fmaxf`/`fminf`, which return the non-`NaN` argument. So `T.clamp(NaN, lo, hi)` computes `min(max(NaN, lo), hi) = min(lo, hi) = lo`.\n\n<details>\n<summary>Emitted CUDA + the T.max/T.min neighbor (both run on 0.1.13, L40S)</summary>\n\nEmitted device expression for the repro:\n\n```c++\nC[((int)threadIdx.x)] = min(max(A[((int)threadIdx.x)], -0x1p+0f), 0x1p+0f);   // min(max(A[i], -1.0f), 1.0f)\n```\n\nThe building blocks drop `NaN` on their own, so this is not specific to `clamp`:\n\n```\nT.max(nan, 0.0) -> 0.0    (torch.maximum -> nan)\nT.min(nan, 0.0) -> 0.0    (torch.minimum -> nan)\n```\n\nA fix targeting `clamp`'s composition (e.g. an explicit `dst != dst` re-injection of `NaN`, or a `NaN`-propagating min/max) closes `T.clamp`; the `T.max`/`T.min` primitives keep the CUDA `fmaxf`/`fminf` behavior unless they are separately changed.\n</details>\n\n**Suggested fix.** One direction is to make `clamp` `NaN`-transparent — carry the input `NaN` through, or build it from a `NaN`-propagating min/max — so `T.clamp(NaN, …)` returns `NaN` like `torch.clamp`. Whether `T.max`/`T.min` themselves should also propagate `NaN` (vs keep the `fmaxf`/`fminf` reduction semantics used by softmax-style `m_i = T.max(m_i, …)` accumulators) is a separate call for the maintainers. Not built/verified.\n\n**Provenance.** `clamp` was introduced in [#192](https://github.com/tile-ai/tilelang/pull/192) (\"[Language] Support clamp in language\", merged 2025-03-12) with the `min(max(...))` body it still has; the `NaN`-dropping behavior has been present since then. Not a regression.\n\n**Dedup.** I searched the open and closed tracker for `clamp` / `NaN` / `min max`. Distinct from [#2882](https://github.com/tile-ai/tilelang/issues/2882) (`T.max`/`T.min` on two compile-time float **constants** fold order-dependently on `NaN` via the host `const_fold.h` path); that report treats the **runtime** `max(...)` lowering as the well-defined reference — this issue is that the runtime lowering itself drops `NaN` versus `torch.clamp`/`numpy.clip` (and IEEE-754-2019). No existing report covers `T.clamp`.\n\n**Reach.** `T.clamp` is documented and appears in shipped examples. On 0.1.13 the cited sites are present and verbatim: `T.clamp(y_local[i,j] / y_s_local[i], fp8_min, fp8_max)` at `examples/cast/example_per_token_cast_to_fp8.py:35` and `example_group_per_split_token_cast_to_fp8.py:48`, and `T.clamp(x_local[i,j] / s_local[i], fp8_min/fp8_max)` at `examples/deepseek_v32/inference/kernel.py:72` and `examples/deepseek_v4/act_quant.py:78,156`. The underlying `T.max`/`T.min` appear in ~100 example sites (softmax `m_i = T.max(...)`, `T.max(x, 0)` ReLU).\n\nI **ran** `example_per_token_cast_to_fp8.py`'s shipped kernel verbatim on 0.1.13 (L40S). Two results: (1) with the example's shipped input (`torch.randn`, all finite) the correctness check passes exactly — the shipped example **dodges** this bug because no `NaN` ever reaches the clamp, and no test in the tree injects one. (2) Injecting a single `NaN` into one input row of the same kernel makes that row diverge from the torch reference: torch propagates `NaN` across the whole 128-element group (row0 fp8 = `[nan, nan, …]`) while TileLang emits finite fp8 values (`[-448.0, -60.0, -384.0, …]`). So the bug is reachable through the shipped kernel; the shipped test just never exercises `NaN`. (This particular divergence is compounded by `T.reduce_absmax` also dropping the `NaN` by default — see the generalization table.) The trigger requires a `NaN` reaching the clamp; finite inputs clamp correctly (control passes). CI is green because no test asserts `NaN` propagation through `T.clamp`.\n\n**Generalization.** Root, two levels: (a) SOURCE-level — `clamp` at `tilelang/language/customize.py#L55-L56` composes out of `T.max`/`T.min` (`tilelang/language/tir/ir.py:184-193` → TIR `min`/`max`), which lower to the CUDA NaN-dropping intrinsics (`fmaxf`/`fminf` for fp32, `__hmax`/`__hmin` for fp16/bf16 — never the `_nan` variants). (b) OPERATOR-level — every op built on scalar `T.max`/`T.min` inherits the drop: `T.clamp`, elementwise `T.max`/`T.min` themselves, and (by default) the `reduce_max`/`reduce_min`/`reduce_absmax` family. 4-axis sweep, all cells RUN on 0.1.13 (L40S):\n\n| axis | cell tested (input → observed) | same-root? |\n|---|---|---|\n| related-operator | `T.max(nan,0.0)→0.0`, `T.min(nan,0.0)→0.0` (torch: `nan`); but `T.max(nan,nan)→nan` (`fmaxf` returns the other operand, so `NaN` survives only when BOTH are `NaN`) | same root (elementwise; the clamp building blocks) |\n| related-operator | `T.reduce_absmax` fp32, default: `[…,nan,…]→3.0` (torch amax(abs)=`nan`) | same root, **distinct face** — reduce family has a `nan_propagate=True` opt-in (`reduce_op.py`, GH-#2697) that clamp lacks; but it **defaults to dropping** and is fp16/bf16-only |\n| related-type | `T.clamp` fp16: `clamp(nan,-1,1)→-1.0` (device emits `__hmax`/`__hmin`, not `__hmax_nan`) | same root |\n| related-type | `T.clamp` bf16: `clamp(nan,-1,1)→-1.0` (device emits `cutlass::bfloat16 __hmax/__hmin`) | same root |\n| related-type | `T.clamp` int32/int (no `NaN` representation) | N/A — does not apply (integers have no `NaN`) |\n| similar-logic | reduce family got a per-call `nan_propagate` escape hatch; elementwise `T.max`/`T.min`/`T.clamp` have NO such kwarg (`git grep nan_propagate` hits only `reduce_op.py`) | same root, asymmetric coverage — the fix already exists for reductions but not for the elementwise/clamp composition |\n\nFraming kept **specific to `T.clamp`** (title/Problem unchanged): the elementwise `T.max`/`T.min` NaN-drop is arguably intended (softmax `m_i = T.max(...)` accumulators rely on `fmaxf` reduction semantics), and the reduce family already has an opt-in, so the crisp defect is that `T.clamp` — which per `torch.clamp`/IEEE should be `NaN`-transparent — has no way to preserve `NaN`. The reduce-default NaN-drop (`reduce_absmax` etc.) is a **distinct, adjacent** behavior with an existing opt-in and test coverage; not filed here.\n\n**Impact.** Silent-wrong, deterministic, bounded to the clamped value: a `NaN` at the input becomes the `min` bound (`lo`) with no error, so a `NaN` that a workload relies on to detect (or propagate through a quantization step) is silently replaced by a finite in-range value every run. Fixing it aligns `T.clamp` with the `torch.clamp`/`numpy.clip` `NaN` semantics kernels are validated against (and the IEEE-754-2019 direction) and removes a silent frontend/runtime disagreement.", "url": "https://github.com/tile-ai/tilelang/issues/3024"}
{"number": 3274, "title": "[Performance][CUDA] Late SIMT im2col lowering misses scheduling and vectorization opportunities", "state": "open", "created_at": "2026-09-24T04:48:52Z", "updated_at": "2026-09-24T04:48:52Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "SIMT im2col is expanded after warp specialization and pipeline planning. These passes see an opaque tile operation rather than its guarded global-to-shared accesses, limiting scheduling opportunities.\n\n## IR / SASS Analysis\n\nIn the captured IR after `InjectSoftwarePipeline`, weights are assigned to the producer, but im2col remains on the consumer. Simplified excerpt; regions and barrier indices omitted:\n\n```python\nif tx < 128:  # producer\n    for k in range(18):\n        T.mbarrier_wait_parity(...)\n        T.tma_copy(kernel_flat, kernel_shared, ...)\nelse:  # consumer\n    for k in range(18):\n        T.im2col(data, data_shared, ...)\n        T.mbarrier_wait_parity(...)\n        T.gemm(data_shared, kernel_shared, out_local, ...)\n```\n\nThe corresponding consumer-side SASS already uses vector loads, followed by shared-memory stores (intervening instructions omitted):\n\n```text\n@!P0 LDG.E.128.CONSTANT R56, desc[UR8][R2.64+0x1000]\n...\nSTS.128 [R54+-0x2000], R56\n```\n\nExposing the accesses before scheduling instead produces producer-side `cp_async_gs_conditional<16>`, lowered to:\n\n```text\nLDGSTS.E.BYPASS.128 [R17], desc[UR4][R6.64], P0\n```\n\nThe distinction here is **which warp group loads the input, and whether that transfer can overlap computation**, not simply vector width.\n\nThe issue is lowering order, not a new indexing identity: factored and unfactored explicit indexing generated identical CUDA in the ablation.\n\nThe compiler should expose SIMT accesses before scheduling while preserving vectorization and bounds protection. Eligibility should follow the selected implementation—not an architecture threshold—so specialized paths such as Hopper TMA remain unchanged.\n", "url": "https://github.com/tile-ai/tilelang/issues/3274"}
{"number": 3261, "title": "[Feature Request] [Perf] LowerMagicDiv: host-precomputed magic division for launch-invariant dynamic divisors", "state": "open", "created_at": "2026-09-21T05:38:09Z", "updated_at": "2026-09-23T14:10:36Z", "closed_at": null, "labels": ["enhancement"], "comments_count": 0, "is_pr": false, "body": "## Summary\n\n`tl.LowerMagicDiv` rewrites integer division/modulo by a **launch-invariant but compile-time-unknown positive int32 divisor** into host-precomputed magic numbers, with each device thread executing only `umulhi + shift`:\n\n```text\nq = umulhi(uint32(x), M) >> s\nr = x - q * d\n```\n\nA plain dynamic integer division typically expands into long reciprocal-estimation sequences, corrections, or helper calls. Magic division moves all `d`-dependent expensive work to the host (computed **once per kernel launch**), so every device thread performs only an unsigned multiply-high and a right shift. This is **not** a floating-point approximate division — within the specified input domain the result is bit-exact with integer division.\n\n## 1. Mathematical Principle\n\nFor `x = q * d + r` (dividend `x`, divisor `d`, quotient `q`, remainder `r`) with `d ≥ 2`:\n\n```text\nk = ceil(log2(d))\np = 31 + k\nM = ceil(2^p / d)\ns = p - 32 = k - 1\n```\n\nFor `0 ≤ x < 2^31`:\n\n```text\nfloor(x / d) = floor(x * M / 2^p)\n             = umulhi(uint32(x), M) >> s\n```\n\nwhere `umulhi(a,b)` returns the high 32 bits of the unsigned 32×32 product, i.e. `umulhi(a,b) = floor(uint64(a) * uint64(b) / 2^32)`; the following right shift by `s = p - 32` completes the divide-by-`2^p` floor.\n\n**Why it is exact.** The key is that `M` is rounded *up*. Writing `M = 2^p/d + ε` with `0 ≤ ε < 1`:\n\n```text\nx * M / 2^p = x / d + x * ε / 2^p\n```\n\nSince `x < 2^31` and `p = 31 + k`:\n\n```text\n0 ≤ x * ε / 2^p < 1 / 2^k ≤ 1 / d\n```\n\nWith `x = q·d + r` (`0 ≤ r < d`), `x/d` is at least `1/d` away from the next integer; the additive error is strictly smaller than that distance, so it never crosses an integer boundary and the floor still yields the exact `q = floor(x/d)`. This is also why the fast path requires a non-negative dividend below `2^31`.\n\n### Worked example: 100 / 7\n\nHost precomputation (`d = 7`):\n\n```text\nk = ceil(log2(7)) = 3,  p = 31 + k = 34\nM = ceil(2^34 / 7) = 2454267027 = 0x92492493\ns = p - 32 = 2\n```\n\nAlthough `M / 2^34 = 0.142857142898720…` is only an upward fixed-point approximation of `1/7 = 0.142857142857143…`, the error bound above guarantees the result never crosses the next integer. On device (`x = 100`):\n\n```text\numulhi(100, 2454267027) = floor(245426702700 / 2^32) = 57\nq = 57 >> 2 = 14\nr = x - q * d = 100 - 14 * 7 = 2\n```\n\nwhich matches `100 // 7 = 14`, `100 % 7 = 2` exactly.\n\n**Special cases:**\n- `d == 1`: host sets `M = 0, s = 0`; device directly selects `q = x, r = 0`.\n- `d == 0`: the host helper returns zero parameters without performing a division; a shape-derived zero divisor normally never launches the device work, and the domain of an executed divide-by-zero site is unchanged — magic div does not extend it.\n- `x < 0`, `d < 1`, or signed int32 wrap: the fast path is not taken; an exact fallback is used.\n\n## 2. Reference: CUTLASS Implementation\n\nPrimary reference: `3rdparty/cutlass/include/cutlass/fast_math.h`\n\n- `find_log2`: computes `ceil(log2(d))`\n- `find_divisor`: computes `multiplier` and `shift_right`\n- `fast_divmod`: device-side `__umulhi(src, mul) >> shr`, then `rem = src - quo * div`\n- `cutlass::FastDivmod`: intended to be constructed on the host, with precomputed parameters passed through kernel `Params`\n\nCore logic:\n\n```cpp\np   = 31 + ceil_log2(d);\nmul = ceil(2^p / d);\nshr = p - 32;\nquo = d != 1 ? __umulhi(src, mul) >> shr : src;\nrem = src - quo * d;\n```\n\nTileLang does **not** instantiate `cutlass::FastDivmod` in generated kernels. Instead it reuses the same math and the same host-precompute / device-mulhi structure, expressed as **TIR intrinsics**, so the optimization is applied automatically by a compiler pass to dynamic-shape indexing and can be lowered separately by the CUDA, HIP, and C codegens. Compared with using CUTLASS directly, TileLang additionally handles:\n\n- Which TIR div/mod sites are safe to transform\n- The semantic gap between TIR `FloorDiv`/`FloorMod` and C/CUDA truncating div/mod\n- Fallback for invalid runtime values\n- Quotient/remainder CSE for the same `(x, d)`\n- Semantic correlation and validity-predicate reuse in conditions\n- Interaction with `FlattenBuffer`, int64 promotion, and later Simplify on index algebra\n\n## 3. Design in TileLang\n\nPipeline overview:\n\n```text\n     dynamic shape / scalar divisor d\n              │\n              ▼\n     LowerMagicDiv (device IR still uses multi-dim indexing)\n       1. Normalize remainder expressions\n       2. Prove divisor is launch-invariant, positive, int32\n       3. Prove dividend non-negativity and condition correlation\n       4. Deduplicate by equivalent divisors\n       5. Rewrite div/mod into tl.magic_* intrinsics\n              │\n              ├────── host Bind ──────┐\n              │   TileLangHostFastDivmodU32Mul(d)\n              │   TileLangHostFastDivmodU32Shift(d)\n              ▼                       ▼\n     FlattenBuffer → ConfigIndexBitwidth → SplitHostDevice\n              host computes M/s and passes them as extra\n              kernel params; d still comes from the original\n              dynamic shape/scalar parameter\n              │\n              ▼\n     MagicCallHoist (after the final Simplify)\n       - Merge duplicate calls by (x, d)\n       - div/mod share one quotient\n       - Reuse results across condition branches\n       - Merge runtime validity guards\n              │\n              ▼\n     CUDA / HIP / C codegen\n       valid:   mulhi + shift, r = x - q * d\n       invalid: exact floor-div / floor-mod helper\n```\n\n### End-to-end example\n\nGiven `q = x // d; r = x % d` where `d` is a dynamic shape/scalar parameter, invariant within one launch. This launch has `d = 7`; one device thread has `x = 100`.\n\nAt **compile time**, LowerMagicDiv does not know `d = 7`, but proves it is a launch-invariant divisor and rewrites:\n\n```text\nq = floordiv(x, d)          m = host_magic_multiplier(d)\nr = floormod(x, d)    →     s = host_magic_shift(d)\n                            q = tl.magic_div(x, d, m, s)\n                            r = tl.magic_mod(x, d, m, s)\n```\n\nAfter MagicCallHoist, div/mod on the same `(x, d)` share one quotient and one validity check:\n\n```text\nvalid = (x ≥ 0) && (1 ≤ d ≤ INT32_MAX)\nq = tl.magic_div_with_validity(x, d, m, s, valid)\nr = tl.magic_mod_from_quotient(x, d, q, valid)\n```\n\nAt **launch time**, the host sees the actual `d = 7` and computes `m = 0x92492493, s = 2` once; all threads reuse them:\n\n```text\nkernel(..., d=7, tl_magic_m=0x92492493, tl_magic_s=2)\n```\n\nNote: `m` is mathematically a `uint32`; TileLang currently transmits its bit pattern through an `int32` kernel parameter (`0x92492493` reads as negative when interpreted as signed), and the device codegen re-casts to `unsigned` before `__umulhi`, so all 32 bits survive intact.\n\nOn the **device thread** with `x = 100`:\n\n```text\nvalid  = (100 ≥ 0) && (uint32(7 - 1) ≤ 0x7ffffffe) = true\nhigh32 = __umulhi(uint32(100), uint32(0x92492493)) = 57\nq      = 57 >> 2 = 14\nr      = x - q * d = 100 - 14 * 7 = 2\n```\n\nResponsibility split:\n\n| Stage | Work | Example |\n|---|---|---|\n| Compile time | Identify eligible sites; emit host helpers, magic intrinsics, guards, q/r CSE | `d = 7` not yet known |\n| Host, per launch | Compute `m/s` from the actual dynamic divisor | `d=7, m=0x92492493, s=2` |\n| Each device thread | Validity + `umulhi`+shift + mul-sub with its own `x` | `x=100 → q=14, r=2` |\n\nIf one thread's `x` violates the fast-path contract, only that thread's `valid` is false and it falls back to the original floor-div/mod — other threads keep the fast path.\n\n\n### PR\n\n#3267 \nregission case in h200\n| Case | main | dev branch | speed up\n| -- | ---: | ---: | ---: |\n| permute_scales_dsv3_e256_n2048_k7168_g32_w4a16 | 0.9374 | 0.8507 | 9.2% |\n| permute_scales_dsv3_e256_n2048_k7168_g32_w4a8 | 0.8760 | 0.7791 | 11.1% |\n| permute_scales_llama4_e8_n4096_k8192_g128_w4a16 | 0.0183 | 0.0167 | 8.8% |\n\n09.22 update\n| Case | main | dev branch | speed up\n| -- | ---: | ---: | ---: |\n| permute_scales_dsv3_e256_n2048_k7168_g32_w4a16 | 0.9374 | 0.7115 | 24% |\n| permute_scales_dsv3_e256_n2048_k7168_g32_w4a8 | 0.8760 | 0.6454 | 26.3% |\n| permute_scales_llama4_e8_n4096_k8192_g128_w4a16 | 0.0183 | 0.0142 | 22.4% |", "url": "https://github.com/tile-ai/tilelang/issues/3261"}
{"number": 3271, "title": "[Feature Request][Perf] Range contracts and optimization diagnostics for T.dynamic", "state": "open", "created_at": "2026-09-23T07:15:29Z", "updated_at": "2026-09-23T07:15:29Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] Searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) for existing requests.\n\n### Motivation\n\nTileLang silently handles many cases where missing range information prevents boundary checks or arithmetic guards from being eliminated. Compilation succeeds, but there is no general diagnostic connecting the missing condition to the retained checks or expression growth. For a DSL intended for high-performance kernels, this is a significant gap in performance feedback.\n\nThe cost is not necessarily a single branch. A missing sign, range or non-overflow condition can prevent layout simplification and leave repeated guarded calculations that expand through subsequent lowering. Even a case producing thousands of extra instructions can pass through compilation without a diagnostic pointing back to that missing condition. For an otherwise carefully tuned operator, this hidden cost may be the remaining performance bottleneck.\n\n`T.dynamic` currently accepts a name and dtype, but no declaration-site range contract. Shape, stride and layout parameters often have small application-specific ranges that are known to the kernel author but absent from the IR. For example, `num_groups * size_n` may be tightly bounded in every intended invocation, yet the compiler cannot establish that its int32 evaluation agrees with multiplication after int64 widening from positivity alone.\n\nOne concrete anti-pattern in the `permute_scales` investigation in [#3265](https://github.com/tile-ai/tilelang/pull/3265) was **computing layout coordinates to check an access, then recomputing them at a wider width to perform it**. Simplified generated-device pseudocode, with fallback paths omitted:\n\n```cpp\nif (idx32 < num_experts * num_groups * size_n) {\n    int32_t f32 = idx32 % (num_groups * size_n);\n    if (f32 / size_n < num_groups) {\n        int32_t u32 = swizzle(f32);\n        if (0 <= u32 / size_n && u32 / size_n < num_groups) {\n            int64_t f64 = idx64 % (int64_t(num_groups) * int64_t(size_n));\n            int64_t u64 = swizzle(f64);\n            // Use u64 to compute the actual load address.\n        }\n    }\n}\n```\n\nThese are device-side coordinate guards, not host parameter validation or a zero-divisor fallback. Missing non-overflow and swizzle-range proofs leave both the guards and the computations feeding them; unproven cross-width equivalence prevents reuse between the two chains. The compiler silently retains this duplication. Useful parameter bounds, combined with the swizzle's block-preserving and alignment properties, could justify eliminating it without specializing every shape. Upper bounds alone do not establish all these properties or remove legitimate tail checks.\n\nTileLang should encourage explicit contracts for these performance-sensitive parameters and make costly missing proofs visible, rather than requiring users to discover them by inspecting generated code. This could help dynamic kernels approach constant-specialized performance without compiling a variant for every shape.\n\n### Solution\n\nConsider range annotations on `T.dynamic`, backed by existing assumption and range-analysis infrastructure. Illustrative syntax only; bounds below are inclusive:\n\n```python\nnum_groups = T.dynamic(\"num_groups\", bound=(1, 1024))\nsize_n = T.dynamic(\"size_n\", bound=(1, 1 << 20))\n```\n\nThese declarations leave both values dynamic, while establishing that their product fits positive int32. Bounds should be available before layout lowering and remain associated with the variables through index widening and host/device splitting. Existing `T.assume` conditions should supply the same facts; relational constraints need not be forced into independent variable intervals.\n\nDiagnostics are central to this proposal, not just a convenience for the new syntax. When a missing proof retains costly checks or duplicated calculations, report the source expression, the blocked transformation and a sufficient condition, for example:\n\n```text\nCannot reuse the int32 layout product after index widening.\nMissing condition: 0 < num_groups * size_n <= INT32_MAX\nProvide parameter bounds or a relational T.assume constraint.\n```\n\nHere the condition describes the mathematical product; any generated check must use arithmetic wide enough to evaluate it safely. Inferring a sufficient condition must not silently turn it into an assumption.\n\n`T.dynamic` could be range-sensitive by default, prioritizing diagnostics for retained per-thread checks and substantial expression growth rather than warning about every unannotated variable or failed proof. Where available, include the affected checks or IR-size growth; neither should be presented as a measured latency estimate. An explicit opt-out, such as `bound=False`, could suppress these diagnostics without changing integer semantics or removing required checks. Zero-sized shapes and intentionally signed parameters remain valid where the operation permits them.\n\nFor host-known parameters, contract validation should be shared at launch time rather than repeated per thread, with CUDA Graph capture/replay behavior preserved. Device guards should disappear only when the contract and access relation prove them redundant; a parameter upper bound alone does not eliminate tail masks.\n\nRange propagation should retain shared bindings and bound its analysis work. An annotation should not trigger unrestricted substitution, and range metadata alone should not be presented as a fix for expression expansion.\n\n### Alternatives\n\n- **Inspect existing diagnostics and traces:** IR Lower Trace exposes pass-by-pass IR and generated source; JIT diagnostics and pass profiling report compilation activity and timing. Existing vectorization warnings and optional out-of-bounds warnings cover specific failures. These tools help locate symptoms, but do not generally identify the missing range contract responsible for retained checks or expression growth. The proposed diagnostics could integrate with them.\n- **Use `T.assume` everywhere:** already useful, and a reasonable implementation foundation, but less discoverable than declaration-site bounds and provides no targeted explanation of missing optimization conditions.\n- **Use unsigned types:** establishes nonnegativity, not nonzero values or non-wrapping products.\n- **Specialize on constants:** often generates good code, but increases kernel variants and compilation cost.\n- **Rely on more aggressive inference:** still needed for derived expressions, but should complement explicit contracts rather than guess application limits or repeatedly expand shared expressions.\n\n### Additional context\n\nRelated requests include [#2431](https://github.com/tile-ai/tilelang/issues/2431) (warnings for vectorization fallback), [#1012](https://github.com/tile-ai/tilelang/issues/1012) (unnecessary bounds guards), and [#2065](https://github.com/tile-ai/tilelang/issues/2065) (scalar bindings to avoid expression expansion). This proposal focuses on explicit dynamic-parameter contracts and diagnostics connecting missing bounds to their optimization consequences.\n\nThe dynamic/constant comparisons in [#3265](https://github.com/tile-ai/tilelang/pull/3265) point to missing upper-bound guarantees as an important last-mile barrier toward constant-specialized performance. Constants additionally expose power-of-two divisors and address-folding opportunities, so the remaining gap is not solely the cost of checks. Related arithmetic work: [#3261](https://github.com/tile-ai/tilelang/issues/3261) and [#3267](https://github.com/tile-ai/tilelang/pull/3267).\n\nThis frontend feature could be developed separately from arithmetic lowering, with initial experiments on the current branch. Evaluation should retain the full existing benchmark matrix and compare generated source size, compilation time, GPU time and host wall time, including contract checks. Boundary values, wrapping, cross-width equivalence and graph replay need correctness coverage alongside performance measurements.\n", "url": "https://github.com/tile-ai/tilelang/issues/3271"}
{"number": 3231, "title": "[BUG] Reusing an alloc_var name as a loop target causes an out-of-scope error", "state": "closed", "created_at": "2026-09-15T08:10:07Z", "updated_at": "2026-09-23T05:54:35Z", "closed_at": "2026-09-23T05:54:35Z", "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n 0.1.14 and 0.1.14+cuda.gitd178b4c9\n\n### System information\n\nReproduced in:\n\n- Linux, Python 3.13, TileLang 0.1.14, PyTorch 2.7.1+cu128, RTX 5090.\n- Windows, Python 3.13, TileLang source build at d178b4c9, PyTorch 2.11.0+cu130, RTX 5060 Laptop.\n\nThe failure occurs during frontend IR construction, before CUDA compilation.\n\n### Problem description\n\nReusing a previous T.alloc_var name as the induction variable of a subsequent loop raises an out-of-scope error.\n\nThe second loop introduces a new induction variable, but the frontend treats its binding as an assignment to the previous mutable scalar. Renaming only the second loop target avoids the error.\n\n### Reproducible example code\n\n```python\nimport tilelang.language as T\n@T.prim_func\ndef kernel(A: T.Tensor((2,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for j in T.serial(2):\n            d = T.alloc_var(\"int32\")\n            d = j\n            A[j] = d\n        for d in T.Parallel(2):\n            A[d] = d\nControl case: replace the second loop with:\nfor k in T.Parallel(2):\n    A[k] = k\n```\n\n\n### Traceback\n\n```pytb\nRelevant excerpt:\nFile \".../repro_loop_rebind.py\", line 17, in kernel\n    A[d] = d\nFile \".../tilelang/language/eager/builder.py\", in rval\n    raise RuntimeError(...)\nRuntimeError: Immutable variable `d` is used outside its defining region!\nThe error dump shows the second loop incorrectly storing its induction variable into the old buffer:\nd[0] = v\n```\n\n### Expected behavior\n\nThe second loop should bind d to its own induction variable, without writing to the previous alloc_var. The kernel should produce [0, 1].\nActual reads of expired bindings should remain rejected.\n\n### Additional context\n\nIn Builder.bind(), an existing local.var binding takes the mutable-assignment path when the incoming value is a PrimExpr. Loop targets currently use this same path.\nDistinguishing loop-target binding from ordinary assignment avoids the unintended buffer store while preserving mutable-scalar updates and scope validation.", "url": "https://github.com/tile-ai/tilelang/issues/3231"}
{"number": 3007, "title": "[BUG][Fuzzer][diagnostic] `T.copy(coalesced_width=N)` `LOG(FATAL)`s when N doesn't divide the geometry-derived vector size, instead of clamping-with-warning or a clean diagnostic", "state": "closed", "created_at": "2026-08-16T07:20:36Z", "updated_at": "2026-09-23T03:52:41Z", "closed_at": "2026-09-23T03:52:41Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13 / PyTorch 2.8.0 / CUDA 12.8 / NVIDIA L40S (sm_89). The defect is in an architecture-independent layout-planning pass; also present on `main` (see Root cause / Provenance).\n\n### Problem description\n\n`coalesced_width` is a documented, `int`-typed public keyword of [`T.copy`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/copy_op.py#L58-L70) (\"Width for coalesced memory access\"). When the copy's auto-derived per-thread vector size is *smaller* than the requested width, the compiler aborts with an internal `LOG(FATAL)` instead of using the value as a hint:\n\n```\ntvm.error.InternalError: Vector size 1 is not divisible by coalesced width 2\n```\n\nThe value is legal and the copy itself compiles fine — removing only the `coalesced_width` kwarg makes the identical kernel compile and produce correct results. The abort fires whenever the copy region's geometry forces the auto-derived vector size below the requested width (here a ragged 1-column tail block makes the SIMT loop non-vectorizable, so the derived width is 1 and `1 % 2 != 0`). The same `coalesced_width=2` compiles for tiles whose geometry admits a ≥2-wide vector, so it is the hint-vs-geometry interaction, not the value, that crashes. Not a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nM, N, BM, BN = 32, 33, 32, 32   # N=33 => one ragged tail column beyond the 32-wide tile\n\ndef build(cw):                  # cw=None -> control; cw=2 -> trigger\n    @T.prim_func\n    def main(A: T.Tensor((M, N), \"float32\"), B: T.Tensor((M, N), \"float32\")):\n        with T.Kernel(T.ceildiv(N, BN), T.ceildiv(M, BM), threads=128) as (bx, by):\n            sh = T.alloc_shared((BM, BN), \"float32\")\n            if cw is None:\n                T.copy(A[by*BM, bx*BN], sh)\n                T.copy(sh, B[by*BM, bx*BN])\n            else:\n                T.copy(A[by*BM, bx*BN], sh, coalesced_width=cw)   # documented int kwarg -> FATAL\n                T.copy(sh, B[by*BM, bx*BN], coalesced_width=cw)\n    return main\n\ndef run(cw):\n    try:\n        k = tilelang.compile(build(cw), out_idx=[1])\n        a = torch.randn(M, N, device=\"cuda\"); b = k(a)\n        return \"PASS\" if torch.allclose(b, a, atol=1e-4) else \"MISMATCH\"\n    except Exception as e:\n        return \"CRASH: \" + str(e).split(\"Fatal:\")[-1].strip().splitlines()[0][:60]\n\nprint(\"control  no coalesced_width :\", run(None))   # -> PASS\nprint(\"trigger  coalesced_width=2  :\", run(2))      # -> CRASH: Vector size 1 is not divisible by coalesced width 2\n```\n\n### Traceback\n\n```\n[TileLang] : Fatal: Vector size 1 is not divisible by coalesced width 2\nTraceback (most recent call last):\n  File \"<unknown>\", line 0, in tvm::runtime::detail::LogFatal::~LogFatal()\ntvm.error.InternalError: Vector size 1 is not divisible by coalesced width 2\n```\n\n### Expected behavior\n\nA documented public `int` hint should not reach an internal `LOG(FATAL)`. When the requested width cannot be met by the copy's geometry, the natural behavior is to clamp/decline the hint (use the largest achievable width) or reject with a user-facing diagnostic — not abort with an internal assert. The pass already does exactly this graceful clamp for the *auto-derived* width one branch earlier: it halves `vector_size` until it divides the thread/loop geometry ([parallel.cc:783-787](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L783-L787)). Applying the same clamp when the user supplies `coalesced_width` would keep the hint honored where possible and non-fatal where not.\n\n### Additional context\n\n**Root cause.** The parallel-loop layout planner treats `coalesced_width` as a hard divisor of the auto-derived vector size and aborts when it doesn't divide, rather than clamping it. In [`ParallelOpNode::ComputePlanCandidate`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L798-L808) the derived `vector_size` (already reduced to fit the thread bounds by the loop at [:783-787](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L783-L787)) is checked with `vector_size % expected != 0` → `LOG(FATAL)`. For a ragged region whose loop is non-vectorizable, `vector_size == 1`, so any `coalesced_width > 1` fails the modulo and aborts. The check assumes the user width is always ≤ and a divisor of the achievable width; when the geometry forces a smaller vector, that assumption breaks with no fallback.\n\n<details>\n<summary>boundary: the crash is width-vs-geometry, and the value is honorable on a fitting tile</summary>\n\nSame `coalesced_width` value, different tile geometry (float32):\n\n```\ntile 128x128, threads 128  (derived vector_size 4):  cw=1 PASS, cw=2 PASS, cw=4 PASS, cw=8 CRASH (\"Vector size 4 not divisible by coalesced width 8\")\ntensor 32x33, tile 32x32, threads 128 (ragged tail -> derived vector_size 1):  cw=2/4/8 CRASH (\"Vector size 1 ...\")\ntensor 32x32, tile 32x32, threads 128 (derived vector_size >=2):  cw=2 PASS\n```\n\nSo `coalesced_width=2/4` are values the planner can honor (they compile and return correct results on a tile whose derived vector size is a multiple); the abort is specific to the case where the region geometry drops the derived width below the request.\n\nRe-verified this session (0.1.13, L40S), each a separate compile: `128×128` tile → `cw=4` PASS, `cw=8` CRASH (\"Vector size 4 is not divisible by coalesced width 8\"); `32×32` exact tile → `cw=2` PASS; `32×33` ragged tail → `cw=2` CRASH (\"Vector size 1 ...\"). This is a **tight-guard** defect: the planner treats the user width as a hard divisor of the (already geometry-reduced) derived vector size instead of clamping it. It is a *distinct* branch from the sibling `T.Parallel` bare-int abort (\"`coalesced_width should be an IntImmNode`\", line 808): that is a type-coercion gap on a raw Python int; this is the divisibility check on line 804 — same function, adjacent code, different root, reported separately.\n</details>\n\n**Suggested fix.** `coalesced_width` is documented as a *hint* (\"Width for coalesced memory access\"), so an unmeetable value should degrade, not abort. In [parallel.cc:798-808](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L798-L808), when the requested width doesn't divide the geometry-derived `vector_size`, clamp the effective width to `gcd(vector_size, expected)` (or `min` when it divides) **and emit a warning** that the hint was reduced (naming the requested vs achievable width) — reusing the graceful-degradation policy the auto path at [:783-787](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L783-L787) already applies to the derived width. If instead the project intends `coalesced_width` to be a hard requirement rather than a hint, then at minimum replace the internal `LOG(FATAL)` with a clean user-facing diagnostic naming the kwarg and the achievable width — the current internal assert is the wrong failure mode either way. (Fix not built end-to-end this session.)\n\n**Provenance.** Present at least since the block was last rewritten by [#1559](https://github.com/tile-ai/tilelang/pull/1559) (\"[Parallel][Infer] Free-mode chooses minimal replication…\", merged 2025-12-30) — blame attributes lines 798-808 to that commit, which was a layout-inference refactor that relocated/rewrote the surrounding code rather than introducing the check; the `coalesced_width` divisibility abort predates it (it exists unchanged in the 0.1.13 wheel I ran). Origin before #1559 is unverified. Not a regression. The permalinks above pin v0.1.13 (`8001cc4`).\n\n**Dedup.** I searched the open and closed tracker (issues and PRs) for `coalesced_width`, `\"not divisible by coalesced width\"`, and `ComputePlanCandidate` and found no existing report of this defect. (#1802 concerns a stray `coalesced_width` kwarg passed to a Python test helper — a `TypeError`, unrelated to this compiler check; [#1424](https://github.com/tile-ai/tilelang/issues/1424) is a vectorization feature request.)\n\n**Reach.** `coalesced_width` is documented on [`T.copy`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/copy_op.py#L58-L70) and used in shipped examples: `git grep -n coalesced_width v0.1.13 -- examples/ testing/` finds it in the AMD flash-attention examples (`examples/amd/example_amd_flash_attn_fwd.py:183/192/193`, `example_amd_flash_attn_bwd.py`) which pass `coalesced_width=8` (qk) / `4` (v) into `T.copy`, and in `testing/python/amd/test_tilelang_gfx950_copy_async.py` (cw=4/8 into `T.copy`/`T.async_copy`).\n\nI verified the dodge by RUNNING the load-bearing copy geometry from the fwd example verbatim on 0.1.13 (L40S), isolated from the AMD/RDNA-only kernel body: `Q_shared[block_M, dim=128]` fp16 copies with `coalesced_width=8`, and `V` copies with `coalesced_width=4`, for the non-RDNA configs (`block_M∈{64,128,256}`, `dim=128`, `threads∈{128,256}`):\n\n```\nQ-copy block_M=64  dim=128 threads=128 cw=8 : PASS\nV-copy block_M=64  dim=128 threads=128 cw=4 : PASS\nQ-copy block_M=128 dim=128 threads=256 cw=8 : PASS\n```\n\nAll PASS: the head-dim is a full contiguous 128-wide fp16 span, so the auto-derived vector size is ≥ the requested width and it divides cleanly — the abort is dodged, which is why CI is green. (The full `example_amd_flash_attn_fwd.py` targets ROCm/RDNA — `IsRDNA()` gates the kernel body and its config grid — so end-to-end it autotunes but is not the correct target on an NVIDIA sm_89; the standalone copy-geometry run above is the faithful, runnable slice of its `coalesced_width` usage.) The crash needs the intersection of (a) a user-supplied `coalesced_width` (rare — only the AMD examples/tests do it in-repo) with (b) a copy region whose geometry forces the derived vector size below the requested width — a ragged/partial tile at the array edge, or simply a `coalesced_width` larger than or not dividing the achievable vector (e.g. `cw=8` on a tile whose derived vector is 4). No shipped test exercises `coalesced_width` on such a region, so the branch has no coverage.\n\n**Impact.** Firing needs the narrow intersection of a user-supplied `coalesced_width` hint and a copy region whose geometry forces the derived vector size below it (a ragged edge tile). When it fires it is a compile-time `LOG(FATAL)` — loud and caught immediately at build, blocking that one kernel from compiling but corrupting no output and unable to reach a running workload silently. Fixing it removes the internal-assert failure on a legal documented kwarg (clamping the hint like the auto path already does), turning a crash on a valid input into a compiled kernel or a user-facing diagnostic.\n\n**Generalization (root + 4-axis sweep).**\n\nTwo-level root:\n- **SOURCE-level.** [`ParallelOpNode::ComputePlanCandidate`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L798-L810) consumes the `kCoalescedWidth` annotation as a **hard `%`-divisor** of the already-geometry-reduced `vector_size`: [line 802-804](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L802-L804) `LOG(FATAL)`s when `vector_size % expected != 0`, and [line 808](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L808) `LOG(FATAL)`s when the annotation is not an `IntImmNode`. Neither clamps, unlike the auto-derived path just above ([:783-787](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L783-L787) halves until it fits). Note [:789](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L789): when `coalesced_width` is set, the `SelectMinPaddingVectorSize` softening is *skipped*, so the user value meets the raw derived vector head-on.\n- **OPERATOR-level.** Any frontend op that attaches the `coalesced_width` annotation and lowers through this planner: `T.copy` / `T.async_copy` (both variants in [copy_op.py](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/copy_op.py)) and the raw `T.Parallel` loop ([loop.py:79-80](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/loop.py#L79-L80)).\n\n**4-axis sweep** (all cells RUN on 0.1.13 / L40S, one fresh process each):\n\n| axis | cell tested | observed result | same root? |\n|---|---|---|---|\n| (repro) | `T.copy` global↔shared fp32, N=33 ragged tail, cw=2 | `CRASH: Vector size 1 is not divisible by coalesced width 2` | — |\n| similar-logic | `T.copy` full 128×128 tile, derived vector=4, **cw=4** | PASS | same (divides) |\n| similar-logic | `T.copy` full 128×128 tile, **cw=8** (exceeds derived 4) | `CRASH: Vector size 4 is not divisible by coalesced width 8` | **YES** — not ragged-only; any cw>derived |\n| similar-logic | `T.copy` full 128×128 tile, **cw=3** (non-divisor of 4) | `CRASH: Vector size 4 is not divisible by coalesced width 3` | **YES** — strict `%`-divisor, not just `>` |\n| related-operator | `T.async_copy` global→shared, N=33 ragged, cw=2 | `CRASH: Vector size 1 is not divisible by coalesced width 2` | **YES** — same line 804 |\n| related-operator | `T.Parallel(M,N,coalesced_width=2)`, N=33 ragged | `CRASH: coalesced_width should be an IntImmNode.` | **distinct** — line 808, fires *before* 804 |\n| related-operator | `T.Parallel(M,N,coalesced_width=2)`, N=32 clean/vectorizable | `CRASH: coalesced_width should be an IntImmNode.` | **distinct** — geometry-independent, always fires |\n| related-type | `T.copy` **fp16** N=33 ragged, cw=2 | `CRASH: Vector size 1 is not divisible by coalesced width 2` | **YES** — dtype-independent (check is on element count) |\n| related-type | `T.copy` **int8** N=33 ragged, cw=2 | `CRASH: Vector size 1 is not divisible by coalesced width 2` | **YES** — dtype-independent |\n| related-type (scope) | `T.copy` global↔**fragment** (register) fp32, N=33 ragged, cw=2 | `CRASH: Vector size 1 is not divisible by coalesced width 2` | **YES** — fragment-access branch ([:782](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L782)) reaches the same 804 |\n\n**What the sweep establishes.**\n- The class is **broader than \"ragged tail\"**: the divisibility abort fires for *any* `coalesced_width` that is not a divisor of the geometry-derived vector size — a too-large width (`cw=8` on derived-4) and a non-power-of-2 width (`cw=3`) crash on a fully-rectangular tile too. The draft title/Problem are accordingly framed around the width-vs-derived-vector divisibility guard, with the ragged tail as the most common way to force the derived vector to 1. Kept the ragged-tail example as the minimal repro but the boundary note (`<details>`) already covers the full-tile cw=8 face; both are now RUN-confirmed this session.\n- It is **operator-general** across `T.copy` and `T.async_copy` (both hit line 804) and **dtype/scope-general** (fp32/fp16/int8, shared and fragment destinations) — one root, reachable through every data-movement op that annotates `coalesced_width`.\n- **Distinct-adjacent bug (not filed here).** `T.Parallel(..., coalesced_width=<python int>)` hits the *other* `LOG(FATAL)` at [line 808](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L808) (\"coalesced_width should be an IntImmNode\") **unconditionally, on any geometry** — the raw Python int stored by [loop.py:80](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/loop.py#L80) is never wrapped to an `IntImm`, so it never reaches the divisibility check. This is a *separate* root (missing type coercion on the `T.Parallel` frontend path, not the divisibility guard) and would be reported separately; noted here as the adjacent sibling this sweep confirmed. `T.copy`/`T.async_copy` do not hit it because their op machinery wraps the int into an `IntImm` before it reaches the planner.\n- **Example-run result (PART 1).** The only in-repo `coalesced_width` consumers are the AMD flash-attention examples/tests. Their `T.copy(... coalesced_width=8/4)` on full contiguous head-dim-wide (128-elem fp16) tiles was run standalone on 0.1.13 and all PASS (see Reach) — they **dodge** the abort because their derived vector ≥ requested width. No shipped example/test exercises `coalesced_width` on a ragged or under-vectorizable region, so the branch is uncovered. No NEW bug surfaced while sweeping (the `T.Parallel` non-IntImm abort is the pre-noted distinct sibling).", "url": "https://github.com/tile-ai/tilelang/issues/3007"}
{"number": 3017, "title": "[BUG][Fuzzer][ice-on-valid-code] Dynamic-shape output placed before the input supplying its symbolic dim crashes at call time (`IndexError`) instead of allocating the output", "state": "closed", "created_at": "2026-08-17T03:05:52Z", "updated_at": "2026-09-21T17:42:28Z", "closed_at": "2026-09-21T17:42:28Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13 / PyTorch 2.8 / CUDA 12.8, single NVIDIA L40S (sm_89). Backend `tvm_ffi` (the default) and `cython` both affected.\n\n### Problem description\n\nA `@tilelang.jit` kernel whose **output** tensor has a symbolic (dynamic) dimension crashes at call time with `IndexError: list index out of range` whenever that output parameter appears in the signature *before* the input parameter that supplies the symbolic size. The kernel compiles fine; the crash is in the host wrapper that allocates the output.\n\nThe size resolution assumes every symbolic output dim is backed by an *already-allocated* tensor. The wrapper builds its tensor list in parameter order in a single pass, so when the output comes first it looks up a slot that has not been filled yet.\n\nThe precise root is in `_process_dynamic_symbolic`: it records each symbolic dim against the **first parameter (in signature order) whose buffer shape mentions it**, and does **not exclude output buffers**. When the output is the first param to mention the dim, the recorded owner *is the output itself*, and the allocation loop then reads the output's own not-yet-appended slot (`tensor_list[ref_tensor_idx]` where `ref_tensor_idx` is the output's own index). It is a self-reference, not (as one might assume) a reference to a later input. Verified by inspecting the map: for `main(B(N,), A(N,))` with `out_idx=[0]` the map is `{'N': (0, 0, 0, 1)}` — owner index 0 = B, the output.\n\nThe same kernel with the output parameter placed *after* the input (the shape it copies from) works correctly, which is why every shipped example — and the dynamic-symbolic test, which uses `out_idx=[-1]` — dodges it.\n\n<details><summary>Console output (repro below): crash vs working control</summary>\n\n```\noutput-before-input: CRASH IndexError: list index out of range\noutput-after-input : PASS\n```\n\nTraceback for the crashing case:\n\n```\n  File \".../tilelang/jit/adapter/tvm_ffi.py\", line 255, in func\n    shape.append(tensor_list[ref_tensor_idx].shape[ref_shape_idx])\n                 ~~~~~~~~~~~^^^^^^^^^^^^^^^^\nIndexError: list index out of range\n```\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\nN = T.symbolic(\"N\")\n\n# out_idx=[0]: output B is param 0, input A is param 1.\n# B's shape (N,) is resolved from a LATER input param -> crash.\n@tilelang.jit(out_idx=[0])\ndef out_first():\n    @T.prim_func\n    def main(B: T.Tensor((N,), \"float32\"), A: T.Tensor((N,), \"float32\")):\n        with T.Kernel(1, threads=128):\n            for i in T.Parallel(N):\n                B[i] = A[i] + T.float32(9)\n    return main\n\n# Control: same kernel, output B placed AFTER input A.\n@tilelang.jit(out_idx=[1])\ndef out_last():\n    @T.prim_func\n    def main(A: T.Tensor((N,), \"float32\"), B: T.Tensor((N,), \"float32\")):\n        with T.Kernel(1, threads=128):\n            for i in T.Parallel(N):\n                B[i] = A[i] + T.float32(9)\n    return main\n\na = torch.randn(64, device=\"cuda\")\nref = a.double() + 9\n\ntry:\n    b = out_first()(a)\n    print(\"output-before-input:\", \"PASS\" if torch.allclose(b.double(), ref, atol=1e-4) else \"FAIL\")\nexcept Exception as e:\n    print(f\"output-before-input: CRASH {type(e).__name__}\")   # -> IndexError\n\nb = out_last()(a)\nprint(\"output-after-input :\", \"PASS\" if torch.allclose(b.double(), ref, atol=1e-4) else \"FAIL\")  # -> PASS\n```\n\n### Traceback\n\n```\nTraceback (most recent call last):\n  File \".../tilelang/jit/kernel.py\", line 204, in __call__\n    return self.torch_function(*args, **kwds)\n  File \".../tilelang/jit/adapter/tvm_ffi.py\", line 255, in func\n    shape.append(tensor_list[ref_tensor_idx].shape[ref_shape_idx])\n                 ~~~~~~~~~~~^^^^^^^^^^^^^^^^\nIndexError: list index out of range\n```\n\n### Expected behavior\n\nThe kernel should allocate the output and run regardless of where the output parameter sits in the signature — as it already does when the output is placed last. `out_idx` is documented as \"Index(es) of the output tensors to return\" with no ordering restriction, so an output at index 0 whose dim is shared with a later input is a legal signature that the compiler already compiles; only the runtime output-allocation step fails on it.\n\n### Additional context\n\n**Root cause.** The symbolic-dim owner recording does not exclude output buffers, so when the output is the first parameter to name a shared dim it records itself as that dim's owner, and the output-allocation loop then reads the output's own not-yet-appended slot.\n\n<details><summary>Mechanism</summary>\n\nTwo-level:\n\n- **Source root (where the wrong index is recorded).** [`_process_dynamic_symbolic`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/tvm_ffi.py#L145-L175) records each symbolic shape dim against the **first parameter, in signature order, whose buffer shape mentions it** — [`dynamic_symbolic_map[shape] = (0, i, j, 1)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/tvm_ffi.py#L166), gated only by `shape not in dynamic_symbolic_map` (first-wins) — with **no exclusion of `result_idx` (output) parameters**. So if the output is the first param to name the dim, the owner index it stores is the output's own index.\n- **Consumption root (where it crashes).** In [`_convert_torch_func.func`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/tvm_ffi.py#L224-L282) the allocation loop walks parameters in order and for an output param resolves each symbolic dim via [`tensor_list[ref_tensor_idx].shape[ref_shape_idx]`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/tvm_ffi.py#L255). `tensor_list` only holds the params processed *before* the current one, so a `ref_tensor_idx` at-or-after the current output — including the **self-reference** where the owner is the output itself — indexes past the end and raises `IndexError`.\n\nThe self-reference case is the common one: whenever the output precedes every input that shares the dim, `_process_dynamic_symbolic` points the dim at the output's own slot.\n</details>\n\n<details><summary>Why the control passes, and why it hits both backends</summary>\n\nWhen the output is last (`out_idx=[-1]`, the form used everywhere in `examples/`/`testing/`), an input already names the shared dim, so `_process_dynamic_symbolic` records that input as the owner; by the time the output is allocated the input is already in `tensor_list`, so the lookup succeeds. The `cython` backend carries the identical single-pass resolution at [`cython_wrapper.pyx#L202`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/cython/cython_wrapper.pyx#L202) and crashes the same way (tested: `IndexError` on the repro kernel with `execution_backend=\"cython\"`).\n</details>\n\n**Suggested fix.** Two independent points would each close it: (1) in `_process_dynamic_symbolic`, prefer a non-output (`result_idx`) parameter as the recorded owner of a shape dim, so a shared dim is always bound to an input that is populated before any output; and/or (2) in the allocation loop of both adapters, split into two passes (bind all inputs first, then allocate outputs) so `tensor_list[ref_tensor_idx]` is always populated, or resolve output shapes from a position-keyed inputs map rather than the `tensor_list` under construction. Both adapters carry the same allocation-loop shape (not verified end-to-end).\n\n**Provenance.** The single-pass `tensor_list[ref_tensor_idx]` resolution plus the output-inclusive owner recording are present in both adapters at the 0.1.13 release; I did not pin the introducing PR. Not established as a regression of previously-correct behavior.\n\n**Dedup.** Searched the open and closed tracker and found no existing report of this defect.\n\n**Tested boundary.** The trigger is exactly whether the **recorded owner index** of a symbolic dim (the first buffer param, in order, that mentions it — recorded in [`_process_dynamic_symbolic`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/tvm_ffi.py#L145-L175)) is >= the index of the output being allocated. Because output buffers are not excluded from that recording, the common case is a **self-reference**: the output is the first param to name the dim. Cells run on 0.1.13/L40S (full sweep in the Generalization table below): (a) 2-param output-first (`out_idx=[0]`) — `IndexError`, map `N:(0,0,0)` (owner = the output itself); (b) same kernel `execution_backend=\"cython\"` — identical `IndexError` (`cython_wrapper.pyx:202`); (c) 3-param middle output (`out_idx=[1]`) whose dim is owned by input idx 0 (processed first) — runs correctly, map `N:(0,0,0)`. So (a)/(b) and (c) share one root; the boundary is owner-index-vs-output-index, not the output's absolute position.\n\n**Generalization — two-level root.** (a) *Source-level:* `_process_dynamic_symbolic` (`tvm_ffi.py:161-166`, twin in `cython_wrapper.pyx`) records a symbolic dim against the first buffer param that mentions it, first-wins, **not excluding output (`result_idx`) buffers**. (b) *Operator-level:* every path that returns a symbolic-shaped output through the same allocation loop — single output, multi-output, any output dtype/rank — since the symbol resolution runs before the output tensor is created and is dtype/rank-agnostic. 4-axis sweep, all cells run on 0.1.13 / L40S:\n\n| axis | cell tested (input) | observed result | same-root? |\n|---|---|---|---|\n| repro (baseline) | `out_idx=[0]`, `main(B(N,),A(N,))`, call `(a[64])` | `IndexError` @ `tvm_ffi.py:255`; map `N:(0,0,0)` (owner=output) | yes |\n| related-source | same kernel, `execution_backend=\"cython\"` | `IndexError` @ `cython_wrapper.pyx:202` | yes (identical logic, twin file) |\n| related-source | `out_idx=[1]`, `main(X(8,)static, B(M,)out, A(M,)in)` — var first named by output | `IndexError`; map `M:(0,1,0)` (owner=output idx1, **self-ref**, not the later input idx2) | yes — sharpens root to self-reference |\n| related-operator | multi-output `out_idx=[0,1]`, `main(B(N,),C(N,),A(N,))` | `IndexError` | yes |\n| related-operator | scalar-owner: `out_idx=[0]`, `main(B(N,), n:int32, A(N,))` | `IndexError` @ 255 — N still recorded as buffer-shape (ref_id 0) against output B, NOT the scalar (ref_id 2); scalar `ref_id==2` branch (`inputs[...]`) never reached | yes (same shape-loop path) |\n| related-type | 2D int8 output `out_idx=[0]`, `main(B(M,4)int8, A(M,4)int8)` | `IndexError` | yes (resolution precedes tensor creation; dtype/rank orthogonal) |\n| control (dodge) | `out_idx=[1]`, `main(A(N,)in, B(N,)out)` — input names dim first | PASS, correct value | n/a (owner idx0 < output idx1) |\n| control (dodge) | 3-param `out_idx=[1]`, `main(A(N,)in idx0, B out, C in)` | PASS | n/a (owner idx0 processed first) |\n\n**Reframe.** Kept the specific title/framing but the Problem now names the class: *output is the first param to name a shared symbolic dim* (subsumes both \"output before its input\" and the self-reference case). The `ref_id==1` stride branch (`tvm_ffi.py:257`, `cython_wrapper.pyx` twin) is code-identical to the crashing shape branch and shares the exact `tensor_list[ref_tensor_idx]` lookup, so it is **same-root by inspection** (symbolic output strides are uncommon and awkward to express via `T.Tensor`, not run here). No distinct-adjacent bug and no new bug surfaced during the sweep. The scalar-owner cell revealed the draft's original \"later input\" narrative was imprecise — the recorded owner is a *buffer* (here the output itself), and the `ref_id==2` scalar branch that reads the fully-available `inputs` list is not the crashing path.\n\n**Impact.** The trigger is narrow: a dynamic-shape kernel whose symbolic output parameter is the first param to name a shared dim (e.g. declared before the input it borrows the dim from) — an ordering the shipped `out_idx=[-1]` convention never uses. When it fires it is a deterministic call-time `IndexError` in the host output-allocation wrapper: the kernel never runs, nothing is computed, and the failure is loud and immediate rather than a silent wrong value, so it cannot reach a workload's results undetected. Fixing it closes an output-parameter-ordering class that `out_idx` documents as legal but the runtime allocator rejects, aligning the two adapters' allocation path with what the compiler already accepts.\n\n**Reach.** (Run, not asserted.) The trigger is a legal signature (`out_idx` is documented with no ordering constraint) with two ingredients: a **symbolic output dimension** shared with an input (common in dynamic-shape kernels) and the output being the **first parameter to name that dim** (e.g. placed before the input). Placing the output last is the overwhelmingly common convention. I ran the shipped citations on 0.1.13/L40S: the dynamic-symbolic test `testing/python/profiler/test_tilelang_profiler_dynamic_symbolic.py` (`out_idx=[-1]`) — **11 passed** (output C last, dim owned by input A idx 0, dodges); the canonical `examples/dynamic_shape/example_dynamic.py` (eager `C = T.empty((M,N),...)`, `out_idx=[-1]`) — **runs, matches torch** (output constructed after inputs, dodges). A grep of `examples/` at v0.1.13 found no `out_idx=[0]` and no symbolic kernel whose output precedes its owning input; the only symbolic example with a non-trailing out_idx is `deepseek_v32/inference/kernel.py` (`out_idx=[4]`, output after its inputs, dodges). So no shipped example or test exercises the output-first ordering that trips it, which is why CI is green.", "url": "https://github.com/tile-ai/tilelang/issues/3017"}
{"number": 3019, "title": "[BUG][Fuzzer][ice-on-valid-code] A plain `assert` in a kernel body emits a host-only symbol into device code and aborts nvcc instead of a device-legal assertion", "state": "closed", "created_at": "2026-08-17T03:06:06Z", "updated_at": "2026-09-21T17:41:43Z", "closed_at": "2026-09-21T17:41:43Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (+1 or comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (also reproduces on 0.1.9).\n\n### System information\n\nTileLang 0.1.13 / CUDA 12.x / nvcc `sm_89` (NVIDIA L40S). The mechanism is not arch-gated (it is a codegen text emission), so any CUDA target is affected.\n\n### Problem description\n\nA plain Python `assert` written inside a kernel body compiles to a kernel that **fails to build**: nvcc aborts with `error: identifier \"TVMFFIErrorSetRaisedFromCStrParts\" is undefined`. `assert cond, \"msg\"` is a first-class TVMScript statement — the parser maps it to `T.Assert`/`tir.AssertStmt` — so this is legal input, but the emitted `.cu` contains a host-only error-reporting call inside a `__global__` function.\n\nThe emitted device code for `assert i < 64, \"oob thread\"`:\n\n```cuda\nextern \"C\" __global__ void __launch_bounds__(64, 1) main_kernel(const float* __restrict__ A, float* __restrict__ C) {\n    if (!((bool)1)) {\n      const char* __tvm_assert_parts[1] = {\"oob thread\"};\n      TVMFFIErrorSetRaisedFromCStrParts(\"RuntimeError\", __tvm_assert_parts, 1);   // host-only symbol, undefined in device code\n      return -1;                                                                  // and returns int from a void kernel\n    }\n    C[((int)threadIdx.x)] = A[((int)threadIdx.x)];\n}\n```\n\n`TVMFFIErrorSetRaisedFromCStrParts` is a host-side TVM-FFI runtime symbol with no `__device__` definition, and `return -1` returns a value from a `void` kernel. Both are illegal in device code. A bare `assert i < 64` (no message) behaves identically — the parser attaches a default `\"Assertion failed\"` message part, so it also takes this branch. On 0.1.9 the same path emitted `ICHECK((bool)1) << \"oob thread\";` (a host GLOG macro) and aborted with `error: identifier \"ICHECK\" is undefined`; the symbol changed with the tvm-ffi migration, the defect did not. Not a regression — see Provenance.\n\n<details><summary>Reproduced on 0.1.13 and 0.1.9; controls compile</summary>\n\n```\n# 0.1.13, repro (assert with message):\ntvm_kernels.cu(21): error: identifier \"TVMFFIErrorSetRaisedFromCStrParts\" is undefined\n\n# 0.1.9, repro:\ntvm_kernels.cu(13): error: identifier \"ICHECK\" is undefined\n    ICHECK((bool)1) << \"oob thread\";\n\n# CONTROL A (drop the assert)               -> compiles OK, no host symbol in source\n# CONTROL B (T.device_assert(i<64,\"...\"))   -> compiles OK, no host symbol in source\n```\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\n\n@T.prim_func\ndef main(A: T.Tensor((64,), \"float32\"), C: T.Tensor((64,), \"float32\")):\n    with T.Kernel(1, threads=64) as bx:\n        i = T.get_thread_binding(0)\n        assert i < 64, \"oob thread\"     # TVMScript T.Assert -> tir.AssertStmt\n        C[i] = A[i]\n\ntilelang.compile(main, out_idx=[-1])     # nvcc: identifier \"TVMFFIErrorSetRaisedFromCStrParts\" is undefined\n\n# CONTROL: the intended device-assert path compiles fine, so a device assertion IS achievable:\n@T.prim_func\ndef ok(A: T.Tensor((64,), \"float32\"), C: T.Tensor((64,), \"float32\")):\n    with T.Kernel(1, threads=64) as bx:\n        i = T.get_thread_binding(0)\n        T.device_assert(i < 64, \"oob thread\")\n        C[i] = A[i]\n\ntilelang.compile(ok, out_idx=[-1])       # -> compiles OK\n```\n\n### Traceback\n\n```\nRuntimeError: ... Compilation error:\ntvm_kernels.cu(21): error: identifier \"TVMFFIErrorSetRaisedFromCStrParts\" is undefined\n      TVMFFIErrorSetRaisedFromCStrParts(\"RuntimeError\", __tvm_assert_parts, 1);\n      ^\n```\n\n### Expected behavior\n\nA kernel-body `assert` should either lower to a device-legal assertion (the same outcome `T.device_assert` already produces, which compiles and runs) or be rejected with a clear frontend diagnostic — rather than emitting a host-only symbol that aborts nvcc with an `undefined identifier` error deep in generated code. The CUDA codegen already has a device-legal fallback for the message-less case (`assert(cond);`), and `T.device_assert` demonstrates the device path works, so emitting a runnable assertion appears achievable.\n\n### Additional context\n\n**Root cause.** TileLang's CUDA codegen does not handle `AssertStmt` for device output, so it inherits the base `CodeGenC::VisitStmt_(AssertStmtNode)`, which emits a **host** error-reporting call into a `__global__` function. [`CodeGenTileLangCUDA`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.h#L23) extends `CodeGenC` and does not override `VisitStmt_(AssertStmtNode)`. When an assert carries a message (always true for a TVMScript `assert`), the inherited [`VisitStmt_(const AssertStmtNode*)`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L1176) takes the `num_parts > 0` branch and streams `TVMFFIErrorSetRaisedFromCStrParts(...); return -1;` — host-only constructs — instead of a `__device__`-legal form. Only the message-less `else` branch emits a device-legal `assert(cond);`, and that branch is never reached from the frontend.\n\n**Suggested fix.** Override `VisitStmt_(AssertStmtNode)` in `CodeGenTileLangCUDA` to route to the same device path `T.device_assert` uses (or emit a plain device `assert(cond)`), so a kernel-body `assert` lowers to a runnable check. Alternatively, reject `assert`-with-message at the frontend with a message pointing users at `T.device_assert` (not verified end-to-end).\n\n**Provenance.** Inherited-upstream, not a TileLang regression: the device emission lives in the Apache-TVM-derived `codegen_c.cc` vendored via the `tile-ai/tvm` submodule, and it is unguarded for device targets across the releases tested. On 0.1.9 the branch emitted a GLOG `ICHECK(...)`; the current tvm-ffi form (`TVMFFIErrorSetRaisedFromCStrParts`) replaced it but is equally host-only. TileLang's own CUDA codegen inheriting it without a device override is what exposes it.\n\n**Dedup.** I searched the open and closed tracker (`AssertStmt`, `ICHECK`, `TVMFFIErrorSetRaisedFromCStrParts`, `assert in kernel`) and found no existing report of this defect.\n\n**Tested boundary (input family).** Cells run on 0.1.13/nvcc sm_89: (a) `assert i < 64, \"oob thread\"` — nvcc `error: identifier \"TVMFFIErrorSetRaisedFromCStrParts\" is undefined` at `tvm_kernels.cu(21)` plus a `return value type does not match` error at line 22; (b) bare `assert i < 64` (no message) — identical `TVMFFIErrorSetRaisedFromCStrParts` error, because the parser attaches a default message part so it still takes the `num_parts > 0` branch; (c) `T.device_assert(i < 64, \"oob thread\")` — compiles OK. So (a)/(b) are one class (any kernel-body `assert` hits the host-only branch, message or not); (c) is the working device path the fix should route to.\n\n**Reach.** The trigger is a documented TVMScript statement: the TVMScript parser maps a Python `assert` to `T.Assert`/`tir.AssertStmt`, and `T.Assert(condition, message)` is a public IR-builder API (verified: using `with T.Assert(i < 64, \"...\"):` directly hits the identical `TVMFFIErrorSetRaisedFromCStrParts` error, so this is not a parser artifact but the `AssertStmt` node itself). The related device-assert idiom is exercised in the repo (`T.device_assert` in `testing/python/debug/test_device_assert.py`, `testing/python/language/test_tilelang_language_frontend_v2.py:263`, `testing/python/issue/test_tilelang_issue_2307.py`, and `examples/gemm_sp/sparse_utils.py`). I ran two of these verbatim on 0.1.13 — `test_device_assert.py` (`1 passed`) and `test_tilelang_issue_2307.py` (`1 passed`) — and both compile fine because they use `T.device_assert`, which lowers to a `tir.Call` intrinsic (`device_assert`/`device_assert_with_msg`) that `CodeGenTileLangCUDA` explicitly handles (device-legal `device_assert(cond)` at [`codegen_cuda.cc#L4622`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4622)). So these shipped tests DODGE the bug: they exercise the sibling operator on its own (working) code path, never `tir.AssertStmt`. No shipped test uses a plain `assert` inside a kernel body, which is why the `AssertStmt` device path is untested and this went unnoticed.\n\n**Impact.** The trigger is narrow — a plain kernel-body `assert` rather than `T.device_assert` — and it fails loudly at nvcc compile time with an `undefined identifier` error, so nothing is silently miscomputed and no wrong value can reach a workload; the sole cost is that this valid kernel refuses to build (with a confusing generated-code error rather than a clear diagnostic). Fixing it closes a boundary class by making the plain `assert` path either lower to the working device assertion `T.device_assert` already produces or be rejected with a clear frontend message.\n\n**Generalization (root + 4-axis sweep, all cells RUN on 0.1.13 / nvcc sm_89 unless noted).**\n\n_Two-level root._\n- **SOURCE-level root:** `CodeGenC::VisitStmt_(const AssertStmtNode*)` at [`codegen_c.cc#L1176`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L1176) (bundled `tile-ai/tvm`). The `num_parts > 0` branch streams `TVMFFIErrorSetRaisedFromCStrParts(...); return -1;` — host-only. `CodeGenTileLangCUDA` ([`codegen_cuda.h#L23`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.h#L23)) does **not** override `VisitStmt_(AssertStmtNode)`, so it inherits this host path for device output. Notably the sibling backend `CodeGenTileLangPY` (used by CuTeDSL) *does* override it at `src/cuda/codegen/codegen_py.cc:573`, emitting a legal Python `assert cond, \"msg\"` — so the CUDA backend is the one missing the guard.\n- **OPERATOR-level root:** the `tir.AssertStmt` node. Any frontend construct that produces it (`assert`, `T.Assert`) breaks; the adjacent operator `T.device_assert` produces a `tir.Call` intrinsic instead, which has an explicit CUDA-codegen handler and stays device-legal. The class boundary is *statement-node (`AssertStmt`) vs call-intrinsic (`device_assert`)*, not \"message vs no message\".\n\n_4-axis sweep._\n\n| axis | cell tested | input | observed result | same-root? |\n|---|---|---|---|---|\n| similar-logic | assert with a genuinely runtime (non-const-foldable) condition | `assert A[i] < 1.0, \"data-dep\"` | nvcc `error: identifier \"TVMFFIErrorSetRaisedFromCStrParts\" is undefined` + `return -1` type error | same root (rules out const-fold artifact — the branch is emitted for real runtime conds) |\n| similar-logic | other host-only FFI emissions in same file (`TVMFFIAny`/`TVMValue` packed-call ABI, `codegen_c.cc:336-375`) | source read only | reachable only via packed-func-call builtins, not from a normal device kernel statement | distinct reachability (same fragile file, NOT filed) |\n| related-source | `CodeGenTileLangPY::VisitStmt_(AssertStmtNode)` (CuTeDSL backend) | source read (`codegen_py.cc:573`) | HAS a correct override → legal `assert cond, \"msg\"`; no frontend selector exposed in the pip package to run it | shows the CUDA gap is the anomaly; not run |\n| related-operator | `T.Assert` IR-builder API directly | `with T.Assert(i < 64, \"oob via T.Assert\"):` | identical `TVMFFIErrorSetRaisedFromCStrParts` error | same root (confirms node-level, not parser-level) |\n| related-operator | `T.device_assert` WITH message (working sibling) | `T.device_assert(i < 64, \"oob thread\")` | `COMPILED_OK` | distinct path (the intended fix target) |\n| related-operator | `T.device_assert` WITHOUT message, runtime cond | `T.device_assert(A[i] < 1.0)` | `COMPILED_OK` | distinct path (robust across message/no-message) |\n| related-type | bare `assert` with NO message | `assert i < 64` | identical `TVMFFIErrorSetRaisedFromCStrParts` error | same root (parser attaches a default message part → still `num_parts>0`; the device-legal `else`-branch `assert(cond);` is never reached from the frontend) |\n\n_Class verdict._ All `AssertStmt`-producing forms (`assert` with/without message, `T.Assert`, const or runtime condition) collapse to ONE root: the missing CUDA-backend `AssertStmt` override. `T.device_assert` (call-intrinsic) is a genuinely distinct, working code path and is the correct fix destination. The other host-only FFI emissions in `codegen_c.cc` share the same \"inherited unguarded base visitor\" *shape* but are not reachable from a device kernel — recorded as distinct, NOT filed. No NEW bug found while sweeping. Title/Problem kept scoped to kernel-body `assert`/`T.Assert` since that is the exact reachable class.\n\n_Example-run (PART 1) result._ The three cited example/test sites all use `T.device_assert`, so they DODGE this bug. Ran verbatim on 0.1.13: `testing/python/debug/test_device_assert.py` → `1 passed`; `testing/python/issue/test_tilelang_issue_2307.py` → `1 passed` (this one only asserts on generated-source text, not an nvcc build, but its kernel also uses `device_assert`). This weakens \"Reach\" to: the *bug itself* has no shipped example — but that is precisely why the `AssertStmt` device path is untested; the working-sibling idiom `T.device_assert` is well-exercised, which is the fix target.", "url": "https://github.com/tile-ai/tilelang/issues/3019"}
{"number": 3259, "title": "[BUG] NVRTC fails to compile warp reductions", "state": "closed", "created_at": "2026-09-21T01:48:22Z", "updated_at": "2026-09-21T16:16:01Z", "closed_at": "2026-09-21T16:16:01Z", "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.14+cuda.giteab74a4a\n\n### System information\n\nWindows, Python 3.12.13, PyTorch 2.11.0+cu130, NVRTC 13.0, SM120.\n\n### Problem description\n\n`T.warp_reduce_sum` fails with `execution_backend=\"nvrtc\"`. The NVRTC shim lacks `std::is_integral_v`, which `reduce.h` uses. After adding the trait, its explicit-template lambda also fails under NVRTC's default C++17 dialect.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\n\n@T.prim_func\ndef main(A: T.Tensor((32,), T.float32), O: T.Tensor((32,), T.float32)):\n    with T.Kernel(1, threads=32):\n        tid = T.get_thread_binding()\n        O[tid] = T.warp_reduce_sum(A[tid])\n\n\ntilelang.compile(main, execution_backend=\"nvrtc\")\n```\n\n\n### Traceback\n\n```pytb\nreduce.h(370): error: namespace \"std\" has no member \"is_integral_v\"\n\nAfter adding the trait:\n\n\nreduce.h(352): error: explicit lambda template parameters are a C++20 feature\n```\n\n### Expected behavior\n\nCompile and execute warp reductions with the default NVRTC options, as with NVCC.\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/3259"}
{"number": 3263, "title": "[BUG] NVRTC wrapper omits host-computed scalar kernel arguments", "state": "open", "created_at": "2026-09-21T06:53:39Z", "updated_at": "2026-09-21T16:09:32Z", "closed_at": null, "labels": [], "comments_count": 3, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n`0.1.14+cuda.git6ba187e2` (source checkout).\n\nInitially reproduced at `eab74a4a`; the affected wrapper is unchanged in `6ba187e2`.\n\n### System information\n\n- Installation: source build with CUDA enabled, CMake/Ninja.\n- OS: Windows x64 (`win32`).\n- Python: 3.12.13, 64-bit.\n- PyTorch: `2.11.0+cu130`.\n- NVRTC: 13.0.\n\n### Problem description\n\nThe NVRTC wrapper can omit scalar kernel arguments computed on the host after host/device splitting.\n\nFor valid host IR such as:\n\n```python\np = n * 3 + 1\nq = p * 2 + 5\nT.call_packed(\"main_kernel\", A, B, p, q, 128)\n```\n\nthe device signature is `main_kernel(A, B, p, q)`, but the generated NVRTC dispatcher contains only:\n\n```python\narg_values = A.data_ptr(), B.data_ptr()\narg_types = ctypes.c_void_p, ctypes.c_void_p\n```\n\n`SplitHostDevice` correctly captures the prepared values. The wrapper subsequently matches CUDA parameter names against the original function's arguments, where `p` and `q` do not exist. The existing TVM-FFI path executes the same host preparation correctly.\n\nThis may have gone unnoticed because ordinary compilation inlines this example's arithmetic into the kernel before `SplitHostDevice`: `A[i] + p + q` becomes `A[i] + 9 * n + 8`, leaving only the original scalar parameter `n` to forward. This happens before the NVRTC adapter; writing the bindings outside `T.Kernel` does not prevent it.\n\nThe reproducer preserves the host bindings to exercise the adapter boundary. It does not demonstrate a failure through the default compilation pipeline. Launch-time hoisting such as #3261 could expose this gap if the computed values remain on the host and become additional kernel arguments.\n\n\n### Reproducible example code\n\nRequires a CUDA-enabled TileLang source build. This inspects generated source without launching the incomplete argument list.\n\n```python\nimport tilelang as tl\nimport tilelang.language as T\nfrom tilelang import tvm\nfrom tilelang.backend.module import create_backend_context\nfrom tilelang.engine.lower import device_codegen_without_compile, get_device_call, get_host_call\nfrom tilelang.jit.adapter.nvrtc.wrapper import TLNVRTCSourceWrapper\n\n\n@T.prim_func\ndef main(A: T.Tensor((128,), \"int32\"), B: T.Tensor((128,), \"int32\"), n: T.int32):\n    p = T.bind(n * 3 + 1)\n    q = T.bind(p * 2 + 5)\n    with T.Kernel(1, threads=128):\n        i = T.get_thread_binding()\n        B[i] = A[i] + p + q\n\n\nctx = create_backend_context({\"kind\": \"cuda\", \"arch\": \"sm_80\"}, \"c\", \"nvrtc\")\noriginal = tvm.IRModule({\"main\": main})\n# Preserve host bindings to exercise the adapter boundary.\nmod = tvm.transform.Sequential(\n    [\n        tvm.tirx.transform.BindTarget(ctx.target),\n        tl.transform.MaterializeKernelLaunch(),\n        tl.transform.LowerOpaqueBlock(),\n        tl.transform.AnnotateDeviceRegions(),\n        tl.transform.SplitHostDevice(),\n        tvm.tirx.transform.AnnotateEntryFunc(),\n        tl.transform.MakePackedAPI(),\n        tl.transform.LowerDeviceKernelLaunch(),\n    ]\n)(original)\nhost = tvm.tirx.transform.Filter(get_host_call())(mod)\ndevice = tvm.tirx.transform.Filter(get_device_call())(mod)\nsource = device_codegen_without_compile(device, ctx).inspect_source()\nwrapper = TLNVRTCSourceWrapper(original, source, ctx.target, device_mod=device, host_mod=host)\nprint(source)\nprint(wrapper.host_func)\n```\n\n\n### Traceback\n\nNo traceback is produced by the source-generation reproducer. The malformed dispatcher is inspected, not executed with an incomplete CUDA argument list.\n\n### Expected behavior\n\nResolve launch operands from the actual host call, preserve the required scalar bindings and their execution scope, and marshal every device argument in signature order. Unsupported host computations should produce an explicit diagnostic rather than an incomplete CUDA argument list.\n\n\n### Additional context\n\nThe existing TVM-FFI path and a separate IR-driven NVRTC prototype passed changing-input and guarded-launch checks on the same host/device program.\n\nThis differs from #2755/#2756, which concerned forwarding original scalar parameters and dynamic strides. The missing values here are introduced by host preparation and are not original function parameters. Related preparation work is discussed in #3261.\n", "url": "https://github.com/tile-ai/tilelang/issues/3263"}
{"number": 2308, "title": "[Feature Request] zero-cost expression-list indexing inside `T.unroll`", "state": "closed", "created_at": "2026-05-29T14:41:48Z", "updated_at": "2026-09-21T05:28:11Z", "closed_at": "2026-09-21T05:28:11Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "## Summary\n\nI would like to request syntax support for small expression lists that can be indexed inside `T.unroll` and lowered as scalar expressions, without materializing `T.alloc_local` storage.\n\nThe motivating pattern is:\n\n```python\nk0 = k_block_base + nj * 16 + tg * 2\nks = [k0, k0 + 1, k0 + 8, k0 + 9]\n\nfor i in T.unroll(4):\n    value = X[row, ks[i]]\n```\n\nToday, plain Python lists do not work because the unrolled index is represented as a TIR `Var`:\n\n```text\nTypeError: list indices must be integers or slices, not Var\n```\n\nThe current workaround is to use `T.alloc_local((4,), \"int32\")`, but that is not zero-cost and can lead to noticeably worse generated code/performance in MMA-heavy kernels.\n\n### Minimal Repro\n\nSelf-contained PoC:\n\n```bash\nuv run python alloc_local_poc.py \\\n  --experiment dense-lora \\\n  --rows 4608 \\\n  --k 3840 \\\n  --rank 128 \\\n  --warmup 5 \\\n  --rep 20 \\\n  --json\n```\n\nThe PoC compares two equivalent TileLang kernels for a synthetic LoRA-down partial reduction:\n\n```text\nOut[M, R] += X[M, K] @ W[K, R]\n```\n\nBoth versions use the same `m16n8k16` MMA and atomic-add epilogue. The only relevant difference is:\n\n- scalar version: explicit `k0/k1/k2/k3`, `row0/row1`, `rank0/...`\n- alloc_local version: small `T.alloc_local` arrays indexed under `T.unroll`\n\n### Result\n\nOn my machine:\n\n```json\n{\n  \"correctness\": {\n    \"max_abs_err\": 3.0517578125e-05,\n    \"mean_abs_err\": 1.3322440395313606e-07,\n    \"pass\": true\n  },\n  \"timing\": {\n    \"scalar\": {\"p50_ms\": 0.332576, \"p90_ms\": 0.342048},\n    \"alloc_local\": {\"p50_ms\": 0.825344, \"p90_ms\": 0.835328},\n    \"alloc_local_over_scalar_p50\": 2.48167\n  }\n}\n```\n\nSo the `T.alloc_local` workaround is functionally correct, but substantially slower in this pattern.\n\n### Request\n\nIt would be useful if TileLang supported a zero-cost expression-list construct, such as:\n\n```python\nk0 = k_block_base + nj * 16 + tg * 2\nks = [k0, k0 + 1, k0 + 8, k0 + 9]\n```\n\nand when indexed by a `T.unroll` loop variable, lowered it as if the scalar expressions were written manually.\n\nExpected lowering:\n\n```python\nfor i in T.unroll(4):\n    X[row, ks[i]]\n```\n\nshould lower equivalently to hand-written scalar expressions, not to local array storage.\n\nThis is not necessarily a bug in `T.alloc_local`; rather, `T.alloc_local` is the wrong abstraction when the user wants compile-time expression aliases rather than actual local storage.\n```\n\n## Additional Context\n\n```python\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport logging\nimport statistics\nfrom pathlib import Path\nfrom typing import Any, Callable\n\nimport torch\nimport tilelang\nimport tilelang.language as T\n\nlogging.getLogger(\"tilelang\").setLevel(logging.WARNING)\nlogging.getLogger(\"tilelang.env\").setLevel(logging.ERROR)\nlogging.getLogger(\"tilelang.jit.kernel\").setLevel(logging.WARNING)\n\n\n@tilelang.jit\ndef clear_kernel(numel: int, block: int = 256):\n    @T.prim_func\n    def main(out: T.Tensor((numel,), \"float32\")):\n        with T.Kernel(T.ceildiv(numel, block), threads=block) as bx:\n            tx = T.get_thread_binding()\n            idx = bx * block + tx\n            if idx < numel:\n                out[idx] = T.Cast(\"float32\", 0.0)\n\n    return main\n\n\n@tilelang.jit\ndef lora_down_scalar_kernel(M: int, K: int, R: int):\n    block_M = 256\n    block_K = 128\n    block_R = 16\n    warp_M = 32\n    warp_M_tiles = 2\n    warp_K16_tiles = 8\n    threads = 256\n    dtype = \"bfloat16\"\n    accum_dtype = \"float32\"\n\n    num_bm = M // block_M\n    num_bk = K // block_K\n    num_br = R // block_R\n    if M % block_M != 0 or K % block_K != 0 or R % block_R != 0:\n        raise ValueError(f\"expected aligned M/K/R, got M={M}, K={K}, R={R}\")\n\n    @T.prim_func\n    def main(\n        X: T.Tensor((num_bm * block_M, num_bk * block_K), dtype),\n        W: T.Tensor((num_bk * block_K, num_br * block_R), dtype),\n        Out: T.Tensor((num_bm * block_M, num_br * block_R), \"float32\"),\n    ):\n        with T.Kernel(num_bm, num_bk, threads=threads) as (bm, bk):\n            tx = T.get_thread_binding()\n            lane = tx & 31\n            warp = tx >> 5\n            g = lane >> 2\n            tg = lane & 0x3\n            row_warp_base = bm * block_M + warp * warp_M\n            k_block_base = bk * block_K\n\n            a_regs = T.alloc_local((warp_M_tiles, 4), \"uint32\")\n            b_regs = T.alloc_local((4,), \"uint32\")\n            acc = T.alloc_local((warp_M_tiles * 8,), accum_dtype)\n\n            for br in T.unroll(num_br):\n                for ai in T.unroll(warp_M_tiles * 8):\n                    acc[ai] = T.Cast(accum_dtype, 0.0)\n                for nj in T.unroll(warp_K16_tiles):\n                    k0 = k_block_base + nj * 16 + tg * 2\n                    k1 = k0 + 1\n                    k2 = k0 + 8\n                    k3 = k2 + 1\n                    for mi in T.unroll(warp_M_tiles):\n                        row0 = row_warp_base + mi * 16 + g\n                        row1 = row0 + 8\n                        a_regs[mi, 0] = T.pack_b16(X[row0, k0], X[row0, k1])\n                        a_regs[mi, 1] = T.pack_b16(X[row1, k0], X[row1, k1])\n                        a_regs[mi, 2] = T.pack_b16(X[row0, k2], X[row0, k3])\n                        a_regs[mi, 3] = T.pack_b16(X[row1, k2], X[row1, k3])\n\n                    rank_low = br * block_R + g\n                    rank_high = rank_low + 8\n                    b_regs[0] = T.pack_b16(W[k0, rank_low], W[k1, rank_low])\n                    b_regs[1] = T.pack_b16(W[k2, rank_low], W[k3, rank_low])\n                    b_regs[2] = T.pack_b16(W[k0, rank_high], W[k1, rank_high])\n                    b_regs[3] = T.pack_b16(W[k2, rank_high], W[k3, rank_high])\n\n                    for mi in T.unroll(warp_M_tiles):\n                        off = mi * 8\n                        T.ptx_mma(\n                            \"float32\",\n                            \"m16n8k16\",\n                            \"row\",\n                            \"col\",\n                            dtype,\n                            dtype,\n                            \"float32\",\n                            a_regs.data,\n                            mi * 4,\n                            b_regs.data,\n                            0,\n                            acc.data,\n                            off,\n                            False,\n                        )\n                        T.ptx_mma(\n                            \"float32\",\n                            \"m16n8k16\",\n                            \"row\",\n                            \"col\",\n                            dtype,\n                            dtype,\n                            \"float32\",\n                            a_regs.data,\n                            mi * 4,\n                            b_regs.data,\n                            2,\n                            acc.data,\n                            off + 4,\n                            False,\n                        )\n\n                for mi in T.unroll(warp_M_tiles):\n                    row0 = row_warp_base + mi * 16 + g\n                    row1 = row0 + 8\n                    rank0 = br * block_R + tg * 2\n                    rank1 = rank0 + 1\n                    rank2 = rank0 + 8\n                    rank3 = rank2 + 1\n                    off = mi * 8\n                    T.atomic_add(Out[row0, rank0], acc[off + 0])\n                    T.atomic_add(Out[row0, rank1], acc[off + 1])\n                    T.atomic_add(Out[row1, rank0], acc[off + 2])\n                    T.atomic_add(Out[row1, rank1], acc[off + 3])\n                    T.atomic_add(Out[row0, rank2], acc[off + 4])\n                    T.atomic_add(Out[row0, rank3], acc[off + 5])\n                    T.atomic_add(Out[row1, rank2], acc[off + 6])\n                    T.atomic_add(Out[row1, rank3], acc[off + 7])\n\n    return main\n\n\n@tilelang.jit\ndef lora_down_alloc_local_kernel(M: int, K: int, R: int):\n    block_M = 256\n    block_K = 128\n    block_R = 16\n    warp_M = 32\n    warp_M_tiles = 2\n    warp_K16_tiles = 8\n    threads = 256\n    dtype = \"bfloat16\"\n    accum_dtype = \"float32\"\n\n    num_bm = M // block_M\n    num_bk = K // block_K\n    num_br = R // block_R\n    if M % block_M != 0 or K % block_K != 0 or R % block_R != 0:\n        raise ValueError(f\"expected aligned M/K/R, got M={M}, K={K}, R={R}\")\n\n    @T.prim_func\n    def main(\n        X: T.Tensor((num_bm * block_M, num_bk * block_K), dtype),\n        W: T.Tensor((num_bk * block_K, num_br * block_R), dtype),\n        Out: T.Tensor((num_bm * block_M, num_br * block_R), \"float32\"),\n    ):\n        with T.Kernel(num_bm, num_bk, threads=threads) as (bm, bk):\n            tx = T.get_thread_binding()\n            lane = tx & 31\n            warp = tx >> 5\n            g = lane >> 2\n            tg = lane & 0x3\n            row_warp_base = bm * block_M + warp * warp_M\n            k_block_base = bk * block_K\n\n            a_regs = T.alloc_local((warp_M_tiles, 4), \"uint32\")\n            b_regs = T.alloc_local((4,), \"uint32\")\n            acc = T.alloc_local((warp_M_tiles * 8,), accum_dtype)\n            rows = T.alloc_local((2,), \"int32\")\n            ks = T.alloc_local((4,), \"int32\")\n            ranks = T.alloc_local((4,), \"int32\")\n\n            for br in T.unroll(num_br):\n                for ai in T.unroll(warp_M_tiles * 8):\n                    acc[ai] = T.Cast(accum_dtype, 0.0)\n                for nj in T.unroll(warp_K16_tiles):\n                    ks[0] = k_block_base + nj * 16 + tg * 2\n                    ks[1] = ks[0] + 1\n                    ks[2] = ks[0] + 8\n                    ks[3] = ks[2] + 1\n                    for mi in T.unroll(warp_M_tiles):\n                        rows[0] = row_warp_base + mi * 16 + g\n                        rows[1] = rows[0] + 8\n                        for half in T.unroll(2):\n                            a_regs[mi, half * 2 + 0] = T.pack_b16(\n                                X[rows[0], ks[half * 2 + 0]],\n                                X[rows[0], ks[half * 2 + 1]],\n                            )\n                            a_regs[mi, half * 2 + 1] = T.pack_b16(\n                                X[rows[1], ks[half * 2 + 0]],\n                                X[rows[1], ks[half * 2 + 1]],\n                            )\n\n                    ranks[0] = br * block_R + g\n                    ranks[1] = ranks[0] + 8\n                    for rank_half in T.unroll(2):\n                        for half in T.unroll(2):\n                            b_regs[rank_half * 2 + half] = T.pack_b16(\n                                W[ks[half * 2 + 0], ranks[rank_half]],\n                                W[ks[half * 2 + 1], ranks[rank_half]],\n                            )\n\n                    for mi in T.unroll(warp_M_tiles):\n                        off = mi * 8\n                        T.ptx_mma(\n                            \"float32\",\n                            \"m16n8k16\",\n                            \"row\",\n                            \"col\",\n                            dtype,\n                            dtype,\n                            \"float32\",\n                            a_regs.data,\n                            mi * 4,\n                            b_regs.data,\n                            0,\n                            acc.data,\n                            off,\n                            False,\n                        )\n                        T.ptx_mma(\n                            \"float32\",\n                            \"m16n8k16\",\n                            \"row\",\n                            \"col\",\n                            dtype,\n                            dtype,\n                            \"float32\",\n                            a_regs.data,\n                            mi * 4,\n                            b_regs.data,\n                            2,\n                            acc.data,\n                            off + 4,\n                            False,\n                        )\n\n                ranks[0] = br * block_R + tg * 2\n                ranks[1] = ranks[0] + 1\n                ranks[2] = ranks[0] + 8\n                ranks[3] = ranks[2] + 1\n                for mi in T.unroll(warp_M_tiles):\n                    rows[0] = row_warp_base + mi * 16 + g\n                    rows[1] = rows[0] + 8\n                    off = mi * 8\n                    for half in T.unroll(2):\n                        for rr in T.unroll(2):\n                            T.atomic_add(Out[rows[0], ranks[half * 2 + rr]], acc[off + half * 4 + rr])\n                            T.atomic_add(Out[rows[1], ranks[half * 2 + rr]], acc[off + half * 4 + 2 + rr])\n\n    return main\n\n\ndef bench(fn: Callable[[], object], *, warmup: int, rep: int) -> dict[str, float | str]:\n    for _ in range(warmup):\n        fn()\n    torch.cuda.synchronize()\n    events = []\n    for _ in range(rep):\n        start = torch.cuda.Event(enable_timing=True)\n        end = torch.cuda.Event(enable_timing=True)\n        start.record()\n        fn()\n        end.record()\n        events.append((start, end))\n    torch.cuda.synchronize()\n    times = sorted(float(start.elapsed_time(end)) for start, end in events)\n    return {\n        \"p50_ms\": float(statistics.median(times)),\n        \"p90_ms\": float(times[min(len(times) - 1, int(len(times) * 0.9))]),\n        \"mode\": \"eager\",\n    }\n\n\ndef dump_cuda(args: argparse.Namespace) -> dict[str, Any] | None:\n    if args.dump_cuda is None:\n        return None\n    args.dump_cuda.mkdir(parents=True, exist_ok=True)\n    scalar = lora_down_scalar_kernel.compile(args.rows, args.k, args.rank)\n    alloc_local = lora_down_alloc_local_kernel.compile(args.rows, args.k, args.rank)\n    scalar_source = scalar.get_kernel_source(kernel_only=True)\n    alloc_source = alloc_local.get_kernel_source(kernel_only=True)\n    scalar_path = args.dump_cuda / \"lora_down_scalar_poc.cu\"\n    alloc_path = args.dump_cuda / \"lora_down_alloc_local_poc.cu\"\n    scalar_path.write_text(scalar_source, encoding=\"utf-8\")\n    alloc_path.write_text(alloc_source, encoding=\"utf-8\")\n    return {\n        \"scalar_path\": str(scalar_path),\n        \"alloc_local_path\": str(alloc_path),\n        \"scalar_source\": {\"bytes\": len(scalar_source.encode(\"utf-8\")), \"lines\": scalar_source.count(\"\\n\") + 1},\n        \"alloc_local_source\": {\"bytes\": len(alloc_source.encode(\"utf-8\")), \"lines\": alloc_source.count(\"\\n\") + 1},\n    }\n\n\ndef run(args: argparse.Namespace) -> dict[str, Any]:\n    if args.experiment != \"dense-lora\":\n        raise ValueError(\"this PoC keeps the old command shape and only supports --experiment dense-lora\")\n    if args.rows <= 0 or args.rows % 256 != 0:\n        raise ValueError(f\"--rows must be positive and divisible by 256, got {args.rows}\")\n    if args.k <= 0 or args.k % 128 != 0:\n        raise ValueError(f\"--k must be positive and divisible by 128, got {args.k}\")\n    if args.rank <= 0 or args.rank % 16 != 0:\n        raise ValueError(f\"--rank must be positive and divisible by 16, got {args.rank}\")\n\n    torch.manual_seed(args.seed)\n    x = torch.randn((args.rows, args.k), device=\"cuda\", dtype=torch.float32).to(torch.bfloat16)\n    w = torch.randn((args.k, args.rank), device=\"cuda\", dtype=torch.float32).to(torch.bfloat16)\n    out_scalar = torch.empty((args.rows, args.rank), device=\"cuda\", dtype=torch.float32)\n    out_alloc = torch.empty_like(out_scalar)\n\n    clear = clear_kernel(args.rows * args.rank)\n    scalar_kernel = lora_down_scalar_kernel(args.rows, args.k, args.rank)\n    alloc_kernel = lora_down_alloc_local_kernel(args.rows, args.k, args.rank)\n\n    def run_scalar() -> None:\n        clear(out_scalar.reshape(-1))\n        scalar_kernel(x, w, out_scalar)\n\n    def run_alloc_local() -> None:\n        clear(out_alloc.reshape(-1))\n        alloc_kernel(x, w, out_alloc)\n\n    with torch.inference_mode():\n        run_scalar()\n        run_alloc_local()\n    torch.cuda.synchronize()\n\n    err = (out_alloc - out_scalar).abs()\n    max_abs = float(err.max().item())\n    mean_abs = float(err.mean().item())\n\n    torch.cuda.reset_peak_memory_stats()\n    scalar_timing = bench(run_scalar, warmup=args.warmup, rep=args.rep)\n    scalar_peak = float(torch.cuda.max_memory_allocated() / (1024**3))\n    torch.cuda.reset_peak_memory_stats()\n    alloc_timing = bench(run_alloc_local, warmup=args.warmup, rep=args.rep)\n    alloc_peak = float(torch.cuda.max_memory_allocated() / (1024**3))\n\n    return {\n        \"status\": \"ok\" if max_abs <= 1.0e-3 and mean_abs <= 1.0e-5 else \"fail\",\n        \"experiment\": \"dense-lora\",\n        \"rows\": args.rows,\n        \"k\": args.k,\n        \"rank\": args.rank,\n        \"warmup\": args.warmup,\n        \"rep\": args.rep,\n        \"correctness\": {\n            \"max_abs_err\": max_abs,\n            \"mean_abs_err\": mean_abs,\n            \"pass\": max_abs <= 1.0e-3 and mean_abs <= 1.0e-5,\n        },\n        \"timing\": {\n            \"scalar\": scalar_timing,\n            \"alloc_local\": alloc_timing,\n            \"alloc_local_over_scalar_p50\": float(alloc_timing[\"p50_ms\"] / scalar_timing[\"p50_ms\"]),\n        },\n        \"peak_memory_gb\": {\"scalar\": scalar_peak, \"alloc_local\": alloc_peak},\n        \"source\": dump_cuda(args),\n    }\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=\"Minimal TileLang alloc_local LoRA-down PoC\")\n    parser.add_argument(\"--experiment\", choices=(\"dense-lora\",), default=\"dense-lora\")\n    parser.add_argument(\"--rows\", type=int, default=4608)\n    parser.add_argument(\"--k\", type=int, default=3840)\n    parser.add_argument(\"--rank\", type=int, default=128)\n    parser.add_argument(\"--warmup\", type=int, default=5)\n    parser.add_argument(\"--rep\", type=int, default=20)\n    parser.add_argument(\"--seed\", type=int, default=0)\n    parser.add_argument(\"--dump-cuda\", type=Path, default=None)\n    parser.add_argument(\"--json\", action=\"store_true\")\n    args = parser.parse_args()\n\n    payload = run(args)\n    if args.json:\n        print(json.dumps(payload, separators=(\",\", \":\")))\n    else:\n        print(json.dumps(payload, indent=2))\n\n\nif __name__ == \"__main__\":\n    main()\n```", "url": "https://github.com/tile-ai/tilelang/issues/2308"}
{"number": 2451, "title": "[Feature Request] TypeError: 'ForFrame' object is not iterable", "state": "closed", "created_at": "2026-06-24T12:47:03Z", "updated_at": "2026-09-21T05:28:10Z", "closed_at": "2026-09-21T05:28:10Z", "labels": ["enhancement"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Motivation\n\nscale_idx_table = [((j >> 4) & 7) | ((j >> 7) << 3) for j in range(block_K)]，不支持这种硬编码在@T.prim_func里\n\n### Solution\n\n_No response_\n\n### Alternatives\n\n_No response_\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/2451"}
{"number": 3256, "title": "[Feature Request] SM120 block-scaled GEMM support for register-resident attention", "state": "open", "created_at": "2026-09-20T17:22:54Z", "updated_at": "2026-09-20T17:22:54Z", "closed_at": null, "labels": ["enhancement"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Motivation\n\nReplacing the MMA macros in #2253 with `T.gemm_blockscaled` is blocked by:\n\n- **Operand scope:** fragment A / shared B is rejected. SageAttention3 keeps Q and quantized P in registers to avoid reloads and shared-memory round trips.\n- **Warp geometry:** for M=N=K=128 and 256 threads, `FullRow` with `sf_layout=\"blockscaled_chunk_kmajor\"` rejects the per-warp `1x8` MMA atom grid.\n\n### Solution\n\nSupport register-resident A and scales, and the 8x1 consumer warp partition, through the existing SM120 block-scaled GEMM API. Preserve the register-only path without implicit shared-memory staging.\n\n### Alternatives\n\nKeep the low-level MMA macros. Shared/shared GEMM works with Square + compact scales or FullRow + rowmajor scales, but does not preserve the current attention pipeline.\n\n### Additional context\n\nTested on main `eab74a4a`, with unchanged block-scaled GEMM implementation; PyTorch 2.11.0+cu130, NVRTC, `sm_120a`.\n\nBoth shared/shared controls passed GPU correctness checks. The replacement fails compilation, so no performance regression is claimed. Migration should retain correctness and stay within 5% of CUDA SageAttention3 raw-core latency on the same GPU.\n\n#### Minimal PoC\n\nLowering only; no GPU execution or external inputs required.\n\n```python\nimport tilelang\nimport tilelang.language as T\n\n\ndef repro(fragment_a):\n    @T.prim_func\n    def main(O: T.Tensor((128, 128), T.float32)):\n        with T.Kernel(1, threads=256):\n            A = (T.alloc_fragment((128, 128), T.float4_e2m1fn)\n                 if fragment_a else T.alloc_shared((128, 128), T.float4_e2m1fn))\n            B = T.alloc_shared((128, 128), T.float4_e2m1fn)\n            S = T.alloc_shared((128, 2), T.uint32)\n            C = T.alloc_fragment((128, 128), T.float32)\n            T.clear(A)\n            T.clear(B)\n            T.clear(S)\n            T.gemm_blockscaled(\n                A, B, C, S, S, transpose_B=True, clear_accum=True,\n                policy=T.GemmWarpPolicy.FullRow, k_start=0,\n                sf_a_granularity_k=16, sf_b_granularity_k=16,\n                sf_layout=\"rowmajor\" if fragment_a else \"blockscaled_chunk_kmajor\",\n            )\n            T.copy(C, O)\n    return main\n\n\ntarget = tilelang.tvm.target.Target({\"kind\": \"cuda\", \"arch\": \"sm_120a\"})\nfor fragment_a in (True, False):\n    try:\n        with target:\n            tilelang.lower(repro(fragment_a), target=target, enable_device_compile=False)\n    except Exception as error:\n        print(f\"fragment_a={fragment_a}: {error}\")\n```\n\nObserved failures:\n\n- `fragment_a=True`: rejects `A scope=local.fragment`.\n- `fragment_a=False`: `SM120 compact scale packages require a positive even MMA atom grid per warp, got 1x8`.", "url": "https://github.com/tile-ai/tilelang/issues/3256"}
{"number": 2906, "title": "[BUG] `makeBufferWithLayout` segfaults on a symbolic shared-buffer extent instead of hitting its own `ICHECK`", "state": "closed", "created_at": "2026-08-06T17:01:35Z", "updated_at": "2026-09-20T00:03:40Z", "closed_at": "2026-09-18T06:56:04Z", "labels": [], "comments_count": 2, "is_pr": false, "body": "\n**Version:** v0.1.11 (`VERSION` = 0.1.11), commit `607a9144cf375e1176ad487dd5b0e3d87c2cb79a`\n**Platform:** Linux, CUDA 13.0, NVIDIA RTX 4000 Ada Generation (sm_89), Python 3.10, torch 2.12.1+cu130\n**Still present on `main`:** the two loops below are unchanged as of `af4753045895`\n(source inspected; the reproduction itself was run on `607a9144`).\n\n## Summary\n\n`makeBufferWithLayout` in `src/transform/lower_tile_op.cc` dereferences the result of\n`.as<IntImmNode>()` without a null check when computing a shared buffer's replication\nfactor. A symbolic (non-constant) extent therefore crashes the compiler with SIGSEGV.\n\nThe loop immediately after it guards the identical condition with an `ICHECK`, so the\ncrash appears to be an oversight rather than intended behaviour — the diagnostic that was\nwritten for this case can never fire, because the unguarded loop runs first.\n\n## Location\n\n`src/transform/lower_tile_op.cc`, inside `if (IsSharedBuffer(buffer))`:\n\n```cpp\nArray<PrimExpr> buffer_shape = buffer->shape;\nint buffer_extent = 1;\nint layout_extent = 1;\nfor (size_t i = 0; i < buffer_shape.size(); i++) {\n  auto shape = buffer_shape[i].as<IntImmNode>();\n  buffer_extent *= shape->value;                     // <-- no null check\n}\nfor (size_t i = 0; i < layout_shape.size(); i++) {\n  auto shape = layout_shape[i].as<IntImmNode>();\n  ICHECK(shape) << \"Layout output shape must be constant integer, but got: \"\n                << layout_shape[i];                  // <-- guarded\n  layout_extent *= shape->value;\n}\n```\n\nIf `buffer_shape[i]` is not an `IntImmNode`, `.as<IntImmNode>()` returns `nullptr` and\n`shape->value` dereferences it.\n\n## Reproduction\n\nRun this **from a file**, not `python -c` — TVMScript's `@T.prim_func` parser calls\n`inspect.getsourcelines`, which has no source under `-c`, so the parser fails first and the\ncrash never surfaces.\n\n```python\nimport tilelang\nimport tilelang.language as T\nfrom tilelang.layout import Layout\n\nm = T.dynamic(\"m\", \"int32\")\nN = 256\n\n@tilelang.jit(out_idx=[1])\ndef build():\n    @T.prim_func\n    def k(A: T.Tensor((m, N), \"float16\"), B: T.Tensor((m, N), \"float16\")):\n        with T.Kernel(1, threads=128) as bx:\n            s = T.alloc_shared([m, N], \"float16\")\n            # element counts match, so the layout.cc:678 product check passes and\n            # execution reaches makeBufferWithLayout with a symbolic shape\n            T.annotate_layout({s: Layout([m, N], lambda i, j: [i, j])})\n            T.copy(A[0:m, :], s)\n            T.copy(s, B[0:m, :])\n    return k\n\nbuild()\n```\n\n**Actual:** process dies with SIGSEGV (exit `-11`), no diagnostic.\n\n**Expected:** the existing message, i.e.\n`Layout output shape must be constant integer, but got: m`\n\n## Notes\n\nA dynamic shared extent *without* an explicit layout annotation compiles fine — that path\nnever reaches `makeBufferWithLayout`, so the bug only surfaces when a layout annotation\nforces the buffer through the remap path.\n\n`T.dynamic(\"m\", \"int32\")` and `T.symbolic(\"m\")` behave identically here — both segfault with\nthe same stack. That is expected from `tilelang/language/symbolics.py`, where `symbolic` is a\n`@deprecated(..., \"v0.1.9\")` alias that calls `dynamic(name, dtype)` and returns the same\n`tirx.Var`. The reproduction uses `T.dynamic` as the non-deprecated spelling.\n\n## Suggested fix\n\nGuard the first loop the same way as the second, and report which buffer failed:\n\n```cpp\nfor (size_t i = 0; i < buffer_shape.size(); i++) {\n  auto shape = buffer_shape[i].as<IntImmNode>();\n  ICHECK(shape) << \"Shared buffer '\" << buffer->name\n                << \"' extent must be a constant integer, but got: \" << buffer_shape[i];\n  buffer_extent *= shape->value;\n}\n```\n\nHappy to send a PR if that shape looks right.\n", "url": "https://github.com/tile-ai/tilelang/issues/2906"}
{"number": 2817, "title": "[BUG] Unsafe cloudpickle Deserialization in Kernel Cache Leading to Remote Code Execution", "state": "closed", "created_at": "2026-07-30T15:56:53Z", "updated_at": "2026-09-19T23:25:11Z", "closed_at": "2026-09-06T08:06:22Z", "labels": ["bug"], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\nmain (all versions using cloudpickle)\n\n### System information\n\nThe tilelang library uses cloudpickle (a superset of Python pickle) to serialize and deserialize kernel parameters and compiled functions in its on-disk cache system. An attacker who can write files to the cache directory can achieve remote code execution when a victim loads a cached kernel because cloudpickle.load() allows arbitrary code execution during deserialization. This is a classic CWE-502 (Deserialization of Untrusted Data) vulnerability.\n\n\n\n### Problem description\n\n## Vulnerability type\n- [x] Other or Unknown\n\n## CWE\nCWE-502: Deserialization of Untrusted Data\n\n## Vendor of the product(s)\ntile-ai\n\n## Affected product(s)/code base\n### Product\ntilelang\n\n### Version\nmain (all versions using cloudpickle)\n\n## Attack type\n- [x] Remote\n\n## Impact\n- [x] Code Execution\n- [x] Information Disclosure\n\n## Affected component(s)\ntilelang.cache.kernel_cache.KernelCache._load_kernel_from_disk, tilelang.autotuner.param.AutotuneResult._load_kernel_from_disk, tilelang.autotuner.param.AutotuneResult.load_from_disk\n\n## Core vulnerable code path\nThe vulnerability exists in three code paths: (1) tilelang/cache/kernel_cache.py:589-597 - KernelCache._load_kernel_from_disk reads params.pkl and calls cloudpickle.load(f); (2) tilelang/autotuner/param.py:363-370 - AutotuneResult._load_kernel_from_disk reads params.pkl and calls cloudpickle.load(f); (3) tilelang/autotuner/param.py:493-497 - AutotuneResult.load_from_disk reads function.pkl and calls cloudpickle.load(f).\n\nCore vulnerable code path:\n\n```python\n# tilelang/cache/kernel_cache.py:589-597\n# Load kernel parameters\nkernel_params: list[KernelParam] | None = None\ntry:\n    if verbose:\n        self.logger.debug(f\"Loading kernel parameters from file: {params_path}\")\n    with open(params_path, \"rb\") as f:\n        kernel_params = cloudpickle.load(f)\nexcept Exception:\n    self.logger.exception(\"Error loading kernel parameters from disk\")\n```\n\nThis is the primary vulnerable sink in KernelCache._load_kernel_from_disk. cloudpickle.load() unconditionally deserializes attacker-controlled pickle bytes, allowing arbitrary code execution.\n\n```python\n# tilelang/autotuner/param.py:363-370\n# Load kernel parameters\ntry:\n    if verbose:\n        logger.debug(f\"Loading kernel parameters from file: {params_path}\")\n    with open(params_path, \"rb\") as f:\n        kernel_params = cloudpickle.load(f)\nexcept Exception as e:\n    logger.error(f\"Error loading kernel parameters from disk: {e}\")\n```\n\nSecond sink occurrence in AutotuneResult._load_kernel_from_disk. Same unsafe cloudpickle.load() with no integrity verification.\n\n## Attack vector(s)\nThe attacker must be able to write files to the tilelang kernel cache directory. This can be achieved by: (1) controlling the TILELANG_CACHE_DIR environment variable to point to an attacker-controlled directory; (2) writing to the default cache directory (~/.tilelang/cache) on shared multi-user systems or CI/CD environments; (3) exploiting another vulnerability that allows filesystem write access. Once file write access is achieved, the attacker places a malicious params.pkl or function.pkl file at a predictable cache path. The attack is triggerable when any user or process loads a tilelang kernel from the cache.\n\n## Suggested description of the vulnerability for use in the CVE\nTileLang contains an unsafe deserialization vulnerability in its kernel cache system. The software uses cloudpickle (a superset of Python pickle) to serialize and deserialize kernel parameters and compiled function objects to and from disk. An attacker who can write files to the TILELANG_CACHE_DIR directory (default: ~/.tilelang/cache) can craft a malicious pickle file that executes arbitrary Python code via the cloudpickle.load() __reduce__ protocol when a victim loads a cached kernel. This affects KernelCache._load_kernel_from_disk (tilelang/cache/kernel_cache.py:595) and AutotuneResult._load_kernel_from_disk / load_from_disk (tilelang/autotuner/param.py:368, 497).\n\n## Discoverer(s)/Credits\nCustomeres\n\n## Reference(s)\n\n\n## Additional information\nAll three cloudpickle.load() calls lack any form of integrity verification, signature checking, or input sanitization. The cache key is a SHA256 hash derived from kernel code and compile parameters, but the attacker can compute the same hash and place malicious files in the corresponding cache subdirectory. The vulnerability was identified through static source code audit of the tilelang library.\n\n### Reproducible example code\n\nThe Python snippets:\n\n```python\n\n```\n\n\n### Traceback\n\n```pytb\n\n```\n\n### Expected behavior\n\n_No response_\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/2817"}
{"number": 3240, "title": "[BUG][CuTeDSL][FP4/FP8] DeepSeek V4 act quant: FP4 conversion/store broken against CUTLASS DSL 4.7, FP8 store rejected by libNVVM", "state": "open", "created_at": "2026-09-17T10:43:00Z", "updated_at": "2026-09-17T10:46:17Z", "closed_at": null, "labels": [], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I searched the issue tracker for an existing dedicated report.\n\n### What version of TileLang are you using?\n\nCurrent `main` at `e688a439a8f79d69497d02c4fad7c75fada954ad`.\n\n### System information\n\n- GPU: NVIDIA L20 (sm_89)\n- CUDA / nvcc: 12.8.61 (`/usr/local/cuda-12.8`)\n- PyTorch: 2.10.0+cu128\n- TileLang: editable build of `main` (`0.1.14+cuda.gite688a439`)\n- CuTeDSL / CUTLASS DSL: `nvidia-cutlass-dsl==4.7.0` with `nvidia-cutlass-dsl-libs-cu12==4.7.0`\n\n### How the CuTeDSL backend is really selected\n\n`examples/conftest.py` xfails `deepseek_v4/test_tilelang_example_deepseek_v4.py::test_example_act_quant`\nwhen `TILELANG_TARGET=cutedsl`, and `.github/workflows/ci.yml` runs the examples with\n`TILELANG_TARGET: cutedsl`. But nothing in the library reads `TILELANG_TARGET` — only\n`examples/conftest.py` and two example tests do. Verified on current `main`:\n\n```text\n$ TILELANG_TARGET=cutedsl python -c \"...compile a trivial kernel, print adapter/target...\"\nTILELANG_TARGET= cutedsl\nadapter: TVMFFIKernelAdapter\ntarget: {\"kind\":\"cuda\", ..., \"arch\":\"sm_89\"}\n```\n\nSo that env var only toggles the xfail markers; the examples still run on the default\nbackend. The backend is actually selected with `TILELANG_DEFAULT_TARGET=cutedsl`\n(or `tilelang.compile(..., target=\"cutedsl\")`). Worth fixing in its own right, because it\nmakes the CuTeDSL CI job and every \"reproduce under CuTeDSL\" recipe a no-op.\n\n### Problem description\n\nWith the backend actually selected, the DeepSeek V4 activation-quantization example fails\nin two independent ways: the FP4 conversion/store helpers call MLIR ops that no longer\nexist in CUTLASS DSL 4.7, and the FP8 store path is rejected by libNVVM.\n\n```text\n$ TILELANG_DEFAULT_TARGET=cutedsl python -c \"import act_quant; act_quant.test_fp8_act_quant(M=64,N=256,block_size=128)\"\nCompilerDiagnosticError: error: NVVM backend compilation failed\n  error: libNVVM failed while compiling generated device IR.\n  note: target architecture: sm_89\n  raw: NVVM_ERROR_COMPILATION, libNVVM extra log: NVVM backend compilation failed\n\n$ TILELANG_DEFAULT_TARGET=cutedsl python -c \"import act_quant; act_quant.test_fp4_act_quant(M=64,N=256,block_size=32)\"\nAttributeError: module 'cutlass._mlir.dialects.vector' has no attribute 'extractelement'\n  at tilelang/contrib/cutedsl/utils.py:256 in _float32_to_f4e2m1_tensor\n```\n\n`test_round_trip_error` fails with the same libNVVM error because it calls the FP8 path first.\n\n### Root causes found so far\n\n**1. FP4 conversion helpers use removed MLIR ops.**\n`tilelang/contrib/cutedsl/utils.py` calls `vector.extractelement` / `vector.insertelement`\nin `_f4e2m1_to_float32_tensor` and `_float32_to_f4e2m1_tensor`. CUTLASS DSL 4.7 ships\n`vector.extract` / `vector.insert` (with explicit dynamic/static position lists) instead:\n\n```text\n$ python -c \"from cutlass._mlir.dialects import vector; print([n for n in dir(vector) if 'extract' in n or 'insert' in n])\"\n['ExtractOp', 'ExtractStridedSliceOp', 'InsertOp', 'InsertStridedSliceOp', 'extract', 'extract_strided_slice', 'insert', 'insert_strided_slice']\n```\n\nPorting the four call sites (`vector.extract(v, [], [idx])`, `vector.insert(v, dst, [], [idx])`)\nmakes the FP4 kernel compile and run.\n\n**2. The CuTeDSL adapter materializes sub-byte outputs unpacked.**\n`CuTeDSLKernelAdapter._convert_torch_func` allocates outputs as\n`torch.empty(*param_shapes[i], dtype=param_dtypes[i])` using the *logical* TIR shape. For\n`float4_e2m1fn` (4-bit) the torch storage dtype `float4_e2m1fn_x2` packs two elements per\nbyte, so the last dimension must be divided by the packing factor — which is what the CUDA\nadapters already do (see the `stride_scale` note in `tilelang/jit/adapter/tvm_ffi.py`).\nWithout it, `fp4_act_quant` returns a `(M, N)` tensor instead of `(M, N/2)`:\n\n```text\nAssertionError: Shape mismatch: torch.Size([64, 256]) vs torch.Size([64, 128])\n```\n\n**3. The wrapper's FP8 dtype mapping disagrees with the codegen.**\n`TLCuTeDSLSourceWrapper._TYPE_MAP` maps the TIR dtype string to `cutlass.Float8E4M3`, while\n`src/cuda/codegen/codegen_cutedsl.cc` emits `Float8E4M3FN` (\"Only Float8E4M3FN is supported\nat the moment\"). The TIR string for `float8_e4m3fn` is `float8_e4m3`, so the generated fake\ntensors got `Float8E4M3` and every store tried `Float8E4M3FN.to(Float8E4M3)`:\n\n```text\nTypeError: Unsupported destination float type: Float8E4M3\n  at cutlass/base_dsl/typing.py:1123 in Numeric.to\n```\n\n**Status after fixing 1–3** (local patch, no upstream PR yet):\n\n| target | `test_fp8_act_quant` | `test_fp4_act_quant` | `test_round_trip_error` |\n|---|---|---|---|\n| `cuda` | pass | pass | pass |\n| `cutedsl` | **libNVVM failure** | **pass** (`85/8192` bytes differ from FP4 tie-breaking) | **libNVVM failure** |\n\n### Minimal reproducers\n\nFP4 (fails before fix 1; passes after fixes 1–3 when written as a vectorized copy, which is\nwhat the DeepSeek kernel does):\n\n```python\nimport tilelang, tilelang.language as T\ntilelang.disable_cache()\n\n@T.prim_func\ndef k(A: T.Tensor((256,), \"bfloat16\"), B: T.Tensor((256,), \"float4_e2m1fn\")):\n    with T.Kernel(1, threads=256):\n        tx = T.get_thread_binding()\n        B[tx] = T.cast(A[tx], \"float4_e2m1fn\")\n\ntilelang.compile(k, target=\"cutedsl\")\n```\n\nA *scalar* FP4 store is separately unsupported and reports\n`ValueError: Sub-byte scalar dereference not supported for type Float4E2M1FN`; the DeepSeek\nkernel avoids it by storing through `T.copy`, so that is not part of this report.\n\nFP8 — this is the remaining blocker and it is not specific to the DeepSeek kernel:\n\n```python\nimport torch, tilelang, tilelang.language as T\ntilelang.disable_cache()\n\n@T.prim_func\ndef k(A: T.Tensor((256,), \"bfloat16\"), B: T.Tensor((256,), \"float8_e4m3fn\")):\n    with T.Kernel(1, threads=256):\n        tx = T.get_thread_binding()\n        B[tx] = T.cast(A[tx], \"float8_e4m3fn\")\n\nkernel = tilelang.compile(k, target=\"cutedsl\")     # source generation succeeds\na = torch.randn(256, dtype=torch.bfloat16, device=\"cuda\")\nb = torch.empty(256, dtype=torch.float8_e4m3fn, device=\"cuda\")\nkernel(a, b)   # CompilerDiagnosticError: NVVM backend compilation failed (sm_89)\n```\n\nThe same kernel with `target=\"cuda\"` runs and produces correct FP8 output. `T.copy`-based\nvectorized FP8 stores, `64`/`256`-thread blocks and both `round_scale` settings all fail at\nlibNVVM. `libNVVM extra log` is empty, so the failure carries no further detail; compiling\nthe same source for `sm_90`/`sm_100` targets succeeds at the TileLang level (the cubin\nbuild happens lazily at launch, which cannot run on this GPU, so arch attribution is not\nproven).\n\n### Expected behavior\n\nThe DeepSeek V4 activation-quantization example should compile and pass under the CuTeDSL\nbackend, and its entry should be removed from `CUTEDSL_KNOWN_FAILURES`.\n\n### Proposed scope\n\n1. Port the FP4 conversion helpers to the CUTLASS DSL 4.7 `vector.extract` / `vector.insert`\n   API.\n2. Pack sub-byte output shapes in `CuTeDSLKernelAdapter._convert_torch_func`.\n3. Align `_TYPE_MAP[\"float8_e4m3\"]` with the codegen's `Float8E4M3FN`.\n4. Diagnose the remaining libNVVM failure for FP8 stores (needs a green FP8 path before the\n   example can pass and the xfail can be dropped).\n5. Add a focused CuTeDSL regression test for FP4/FP8 conversion/store lowering instead of\n   relying on the whole DeepSeek example.\n6. Separately: make `TILELANG_TARGET=cutedsl` actually select the backend, or update the CI\n   job and the documented reproduction to use the variable the library reads.\n\n### Related\n\n- #1454 — CuTeDSL backend roadmap\n- #2187 — added the CuTeDSL FP4 dtype mapping and documented the remaining limitation\n", "url": "https://github.com/tile-ai/tilelang/issues/3240"}
{"number": 3034, "title": "[BUG][Fuzzer][ice-on-invalid-code] `T.rng_rand`/`T.rng_rand_float` with no preceding `T.rng_init` emit `curand…(&)` and crash nvcc, instead of a clean \"init required\" error", "state": "open", "created_at": "2026-08-17T03:26:34Z", "updated_at": "2026-09-17T10:15:47Z", "closed_at": null, "labels": [], "comments_count": 3, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\n- Installed via pip into a clean venv\n- Python 3.13\n- `tilelang` 0.1.13, `torch` 2.8.0\n- GPU: NVIDIA L40S (sm_89); CUDA 12.x, `nvcc` invoked with `-arch=sm_89`\n\n### Problem description\n\nCalling `T.rng_rand()` (or `T.rng_rand_float()`) in a kernel that does **not** also call `T.rng_init()` produces a kernel that fails to compile with an opaque nvcc error, instead of a clean TileLang-level message that `rng_init` is required first.\n\nThe CUDA codegen for `rng_rand` emits `curand(&<state>)`, where `<state>` is the name of the curand state variable. That name is only ever set when an `rng_init` call is codegen'd; with no `rng_init` present it stays the empty string, so the emitted line is literally `curand(&)` — a bare `&` with no operand — and nvcc aborts:\n\n```\n/tmp/.../tvm_kernels.cu(20): error: expected an expression\n    B[((int)threadIdx.x)] = curand(&);\n                                    ^\n```\n\nThe same kernel *with* an `T.rng_init(...)` compiles and codegens correctly (control below), so the failure is specific to the missing-init case.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\n\ndef build(with_init):\n\n    @T.prim_func\n    def main(B: T.Tensor((128,), \"uint32\")):\n        with T.Kernel(1, threads=128) as _:\n            tx = T.get_thread_binding()\n            if with_init:\n                T.rng_init(0)        # -> curand_init(...); state var declared\n            B[tx] = T.rng_rand()     # -> curand(&<state>)\n\n    return main\n\n\n# CONTROL: identical kernel WITH T.rng_init -> compiles fine\ntilelang.compile(build(True), target=\"cuda\")     # OK\n#   emitted: curandStatePhilox4_32_10_t __random_generator_state;\n#            curand_init(0, ((int)threadIdx.x), 0, &__random_generator_state);\n#            B[((int)threadIdx.x)] = curand(&__random_generator_state);\n\n# TRIGGER: identical kernel WITHOUT T.rng_init -> nvcc \"expected an expression\"\ntilelang.compile(build(False), target=\"cuda\")     # RuntimeError\n#   emitted: B[((int)threadIdx.x)] = curand(&);   <-- bare '&', no operand\n```\n\n`T.rng_rand_float()` (which emits `curand_uniform(&<state>)` etc.) fails identically for the same reason.\n\n### Traceback\n\n```pytb\nTraceback (most recent call last):\n  ...\n    raise RuntimeError(msg)\nRuntimeError: ...\nCompilation error:\n/tmp/.../tvm_kernels.cu(20): error: expected an expression\n    B[((int)threadIdx.x)] = curand(&);\n                                    ^\n1 error detected in the compilation of \"/tmp/.../tvm_kernels.cu\".\n```\n\n### Expected behavior\n\nA `T.rng_rand` / `T.rng_rand_float` used without a preceding `T.rng_init` reads an uninitialized (in fact, never-declared) curand state, so there is no valid value to produce. The natural expectation is a clear error at build time — e.g. \"`rng_rand` used without a preceding `rng_init`\" — rather than emitting `curand(&)` and surfacing an opaque `expected an expression` from nvcc. The frontend already asserts other RNG preconditions eagerly (`random.py` asserts the `generator`/`bit`/`dist` values), so an early check here would be consistent with that.\n\n### Additional context\n\n**Root cause.** The CUDA codegen prints the curand state operand from a member string that is only assigned inside the `rng_init` branch, with no check that an `rng_init` was seen for the current function; with no `rng_init` the member stays empty and the emit collapses to `curand(&)`.\n\n<details>\n<summary>Where the empty state name comes from (pinned to v0.1.13)</summary>\n\n`rng_rand` emits [`curand(&\" << this->curand_random_generator_state << \")\"`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4797-L4799) and `rng_rand_float` [does the same](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4801-L4807). `curand_random_generator_state` is a plain [`std::string` member](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.h#L92-L93) that defaults to empty and is set only in the [`rng_init` branch](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4781-L4788). The per-function pre-scan in `AddFunction` that declares the state variables walks the body [only for `rng_init` calls](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L6143-L6158); when the body has `rng_rand` but no `rng_init`, nothing is declared and the member stays empty, so the emit becomes `curand(&)`. Nothing in the [frontend `rng_rand`/`rng_rand_float`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/language/random.py#L42-L50) requires or checks for a prior `rng_init` either.\n\n</details>\n\n**Impact.** A user who forgets (or mis-scopes) the `rng_init` call gets no TileLang-level guidance — the kernel is accepted through the whole frontend and lowering pipeline, then dies deep in `nvcc` with `expected an expression` on a machine-generated `.cu` line the user never wrote. The failure surfaces far from its cause (a missing frontend call), so the message is actively misleading rather than merely unhelpful; diagnosing it requires reading the emitted CUDA. It is a build-time failure, not a silent miscompile, so no wrong results reach silicon — but it costs debugging time on a precondition the compiler already knows how to detect.\n\n**Suggested fix.** Add a frontend guard that a function using `rng_rand`/`rng_rand_float` contains at least one `rng_init`, raising a clear error otherwise. Alternatively the codegen could `ICHECK(!curand_random_generator_state.empty())` at the `rng_rand`/`rng_rand_float` emit sites (the `rng_init` branch already `ICHECK`s its own pre-scan registration), turning the silent `curand(&)` into an actionable message.\n\n**Provenance.** Introduced with the RNG feature in [#1461](https://github.com/tile-ai/tilelang/pull/1461) (\"[Language] Adds a random number generation capability through curand_kernel\", merged 2025-12-18): as first written, `rng_rand` emitted `curand(&\" << this->curand_philox_state << \")\"` where the state member is set only in the `rng_init` branch — the same missing-precondition gap. [#2297](https://github.com/tile-ai/tilelang/pull/2297) (2026-06-02) only relocated the code into a backend namespace and renamed the member to `curand_random_generator_state`; it did not introduce or fix the gap. The pattern has therefore shipped since RNG first landed; not a regression.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect. The nearest reports are a different RNG bug — [#2625](https://github.com/tile-ai/tilelang/issues/2625) (`rng_init`'s default `seq` is x-dim-only; a silent wrong-value bug in `rng_init`, not a compile failure) — and a same-shaped-but-unrelated emit — [#2667](https://github.com/tile-ai/tilelang/issues/2667) (`atomic_min`/`atomic_max` `return_prev=True` emits `int prev = ;`); both have a different root and site.\n\n**Reach.** Triggering requires calling `rng_rand`/`rng_rand_float` with no `rng_init` anywhere in the function. Every RNG site shipped at 0.1.13 pairs them, so no shipped example or test exercises the no-init path (which is why CI does not catch it). **Verified by running the shipped example on 0.1.13 (L40S, sm_89):** `examples/rand/rand_uint.py` — where `T.rng_init` is on L18 and `T.rng_rand()` on L23 of the same kernel — runs to completion (exit 0; all three configs `(1024,42)/(512,123)/(128,0)` pass its torch-vs-triton `assert_close`). It **dodges the bug because it always calls `T.rng_init` before `T.rng_rand`**. The RNG tests likewise precede every producer with a `T.rng_init` in the same kernel: `testing/python/language/test_tilelang_language_rand.py` (including `test_rand_init_in_split_guard`, which puts the init in a runtime guard — the pre-scan still registers it), and `testing/python/language/test_tilelang_cast_rounding.py` (`T.rng_init(42)` L473 before `T.rng_rand()` L475). No shipped site hits the no-init path.\n\n**Generalization / boundary (all cells run on 0.1.13, L40S, sm_89).**\n\n- **SOURCE-level root:** `CodeGenTileLangCUDA::VisitExpr_` in `src/cuda/codegen/codegen_cuda.cc`. The `curand_random_generator_state` `std::string` member is assigned *only* in the `rng_init` branch ([L4787](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4787)) and read unconditionally by the two producer branches ([L4799](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4799), [L4806](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4806)), with no non-empty guard. The `AddFunction` pre-scan ([L6143-L6158](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L6143-L6158)) only registers `rng_init` calls, so absent any init the member stays empty and every producer collapses to `curand…(&)`.\n- **OPERATOR-level root:** the curand *producer* intrinsics that consume the shared state member — `tl.rng_rand` and the four `tl.rng_rand_float` shapes.\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| related-operator | `rng_rand_float()` (uint→f32 uniform), no init | `curand_uniform(&);` → nvcc `expected an expression` | **same root** |\n| related-operator | `rng_rand_float(bit=64)`, no init | `curand_uniform_double(&);` → same nvcc error | **same root** |\n| related-operator | `rng_rand_float(dist=\"normal\")`, no init | `curand_normal(&);` → same nvcc error | **same root** |\n| related-operator | `rng_rand_float(bit=64, dist=\"normal\")`, no init | `curand_normal_double(&);` → same nvcc error | **same root** |\n| related-source | `rng_init` alone, no producer | compiles OK | n/a (no producer reads the member) |\n| related-source (boundary) | `rng_init` inside runtime guard `if d!=0:` then top-level `rng_rand` | compiles OK — pre-scan is `PostOrderVisit`, not flow-sensitive | boundary: crash needs *total absence* of `rng_init`, not scope/branch mismatch |\n| similar-logic | any other codegen intrinsic reading a `std::string` member set in a sibling branch | none exist — `curand_random_generator_state[_type]` are the only such members in `codegen_cuda.h` (L92-93) | class confined to RNG |\n| related-type | `generator` knob (Philox/MRG32k3a/XORWOW) | does not apply — the generator name is only consumed inside `rng_init`, which is absent in the trigger; the empty-state emit is generator-independent | n/a |\n\nAll five producer shapes (`rng_rand` + 4 × `rng_rand_float`) share one root; the fix (frontend \"`rng_init` required\" guard, or an `ICHECK(!curand_random_generator_state.empty())` at the producer emit sites) covers the whole class. The nearest same-*shape* emit, `atomic_min/max` `return_prev=True` emitting `int prev = ;` ([#2667](https://github.com/tile-ai/tilelang/issues/2667)), is a **distinct-adjacent bug (not filed here)** — different site (atomic branch ~L4839) and a different empty-operand mechanism. No new bug was found while sweeping.", "url": "https://github.com/tile-ai/tilelang/issues/3034"}
{"number": 3029, "title": "[BUG][Fuzzer][ice-on-valid-code] Binding `T.rng_init()`'s documented state handle to a variable emits `void state = ;` and nvcc rejects the kernel", "state": "open", "created_at": "2026-08-17T03:25:55Z", "updated_at": "2026-09-17T10:13:57Z", "closed_at": null, "labels": [], "comments_count": 2, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (reproduced on 0.1.13).\n\n### System information\n\nTileLang 0.1.13; NVIDIA L40S (sm_89), CUDA 12.8. The defect is in the frontend `rng_init` return type + the generic C codegen of a `void`-typed variable binding, so it is not specific to one GPU.\n\n### Problem description\n\n`T.rng_init(...)` documents its return value as the RNG state handle (`Returns — state: PrimExpr — The random number generator state handle`). Capturing it the natural, documented way — `state = T.rng_init(seed)` — makes the kernel fail to compile: the emitted CUDA contains `void state = ;`, which nvcc rejects with `error: incomplete type \"void\" is not allowed`. The byte-for-byte identical kernel that *discards* the return value compiles and runs correctly (uniform values in `[0,1)`), so the RNG lowering itself is fine — only capturing the documented handle breaks it.\n\nThe abort happens the moment the binding exists, even if `state` is never read again and no random number is ever drawn.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\nN = 256\n\n# CRASHES: bind the rng_init state handle to a variable (the documented \"Returns: state\" usage)\n@T.prim_func\ndef rng_bind(B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(1, threads=256) as bx:\n        tx = T.get_thread_binding()\n        state = T.rng_init(1234)                 # docstring: \"Returns state: the RNG state handle\"\n        B[tx] = T.rng_rand_float(32, \"uniform\")\n\n# CONTROL: identical kernel, rng_init result NOT bound -> compiles & runs, values in [0,1)\n@T.prim_func\ndef rng_nobind(B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(1, threads=256) as bx:\n        tx = T.get_thread_binding()\n        T.rng_init(1234)                         # same call, return value discarded\n        B[tx] = T.rng_rand_float(32, \"uniform\")\n\ntry:\n    tilelang.compile(rng_bind, out_idx=[0])()\n    print(\"bind:   compiled\")\nexcept Exception as e:\n    print(\"bind:   CRASH\", type(e).__name__)          # -> CRASH RuntimeError (nvcc: incomplete type \"void\")\n\nb = tilelang.compile(rng_nobind, out_idx=[0])().cpu().numpy()\nprint(\"nobind: min\", b.min(), \"max\", b.max())          # -> min ~0.004  max ~0.998 (uniform [0,1))\n```\n\n### Traceback\n\nNo Python-level traceback with a clean message — the failure is an nvcc compile error surfaced as a `RuntimeError` from `tilelang.contrib.nvcc.compile_cuda`.\n\n<details><summary>Emitted CUDA (the malformed line) + nvcc error (0.1.13, sm_89)</summary>\n\n```cpp\ncurandStatePhilox4_32_10_t __random_generator_state;\ncurand_init(1234, ((int)threadIdx.x), 0, &__random_generator_state);\nvoid state = ;                                             // <- the bound handle; void-typed, empty init\nB[((int)threadIdx.x)] = curand_uniform(&__random_generator_state);\n```\n```\ntvm_kernels.cu(22): error: incomplete type \"void\" is not allowed\n      void state = ;\ntvm_kernels.cu(22): error: expected an expression\n      void state = ;\n2 errors detected in the compilation of \"tvm_kernels.cu\".\n```\n\n`rng_init` lowers to a `void`-typed intrinsic, so binding it produces a `void`-typed variable declaration with no initializer — `void state = ;` — which is doubly illegal (void has no storage, and the RHS is empty). The discard-the-result control never forms a binding, so this line is never emitted.\n</details>\n\n### Expected behavior\n\nEither the kernel should compile — capturing the state handle the docstring's `Returns` section describes should be a legal, no-op-if-unused binding — or `rng_init` should not advertise a returnable handle it cannot actually hand back. The RNG codegen (`curand_init`/`curand_uniform`) is already present and correct (the discard control produces valid uniform values), so the value is computable; the defect is that a documented return handle is `void` and binding it emits illegal CUDA. Making `rng_init` return a usable handle type (or the codegen elide a `void` binding) would honor the docstring; alternatively the docstring should stop promising a bindable state handle.\n\n### Additional context\n\n**Root cause.** `rng_init` builds its call with a `void` result dtype, while its docstring promises a returnable `state` handle. At [`tilelang/cuda/language/random.py#L39`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/language/random.py#L39):\n\n```python\nreturn tirx.call_intrin(\"void\", tirx.op.Op.get(\"tl.rng_init\"), seed, seq, off, generator)\n```\n\nbut the [docstring (L8-L27)](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/language/random.py#L8-L27) states `Returns — state: PrimExpr — The random number generator state handle`. When the caller follows that and writes `state = T.rng_init(...)`, the frontend forms a variable binding whose value is this `void`-typed expression; the C codegen prints the binding's declaration as `PrintType(void) name = <value>`, i.e. `void state = ;`, which nvcc rejects. The actual curand state is a separate module-level variable (`__random_generator_state`) the intrinsic writes to as a side effect — so the intrinsic genuinely has nothing to return, and the docstring's \"returns a handle\" is the inaccurate half.\n\n**Suggested fix.** Make the documented contract and the implementation agree: either return a real handle to the curand state (so binding it is meaningful and codegens as a reference to `__random_generator_state`), or change `rng_init` to not claim a returnable handle (return `None`/no value and update the docstring), so a user cannot form a `void state = ;` binding. A narrower codegen guard — refuse to emit (or elide) a `void`-typed variable binding with a clear frontend error — would at least turn the opaque nvcc failure into an actionable one.\n\n**Provenance.** The RNG API (`rng_init` returning a `void`-typed intrinsic while documenting a state handle) was added by [#1461](https://github.com/tile-ai/tilelang/pull/1461) (merged 2025-12-18). The `void`-return + documented-handle mismatch is present as written at the v0.1.13 tag (`random.py#L39` vs the `Returns` docstring). Reproduced at runtime on 0.1.13 this session (bind → nvcc `void state = ;`; discard control → uniform `[0,1)`). Not a recent regression.\n\n> Note: on 0.1.9 the same binding aborted *earlier*, inside `VerifyParallelLoop` (an `InternalError: A fixed length vector doesn't have a vscale factor.` from feeding the `void` let-value to the arithmetic analyzer). That pass path no longer forms the constraint on 0.1.13, so the crash now surfaces downstream in nvcc codegen; the underlying cause (a `void`-typed `rng_init` handle bound to a variable) is the same.\n\n**Generalization (tested this session, 0.1.13, L40S sm_89).**\n\n<details><summary>Two-level root, 4-axis sweep, and example-run result</summary>\n\n**Root — two levels.**\n- **SOURCE-level (fragile implementation).** `rng_init` returns `tirx.call_intrin(\"void\", tl.rng_init, ...)` at [`random.py#L39`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/language/random.py#L39) while its `Returns` docstring (L23-26) names a `state` handle. Downstream, the generic C codegen prints a let-binding as `PrintType(dtype) name = <value>;`; when `dtype` is `void` and the intrinsic hands back no value, it emits `void state = ;`. The fragile code is the pairing of a *no-value* frontend intrinsic dtype with the let-binding codegen — nothing rejects a valueless capture at the frontend.\n- **OPERATOR-level (what correlates).** Any TileLang frontend op that returns `call_intrin(\"void\"/\"handle\", ...)` (a valueless/side-effect-only intrinsic) and is allowed to be bound to a variable hits the same `PrintType(dtype) name = ;` codegen. `rng_init` is the *only* such op whose own docstring documents a returnable handle, so it is the in-contract representative; the others are out-of-contract siblings.\n\n**4-axis sweep (every cell RUN, result observed):**\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| the knob | `state = T.rng_init(1234)` with NO subsequent `rng_rand*` | `void state = ;` → nvcc `incomplete type \"void\"` | yes (binding alone triggers; drawing RNG irrelevant) |\n| similar-logic / related-operator | bind void intrinsic `s = T.sync_warp()` | `void s = ;` → nvcc `incomplete type \"void\" is not allowed` | **same root** |\n| related-type (intrinsic dtype) | bind `handle`-typed `h = T.named_barrier_arrive(0,256)` | `void* h = ;` → nvcc `expected an expression` (only) | same mechanism (empty-RHS let-binding), **distinct face**: `void*` is a complete type, so no \"incomplete type\" error |\n| related-source / related-type | bind non-void sibling `r = T.rng_rand()` (uint32, same file) | compiles + runs; samples 546353992, 3507855656 | no — non-void return binds fine (control: confirms root is the `void`/no-value dtype, not \"rng ops\") |\n\n- **Boundary — kept specific.** Reframed the *class* to \"binding a valueless (`void`/`handle`) intrinsic → empty-RHS declaration\", but the FILED bug stays the `rng_init` documented-handle case, because `rng_init` is the only op whose docstring invites the capture. `sync_warp`/`named_barrier_arrive` have no documented return (their docstrings show bare-statement use), so binding them is user error — recorded here as **distinct-adjacent, not filed**.\n\n**Example-run result (PART 1).** Both cited RNG sites run clean on 0.1.13 and dodge the bug: `examples/rand/rand_uint.py` compiles + passes its Triton `assert_close` for all 3 default configs; `testing/python/language/test_tilelang_language_rand.py` → `6 passed`. Every shipped `rng_init` call is a bare discard statement (verified: `random.py` example + all 4 test-file call sites), so no shipped code exercises the crash.\n</details>\n\n**Dedup.** I searched the open and closed tracker (`rng_init`, `void state`, `VerifyParallelLoop`, `incomplete type void`) and found no existing report of this defect. Two related-but-distinct issues exist:\n- **#2625** also concerns `rng_init` but is a different defect (default `seq` giving y/z threads one stream — a silent wrong result, not a compile crash) — different operator behavior, unrelated root.\n- **#2667** (closed) shares the *downstream codegen symptom* — a `void`-typed op bound to a variable emits an empty-initializer declaration nvcc rejects (`int prev = ;` there, `void state = ;` here). But the *root differs*: #2667 is an atomic op-selection bug (`atomic_min`/`max` `return_prev=True` wrongly dispatches the void `*_elem_op` when it should return a value), whereas here `rng_init` genuinely has nothing to return and the docstring over-promises a handle. Fixing #2667 (route min/max to a value-returning path) does not fix this (rng has no value); they are the same codegen weakness reached from two different fronts, not the same bug. That #2667 was accepted and fixed confirms this class of empty-binding crash is treated as a real defect.\n\n**Reach.** The trigger is a documented input: `rng_init`'s docstring names its return value the RNG state handle, and capturing a documented return value in a variable is the ordinary way to use an API. Ran the shipped RNG sites verbatim on 0.1.13 this session: `examples/rand/rand_uint.py` (`python rand_uint.py`, all 3 default configs) compiles and passes its Triton `assert_close`; `testing/python/language/test_tilelang_language_rand.py` runs `6 passed`. Every shipped site calls `T.rng_init(...)` as a bare statement and discards the return, so all of them dodge the crash and CI stays green — i.e. the shipped examples do NOT exercise this bug. A user who instead follows the docstring's `Returns` section and binds the handle hits the crash on the first compile.\n\n**Impact.** The trigger is narrow — it needs the specific act of binding `rng_init`'s documented `void`-typed return handle to a variable (a code-shape gate, not a data value). When it fires it is a compile-time abort (an nvcc `void state = ;` error surfaced as a `RuntimeError`), so it is loud and caught on the first build, blocks that one kernel, and cannot silently corrupt output. Fixing it makes a documented `rng_init` usage compile as the docstring implies, removing a docstring/implementation disagreement.", "url": "https://github.com/tile-ai/tilelang/issues/3029"}
{"number": 2625, "title": "[BUG][Fuzzer][wrong-code] `T.rng_init()` default `seq` is x-dim-only, silently giving y/z-differing threads one RNG stream", "state": "open", "created_at": "2026-07-12T06:05:04Z", "updated_at": "2026-09-17T10:13:50Z", "closed_at": null, "labels": [], "comments_count": 2, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.11 (source at `cd37ed5fc35ae7a60a1277c8eb49028174ac51e6`).\n\n### System information\n\nReproduced on NVIDIA L40S (sm_89), CUDA 13.0, PyTorch 2.11.0, tilelang 0.1.11 (dev wheel, byte-identical to the source tag above). The defect is in the arch-independent Python frontend id-computation (the codegen simply forwards `seq` to `curand_init` on every CUDA target), so it is expected on all arches; I tested sm_89.\n\n### Problem description\n\n`T.rng_init(seed, seq=None, off=0)` initializes a per-thread curand stream for \"parallel random number generation\". When `seq` is omitted (the default), the per-thread sequence number is auto-derived from **only the x dimension** of the thread and block indices:\n\n```python\n# tilelang/language/random.py  (the `seq is None` branch)\nif seq is None:\n    bx = T.get_block_binding()          # blockIdx.x only  (dim=0)\n    ex = T.kernel.get_thread_extent()   # blockDim.x only  (dim=0)\n    tx = T.get_thread_binding()         # threadIdx.x only (dim=0)\n    id = tx + bx * ex\n    seq = tirx.convert(id)\n```\n\n`seq` is passed straight through to `curand_init(seed, seq, offset, &state)` — it is the subsequence key that decorrelates parallel streams. Because `threadIdx.y`, `threadIdx.z`, `blockIdx.y`, and `blockIdx.z` are all dropped from `id`, any two threads that differ **only** in one of those dimensions receive the **same `seq`** and therefore the **same random stream**. The kernel compiles and runs with no diagnostic; the outputs are silently identical. This bites any kernel that uses a 2-D/3-D thread block (`threads=(x,y[,z])`) or a multi-dimensional grid with the default `seq`.\n\n### Reproducible example code\n\n```python\nimport torch, tilelang, tilelang.language as T\ntilelang.disable_cache()\n\ndef build(explicit_seq):\n    @tilelang.jit\n    def kf():\n        @T.prim_func\n        def main(Out: T.Tensor((2, 2), \"int32\")):     # int32 buffer; uint32 is not torch-feedable\n            with T.Kernel(1, threads=(2, 2)):\n                tx = T.get_thread_binding(0); ty = T.get_thread_binding(1)\n                if explicit_seq:\n                    T.rng_init(seed=1234, seq=tx * 2 + ty)   # CONTROL: unique per (tx,ty)\n                else:\n                    T.rng_init(seed=1234)                    # seq=None default (drops threadIdx.y)\n                Out[tx, ty] = T.reinterpret(T.rng_rand(), dtype=\"int32\")\n        return main\n    return kf()\n\nfor label, ex in [(\"default seq=None\", False), (\"explicit seq (control)\", True)]:\n    out = torch.zeros((2, 2), device=\"cuda\", dtype=torch.int32); build(ex)(out)\n    a = out.cpu().numpy()\n    print(f\"{label}: distinct={len(set(a.flatten().tolist()))}/4  Out[tx,0]==Out[tx,1]: \"\n          f\"{bool(a[0,0]==a[0,1])},{bool(a[1,0]==a[1,1])}\\n{a}\")\n# default seq=None      : distinct=2/4  Out[tx,0]==Out[tx,1]: True,True   <-- threadIdx.y dropped\n# explicit seq (control): distinct=4/4  Out[tx,0]==Out[tx,1]: False,False\n```\n\nSame collision with a 2-D grid `T.Kernel(2, 2, threads=1)` (drops `blockIdx.y`): `Out[bx,0] == Out[bx,1]`.\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion. The random outputs are silently identical across the dropped dimensions.\n\n### Expected behavior\n\nWith the default `seq`, distinct threads/blocks should get distinct curand streams — that is the purpose of the `seq` argument (see the [curand device API](https://docs.nvidia.com/cuda/curand/device-api-overview.html#device-api-overview), cited in the source itself). The control (a `seq` folding in all thread/block dimensions) gives four distinct values, which is the correct output the default should also produce.\n\n### Additional context\n\n**Root cause.** The default sequence-id derivation collapses the thread/block index to its x component only, so it is not unique across a multi-dimensional launch and hands colliding `seq` values to `curand_init`. Concretely, [`tilelang/language/random.py` L28–L33](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/tilelang/language/random.py#L28-L33) builds `id` from only the x components of the bindings (`get_thread_binding()`/`get_block_binding()`/`get_thread_extent()` all default to `dim=0`), dropping `threadIdx.y/z` and `blockIdx.y/z`. That `seq` is forwarded to `curand_init` as arg[1] at [`src/cuda/codegen/codegen_cuda.cc` L4298](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/codegen/codegen_cuda.cc#L4298).\n\n**Suggested fix.** Fold in the remaining dimensions — a row-major flatten over `get_thread_bindings()` / `get_block_bindings()` weighted by the corresponding extents — so the default id is unique across the full launch grid. (Minimally: document that the default `seq` is x-dimension-only and require an explicit `seq` for multi-dimensional launches — but silent correlation is a poor default for an RNG.)\n\n**Provenance.** Added in PR [#1582](https://github.com/tile-ai/tilelang/pull/1582) (\"Add more curand operations\"); current form last touched by `b939fa01` (#2216, tirx rename). The x-only derivation has been present since the feature landed — not a regression.\n\n**Dedup.** Searched the open and closed tracker (`rng_init`, `curand`, `rng_rand`, `sequence`) and FILED_ISSUES.md — no existing report.\n\nThe `seq is None` default branch appears to have no test coverage — the tests/examples pass an explicit `seq`, so the x-only default is never exercised against a multi-dimensional launch.\n", "url": "https://github.com/tile-ai/tilelang/issues/2625"}
{"number": 2988, "title": "[BUG][Fuzzer][ice-on-valid-code] `cython`/`nvrtc` backend aborts with `Unsupported dtype uint64` on a `uint64` tensor instead of compiling it", "state": "closed", "created_at": "2026-08-15T20:11:02Z", "updated_at": "2026-09-17T09:57:57Z", "closed_at": "2026-09-17T09:57:57Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar report exists.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13 · PyTorch 2.13.0+cu130 · CUDA 13.0 · NVIDIA L40S (sm_89) · Python 3.13 · `execution_backend=\"cython\"`.\n\n### Problem description\n\nA kernel with a `uint64` tensor parameter aborts host-glue codegen with\n\n```\nAssertionError: Unsupported dtype uint64\n```\n\nwhen compiled through the `cython` backend, even though `uint64` is a valid tensor dtype: the identical kernel compiles and runs correctly through the `tvm_ffi` backend (below), and the identical kernel with an `int64` tensor compiles fine on `cython` (so the crash is specific to `uint64`, not to 64-bit-integer tensors or the `cython` path in general).\n\nThe `cython` and `nvrtc` backends generate a C/CUDA host launcher and look each parameter's dtype up in `TLCUDASourceWrapper._TYPE_MAP` to spell its C type. That map has `int64`, `uint32`, `uint16`, `uint8` rows but no `uint64`, so `uint64` falls through to the `assert result is not None` and aborts the compile.\n\n| dtype | `cython` | `tvm_ffi` |\n|---|---|---|\n| `uint64` tensor | `AssertionError: Unsupported dtype uint64` (TESTED, L40S) | compiles + runs, correct (TESTED, L40S) |\n| `int64` tensor (control) | compiles OK (TESTED, L40S) | compiles OK |\n\nThe `nvrtc` backend goes through the same `_TYPE_MAP` (via `TLPyWrapper(TLWrapper)`, which does not override it) and is affected by inspection; I could not run `nvrtc` here (libnvrtc not loadable in this environment), so that cell is INFERRED from source, not tested. Not a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport torch, tilelang, tilelang.language as T\n\ndef kernel(dtype):\n    @T.prim_func\n    def k(A: T.Tensor((128,), dtype), B: T.Tensor((128,), dtype)):\n        with T.Kernel(1, threads=128):\n            i = T.get_thread_binding(0)\n            B[i] = A[i] + T.Cast(dtype, 1)\n    return k\n\na = torch.arange(128, dtype=torch.int64, device=\"cuda\").view(torch.uint64)\n\n# CONTROL 1: same kernel on tvm_ffi -> compiles and computes correctly\no = tilelang.compile(kernel(\"uint64\"), out_idx=[1], execution_backend=\"tvm_ffi\")(a)\nprint(\"tvm_ffi uint64:\", int(o[3].item()))          # -> 4   (arange[3]=3, +1)\n\n# CONTROL 2: int64 tensor on cython -> compiles fine (int64 IS in the map)\ntilelang.compile(kernel(\"int64\"), out_idx=[1], execution_backend=\"cython\")\nprint(\"cython  int64 : COMPILE_OK\")\n\n# TRIGGER: uint64 tensor on cython -> AssertionError: Unsupported dtype uint64\ntilelang.compile(kernel(\"uint64\"), out_idx=[1], execution_backend=\"cython\")\nprint(\"cython  uint64: COMPILE_OK\")                  # never reached\n```\n\n### Traceback\n\n```\nAssertionError: Unsupported dtype uint64\n```\n\n<details><summary>where it fires</summary>\n\nRaised from `TLCUDASourceWrapper._lookup_type` while building the host launcher's argument list (`create_dispatch_func` → `_lookup_type(buffer.dtype)`), before any CUDA compilation. The device kernel source itself is generated fine; only the host-glue type lookup aborts.\n</details>\n\n### Expected behavior\n\n`uint64` compiles through `cython`/`nvrtc` and runs, returning the same result the `tvm_ffi` backend already produces. `uint64` is a supported tensor dtype end-to-end: the `tvm_ffi` backend compiles and runs it correctly (repro), and the sibling wrappers already map it — the [HIP map has `\"uint64\": \"uint64_t\"`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/wrapper.py#L688) and the CPU map does too — so the CUDA map is the lone omission.\n\n### Additional context\n\n**Root cause.** `TLCUDASourceWrapper` does not recognize `uint64` when emitting the host launcher's C argument types, so a valid `uint64` tensor aborts the compile. Its [`_TYPE_MAP`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/wrapper.py#L212) has `int64`/`uint32`/`uint16`/`uint8` but no `uint64` row, and [`_lookup_type` asserts on a missing key](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/wrapper.py#L271). The `nvrtc` backend inherits this map through `TLPyWrapper(TLWrapper)`, which does not override it.\n\n**Generalization (tested this session, 0.1.13 / L40S).** This is a *single missing map row*, not a broken class: the sibling unsigned dtypes that DO have a row — `uint32`, `uint16`, `uint8` — each compile fine on `cython` (tested), and the `int64` control compiles, so the failure is specific to `uint64` being the one omitted key, not to unsigned or 64-bit integers in general. The title is kept specific to `uint64`; the tested boundary (uint32/uint16/uint8/int64 all pass) is recorded here.\n\n**Suggested fix.** Add `\"uint64\": \"uint64_t\"` to `TLCUDASourceWrapper._TYPE_MAP` (`wrapper.py:212`) — the exact row the [HIP subclass](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/jit/adapter/wrapper.py#L688) and the CPU wrapper already carry, the natural counterpart to the `int64`/`uint32` rows there. The same kernel already runs correctly on `tvm_ffi` and the `int64` control compiles on `cython`, so the value is compilable and the fix is to map it rather than reject (not verified end-to-end — the `cython` `.so` was not rebuilt with the added row this session).\n\n**Provenance.** The CUDA `_TYPE_MAP` has had no `uint64` row since at least v0.1.9; origin before that is unverified. [#1230](https://github.com/tile-ai/tilelang/pull/1230) (merged 2025-11-11) added `uint64` to the *CPU* wrapper's map (and the HIP subclass carries it), without touching the CUDA map — so the omission is CUDA-specific and still present on `main`.\n\n**Dedup.** I searched the open and closed tracker (`uint64 Unsupported dtype`, `uint64 TYPE_MAP`, `uint64 cython`) and found no existing report of this defect. [#2582](https://github.com/tile-ai/tilelang/issues/2582) is a different `uint64` bug (a TVM arith constant-fold crash for results in `[2^63, 2^64)`), unrelated to this host-wrapper type map.\n\n**Reach.** The trigger is a `uint64` tensor parameter compiled through `cython` or `nvrtc`. `uint64` is a supported/typed tensor dtype — `tvm_ffi` compiles and runs it, and the CPU/HIP maps carry it. `cython` is not the default for CUDA — `resolve_execution_backend` selects `tvm_ffi` for a CUDA target (verified this session), so a user hits this only by explicitly requesting `execution_backend=\"cython\"` (or `\"nvrtc\"`). Why CI is green: the `cython` JIT tests (`testing/python/jit/test_tilelang_jit_cython.py`) all use `float32`; the `uint64` buffers that appear in tests are internal `alloc_buffer`/`decl_buffer` descriptors and mbarriers (`test_tilelang_transform_inject_fence_proxy.py`) that never become host-visible parameters, so they never reach `_lookup_type`; no test or `examples/` kernel passes a `uint64` tensor parameter through the `cython`/`nvrtc` host wrapper (`grep`: no `Tensor(...uint64` param in `examples/`/`testing/`).\n\n**Impact.** The trigger is narrow: a `uint64` tensor parameter routed through the non-default `cython`/`nvrtc` host wrapper (CUDA defaults to `tvm_ffi`, which is unaffected). When it fires it is a compile-time `AssertionError` before any CUDA code is built — loud and immediate, blocking that one kernel from compiling on those backends but corrupting no values and unable to reach production silently. Fixing it adds the one map row so a supported dtype compiles on `cython`/`nvrtc` as it already does on `tvm_ffi`/CPU/HIP, closing the backend disagreement.", "url": "https://github.com/tile-ai/tilelang/issues/2988"}
{"number": 2311, "title": "[Question] T.Parallel rejects sibling fragment reads from FullRow GEMM accumulator", "state": "closed", "created_at": "2026-05-30T12:38:56Z", "updated_at": "2026-09-17T09:24:06Z", "closed_at": "2026-09-17T09:24:06Z", "labels": ["question"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Questions\n\n```python\nimport tilelang as tl\nimport tilelang.language as T\nimport torch\n\n\n@tl.jit(out_idx=[-1])\ndef sibling_fragment_access_repro(\n    rows: int = 16,\n    hidden: int = 32,\n    out_cols: int = 64,\n    block_m: int = 16,\n    block_n: int = 64,\n    block_k: int = 32,\n    threads: int = 128,\n    dtype=T.float16,\n    accum_dtype=T.float32,\n):\n    paired_cols = out_cols // 2\n\n    @T.prim_func\n    def main(\n        a: T.Tensor((rows, hidden), dtype),\n        b: T.Tensor((hidden, out_cols), dtype),\n        out: T.Tensor((rows, paired_cols), dtype),\n    ):\n        with T.Kernel(1, 1, threads=threads):\n            a_shared = T.alloc_shared((block_m, block_k), dtype)\n            b_shared = T.alloc_shared((block_k, block_n), dtype)\n            acc = T.alloc_fragment((block_m, block_n), accum_dtype)\n\n            T.clear(acc)\n            T.copy(a[0, 0], a_shared)\n            T.copy(b[0, 0], b_shared)\n            T.gemm(a_shared, b_shared, acc, policy=T.GemmWarpPolicy.FullRow)\n\n            for i, j in T.Parallel(block_m, paired_cols):\n                # This pair of sibling reads is rejected by layout inference:\n                #   acc: (i, j * 2 + 1) and (i, j * 2)\n                gate = acc[i, j * 2]\n                up = acc[i, j * 2 + 1]\n                out[i, j] = T.cast(gate + up, dtype)\n\n    return main\n\n\ndef main() -> None:\n    print(\"tilelang\", getattr(tl, \"__version__\", \"unknown\"))\n    print(\"torch\", torch.__version__)\n    if torch.cuda.is_available():\n        print(\"device\", torch.cuda.get_device_name())\n        print(\"capability\", torch.cuda.get_device_capability())\n\n    kernel = sibling_fragment_access_repro()\n    a = torch.randn(16, 32, device=\"cuda\", dtype=torch.float16)\n    b = torch.randn(32, 64, device=\"cuda\", dtype=torch.float16)\n\n    # The failure happens during JIT compile/lowering before the kernel runs.\n    kernel(a, b)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nTileLang layout inference rejects a kernel that reads neighboring lanes from the same GEMM accumulator fragment inside one T.Parallel loop. Here is my environment:\n\n```\n  tilelang 0.1.10\n  torch 2.12.0+cu130\n  GPU: NVIDIA GeForce RTX 3090\n  SM capability: 8.6\n```\n\nObserved error:\n```\n...\n  tvm.error.InternalError:\n  Check failed: (StructuralEqual()(it->second.indices, indices)) is false:\n  acc: (i, j * 2 + 1) and (i, j * 2)\n```\n\nExpected behavior: this pattern should compile, especially with `GemmWarpPolicy.FullRow`, because paired epilogues such as SwiGLU need to consume neighboring GEMM output lanes without first copying them into separate fragment buffers. (I'm reproducing [CODA](https://arxiv.org/pdf/2605.19269)'s fused GEMM-SwiGLU)\n\nI'm not quite sure even with `FullRow`, whether tilelang will distribute logically sibling fragment variables in one thread. Am I missing something important here?\n", "url": "https://github.com/tile-ai/tilelang/issues/2311"}
{"number": 2993, "title": "[BUG][Fuzzer][ice-on-valid-code] Vectorized `~x` (bitwise-NOT) on 16/32-bit ints fails to compile instead of computing the complement", "state": "closed", "created_at": "2026-08-16T06:19:58Z", "updated_at": "2026-09-16T04:21:07Z", "closed_at": "2026-09-16T04:21:07Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and found no existing report of this defect.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nPython 3.13, linux\ntilelang 0.1.13\ntorch 2.8.0+cu128\nGPU: NVIDIA L40S (sm_89), CUDA 12.8\n\n### Problem description\n\nAn elementwise bitwise-NOT `B[i] = ~A[i]` (equivalently `T.bitwise_not(A[i])`) on a 16- or 32-bit integer tensor fails to compile whenever the `T.Parallel` loop is auto-vectorized — i.e. whenever a thread handles more than one element (`threads < N`), which is the default for elementwise kernels. The generated CUDA is:\n\n```cuda\n*(int4*)(B + ...) = (~*(int4*)(A + ...));   // int32, 4 elems/thread\n```\n\nand `nvcc` rejects it: `no operator \"~\" matches these operands / operand types are: ~ int4`. The same kernel with one element per thread (`threads == N`, no vectorization) compiles and returns the correct values, and the binary bitwise operators (`&`, `|`, `^`), the shifts, and `int8` all vectorize correctly. So the elementwise operation the user wrote is well-defined and computable on this hardware; only the vector codegen for the unary `~` is broken.\n\nNot a regression — see Provenance.\n\n<details>\n<summary>nvcc error and emitted line (int32 and int16)</summary>\n\n```\ntvm_kernels.cu(19): error: no operator \"~\" matches these operands\n            operand types are: ~ int4\n    *(int4*)(B + (((int)threadIdx.x) * 4)) = (~*(int4*)(A + (((int)threadIdx.x) * 4)));\n\n# int16 takes the same path -> short4:\n    *(short4*)(B + (((int)threadIdx.x) * 4)) = (~*(short4*)(A + (((int)threadIdx.x) * 4)));\n```\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\nimport numpy as np, torch\n\nN, TH = 128, 32          # N/TH = 4 elements per thread -> auto-vectorized to int4\n\n@T.prim_func\ndef bitnot(A: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=TH):\n        for i in T.Parallel(N):\n            B[i] = ~A[i]                 # <-- vectorized unary ~  => nvcc reject\n\n@T.prim_func\ndef control(A: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=TH):\n        for i in T.Parallel(N):\n            B[i] = A[i] ^ T.int32(-1)    # same complement via binary ^, SAME vector config\n\na   = np.arange(-64, 64, dtype=np.int32)\nexp = ~a\n\n# control: binary bitwise op scalarizes per lane -> compiles, correct\nkc  = tilelang.compile(control, out_idx=[1])\noc  = kc(torch.tensor(a, device=\"cuda\")).cpu().numpy()\nprint(\"control (^ -1):\", \"PASS\" if np.array_equal(oc, exp) else \"FAIL\")   # -> PASS\n\n# bug: vectorized unary ~ -> nvcc compile crash\ntry:\n    kb = tilelang.compile(bitnot, out_idx=[1])\n    ob = kb(torch.tensor(a, device=\"cuda\")).cpu().numpy()\n    print(\"bitnot  (~):\", \"PASS\" if np.array_equal(ob, exp) else \"FAIL\")\nexcept Exception as e:\n    print(\"bitnot  (~): COMPILE CRASH ->\", str(e).splitlines()[-1][:80])  # no operator \"~\" ... int4\n```\n\n### Traceback\n\nNo traceback in the Python sense — the failure is an `nvcc` compile error surfaced as a `RuntimeError` from `tilelang.compile`; the key line is:\n\n```\nerror: no operator \"~\" matches these operands\n            operand types are: ~ int4\n```\n\n### Expected behavior\n\nThe vectorized `~` should compute the elementwise complement and compile, matching the scalar path and the `^ -1` binary path, both of which return the correct result over the same inputs on this hardware. The binary bitwise operators already handle the vector case by scalarizing per lane; the natural expectation is that unary `~` does the same rather than emitting `~` on a packed vector type CUDA has no operator for.\n\n### Additional context\n\n**Root cause.** The C codegen's `bitwise_not` handler does not scalarize a vector operand, so for a vectorized lane it emits `~` applied to a packed vector type (`int4`/`short4`) that has no `operator~`. The binary bitwise/shift operators go through [`PrintBinaryIntrinsic`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L536), which branches on `op->dtype.lanes()` and, for `lanes() > 1`, calls `PrintVecBinaryOp` to emit the op lane-by-lane. The [`bitwise_not` branch](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L677) has no such lanes check — it unconditionally emits `os << \"(~\"; PrintExpr(arg); os << \")\"`, which becomes `(~*(int4*)...)` for a vectorized store. `int8` dodges it because 4×int8 is vectorized as a single scalar `int` reinterpret (`~*(int*)...`), on which `~` is legal C. The CUDA codegen (`codegen_cuda.cc` at `8df8ebd6`) overrides neither the `bitwise_not` branch nor `NotNode`, so the base-class scalar `~`/`!` reaches nvcc verbatim.\n\n**Suggested fix.** One direction is to give `bitwise_not` the same lane handling the binary intrinsics have — when `op->dtype.lanes() > 1`, emit the complement per lane (the analogue of `PrintVecBinaryOp`) rather than `~` on the whole vector. Whether that is preferable to declining to vectorize the unary `~` in the vectorizer is a call for the maintainers; either way the current outcome (a hard nvcc reject on a legal, computable elementwise op) seems to be the problem.\n\n**Generalization (roots + 4-axis sweep, all cells RUN on 0.1.13 / sm_89).**\n\n*Two-level root.*\n- **SOURCE-level:** `CodeGenC::VisitExpr_(CallNode)` `bitwise_not` branch at [`codegen_c.cc:677`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L677) emits `os << \"(~\"; PrintExpr(arg); os << \")\"` with **no** `op->dtype.lanes() > 1` guard — unlike `PrintBinaryIntrinsic` ([`codegen_c.cc:536`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L536)) which routes `lanes>1` to `PrintVecBinaryOp`. The class is: **the unary-operator visitors in `CodeGenC` that print their operator token directly onto a possibly-vectorized operand, lacking the per-lane scalarize the binary path has.** There are exactly two such sites: the `bitwise_not` branch and `NotNode` ([`codegen_c.cc:617`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L617), `os << '!'`). `codegen_cuda.cc` overrides neither.\n- **OPERATOR-level:** the TIR unary builtins/nodes that go through those two sites — `bitwise_not` (`~`) and logical `Not` (`!`). Adjacent-looking unary spellings (`-x`) do **not** route here.\n\n*4-axis sweep.*\n\n| axis | cell tested (vectorized `T.Parallel`, N=128 TH=32 → 4/thread) | observed result | same-root? |\n|------|--------------------------------------------------------------|-----------------|------------|\n| related-operator | logical `not A[i]` on `bool` (`NotNode`) | nvcc `no operator \"!\" matches these operands / operand types are: ! ushort4` | **same** (sibling site, line 617) |\n| related-operator | unary neg `-A[i]` on int32 | COMPILED OK + values PASS; emitted per-lane `__1.x = (v_.x*v__1.x)` (lowered to `Sub`/mul-by-−1, routes through binary `PrintVecBinaryOp`) | **distinct** (not a unary visitor) |\n| related-type | int16 `~A[i]` | nvcc `operand types are: ~ short4` | same |\n| related-type | uint32 `~A[i]` | nvcc `operand types are: ~ uint4` | same |\n| related-type | int64 `~A[i]` (N=128 → longlong2) | nvcc `operand types are: ~ longlong2` | same |\n| related-type | int8 `~A[i]` | COMPILED OK + values PASS; emitted `(~*(int*)...)` — 4 bytes reinterpreted as scalar `int`, `~` legal & bit-correct | **dodges** (scalar-int path, not packed vector) |\n| similar-logic | binary `A[i] ^ -1` (draft control), same vector config | COMPILED OK + PASS (routes through `PrintBinaryIntrinsic` → per-lane) | n/a — the correctly-scalarized comparand |\n| related-source | `NotNode` (same file, sibling unary visitor) | see logical-`not` row above — crashes identically | same |\n\n**Reframe decision.** The defect is a **two-site class**: `bitwise_not` (`~`, `no operator \"~\" ... {int4,short4,uint4,longlong2}`) and `NotNode` (`!`, `no operator \"!\" ... ushort4`). Both are the same missing-per-lane-scalarize pattern in the same file, and both were run and crash identically across integer widths. Kept as **two separate issues** (this one = the bitwise `~` half; the logical `!` half is filed separately) because they are two distinct visitor sites and can be fixed independently — but they should reference each other as one root class.\n\n**Distinct-adjacent (not filed):** unary neg `-x` looks like a sibling but is lowered to a binary `Sub`/multiply and scalarizes correctly (PASS) — a *distinct, non-bug* path, recorded so the class is not over-claimed to \"all unary ops\". No NEW bug was found while sweeping.\n\n**Example-run (PART 1):** `examples/deepseek_v32/topk_selector.py` — RAN verbatim on 0.1.13, compiles and passes; its `~bits_uint` is scalar-inside-`if_then_else`, so it **dodges** this bug (confirms the draft's Reach claim, corrected in the Reach section).\n\n**Provenance.** Present in the vendored TVM C codegen (`3rdparty/tvm`, pinned at `8df8ebd6` for the 0.1.13 tag). The `bitwise_not` branch has carried this scalar-only form since it was vendored; introduced-vs-relocated before the TileLang fork is unverified. Not introduced by a recent change.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of a vectorized `bitwise_not` codegen failure.\n\n**Reach.** `~` in a kernel maps to the documented TIR `bitwise_not` builtin and is used in-kernel in a shipped example (`examples/deepseek_v32/topk_selector.py` uses `~bits_uint`), so the operator is in real use. I ran that shipped example verbatim on 0.1.13 (`git show v0.1.13:examples/deepseek_v32/topk_selector.py`, default `test_topk_selector` config): it compiles and passes (`selected/all: 2048 / 2048 = 1.0` for every batch). It dodges this bug because its two `~` uses are on a scalar `bits_uint` inside `T.if_then_else`, not in a vectorized `T.Parallel` store, so the `~` never lands on a packed vector type. Triggering the crash requires the elementwise loop to auto-vectorize — a thread covering >1 element (`threads < N`), the common default for a plain elementwise `T.Parallel` kernel — on a 16-, 32-, or 64-bit integer. `int8` dodges for a different reason: 4×int8 vectorizes by reinterpreting the four bytes as a single scalar `int` (`*(int*)`), so the emitted `~` lands on a scalar C `int`, which is legal and — because bitwise-NOT distributes over byte boundaries — also numerically correct (verified PASS). No test exercises a vectorized elementwise `~`, which is why CI is green.\n\n**Impact.** The trigger is narrow: a vectorized elementwise `~` on a 16/32-bit int, i.e. a thread covering >1 element (`threads < N`) — the common default, but int8 and the scalar/`^ -1` paths all dodge it. When it fires it is a hard `nvcc` compile error, so it fails loudly at build time on that exact kernel and corrupts no data and cannot slip into a running workload silently. Fixing it closes a vector-codegen gap for one unary builtin, letting a legal, computable elementwise op build the same way its binary-bitwise siblings already do.", "url": "https://github.com/tile-ai/tilelang/issues/2993"}
{"number": 2976, "title": "[BUG][Fuzzer][wrong-code] `~` on a `bool` buffer silently returns all-True instead of the logically-negated bool", "state": "closed", "created_at": "2026-08-15T08:52:56Z", "updated_at": "2026-09-16T04:21:07Z", "closed_at": "2026-09-16T04:21:07Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n- [x] I have tried the latest version of TileLang.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 / bundled TVM fork `tile-ai/tvm@8df8ebd6` · PyTorch 2.13.0+cu130 · CUDA 13.0 · Python 3.13 · GPU: NVIDIA L40S (sm_89). Root cause is in an architecture-independent code path (frontend gate + generic C codegen), so the wrong result is expected on any target.\n\n### Problem description\n\nThe bitwise-not operator `~` on a `bool` buffer silently returns `True` for **every** element, regardless of input. A kernel that stores `B[i] = ~A[i]` over `A = [True, False, True, False]` produces `[True, True, True, True]` instead of the expected `[False, True, False, True]`. No error, no warning — the kernel compiles and runs to completion with a deterministically wrong result.\n\nThe same `~` operator is correct on every integer width — `int8`/`int32`/`uint8` all match NumPy/PyTorch exactly (verified below) — so the defect is specific to the `bool` operand of `~`. The other bitwise operators (`&`, `|`, `^`) on `bool` are also correct; only `~` miscompiles. The computed value is genuinely wrong, not just its bool read-back: `~bool` cast to `int32` returns `[-2, -1, -2, -1]` instead of `[0, 1, 0, 1]` (see below).\n\nNot a regression — see Provenance.\n\n<details>\n<summary>Console: bool wrong, int8/int32 controls correct</summary>\n\n```\n# bool\ninput : [True, False, True, False]\ngot   : [True, True, True, True]\nexpect: [False, True, False, True]      # numpy/torch: (~torch.tensor([...bool...]))\n\n# int32 control  -> correct\ngot   : [-2, -1, -2, -1]   expect [-2, -1, -2, -1]   PASS\n# int8 control   -> correct\ngot   : [-2, -1, -6, 0]    expect [-2, -1, -6, 0]    PASS  (matches numpy ~int8)\n```\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\n# --- bug: ~ on a bool buffer ---\n@T.prim_func\ndef bool_not(A: T.Tensor((4,), \"bool\"), B: T.Tensor((4,), \"bool\")):\n    with T.Kernel(1, threads=1):\n        for i in T.serial(4):\n            B[i] = ~A[i]\n\n# --- control: ~ on int32 (identical kernel, int dtype) ---\n@T.prim_func\ndef int_not(A: T.Tensor((4,), \"int32\"), B: T.Tensor((4,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for i in T.serial(4):\n            B[i] = ~A[i]\n\na_bool = torch.tensor([True, False, True, False], dtype=torch.bool, device=\"cuda\")\nout_bool = tilelang.compile(bool_not, out_idx=[1])(a_bool)\nprint(\"bool  ~:\", out_bool.tolist())      # -> [True, True, True, True]   (WRONG)\nprint(\"expected:\", (~a_bool).tolist())    # -> [False, True, False, True] (torch reference)\n\na_int = torch.tensor([1, 0, 1, 0], dtype=torch.int32, device=\"cuda\")\nout_int = tilelang.compile(int_not, out_idx=[1])(a_int)\nprint(\"int32 ~:\", out_int.tolist())       # -> [-2, -1, -2, -1]           (correct)\n```\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion; the result is silently wrong and deterministic.\n\n### Expected behavior\n\n`~` on a `bool` buffer should return the logically-negated bool (`~True → False`, `~False → True`), matching NumPy (`np.invert`) and PyTorch (`~torch.bool_tensor`), both of which return `[False, True, False, True]` for this input. The same logical negation is already computable in TileLang today via a comparison — `B[i] = A[i] == False` returns `[False, True, False, True]` correctly — so the value is not intractable, only the `~` lowering drops it.\n\nNote this is not merely a mismatch with numpy/torch — it is inconsistent with TileLang's *own* type system: `bool` is a 1-bit type (`DataType::Bool().bits() == 1`, and the codegen keys on `t.bits() == 1`). A bitwise-NOT of a 1-bit value is `~0→1`, `~1→0` — exactly logical negation, always in `{0,1}`. Emitting a bare C `~` cannot honor that, because C's integer-promotion rules widen a `bool` operand of `~` to a 4-byte `int` before complementing (confirmed by compiling the emitted expression: `sizeof(~(bool)true) == 4`, and `~true → -2`, `~false → -1`), so the result is always an out-of-range `int`, never a bool. The codegen therefore has to special-case a `bool` operand — emit logical negation (`!x`) or renormalize (`(~x) & 1`) — rather than defer to C's `~`, which is structurally incapable of producing a bool here. Alternatively, if `~` on `bool` is considered out of scope, the frontend could reject it the way it already rejects `~` on float; what it should not do is silently return values (`-1`/`-2`, all-`True`) outside `{True, False}`.\n\n### Additional context\n\n**Root cause.** The `~` frontend gate admits `bool` operands but the codegen emits an unmasked C `~`, whose result is never renormalized back into `{0, 1}` — so both `~false` and `~true` come out non-zero and read back as `True`. Concretely, [`ExprOp.__invert__`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/tirx/expr.py#L157-L160) rejects only *float* operands (`_dtype_is_float`, which checks only [`DataTypeCode.FLOAT`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/tirx/expr.py#L62-L64)) and passes everything else — including `bool` — to `bitwise_not`; the C codegen at [`codegen_c.cc` `bitwise_not`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L677-L681) then emits a bare `(~expr)` with no per-dtype handling.\n\n<details>\n<summary>Emitted CUDA + why every element becomes <code>True</code></summary>\n\nThe generated kernel body is:\n\n```c\nB[i] = ((signed char)(~((bool)A[i])));\n```\n\nIn C/C++, `~` integer-promotes its `bool` operand to `int`, so `~false → ~0 → -1` and `~true → ~1 → -2`. Both are non-zero, so when the byte is read back as a `bool`, both are `True`:\n\n| input | `~(bool)x` | stored byte | read as bool | correct? |\n|---|---|---|---|---|\n| `false` | `-1` | `0xFF` | `True` | accidentally right |\n| `true`  | `-2` | `0xFE` | `True` | **wrong** (should be `False`) |\n\nInteger dtypes escape this because the truncated low bits are the correct value and are read back as an integer, not as a truthiness test — which is why `int8`/`int32` are correct.\n\nThe `-1`/`-2` is not just a read-back artifact — the computed value is itself wrong, and it leaks into any downstream integer use. `C[i] = T.Cast(\"int32\", ~A[i])` returns `[-2, -1, -2, -1]` where `~bool` cast to int should be `[0, 1, 0, 1]` (torch). So both halves of the result are wrong; the all-`True` read-back merely hides it (both `-1` and `-2` collapse to `True`).\n</details>\n\n<details>\n<summary>Scope: only <code>~</code> is affected; <code>&amp;</code> <code>|</code> <code>^</code> on bool are correct</summary>\n\nThe defect is specific to `~` — it is the only bitwise operator that produces bits above bit 0 from a `bool` operand. The others stay within `{0,1}` and are correct on the same inputs (verified this session, `A=[T,F,T,F]`, `B=[T,T,F,F]`):\n\n| op on bool | result | correct? |\n|---|---|---|\n| `A & B` | `[T,F,F,F]` | yes |\n| `A \\| B` | `[T,T,T,F]` | yes |\n| `A ^ B` | `[F,T,T,F]` | yes |\n| `~A` | `[T,T,T,T]` | **no** |\n\n`~` on integer dtypes (`uint8`/`int8`/`int32`) is also correct — only the `bool` operand of `~` miscompiles.\n</details>\n\n**Suggested fix.** For a `bool` (1-bit) operand, `~` needs to produce the negated boolean rather than a raw bitwise complement — e.g. emit `!x` (or `(x) ^ true`), or normalize the result to `{0,1}`. If instead `bool` is meant to be out of scope for `~`, the cleaner place is the `__invert__` gate, which already special-cases float — extending it to reject `bool` would surface the misuse instead of miscomputing. Which direction is right depends on whether `~` on `bool` is intended to be supported; NumPy/PyTorch both define it as logical negation.\n\n**Provenance.** The `__invert__` float-only gate and the generic `bitwise_not` codegen are both present at the TVM fork commit pinned by tilelang 0.1.13 (`8df8ebd6`); reproduced at runtime on 0.1.13 this session (bool `[T,F,T,F]` → `[T,T,T,T]`, int8/int32 controls correct). Not a recent regression — origin before this pin not traced.\n\n**Dedup.** No filed issue covers scalar `~`/`bitwise_not` on `bool`. The nearest bool-typed item is the reduce-op crash (`T.reduce_max`/`reduce_min`/`reduce_bitand` on a `bool` buffer, #2568) — a different path (the reduce init-value builder) and a different symptom (internal assert / crash), not this operator's silent integer-promotion.\n\n**Reach.** The trigger is the intersection of a common operator and a rare operand type: `~` is a core builtin, but the miscompile fires only when its operand is a `bool` buffer inside device code (every integer width is correct). Grepping `examples/` and `testing/` this session: `bool` tensors do appear in examples (8 files, e.g. the deepseek sparse-MLA kernels and blocksparse GEMM), and `~`/`bitwise_not` do appear — but every `~`-on-bool occurrence is in *host-side* reference/mask code (`masked_fill(~mask, …)`, numpy `~m0 & m1`), and the one `bitwise_not` in tests is a torch host reference on ints. No kernel (device) `~` on a `bool` buffer exists in the shipped tree, which is why CI stays green.\n\n**Impact.** The trigger is narrow — `~` must be applied to a `bool` operand in device code, and no shipped kernel does this today (every integer width is correct). When it does fire it is silent and deterministic, and the computed value is genuinely wrong rather than just a bool read-back artifact: `~bool` cast to int yields `-1`/`-2` instead of `0`/`1`, so it corrupts both boolean masks (all elements read back `True`) and any integer arithmetic derived from them.\n", "url": "https://github.com/tile-ai/tilelang/issues/2976"}
{"number": 3235, "title": "[BUG] Unsigned condition analysis constructs a negative unsigned coefficient", "state": "open", "created_at": "2026-09-15T12:47:59Z", "updated_at": "2026-09-15T12:47:59Z", "closed_at": null, "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.14, source build at `3a1aeb08`\n\n### System information\n\nReproduced on Windows (Python 3.12.13, PyTorch 2.11.0+cu130) and Linux\n(Python 3.13, PyTorch 2.7.1+cu128), using matching TileLang source builds.\nThe reproducer only lowers IR; device compilation and execution are disabled.\n\n### Problem description\n\nA valid `uint32` zero comparison fails during block read/write-region analysis\nwith `cannot make uint from negative value -1`. **There is no negative constant\nin the user program.** The negative value is introduced by the constraint solver.\n\nChanging the input dtype to `int32` succeeds; `uint64` also fails.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\n@T.prim_func\ndef kernel(A: T.Tensor((32,), 'uint32'), B: T.Tensor((2, 32), 'int32')):\n    with T.Kernel(1, threads=32):\n        tx = T.get_thread_binding()\n        for i in T.serial(2):\n            scratch = T.alloc_var('int32', init=0)\n            mask = A[tx]\n            B[i, tx] = T.if_then_else(mask != 0, scratch + 1, 0)\n\ntilelang.lower(\n    kernel,\n    target={'kind': 'cuda', 'arch': 'sm_120'},\n    enable_host_codegen=False,\n    enable_device_compile=False,\n)\n```\n\n### Traceback\n\n```pytb\nPlanAndUpdateBufferAllocationLocation\n  -> GetSBlockReadWriteRegion\n  -> ConditionalBoundsContext::TrySolveCondition\n  -> SolveInequalitiesToRange\n  -> SolveLinearInequalities\n  -> make_const / MakeConstScalar\ntvm.error.InternalError: cannot make uint from negative value -1\n```\n\n### Expected behavior\n\nSuccessful lowering: `B[i, tx] = int(A[tx] != 0)`. All accesses are in bounds\nand the local variable is initialized. Unsupported constraint refinement should\nconservatively retain valid access regions rather than reject this program.\n\n### Additional context\n\nThe loop-local allocation triggers region analysis. The analyzer visits both\nbranches, including the implicit else condition **`mask == 0`**. Solving this\nequality introduces negative coefficients, but `SolveLinearInequalities` creates\nthem using the unsigned variable's dtype. This conflates the program's unsigned\nvalue domain with the solver's internal coefficient representation.\n\nThe same failure reproduces by directly calling\n`s_tir.analysis.GetSBlockReadWriteRegion` on a small equivalent SBlock, without\nthe TileLang pass pipeline. Merely wrapping `-1` as an unsigned constant would\nnot preserve ordered-integer inequality semantics.\n\nRelated search result: [#1299](https://github.com/tile-ai/tilelang/issues/1299)\nhas the same error text but concerns the explicit literal `0xFFFFFFFF` during\nexpression construction; this reproducer has no such literal and fails in\nconstraint analysis instead.", "url": "https://github.com/tile-ai/tilelang/issues/3235"}
{"number": 3075, "title": "[BUG] $ORIGIN RUNPATH breaks under symlink-based installs (uv --link-mode symlink) → OSError: libz3.so.X cannot open shared object file", "state": "closed", "created_at": "2026-08-24T07:12:54Z", "updated_at": "2026-09-15T04:09:35Z", "closed_at": "2026-09-15T04:09:35Z", "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.11\n\n### System information\n\n3.13.14 (main, Jun 23 2026, 15:18:27) [Clang 22.1.3 ] linux\n0.1.11\n2.11.0+cu130\n\n[pip3] nccl4py==0.3.1\n[pip3] numpy==2.5.1\n[pip3] nvidia-cublas==13.5.1.27\n[pip3] nvidia-cuda-cupti==13.0.85\n[pip3] nvidia-cuda-nvrtc==13.0.88\n[pip3] nvidia-cuda-runtime==13.0.96\n[pip3] nvidia-cudnn-cu13==9.20.0.48\n[pip3] nvidia-cufft==12.0.0.61\n[pip3] nvidia-curand==10.4.0.35\n[pip3] nvidia-cusolver==12.0.4.66\n[pip3] nvidia-cusparse==12.6.3.3\n[pip3] nvidia-cusparselt-cu13==0.8.0\n[pip3] nvidia-nccl-cu13==2.30.7\n[pip3] nvidia-nvjitlink==13.0.88\n[pip3] nvidia-nvtx==13.0.85\n[pip3] nvtx==0.2.15\n[pip3] open_clip_torch==3.3.0\n[pip3] torch==2.11.0+cu130\n[pip3] torch_c_dlpack_ext==0.1.5\n[pip3] torchdata==0.11.0\n[pip3] torchvision==0.26.0+cu130\n[pip3] triton==3.6.0\n\n### Problem description\n\n## Bug\n\n`import tilelang` crashes with:\n\n```\nOSError: libz3.so.4.15: cannot open shared object file: No such file or directory\n```\n\nwhen tilelang is installed with a symlink-based installer, e.g. `uv --link-mode symlink` (common for keeping large multi-venv images small).\n\n### Reproducible example code\n\n```bash\nuv venv --python 3.13 /tmp/repro-venv\nUV_PROJECT_ENVIRONMENT=/tmp/repro-venv uv pip install --link-mode symlink tilelang==0.1.11\n/tmp/repro-venv/bin/python -c \"import tilelang\"\n```\n\n\n### Traceback\n\n```pytb\n\n```\n\n### Expected behavior\n\n_No response_\n\n### Additional context\n\n## Root cause\n\n`libtvm_compiler.so`'s `INSTALL_RPATH` is set relative to `$ORIGIN`, assuming `tilelang`/`z3`/`tvm_ffi` are physical sibling directories under one `site-packages`:\n\nhttps://github.com/tile-ai/tilelang/blob/75382444d6b0e13347cb34c620308c095e087615/CMakeLists.txt#L666-L671\n\n```cmake\nelseif(UNIX)\n  set(TILELANG_BUILD_RPATH \"\\$ORIGIN\")\n  set(TILELANG_INSTALL_RPATH \"\\$ORIGIN:\\$ORIGIN/../../tvm_ffi/lib\")\n  if(USE_Z3 AND USE_PYPI_Z3)\n    string(APPEND TILELANG_INSTALL_RPATH \":\\$ORIGIN/../../z3/lib\")\n  endif()\n```\n\nWith `--link-mode symlink`, `site-packages/tilelang/lib/libtvm_compiler.so` is itself a symlink into a per-wheel cache dir (e.g. uv's `archive-v0/<hash>/tilelang/lib/`). Linux resolves `$ORIGIN` against the *realpath after following symlinks*, so `$ORIGIN/../../z3/lib` resolves inside tilelang's own private cache dir — not `z3`'s actual (differently-hashed) cache dir — so `libz3.so.4.15` can never be found, even though it exists on disk elsewhere.\n\nNotably, TileLang already works around this exact \"sibling package\" problem on Windows via `importlib.util.find_spec` + `os.add_dll_directory`, but it's gated to Windows only, with no Linux/macOS fallback:\n\nhttps://github.com/tile-ai/tilelang/blob/75382444d6b0e13347cb34c620308c095e087615/tilelang/env.py#L434-L461", "url": "https://github.com/tile-ai/tilelang/issues/3075"}
{"number": 3225, "title": "[BUG] Nested data-dependent serial loops inside T.Parallel fail during LowerTileOp", "state": "open", "created_at": "2026-09-14T14:48:03Z", "updated_at": "2026-09-14T14:48:03Z", "closed_at": null, "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.14+cuda.git030556de\n\n### System information\n\n- Windows x86-64; freshly rebuilt from commit `030556de976356b21061d413843b4a29f75b20a8`.\n- Python 3.12.13, PyTorch 2.11.0+cu130, apache-tvm-ffi 0.1.12.\n- RTX 5060 Laptop GPU; CUDA toolkit 13.2.78.\n\n### Problem description\n\nTwo nested `T.serial` loops with tensor-dependent bounds inside `T.Parallel` fail with an internal analyzer assertion in `tl.LowerTileOp`.\n\nThe example has fixed tensor shapes and only performs lowering: no GPU execution, NVCC compilation, or CUDA Graph is involved. Assume input bounds are in `[0, 4]`; each parallel iteration writes a distinct output slice.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\n\n@T.prim_func\ndef main(bounds: T.Tensor[(32, 2), \"int32\"],\n         out: T.Tensor[(32, 4, 4), \"int32\"]):\n    with T.Kernel(1, threads=32):\n        for p in T.Parallel(32):\n            for y in T.serial(bounds[p, 0]):\n                for x in T.serial(bounds[p, 1]):\n                    out[p, y, x] = p * 100 + y * 10 + x\n\n\ntarget = tilelang.tvm.target.Target({\"kind\": \"cuda\", \"arch\": \"sm_120\"})\nwith target:\n    tilelang.lower(main, target=target,\n                   enable_host_codegen=False, enable_device_compile=False)\n```\n\n\n### Traceback\n\n```pytb\nmod = tilelang.transform.LowerTileOp()(mod)\n  ...\n  File \"..\\3rdparty\\tvm\\src\\arith\\int_set.cc\", line 701,\n    in tvm::arith::IntSetAnalyzer::Impl::Update(...)\ntvm.error.InternalError: Check failed: (ExprDeepEqual()(old_info.max(), info.max())) is false:\nTrying to update var 'y' with a different maximum value:\noriginal=bounds[p, 0] - 1, new=bounds[tx, 0] - 1\n```\n\n### Expected behavior\n\nSuccessful lowering, allowing each thread to execute its own dynamically bounded serial loops.\n\n### Additional context\n\n- [#1442](https://github.com/tile-ai/tilelang/issues/1442), fixed by [#1446](https://github.com/tile-ai/tilelang/pull/1446): a related analyzer-binding conflict around loop partitioning/vectorization; that fix snapshots the analyzer before visiting the loop body in layout inference.\n- [#1472](https://github.com/tile-ai/tilelang/issues/1472), fixed by [#1649](https://github.com/tile-ai/tilelang/pull/1649): conflicting let-variable bindings in vectorization. This reproducer instead fails when binding a nested `For` variable's range.\n- [#1728](https://github.com/tile-ai/tilelang/issues/1728), fixed by [#1735](https://github.com/tile-ai/tilelang/pull/1735): the same maximum-value assertion, but in `Simplify`, rather than this reproducer's `LowerTileOp` path.", "url": "https://github.com/tile-ai/tilelang/issues/3225"}
{"number": 3223, "title": "[BUG] T.gemm silently downgrades WGMMA to mma.sync for FP8 operands on SM90", "state": "open", "created_at": "2026-09-14T13:28:11Z", "updated_at": "2026-09-14T14:19:54Z", "closed_at": null, "labels": ["bug"], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.14\n\n### System information\n\n3.12.3 linux\ntilelang 0.1.14\ntorch 2.11.0+cu130\ncuda 13.0\ngpu NVIDIA H800 SM 9.0\nnvcc release 13.0, V13.0.88\ninstalled via pip (wheel)\n\n### Problem description\n\n### Describe the issue\n\nOn Hopper, `T.gemm` silently drops from WGMMA to Ampere-class `mma.sync` when an\nFP8 operand is not K-major. There is no warning, no log, and no note in the\ngenerated code — the only way to notice is to dump the CUDA source and count\ninstructions.\n\n### Background\n\nThe FP8 form of `wgmma.mma_async` has no `imm-trans-a` / `imm-trans-b` operands,\nunlike the bf16/fp16 form, so both operands must be K-major. The toolchain\nenforces this:\n\n```\nwgmma.mma_async.sync.aligned.m64n8k16.f32.bf16.bf16 {...}, %a, %b, p, 1, 1, 0, 1;  // compiles\nwgmma.mma_async.sync.aligned.m64n8k32.f32.e4m3.e4m3 {...}, %a, %b, p, 1, 1, 0, 1;\n// ptxas: error : Arguments mismatch for instruction 'wgmma.mma_async with FP8 types'\n```\n\n`CheckWgmma()` in `src/cuda/op/gemm.cc` models this correctly:\n\n```cpp\nif (op.a_->dtype.is_float8() && op.b_->dtype.is_float8())\n  return (!op.transA_) && op.transB_ && op.k_ % 32 == 0;\n```\n\nbut when it returns false, `GetGemmInst()` just falls through:\n\n```cpp\nif (AllowTcgen5Mma(op, target)) return kCudaTCGEN05;\nif (AllowWgmma(op, block_size, target)) return kCudaWGMMA;\n...\nreturn kCudaMMA;   // no diagnostic\n```\n\n### Impact\n\nMeasured on an idle H800 with an FP8 sparse MLA forward kernel\n(S=8192, H=128, topk=2048, DQK=576, DV=512):\n\n| PV operand | time | throughput | instruction mix |\n| --- | --- | --- | --- |\n| row-major (silently `mma.sync`) | 25.79 ms | 181 TFLOP/s | 4 wgmma + 4 mma_sync + 2 ldmatrix |\n| K-major (WGMMA) | 14.24 ms | 328 TFLOP/s | 6 wgmma, no mma_sync |\n\n1.81x, entirely invisible to the user. The natural way to write the PV product\nof an attention kernel — `T.gemm(P_shared, V_shared, acc)` — is exactly the case\nthat hits this path.\n\nThis is not hypothetical: SGLang's production TileLang DSA kernel\n(`sparse_mla_fwd_decode_partial_fp8` in `tilelang_kernel.py`) has four PV gemms\nthat all land on the fallback path.\n\n### Suggested fix\n\nEmit a `LOG(WARNING)` in `GetGemmInst()` when `AllowWgmma()` returns false on a\nHopper target for a dtype that carries layout constraints (FP8 / INT8 / TF32),\nnaming the failing condition and how to fix it, e.g.\n\n```\nWGMMA disabled for this gemm: FP8 requires (!trans_A && trans_B) on SM90\n(got trans_A=0, trans_B=0); falling back to mma.sync. Store B as K-major to\nenable WGMMA.\n```\n\nThis matches the direction already taken elsewhere: `T.tcgen05_gemm` documents\nthat \"compilation fails instead of silently falling back if the requested ISA\npath is unavailable\", and #2117 moved FP6 vector-type rejection earlier for the\nsame reason.\n\n\n**Clarifying GitHub markdown formatting requirements**\n\n### Reproducible example code\n\n```python\nimport tilelang\nfrom tilelang import language as T\n\n\n@tilelang.jit(out_idx=[2])\ndef gemm_fp8(M, N, K, trans_B, threads=128):\n    b_shape = (N, K) if trans_B else (K, N)\n\n    @T.prim_func\n    def main(\n        A: T.Tensor((M, K), T.float8_e4m3),\n        B: T.Tensor(b_shape, T.float8_e4m3),\n        C: T.Tensor((M, N), T.float32),\n    ):\n        with T.Kernel(1, threads=threads):\n            A_s = T.alloc_shared((M, K), T.float8_e4m3)\n            B_s = T.alloc_shared(b_shape, T.float8_e4m3)\n            acc = T.alloc_fragment((M, N), T.float32)\n\n            T.clear(acc)\n            T.copy(A, A_s)\n            T.copy(B, B_s)\n            T.gemm(A_s, B_s, acc, transpose_B=trans_B)\n            T.copy(acc, C)\n\n    return main\n\n\nfor trans_B in (True, False):\n    src = gemm_fp8(64, 64, 64, trans_B).get_kernel_source()\n\n    print(\n        f\"transpose_B={trans_B!s:<5} \"\n        f\"wgmma={src.count('tl::wgmma')} \"\n        f\"mma_sync={src.count('tl::mma_sync')} \"\n        f\"ldmatrix={src.count('ptx_ldmatrix')}\"\n    )\n\n# Output:\n# transpose_B=True  wgmma=1 mma_sync=0 ldmatrix=0\n# transpose_B=False wgmma=0 mma_sync=2 ldmatrix=1\n```\n\n\n### Traceback\n\n```pytb\nN/A -- the kernel compiles and produces correct results; the issue is a silent\nperformance downgrade, not a crash.\n```\n\n### Expected behavior\n\nWhen `T.gemm` cannot use WGMMA on a Hopper target because of an operand layout\nconstraint that is specific to the dtype (FP8 / INT8 / TF32 all require\n`!trans_A && trans_B`), the compiler should say so.\n\nA one-line warning naming the failing condition would be enough, e.g.\n\n    WGMMA disabled for this gemm: FP8 requires (!trans_A && trans_B) on SM90\n    (got trans_A=0, trans_B=0); falling back to mma.sync. Store B as K-major to\n    enable WGMMA.\n\nRight now the only way to discover the downgrade is to call\n`get_kernel_source()` and count instructions, which is not something a user\nwould think to do -- the kernel works, it is just ~1.8x slower than it should be\nin a full attention kernel.\n\nRaising an error would also be defensible (`T.tcgen05_gemm` already documents\nthat it \"fails instead of silently falling back\"), but a warning keeps existing\ncode working.\n\n### Additional context\n\nFound while adding an FP8 sparse MLA forward example for DeepSeek V3.2. In that\nkernel the PV product contracts over the top-k tile dimension while V is stored\n`[block_I, dim]`, so the natural `T.gemm(P_shared, V_shared, acc)` hits this\npath: 25.79 ms instead of 14.24 ms at S=8192, H=128, topk=2048 on an idle H800.\n\nThe constraint itself comes from the instruction, not from TileLang -- the FP8\nform of `wgmma.mma_async` has no `imm-trans-a` / `imm-trans-b` operands, and\nptxas rejects them:\n\n    wgmma.mma_async.sync.aligned.m64n8k32.f32.e4m3.e4m3 {...}, %a, %b, p, 1, 1, 0, 1;\n    // ptxas: error : Arguments mismatch for instruction 'wgmma.mma_async with FP8 types'\n\nSo the fix is purely about reporting, not about capability.", "url": "https://github.com/tile-ai/tilelang/issues/3223"}
{"number": 3222, "title": "[Feature Request] [Metal] MLX adapter for Metal kernels (vllm-metal)", "state": "open", "created_at": "2026-09-13T23:09:13Z", "updated_at": "2026-09-13T23:09:13Z", "closed_at": null, "labels": ["enhancement"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Motivation\n\nWe work on vLLM-metal (https://github.com/vllm-project/vllm-metal), which uses MLX and has many handwritten Metal kernels. These kernels are difficult to write, review, and maintain.\n\nI recently discovered TileLang's Metal DSL and would like to experiment with it as an alternative. \n\nI tried a small local experiment on GDN in-place cache updates, and it indeed made the kernel code easier to express!!\n\nHowever, the existing PyTorch Metal adapter does not directly fit our MLX runtime, so the experiment required custom MLX integration.\n\nWould you be open to supporting an MLX adapter?\n\n\n### Solution\n\n_No response_\n\n### Alternatives\n\n_No response_\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/3222"}
{"number": 3220, "title": "[Transform][Arith] ConstrSet::Populate ordering is load-bearing in two incompatible directions (eager Analyzer::Bind snapshots)", "state": "open", "created_at": "2026-09-13T17:31:16Z", "updated_at": "2026-09-13T17:31:16Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "## Summary\n\n`ConstrSet::Populate` (in `src/transform/common/constr_visitor.h`) replays a lexically-collected list of binds and predicates into a fresh `arith::Analyzer`. The replay **order** turns out to be load-bearing in two *incompatible* directions:\n\n- **Program order** (current upstream behavior, rationale documented since #2805) is required so that a bind evaluated under a preceding predicate captures the tightened bound.\n- **Binds-first** (what our downstream Ascend fork carries) is required so that a predicate held in a bound boolean var can be expanded back into the condition it represents, even after `Merge` / `RenameFrom` scrambled the definition order.\n\nBoth orderings are workarounds for the same root cause: `arith::Analyzer::Bind` **eagerly snapshots** the value's integer bounds at bind time and never re-evaluates them. We would like to converge on one shared implementation instead of carrying a downstream divergence, and we have empirical data plus three candidate fixes below. We are happy to send a PR once a direction is agreed on.\n\n## Background\n\n`ConstrVisitor` (introduced in #1622, used by ThreadSync since #1631, extended with snapshot mode in #3174) collects two kinds of entries while walking the IR:\n\n- **binds** — `v = expr` (flat `Bind`) or `v ∈ range` (`thread_extent`), and\n- **predicates** — branch conditions, asserts, assumes.\n\n`ConstrSet::Populate` replays them into an analyzer so passes can ask `CanProve` questions (cross-thread race checks, and in our fork, sync-flag allocation).\n\nThe two analyzer entry points have **opposite information-flow requirements**:\n\n- `Analyzer::Bind(v, expr)` eagerly evaluates and caches the value's bounds *at bind time* (vendored TVM, `src/arith/analyzer.cc`):\n\n  ```cpp\n  void Analyzer::Bind(const Var& var, const PrimExpr& expr, bool allow_override) {\n    ...\n    this->const_int_bound.Update(var, this->const_int_bound(new_expr), allow_override);\n    this->modular_set.Update(var, this->modular_set(new_expr), allow_override);\n    ...\n  }\n  ```\n\n  So a bind wants every predicate that constrains its value to be entered **before** it.\n\n- `EnterConstraint(pred)` digests the predicate at entry. If `pred` is a bound boolean var (`cond` where `cond = (i < n)` is a bind), the analyzer can only digest it after simplification expands it — which requires the bind to be installed **before** the predicate.\n\nNo single static order satisfies both once the sequence is scrambled.\n\n## The two orderings and where each one is needed\n\n### Program order (upstream, since #2805)\n\n```\nif (tx < 64) {     // predicate P: tx < 64\n  v = tx;          // bind B: v = tx\n}\n```\n\nCollected order `[P, B]`. Program-order replay enters `tx < 64` first, so `Bind(v, tx)` snapshots `v ∈ [0, 63]`. Binds-first replay snapshots `v` with only the thread-extent bound (e.g. `[0, 255]`) and the cached bound never tightens afterwards. This is exactly the comment in the current code:\n\n> Keep program order: `Analyzer::Bind` evaluates the bounds and modular set of the value at bind time, so entering the binds first would widen them -- a `v = tx` inside `if tx < 64` would lose its upper bound.\n\n### Binds-first (our downstream fork)\n\nOur control-flow normalization (while-loop modeling for the Ascend backend) names branch conditions:\n\n```\ncond = (i < n)     // bind B\nif (cond) { ... }  // predicate P\n```\n\nIn straight-line collection `B` precedes `P` and *program order works fine too*. The problem appears when constraint sets are **merged**: our auto-schedule flag allocation merges per-task `ConstrSet`s (`Merge` appends the other set's entries), so a predicate `cond` originating from set A can end up *before* the bind `cond = (i < n)` originating from set B. Program-order replay then enters an opaque boolean the analyzer cannot use; the guard fact is effectively lost, guard disjointness becomes unprovable, and flag-slot reuse is rejected. Nine of our downstream tests fail with program order and pass with binds-first.\n\nNote this also requires simplifying the predicate at entry (`EnterConstraint(analyzer.Simplify(c.value), ...)` in `Constr::Populate`) so the bound boolean actually expands — a second small delta we carry.\n\n## Empirical evidence\n\n- We ran the **entire upstream `testing/python/transform` suite under both orderings** (including the thread-sync tests added by #2805) on our runners: the results are *identical* per test. As far as current upstream tests are concerned, the two orderings are indistinguishable — presumably because `CanProve` queries on the `v = tx` pattern can still succeed through the rewrite path (`v → tx` substitution plus the scoped constraint) even when the cached `const_int_bound` is wide.\n- Our downstream suite strictly requires binds-first (9 failures otherwise, all in sync-flag allocation on merged constraint sets).\n\nSo today the divergence costs upstream nothing observable, but we would much rather delete it than depend on that remaining true.\n\n## A minimal illustration of the root cause\n\n```cpp\narith::Analyzer a;\na.Bind(tx, Range(0, 256));                 // tx ∈ [0,255]\na.Bind(v, tx);                             // eager snapshot: v ∈ [0,255]\n{\n  With<arith::ConstraintContext> ctx(&a, tx < 64);\n  a.const_int_bound(tx);                   // [0,63]  — evaluated on demand, fresh\n  a.const_int_bound(v);                    // [0,255] — bind-time snapshot, stale\n}\n```\n\n`v ≡ tx` yet their bounds disagree under the same constraint stack. Every ordering debate in `Populate` is downstream of this premature evaluation.\n\n## Candidate fixes, ranked by invasiveness\n\n1. **Dependency-aware ordering in `Merge` (preferred, least invasive).** Keep program order *within* each source set, and topologically hoist a bind before any predicate that references its var when concatenating sets. Binds cannot form cycles in SSA form, so the order always exists. Straight-line behavior (the #2805 guarantee) is untouched; only scrambled merged sequences are repaired. We can send a PR for this, together with the `Simplify`-at-entry change and regression tests for the merged-set case.\n2. **Two-phase `Populate` (binds first, then predicates).** Simplest; it is what we run today, and per the evidence above it does not change any current upstream test outcome. Downside: it silently gives up the bind-time tightening guarantee that #2805 documented, and that guarantee may be protecting untested cases.\n3. **Lazy / re-evaluable `Bind` in `arith::Analyzer` (root fix, heaviest).** Store the definition rather than the bound snapshot, and evaluate bounds on demand under the constraint stack active at query time (memoized per constraint epoch). This dissolves the ordering question entirely, but it touches a hot path (`const_int_bound` inside `Simplify`) in vendored TVM and has a large blast radius; we mention it for completeness rather than as a near-term proposal.\n\n## What we would like from this issue\n\n- Confirm whether the #2805 program-order guarantee protects any scenario beyond the documented `v = tx` example (we could not find a discriminating test).\n- Pick a direction between (1) and (2). We will follow up with a PR including the merged-set regression tests either way.\n", "url": "https://github.com/tile-ai/tilelang/issues/3220"}
{"number": 3175, "title": "[Transform] FreshenMutableReads: rationale for Mode and a path to remove it", "state": "closed", "created_at": "2026-09-07T07:40:13Z", "updated_at": "2026-09-10T16:15:46Z", "closed_at": "2026-09-10T16:15:46Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "`FreshenMutableReads` keeps mutable reads out of persistent arithmetic-analyzer substitutions. #3174 proposes an explicit `Mode` so the existing visitor can also serve analyses that describe one captured value in several expressions. This issue records the rationale and proposes a path to remove that mode once captured values have explicit identities in the analysis representation.\n\nRelated: #2805 introduced the upstream per-occurrence behavior; #3174 adds the opt-in snapshot behavior. The implementation discussed here is [the version proposed in #3174](https://github.com/tile-ai/tilelang/blob/093a568c3dff0bfbc3bbd56479235fb7a8ad8b00/src/transform/common/constr_visitor.h).\n\n**What `FreshenMutableReads` does**\n\n`Analyzer::Bind(v, expr)` installs a reusable substitution. Keeping an expression that reads mutable state in that substitution can incorrectly equate a value captured earlier with a later read:\n\n```text\nv = A[i]        // v captures the old value\nA[i] = new_value\n... A[i] ...   // this read need not equal v\n```\n\nThe helper replaces mutable or state-dependent expressions with fresh symbolic variables before such definitions are bound. The current implementation handles buffer loads, producer loads, reductions, and calls whose effect classification (including their arguments) is not pure. It preserves ordinary SSA variables and pure arithmetic around the replaced expressions.\n\nThis is an analysis abstraction: it neither inserts runtime loads nor makes memory immutable. It deliberately loses some information so that the analyzer does not invent equalities across stores, access points, or independently modeled executions. `Constr::FreshenReads()` applies it to value/range bindings before `Constr::Populate()` installs them in the analyzer.\n\n**Why introduce two modes?**\n\nTwo uses currently present similar expression trees with different meanings:\n\n| Mode | Caller contract | Replacement |\n| --- | --- | --- |\n| `kPerOccurrence` (unchanged default) | Each occurrence may be a separate evaluation. | Allocate an independent unknown for each visited occurrence. |\n| `kSnapshot` (opt-in) | Repeated structurally equal expressions refer to the same captured value within this instance. | Reuse an unknown through an instance-local `memo_`. |\n\nFor example, two evaluations of an opaque call must remain independent:\n\n```text\nf() - f()  ->  u0 - u1\n```\n\nMemoizing those calls would produce `u0 - u0` and incorrectly prove zero. Likewise, identical-looking loads on opposite sides of a store must not be equated. Structural equality, or even reuse of the same IR node object, does not establish a shared runtime evaluation.\n\nThe motivating snapshot use is Ascend region dependency analysis. When one captured region base is copied into derived bounds, both copies must retain the same symbolic value:\n\n```text\nmin = captured_base\nmax = captured_base + 15\n\nper occurrence: min = u0, max = u1 + 15\nsnapshot:       min = u0, max = u0 + 15\n```\n\nThe second representation retains `max - min == 15`. Losing that relationship can prevent valid disjointness proofs and retain unnecessary dependencies or synchronization. Producer and consumer access points use separate snapshots because mutable state may change between them.\n\nThe Ascend integration currently needs a separate `SnapshotFreshenMutableReads` visitor for this behavior. #3174 consolidates the traversal while making the semantic choice explicit and preserving all existing default callers. The enum is scoped to `FreshenMutableReads` because it describes this visitor's contract.\n\nSnapshot mode relies on the caller to establish the shared-value contract. The mode itself does not prove that reads are stable or prevent a caller from merging independent stateful evaluations. One visitor per access point is necessary for this use, but does not by itself justify merging every identical expression inside that access point.\n\n**Proposed path to remove `Mode`**\n\nMove captured-value identity into the region analysis data, at the point where the origin of each evaluation is still known:\n\n1. Represent each actual mutable evaluation by an explicit symbolic variable before deriving or duplicating bounds. Reuse that variable wherever the analysis refers to that captured result. Distinct evaluations, including separate opaque calls, get distinct variables. Preserve any existing SSA value identity.\n2. Derive min/max/extent and other region expressions from those variables. For a range with a captured minimum, the essential change is to freshen the minimum once and construct the maximum from the resulting expression, rather than freshening two already-expanded bounds independently:\n\n```text\nmin = freshen(raw_min)          // ordinary per-occurrence freshening\nextent = freshen(raw_extent)\nmax = min + extent - 1\n```\n\nThis illustrates the simple case where `raw_min` and `raw_extent` are independent source expressions. If several fields or dimensions already refer to one captured result, their common variable must be established before those fields are built. Freshening every field separately after that provenance has been lost is insufficient.\n\n3. Carry these identities through region construction, substitutions, and comparisons. Keep independent access points independent, and rename execution-private symbols when modeling different threads or loop iterations. Start with analysis-local data; introducing these symbols does not require inserting runtime captures or a whole-program memory-SSA pass.\n4. Migrate the snapshot callers to that representation. Once no supported caller needs structural memoization to recover captured-value identity, remove `kSnapshot`, the `Mode` constructor parameter, and `memo_`. Keep `FreshenMutableReads` with its original per-occurrence semantics for remaining mutable expressions.\n\nThe intended reduction is to preserve identity when constructing analysis expressions, eliminating the need to reconstruct it later with a policy switch. Moving the same structural memoization into another generic visitor would leave the underlying ambiguity in place.\n\n**Completion criteria**\n\n- [ ] Audit snapshot consumers and identify where each captured value originates, including relationships across region fields and dimensions.\n- [ ] Preserve region-bound relationships such as `max - min == extent - 1` without snapshot-mode freshening.\n- [ ] Test separate opaque evaluations, reads separated by writes (including aliases), and independent producer/consumer, thread, and iteration instances; none may acquire unsupported equalities.\n- [ ] Retain tests where several derived expressions intentionally share one captured result, including multi-dimensional regions.\n- [ ] Validate ThreadSync, VerifyParallelLoop, and the affected Ascend dependency/synchronization paths, checking both correctness and unwanted increases in dependencies or barriers.\n- [ ] Remove all snapshot-mode consumers, then delete the enum and memoization.\n\nThis is a follow-up design proposal. #3174 provides the explicit contract needed by current callers; it does not implement the representation change above or make the mode immediately removable.\n", "url": "https://github.com/tile-ai/tilelang/issues/3175"}
{"number": 3188, "title": "[Maint] layout_inference golden snapshots are stale on main; harness has no CI hook", "state": "open", "created_at": "2026-09-09T16:36:49Z", "updated_at": "2026-09-10T10:00:44Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "`maint/layout_inference/run.py` currently reports **26 `layout drift` failures on a pristine main build** (verified on main @ 11ec2397 and again on a tree merged with 752312c2 — identical failure set), across the register-count and io-aware snapshots of:\n\n- `broadcast_read/rows4_cols1024_t128`\n- `elementwise_copy/fp16_128x128_t128`, `fp32_64x256_t256`\n- `fp8_copy/128x256_t128`, `64x128_t64`\n- `mixed_dtype_chain/128x128_t128`, `64x512_t256`\n- (and further cases; full list from any local run)\n\nThe `--cute` symbolic-vs-oracle checks still pass, so this is golden staleness, not a scorer bug: some layout-affecting change landed after the snapshots were recorded, and since the harness is not wired into CI, the drift went unnoticed. The harness's guarantee has effectively lapsed — new layout work (e.g. #3176, whose own new goldens do pass) cannot use the existing goldens as a regression net.\n\nSuggested fix:\n1. Re-record the stale goldens (`run.py --record`) with human review of the diff, ideally after bisecting which change moved them so the new snapshots are known-good rather than blessed-blind.\n2. Add a CI hook for at least the golden check (it is fast; `--cute` parity could be a second step or stay maintainer-run).\n\nObserved while validating #3176.", "url": "https://github.com/tile-ai/tilelang/issues/3188"}
{"number": 56, "title": "[Roadmap] Roadmap for Metal, OpenCL, VHLS, WebGPU, and SPIR-V Support", "state": "open", "created_at": "2025-01-26T05:09:04Z", "updated_at": "2026-09-09T22:03:20Z", "closed_at": null, "labels": [], "comments_count": 6, "is_pr": false, "body": "It's simple for us to extend the codegen and runtime from apache tvm.\n\n- [x] Support Metal\n- [ ] Support OpenCL\n- [ ] Support VHLS\n- [x] Support WebGPU\n- [ ] Support SPIR-V\n- [ ] Support Hexagon\n", "url": "https://github.com/tile-ai/tilelang/issues/56"}
{"number": 3178, "title": "[Feature Request] [RFC] Add explicit CUDA cache policies for normal global-memory copies", "state": "open", "created_at": "2026-09-07T09:30:08Z", "updated_at": "2026-09-08T11:23:38Z", "closed_at": null, "labels": ["enhancement"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Motivation\n\n### Summary\n\nTileLang currently has no first-class way to express NVIDIA PTX per-instruction cache operators for the normal SIMT path of `T.copy`.\n\nThe existing `eviction_policy` argument is consumed by TMA lowering and represents TMA L2 eviction priority (`evict_normal`, `evict_first`, and `evict_last`). It does not control the cache operator used by ordinary global-memory load/store instructions.\n\nNVIDIA PTX separately provides cache operators for normal global-memory accesses:\n\n- Loads: `.ca`, `.cg`, `.cs`, `.lu`, and `.cv`\n- Stores: `.wb`, `.cg`, `.cs`, and `.wt`\n\nThese are performance hints and can matter for streaming or single-use data, L1-pollution avoidance, and explicitly managed memory-access patterns. Today, expressing them from TileLang requires an imported helper or a low-level external call. That bypasses the normal `T.copy` lowering path and makes it harder to preserve TileLang's vectorization, predication, tail handling, and instruction-selection behavior.\n\nThis RFC proposes a distinct cache-policy contract for normal CUDA global-memory copies. It is intentionally separate from TMA eviction priority.\n\n### Solution\n\n### Proposed API\n\nExpose load and store policies independently on `T.copy`. Separate fields are useful because the legal PTX sets differ by direction and a global-to-global copy contains both a load and a store.\n\n```python\nT.copy(\n    src,\n    dst,\n    load_cache_policy=\"cg\",\n    store_cache_policy=\"wt\",\n    prefer_instruction=\"sync\",\n)\n```\n\nAn initial CUDA-oriented policy set could mirror PTX:\n\n```text\nload_cache_policy:  None | \"ca\" | \"cg\" | \"cs\" | \"lu\" | \"cv\"\nstore_cache_policy: None | \"wb\" | \"cg\" | \"cs\" | \"wt\"\n```\n\nThe exact public names are open for discussion: they may either match PTX or use backend-neutral semantic names with a CUDA mapping.\n\n### Proposed behavior\n\n- `None` preserves the current generated code and performance.\n- CUDA normal-copy lowering emits the matching `ld.global.*` / `st.global.*` operator, either directly or through typed CUDA intrinsics/helpers.\n- Scalar and vectorized accesses, predicated accesses, and tail paths retain the requested policy.\n- Invalid direction combinations, such as `load_cache_policy=\"wt\"` or `store_cache_policy=\"cv\"`, are rejected during compilation.\n- A requested policy must never be silently ignored.\n- If `T.copy` instruction selection chooses an incompatible path, the compiler should either select a compatible path or report a diagnostic. A first implementation may limit these options to `prefer_instruction=\"sync\"`.\n- `eviction_policy` remains the independent TMA eviction-priority interface.\n\n### Scope and non-goals\n\nThis proposal targets normal CUDA global-memory loads and stores. It does not propose:\n\n- changing TMA eviction semantics;\n- automatically inferring the best policy;\n- changing CUDA memory-consistency semantics;\n- guaranteeing that a non-default policy improves every workload.\n\nThe implementation should include CUDA source/codegen tests for each supported load/store policy, invalid-combination tests, predicated and vectorized copy coverage, correctness tests, and at least one representative streaming-memory benchmark.\n\n### Alternatives\n\n1. **Add individual low-level intrinsics**, following the existing `T.__ldg` precedent, such as `T.__ldcg` and `T.__stwt`. This is explicit but does not integrate naturally with region copies, vectorization, predication, or automatic instruction selection.\n\n2. **Use the generic `annotations` dictionary as an escape hatch.** This minimizes API surface but is difficult to discover, type-check, document, and validate.\n\n3. **Extend `eviction_policy`.** This is not preferred because TMA eviction priority and PTX load/store cache operators are different concepts with different legal value sets.\n\n4. **Rely only on compiler inference.** Automatic selection may be valuable later, but it does not provide deterministic control for performance-sensitive kernels or a way to validate the lowering independently.\n\n### Additional context\n\n### Related TileLang work\n\n- TMA eviction-policy support: https://github.com/tile-ai/tilelang/pull/690\n- CUDA L2 access-policy-window annotation: https://github.com/tile-ai/tilelang/pull/539\n- Explicit read-only cached load via `T.__ldg`: https://github.com/tile-ai/tilelang/pull/1414\n\nThese features are complementary but do not expose normal PTX load/store cache operators through `T.copy`.\n\n### NVIDIA references\n\n- PTX cache operators: https://docs.nvidia.com/cuda/parallel-thread-execution/index.html#cache-operators\n- CUDA load functions using cache hints: https://docs.nvidia.com/cuda/archive/11.8.0/cuda-c-programming-guide/index.html#ldx-functions\n- CUDA store functions using cache hints: https://docs.nvidia.com/cuda/archive/11.8.0/cuda-c-programming-guide/index.html#stx-functions\n\n### Questions for maintainers\n\n1. Should the public values follow PTX spelling or use backend-neutral semantic names?\n2. Should an explicit cache policy constrain copy instruction selection, or be accepted only with an explicitly selected normal-copy path?\n3. Should direct scalar accesses and `T.copy` share one lower-level cache-policy IR representation?\n4. Is extending `T.copy` preferred over adding a family of explicit load/store intrinsics?", "url": "https://github.com/tile-ai/tilelang/issues/3178"}
{"number": 3183, "title": "希望官网介绍保留历史版本内容，或者增加release note", "state": "closed", "created_at": "2026-09-08T09:44:15Z", "updated_at": "2026-09-08T11:21:51Z", "closed_at": "2026-09-08T11:20:24Z", "labels": [], "comments_count": 2, "is_pr": false, "body": "https://tilelang.com/index.html，当前官网只有最新版本介绍", "url": "https://github.com/tile-ai/tilelang/issues/3183"}
{"number": 2566, "title": "[BUG][Fuzzer][ice-on-valid-code] Single-iteration warp-specialized pipelined loop deadlocks at runtime on Hopper (sm_90) instead of completing", "state": "open", "created_at": "2026-07-09T00:38:26Z", "updated_at": "2026-09-08T09:26:03Z", "closed_at": null, "labels": ["bug"], "comments_count": 2, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.11\n\n### System information\n\n\n- Installed from the `0.1.11` wheel; source pinned to tag `v0.1.11` (commit `cd37ed5fc35ae7a60a1277c8eb49028174ac51e6`).\n- Python 3.12.3, Linux; PyTorch 2.12.1+cu130 (CUDA 13.0).\n- GPU: **NVIDIA H100 80GB HBM3** (compute capability 9.0, sm_90). Reproduced on this H100; the defect is on the warp-specialized TMA producer/consumer path, which is the default on sm_90, so it is expected to be sm_90-gated (not tested on other architectures).\n\n```python\nimport sys, tilelang, torch\nprint(sys.version.split()[0], sys.platform)   # 3.12.3 linux\nprint(tilelang.__version__)                    # 0.1.11\nprint(torch.__version__)                        # 2.12.1+cu130, CUDA 13.0\nprint(torch.cuda.get_device_name(), torch.cuda.get_device_capability())  # NVIDIA H100 80GB HBM3 (9, 0)\n```\n\n\n### Problem description\n\n\nA well-formed kernel that compiles cleanly (no error, no warning) **hangs forever** when launched on Hopper.\n\nThe trigger is a warp-specialized pipelined loop that **executes exactly one iteration** — e.g. `for k in T.Pipelined(1, num_stages=S)`. The launch never returns: the kernel dispatches, `torch.cuda.synchronize()` blocks forever, and the GPU sits pinned on an unsatisfiable barrier wait until the process is killed. There is no traceback and no compile diagnostic.\n\nThe trigger is **exactly `trip_count == 1`, independent of `num_stages`**. Compiling the identical kernel with `pass_configs={\"tl.disable_warp_specialized\": True}` runs to completion and returns the correct result, which isolates the defect to the Hopper warp-specialization path.\n\n### Observed (H100, TileLang 0.1.11, default warp-spec on; each cell run in isolation under a 120 s timeout)\n\n| `num_stages` | trip count | result |\n|---|---|---|\n| 1 | 1 | **HANG** (timeout, no return) |\n| 2 | 1 | **HANG** (timeout, no return) |\n| 3 | 1 | **HANG** (timeout, no return) |\n| 1 | 2 | returns (~9 s) |\n| 1 | 3 | returns (~9 s) |\n| 2 | 2 | returns (~9 s) |\n| 2 | 3 | returns (~9 s) |\n| 3 | 2 | returns (~9 s) |\n| 3 | 3 | returns (~9 s) |\n| 4 | 2 | returns (~9 s) |\n| 4 | 3 | returns (~9 s) |\n\nThe 11 cells partition cleanly: **`trip == 1` → HANG (3/3), `trip >= 2` → returns (8/8)**, for every `num_stages` tested. In particular `num_stages=4, trip=2` and `num_stages=4, trip=3` return normally even though `trip < num_stages`, and `num_stages=1, trip=1` hangs with a single stage. `num_stages` plays no role in whether the kernel hangs; only the single-iteration trip count does.\n\n\n### Reproducible example code\n\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\nSTAGES = 2\nNTILES = 1          # trip count == 1  =>  HANG with warp-spec; correct when disabled\nbM, bN = 64, 128\nTHREADS = 256\n\n\ndef make(M, N, T_):\n    dt = \"float16\"\n\n    @T.prim_func\n    def main(A: T.Tensor((T_, M, N), dt), C: T.Tensor((M, N), \"float32\")):\n        with T.Kernel(T.ceildiv(N, bN), T.ceildiv(M, bM), threads=THREADS) as (bx, by):\n            As = T.alloc_shared((bM, bN), dt)\n            Cf = T.alloc_fragment((bM, bN), \"float32\")\n            T.clear(Cf)\n            for k in T.Pipelined(T_, num_stages=STAGES):\n                T.copy(A[k, by * bM, bx * bN], As)\n                for i, j in T.Parallel(bM, bN):\n                    Cf[i, j] += As[i, j]\n            T.copy(Cf, C[by * bM, bx * bN])\n    return main\n\n\ndef run(ws, A, ref, M, N, T_):\n    f = make(M, N, T_)\n    k = tilelang.compile(f, pass_configs={\"tl.disable_warp_specialized\": (not ws)})\n    C = torch.zeros(M, N, device=\"cuda\", dtype=torch.float32)\n    print(f\"launching {'WS' if ws else 'NOWS'} kernel...\", flush=True)\n    k(A, C)\n    torch.cuda.synchronize()\n    print(f\"{'WS' if ws else 'NOWS'} RETURNED err={(C - ref).abs().max().item()}\", flush=True)\n\n\ndef main():\n    M = N = 128\n    T_ = NTILES\n    torch.manual_seed(0)\n    A = torch.randint(0, 4, (T_, M, N), device=\"cuda\").to(torch.float16)\n    ref = A.float().sum(0)\n    run(False, A, ref, M, N, T_)   # control: disable_warp_specialized -> returns\n    run(True, A, ref, M, N, T_)    # warp-spec default: HANGS (never reaches RETURNED)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nOutput (deterministic; warp-spec branch never returns, killed by the timeout — `exit 124`):\n\n```\nlaunching NOWS kernel...\nNOWS RETURNED err=0.0\nlaunching WS kernel...\n<hangs forever — no further output; GPU pinned at 100% util on the unsatisfiable barrier wait>\n```\n\nSetting `NTILES = 2` (or larger) makes the warp-spec build return as well.\n\n\n### Traceback\n\n```pytb\nThere is **no traceback** — the kernel compiles cleanly (no compile error, no warning) and the launch hangs. The process must be killed (`timeout`/SIGKILL); the GPU is at 100% utilization for the duration of the hang. The behavior is deterministic across repeated runs.\n```\n\n### Expected behavior\n\nA warp-specialized pipelined loop with a single iteration should run to completion (and return the same result as the non-warp-specialized path, which already computes it correctly — see the control), rather than emitting a kernel that deadlocks. The single-iteration TMA copy must emit a complete producer/consumer mbarrier handshake (both the transaction-bytes count *and* the arrival) so the consumer's data-ready wait can complete.\n\n\n### Additional context\n\n\n**Root cause — the single-iteration TMA load is lowered through a 1D bulk-copy path that drops the forward-barrier `mbarrier.arrive`.**\n\nWhen the pipelined loop has exactly one iteration, the global slice `A[k=0, ...]` folds to a fully-contiguous constant-offset region and the shared destination `As` keeps a plain (non-swizzled) linear layout, so the copy is classified as a **1D bulk copy** (`kBulkLoad1D`) and lowered by `LowerBulk1D`. In that function's TMA-copy branch the forward barrier gets a bare `mbarrier.expect_tx` and **no `mbarrier.arrive`** — the `emit_arrive` annotation that the warp-spec pass sets is ignored there. The forward barrier is `init(1)`, so its phase-0 completion needs **both** the transaction bytes (delivered by `cp.async.bulk...complete_tx::bytes`) *and* one arrival. The bytes arrive; the arrival never does, so the consumer's data-ready `mbarrier.wait(0)` blocks forever.\n\nFor `trip >= 2` the `k`-varying global slice needs a multidimensional `CUtensorMap` descriptor and `As` is swizzled, so the 1D path is rejected and the descriptor path `LowerBulk` — which *does* honor `emit_arrive` and emits `arrive_and_expect_tx` — is used, so the handshake closes. That is why only the single-iteration case hangs.\n\n<details>\n<summary>Source trace (verified against two generated-CUDA dumps, trip=1 vs trip=2, on H100 @ <code>cd37ed5f</code>)</summary>\n\n1. The warp-spec pass sets `is_tma_copy=1` and `emit_arrive=1` on the TMA copy ([`producer_consumer_ws.cc#L792`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/transform/producer_consumer_ws.cc#L792), [`#L1739-L1742`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/transform/producer_consumer_ws.cc#L1739-L1742)), so a downstream arrive is expected on the forward barrier.\n\n2. Copy classification: for `trip==1`, `CheckBulkCopy1D` is true (`shared_is_contiguous && global_is_contiguous && element_match`, [`copy_analysis.cc#L268-L309`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy_analysis.cc#L268-L309)). `SelectTmaInst` checks `can_bulk_load_1d` **before** `can_bulk_load` ([`copy_analysis.cc#L505-L521`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy_analysis.cc#L505-L521)), so `kBulkLoad1D` wins and dispatch routes to `LowerBulk1D` ([`copy.cc#L690-L694`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy.cc#L690-L694)). This selects the raw-pointer `tma_load(void* smem, void const* gmem, ...)` overload (`cp.async.bulk.shared::cta.global.mbarrier::complete_tx::bytes`, [`copy_sm90.h#L18-L26`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/tl_templates/cuda/copy_sm90.h#L18-L26)), not a `CUtensorMap` descriptor TMA.\n\n3. Bug site: `LowerBulk1D`, the `is_load && barrier_base_id >= 0` block ([`copy.cc#L2064-L2090`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy.cc#L2064-L2090)). In the `GetIsTmaCopy(op)==true` branch it emits **only** `mbarrier_expect_tx` and leaves `barrier_after_tma_stmt = std::nullopt` — it never emits `ptx_arrive_barrier` and it ignores `emit_arrive` entirely; then at `#L2088` `if (GetIsTmaCopy(op)) return producer;` returns the expect_tx-only sequence. Contrast the descriptor path `LowerBulk`, whose TMA branch honors `emit_arrive` and emits `ptx_arrive_barrier` ([`copy.cc#L1757-L1762`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy.cc#L1757-L1762)).\n\n4. Generated CUDA, **trip=1 (HANG):** `mbarrier[0].init(1); ... mbarrier[0].expect_transaction(16384); tl::tma_load((&As[0]), (&A[...]), mbarrier[0], 16384); ... mbarrier[0].wait(0);` — the raw-pointer overload, **no arrive** between the copy and the wait. `expect_transaction` maps to the cutlass `mbarrier.expect_tx` (no arrive). Phase 0 never flips → `wait(0)` blocks forever.\n\n5. Generated CUDA, **trip=2 (returns):** `mbarrier[k].arrive_and_expect_tx(16384); tl::tma_load(A_desc, mbarrier[k], ...); ... mbarrier[k].wait(0);` — the `CUtensorMap` descriptor overload with `arrive_and_expect_tx` (cutlass `mbarrier.arrive.expect_tx`), so phase 0 completes.\n\nThe distinction is confirmed in the vendored cutlass barrier API: `expect_transaction` → `mbarrier.expect_tx` (tx-count only, no arrive), `arrive_and_expect_tx` → `mbarrier.arrive.expect_tx` (arrive + tx-count).\n</details>\n\n**Provenance.** Introduced in the `0.1.9 → 0.1.10` release window: an earlier build (0.1.9) ran the same single-iteration repro to completion, and the deadlock is present on 0.1.10, 0.1.11, and 0.1.12. The exact introducing commit has not been re-bisected for this write-up, so treat the `0.1.9 → 0.1.10` window as approximate. Verification: the trigger boundary was established by an 11-cell `num_stages × trip` sweep on H100 (0.1.11), each cell run in isolation with a 120 s timeout, with HANG confirmed by three signals (exit 124, ~120 s wall, success line absent); the mechanism was verified by two compile-time CUDA dumps (trip=1 vs trip=2) on H100 at commit `cd37ed5f`, cross-checked against the cutlass mbarrier API. The fix below is **read-verified against source but not built or run** (it lands in compiled C++).\n\n<details>\n<summary>Suggested fix (proposed; read-verified, not built)</summary>\n\nMake `LowerBulk1D`'s TMA-copy branch emit the paired `mbarrier.arrive` for the forward barrier, honoring `emit_arrive` exactly like the descriptor path `LowerBulk` does. In [`copy.cc#L2064-L2090`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy.cc#L2064-L2090), inside the `GetIsTmaCopy(op)` true-branch, after emitting `mbarrier_expect_tx`, mirror [`copy.cc#L1757-L1762`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy.cc#L1757-L1762):\n\n```cpp\nif (auto emit_arrive_val = annotations.Get(\"emit_arrive\")) {\n  if (Downcast<IntImm>(emit_arrive_val.value())->value != 0) {\n    barrier_after_tma_stmt =\n        Evaluate(Call(DataType::Handle(), builtin::ptx_arrive_barrier(),\n                      {mbar_handle}));\n  }\n}\n```\n\n`barrier_after_tma_stmt` is already appended to the producer sequence when defined ([`copy.cc#L2080-L2082`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/copy.cc#L2080-L2082)), and the arrive is fused downstream into `arrive_and_expect_tx`, so this makes the 1D warp-spec producer satisfy the forward barrier's `init(1)` arrival count and the consumer's `wait(0)` completes — matching the working descriptor path.\n\nDefensive alternative: in `SelectTmaInst`/`SelectCopyInstForLowering`, do not prefer `kBulkLoad1D` for a warp-spec producer copy (i.e. when `emit_arrive`/`is_tma_copy` is set with an attached barrier), so the single-iteration copy takes the same descriptor path (`LowerBulk`) as the multi-iteration case. Either way, the targeted arrive-emission fix is minimal and behavior-preserving for the existing (correct) non-warp-spec 1D path, which manages `mbarrier_expect_tx`/`wait` itself.\n\nNote: clamping `num_stages` to the trip count does **not** fix this — the `num_stages=1, trip=1` cell still hangs, so `num_stages` is not on the causal path.\n</details>\n\n<details>\n<summary>Distinct from previously reported issues</summary>\n\nNot a duplicate of the nearby Hopper warp-spec reports:\n\n- **#2559** — closest sibling: a warp-specialized GEMM that hangs forever at launch (also silent, also H100/sm_90). But its trigger is adding `T.annotate_min_blocks_per_sm(2)`, and its root cause is a register-budget reconciliation failure: `min_blocks_per_sm` halves the `__launch_bounds__` register ceiling while `AnnotateWarpGroupRegAlloc`'s `setmaxnreg` counts are unchanged (never read `min_blocks_per_sm`). That is a different pass, a different trigger (an occupancy annotation, not the trip count), and a different mechanism (inconsistent `setmaxnreg`, not a dropped `mbarrier.arrive`). This report needs **no** annotation and fires on a bare single-iteration loop.\n- **#2547** — two `T.gemm` into one fragment fail `LayoutInference` under default WS: a **compile-time abort**, not a runtime deadlock; unrelated trigger.\n- **#2548** — WS pass hard-aborts (internal `ICHECK`) on a `T.Pipelined` inside a `while`: a **compile-time crash**, not a silent runtime hang; different trigger.\n- **#2556** — `tl.ThreadSync` drops a `__syncthreads()` for non-32-multiple thread counts: a synchronization race in a different pass, unrelated.\n- **#2346** (`Producer-Consumer TMA + Thread ID and Hangs`) and **#2192** (unexpected CTA-wide `__syncthreads()` between WS scopes) are also WS-path issues but with distinct triggers — absolute-vs-relative thread-ID use inside a pipeline, and a serializing barrier between producer/consumer scopes (a performance defect, not a hang), respectively. Neither involves the single-iteration `LowerBulk1D` arrive-drop.\n\nThis report is a **silent runtime deadlock** whose sole trigger is a single-iteration (`trip==1`) warp-specialized pipelined loop, rooted in the dropped `mbarrier.arrive` in `LowerBulk1D`. No open or closed issue covers this specific trigger + root cause.\n</details>\n", "url": "https://github.com/tile-ai/tilelang/issues/2566"}
{"number": 3037, "title": "[BUG][Fuzzer][ice-on-valid-code] 8-bit-operand sparse `T.gemm_sp` with `K > block_K` crashes `ThreadSync` (`Cannot match type int32 vs handle`) instead of compiling", "state": "open", "created_at": "2026-08-17T05:34:00Z", "updated_at": "2026-09-08T07:46:51Z", "closed_at": null, "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (+1 or comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13; NVIDIA L40S (sm_89), CUDA 12.8, PyTorch 2.8.0. Reproduced this session on sm_89. The crashing pass (`ThreadSync`) is target-agnostic; the sparse 8-bit-operand path is the sm8x metadata path (`arch=\"8.0\"`).\n\n### Problem description\n\nAn 8-bit-operand 2:4 structured-sparse GEMM built with `T.gemm_sp` aborts at compile time with\n\n```\nInternalError: Cannot match type int32 vs handle\n```\n\nas soon as `K > block_K`, i.e. whenever the standard K-loop iterates more than once. This session both `in_dtype=int8` and `in_dtype=float8_e5m2` (the two 8-bit sparse operand dtypes) crash identically in the same `ThreadSync`/`Summarize`/`BinaryOpMatchTypes` frame; `float16`/`bfloat16` sparse (16-bit metadata layout) compiles. The rest of this report uses `int8` as the running example, but see §17 for the fp8 twin. The identical kernel with `block_K == K` (a single K-block) compiles cleanly (it reaches a runnable kernel; see the note on numeric correctness below). Splitting K into tiles is the ordinary way GEMM pipelines the K dimension (the shipped `examples/gemm_sp/example_gemm_sp.py` runs `K=1024, block_K=64/128` = 16/8 K-iterations by default), so this is a supported, in-contract shape rather than an odd size.\n\nThe crash is raised in the `ThreadSync(\"shared.dyn\")` pass, not at codegen; there is no way to reach a kernel.\n\n<details><summary>Trigger boundary (what flips PASS→crash), tested on sm_89</summary>\n\n| in_dtype | metadata | K | block_K | K-iterations | result |\n|---|---|---|---|---|---|\n| int8 | int32 | 256 | 256 | 1 | compiles to a runnable kernel (no crash) |\n| int8 | int32 | 256 | 128 | 2 | `Cannot match type int32 vs handle` (compile abort) |\n\n`float16` sparse GEMM with the same multi-iteration K-loop (`K=256, block_K=128`, verified this session) compiles — the crash is specific to the 8-bit-operand (int8/fp8_e5m2) sparse metadata read path. `fp8_e5m2` with the same multi-K loop crashes identically (see §17).\n\nThe boundary that flips is *compile vs compile-crash*: the single-K-block int8 kernel gets through the whole pass pipeline to a launchable kernel, while adding a second K-iteration aborts in `ThreadSync`. (Separately, the numeric result of the single-K-block int8 sparse kernel did not match the dense reference on this sm_89 run — an unrelated int8-sparse-on-sm_89 correctness question, not the ICE reported here; this issue is purely about the multi-K-iteration compile abort.)\n</details>\n\n### Reproducible example code\n\n```python\nimport os, sys, torch, tilelang\nimport tilelang.language as T\nfrom tilelang.layout import make_cutlass_metadata_layout\nfrom tilelang.cuda.intrinsics.sparse_layout import get_e_factor\n\n# 0.1.13: compress / randint_semi_sparse live in the gemm_sp example, not in\n# tilelang.utils.sparse. Point at that dir (adjust to your checkout).\nsys.path.insert(0, os.path.join(os.path.dirname(tilelang.__file__),\n                                \"..\", \"examples\", \"gemm_sp\"))\nfrom sparse_utils import compress, randint_semi_sparse\n\n# int8 2:4 structured-sparse GEMM (M=N=128, K=256). The ONLY thing that varies\n# between the two runs is block_K: 128 -> the K-loop runs twice, 256 -> one\n# iteration. Everything else (dtypes, layout, API) is identical.\ndef build(block_K):\n    M, N, K = 128, 128, 256\n    block_M, block_N = 128, 128\n    in_dtype, e_dtype, accum = \"int8\", \"int32\", \"int32\"\n    E_factor = get_e_factor(in_dtype, e_dtype)   # 0.1.13: get_e_factor('int8','int32') == 32\n\n    @T.prim_func\n    def main(A_sparse: T.Tensor((M, K // 2), in_dtype),\n             E: T.Tensor((M, K // E_factor), e_dtype),\n             B: T.Tensor((K, N), in_dtype),\n             C: T.Tensor((M, N), accum)):\n        with T.Kernel(T.ceildiv(N, block_N), T.ceildiv(M, block_M), threads=128) as (bx, by):\n            A_shared = T.alloc_shared((block_M, block_K // 2), in_dtype)\n            B_shared = T.alloc_shared((block_K, block_N), in_dtype)\n            E_shared = T.alloc_shared((block_M, block_K // E_factor), e_dtype)\n            C_frag = T.alloc_fragment((block_M, block_N), accum)\n            T.annotate_layout({\n                E: make_cutlass_metadata_layout(E, mma_dtype=in_dtype, arch=\"8.0\"),\n                E_shared: make_cutlass_metadata_layout(E_shared, mma_dtype=in_dtype, arch=\"8.0\")})\n            T.clear(C_frag)\n            for k in T.Pipelined(T.ceildiv(K, block_K), num_stages=1):\n                T.copy(E[by * block_M, k * block_K // E_factor], E_shared)\n                T.copy(A_sparse[by * block_M, k * block_K // 2], A_shared)\n                T.copy(B[k * block_K, bx * block_N], B_shared)\n                T.gemm_sp(A_shared, E_shared, B_shared, C_frag, transpose_A=False, transpose_E=False)\n            T.copy(C_frag, C[by * block_M, bx * block_N])\n    return main, (M, N, K)\n\ndef run(block_K):\n    main, (M, N, K) = build(block_K)\n    kernel = tilelang.compile(main, out_idx=[3])           # crashes here for block_K=128\n    A = randint_semi_sparse(M, K, -3, 4, dtype=torch.int8, device=\"cuda\")\n    B = torch.randint(-3, 4, (K, N), device=\"cuda\", dtype=torch.int8)\n    A_sparse, E = compress(A, meta_dtype=torch.int32)\n    kernel(A_sparse, E, B)                                 # launches (single-K-block only)\n    return \"compiled + launched\"\n\n# Control: single K-block (block_K == K). Compiles to a runnable kernel (no crash).\nprint(\"block_K=256 (1 K-iteration):\", run(256))           # -> compiled + launched\n\n# Trigger: same kernel, K split into two block_K=128 tiles (2 K-iterations).\nprint(\"block_K=128 (2 K-iterations):\", run(128))\n# -> InternalError: Cannot match type int32 vs handle  (compile-time abort)\n```\n\n### Traceback\n\n```\nFile \".../tilelang/cuda/pipeline.py\", line 236, in CUDAPassPipelineBody\n    mod = tilelang.transform.ThreadSync(\"shared.dyn\")(mod)\n  ...\n  tvm::tl::TileLangThreadSyncPlanner::Summarize(std::vector<...StmtEntry...>, tvm::tirx::ForNode const*)\n  tvm::operator<(tvm::PrimExpr, tvm::PrimExpr)\n  tvm::less(tvm::PrimExpr, tvm::PrimExpr, tvm::Span)\n  .../3rdparty/tvm/src/tirx/op/op.cc:249, in tvm::BinaryOpMatchTypes(PrimExpr&, PrimExpr&, Span)\ntvm.error.InternalError: Cannot match type int32 vs handle\n```\n\nImmediately before the fatal check the pass logs the offending pair (the `LOG(INFO)` at `op.cc:248`), e.g.\n\n```\ntx1 % 64 // 32 * 4096 + tx1 % 16 * 64 + (tx1 % 32 // 16 + tx1 % 4 // 2) % 2 * 16 + 5159 neg_inf\n```\n\n— an `int32` index on the left and a `neg_inf` sentinel (`handle` dtype) on the right. (`FindConflict` / `PointerAccessIsDisjoint` are inlined, so the last named frame is `Summarize`.)\n\n### Expected behavior\n\nThe kernel should compile (as the `block_K==K` control shows, the single-K-block variant of the identical kernel reaches a runnable kernel without a `ThreadSync` abort); an `int8` sparse GEMM with `K` split across multiple `block_K` tiles is the ordinary pipelined K-loop. The single-K-block variant compiles on the same path, so the multi-K-iteration case should also compile rather than aborting in `ThreadSync`.\n\n### Additional context\n\n**Root cause.** `ThreadSync`'s disjointness test compares an `int32` shared-memory offset against a `±inf` sentinel — the sentinel that `EvalSet` returns when it cannot bound the composed 8-bit sparse metadata index over the K-loop var — and the type-matcher rejects `int32 vs handle`.\n\n<details><summary>Mechanism</summary>\n\nThis is a cross-pass interaction between two passes (the int8-vs-fp16 divergence is explained below — each index subterm is individually boundable; the unboundedness comes from `EvalSet`'s loop-var relaxation of the composed int8 index):\n\n1. The **`gemm_sp` lowering** — `ldmatrix_e` in [`mma_sp_macro_generator.py`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_sp_macro_generator.py#L299) picks a per-dtype thread→(row,col) metadata layout (for `int8`, `metadata_32bit_load_32x1_to_shared_16x2_layout_8bit`) and composes it with the cutlass-interleaved metadata layout from [`make_cutlass_metadata_layout_sm8x`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/layout/gemm_sp.py#L121), whose inner `ColumnMajorInterleaved` contains `bitwise_and` subterms ([`gemm_sp.py:140-141`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/layout/gemm_sp.py#L140)). The result is a shared-memory index built from `%`, `//`, and `&` over the thread var.\n\n2. The **`ThreadSync(\"shared.dyn\")`** barrier-planning pass. When the K-loop iterates more than once, its per-loop handler [relaxes each access's touched `IntSet` over the loop variable](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/thread_storage_sync.cc#L878) via [`arith::EvalSet`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/thread_storage_sync.cc#L887). For the composed `int8` metadata index this relaxation yields an **unbounded** `IntSet`, so `.min()`/`.max()` return the `neg_inf`/`pos_inf` sentinel (a `handle`-typed node). The subsequent disjointness test in [`PointerAccessIsDisjoint`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/thread_storage_sync.cc#L1503) forms [`lhs_max < rhs_min`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/thread_storage_sync.cc#L1548) with one side the `int32` offset and the other the sentinel, which fails [`BinaryOpMatchTypes` — `Cannot match type`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L249) (the preceding [`LOG(INFO)` at `op.cc:248`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L248) is the `... neg_inf` line above).\n\nWith a single K-block the loop is one iteration, so the loop-var relaxation at `thread_storage_sync.cc:878` never runs and the sentinel is never formed — which is why `block_K==K` compiles.\n\nThe int8-vs-fp16 divergence is confirmed empirically (the `float16` sparse metadata read with the same multi-K K-loop compiles cleanly — see Reach — while `int8` aborts) and both dtypes share the *same* `ColumnMajorInterleaved` layout, so the `bitwise_and` subterm is present for both. Probing the individual subterms this session (via `arith::Analyzer::const_int_bound` over `tx∈[0,128)`, `k∈[0,4)`) shows **each subterm is individually bounded** — `tx & 15` → `[0,15]`, `tx % 16` → `[0,15]`, `tx // 16` → `[0,7]`, and the composed forms `(tx&15)+k*32` (int8 `E_factor`) → `[0,111]` and `(tx%16)+k*16` (fp16) → `[0,63]` are all finite. So the unbounded `IntSet` is **not** produced by `bitwise_and` alone (a common suspicion, refuted here); it arises in `EvalSet`'s *loop-variable relaxation* of the fully-composed int8 metadata index — a different algorithm from `const_int_bound` that gives up (returns the ±inf sentinel) on the int8 index's particular nesting of `%`/`//`/`&` over the relaxed loop var, whereas the shallower fp16 index (`E_factor=16`, `metadata_16bit` thread layout) stays boundable. The precise nesting that defeats `EvalSet` is the int8 `metadata_32bit_load_32x1_to_shared_16x2_layout_8bit`-composed index; a maintainer fixing this need only make the sentinel non-fatal (see Suggested fix), independent of which subterm triggers it.\n\n</details>\n\n**Suggested fix.** Make the touched-set relaxation robust to unbounded/`inf` bounds — an access whose relaxed `EvalSet` bound is `±inf` should be treated as a conflict (conservatively insert a barrier) rather than feeding a `handle`-typed sentinel into the `<` comparison in `PointerAccessIsDisjoint`. A guard for `is_pos_inf`/`is_neg_inf` before building `lhs_max < rhs_min` (returning \"not disjoint\") would turn the crash into a correctly-synced kernel. Independently, expressing the int8 metadata index so `EvalSet` can bound it would also close the path (not verified end-to-end).\n\n**Provenance.** Reproduced on 0.1.13 (the version tested; the older `T.gemm_sp_v2` spelling was merged into `T.gemm_sp` in the interim). The sparse GEMM backend that carries this int8 metadata path was reworked in [#2048](https://github.com/tile-ai/tilelang/pull/2048) (`[Backend] Refactor gemm_sp`, merged 2026-05-19, first in 0.1.10), but the crash predates it and survives it. Not a functionality regression: the int8 multi-K-iteration configuration has no passing history — see Reach.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this compile crash. The nearby sparse issues are different defects: silent K-tail drop on non-atom-multiple K (#2605), the `block_N ≡ 8 (mod 16)` sm_90 miscompute (#2603), the metadata-layout non-bijection (#2606), and the missing proxy fence (#2634) — all silent-value/wrong-code, none a `ThreadSync` type-mismatch ICE.\n\n**Impact.** The narrow ingredient is the 8-bit-operand (int8/fp8_e5m2) sparse metadata path combined with a multi-iteration K-loop (`K > block_K`); float16/bfloat16 and single-K-block 8-bit are unaffected. When it fires it is a compile-time `ThreadSync` abort — loud and deterministic (identical error on 3 repeat runs — not a race), caught immediately at `tilelang.compile`, so it blocks that sparse kernel from building but corrupts no data and cannot reach a running workload silently. Fixing it closes this compile-time boundary class (making any 8-bit-operand sparse GEMM with a real pipelined K-loop buildable) rather than changing any numeric result.\n\n**Reach.** The trigger is the ordinary pipelined GEMM K-loop, `K > block_K` — a canonical GEMM idiom, and the shipped `examples/gemm_sp/example_gemm_sp.py` runs [`for k in T.Pipelined(T.ceildiv(K, block_K), ...)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/gemm_sp/example_gemm_sp.py#L33) with a default `K=1024, block_K=64` (16 K-iterations). The one rare ingredient is an **8-bit operand** (`int8`/`fp8_e5m2`): every 8-bit case in [`testing/python/tilelibrary/test_tilelang_tilelibrary_gemm_sp.py`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/testing/python/tilelibrary/test_tilelang_tilelibrary_gemm_sp.py) uses `K == block_K` (single K-block: `(128,128,64,block_K=64)` int8, `(64,128,128,block_K=128)` int8, `(128,128,64,block_K=64)` fp8_e5m2), the only multi-K rows are `float16`/`bfloat16`, and the shipped example is `matmul_sp_fp16` (fp16-only). So the 8-bit × (`K > block_K`) intersection is exercised nowhere in examples or tests, which is why CI is green.\n\n**Example-run result (this session, 0.1.13, v0.1.13 `examples/gemm_sp/`).** I ran the shipped `example_gemm_sp.py` verbatim. Its default 1024³ config compiles cleanly (no `ThreadSync` abort) but fails at *runtime* on sm_89 with `Failed to set the allowed dynamic shared memory size to 167936` — an sm_89 SMEM-capacity limit, unrelated to this ICE. Re-run on a config that fits sm_89 (`--m 256 --n 256 --k 256 --block_M 128 --block_N 128 --block_K 64 --num_stages 1`, 4 K-iterations, multi-K) it **compiles and passes precision** (`Precision check passed. diff: 0.00158`). So the shipped example **dodges this bug because it is fp16** (16-bit metadata layout, group=32/interweave=4), not because it avoids the multi-K loop — a multi-K fp16 sparse GEMM is exactly what it exercises and it works. The bug requires the 8-bit-operand metadata layout, which no shipped example or test uses together with `K > block_K`.\n\nAdjacent configurations tested this session on sm_89 (0.1.13), each in a fresh process/cache (see §17 for the full 4-axis sweep):\n\n| config | K / block_K | K-iters | result |\n|---|---|---|---|\n| int8 sparse (trigger) | 256 / 128 | 2 | `Cannot match type int32 vs handle` |\n| int8 sparse | 256 / 64 | 4 | `Cannot match type int32 vs handle` (same root) |\n| **fp8_e5m2** sparse | 256 / 128 | 2 | `Cannot match type int32 vs handle` (**same root**, same frame) |\n| int8 sparse, transpose_E | 256 / 128 | 2 | `Cannot match type int32 vs handle` (same root) |\n| int8 sparse (control) | 256 / 256 | 1 | compiles + launches |\n| **float16** sparse | 256 / 128 | 2 | **compiles** (16-bit metadata layout, no crash) |\n| **dense** int8 `T.gemm` (control) | 256 / 128 | 2 | **compiles** (no sparse metadata → no crash) |\n| int8 sparse, repeat-run | 256 / 128 | 2 | identical crash ×3 (deterministic ICE, not a race) |\n\nSame-root vs distinct: all the 8-bit-operand multi-K rows (int8 and fp8_e5m2, block_K = 128 and 64, transpose_E on/off) are the **same** root — any `K > block_K` 8-bit sparse loop aborts identically in the same `Summarize`/`BinaryOpMatchTypes` frame. The non-crashing rows delimit the boundary: single-K-block int8 (no loop-var relaxation), fp16 multi-K (16-bit metadata layout), and dense int8 multi-K (no sparse metadata layout at all) all compile, isolating the defect to *8-bit-operand sparse metadata × multi-iteration K-loop*. (Separately: the single-K-block int8 sparse kernel launches but its result did not match a dense `int8→int32` reference on this sm_89 run — max abs diff 188; an unrelated int8-sparse-on-sm_89 correctness question, deliberately out of scope for this compile-abort issue.)\n\n**Generalization (root levels + 4-axis sweep, all cells RUN this session).**\n\n*Two-level root.*\n\n- **SOURCE-level root** (which implementation is fragile): [`PointerAccessIsDisjoint`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/thread_storage_sync.cc#L1503) forms [`lhs_max < rhs_min`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/thread_storage_sync.cc#L1548) from touched-`IntSet` bounds that the per-loop handler [relaxes over the loop var via `arith::EvalSet`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/thread_storage_sync.cc#L887). When `EvalSet` cannot bound the composed metadata index it returns the `±inf` sentinel (a `handle`-typed node); `<` on `int32` vs `handle` fails [`BinaryOpMatchTypes`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L249). The missing guard (no `is_pos_inf`/`is_neg_inf` check before the `<`) is the true fragile site — it is generic to *any* access whose relaxed `IntSet` is unbounded.\n- **OPERATOR-level root** (what correlates): the `T.gemm_sp` **8-bit-operand metadata read** — [`ldmatrix_e`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_sp_macro_generator.py#L299) selects a `metadata_*_layout_8bit` thread→(row,col) map composed with the 32-bit-metadata `ColumnMajorInterleaved` (`group=16, interweave=2`) that [`make_cutlass_metadata_layout_sm8x`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/layout/gemm_sp.py#L121) *forces* onto 8-bit operands ([the sm8x layout requires 32-bit metadata for `int8`/`uint8`/fp8, 16-bit for fp16/bf16](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/layout/gemm_sp.py#L128-L132)). This 32-bit-metadata interleave is the composed index `EvalSet` gives up on; the 16-bit fp16 interleave (`group=32, interweave=4`) stays boundable.\n\n**4-axis sweep** (each cell run in a fresh process + fresh `TILELANG_CACHE_DIR`, `K=256`, `block_M=block_N=128`, `threads=128`, `num_stages=1`; observed result recorded, not asserted):\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| related-type | `fp8_e5m2` a_dtype + int32 meta, block_K=128 (2 K-iters) | `Cannot match type int32 vs handle` — identical `ThreadSync`/`Summarize`/`BinaryOpMatchTypes` frame | **SAME** — bug is the 8-bit family, not int8 alone |\n| related-type | `float16` a_dtype + int16 meta, block_K=128 (2 K-iters) | compiles cleanly | distinct — fp16 uses the 16-bit metadata layout that `EvalSet` can bound |\n| related-type | `int8` a_dtype + **int16** meta, multi-K | `ValueError: metadata should be 32 bit, got int16` at layout build | unreachable config — sm8x forces 32-bit meta on 8-bit operands |\n| related-type | `float16` a_dtype + **int32** meta, multi-K | `ValueError: metadata should be 16 bit, got int32` at layout build | unreachable config — sm8x forces 16-bit meta on fp16 (cannot cross fp16 into the crashing layout) |\n| related-type | `uint8` a_dtype + int32 meta, multi-K | `KeyError: uint8` in sparse `GROUP_CONFIG` | unreachable config — uint8 is not a supported sparse operand |\n| related-operator | `int8` `gemm_sp` **transpose_E=True**, block_K=128 (2 K-iters) | `Cannot match type int32 vs handle` (same frame) | **SAME** — the other `ldmatrix_e` branch still composes the same metadata index |\n| related-operator | dense `int8` `T.gemm`, block_K=128 (2 K-iters) | compiles cleanly | distinct — no sparse metadata layout, so no unbounded index |\n| related-source | `int8` sparse, **block_K=64** (4 K-iters) | `Cannot match type int32 vs handle` (same frame) | **SAME** — any `K > block_K` count relaxes over the loop var |\n\n**Example-run (PART 1).** The shipped `examples/gemm_sp/example_gemm_sp.py` is fp16-only (`matmul_sp_fp16`). Run verbatim (v0.1.13 sources), it **compiles and passes precision** on a sm_89-fitting config (`--k 256 --block_K 64`, 4 K-iters, multi-K): `Precision check passed. diff: 0.00158`. Its 1024³ default compiles but hits an unrelated sm_89 SMEM-capacity runtime error (`Failed to set the allowed dynamic shared memory size to 167936`). So the shipped example **dodges this bug because it is fp16, not because it avoids the multi-K loop** — it runs the multi-K loop successfully. Verified there is no shipped example or test that combines an 8-bit operand with `K > block_K`.\n\n**Reframe.** Boundary is wider than the original int8-only framing but still bounded: the bug is the **8-bit-operand sparse family (int8 and fp8_e5m2)** — precisely the set that `make_cutlass_metadata_layout_sm8x` forces onto the 32-bit-metadata ColumnMajorInterleaved. It is *not* reachable for fp16/bf16 (different, boundable layout) nor for uint8 (unsupported operand). Title and Problem updated to \"8-bit-operand\". No new distinct bug was found while sweeping (the fp16/int16-meta and fp16/int32-meta `ValueError`s and the uint8 `KeyError` are input-validation guards firing correctly, not defects).", "url": "https://github.com/tile-ai/tilelang/issues/3037"}
{"number": 3038, "title": "[BUG][Fuzzer][wrong-code] Auto-vectorized `float32` `T.atomic_add` into shared emits a global-only vector builtin and crashes at launch (sm_90)", "state": "closed", "created_at": "2026-08-17T05:34:02Z", "updated_at": "2026-09-07T03:37:15Z", "closed_at": "2026-09-07T03:37:15Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13 / NVIDIA H100 (sm_90), CUDA 12.x. The launch failure requires sm_90 (the float32 vector-atomic gate) and was reproduced this session on an H100: the repro emits a `float32`-width `AtomicAddx2` on the shared buffer and aborts at launch (`InternalError`, CUDA launch failure, under `CUDA_LAUNCH_BLOCKING=1`). The sm_89 non-crash controls below were run on an L40S with TileLang 0.1.13.\n\n### Problem description\n\nA `T.atomic_add` of a `float32` value into a **shared-memory** destination, written inside a `T.Parallel` loop that gives each thread ≥2 elements, aborts the kernel at launch with `CUDA error: unspecified launch failure` (`cudaErrorLaunchFailure`). The identical accumulation is correct when the same loop maps one element per thread (scalar `AtomicAdd`), and the identical vectorized code is correct when the destination is global. So the crash is the intersection of *vectorization* and *shared scope*. Not a regression — see Provenance.\n\nTwo passes interact. The `VectorizeLoop` pass picks the atomic-add width from the destination dtype and SM version only (`GetMaxAtomicVectorSize`), never the destination memory scope — for `float32` on sm_90 it returns 4, so the per-thread atomics are rewritten to the `atomic_addx2`/`atomic_addx4` intrinsic. `CodeGenCUDA` then emits `AtomicAddx2((&Bs[...]), *(float2*)(...))` / `AtomicAddx4(...)`, which resolve (in the `atomic.h` codegen-time template) to the sm≥90 `float32` overloads whose relaxed branch is the CUDA `atomicAdd(reinterpret_cast<float2*>(ref), ...)` / `float4*` builtin. Those vector overloads are defined only for the global address space; passing a shared pointer yields an illegal memory access at launch. The scalar `atomicAdd(float*)` and the `float16` vectorized `atomicAdd(half2*)` both accept shared, so only the `float32`-vectorized-into-shared case faults.\n\n<details><summary>Trigger boundary and controls (0.1.13). The sm_90 float32-into-shared crash row was reproduced this session on an H100; the sm_89 rows were run this session on an L40S. Only the sm_90 float32-GLOBAL-destination row is inferred (not run).</summary>\n\n| arch | dst dtype | dst scope | per-thread elems | emitted | result |\n|---|---|---|---|---|---|\n| sm_90 | `float32` | shared | 2 | `AtomicAddx2((&Bs[..]), *(float2*)..)` | **launch failure** (`InternalError`, CUDA launch failure) (RUN, H100) |\n| sm_90 | `float32` | shared | 4 | `AtomicAddx4((&Bs[..]), *(float4*)..)` | **launch failure** (inferred, same overload family) |\n| sm_89 | `float32` | shared | 2 | scalar `AtomicAdd(&Bs[..], ..)` (fp32 width=1 on sm<90) | correct, maxerr 0.0 (RUN) |\n| sm_89 | `float32` | shared | 1 (threads=256) | scalar `AtomicAdd(&Bs[..], ..)` | correct, maxerr 0.0 (RUN) |\n| sm_89 | `float32` | **global** | 2 | scalar `AtomicAdd(&B[..], ..)` (fp32 width=1 on sm<90) | correct, maxerr 0.0 (RUN) |\n| sm_89 | `float16` | shared | 2 | `AtomicAddx2((&Bs[..]), *(uint1*)..)` (→ `atomicAdd(half2*)`) | correct, maxerr 0.0 (RUN) |\n| sm_89 | `bfloat16` | shared | 2 | `AtomicAddx2((&Bs[..]), *(uint1*)..)` (→ `atomicAdd(__nv_bfloat162*)`) | correct, maxerr 0.0 (RUN) |\n\nThe sm_90 float32-into-shared row was reproduced this session on an H100: the emitted `AtomicAddx2` on the shared `Bs` aborts at launch (`InternalError`, CUDA launch failure). The sm_89 rows isolate two things. First, `float32` atomic never vectorizes below sm_90 (`GetMaxAtomicVectorSize` returns 1), so the crash cannot appear there — this is why the fault is sm_90-only. Second, the `float16`/`bfloat16` vectorized-into-shared path DOES fire on sm_89 and runs clean, because its relaxed vectorized builtin (`atomicAdd(half2*)` / `atomicAdd(__nv_bfloat162*)`) accepts a shared pointer — showing the width-selection scope-blindness is shared across dtypes, but only the `float32` overloads reject shared. On sm_90 the `float32` global-destination row is expected to remain correct (same `AtomicAddx2`, global pointer is legal), isolating scope as the crash trigger; that global-destination row is the only inferred cell (not run).\n\n</details>\n\n### Reproducible example code\n\n```python\n# REPRO (sm_90): float32 T.atomic_add into a SHARED buffer, auto-vectorized to AtomicAddx2.\n# threads=128 over 256 elements -> 2 elems/thread -> on sm_90, width-4 fp32 vectorization\n# emits AtomicAddx2/x4(float2*/float4*), a global-only builtin, on a shared ptr -> launch failure.\n# (On sm<90 fp32 atomic keeps width 1 -> scalar AtomicAdd -> runs clean; the crash needs sm_90.)\nimport torch, tilelang, tilelang.language as T\n\nN = 256\n@T.prim_func\ndef f(A: T.Tensor((N,), \"float32\"), B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(1, threads=128) as bx:\n        Bs = T.alloc_shared((N,), \"float32\")\n        for i in T.Parallel(N):\n            Bs[i] = 0.0\n        for i in T.Parallel(N):\n            T.atomic_add(Bs[i], A[i])      # -> AtomicAddx2((&Bs[..]), *(float2*)..)\n        for i in T.Parallel(N):\n            B[i] = Bs[i]\n\nk = tilelang.compile(f)\nA = torch.arange(N, dtype=torch.float32).cuda()\nB = torch.zeros(N, dtype=torch.float32).cuda()\nk(A, B)                                    # CUDA error: unspecified launch failure\nprint((B - A).abs().max().item())          # never reached\n```\n\n```python\n# CONTROL: identical kernel, threads=256 -> 1 elem/thread -> scalar AtomicAdd on shared.\nimport torch, tilelang, tilelang.language as T\n\nN = 256\n@T.prim_func\ndef f(A: T.Tensor((N,), \"float32\"), B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(1, threads=256) as bx:\n        Bs = T.alloc_shared((N,), \"float32\")\n        for i in T.Parallel(N):\n            Bs[i] = 0.0\n        for i in T.Parallel(N):\n            T.atomic_add(Bs[i], A[i])      # -> scalar AtomicAdd(&Bs[..], ..)\n        for i in T.Parallel(N):\n            B[i] = Bs[i]\n\nk = tilelang.compile(f)\nA = torch.arange(N, dtype=torch.float32).cuda()\nB = torch.zeros(N, dtype=torch.float32).cuda()\nk(A, B)\nprint((B - A).abs().max().item())          # -> 0.0 (correct)\n```\n\n### Traceback\n\n(Reproduced this session on an H100 with `CUDA_LAUNCH_BLOCKING=1`: `grid=(1,1,1) block=(128,1,1) dyn_smem_bytes=1024`, `InternalError` / CUDA launch failure.)\n\n```\ntorch.AcceleratorError: CUDA error: unspecified launch failure\nSearch for `cudaErrorLaunchFailure' in https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__TYPES.html for more information.\nCUDA kernel errors might be asynchronously reported at some other API call, so the stacktrace below might be incorrect.\nFor debugging consider passing CUDA_LAUNCH_BLOCKING=1\n```\n\n### Expected behavior\n\nThe `float32` shared-buffer accumulation should complete and produce the correct sum, as the scalar lowering of the same call already does (control above, maxerr 0.0) and as the `float16` vectorized path already does on shared. Shared-memory atomic add with relaxed ordering is an intended use — [#1998](https://github.com/tile-ai/tilelang/issues/1998) confirms the `memory_order == relaxed` atomics go through the generic `atomicAdd(...)` builtins for exactly this.\n\n### Additional context\n\n**Root cause.** `VectorizeLoop` picks the `float32` atomic-add vector width from the destination dtype and SM version alone and never inspects the destination's memory scope, so a `float32` shared-buffer atomic on sm_90 is vectorized into a `CodeGenCUDA` overload whose relaxed branch is the global-address-space-only `atomicAdd(float2*/float4*)` builtin, which faults at launch on a shared pointer. This is a cross-pass interaction between two passes that each make a locally-defensible decision. `VectorizeLoop` rewrites the `float32` shared-buffer atomic on sm_90 to the `atomic_addx2`/`atomic_addx4` intrinsic. `CodeGenCUDA` then lowers that intrinsic to the sm≥90 `float32` `AtomicAddx2`/`AtomicAddx4` template overloads, whose relaxed branch is the CUDA `atomicAdd(float2*/float4*)` builtin — a global-address-space-only operation. Neither pass owns the \"shared destinations cannot use the vector builtin\" constraint, so nothing rejects or de-vectorizes the shared case, and the emitted code faults at launch.\n\n<details><summary>Mechanism and source (the two passes)</summary>\n\n- **Pass 1 — `VectorizeLoop` (`src/transform/vectorize_loop.cc`), scope-blind width choice.** The atomic-add SIMT loop is vectorized (enabled by [`LowerSIMT` → `LowerParallelLoop(..., should_vectorize=true)`, `src/cuda/op/atomic_add.cc:250-254`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/atomic_add.cc#L250-L254)), and the width is chosen by [`GetMaxAtomicVectorSize`, `vectorize_loop.cc:173-181`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/vectorize_loop.cc#L173-L181) — `4` for `float32` when `TargetHasSMVersionGE(target, 90)`, `2` for fp16/bf16, `1` otherwise. The width then selects the intrinsic via [`GetVectorizedAtomicOp` at `vectorize_loop.cc:657-664`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/vectorize_loop.cc#L657-L664). Neither function reads `dst`'s storage scope.\n- **Pass 2 — `CodeGenCUDA` (`src/cuda/codegen/codegen_cuda.cc`), emits the global-only overload.** The `atomic_addx2`/`atomic_addx4` intrinsic is printed as `AtomicAddx2`/`AtomicAddx4` at [`codegen_cuda.cc:4847-4859`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4847-L4859). For `float32` on sm≥90 these resolve to [`AtomicAddx2(float*)`, `atomic.h:772-783`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/atomic.h#L772-L783) whose relaxed branch is `atomicAdd(reinterpret_cast<float2*>(ref), add_val)` (line 776), and the width-4 sibling [`AtomicAddx4`, `atomic.h:802-812`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/atomic.h#L802-L812) whose relaxed branch is `atomicAdd(reinterpret_cast<float4*>(ref), add_val)`. Those `float2`/`float4` builtins operate only in the global address space; the header comment even states \"Vectorized atomic operations only support global space\" (line 810).\n- Contrast the fp16 helper, which tolerates shared: [`AtomicAddx2(half_t*)`, `atomic.h:561-575`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/atomic.h#L561-L575) — relaxed branch is `atomicAdd(reinterpret_cast<half2*>(ref), ...)`, which accepts a shared pointer (confirmed on L40S). Below sm_90 the `float32` overloads take a `#else` scalar branch at [`atomic.h:836-869`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/atomic.h#L836-L869), but `GetMaxAtomicVectorSize` already returns 1 there, so that scalar overload is never even reached for fp32 on sm<90.\n\n</details>\n\n**Suggested fix.** The clean fix is in Pass 1: make `GetMaxAtomicVectorSize` (or the caller at `vectorize_loop.cc:657-664`) return width 1 for a `float32` atomic whose destination is a shared buffer, so it lowers to the scalar `AtomicAdd` (correct there). Alternatively, in Pass 2, route a shared pointer in the `float32` `AtomicAddx2`/`AtomicAddx4` overloads through the per-element fallback `AtomicAddx2Scalar`/`AtomicAddx4Scalar` (already present at [`atomic.h:285-296`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/atomic.h#L285-L296)) — but the device-side overload cannot see the compiler's scope information, so the Pass-1 fix is preferable. The bug itself is runtime-confirmed on sm_90 (H100, this session); the fix direction is unverified as a patch (not built/tested).\n\n**Provenance.** The vectorized atomic-add feature was introduced in [#1676](https://github.com/tile-ai/tilelang/pull/1676) \"Atomic Reduction Operations and Vectorization Enhancement\". At v0.1.13 the width is chosen by `GetMaxAtomicVectorSize` in `src/transform/vectorize_loop.cc`, which reads dtype and SM version but not the destination scope; no scope check exists in that path. Not a regression: the `float32` vector overloads have been global-only since the feature shipped. The crash is reproduced at runtime on 0.1.13 (H100) this session. (The PR history above is from the tracker, not re-queried this session — no network.)\n\n**Dedup.** I searched the open and closed tracker and found no existing report of the shared-scope face. The nearby atomic-add reports concern different mechanisms: #2688 (vectorized atomic drops `memory_order`), #2478 / #2561 (fp16/bf16 and integer `AtomicAddx4` dtype/width reinterpret), #2550 (sliced-destination crash); #1998 is a feature request that establishes shared relaxed atomics are supported through the generic builtins. (Tracker cross-references not re-queried online this session — verify before filing.)\n\n**Reach.** This is a cross-pass interaction, not a single-site defect, and I want to be precise about what was and was not confirmed. The two passes are `VectorizeLoop` (`src/transform/vectorize_loop.cc`, picks the atomic width scope-blind) and `CodeGenCUDA` (`src/cuda/codegen/codegen_cuda.cc`, lowers the vectorized intrinsic to the global-only `float32` overload in `atomic.h`). The trigger needs three ingredients: (1) a `float32` atomic add into a **shared** destination, (2) written as a `T.Parallel` loop that maps ≥2 elements per thread (the auto-vectorized form, not explicit `T.get_thread_binding`), (3) run on sm_90 — the `GetMaxAtomicVectorSize` gate returns width>1 for `float32` only at sm≥90. The sm_90 float32-into-shared launch failure was run in the original session on an H100: the repro emits the width-2 `AtomicAddx2` on the shared `Bs` and aborts at launch (`InternalError`, CUDA launch failure). The only inferred (not-run) cell is the sm_90 float32-GLOBAL-destination row, which follows from the same width gate but with a legal global pointer.\n\n**Two-level root and generalization (swept + run on L40S/sm_89, 0.1.13, this session).**\n\n- **SOURCE-level root:** `GetMaxAtomicVectorSize` (`vectorize_loop.cc:173-181`) picks the atomic-add vector width from **dtype + SM version only, never the destination storage scope**; `GetVectorizedAtomicOp` (`vectorize_loop.cc:159-167`) then maps width→intrinsic. The fragile fact is confirmed at source: the only atomic op that reaches this vectorizer is `atomic_add` (`MutateAtomicAddExpr_` asserts `atomic_add_elem_op()` at `vectorize_loop.cc:630`). The crash surfaces only because the fp32 Pass-2 overload (`atomic.h:772-812`) resolves its relaxed branch to `atomicAdd(float2*/float4*)`, a global-address-space-only builtin.\n- **OPERATOR-level root:** any op that (a) routes through `GetVectorizedAtomicOp` and (b) whose vectorized codegen overload requires the global address space. Today that set is exactly `{atomic_add}` × `{fp32 on sm≥90}`.\n\nAxes swept (each cell RUN; scalar/vector decision read from emitted CUDA; values on sm_89 L40S — the fp32-shared **crash** itself is sm_90-only and was run earlier on H100, not re-runnable here):\n\n| axis | cell tested (0.1.13) | emitted / observed | same-root? |\n|---|---|---|---|\n| main repro | fp32 shared, threads=128, 2/thread (sm_89) | scalar `AtomicAdd`, maxerr 0.0 | same root, **gated off** below sm_90 (width=1) |\n| related-type | fp16 shared, 2/thread | `AtomicAddx2((&Bs..),*(uint1*)..)`, maxerr 0.0 | same Pass-1 scope-blind width; **distinct outcome** (fp16 overload accepts shared) |\n| related-type | bfloat16 shared, 2/thread | `AtomicAddx2((&Bs..),*(uint1*)..)`, maxerr 0.0 | same Pass-1 root; distinct outcome (bf16 overload accepts shared) |\n| similar-logic | fp16 vectorized atomic into **global**, 2/thread | `AtomicAddx2((&B..),*(uint1*)..)`, maxerr 0.0 | isolates scope: Pass-1 vectorizes regardless of scope; fp16 Pass-2 works in **both** scopes |\n| related-operator | `atomic_max` fp16 shared, 2/thread | scalar `AtomicMax` (NOT `AtomicMaxx2`), maxerr 0.0 | **distinct, not filed** — max/min never vectorize (only `atomic_add` at `vectorize_loop.cc:630`), so they cannot reach the global-only vector overload |\n| related-operator | `atomic_min` fp16 shared, 2/thread | scalar `AtomicMin` (NOT vectorized) | **distinct, not filed** — same reason (immune) |\n| related-source | the atomic vectorizer path itself (`GetMaxAtomicVectorSize`/`GetVectorizedAtomicOp`) | source-confirmed: sole atomic vectorizer, `atomic_add`-only | same root; no sibling atomic op shares the fragile path |\n\nNet: the scope-blind width choice in Pass 1 is **shared across fp16/bf16/fp32**, but the launch failure is confined to `float32`-into-shared-on-sm_90 because only the fp32 Pass-2 overload is global-only. fp16/bf16 exercise the identical Pass-1 root and run clean (their Pass-2 overload accepts shared). `atomic_max`/`atomic_min` are immune (never vectorized) — distinct, not filed. Boundary is narrow but real; kept specific to fp32×shared×sm_90 with the root named at the class level.\n\n**Shipped-example run (PART 1, this session, L40S/sm_89 — verifies the \"dodge\" claims).** Both cited examples were located in the v0.1.13 tree and RUN verbatim:\n- `examples/gemv/example_gemv.py` — `main(do_bench=False)` (N=K=256): **\"Test passed!\"**, no crash. It does a `float32` (`accum_dtype = T.float`) `atomic_add(C_shared[tn], ...)` into a **shared** buffer, but via explicit `T.get_thread_binding(0)` (scalar per thread, no `T.Parallel` vectorization) — dodges the bug exactly as the draft claims. (Also would not vectorize on sm_89 anyway.)\n- `examples/deepseek_v32/topk_selector.py` — `test_topk_selector(batch=4, seq_len=4096, topk=128)`: ran clean, `selected/all = 1.0` for all rows. All its `atomic_add`s target `int32` shared histograms (`s_histogram`, `s_num_input` — integer → width 1), dodging the fp32 vector path as the draft claims.\nBoth example claims are CONFIRMED: the shipped code exercises float32/int shared atomics but avoids the ≥2-per-thread `T.Parallel` fp32 form, so CI stays green.\n\n**Impact.** The trigger is narrow: `float32` atomic-add into a shared destination, written as a `T.Parallel` loop mapping ≥2 elements per thread, on sm_90. When it fires it aborts at launch with `cudaErrorLaunchFailure` — a loud, deterministic crash that blocks that kernel from running rather than silently corrupting any output, so it cannot slip into a workload and produce wrong results. Fixing it closes this scope×vectorization boundary class so the `float32` shared-atomic form lowers correctly like its scalar and fp16 siblings already do.", "url": "https://github.com/tile-ai/tilelang/issues/3038"}
{"number": 2979, "title": "[BUG][Fuzzer][accepts-invalid] Region ops (`T.fill`/`T.clear`/`T.copy`) accept a sliced region whose `min+extent` exceeds the buffer, silently emitting an out-of-bounds write", "state": "open", "created_at": "2026-08-15T10:17:49Z", "updated_at": "2026-09-06T14:34:53Z", "closed_at": null, "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the Issue Tracker that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (reproduced on 0.1.13; source cited at the `v0.1.13` tag).\n\n### System information\n\nNVIDIA L40S (`sm_89`), CUDA 12.8, Python 3.13, tilelang 0.1.13. The defect is in the target-independent `Fill` op constructor bound check, so it is not specific to one GPU.\n\n### Problem description\n\nA sliced `T.fill` whose start offset plus length runs past the destination buffer — e.g. `T.fill(As[6:12], 7)` into a shape-`8` buffer — is silently accepted and lowered to an out-of-bounds store, even though the op's own contract says such input must be rejected. The `Fill` constructor's static bound check validates `min >= 0` and `extent <= shape` per dimension, but never checks `min + extent <= shape`, so a slice with a nonzero start slips through: `min = 6 >= 0` ✓ and `extent = 6 <= 8` ✓ both pass, while `min + extent = 12 > 8` is out of bounds.\n\nThe generated CUDA writes 4 elements past the end of the shared buffer:\n\n<details><summary>Generated store (this session)</summary>\n\n```cpp\n// As is alloc_shared((8,), int32); the fill emits, for threadIdx.x in [0,6):\nAs[(((int)threadIdx.x) + 6)] = 7;      // writes indices 6..11 -> 8,9,10,11 are OOB\n```\n\n</details>\n\nThe nearest sibling — a pure-extent overflow `T.fill(As[0:12], 7)` (extent `12 > 8`) — **is** cleanly rejected by the very same check (`region[0] = 12 > 8`), which shows the guard exists and is meant to cover this; the min-offset case escapes it only because the start term is omitted.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\n\nNBUF = 8\n\n@T.prim_func\ndef main(B: T.Tensor((NBUF,), \"int32\")):\n    with T.Kernel(1, threads=32):\n        As = T.alloc_shared((NBUF,), \"int32\")\n        T.fill(As[6:12], 7)        # min=6, extent=6 -> writes indices 6..11, 4 past shape 8\n        T.copy(As, B)\n\ntilelang.compile(main)             # SILENTLY ACCEPTED (compiles); emits As[tid+6]=7\n\n# CONTROL A (rejected as it should be) — pure extent overflow, same buffer:\n#   T.fill(As[0:12], 7)  ->  InternalError \"region[0] = 12 > 8\"\n# CONTROL B (correctly accepted) — in-bounds slice:\n#   T.fill(As[2:8], 7)   ->  compiles, fills exactly indices 2..7\n```\n\n### Traceback\n\n```\nNo traceback — the kernel compiles and runs to completion; the fill silently\nwrites 4 elements (indices 8..11) past the end of the 8-element shared buffer.\n```\n\n### Expected behavior\n\nAn out-of-bounds fill region has no valid target — indices 8..11 are past the end of an 8-element buffer, so there is nothing legal to write there — and the op should reject it, exactly as its docstring promises (\"checks that mins >= 0 and extents do not exceed the corresponding destination shape extents … will terminate (via ICHECK) if inputs are … out of bounds\") and exactly as the pure-extent-overflow sibling already does. Instead it silently accepts and emits an out-of-bounds shared-memory store.\n\n### Additional context\n\n**Root cause.** The `Fill` operator's static bound check mis-analyzes a sliced fill region: it checks the start and the length independently (`min >= 0`, `extent <= shape`) but never their sum against the shape, so a slice whose start-plus-length exceeds the buffer is treated as in-bounds. The lowering then honors the nonzero start (`region[i]->min + var`) and emits the out-of-range store.\n\n- The incomplete check: [`src/op/fill.cc#L111-L127`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/fill.cc#L111-L127) — `ICHECK_GE(min, 0)` and `ICHECK_LE(extent, shape)`, no `min + extent <= shape`.\n- The min-offset store the missing check lets through: [`src/op/fill.cc#L164`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/fill.cc#L164) (`dst_indices.push_back(region[i]->min + var)`).\n- The documented contract it violates: [`src/op/fill.cc#L78-L92`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/fill.cc#L78-L92).\n\n**Suggested fix.** In the `Fill` constructor bound check (`src/op/fill.cc#L111-L127`), when both `region[i].min` and `region[i].extent` (and the shape) are statically known, add `ICHECK_LE(min + extent, shape)` alongside the existing checks, so a sliced fill past the end is rejected with the same actionable message the pure-extent case already produces.\n\n**Same root, wider scope — the OOB-region check is per-op and inconsistent.** `Fill` is not the only region op that accepts an out-of-bounds sliced region; the bound check is implemented separately in each op rather than once at the slice/`BufferRegion` layer, so the ops disagree. Verified this session on 0.1.13 (dst slice `[6:12]` into a shape-8 buffer):\n\n| op | check in source | OOB sliced region → | \n|---|---|---|\n| `T.fill` | only `extent <= shape` (`fill.cc:122`, no `min` term) | accepted → emits `S[tid+6]=7` (writes 8..11) |\n| `T.clear` | same `Fill` op (`fill_op.py:40`) | accepted (same path as fill) |\n| `T.copy` | a `min + extent <= shape` expression exists but is **not** a bound guard (see below) | accepted → emits `Bs[tid+6]=Src[tid]`, runs and corrupts `B[7]` |\n\n`fill`/`clear` lack the `min` term entirely. `copy` *appears* to have the right check — `min >= 0 && min + extent <= shape` at [`copy_analysis.cc:374`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/copy_analysis.cc#L374) — but that is `CanProveCopyInBounds`, whose only caller ([`copy_analysis.cc:656`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/copy_analysis.cc#L656)) uses it as an **optimization gate**: `layout_dependent_tma_available = has_layout_map && !is_cutedsl && CanProveCopyInBounds(...)`. When the region is *not* provably in bounds it returns false, which merely disables the TMA 1D fast path and falls back to the ordinary copy — it never rejects. So an OOB copy is not caught at all; it just takes the slow path and emits the out-of-bounds store. In other words no region op rejects an out-of-bounds sliced region — `fill`'s incomplete `extent <= shape` is the *only* actual reject check in the family, and even it misses the `min` term. Because there is no shared bound check, fixing `fill` alone leaves `copy`/`clear` writing out of bounds; a single bound check at the region-construction layer (where every op's slice is formed) would cover all of them. `T.fill` on a `fragment` is rejected here only incidentally — a layout check trips first, not a bound check.\n\n**Provenance.** The `region.min + var` store offset and the extent-only bound check were both introduced by **#1189 (`a03df604`, \"[Feature] Enhance fill operation to support various buffer types\")** — #1189 added the nonzero-start slice support but wrote the guard as `extent <= shape` without the `min` term, so the sliced-fill OOB has never been caught (never-worked, not a regression). The file was later relocated (not re-introduced) by the `TileOperator`/backend refactors; the check is unchanged through `v0.1.13`. Reproduced at runtime on 0.1.13 this session: `T.fill(As[6:12],7)` into a shape-8 shared buffer silently compiles and emits `As[threadIdx.x + 6] = 7` (writes 8..11), while the pure-extent sibling `As[0:12]` is rejected (`extent 12 > shape 8`) and the in-bounds `As[2:8]` compiles.\n\n**Dedup.** I searched the open and closed Issue Tracker and found no existing report of this bound-check defect; none names the omitted `min + extent` term in the sliced-fill bound check. It is distinct from #2942 (`T.fill` of a row-offset **fragment** sub-region silently fills the *wrong rows* — a non-injective layout-mapping miscompile, disposition compute-correctly), which is a different object (fragment vs shared), symptom (wrong data vs out-of-bounds write), and root (layout mapping vs the missing bound-check term). This one is the constructor bound check accepting an illegal out-of-bounds region.\n\n**Reach.** Sliced `T.fill` (`T.fill(buf[a:b], v)`) is ordinary usage for initializing a sub-region of a tile. The trigger is any slice whose `start + length` exceeds the buffer — an easy off-by-one when a stride/offset is computed. No shipped example or test passes such a slice (they fill in-bounds), so CI stays green.\n\n**Impact.** The trigger is narrow — it needs a statically-out-of-bounds *sliced* fill (nonzero start whose start+length exceeds the shape); a pure-extent overflow is already caught, and in-bounds slices are fine. But when it fires the consequence is severe: instead of a diagnostic it silently emits an out-of-bounds shared-memory store (a memory-safety / soundness gap), deterministic and past the buffer end, corrupting whatever aliases those slots — the kind of error the bound check exists to prevent.\n", "url": "https://github.com/tile-ai/tilelang/issues/2979"}
{"number": 3039, "title": "[BUG][Fuzzer][ice-on-valid-code] Reading a `T.Pipelined` producer shared buffer after the loop crashes the compiler instead of compiling", "state": "closed", "created_at": "2026-08-17T05:34:05Z", "updated_at": "2026-09-06T14:28:52Z", "closed_at": "2026-09-06T14:28:52Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar issue was found.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 / PyTorch 2.8.0 / CUDA 12.8; NVIDIA L40S (sm_89). The crash is in a target-independent host pass (`LayoutInference`), so it is not hardware-specific.\n\n### Problem description\n\nA shared buffer that is written inside a `T.Pipelined(..., num_stages>=2)` loop and then read *after* the loop crashes compilation with an internal assert, while the identical kernel with `num_stages=1` compiles and runs:\n\n```\nInternalError: Check failed: (az->CanProveEqual(input_shape_product * rescale_num, shape_product * rescale_den)) is false:\nInputShape() = (3, 64, 32) shape = (64, 32), rescale_num = 16, rescale_den = 16\n```\n\nThe `(3, 64, 32)` is the buffer after pipeline multi-versioning has prepended the `num_stages=3` version dimension; `(64, 32)` is the buffer's original shape as seen at the post-loop read. Same trigger with `num_stages=1` (no versioning) compiles and runs correctly, so the crash is specific to the multi-versioning path — not the read itself. Not a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\nM, K, BK = 64, 128, 32\n\ndef build(num_stages):\n    @tilelang.jit(out_idx=[1])\n    def kern():\n        @T.prim_func\n        def main(A: T.Tensor((M, K), \"float16\"), Out: T.Tensor((M, BK), \"float16\")):\n            with T.Kernel(1, threads=128) as bx:\n                As = T.alloc_shared((M, BK), \"float16\")   # producer, written each stage\n                Bs = T.alloc_shared((BK, M), \"float16\")\n                Cf = T.alloc_fragment((M, M), \"float32\")\n                T.clear(Cf)\n                for k in T.Pipelined(K // BK, num_stages=num_stages):\n                    T.copy(A[0:M, k * BK:(k + 1) * BK], As)\n                    for i, j in T.Parallel(BK, M):\n                        Bs[i, j] = T.float16(1.0)\n                    T.gemm(As, Bs, Cf)\n                T.copy(As, Out)                            # read As AFTER the pipelined loop\n        return main\n    return kern()\n\n# CONTROL: same kernel, same post-loop read, num_stages=1 (no multi-versioning) -> compiles + runs\nf = build(num_stages=1)\na = torch.randn(M, K, device=\"cuda\", dtype=torch.float16)\no = f(a)\nprint(\"num_stages=1 :\", \"COMPILED+RAN, out\", tuple(o.shape))   # -> COMPILED+RAN, out (64, 32)\n\n# TRIGGER: num_stages>=2 multi-versions As; the post-loop read then crashes the compiler\nf = build(num_stages=3)                                        # -> InternalError (see Traceback)\nprint(\"num_stages=3 : COMPILED (unexpected)\")\n```\n\n### Traceback\n\n```\n  File \"tilelang/cuda/pipeline.py\", line 117, in CUDAPassPipelineBodyPrologue\n    mod = tilelang.transform.LayoutInference()(mod)\n  ...\n  in tvm::tl::LayoutInferencer::Substitute(tvm::tirx::PrimFunc)\n  in tvm::tl::BufferUseDefCollector::Run()\n  in tvm::tl::BufferUseDefCollector::RunInferStep(...)\n  in tvm::tl::BufferUseDefCollector::RunInferStep(...)::{lambda(Buffer, Layout)#1}::operator()(...)\n  File \"/project/src/layout/layout.cc\", line 871, in tvm::tl::LayoutNode::Reshape(...)\ntvm.error.InternalError: Check failed: (az->CanProveEqual(input_shape_product * rescale_num, shape_product * rescale_den)) is false: InputShape() = (3, 64, 32) shape = (64, 32), rescale_num = 16, rescale_den = 16\n```\n\n### Expected behavior\n\nCompilation completes instead of tripping an internal assert. The two consistent outcomes both seem reasonable: either the post-loop read is honored (reading the buffer's last-written version), or, if reading a pipeline-versioned producer after the loop is not intended to be supported, a clean diagnostic at the frontend that names the buffer and the pattern. An internal `CanProveEqual` assert deep in layout inference is neither.\n\n### Additional context\n\n**Root cause.** Two passes disagree about a shared buffer that is both a `T.gemm` operand *and* read after the pipelined loop. The multi-version buffer rewriter (run by the software-pipeline stack) rewrites the buffer's allocation to carry a leading `num_stages` version dimension, so its declared shape becomes `[3, 64, 32]`. `LayoutInference` independently infers an MMA-operand layout for that buffer over its original `[64, 32]` shape (from the `T.gemm` use), then — because the versioned allocation and the operand use are the same underlying storage — propagates the `[64, 32]` layout onto the `[3, 64, 32]` alias via `LayoutNode::Reshape`. That reshape assumes the two shapes differ only by element width (an alias-reinterpret rescale) and fails the storage-product invariant: `prod([3,64,32])*16 != prod([64,32])*16`. The trigger is narrow and specific: adjacent-cell probes (see Reach) show it needs the read-after buffer to carry an inferred MMA-operand layout — a versioned shared buffer that is *not* a gemm operand, or the gemm accumulator fragment, is read after the loop without crashing.\n\n<details><summary>Where it happens (0.1.13)</summary>\n\n- The version dimension is prepended in [`RewriteAllocBuffer`, `multi_version_buffer_rewriter.cc:494`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/transform/multi_version_buffer_rewriter.cc#L494) — [`shape.insert(begin, num_versions)`, :503](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/transform/multi_version_buffer_rewriter.cc#L503) → `[64,32]` becomes `[3,64,32]`.\n- The rewriter's own [BufferLoad guard, `:769`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/transform/multi_version_buffer_rewriter.cc#L769) (`ICHECK(version_index.defined()) << \"Versioned buffer load escaped pipeline stage context\"`) shows the escaped-use case was anticipated, but a post-loop read reaching layout inference is not caught there.\n- The crash surfaces in the alias-propagation path of layout inference, which reshapes the inferred layout onto a same-data sibling assuming the two shapes differ only by element width: [`layout_inference.cc:184`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/layout_inference.cc#L184).\n- The assert is the storage-product check in [`LayoutNode::Reshape`, `layout.cc:871`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/layout/layout.cc#L871): `prod([3,64,32])*16 != prod([64,32])*16`.\n\n</details>\n\n**Suggested fix.** The multi-version rewriter should detect a use of a versioned buffer outside the pipeline stage context (the case its `:769` guard already describes) and either keep that use on an un-versioned view or reject it at the frontend with a clear message; alternatively the layout-inference alias propagation at `:184` should account for a prepended version dimension rather than treating the shape difference as an element-width rescale. This touches either the pipeline rewriter or layout inference, so it is not a one-line change (not verified end-to-end).\n\n**Provenance.** The version-dimension prepend is part of the multi-version buffer rewriter, present since it shipped (`multi_version_buffer_rewriter.cc`, created in [#2166](https://github.com/tile-ai/tilelang/pull/2166), merged 2026-05-12; software-pipeline transforms later refactored in [#2245](https://github.com/tile-ai/tilelang/pull/2245)). The equivalent versioning existed inline in the pipeline pass before that split. The crash is the interaction of this versioning with layout-inference alias propagation; I did not pin a single introducing PR for the interaction.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this exact crash. The closest relative is [#2309](https://github.com/tile-ai/tilelang/issues/2309) (open, \"LayoutInference crash for sibling `T.Pipelined` loops sharing `alloc_shared` buffers with different `num_stages`\") — a **distinct bug that shares this one's underlying theme**: both are a `T.Pipelined` × shared-buffer interaction where the buffer's *actual multi-versioned storage layout* disagrees with the shape a later use assumes, and `LayoutInference` cannot reconcile the two. They differ in the triggering structure: #2309 needs **two sibling pipelined loops** that request **different `num_stages`** for the *same* `alloc_shared` (the conflict is between the two loops' version counts), whereas this bug needs a **single** pipelined loop whose versioned MMA-operand buffer is **read after the loop** (the conflict is between the versioned allocation `[num_stages,…]` and the operand's original `[…]` shape at the post-loop read). Same family — the multi-version rewriter's prepended version dimension not being accounted for downstream — but a different trigger and a different reconciliation site, so they warrant separate reports (and likely a shared fix in how versioned aliases carry their layout). Two further, more distant relatives: [#2528](https://github.com/tile-ai/tilelang/issues/2528) also involves the multi-version rewriter but is a distinct defect — a runtime `CUDA error: misaligned address` from missing 128B padding on a 1-D TMA copy, not a compile-time layout-inference assert; and [#2609](https://github.com/tile-ai/tilelang/issues/2609) is also a `LayoutNode::Reshape` failure but for a cross-element-width swizzled `T.view`, with no pipeline versioning involved.\n\n**Impact.** The trigger is narrow: a shared `T.gemm`-operand buffer read after a `num_stages>=2` pipelined loop, a pattern the shipped examples happen to dodge by reading the fragment accumulator instead. `examples/gemm/example_gemm.py` was **run verbatim on 0.1.13** and compiles+runs+passes (`All check passed.`, latency 0.033 ms): it uses `num_stages=3` but copies the fragment accumulator `C_local` — not a shared operand — after the loop, so it never hits the versioned-alias reshape. When the bug fires the failure is a compile-time internal assert in `LayoutInference` — loud and deterministic, caught the moment the kernel is built; it corrupts no data and cannot reach a running workload silently. Fixing it either lets a legal \"fill a shared operand tile across the loop, reuse its final contents after\" kernel compile or replaces the deep `CanProveEqual` assert with a clear frontend diagnostic naming the buffer and pattern.\n\n**Reach.** The root is a two-pass interaction — the **multi-version buffer rewriter** (`src/cuda/transform/multi_version_buffer_rewriter.cc`, driven by the software-pipeline stack) prepends the version dimension, and **`LayoutInference`** (`src/transform/layout_inference.cc`, run from `tilelang/cuda/pipeline.py:117`) reshapes an MMA-operand layout onto that versioned alias. Neither pass is wrong in isolation; the assert fires only where they compose. I ran six isolated cells on 0.1.13 (fresh cache each, one kernel per process) to map the boundary:\n\n- `num_stages=1` (control) — **COMPILED+RAN**, `out (64,32)`. No versioning, no crash.\n- `num_stages=2`, gemm-operand `As` read after loop — **CRASH**, `InputShape() = (2,64,32)`. Same assert, version dim = `num_stages`. **Same root** as the `num_stages=3` trigger.\n- `num_stages=3`, gemm-operand `As` read after loop — **CRASH**, `InputShape() = (3,64,32)`. The headline trigger.\n- `num_stages=3`, read the *fragment accumulator* after loop (the canonical GEMM pattern, `examples/gemm/example_gemm.py` copies `C_local` not the shared operand) — **COMPILED+RAN**, `out (64,64)`. **Distinct path**: fragment read-after does not hit the versioned-alias reshape. This is why shipped examples and CI stay green.\n- `num_stages=3`, read-after buffer written in-loop but **not** a gemm operand (gemm uses a separate shared pair) — **COMPILED+RAN**, `out (64,32)`. **Distinct**: without an inferred MMA-operand layout there is nothing to reshape onto the versioned alias.\n- `num_stages=3`, gemm-operand `As` written by `T.Parallel` instead of `T.copy`, read after loop — **CRASH**, `InputShape() = (3,64,32)`. **Same root**: the discriminator is *being an MMA operand read after the loop*, not how the buffer is written.\n\nSo the necessary ingredients are all three of: `num_stages>=2` (versioning), the read-after buffer is a `T.gemm` operand (so `LayoutInference` gives it an MMA layout), and it is read after the loop (so the versioned allocation and the operand use alias the same storage). `T.Pipelined` with `num_stages>=2` is itself ubiquitous (grep at this SHA: **130** example files use `T.Pipelined`, **1105** `num_stages` sites under `examples/`); the rarer combination is reusing an MMA-operand shared tile after the pipelined loop, which nothing in the `T.Pipelined` docstring disclaims.\n\n**Generalization (root + 4-axis sweep, all cells run on 0.1.13).** *Two-level root.*\n- **SOURCE-level root.** The fragile code is the alias-reinterpret branch of layout inference: [`layout_inference.cc:184`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/layout_inference.cc#L184) calls `LayoutNode::Reshape(sib->shape, ..., old_bits, new_bits)` for any two buffers sharing storage, assuming they differ only by element **width**. When one alias is the pipeline-versioned allocation (`[num_stages, ...]`) the shape differs by a prepended *count* dimension, not a width, and the storage-product ICHECK at [`layout.cc:871`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/layout/layout.cc#L871) fails. The version dimension itself is prepended upstream by `RewriteAllocBuffer` in the multi-version buffer rewriter (`multi_version_buffer_rewriter.cc:494/503`). Neither pass is wrong alone; the assert fires only where they compose.\n- **OPERATOR-level root.** Being an **MMA operand read after the pipelined loop.** The `T.gemm` use gives the shared buffer an MMA-operand layout that must be reshaped onto the versioned alias; the *read* is what makes the versioned allocation and the operand use the same storage. Related ops correlate only if they too impose a layout on a versioned shared buffer that is then read post-loop.\n\n**4-axis sweep** (each cell run in a fresh `/tmp/gen_23_*` dir, fresh cache, one kernel/process):\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| baseline | `num_stages=1`, LHS operand read-after (control) | COMPILED+RAN, out (64,32) | n/a (no versioning) |\n| baseline | `num_stages=2/3`, LHS operand `As` read-after | CRASH `InputShape()=(2,64,32)` / `(3,64,32)`, rescale 16/16 | headline |\n| similar-logic | operand read-after via `T.Parallel` elementwise (not `T.copy`) | CRASH `(3,64,32)`, rescale 16 | **same-root** — read *op* is irrelevant |\n| similar-logic | operand written by `T.Parallel` instead of `T.copy`, read-after | CRASH `(3,64,32)` | **same-root** — write *op* is irrelevant |\n| related-source | fragment accumulator read-after (the `example_gemm.py` path) | COMPILED+RAN, out (64,64) | distinct — fragment has no versioned-alias reshape |\n| related-source | versioned shared buffer read-after that is **not** a gemm operand | COMPILED+RAN, out (64,32) | distinct — no MMA layout to reshape |\n| related-operator | read the **RHS** gemm operand `Bs` after loop (vs LHS) | COMPILED+RAN, out (32,64) | distinct — RHS operand layout reshapes cleanly onto the versioned alias; only LHS crashes |\n| related-type | **bf16** LHS operand read-after | CRASH `(3,64,32)`, rescale 16/16 | **same-root** — dtype-independent within 16-bit |\n| related-type | **fp8 (e4m3)** LHS operand read-after | CRASH `(3,64,32)`, rescale **8/8** | **same-root** — width-independent; storage-bits change but the product mismatch (prepended count dim) is unchanged |\n\n**Findings.** The root is dtype/width-independent (fp16, bf16, fp8 all crash identically; the `rescale` numbers track element-storage-bits but the failure is the prepended count dimension, not any width mismatch) and read-op/write-op-independent (`T.copy` and `T.Parallel` both crash). The discriminator is exactly *being an LHS MMA operand read after the loop*: fragment-accumulator reads, non-operand shared reads, and **RHS-operand** reads all compile. The RHS-vs-LHS asymmetry (draft's previously-unpinned note) is now confirmed by direct test — only the LHS operand crashes; the RHS operand's inferred layout reshapes onto the versioned alias without tripping the product check. This is a **narrow, specific** boundary, not a class, so the title and Problem are **kept specific**. No new distinct-adjacent bug and no separately-fileable neighbor were found: every crashing neighbor is the same root, every non-crashing neighbor is a genuinely distinct (correct) path.\n\n**Example-run result (PART 1).** `examples/gemm/example_gemm.py` (v0.1.13) run verbatim: **COMPILED+RAN, `All check passed.`**, latency 0.033 ms. It uses `num_stages=3` but copies the fragment accumulator `C_local` after the loop (source confirmed) — so the shipped example does **not** exercise this bug; it dodges it exactly as the Reach section claims. This is honest evidence that the pattern is off the CI path, which is why the crash went unreported, not evidence that the trigger is common.", "url": "https://github.com/tile-ai/tilelang/issues/3039"}
{"number": 2837, "title": "[BUG] Resolve symlinked nvcc before deriving CUDA_HOME", "state": "closed", "created_at": "2026-08-01T23:50:41Z", "updated_at": "2026-09-04T11:17:59Z", "closed_at": "2026-09-04T11:17:59Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Prerequisite\n\n- [x] I have searched existing issues and related NVRTC include discovery work.\n\n### Problem\n\nWhen nvcc is exposed through a symlink, _find_cuda_home() derives CUDA_HOME from the symlink's directory instead of the compiler's real location. This commonly occurs with NVIDIA's pip CUDA packages.\n\nOn this system:\n\n| Path | Result |\n| --- | --- |\n| /home/morluto/.local/bin/nvcc | symlink |\n| resolved compiler | .../site-packages/nvidia/cu13/bin/nvcc |\n| previous discovered CUDA_HOME | /home/morluto/.local |\n| expected CUDA_HOME | .../site-packages/nvidia/cu13 |\n\nThe incorrect root omits include/cuda_runtime.h, causing TileLang CUDA compilation to fail even though a working NVCC installation is available.\n\n### Reproduction\n\n1. Put a symlink to a standard <CUDA_HOME>/bin/nvcc on PATH.\n2. Leave CUDA_HOME and CUDA_PATH unset.\n3. Call tilelang.env._find_cuda_home().\n\nThe result is the parent of the symlink location rather than the CUDA installation root.\n\n### Expected behavior\n\nAuto-discovery should resolve nvcc before deriving its CUDA root. Explicit CUDA_HOME and CUDA_PATH should retain precedence.\n\n### Proposed fix\n\nResolve the shutil.which(\"nvcc\") result with os.path.realpath() before applying the existing parent-directory logic, with a deterministic symlink regression test.", "url": "https://github.com/tile-ai/tilelang/issues/2837"}
{"number": 2634, "title": "[BUG][Fuzzer][wrong-code] Hopper `T.gemm_sp` emits code that omits the ISA-required `fence.proxy.async` before the sparse WGMMA — silently reads stale operand data", "state": "closed", "created_at": "2026-07-12T06:05:24Z", "updated_at": "2026-09-04T06:50:04Z", "closed_at": "2026-09-04T06:50:04Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.11 (source at `cd37ed5fc35ae7a60a1277c8eb49028174ac51e6`). Also live on `main` (`1e075149`).\n\n### System information\n\nNVIDIA H100 80GB HBM3 (sm_90), CUDA 13.0, tilelang 0.1.11. `fence.proxy.async` is a Hopper construct (`InjectFenceProxy` is gated on `TargetHasBulkCopy`, i.e. sm_90+), so the defect is sm_90-specific. Blackwell (sm_100) is affected by inspection — the blockscaled sparse builtin `ptx_tcgen05_mma_blockscaled_ss` has the same omission — but I did not run it.\n\n### Problem description\n\n**`T.gemm_sp` emits code that omits the `fence.proxy.async` the PTX proxy model requires** between a generic-proxy write to a shared WGMMA operand tile and the async-proxy `wgmma.mma_async.sp` read of that same tile. On the identical kernel, the dense `T.gemm` emits the fence and the sparse `T.gemm_sp` emits **zero**. Per PTX ISA §9.7.16.7.1 — *\"An async proxy fence must be used to establish an ordering between prior writes to shared memory matrices and subsequent reads of the same matrices in a `wgmma.mma_async` instruction\"* — the generated sparse code is **incorrect by the documented contract**: the WGMMA is permitted to read stale/torn operand data, a silent wrong result, with nothing ordering the two.\n\nThis is a wrong-code defect in the emitted PTX/SASS, established at the code level (counts + adjacency below) and grounded in the ISA. I did not force a divergent value out of `T.gemm_sp` itself on the tested H100 — an incidental `bar.sync` (the pipeline `__syncthreads()`) happens to drain the write on that schedule — but that barrier is **not** the ISA-required ordering (§8.7: it is a different proxy, so it establishes no happens-before edge), and a minimal control carrying the exact \"`bar.sync` present, proxy fence absent\" fingerprint **does** race the silicon (see Additional context).\n\nThe two kernels below are identical except `T.gemm` vs `T.gemm_sp`; both put a `B_s[i,j] += 3` generic store right before the matmul. `T.gemm` emits one `fence.proxy.async`; `T.gemm_sp` emits zero.\n\n<details><summary>Emitted PTX and SASS — the required fence is present for dense, absent for sparse (H100, sm_90, CUDA 13.0)</summary>\n\nCounts (grep of the emitted code): CUDA `fence_proxy_async` dense **1** / sparse **0**; PTX `fence.proxy.async` dense **1** / sparse **0**; SASS main-loop proxy fence (`MEMBAR.ALL.CTA` + `FENCE.VIEW.ASYNC.S` right after the generic operand store) dense **present** / sparse **absent** (both kernels share one *prologue* `FENCE.VIEW.ASYNC.S`; the load-bearing difference is the main-loop one).\n\n```\n=== PTX, dense (proxy fence PRESENT) ===\n    wgmma.fence.sync.aligned;                 // register/accumulator fence — NOT the proxy fence\n    fence.proxy.async.shared::cta;            // <-- the generic->async-proxy fence\n    wgmma.mma_async.sync.aligned.m64n128k16.f32.f16.f16 {...};\n\n=== PTX, sparse (proxy fence ABSENT) ===\n    wgmma.fence.sync.aligned;                 // register fence only\n    wgmma.mma_async.sp.sync.aligned.m64n128k32.f32.f16.f16 {...};   // async read, unfenced\n    // the generic operand store st.shared.v4.u32 precedes this with nothing ordering it\n\n=== SASS, dense: STS.128 -> MEMBAR.ALL.CTA ; FENCE.VIEW.ASYNC.S ; WARPGROUP.ARRIVE ; HGMMA.64x128x16.F32\n=== SASS, sparse: STS.128 -> BAR.SYNC.DEFER_BLOCKING ; WARPGROUP.ARRIVE ; HGMMA.SP.64x128x32.F32\n       (full scan of the sparse store->WGMMA.SP window: zero MEMBAR / FENCE.VIEW / DEPBAR)\n```\n\nThe `wgmma.fence.sync.aligned` present in **both** is the accumulator/register fence, not the generic→async-proxy fence — only dense additionally emits `fence.proxy.async`. In SASS, where dense has `FENCE.VIEW.ASYNC.S`, sparse has only a `BAR.SYNC` — a barrier, which the PTX memory model (§8.7) does not accept as a substitute because it does not change an access's proxy.\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\nfrom tilelang.utils.sparse import get_e_factor\n\nM = N = K = 256; bM = bN = 128; bK = 64\ne_dtype = \"uint8\"; e_factor = get_e_factor(T.float16, e_dtype)\n\ndef dense():                                              # CONTROL: gets the fence\n    @T.prim_func\n    def main(A: T.Tensor((M, K), T.float16), B: T.Tensor((K, N), T.float16),\n             C: T.Tensor((M, N), \"float32\")):\n        with T.Kernel(T.ceildiv(N, bN), T.ceildiv(M, bM), threads=128) as (bx, by):\n            A_s = T.alloc_shared((bM, bK), T.float16); B_s = T.alloc_shared((bK, bN), T.float16)\n            C_l = T.alloc_fragment((bM, bN), \"float32\"); T.clear(C_l)\n            for k in T.Pipelined(T.ceildiv(K, bK), num_stages=2):\n                T.copy(A[by * bM, k * bK], A_s); T.copy(B[k * bK, bx * bN], B_s)\n                for i, j in T.Parallel(bK, bN):\n                    B_s[i, j] = B_s[i, j] + T.float16(3)   # generic-proxy write to operand tile\n                T.gemm(A_s, B_s, C_l)\n            T.copy(C_l, C[by * bM, bx * bN])\n    return main\n\ndef sparse():                                             # BUG: no fence\n    @T.prim_func\n    def main(A_sp: T.Tensor((M, K // 2), T.float16), E: T.Tensor((M, K // e_factor), e_dtype),\n             B: T.Tensor((K, N), T.float16), C: T.Tensor((M, N), \"float32\")):\n        with T.Kernel(T.ceildiv(N, bN), T.ceildiv(M, bM), threads=128) as (bx, by):\n            A_s = T.alloc_shared((bM, bK // 2), T.float16); E_s = T.alloc_shared((bM, bK // e_factor), e_dtype)\n            B_s = T.alloc_shared((bK, bN), T.float16); C_l = T.alloc_fragment((bM, bN), \"float32\"); T.clear(C_l)\n            for k in T.Pipelined(T.ceildiv(K, bK), num_stages=2):\n                T.copy(A_sp[by * bM, k * bK // 2], A_s); T.copy(E[by * bM, k * bK // e_factor], E_s)\n                T.copy(B[k * bK, bx * bN], B_s)\n                for i, j in T.Parallel(bK, bN):\n                    B_s[i, j] = B_s[i, j] + T.float16(3)   # same generic write\n                T.gemm_sp(A_s, E_s, B_s, C_l, transpose_A=False, transpose_E=False, transpose_B=False)\n            T.copy(C_l, C[by * bM, bx * bN])\n    return main\n\nfor name, fn, idx in [(\"DENSE T.gemm\", dense, 2), (\"SPARSE T.gemm_sp\", sparse, 3)]:\n    k = tilelang.compile(fn(), out_idx=[idx])\n    print(f\"{name}: fence.proxy.async count = {k.get_kernel_source().count('fence_proxy_async')}\")\n# DENSE  T.gemm:    fence.proxy.async count = 1\n# SPARSE T.gemm_sp: fence.proxy.async count = 0   <-- missing\n```\n\n### Traceback\n\nNo traceback — both kernels compile and run to completion. The defect is missing memory-ordering in the emitted code: the generated sparse PTX/SASS omits the `fence.proxy.async` the ISA requires, so the sparse WGMMA is permitted to read a stale/torn operand — a silent wrong result whenever the (absent) ordering actually matters.\n\n### Expected behavior\n\n`T.gemm_sp` should emit the same `fence.proxy.async` that `T.gemm` emits when a generic-proxy shared write precedes it — i.e. the sparse WGMMA should be treated as an async-proxy op by `InjectFenceProxy`, exactly like the dense WGMMA already is. cutlass emits this fence for the same generic→async-proxy transition: [`cutlass/arch/barrier.h#L715-L721`](https://github.com/NVIDIA/cutlass/blob/b2dd65dc864e09688245b316ac46c4a6cd07e15c/include/cutlass/arch/barrier.h#L715-L721) (`fence_view_async_shared()` = `fence.proxy.async.shared::cta`) and [`cute/arch/copy_sm90_tma.hpp#L1214-L1217`](https://github.com/NVIDIA/cutlass/blob/b2dd65dc864e09688245b316ac46c4a6cd07e15c/include/cute/arch/copy_sm90_tma.hpp#L1214-L1217) (`tma_store_fence()`). The tilelang pass already encodes this rule for the dense path, so the sparse path being unfenced looks like an omission in the intrinsic list rather than an intentional difference.\n\n### Additional context\n\n**Root cause.** `InjectFenceProxy` does not recognize the sparse WGMMA builtins as async-proxy operations, so it omits the `fence.proxy.async` before a generic write that precedes a sparse matmul.\n\n<details><summary>Mechanism — the sparse variants are missing from the async-intrinsic list</summary>\n\nThe pass classifies calls via `IsAsyncIntrinsic` ([`inject_fence_proxy.cc#L99-L106`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/transform/inject_fence_proxy.cc#L99-L106)). That list contains the dense WGMMA builtins `ptx_wgmma_ss`/`ptx_wgmma_rs` and the dense tcgen05 ops, but **omits the sparse variants** `ptx_wgmma_sp_ss`, `ptx_wgmma_sp_rs`, and `ptx_tcgen05_mma_blockscaled_ss` (all public builtins). `T.gemm`/`T.gemm_sp` are lowered by `LowerTileOp` ([`pipeline.py#L117`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/tilelang/cuda/pipeline.py#L117)) **before** `InjectFenceProxy` runs ([`pipeline.py#L229`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/tilelang/cuda/pipeline.py#L229)): dense lowers to `ptx_wgmma_ss` (in the list → fenced), sparse lowers to `ptx_wgmma_sp_ss` (not in the list → not fenced). The default for unlisted intrinsics is now fail-open (`return ProxyEvent::kNone`), so the sparse op slips through with no fence. `T.gemm_sp` is a public op exported into the `T` namespace.\n\n</details>\n\n**Suggested fix (one line).** Add `ptx_wgmma_sp_ss()`, `ptx_wgmma_sp_rs()`, and `ptx_tcgen05_mma_blockscaled_ss()` to the `IsAsyncIntrinsic` disjunction, alongside their dense siblings. All three C++ symbols already exist. After adding them, the sparse repro emits `fence.proxy.async count = 1`, matching the dense control — restoring the ISA-required ordering the emitted code was missing.\n\n**Wrong-code confirmed in the emitted code; runtime symptom masked on the tested silicon.** The defect is confirmed at the level a `[wrong-code]` classification requires — the generated PTX/SASS omits the `fence.proxy.async` that PTX ISA §9.7.16.7.1 says *must* order a generic write to a shared WGMMA operand ahead of the async `wgmma.mma_async.sp` read; dense emits it, sparse does not (counts + adjacency in Problem description). I did **not** force a divergent numerical result out of `T.gemm_sp` itself (~29,000 iterations matched a manual-`T.fence_proxy_async()` control bit-exact), because an incidental partial `bar.sync` (the pipeline `__syncthreads()`) drains the generic write on this schedule. That `bar.sync` is not the ISA-required ordering — per PTX §8.7, two operations are morally strong only if performed via the *same* proxy, so a generic-proxy barrier establishes no ordering against an async-proxy read — so it protects neither other Hopper silicon/schedules, nor other tile shapes, nor Blackwell, nor any refactor that moves or removes it. The code is wrong; the current schedule merely masks the symptom.\n\n<details><summary>Positive control — the silicon <b>does</b> race when the drain is truly absent</summary>\n\nOn a minimally-derived single-warpgroup WGMMA kernel, three variants differ only in the ordering primitive between the generic operand write and the async read: **A** = nothing, **B** = `fence.proxy.async` (correct golden), **C** = `bar.sync` only, fence removed (the exact `T.gemm_sp` fingerprint). Racing C against the fenced golden B on identical fp16 inputs under a concurrent spoiler kernel, on H100 sm_90:\n\n- 100k iterations, seed 0x1001: **2** divergences, worst |diff| **30.9**;\n- 1M iterations, seed 0xBEEF (different GPU): **30** divergences, worst |diff| **31.4**; a captured launch had 512/8192 output elements wrong (a torn/partial operand read);\n- fence-vs-fence control (B vs B, same input): **0** divergences — the golden is deterministic, so every C divergence is a genuine ordering effect.\n\nThis demonstrates that a `bar.sync` without the proxy fence is not a valid substitute — the missing fence is a real, silicon-observable ordering hazard, not a theoretical one. (The divergence rate is low and timing-sensitive — inserting a per-launch sync often suppresses it — consistent with \"the silicon usually drains fast enough, but the ISA ordering guarantee is genuinely absent and *is* occasionally violated under scheduling pressure.\")\n\n</details>\n\n**Provenance.** Regression, introduced by [#2048](https://github.com/tile-ai/tilelang/pull/2048) (`f11954cb`, \"[Backend] Refactor gemm_sp\", 2026-05-19), first shipped in **0.1.10**. Before #2048, the sparse sm90 path emitted the `call_intrin` `tl::tl_gemm_sp()`, which **is** in the `IsAsyncIntrinsic` list ([`inject_fence_proxy.cc:114`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/transform/inject_fence_proxy.cc#L114)) — so sparse gemm **was** fenced at 0.1.9 (verified: `f11954cb^:src/backend/cuda/op/gemm_sp.cc:128` emits `tl::tl_gemm_sp()`). #2048 replaced that template path with direct `ptx_wgmma_sp_ss`/`ptx_wgmma_sp_rs` lowering and did **not** touch `inject_fence_proxy.cc` (git-verified via `--stat`), orphaning the fence and leaving the `tl_gemm_sp` list entry dead for the WGMMA path. The dense `ptx_wgmma_ss`/`ptx_wgmma_rs` entries were added earlier by #977; a later change (#1068) flipped the unlisted-intrinsic default from fail-safe (`kAsync`) toward fail-open, so an unlisted async intrinsic is no longer caught by default. On `main`, the `tl_gemm`/`tl_gemm_sp` fallbacks are removed entirely and the sparse variants are still absent — the defect persists.\n\n**Dedup.** I searched the open and closed tracker (`fence proxy`, `wgmma_sp fence`, `InjectFenceProxy`) and found no existing report. Distinct from the `cp.async.bulk`/cluster 16-byte-size and offset-alignment crash reports — those are alignment UB on the copy path; this is a missing proxy fence on the sparse WGMMA path.\n", "url": "https://github.com/tile-ai/tilelang/issues/2634"}
{"number": 2991, "title": "[BUG][Fuzzer][ice-on-valid-code] Unary `+` on any expression (`+A[i]`) raises `TypeError` at trace time instead of compiling to the identity", "state": "closed", "created_at": "2026-08-15T20:11:04Z", "updated_at": "2026-09-04T06:49:11Z", "closed_at": "2026-09-04T06:49:11Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar issue exists.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nNVIDIA L40S (sm_89) / CUDA 13.0 / PyTorch 2.13.0 / Python 3.13 — but the failure is target-independent: it fires at trace time, before any device code is generated.\n\n### Problem description\n\nA unary plus on a buffer element or any other expression — `Out[i] = +A[i]` — fails at trace time with `TypeError: bad operand type for unary +: 'BufferLoad'`, so a kernel using it never compiles. Unary minus (`-A[i]`), unary bitwise-not (`~A[i]`), and every binary operator on the same operand compile and run normally; only unary `+` is missing.\n\nUnary `+` on a number is the identity operation in Python (`+5 == 5`, and `int`/`float`/`numpy` scalars all define it), so `+A[i]` is a well-formed expression a user can reasonably write — e.g. to mirror a reference formula or to make a sign explicit. The TVMScript operator registry even records the expectation that it is handled (`# doc.UAdd <-- is overloaded`), but the overload it relies on does not exist.\n\nBoth frontends are affected: the default eager `@tilelang.language` builder raises the raw `TypeError` above, and the classic `tvm.script` `@T.prim_func` parser surfaces the same Python exception through its diagnostic renderer (`error: bad operand type for unary +: 'BufferLoad'`). Not a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\n# CONTROL: unary minus on a buffer element compiles and runs correctly.\n@T.prim_func\ndef neg(A: T.Tensor((4,), \"int32\"), Out: T.Tensor((4,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for i in T.serial(4):\n            Out[i] = -A[i]\n\nk = tilelang.compile(neg, out_idx=[1])\na = torch.tensor([-5, 10, 0, 7], dtype=torch.int32, device=\"cuda\")\nprint(k(a).tolist())          # -> [5, -10, 0, -7]   (compiles, correct)\n\n# TRIGGER: unary plus on the same element. Defining this prim_func alone\n# raises at trace time (the @T.prim_func decorator traces eagerly):\n@T.prim_func\ndef pos(A: T.Tensor((4,), \"int32\"), Out: T.Tensor((4,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for i in T.serial(4):\n            Out[i] = +A[i]      # -> TypeError: bad operand type for unary +: 'BufferLoad'\n```\n\n### Traceback\n\n```\n  File \".../repro.py\", line 9, in pos\n    Out[i] = +A[i]\n             ^^^^^\nTypeError: bad operand type for unary +: 'BufferLoad'\n```\n\n### Expected behavior\n\n`+A[i]` should compile and evaluate to the operand unchanged (identity), matching Python's numeric `__pos__` semantics and the fact that the sibling unary operators on the same expression type already work: `-A[i]` lowers to a multiply-by-`-1` and `~A[i]` to a bitwise-not. With a one-line `__pos__` added, `+A[i]` compiles and returns the input bit-for-bit for both integer and float buffers (verified below).\n\n### Additional context\n\n**Root cause.** The expression operator mixin `ExprOp` (in the vendored TVM under `3rdparty/tvm`, which every `PrimExpr` such as `BufferLoad` and `Var` inherits from) defines the full arithmetic/bitwise surface including `__neg__` and `__invert__` but never defines `__pos__`, so a Python unary `+` on any TileLang expression falls through to Python's default and raises `TypeError`. Both frontends route unary `+` straight to this operator: the classic parser's evaluator maps the AST node with `doc.UAdd: lambda a: +a`, and the TIR op registry leaves `UAdd` to Python overloading (`# doc.UAdd <-- is overloaded`) — but the overload it assumes is absent.\n\n<details>\n<summary>Source pointers and scope check</summary>\n\n- [`ExprOp.__neg__`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/tirx/expr.py#L123) and [`ExprOp.__invert__`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/tirx/expr.py#L157) exist; there is no `__pos__` anywhere on the class (grep of `expr.py` finds 0 `__pos__`).\n- classic parser: [`doc.UAdd: lambda a: +a`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/script/parser/core/evaluator.py#L59)\n- TIR op registry: [`# doc.UAdd <-- is overloaded`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/tirx/script/parser/operation.py#L154-L155)\n\n(The bundled TVM renamed `tir`→`tirx`, so `ExprOp` now lives at `python/tvm/tirx/expr.py`.)\n\nThe gap is `ExprOp`-wide, not buffer-specific — verified this session on v0.1.13, each in its own process:\n\n| expression | `-` (neg) | `~` (invert) | `+` (pos) |\n|---|---|---|---|\n| `A[i]` (int32 BufferLoad) | ok `[5,-10,0,-7]` | ok `[4,-11,-1,-8]` | crash `TypeError: bad operand type for unary +: 'BufferLoad'` |\n| `A[i]` (float32 BufferLoad) | — | — | crash `TypeError: bad operand type for unary +: 'BufferLoad'` |\n| `i` (bare loop `Var`) | — | — | crash `TypeError: bad operand type for unary +: 'Var'` |\n\nBoth `BufferLoad` and a bare `Var` fail identically, so the missing overload is on the shared `ExprOp` base, not on any one expression type. Class = **missing-case** (the `__pos__` method is absent from `ExprOp`); sibling unary operators `-`/`~` are present.\n</details>\n\n**Suggested fix.** Add a `__pos__` to `ExprOp` that returns the operand unchanged, e.g.\n\n```python\ndef __pos__(self) -> PrimExpr:\n    return self\n```\n\n<details>\n<summary>Fix verified end-to-end (identity result on int and float)</summary>\n\nWith `ExprOp.__pos__ = lambda self: self` monkeypatched in (v0.1.13), the trigger kernel above compiles and runs, returning the input unchanged:\n\n```\nint32:   +A -> [-5, 10, 0, 7]           (== input, identity)   PASS\n```\n</details>\n\n**Provenance.** The missing overload is long-standing: `ExprOp` ships without `__pos__` in the TVM fork pinned by TileLang 0.1.13 (`3rdparty/tvm` at `8df8ebd`, `python/tvm/tirx/expr.py` has `__neg__`/`__invert__` and zero `__pos__`), and upstream `apache/tvm` also has no `__pos__` on `ExprOp` (a code search finds `__neg__` in its `expr.py` but zero `__pos__`), so the gap predates the TileLang fork; an exact introducing commit was not traced.\n\n**Generalization (tested this session, 0.1.13 / L40S).** The root is a missing `__pos__` on `ExprOp`, so the failure is *independent of dtype and expression shape* — anything Python routes to unary `+` on a PrimExpr fails, nothing else does. Tested (each fresh process): `+A[i]` (float32 load) → `TypeError: bad operand type for unary +: 'BufferLoad'`; `+A[i]` on an `int32` buffer → same `TypeError`; `+(A[i]*2.0)` (compound expression) → same. Control `-A[i]` (`__neg__`, which IS defined) → OK, `[-0,-1,-2,-3]`. So the boundary is exactly \"unary `+` on any `PrimExpr`\" — no dtype/shape knob changes it, because the operator is simply absent; the title is kept as the whole \"unary `+` on any expression\" class (already framed that way).\n\n**Dedup.** I searched this project's filed issues and did not find an existing report of unary `+` / a missing `__pos__` overload; the closest, #2615, is a different mechanism (a Python builtin `min()` over `PrimExpr` operands, not a unary operator).\n\n**Reach.** The trigger is a single unary `+` on a `PrimExpr` inside a `@T.prim_func`; it fires unconditionally, on any dtype and any target, the first time such an expression is traced (no shape/arch gate). A grep of the shipped package (220 `.py` files, excluding `3rdparty`) finds no unary-plus-on-expression site; the sibling unary operators `-`/`~` are the ones that appear in-tree, so no test exercises `+`.\n\n**Impact.** The trigger is narrow but simple — any unary `+` on a traced expression, on any dtype/target. It is a trace-time `TypeError` (not a compiler abort and not a silent miscompile): it produces no wrong data and blocks only the one kernel from tracing, surfacing as an uncatchable Python exception raised eagerly at `@T.prim_func` decoration. A user mirroring a reference formula that writes `+x` hits a hard failure instead of the Python-standard identity.\n\n**Distinctness.** This is a frontend `ExprOp.__pos__` gap — distinct root from the missing half-precision math lowering (`copysign`/`atan2`/`nextafter`) and from the `sync_global` backend `LOG(FATAL)`; kept as a specific report, not folded.", "url": "https://github.com/tile-ai/tilelang/issues/2991"}
{"number": 3164, "title": "[Question][Carver] TensorCorePolicy: tiles whose smem_cost × pipeline_stage exceeds smem_cap are pruned rather than downgraded, and the shared.dyn fallback in _assign_block_size looks unreachable", "state": "open", "created_at": "2026-09-03T14:23:23Z", "updated_at": "2026-09-04T06:09:06Z", "closed_at": null, "labels": ["question"], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Questions\n\nTwo related observations in `tilelang/carver/roller/policy/` (line numbers identical on\n`v0.1.13` and `main` @ `73bbff58`):\n\n1. `TensorCorePolicy.infer_node_smem_usage` multiplies the shared-memory estimate by\n   `self.pipeline_stage` (`tensorcore.py:83`). `DefaultPolicy.compute_tile_dict` then marks\n   the tile invalid as soon as `td.smem_cost > self.arch.smem_cap` (`default.py:558-559`),\n   and invalid tiles are filtered out of the search (`default.py:119`, `:132`). So a tile\n   that would fit with `pipeline_stage = 1` but not with the arch-fixed `pipeline_stage = 2`\n   is dropped entirely instead of being kept with a lower stage count. Is this pruning\n   intended, or would a capacity-driven stage fallback (`max S s.t. S × smem_tile ≤ smem_cap`)\n   be welcome? It mainly affects large tiles (e.g. fp16 128x256x32 at 2 stages is close to\n   the 48 KB default `smem_cap`).\n\n2. `_assign_block_size` (`tensorcore.py:328-333`) has a branch\n   `if td.smem_cost > self.arch.smem_cap: shared_scope = \"shared.dyn\"`. Given (1), a tile\n   reaching this point has already passed the same check, so the branch appears dead. It is\n   then followed by an unconditional `codegen_dict.shared_scope = \"shared.dyn\"` (comment: a\n   dummy to be removed), which also makes the earlier conditional assignment for\n   `out_dtype == float32` (`:324`) dead. Is the unconditional `shared.dyn` meant to stay?\n\nAsking before proposing a change: a stage fallback would touch the same lines, and the\nanswer to (2) decides whether it should also clean up the dead branches.", "url": "https://github.com/tile-ai/tilelang/issues/3164"}
{"number": 2946, "title": "[BUG][Fuzzer][wrong-code] A loop `else:` clause (`for…else`/`while…else`) is silently dropped instead of executed or rejected", "state": "closed", "created_at": "2026-08-12T08:18:11Z", "updated_at": "2026-09-03T05:44:10Z", "closed_at": "2026-09-03T05:44:10Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar issue was found.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release; also present on `main` at 6af825e).\n\n### System information\n\nPython 3.13.13, linux\ntilelang 0.1.13\ntorch 2.13.0+cu130\nGPU: NVIDIA L40S (sm_89)\n\n### Problem description\n\nA `for … else:` (or `while … else:`) written inside a kernel compiles without any error, and the body of the `else:` clause is silently discarded — it never appears in the generated IR and never runs. The kernel returns the result as if the `else:` block were deleted.\n\n`break`/`continue` are accepted inside kernel loops (they lower to `T.break_loop()` / `T.continue_loop()`), so a loop `else:` — \"run this after the loop finishes without a `break`\" — has a well-defined meaning here; it is not a no-op construct.\n\nMinimal repro below: a loop stores `A[i]+1`, and its `else:` clause adds `100` to every element. The correct result is `A+1+100`; the actual result is `A+1` (the `else:` never runs). A control kernel with the `else:` body hand-inlined after the loop returns the correct values, confirming the values are computable and the discrepancy is the dropped `else:`, not a harness artifact. `while…else` behaves identically (collapsed below).\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\nN = 8\n\n@T.prim_func\ndef with_forelse(A: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for i in T.serial(N):\n            B[i] = A[i] + 1\n        else:                       # runs after the loop completes without break\n            for j in T.serial(N):\n                B[j] = B[j] + 100\n\n@T.prim_func\ndef control(A: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for i in T.serial(N):\n            B[i] = A[i] + 1\n        for j in T.serial(N):       # the else body, hand-inlined = the correct behavior\n            B[j] = B[j] + 100\n\nA = torch.arange(N, dtype=torch.int32, device=\"cuda\")\nref = A + 1 + 100                   # else runs -> +100\n\ngot = tilelang.compile(with_forelse, out_idx=[-1])(A)\nctl = tilelang.compile(control,      out_idx=[-1])(A)\n\nprint(\"for...else:\", got.cpu().tolist())   # -> [1, 2, ..., 8]         (else dropped)\nprint(\"control   :\", ctl.cpu().tolist())   # -> [101, 102, ..., 108]\nprint(\"for...else correct?\", torch.equal(got, ref))   # -> False\nprint(\"control    correct?\", torch.equal(ctl, ref))   # -> True\n```\n\n<details><summary><code>while…else</code> face — same root, same silent drop</summary>\n\n```python\n@T.prim_func\ndef while_else(A: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        i = T.alloc_local((1,), \"int32\")\n        i[0] = 0\n        while i[0] < N:\n            B[i[0]] = A[i[0]] + 1\n            i[0] = i[0] + 1\n        else:\n            for j in T.serial(N):\n                B[j] = B[j] + 100\n\n# expected [101..108]; got [1..8] -> while...else correct? False\n```\n</details>\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion; the `else:` body is silently omitted and the result is deterministically wrong.\n\n### Expected behavior\n\nThe `else:` body should either be executed with Python's loop-`else` semantics (it runs when the loop is not exited by `break`), or, if the eager frontend does not intend to support it, the frontend should reject it with a clear error rather than silently drop it. The frontend already raises `NotImplementedError` for other syntax it does not handle (e.g. attribute-target assignment and unsupported `for` targets), so a clean diagnostic is consistent with its own convention.\n\n### Additional context\n\n**Root cause.** The eager AST transform does not emit the `else:` clause of a loop: [`visit_For`](https://github.com/tile-ai/tilelang/blob/6af825e072d305a87ac039be0abd27b83c9a1bdc/tilelang/language/eager/ast.py#L318) and [`visit_While`](https://github.com/tile-ai/tilelang/blob/6af825e072d305a87ac039be0abd27b83c9a1bdc/tilelang/language/eager/ast.py#L482) only pass `node.body` into the rewritten frame and never reference `node.orelse`, so the `else:` statements are dropped during the source-to-source rewrite before any IR is built. By contrast [`visit_If`](https://github.com/tile-ai/tilelang/blob/6af825e072d305a87ac039be0abd27b83c9a1bdc/tilelang/language/eager/ast.py#L291-L301) does thread `node.orelse` through, so the omission is specific to the two loop visitors.\n\n<details><summary>Note on the <code>break</code> interaction (why \"execute\" is not a trivial fix)</summary>\n\n`break` lowers to a runtime `T.break_loop()`, so faithful `for…else` semantics (run the `else:` only if the loop finished without a `break`) require guarding the `else:` body on whether a break fired at runtime, not just appending it after the loop. In the common no-`break` case the `else:` body is unconditional and the hand-inlined control above shows it is directly computable; the general case needs the break-condition guard. Rejecting the clause with a clear error is the simpler alternative if full support is out of scope.\n</details>\n\n**Suggested fix.** `visit_For`/`visit_While` (`eager/ast.py`) should thread `node.orelse` through the rewrite the way `visit_If` already does — emitting the `else:` body with loop-`else` semantics (guarded on whether a `T.break_loop()` fired at runtime) — or, if full support is out of scope, raise a clear `NotImplementedError` for a loop with an `else:` clause rather than silently dropping it, matching the frontend's existing diagnostics for unsupported syntax. Full loop-`else` semantics is the substantial option; the `NotImplementedError` is small.\n\n**Provenance.** The eager AST frontend was introduced/relocated to `tilelang/language/eager/ast.py` no later than #1632 (2026-01-09); `visit_For`/`visit_While` have never emitted `node.orelse` in that file's history, so this is not a regression.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of a dropped loop `else:` clause.\n\n**Reach.** The trigger is a Python `for…else` / `while…else` inside a kernel — it is the standard Python way to express \"do X after the loop unless it exited early via `break`,\" and `break`/`continue` are accepted in kernel loops, so the construct is expressible and meaningful here. That said, loop `else:` is an obscure Python feature most users never write (and often do not know exists), so the trigger is rare in practice. It is not documented as supported by TileLang, and no shipped test exercises a loop `else:` inside a kernel, so nothing in-repo trips it. The silent drop affects any loop `else:` regardless of body contents.\n\n**Impact.** The trigger is narrow: it needs a loop `else:` clause written inside a kernel, an undocumented, untested construct that no shipped code exercises. But when it fires the failure is silent and deterministic — no error, and the entire `else:` body is dropped, so whatever it computes (here a +100 over the whole output region) simply never happens; the wrong result is the same on every run, so it can slip into a workload undetected. Fixing it either honors the construct's loop-`else` semantics or converts it into a clear compile-time rejection, closing a silent-miscompile hole in the eager frontend's handling of a valid Python control-flow form.", "url": "https://github.com/tile-ai/tilelang/issues/2946"}
{"number": 3071, "title": "Silent all-zeros miscompilation of mamba-ssm's Mamba-3 MIMO kernel on GB10 (sm_121) — depends on JIT compile order, reproduces on 0.1.8 and 0.1.13", "state": "closed", "created_at": "2026-08-23T04:43:10Z", "updated_at": "2026-09-03T00:59:19Z", "closed_at": "2026-08-27T02:08:52Z", "labels": [], "comments_count": 5, "is_pr": false, "body": "## Environment\n\n- GPU: NVIDIA GB10 (DGX Spark, sm_121), aarch64\n- CUDA 13.0, torch 2.11.0+cu130, Python 3.12\n- tilelang: reproduces on **0.1.8 and 0.1.13** (both from PyPI aarch64 wheels)\n- Kernel: `mamba_ssm/ops/tilelang/mamba3/mamba3_mimo` (`mamba_mimo_fwd_kernel`), via\n  mamba-ssm @ `e9594ce` (git main 2026-08-22)\n- Model: official `state-spaces/mamba3-mimo-1.5b` (bf16, `mimo_rank: 4`)\n\n## Summary\n\nThe compiled `mamba_mimo_fwd_kernel` produces **all-zero outputs** (downstream: logits\nwhose argmax is the \"0\" token forever) for a specific input shape — but **only when that\nshape is the first kernel compiled in a fresh process**. The same shape compiled after\nother shapes in the same process produces correct output. No error or warning is raised:\nthe kernel compiles \"successfully\" and silently returns zeros.\n\n## Repro (fails: first compile in fresh process)\n\n```python\nimport torch\nfrom mamba_ssm.models.mixer_seq_simple import MambaLMHeadModel\nfrom transformers import AutoTokenizer\n\ntok = AutoTokenizer.from_pretrained(\"NousResearch/Meta-Llama-3-8B\")\nmodel = MambaLMHeadModel.from_pretrained(\n    \"state-spaces/mamba3-mimo-1.5b\", device=\"cuda\", dtype=torch.bfloat16).eval()\nprompt = (\"Inventory record: the component serial number is CN-4821, rated 375 watts, \"\n          \"installed 2024-11-03. The serial number of that component is\")\nids = tok(prompt, return_tensors=\"pt\").input_ids.to(\"cuda\")   # seqlen 35\nwith torch.inference_mode():\n    for _ in range(4):\n        ids = torch.cat([ids, model(ids).logits[:, -1].argmax(-1, keepdim=True)], dim=1)\nprint(tok.decode(ids[0][-4:]))   # -> \" 0000\"   (expected: \" CN-4821…\", which SISO produces)\n```\n\nObserved in 3 independent fresh-process runs (two on 0.1.8, one on 0.1.13):\noutput is `\" 000000000…\"` — the model deterministically emits the `0` token.\n\n## Control (passes: same shape, compiled after others)\n\nSweeping seqlens 24→71 of the same text in one process (so seqlen 35 is compiled ~10th),\nthe seqlen-35 forward is **healthy**: logits std ≈ 2.0, argmax matches the SISO\ncheckpoint's, generation recalls \"CN-4821\" correctly. All 48 shapes in the sweep pass.\nSo the miscompilation is a function of JIT/compile-context state, not of the shape itself.\n\n- Kernel logs in the failing runs show a normal `TileLang begins/completes to compile\n  kernel 'mamba_mimo_fwd_kernel'` pair — nothing distinguishes the bad compile.\n- The SISO checkpoint (triton path, no tilelang) is correct on identical inputs, ruling\n  out weights/tokenizer/driver issues.\n\n## Secondary pain point\n\nEach new sequence length triggers a full ~7-8 s JIT recompile of\n`mamba_mimo_fwd_kernel` (0.1.13 included — the executable reuse added there appears to\napply per-shape only). Token-by-token generation therefore costs ~8 s/token. If there is\na supported way to compile shape-generic / symbolic-seqlen kernels, a pointer would be\nappreciated; otherwise consider this a feature request that would matter a lot for\nautoregressive SSM decode.\n\nPossibly related: #2201 (Mamba-3 MIMO on sm_120: shared-memory overflow, perf issues).\nA companion issue on the mamba-ssm side (their Mamba-3 `step()` path) is filed at\nstate-spaces/mamba#1022 (https://github.com/state-spaces/mamba/issues/1022) — happy to cross-link, provide cache dumps, generated CUDA source\nfrom good vs bad compiles, or run patches on this hardware.\n", "url": "https://github.com/tile-ai/tilelang/issues/3071"}
{"number": 2964, "title": "[BUG][wrong-code] A shared `T.gemm` operand sliced along MN silently reads the wrong K panel when `block_K` spans more than one swizzle atom", "state": "closed", "created_at": "2026-08-14T05:51:40Z", "updated_at": "2026-09-02T10:13:40Z", "closed_at": "2026-09-02T10:13:40Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "## Summary\n\nPassing a shared buffer **sliced along MN** as a `T.gemm` operand silently computes the wrong result whenever `block_K` spans more than one swizzle atom. No warning, no assertion, no illegal access — the kernel reads real data from the wrong place, so it just returns wrong numbers.\n\nThe same GEMM written with one shared buffer per slice is correct, which makes the slice/no-slice pair a clean A/B.\n\n## Reproduction\n\n`main@6af825e0`, H200 (sm_90a), CUDA 13.2, torch 2.13.\n\n```python\nimport torch\n\nimport tilelang\nimport tilelang.language as T\n\nM, N, K = 64, 128, 128\nBM, BN, BK, W0 = 64, 128, 128, 64  # split BN into W0 + (BN - W0) = 64 + 64\n\n\ndef build(sliced: bool):\n    @T.prim_func\n    def main(\n        a: T.Tensor((M, K), \"bfloat16\"),\n        b: T.Tensor((N, K), \"bfloat16\"),\n        c: T.Tensor((M, N), \"bfloat16\"),\n    ):\n        with T.Kernel(T.ceildiv(N, BN), T.ceildiv(M, BM), threads=128) as (bx, by):\n            a_s = T.alloc_shared((BM, BK), \"bfloat16\")\n            c0 = T.alloc_fragment((BM, W0), \"float32\")\n            c1 = T.alloc_fragment((BM, BN - W0), \"float32\")\n            T.clear(c0)\n            T.clear(c1)\n            if sliced:\n                # One shared buffer, consumed as two row slices.\n                b_s = T.alloc_shared((BN, BK), \"bfloat16\")\n            else:\n                # Same math, one buffer per slice.\n                b_s0 = T.alloc_shared((W0, BK), \"bfloat16\")\n                b_s1 = T.alloc_shared((BN - W0, BK), \"bfloat16\")\n            for ko in T.Pipelined(T.ceildiv(K, BK), num_stages=0):\n                T.copy(a[by * BM, ko * BK], a_s)\n                if sliced:\n                    T.copy(b[bx * BN, ko * BK], b_s)\n                    T.gemm(a_s, b_s[0:W0, :], c0, transpose_B=True)\n                    T.gemm(a_s, b_s[W0:BN, :], c1, transpose_B=True)\n                else:\n                    T.copy(b[bx * BN, ko * BK], b_s0)\n                    T.copy(b[bx * BN + W0, ko * BK], b_s1)\n                    T.gemm(a_s, b_s0, c0, transpose_B=True)\n                    T.gemm(a_s, b_s1, c1, transpose_B=True)\n            T.copy(c0, c[by * BM, bx * BN])\n            T.copy(c1, c[by * BM, bx * BN + W0])\n\n    return tilelang.compile(main, out_idx=[2], target=\"cuda\")\n\n\ntorch.manual_seed(0)\na = torch.randn(M, K, device=\"cuda\", dtype=torch.bfloat16)\nb = torch.randn(N, K, device=\"cuda\", dtype=torch.bfloat16)\nref = a.float() @ b.float().T\n\nfor sliced in (False, True):\n    err = (build(sliced)(a, b).float() - ref).abs().max().item()\n    print(f\"{'one buffer, two slices' if sliced else 'two buffers      ':22s}  max_err = {err:.4f}\")\n```\n\n```\ntwo buffers             max_err = 0.1248     <- bf16 accumulation noise\none buffer, two slices  max_err = 43.3982    <- wrong\n```\n\n## Scope\n\nBoth operands and both slice axes, `block_M = block_N = 128`, slicing 64 + 64:\n\n| operand | `block_K=64` | `block_K=128` | `block_K=256` |\n|---|---|---|---|\n| K-major A (`A_s[0:64, :]`) | ok | **err ≈ 113** | **err ≈ 121** |\n| K-major B (`B_s[0:64, :]`, `transpose_B=True`) | ok | **err ≈ 101** | **err ≈ 107** |\n| MN-major A (`A_s[:, 0:64]`, `transpose_A=True`) | ok | ok | ok |\n| MN-major B (`B_s[:, 0:64]`) | ok | ok | ok |\n\nIndependent of `threads`, `num_stages`, warp specialization, and dtype (fp16 and bf16 both). MN-major operands are unaffected — that branch of the offset formula does not use the term below.\n\n`block_K = 64` is correct only by accident: at bf16 that is exactly one 128B swizzle atom, so the buggy term is multiplied by zero (see below).\n\n## Root cause\n\n`tilelang/cuda/intrinsics/macro/wgmma_macro_generator.py`, `wgmma_ss_atom` / `wgmma_rs_atom`:\n\n```python\nB_offset = (ki // bk_atom_size) * n_dim * b_swizzle_atom_elems + ...\n```\n\nWhen `block_K` exceeds one swizzle atom, the shared layout splits the K axis into panels and stores them **panel-major**. A `(128, 128)` bf16 tile decodes as\n\n```\nSw<3,3,3> o 0 o (128,(64,2)):(64,(1,8192))\n                              ^^^^ panel stride = 128 rows x 64 elems\n```\n\nso advancing to the next K panel steps by **the buffer's** MN extent. The formula instead reconstructs that step from `n_dim`, the extent of *this operand*. The two coincide only when the operand covers the whole buffer; for a slice `n_dim` shrinks while the panel spacing does not.\n\nConcretely, for the repro above (bf16, 64-element atom, `bk_atom_size = 4`, so `ki // 4` selects the panel):\n\n| operand | slice origin (correct) | `ki >= 4` should read | actually reads |\n|---|---|---|---|\n| `b_s[0:64, :]` | elem 0 | 8192 = row 0, panel 1 | 4096 = **row 64, panel 0** |\n| `b_s[64:128, :]` | 4096 ✓ | 12288 = row 64, panel 1 | 8192 = **row 0, panel 1** |\n\nEmitted CUDA — the panel step is 8192 bytes where it should be 16384:\n\n```cpp\ntl::initialize_wgmma_descriptor<1, 1, 64>(desc_b_1, (char*)&b_s[0] + 8192);  // origin: correct\ntl::wgmma_ss<...>(..., desc_b_1 + (((ki >> 2) * 8192 + (ki & 3) * 32) >> 4), ...);\n//                                            ^^^^ should be 16384\n```\n\nTwo consequences worth calling out:\n\n- The **slice origin is correct** (`slice_byte_offset` is derived from the layout). Only the panel step is wrong, so the first K atom of every operand is right and everything past it is wrong — the error is a partial-garbage mix rather than an obvious blow-up.\n- The two slices read **each other's rows**, all within the buffer, so there is no out-of-bounds signal.\n\nThe same expression is duplicated in five more places, all with the same defect:\n\n- `wgmma_sp_macro_generator.py` — sparse Hopper, `wgmma_ss` and `wgmma_rs`\n- `tcgen05_macro_generator.py` — Blackwell, `tcgen05_ss_atom`, `tcgen05_ts_atom`, `tcgen05_blockscaled_atom`\n\n## Why nothing caught it\n\n`tilelang/language/gemm_op.py` used to assert `A_offset[-2] == 0`, i.e. reject an operand with a non-zero offset on the second-to-last axis. That assertion was dropped when the region-based lowering landed, on the premise that \"every lowering consumes the complete operand BufferRegions\". That is half true: the *origin* is consumed from the region correctly, but the *panel stride* is still reconstructed from emitter-level tile dims. The guard was removed and no equivalent check replaced it.\n\nExisting coverage misses it on both backends:\n\n- `test_tilelang_kernel_gemm_with_stride.py` does slice a shared operand, but at `block_K = 32` (single atom) and on the pre-Hopper `mma` path.\n- `test_tilelang_language_tcgen05_sliced_operands.py` slices only along **K**, which is exactly the axis where the panel term is unused. MN slicing is validated for atom alignment but never numerically.\n\n## Environment\n\n- tilelang `main@6af825e0`\n- H200 (sm_90a), nvcc 13.2, driver 595.71.05, torch 2.13.0+cu130\n- Also reproduces with `TL_DISABLE_WARP_SPECIALIZED` both on and off\n\nI have a fix and will open a PR shortly.\n", "url": "https://github.com/tile-ai/tilelang/issues/2964"}
{"number": 2997, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.fill`/`T.clear` of an `int4` buffer crashes the compiler with `broadcast_var is not a constant` instead of emitting the fill", "state": "closed", "created_at": "2026-08-16T06:20:02Z", "updated_at": "2026-09-02T08:45:15Z", "closed_at": "2026-09-02T08:45:15Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\n- TileLang 0.1.13, Python 3.13, Linux.\n- GPU: NVIDIA L40S (compute capability 8.9, sm_89). The defect is in the CUDA `Broadcast` codegen (`src/cuda/codegen/codegen_cuda.cc`), which runs at code-generation time before any arch-specific lowering, so it is architecture-independent.\n\n### Problem description\n\n`T.fill` (and `T.clear`, which lowers to `T.fill(..., 0)`) on an `int4` buffer aborts at compile time with\n\n```\nInternalError: Check failed: (p) is false: BroadcastNode ... value: broadcast_var is not a constant\n```\n\n`int4` is a supported element dtype (it is the operand dtype of the shipped `examples/gemm_int4` kernel) and `T.fill`/`T.clear` place no dtype restriction on the buffer, so the kernel is legal. The fill lowering broadcasts the fill value across the vectorized store, but binds that value to a temporary (`broadcast_var`) first, so the `Broadcast` node's value is a `Var`, not a constant. The `int4` branch of the CUDA `Broadcast` visitor unconditionally requires a constant value (`ICHECK(p)` on `as_const_int(op->value)`), so it fatal-aborts. The identical kernel on the sibling `int8` dtype compiles, because the `int8` branch only special-cases the constant sub-case and otherwise falls through to the generic (non-constant) path.\n\nThe crash does not depend on `T.copy` or on any GEMM machinery — a bare `T.fill`/`T.clear` of an `int4` shared buffer reproduces it. Not a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\n\nM, N = 16, 64\n\ndef make(dtype):\n    @T.prim_func\n    def func(B: T.Tensor((M, N), dtype)):\n        with T.Kernel(1, threads=128):\n            sh = T.alloc_shared((M, N), dtype)\n            T.fill(sh, 1)          # T.clear(sh) fails identically\n            T.copy(sh, B)\n    return func\n\ndef try_compile(dtype):\n    try:\n        tilelang.compile(make(dtype)).get_kernel_source()\n        return \"compiled\"\n    except Exception as e:\n        return \"CRASH: \" + str(e).splitlines()[-1]\n\nprint(\"int4:\", try_compile(\"int4\"))   # -> CRASH (\"broadcast_var is not a constant\")\nprint(\"int8:\", try_compile(\"int8\"))   # -> compiled (control)\n```\n\nOutput:\n\n```\nint4: CRASH: Check failed: (p) is false: BroadcastNode 0x... value: broadcast_var is not a constant\nint8: compiled\n```\n\n### Traceback\n\n```\ntvm.error.InternalError: Check failed: (p) is false: BroadcastNode 0x... value: broadcast_var is not a constant\n  ...\n  CodeGenTileLangCUDA::VisitStmt_(BufferStoreNode const*)\n  CodeGenC::PrintExpr(PrimExpr const&)\n  CodeGenTileLangCUDA::VisitExpr_(BroadcastNode const*, std::ostream&)\n  tvm::runtime::detail::LogFatalImpl(...)\n```\n\n### Expected behavior\n\nFilling an `int4` buffer with a runtime (non-constant) broadcast value should emit code rather than aborting, matching what the sibling `int8` path already does: the `int8` branch handles the constant case as an optimization and otherwise falls through to the generic `Broadcast` lowering, so a variable fill value is fine there. The `int4` branch could take the same shape — keep the constant fast-path, and fall through for a non-constant value instead of asserting.\n\n### Additional context\n\n**Root cause.** The CUDA `Broadcast` codegen does not handle a non-constant value in its `int4` branch. In [`CodeGenTileLangCUDA::VisitExpr_(BroadcastNode)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L5614-L5618) the `int4` (`bits() == 4`) branch reads `p = as_const_int(op->value)` and immediately `ICHECK(p) << \"... is not a constant\"`, so any broadcast of a `Var` fatal-aborts.\n\nThe `broadcast_var` in the message is *not* something `T.fill` wrote — it is created by the device-lowering pass [`HoistBroadcastValues`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/transform/hoist_broadcast_values.py), which runs unconditionally in `engine/lower.py` (`device_mod = tilelang.transform.HoistBroadcastValues()(device_mod)`). That pass deliberately takes a `Broadcast` of an *immediate constant* (`IntImm`/`FloatImm`) and rebinds the constant to a let-bound `Var` named `broadcast_var`, leaving a `Broadcast(broadcast_var, lanes)` behind. So a fill value that was a constant in the frontend is a `Var` by the time codegen runs, and `as_const_int` returns null. Verified this session: the same crash fires on a *genuinely* runtime fill value (a kernel scalar argument, no hoist pass involved) with the message `value: T.Cast(\"int4\", s) is not a constant` — the `int4` branch cannot emit any non-constant broadcast, hoisted or not.\n\n<details><summary>Why the <code>int8</code> control compiles</summary>\n\nThe [`int8` branch of the same visitor](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L5503-L5525) guards its constant packing with `if (p) { ... }` and returns only inside it; when `p` is null it does not `return`, so control reaches the generic `Broadcast` path lower in the function. The `int4` branch instead asserts on `p` before doing anything, so it has no non-constant fall-through.\n\n</details>\n\n**Suggested fix.** Give the `int4` branch the same structure as the `int8` one: gate the constant packing on `if (p)` and, when the value is not a constant, fall through to the generic `Broadcast` lowering rather than `ICHECK`-aborting.\n\n**Provenance.** The `int4` `Broadcast` branch (with the unconditional `ICHECK(p)`) was introduced in [#2073](https://github.com/tile-ai/tilelang/pull/2073) (\"[CUDA] Improve int4 GEMM lowering and packed codegen support\", 2026-04-22); a later namespace refactor ([#2297](https://github.com/tile-ai/tilelang/pull/2297)) relocated but did not change it. When it was written, a constant fill value still reached codegen as an `IntImm`, so the `ICHECK(p)` held for the fill/clear path. The `HoistBroadcastValues` pass that rebinds those constants to a `Var` was added three weeks later in [#2166](https://github.com/tile-ai/tilelang/pull/2166) (first commit 2026-05-12) and wired into the default device-lowering pipeline — this is what turns the fill value non-constant before it reaches the branch. So the fill/clear crash is best described as a **regression by pass interaction**: neither PR is wrong alone, but the hoist pass now feeds the `int4` branch exactly the non-constant input its `ICHECK` forbids. (The branch was independently already broken for a *truly* runtime broadcast value, which no shipped kernel produces.)\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect.\n\n**Generalization (root + 4-axis sweep, all cells RUN this session on sm_89 / v0.1.13).** Two-level root:\n- **Source-level root:** `CodeGenTileLangCUDA::VisitExpr_(BroadcastNode)`, the `(is_int()||is_uint()) && bits()==4` branch (codegen_cuda.cc:5614–5618). Fragile idiom = *assert-constant before doing anything*, with no non-constant fall-through — versus every other width's *gate on `if(p)` then fall through*. It is the only `as_const_int(op->value)`-gated branch in the whole visitor that `ICHECK`s instead of guarding (the int8 site at 5504 is the safe twin).\n- **Operator-level root:** any store into an `int4` buffer whose stored value lowers to a *vectorized `Broadcast`*. `T.fill`/`T.clear` are the frontend triggers, but so is a raw vectorized assignment of a scalar. Two independent ways to make the value non-constant both hit it: the `HoistBroadcastValues` pass (constant → `Var`) and a genuinely runtime scalar.\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| related-type | `uint4` fill | CRASH `broadcast_var is not a constant` | **same** (shares the 4-bit branch; not signedness-gated) |\n| related-type | `int1` fill | CRASH `Cannot convert type int1x4 to CUDA type!` | **distinct** — falls through to the generic `make_` path, dies in `PrintType` (int1 codegen gap at :2321), not the `ICHECK` |\n| related-type | `float4_e2m1fn` (fp4) fill | compiled | distinct path — fp4 is excluded by `!is_float4()`, routed through the fp8/fp4 branch (:5776); confirms the predicate boundary |\n| related-type | `int8` / `int16` / `int32` fill | compiled | control — 8-bit gates on `if(p)`, wider ints take generic path |\n| related-type | `float16` / `bfloat16` / `float8_e4m3` / `float8_e5m2` / `float32` fill | compiled | control — those branches `PrintExpr` the (Var) value |\n| related-operator | `T.clear(int4)` | CRASH (same msg) | **same** — `clear` = `fill(...,0)` |\n| related-operator | `T.fill(int4, 0)` / `(int4, 7)` | CRASH (same msg) | **same** — value magnitude irrelevant |\n| related-operator | raw vectorized store of a runtime scalar into int4 | CRASH `value: T.Cast(\"int4\", s) is not a constant` | **same** — direct non-constant, no hoist pass; proves the branch is broken for real runtime values too |\n| similar-logic | int8 branch fed a genuinely runtime scalar (the safe twin) | compiled | control — same idiom done with `if(p)` fall-through |\n| related-source | other `as_const_int(op->value)` sites in the visitor | only two exist (int8 :5504 safe, int4 :5616 buggy) | single buggy instance — no other sibling to fix |\n\n**Boundary is narrow and stays specific.** The defect is exactly the one 4-bit integer branch; `int4` and `uint4` are the *same* branch (one fix covers both), so this is not a per-dtype family but a single missing fall-through. Adjacent widths, fp4, and the int8 twin are all clean. Title/Problem kept specific to the 4-bit `Broadcast` branch.\n\n**Distinct-adjacent bug found (not filed).** `T.fill` of an `int1` buffer also aborts, but with a *different* root: `Cannot convert type int1x4 to CUDA type!` in `PrintType` (the int1 codegen at codegen_cuda.cc:2321 area), reached via the generic `make_` fall-through — not the `ICHECK(p)`. Different mechanism, different fix; recorded here, not filed.\n\n**Impact.** The trigger is narrow: it needs a broadcast store into an `int4` buffer, and no shipped kernel does this (the one int4-buffer example, `gemm_int4`, only clears its `int32` accumulator — verified by running it). Any `T.fill`/`T.clear` of an `int4` buffer fires it, because `HoistBroadcastValues` makes even a constant fill value non-constant before codegen; a genuinely runtime broadcast store fires it too. When it fires it is a compile-time `ICHECK` abort — loud and caught immediately when building that kernel; nothing is silently miscompiled and no wrong value can reach a run. Fixing it closes a dtype-parity gap so a legal `T.fill`/`T.clear` on `int4` compiles the way it already does on `int8`, rather than refusing to build.\n\n**Reach.** (Example run this session, v0.1.13 / sm_89.) The only shipped example that allocates real `T.int4` buffers is `examples/gemm_int4/example_tilelang_gemm_int4.py` — `A_shared`/`B_shared` are `T.alloc_shared(..., T.int4)`. I ran that example verbatim (its default `check_int4_gemm_correctness()` config): it **compiles and prints `Correctness check passed.`** It dodges this bug because it only `T.clear`s the `int32` accumulator `C_local` (line 29) and loads the `int4` operands via `T.copy`, never filling the `int4` buffers themselves — so no shipped kernel fills an `int4` buffer, which is why CI is green. Adding a single `T.clear(A_shared)` to that example-shaped kernel reproduces the crash (ran this session → `broadcast_var is not a constant`). Correction to an earlier claim in this draft: the `examples/dequantize_gemm/` kernels do **not** use `int4` buffers — they carry the 4-bit weights in a packed `storage_dtype` (int8/int32) and dequantize into an `in_dtype` (fp16/bf16) buffer, so none of them allocate or fill a `T.int4` buffer or exercise this branch. `gemm_int4` is the sole shipped int4-buffer site.", "url": "https://github.com/tile-ai/tilelang/issues/2997"}
{"number": 2267, "title": "[CI] Windows wheel build is much slower as ccache is not effectively used", "state": "closed", "created_at": "2026-05-25T09:34:15Z", "updated_at": "2026-09-02T07:36:31Z", "closed_at": "2026-09-02T07:36:31Z", "labels": ["enhancement", "help wanted", "windows"], "comments_count": 2, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Motivation\n\n## Problem\n\nThe Windows dist/wheel build in the `Dist` workflow takes significantly longer  than the other platforms.\n\nExample from run:\nhttps://github.com/tile-ai/tilelang/actions/runs/26380636948\n\nBuild wheel step duration:\n\n- macOS Metal: ~2m13s\n- Linux CUDA x86_64: ~5m02s\n- Linux CUDA arm64: ~5m01s\n- Windows CUDA 13.0: ~39m54s\n\n\n### Solution\n\n_No response_\n\n### Alternatives\n\n_No response_\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/2267"}
{"number": 3120, "title": "[Perf] Lower T.any_of / T.all_of via warp vote intrinsics (__any_sync / __all_sync) where safe", "state": "open", "created_at": "2026-08-31T03:46:43Z", "updated_at": "2026-09-01T04:31:50Z", "closed_at": null, "labels": ["enhancement", "good first issue"], "comments_count": 2, "is_pr": false, "body": "## Current behavior\n\n`T.any_of(buffer)` / `T.all_of(buffer)` (`tilelang/language/logical.py`) lower through the `tl.any_of` / `tl.all_of` builtins (`src/op/logical.cc`) to `call_extern` of the device helpers `tl::Any` / `tl::All` (`src/tl_templates/cuda/common.h:846-863`, HIP equivalents at `src/tl_templates/hip/common.h:304-322`):\n\n```cpp\ntemplate <typename T> TL_DEVICE bool Any(T *a, int size) {\n  for (int i = 0; i < size; i++)\n    if (a[i]) return true;\n  return false;\n}\n```\n\nEvery thread scans the buffer serially and independently. Two costs:\n\n- **Shared buffers:** all 32 lanes of a warp redundantly read all `size` elements — 32x redundant shared-memory traffic per warp, and the loop is O(size) per thread.\n- **Fragments:** each thread reduces only its own local slice, so callers that want a tile-wide / warp-uniform answer still need a manual cross-thread combine (typically a shared-memory round-trip).\n\n## Proposal\n\nUse the warp vote intrinsics that #1858 already wired end-to-end (`tl.any_sync` / `tl.all_sync` → `__any_sync` / `__all_sync` on CUDA at `src/cuda/codegen/codegen_cuda.cc:4492`, `__any` / `__all` on HIP at `src/rocm/codegen/codegen_hip.cc:1589`):\n\n**1. Shared/global buffers — cooperative warp-strided scan + one vote:**\n\n```cpp\ntemplate <typename T> TL_DEVICE bool AnyWarp(T *a, int size, uint32_t mask = 0xffffffff) {\n  bool local = false;\n  for (int i = (threadIdx.x % 32); i < size; i += 32)\n    local |= static_cast<bool>(a[i]);\n  return __any_sync(mask, local);   // AllWarp: &= + __all_sync\n}\n```\n\nPer-thread reads drop by the warp size (32x on CUDA, 64x on CDNA wavefronts), and the result is warp-uniform.\n\n**2. Fragments — fold the warp with one vote:** keep the per-thread local scan, then `__any_sync` combines the 32 partials in a single instruction, replacing the shared-memory staging a tile-wide reduction needs today.\n\n## Why this cannot be a blanket replacement\n\n- **Convergence:** `__*_sync` requires every lane in the mask to reach the instruction. The current `tl::Any` is purely thread-local and is legal in divergent control flow (e.g. under `if (tid == 0)`); a vote at such a call site is UB on Volta+. The lowering must either prove the call site warp-uniform or keep the cooperative path opt-in.\n- **Fragment semantics change** from \"per-thread partial over the local slice\" to \"warp-uniform\" — this must be a new variant, not an in-place swap.\n- **Short-circuiting:** `all_of` currently early-exits on the first false; each lane can still break within its own stride, and a chunked ballot-based early exit can recover most of the rest.\n\n## Suggested implementation plan\n\n1. Add `tl::AnyWarp` / `tl::AllWarp` device templates to `src/tl_templates/cuda/common.h` and the HIP equivalents (`__any`/`__all`, wavefront-width stride) to `src/tl_templates/hip/common.h`.\n2. First step (conservative): an explicit opt-in, e.g. `T.any_of(buf, scope=\"warp\")`, that selects the cooperative lowering in the `FLowerIntrinsic` hooks in `src/op/logical.cc`, leaving convergence responsibility with the caller.\n3. Follow-up: dispatch automatically when the buffer scope is shared and the call site is provably warp-uniform (outside thread-divergent control flow in the tile-op body).\n4. Tests alongside the existing `testing/python/language/test_tilelang_language_any_of.py` / `test_tilelang_language_all_of.py`: numerical equivalence vs. the serial path for shared and fragment buffers on CUDA and HIP, plus a documented-constraint test for divergent call sites.\n", "url": "https://github.com/tile-ai/tilelang/issues/3120"}
{"number": 3042, "title": "[BUG] 屏障误删导致的共享内存的读写冲突", "state": "closed", "created_at": "2026-08-17T06:49:28Z", "updated_at": "2026-08-31T17:41:10Z", "closed_at": "2026-08-31T17:41:10Z", "labels": ["bug"], "comments_count": 3, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.12\n\n### System information\n\nroot@s-113-2-32:/mnt/nas/home/jianpeng.zhu/project/vllm_torch_op/150s/git/tilelang# python3 -m torch.utils.collect_env\n<frozen runpy>:128: RuntimeWarning: 'torch.utils.collect_env' found in sys.modules after import of package 'torch.utils', but prior to execution of 'torch.utils.collect_env'; this may result in unpredictable behaviour\nCollecting environment information...\nPyTorch version: 2.10.0\nIs debug build: False\nCUDA used to build PyTorch: 10.2\nROCM used to build PyTorch: N/A\n\nOS: Ubuntu 24.04.2 LTS (x86_64)\nGCC version: (Ubuntu 13.3.0-6ubuntu2~24.04) 13.3.0\nClang version: 22.1.0git (4.5.0.20260721 7b070f4f757e4199bcefb8b8e743b4000b81e366)\nCMake version: version 4.4.0\nLibc version: glibc-2.39\n\nPython version: 3.12.11 (main, Jul 31 2025, 06:45:11) [GCC 13.3.0] (64-bit runtime)\nPython platform: Linux-5.4.0-42-generic-x86_64-with-glibc2.39\nIs CUDA available: True\nCUDA runtime version: 10.2.89\nCUDA_MODULE_LOADING set to: \nGPU models and configuration: Could not collect\nNvidia driver version: Could not collect\ncuDNN version: Could not collect\nIs XPU available: False\nHIP runtime version: N/A\nMIOpen runtime version: N/A\nIs XNNPACK available: True\nCaching allocator config: N/A\n\nCPU:\nArchitecture:                    x86_64\nCPU op-mode(s):                  32-bit, 64-bit\nAddress sizes:                   46 bits physical, 57 bits virtual\nByte Order:                      Little Endian\nCPU(s):                          160\nOn-line CPU(s) list:             0-159\nVendor ID:                       GenuineIntel\nBIOS Vendor ID:                  Intel(R) Corporation\nModel name:                      Intel(R) Xeon(R) Platinum 8380 CPU @ 2.30GHz\nBIOS Model name:                 Intel(R) Xeon(R) Platinum 8380 CPU @ 2.30GHz  CPU @ 2.3GHz\nBIOS CPU family:                 179\nCPU family:                      6\nModel:                           106\nThread(s) per core:              2\nCore(s) per socket:              40\nSocket(s):                       2\nStepping:                        6\nCPU(s) scaling MHz:              24%\nCPU max MHz:                     3400.0000\nCPU min MHz:                     800.0000\nBogoMIPS:                        4600.00\nFlags:                           fpu vme de pse tsc msr pae mce cx8 apic sep mtrr pge mca cmov pat pse36 clflush dts acpi mmx fxsr sse sse2 ss ht tm pbe syscall nx pdpe1gb rdtscp lm constant_tsc art arch_perfmon pebs bts rep_good nopl xtopology nonstop_tsc cpuid aperfmperf pni pclmulqdq dtes64 monitor ds_cpl vmx smx est tm2 ssse3 sdbg fma cx16 xtpr pdcm pcid dca sse4_1 sse4_2 x2apic movbe popcnt tsc_deadline_timer aes xsave avx f16c rdrand lahf_lm abm3dnowprefetch cpuid_fault epb cat_l3 invpcid_single ssbd mba ibrs ibpb stibp ibrs_enhanced tpr_shadow vnmi flexpriority ept vpid ept_ad fsgsbase tsc_adjust bmi1 avx2 smep bmi2 erms invpcid cqm rdt_a avx512f avx512dq rdseed adx smap avx512ifma clflushopt clwb intel_pt avx512cd sha_ni avx512bw avx512vl xsaveopt xsavec xgetbv1 xsaves cqm_llc cqm_occup_llc cqm_mbm_total cqm_mbm_local wbnoinvd dtherm ida arat pln pts hwp hwp_act_window hwp_epp hwp_pkg_req avx512vbmi umip pku ospke avx512_vbmi2 gfni vaes vpclmulqdq avx512_vnni avx512_bitalg tme avx512_vpopcntdq rdpid md_clear pconfig flush_l1d arch_capabilities\nVirtualization:                  VT-x\nL1d cache:                       3.8 MiB (80 instances)\nL1i cache:                       2.5 MiB (80 instances)\nL2 cache:                        100 MiB (80 instances)\nL3 cache:                        120 MiB (2 instances)\nNUMA node(s):                    2\nNUMA node0 CPU(s):               0-39,80-119\nNUMA node1 CPU(s):               40-79,120-159\nVulnerability Itlb multihit:     Not affected\nVulnerability L1tf:              Not affected\nVulnerability Mds:               Not affected\nVulnerability Meltdown:          Not affected\nVulnerability Spec store bypass: Mitigation; Speculative Store Bypass disabled via prctl and seccomp\nVulnerability Spectre v1:        Mitigation; usercopy/swapgs barriers and __user pointer sanitization\nVulnerability Spectre v2:        Mitigation; Enhanced IBRS, IBPB conditional, RSB filling\nVulnerability Srbds:             Not affected\nVulnerability Tsx async abort:   Not affected\n\nVersions of relevant libraries:\n[pip3] numpy==1.26.4\n[pip3] onnx==1.22.0\n[pip3] onnxruntime-gpu==1.17.1+corex.4.5.0.20260721\n[pip3] torch==2.10.0+corex.4.5.0.20260721\n[pip3] torch_c_dlpack_ext==0.1.5\n[pip3] torch_cluster==1.6.0+corex.4.5.0.20260721\n[pip3] torch_quiver==0.1.0+corex.4.5.0.20260721\n[pip3] torch_scatter==2.1.0+corex.4.5.0.20260721\n[pip3] torch_sparse==0.6.16+corex.4.5.0.20260721\n[pip3] torchaudio==2.10.0+corex.4.5.0.20260721\n[pip3] torchdebug==4.4.0+corex.4.5.0.20260721\n[pip3] torchvision==0.25.0+corex.4.5.0.20260721\n[pip3] triton==3.2.0+corex.4.5.0.20260721\n\n### Problem description\n\n因为线程发散走到这个分支，这边的代码仅对then分支进行了是否删除屏障的判断，就将then和else的屏障都删除了，但else中如果存在共享内存的读写操作，会因此导致读写冲突\n\n\n### Reproducible example code\n\nThe Python snippets:\n\n```python\n\n```\n\n\n### Traceback\n\n```pytb\n\n```\n\n### Expected behavior\n\n_No response_\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/3042"}
{"number": 3026, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.loop_break()` inside `T.unroll(explicit=True)` emits bare `break;` outside any loop, so nvcc rejects the kernel instead of it compiling", "state": "closed", "created_at": "2026-08-17T03:16:52Z", "updated_at": "2026-08-31T13:38:57Z", "closed_at": "2026-08-31T13:38:57Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13; CUDA 13.0 (nvcc), PyTorch 2.13.0+cu130; NVIDIA L40S (sm_89).\n\n### Problem description\n\nA `T.loop_break()` inside a `for i in T.unroll(N, explicit=True)` loop makes the kernel fail to compile: nvcc reports `N` copies of `error: a break statement may only be used within a loop or switch`.\n\n`explicit=True` fully expands the loop body into `N` inline copies with **no surrounding loop**. The body contains a `T.loop_break()`, which codegen lowers verbatim to `break;`, so each of the `N` unrolled copies emits a bare `break;` at kernel scope. Both `T.unroll(..., explicit=True)` and `T.loop_break()` are documented, supported primitives, so this is legal input that fails to compile.\n\nThe same kernel with `explicit=False` (the default) keeps the surrounding `for` loop and compiles, giving a working control in the repro below.\n\nNot a regression in the sense of \"used to work\": the explicit-unroll full-expansion path has behaved this way since it was added (see Provenance).\n\n<details><summary>Emitted CUDA (N=8): 8 body copies, each with a bare <code>break;</code>, no enclosing loop</summary>\n\n```cuda\nextern \"C\" __global__ void bug_kernel(const int* __restrict__ A, int* __restrict__ C) {\n  C[0] = A[0];\n  if (5 < A[0]) {\n    break;\n  }\n  C[1] = A[1];\n  if (5 < A[1]) {\n    break;\n  }\n  // ... 6 more identical copies ...\n  C[7] = A[7];\n  if (5 < A[7]) {\n    break;\n  }\n}\n```\n\nnvcc: `8 errors detected` — one `a break statement may only be used within a loop or switch` per copy.\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\n\nN = 8\n\n# BUG: explicit=True fully expands the body into N inline copies with NO surrounding\n# loop; the T.loop_break() in each copy becomes a bare `break;` -> nvcc rejects it.\n@T.prim_func\ndef bug(A: T.Tensor((N,), \"int32\"), C: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for i in T.unroll(N, explicit=True):\n            C[i] = A[i]\n            if A[i] > 5:\n                T.loop_break()\n\n# CONTROL: explicit=False keeps the surrounding for-loop, so the break is inside a\n# loop and the kernel compiles.\n@T.prim_func\ndef ok(A: T.Tensor((N,), \"int32\"), C: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=1):\n        for i in T.unroll(N, explicit=False):\n            C[i] = A[i]\n            if A[i] > 5:\n                T.loop_break()\n\ntilelang.compile(ok, out_idx=[1])    # -> compiles\ntilelang.compile(bug, out_idx=[1])   # -> RuntimeError: 8 errors, \"a break statement may only be used within a loop or switch\"\n```\n\n### Traceback\n\n<details><summary>nvcc error (truncated to the first two of 8 copies)</summary>\n\n```\ntvm_kernels.cu(21): error: a break statement may only be used within a loop or switch\n      break;\ntvm_kernels.cu(25): error: a break statement may only be used within a loop or switch\n      break;\n...\n8 errors detected in the compilation of \"tvm_kernels.cu\".\n```\n</details>\n\n### Expected behavior\n\nThe kernel should compile. `T.loop_break()` is documented (\"Break from current loop\") and `T.unroll(explicit=True)` is a documented, typed option, so their combination should either preserve the exit semantics or be rejected cleanly at compile time rather than emitting a `break;` that nvcc rejects. The `explicit=False` path already handles the same source correctly by keeping the loop.\n\n### Additional context\n\n**Root cause.** The `UnrollLoop` pass does not account for a `loop_break` in the body when it fully expands an explicit-unrolled loop, so it emits a `break` with no enclosing loop. Concretely, [`LoopUnroller::Unroll`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/unroll_loop.cc#L280-L298) substitutes the loop variable and returns `SeqStmt::Flatten(unrolled)` — a flat sequence of `N` body copies with the `For` node discarded. This path is taken for explicit unroll ([unroll_loop.cc#L211-L214](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/unroll_loop.cc#L211-L214)). CUDA codegen then lowers the `tl.loop_break` intrinsic unconditionally to `break;` ([codegen_cuda.cc#L2912-L2914](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L2912-L2914)), so each copy carries a bare `break;`.\n\n**Suggested fix.** One direction: detect a `loop_break` (or any early-exit intrinsic) in the body and decline explicit full-expansion for that loop — fall back to the pragma-unroll form that keeps the `For`, as `explicit=False` already does. Alternatively, reject the combination at frontend/lowering time with a clear message. Which is appropriate depends on whether explicit unroll is meant to support early exit at all — a call for the maintainers.\n\n**Provenance.** The `SeqStmt::Flatten` full-expansion in the explicit-unroll path has been present at least since [#1654](https://github.com/tile-ai/tilelang/pull/1654) (merged 2026-01-12); the `tl.loop_break` → `break;` codegen since [#563](https://github.com/tile-ai/tilelang/pull/563) (merged 2025-06-11). The interaction has been broken no later than #1654; origin before that is unverified. No review bot flagged it.\n\n**Dedup.** Searched the open and closed tracker (`loop_break`, `unroll break`, `explicit unroll`). Distinct from [#2608](https://github.com/tile-ai/tilelang/issues/2608) (`break`/`T.loop_break()` inside `T.vectorized`): that concerns a different pass (VectorizeLoop). On 0.1.13 the vectorized case does not hit this defect at all — a `loop_break` in the body blocks vectorization, so the loop falls back to serial and the `break;` stays inside a real `for` loop and compiles (verified locally, see Reach). This report is the `UnrollLoop` explicit-expansion path, which discards the `For` entirely and produces a hard nvcc compile error. No other report of this defect.\n\n**Reach.** `T.loop_break()` is documented at [`docs/programming_guides/instructions.md`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/docs/programming_guides/instructions.md?plain=1#L231) but demonstrated in no `examples/`/`testing/` file (`grep`: 0 sites outside the language definitions). `T.unroll(explicit=True)` is a documented, typed keyword (`explicit: bool = False` at [`loop.py#L240`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/loop.py?plain=1#L240)); `T.unroll` appears in 7 example files but none pass `explicit=True` (`grep`: 0 `explicit=True` sites in `examples/` or `testing/`). The bug is the intersection of these two documented-but-rarely-combined ingredients; it fires on any constant trip count `N` (each of the `N` copies emits a `break;`). No test exercises `loop_break` under explicit unroll, which is why CI is green.\n\nThe same `T.loop_break()` in the three adjacent loop forms all compiled on 0.1.13, isolating the defect to the explicit-unroll expansion (verified this session):\n\n| loop form (same body) | result |\n|---|---|\n| `T.unroll(N, explicit=True)` | **fails** — `N` bare `break;`, nvcc rejects (this bug) |\n| `T.unroll(N, explicit=False)` (default) | compiles — keeps the `for`, `break;` is inside it |\n| `T.serial(N)` | compiles — real loop, `break;` inside it |\n| `T.vectorized(N)` | compiles — `loop_break` blocks the vectorization plan, falls back to a serial `for` (a warning is emitted), `break;` stays inside it |\n\nOnly the explicit-unroll path drops the enclosing `For` (via `SeqStmt::Flatten`), which is what strands the `break;`.\n\n**Generalization (verified this session, 0.1.13, sm_89).**\n\n*Two-level root.* (a) SOURCE-level: `LoopUnroller::Unroll` in `src/transform/unroll_loop.cc` (L280-298) full-expands the loop body with `SeqStmt::Flatten` and discards the `For`, with no check for a `loop_break` in the body; the sole raw loop-control emitter is CUDA codegen `tl::loop_break()` → `break;` (`codegen_cuda.cc` L2912-2914). (b) OPERATOR-level: any `For` that reaches the full-expansion `Unroll()` call site (explicit unroll, and the extent-1 auto branch at L211-213) combined with a `loop_break` that targets *that* loop.\n\n`loop_break` is the ONLY intrinsic that codegens to a bare loop-control statement — there is no `continue`/`return` sibling intrinsic (grep of codegen: `break;` is the only such emit), so the related-operator \"sibling raw-stmt intrinsic\" axis has no other cell to test.\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| related-source | extent-1 explicit unroll (`N=1`) | **FAILED**, 1 stranded `break;` — the `auto_max_extent_==1` branch also calls `Unroll()` | yes |\n| related-type (knob) | non-unit `start=2, stop=16, step=3` | **FAILED**, 5 stranded `break;` (= ceil((16−2)/3)) | yes |\n| related-source | `break` belongs to the outer explicit-unroll loop, placed after an inner `T.serial` loop | **FAILED**, 4 stranded `break;` — the outer `For` is discarded; the surviving inner loop does not rescue it | yes |\n| similar-logic | inner `break` inside an inner `T.serial` under an outer explicit unroll (break targets the kept inner loop) | COMPILED, 4 `break;` each inside its own real `for` | boundary — break must target the *expanded* loop |\n| related-operator (loop form) | `unroll_factor=N` (explicit=False) | COMPILED, keeps the `for` (pragma unroll, not full-expansion) | distinct path |\n| related-operator (loop form) | `T.Pipelined(N, num_stages=1)` | COMPILED, keeps the `for` | distinct path |\n| related-operator (loop form) | `T.serial(N)` | COMPILED (real loop) | distinct path |\n| related-operator (loop form) | `T.vectorized(N)` | COMPILED — `loop_break` blocks the vectorization plan, falls back to a serial `for` | distinct path |\n\nNet: the defect is exactly the `Unroll()` full-expansion path (explicit unroll of any constant trip count, including the extent-1 auto branch), fired whenever a `loop_break` targets the loop being expanded. Non-unit start/step and extent-1 all reproduce (same root); every non-expanding loop form and a break that targets a *kept* inner loop are clean. The boundary is narrow but the class is \"loop_break targeting a fully-expanded loop\", not literally \"explicit=True with default start\". No adjacent distinct bug was found while sweeping; no new bug found.\n\n**Example-run (PART 1).** There is no shipped example to run verbatim: `T.loop_break` appears in NO `examples/` or `testing/` file on 0.1.13 (`git grep loop_break v0.1.13 -- examples testing` → 0 hits; it exists only in `docs/`, `src/`, and `tilelang/language/`), and no `examples/`/`testing/` file passes `explicit=True` (0 hits). So the Reach claim \"demonstrated in no example file\" is confirmed by direct grep, and there is no shipped file that either exercises or dodges the bug. In lieu of an example, the four loop-form controls in the table above were RUN (results as shown), plus the primary repro (8 stranded `break;`, nvcc `8 errors detected`).\n\n**Impact.** The trigger is narrow (a `loop_break` under `explicit=True` unroll). When it fires it is a hard compile error, not silent — nvcc rejects the kernel and it never runs, so it is caught immediately rather than corrupting results. Fixing it closes a documented-primitive combination that currently cannot compile, and removes the divergence from the `explicit=False` path that already handles the same source.", "url": "https://github.com/tile-ai/tilelang/issues/3026"}
{"number": 3062, "title": "[BUG] export_library fails on disk-cache hit even with execution_backend=\"tvm_ffi\"", "state": "closed", "created_at": "2026-08-21T11:24:21Z", "updated_at": "2026-08-31T11:22:39Z", "closed_at": "2026-08-31T11:22:39Z", "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13+cuda.gitddf3bdcb\n\n### System information\n\n- Installed from source (editable install, uv-managed)\n- Python 3.11.13, torch 2.13.0+cu130, CUDA 13.0, Linux\n\n### Problem description\n\n`kernel.export_library(...)` works on a **cold compile** but always raises `AttributeError: Runtime module is not available` on the **second run** (when the compiled kernel is loaded from the on-disk cache), even though the kernel was compiled with `execution_backend=\"tvm_ffi\"`.\n\nRoot cause: on a cache hit, `JITKernel.from_database` (`tilelang/jit/kernel.py:150-193`) rebuilds only the adapter and **never populates `self.artifact`**. `export_library` (`tilelang/jit/kernel.py:737-763`) checks `self.artifact is None or self.artifact.rt_mod is None` and raises. On a fresh compile, `_compile_artifact` → `lower_with_context(enable_host_codegen=True)` sets `artifact.rt_mod = host_mod` (`tilelang/engine/lower.py:193-204`), so export works.\n\nAdditional finding: even if `artifact.rt_mod` were set to the module loaded from the cache (`runtime.load_module(kernel_lib_path)`), TVM's `Module.export_library` cannot re-export it — `is_compilation_exportable()` is `False` and it raises `RuntimeError: Module[library] does not support SaveToBytes`. The cached `executable.so` **is** already the exported library, so a viable fix is to copy it, or otherwise record the exported library path on the kernel.\n\n\n### Reproducible example code\n\n```python\nimport torch\nimport tilelang\nimport tilelang.language as T\n\n@tilelang.jit(execution_backend=\"tvm_ffi\")\ndef gemm_relu_eager(A, B, block_M=64, block_N=64, block_K=32, num_stages=2,\n                    dtype=T.float16, accum_dtype=T.float32):\n    M, N, K = T.const(\"M, N, K\")\n    A: T.Tensor((M, K), dtype)\n    B: T.Tensor((K, N), dtype)\n    C = T.empty((M, N), dtype)\n    with T.Kernel(T.ceildiv(N, block_N), T.ceildiv(M, block_M), threads=128) as (bx, by):\n        A_shared = T.alloc_shared((block_M, block_K), dtype)\n        B_shared = T.alloc_shared((block_K, block_N), dtype)\n        C_local = T.alloc_fragment((block_M, block_N), accum_dtype)\n        T.clear(C_local)\n        for ko in T.Pipelined(T.ceildiv(K, block_K), num_stages=num_stages):\n            T.copy(A[by * block_M, ko * block_K], A_shared)\n            T.copy(B[ko * block_K, bx * block_N], B_shared)\n            T.gemm(A_shared, B_shared, C_local)\n        for i, j in T.Parallel(block_M, block_N):\n            C_local[i, j] = T.max(C_local[i, j], 0)\n        T.copy(C_local, C[by * block_M, bx * block_N])\n    return C\n\nkernel = gemm_relu_eager.compile(M=1024, N=1024, K=1024)\nprint(\"backend:\", kernel.execution_backend)\nkernel.export_library(\"gemm_relu.so\")\n\n# Run the script a second time (same process env, disk cache populated):\n# the second run fails before reaching export_library.\n```\n\n\n### Traceback\n\n```pytb\nTileLang begins to compile kernel `gemm_relu_eager` with `out_idx=[-1]`   # only on first run\nTileLang completes to compile kernel `gemm_relu_eager`                     # only on first run\nbackend: tvm_ffi\nTraceback (most recent call last):\n  File \"repro.py\", line 34, in <module>\n    kernel.export_library(\"gemm_relu.so\")\n  File \"/path/to/tilelang/tilelang/jit/kernel.py\", line 754, in export_library\n    raise AttributeError(\nAttributeError: Runtime module is not available. Please compile the kernel with `execution_backend=\"tvm_ffi\"` before exporting.\n```\n\n### Expected behavior\n\n`export_library` should succeed regardless of whether the kernel came from a fresh compile or the disk cache (as long as the backend supports export, e.g. `tvm_ffi`).\n\n**Additional context**\n\nSuspect locations:\n- `tilelang/jit/kernel.py:150-193` — `from_database` never sets `self.artifact`\n- `tilelang/jit/kernel.py:753-762` — `export_library` assumes `artifact.rt_mod` is present\n- `tilelang/jit/adapter/tvm_ffi.py:337-380` — `from_database` loads the executable via `runtime.load_module(kernel_lib_path)` but the loaded module cannot be re-exported by TVM\n\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/3062"}
{"number": 3021, "title": "[BUG][Fuzzer][ice-on-valid-code] Logical `T.Not` on a `bool` buffer in an auto-vectorized loop emits `!ushort4`, crashing nvcc instead of negating lane-by-lane", "state": "closed", "created_at": "2026-08-17T03:06:11Z", "updated_at": "2026-08-31T08:15:51Z", "closed_at": "2026-08-31T08:15:51Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13; L40S (sm_89), CUDA 12.8, PyTorch 2.8.0, Python 3.13.\n\n### Problem description\n\nA logical NOT (`T.Not` or Python `not`) applied elementwise inside a `T.Parallel` loop fails to compile whenever the loop auto-vectorizes (more than one element per thread). The emitted CUDA applies the scalar operator `!` directly to a vector operand, and nvcc rejects it:\n\n(Note: `~` on a `bool` buffer routes through a *different* visitor and emits `~ushortN` — see \"Class\" below; that is the bitwise sibling, filed separately. This issue is specifically the `!` / `NotNode` site.)\n\n```\ntvm_kernels.cu(26): error: no operator \"!\" matches these operands\n            operand types are: ! ushort4\n    ushort4 v__1 = !__2;\n```\n\nThe same kernel compiles and returns the correct result when the loop is not vectorized (one element per thread) — so this is triggered purely by the vector width, not by the `bool` dtype itself.\n\n<details><summary>Emitted CUDA for the vectorized case (width 4) — the <code>!__2</code> is the crash</summary>\n\n```cuda\nextern \"C\" __global__ void __launch_bounds__(64, 1) main_kernel(const signed char* __restrict__ A, signed char* __restrict__ C) {\n  int __1;\n  ushort4 __2;\n  int v_ = *(int*)(A + (((int)threadIdx.x) * 4));\n  __2.x = (bool)(((char)(v_ >> 0)));\n  __2.y = (bool)(((char)(v_ >> 8)));\n  __2.z = (bool)(((char)(v_ >> 16)));\n  __2.w = (bool)(((char)(v_ >> 24)));\n  ushort4 v__1 = !__2;                 // <-- nvcc: no operator \"!\" matches ushort4\n  __1=((signed char)(v__1.x) << 0);\n  ...\n}\n```\n\nThe `bool` vector is correctly represented as a `ushort4`; only the `!` operator is un-scalarized.\n\n</details>\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nN = 256\ndef build(threads):\n    @T.prim_func\n    def main(A: T.Tensor((N,), \"bool\"), C: T.Tensor((N,), \"bool\")):\n        with T.Kernel(1, threads=threads) as bx:\n            for i in T.Parallel(N):          # N/threads elems per thread\n                C[i] = T.Not(A[i])\n    return main\n\na = (torch.rand(N, device=\"cuda\") > 0.5)\nref = ~a\n\n# CONTROL: threads == N -> 1 elem/thread -> scalar `!x`\nc = tilelang.compile(build(256), out_idx=[1])(a)\nprint(\"threads=256 (scalar) :\", \"PASS\" if torch.equal(c, ref) else \"FAIL\")   # -> PASS\n\n# REPRO: threads < N -> 4 elems/thread -> vectorized `!ushort4`\nc = tilelang.compile(build(64), out_idx=[1])(a)                              # -> nvcc compile error\nprint(\"threads=64  (vector) :\", \"PASS\" if torch.equal(c, ref) else \"FAIL\")\n```\n\n### Traceback\n\n```\ntvm_kernels.cu(26): error: no operator \"!\" matches these operands\n            operand types are: ! ushort4\n    ushort4 v__1 = !__2;\n                   ^\ntvm_kernels.cu(26): note #3328-D: built-in operator!(<bool>) does not match because argument #1 does not match parameter\n1 error detected in the compilation of \"tvm_kernels.cu\".\n```\n\n### Expected behavior\n\nThe negation of a `bool` vector should compile and compute lane-by-lane, matching the scalar path (which already returns the correct result over the same inputs). Every other elementwise op in this codegen — `&&`, `||`, `+`, comparisons — already scalarizes over a vector via `PrintBinaryExpr`'s `op->dtype.lanes() == 1` split; `NotNode` is the one unary op that skips that check.\n\n### Additional context\n\n**Root cause.** The C codegen's `NotNode` visitor does not account for a vector `dtype`, so it emits `!` against a vector operand that CUDA has no `operator!` for. In [`CodeGenC::VisitExpr_(const NotNode*)`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L617) it unconditionally writes `os << '!'; PrintExpr(op->a)`, with no `lanes()` branch. Contrast [`PrintBinaryExpr`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L514), which every arithmetic/logical/comparison node routes through: for `lanes() > 1` it defers to `PrintVecBinaryOp` instead of emitting a scalar operator on a vector. The CUDA codegen does not override `NotNode`, so the base-class scalar `!` reaches nvcc verbatim.\n\n**Suggested fix.** One direction is to give `NotNode` the same lane-aware handling the binary nodes have — emit a per-lane expansion (e.g. `make_ushortN(!a.x, !a.y, ...)`) when `op->dtype.lanes() > 1`, mirroring `PrintVecBinaryOp`. Whether the cleaner place is the visitor or an earlier scalarize/legalize pass is a call for the maintainers.\n\n**Class / generalization (two-level root + 4-axis sweep, all cells run on 0.1.13 / sm_89).**\n\n- **Source-level root:** `CodeGenC::VisitExpr_(const NotNode*)` at [codegen_c.cc L617](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L617) unconditionally emits `os << '!'; PrintExpr(op->a)` with no `op->dtype.lanes()` branch. Both binary helpers ([`PrintBinaryExpr` L514](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L514), `PrintBinaryIntrinsic` L536) split on `lanes()==1` and defer wide operands to `PrintVecBinaryOp`; the unary visitors do not.\n- **Operator-level root:** the *only* two visitor sites in `CodeGenC` that emit a C operator on a raw operand with no lane split are `NotNode` (`!`) and the `bitwise_not` intrinsic branch (`os << \"(~\"`, [L677](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L677)). Every other unary (`Cast`, and negation which is lowered to `0 - x` = a binary `SubNode`) already handles vectors. So the class boundary is exactly these two sites.\n\n| axis | cell tested (vectorized, `threads<N`) | observed result | same-root? |\n|---|---|---|---|\n| repro | `T.Not(A[i])`, bool, threads=64 (width 4) | `ushort4 v__1 = !__2;` → `no operator \"!\" matches ... ushort4` | — (the bug) |\n| similar-logic | Python `not A[i]`, bool, width 4 | `ushort4 v__1 = !__2;` → same `operator \"!\"` crash | SAME (NotNode) |\n| related-type | `T.Not(A[i])`, bool, threads=128 (width 2) | `ushort2 v__1 = !__2;` → same `operator \"!\"` crash | SAME (NotNode, any legal width) |\n| related-operator | `T.Not(A[i] > 0)`, int32→bool, width 4 | **PASS** — simplifier folds `!(x>0)` to `(x<=0)`, NotNode eliminated pre-codegen | dodges (NotNode never reaches codegen) |\n| related-operator | `~A[i]`, bool buffer, width 4 | `ushort4 v__1 = ~__2;` → `no operator \"~\" matches ... ushort4` | SIBLING site (`bitwise_not`, not `NotNode`) |\n| related-operator / related-type | `~A[i]`, int32, width 4 | `*(int4*)... = (~*(int4*)...)` → `no operator \"~\" matches ... int4` | SIBLING site (`bitwise_not`) |\n\nTakeaways: (1) the NotNode crash is width-agnostic (widths 2 and 4 both fire) and trigger-agnostic between `T.Not` and Python `not`. (2) The bug only survives when the NotNode reaches codegen — `T.Not` of a *comparison* is constant-folded by the simplifier (`!(x>0)`→`(x<=0)`) and never crashes; the defect needs a NotNode over a value the simplifier can't rewrite, e.g. a plain `bool` buffer load. (3) `~` on any buffer (bool or int) is a **distinct-but-same-root sibling** at the `bitwise_not` site, filed separately as the \"vectorized `~`\" issue; kept separate because they are two independent visitor sites fixable independently. Both are the single missing-`lanes()`-split pattern in `CodeGenC`.\n\n**Provenance.** The `NotNode` visitor has carried this scalar-only form in the vendored TVM `CodeGenC` (`3rdparty/tvm`, pinned at `8df8ebd6` for the 0.1.13 tag). Introduced-vs-relocated before the fork is unverified.\n\n**Dedup.** I searched the open and closed tracker and found no report of a logical-NOT crash on a vectorized `bool`. The closest are distinct mechanisms: #2206 is a `PrintType` `LogFatal` on `boolx8` (type printing, not `operator!`), and #2911/#2382 concern vector `Select`/`atomic_add` value handling.\n\n**Reach.** The trigger is first-class (`T.Not` and Python `not` over a `bool`). Auto-vectorization is the default: any `T.Parallel(N)` with `threads < N` (and `N/threads` a legal vector width) takes the vector path, so the crash fires on the common case; only `threads == extent` (one element per thread) dodges it.\n\nNo shipped example negates a `bool` on the device side inside a `T.Parallel`, so CI is green. The one example that *contains* a boolean negation, `examples/block_causal_attention/block_causal_attention.py`, writes `~q_clean & ~k_clean` (line 429) only in `_dllm_mask` — a **pure-torch host-side reference** operating on `torch.Tensor`s, not device-side TileLang. Its device kernels use `int` masks with `T.if_then_else`, not `T.Not`. I ran it verbatim on 0.1.13: it compiles without the `operator!` crash (it fails only a numeric backward-gradient tolerance check, unrelated to this bug), confirming the example does **not** exercise this defect. So the \"attention/masking\" reach is a *plausible idiom* argument, not a shipped-example one.\n\n**Impact.** The trigger is narrow-looking (a `bool` negation whose loop happens to auto-vectorize) but sits on the default code path for the common `threads < N` case. When it fires it is a nvcc compile-time error, so it is loud and caught immediately: the kernel simply fails to build, and it cannot silently corrupt any output or slip into a running workload. Fixing it lets a legal, common idiom (elementwise negation of a boolean mask) compile in the vectorized path instead of only when one element maps to each thread; it closes a compile-time crash class, not a silent-data hazard.", "url": "https://github.com/tile-ai/tilelang/issues/3021"}
{"number": 3010, "title": "[BUG][Fuzzer][ice-on-invalid-code] `T.gemm` on a tile with a symbolic dimension aborts with an internal `bad_optional_access` instead of clearly rejecting the unsupported symbolic tile dim", "state": "closed", "created_at": "2026-08-16T07:20:48Z", "updated_at": "2026-08-31T06:54:23Z", "closed_at": "2026-08-31T06:54:23Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n- [x] I have read the documentation <https://tilelang.com>.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release).\n\n### System information\n\nLinux, TileLang 0.1.13, CUDA 13.0, PyTorch 2.13.0, Python 3.13, NVIDIA L40S (sm_89). The failure is at compile time (layout inference), so it is host-side and architecture-independent.\n\n### Problem description\n\n`T.gemm` on an operand tile whose shape has a symbolic (dynamic) dimension aborts the compiler with `tvm.error.InternalError: bad_optional_access while parsing tile op call: T.gemm(...)` during `LayoutInference`. The message echoes the serialized call but does not name the offending dimension or say why it failed — it is a caught internal downcast failure, not a diagnostic. The same kernel with a static tile compiles fine.\n\nThe crash comes from the `Gemm` operator's argument deserializer: it reads the tile's M/N/K as `args[i].as<IntImm>().value()`. When a dimension is a symbolic `tir.Var` rather than an `IntImm`, `.as<IntImm>()` returns an empty optional and `.value()` throws the bad-optional exception. The frontend does not reject a symbolic tile dimension before this point, so the user sees only the internal exception.\n\n<details><summary>Isolating which dimension trips it</summary>\n\nMaking only <code>K</code> symbolic (`args[7]`), only <code>M</code> symbolic (`args[5]`), or only <code>N</code> symbolic (`args[6]`), each with strides/offsets left static, reproduces the identical `bad_optional_access` — the crash is the M/N/K downcast block, not the stride/offset reads that follow it.\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\nM = N = 128\n\ndef build(K):\n    @T.prim_func\n    def main(\n        A: T.Tensor((M, K), \"float16\"),\n        B: T.Tensor((K, N), \"float16\"),\n        C: T.Tensor((M, N), \"float16\"),\n    ):\n        with T.Kernel(1, 1, threads=128) as (bx, by):\n            As = T.alloc_shared((M, K), \"float16\")\n            Bs = T.alloc_shared((K, N), \"float16\")\n            Cs = T.alloc_fragment((M, N), \"float32\")\n            T.clear(Cs)\n            T.copy(A, As)\n            T.copy(B, Bs)\n            T.gemm(As, Bs, Cs)          # K is the operand-tile contraction dim\n            T.copy(Cs, C)\n    return main\n\ntilelang.compile(build(128), out_idx=[2])            # control: static K -> COMPILED OK\ntilelang.compile(build(T.symbolic(\"K\")), out_idx=[2])  # symbolic K -> InternalError: bad_optional_access\n```\n\n### Traceback\n\n```\n  File \".../tilelang/engine/lower.py\", line 289, in lower_to_host_device_ir\n    mod = tilelang.transform.LayoutInference()(mod)\n  File \"<unknown>\", line 0, in tvm::tl::LayoutInferencer::Substitute(tvm::tirx::PrimFunc)\n  ...\ntvm.error.InternalError: bad_optional_access while parsing tile op call: T.gemm(T.region(As[0, 0], 1, 128, K), T.region(Bs[0, 0], 1, K, 128), T.region(Cs[0, 0], 3, 128, 128), ..., 128, 128, K, ...)\n```\n\n### Expected behavior\n\nA symbolic `T.gemm` *tile* dimension is genuinely unsupported, so the right outcome is a clear rejection, not an internal crash. The tile M/N/K must be compile-time constants for this code path to work at all: the tensor-core instruction selection (which MMA/WGMMA shape, how many issues), the operand fragment layout inference, and the K-loop unrolling all consume the static tile extents — a symbolic tile dim has no MMA to select and no fragment layout to infer. (This is distinct from dynamic *global* shapes, which TileLang does support: a runtime `M_total` is fine as a grid/loop bound; only the per-block tile extent must be static.) So the fix is a compile-time diagnostic that names the operation and the offending dimension — e.g. `T.gemm requires static tile dimensions, but got a symbolic K` — instead of the internal `bad_optional_access`. TileLang already treats a symbolic extent reaching an unchecked `.as<IntImm>()` as a bug elsewhere (#2906, `makeBufferWithLayout`), where the fix direction is the guard/diagnostic that was intended but never fired; the same framing applies here. (If dynamic-tile GEMM were ever intended, that is a much larger feature — runtime instruction selection — not this defect.)\n\n### Additional context\n\n**Root cause.** The `Gemm` operator's argument deserializer assumes the M/N/K tile dimensions are compile-time constants and downcasts them unconditionally with `.as<IntImm>().value()`; a symbolic dimension makes that optional empty and `.value()` throws. In [`src/op/gemm.cc#L99-L101`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/gemm.cc#L99-L101):\n\n```cpp\nnode->m_ = args[5].as<IntImm>().value()->value;\nnode->n_ = args[6].as<IntImm>().value()->value;\nnode->k_ = args[7].as<IntImm>().value()->value;\n```\n\nThe immediately-following [`clear_accum` argument](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/gemm.cc#L103) is read with `.as<PrimExpr>()`, which accepts a symbolic value — so this same constructor already handles non-constant arguments for one field but not for M/N/K. On the frontend side, M/N/K are taken straight from the operand/tile shapes with no static-shape check ([`gemm_op.py#L77-L89`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/gemm_op.py#L77-L89)), so a symbolic tile dimension flows through to the constructor unguarded.\n\n**Suggested fix.** Check each of M/N/K for `IntImm` (frontend or constructor) and raise a clear error naming `T.gemm` and the symbolic dimension when it isn't constant — a static-tile requirement that MMA atom/warp-partition selection already needs.\n\n**Provenance.** The M/N/K `.as<IntImm>().value()` reads are present in the `Gemm` serialized-argument constructor at the 0.1.13 release (also present in 0.1.9); I did not pin the introducing PR. Not established as a regression.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of the `T.gemm` symbolic-tile-dimension crash. Distinct from #2906 (`makeBufferWithLayout` SIGSEGV on a symbolic shared-buffer extent): same theme (a symbolic extent reaching an unchecked `IntImm` downcast) but a different site (`lower_tile_op.cc` vs the `Gemm` constructor), different trigger (requires a `T.annotate_layout`; this needs only `T.gemm`), and a different symptom (SIGSEGV vs a caught `InternalError`).\n\n**Tested boundary (input family).** Cells run on 0.1.13/L40S, one symbolic tile dim at a time: symbolic `K` (`args[7]`), symbolic `M` (`args[5]`), and symbolic `N` (`args[6]`) each abort with the identical `bad_optional_access`; the fully-static control compiles. The dtype is irrelevant — an `int8`×`int8`→`int32` GEMM with symbolic `K` aborts identically. All share one root — the unconditional `.as<IntImm>().value()` on `args[5..7]` — so this is one missing-guard class over the M/N/K downcast block, not three adjacent bugs.\n\n**Reach.** The trigger is a documented construct (`T.dynamic`/`T.symbolic`, TileLang's dynamic-shape API) used *directly* as a `T.gemm` operand-tile dimension. Dynamic shapes are common in the shipped examples — 52 files under `examples/`+`testing/` use `T.symbolic`/`T.dynamic` at 0.1.13, ~25 of them alongside `T.gemm` — but the dynamic dimension is always the *global* tensor extent, while the GEMM operand tiles are allocated with static `block_M/N/K`. I ran the dedicated [`examples/dynamic_shape/example_dynamic.py`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dynamic_shape/example_dynamic.py) **verbatim at its default config** (M=N=K=16384, block 128/128/32) on 0.1.13/L40S: it compiles, matches the torch reference (`Kernel output matches PyTorch reference.`), and benchmarks (`Latency: 154.26 ms`) — it does **not** trip the crash. It dodges because its `T.gemm(A_shared, B_shared, C_local, ...)` operand tiles are `alloc_shared((block_M, block_K), ...)` etc. with static `block_*`; the symbolic `m/n/k` only size the *global* `A/B/C` tensors and the grid. I confirmed this boundary directly: a kernel with a symbolic *global* extent `M` but a static GEMM tile (the example's pattern) compiles OK — the crash requires the symbolic value to reach a GEMM operand-tile M/N/K, which no shipped example does. This is why CI is green.\n\n**Generalization (§17).**\n\nTwo-level root:\n- **Source-level root (fragile impl):** the `Gemm` serialized-argument constructor in [`src/op/gemm.cc#L99-L101`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/gemm.cc#L99-L101) reads the tile M/N/K with an unconditional `args[i].as<IntImm>().value()->value`. The optional is empty for a symbolic `tir.Var`, and `.value()` throws the caught `bad_optional_access` during `LayoutInference`. The frontend ([`gemm_op.py#L77-L89`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/gemm_op.py#L77-L89)) reads M/N/K straight from operand shapes with no static-shape check, so a symbolic tile dim flows through unguarded.\n- **Operator-level root:** any tile-op constructor that downcasts a *buffer-derived tile shape* via `.as<IntImm>().value()` without first checking it is constant.\n\n**4-axis sweep** (all cells run on 0.1.13/L40S, fresh process + cache each):\n\n| Axis | Cell tested | Observed result | Same-root? |\n|---|---|---|---|\n| input-family (dim) | `T.gemm` symbolic `M` (args[5]) | `InternalError bad_optional_access` | yes — same M/N/K block |\n| input-family (dim) | `T.gemm` symbolic `N` (args[6]) | `InternalError bad_optional_access` | yes |\n| related-type (dtype) | `int8×int8→int32` `T.gemm`, symbolic `K` | `InternalError bad_optional_access` | yes — downcast is on shape, dtype-independent |\n| related-operator | `T.gemm_sp` (sparse), symbolic operand `Kc` | `InternalError bad_optional_access` (identical, `T.gemm_sp(...)` echoed) | **yes** — [`gemm_sp.cc#L110-L112`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/gemm_sp.cc#L110-L112) has the identical M/N/K `.as<IntImm>().value()` idiom |\n| similar-logic (boundary) | symbolic *global* extent `M`, static GEMM tile | `COMPILED OK (no crash)` | boundary — does NOT fire; explains why examples dodge |\n| related-source (distinct) | `reduce.cc:82` / `scan.cc:65` `dim`, `copy.cc:589-593` conv attrs | not run — argument is a compile-time-only attribute (reduction axis / conv kernel/stride/pad), never a buffer-derived symbolic shape | distinct trigger, same code idiom |\n\n**Reframe decision.** The class spans two operators (`T.gemm` and `T.gemm_sp`) that share the identical constructor idiom and produce the identical crash. But `T.gemm_sp` needs Hopper (WGMMA/tcgen05) sparse metadata and a sparse workflow that no user hits casually, whereas `T.gemm` is the common path — and the sparse frontend's own structural K-check (`2*(K//2) == K`) blocks the naive symbolic-K spelling first (a separate syntactic-guard weakness). I keep the issue titled and scoped to `T.gemm` for a minimal, common repro, and record `T.gemm_sp` here as a **same-root sibling** the fix should cover (guard M/N/K in both constructors, or in a shared helper). The reduce/scan/conv `.as<IntImm>()` reads are the same code idiom but a **distinct trigger** (their argument is a genuine compile-time attribute, not a buffer shape) — noted, not filed.\n\n**No new bug found while sweeping.** The `gemm_sp` frontend K-check rejecting `2*(K//2)==K` structurally is a known-class syntactic-guard weakness, not raised here.\n\n**Impact.** The trigger is narrow: only a symbolic (runtime) value used directly as a `T.gemm` operand-tile M/N/K dimension, which no shipped example does. When it fires it is a compile-time abort during `LayoutInference`, so it is loud and caught immediately — it blocks that one kernel from building and cannot silently reach a running workload or corrupt any value. Fixing it does not unblock a new capability (a static tile is still required for MMA atom selection); it replaces an internal `bad_optional_access` with a diagnostic that names `T.gemm` and the offending dimension.", "url": "https://github.com/tile-ai/tilelang/issues/3010"}
{"number": 2947, "title": "[BUG][Example][wrong-code] `_tir_packed_to_unsigned_convert_with_zeros` computes `q - zero` in the unsigned storage dtype, silently wrapping instead of dequantizing", "state": "closed", "created_at": "2026-08-12T08:21:01Z", "updated_at": "2026-08-31T06:12:48Z", "closed_at": "2026-08-31T06:12:48Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (+1 or comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release). On 0.1.13 the helper lives under `examples/dequantize_gemm/quantize/quantization.py` (relocated from `tilelang/quantize/`); the buggy line is unchanged.\n\n### System information\n\nTileLang 0.1.13 / CUDA 12.8 / PyTorch 2.8.0, NVIDIA L40S (sm_89).\n\n### Problem description\n\n`_tir_packed_to_unsigned_convert_with_zeros` (the zero-point dequant helper; `examples/dequantize_gemm/quantize/quantization.py` on 0.1.13, formerly `tilelang.quantize`) subtracts the zero-point **before** the cast to the output dtype and **in the unsigned storage dtype**, so whenever a quantized value is smaller than its zero-point — the normal case for asymmetric quantization — the subtraction wraps modulo `2**storage_nbit` and the decoded value is silently wrong.\n\nWith `uint32` packed storage, decoding nibbles `0..15` with `zero=8` and a `float16` output produces `inf` for every field where `q < zero`, instead of `-8.0, -7.0, …`:\n\n```\ngot  [inf, inf, inf, inf, inf, inf]\nexp  [-8.0, -7.0, -6.0, -5.0, -4.0, -3.0]        # signed (q - zero)\n# same helper with zero=0 (no field underflows) decodes correctly\n```\n\nThe emitted CUDA is `(half_t)(((P[i] >> 0) & 15u) - 8u)`: `0u - 8u` evaluates to `4294967288u` in `uint32`, and `(half_t)4294967288u` is `inf`. No error is raised; the kernel compiles and runs to completion. Not a regression — see Provenance.\n\n<details>\n<summary>Output dtype and storage width only change the wrong value, not the defect</summary>\n\nThe defect is the unsigned subtract; the symptom depends on the output dtype (measured, `uint32` storage, nibble `0`, `zero=8`, expected `-8`): `float16` gives `inf` (the wrapped `uint32` exceeds the fp16 range), `float32` gives `4.29e9` (≈`2**32`). An `int32` output happens to come back correct (`-8`), because the `uint32→int32` reinterpret of the wrapped bit pattern `0xFFFFFFF8` is exactly `-8` — so integer outputs mask the bug the same way narrow storage does, while float outputs expose it.\n\nThe `uint8`/`uint16` storage variants happen to return the right answer for small fields, but only by C accident: `uchar - uchar` is promoted to `int` in the generated CUDA before the cast, so the wrap doesn't occur. The `uint32` (and `uint64`) storage widths get no such promotion. So `storage_nbit=32` reliably reproduces; `storage_nbit=8` masks the same source-level bug.\n\n</details>\n\n### Reproducible example code\n\n```python\nimport sys, numpy as np, torch, tilelang, tilelang.language as T\nfrom tvm import tirx                                    # 0.1.13: tvm.tir -> tvm.tirx\n\n# 0.1.13: the helper moved from `tilelang.quantize` into the dequantize-gemm\n# example, which is NOT shipped in the pip wheel — point at a source checkout's\n# examples/dequantize_gemm/quantize dir and import the helper from there.\nsys.path.insert(0, \"/path/to/tilelang/examples/dequantize_gemm/quantize\")\nfrom quantization import _tir_packed_to_unsigned_convert_with_zeros\n\nN, ZERO = 16, 8                                        # asymmetric zero-point\nfz = _tir_packed_to_unsigned_convert_with_zeros(\"uint\", 32)   # uint32 packed storage\n\ndef build(zero):\n    @T.prim_func\n    def deq(P: T.Tensor((N,), \"uint32\"), O: T.Tensor((N,), \"float16\")):\n        with T.Kernel(1, threads=N) as bx:\n            t = T.get_thread_binding()\n            O[t] = fz(4, P[t], tirx.const(0, \"int32\"), tirx.const(zero, \"uint32\"), \"float16\")\n    return deq\n\nq = np.arange(16, dtype=np.uint32)                     # quantized nibbles 0..15\n\n# TRIGGER: zero=8, so q<8 underflows in uint32\nkt  = tilelang.compile(build(ZERO), out_idx=[1])\ngot = kt(torch.tensor(q, device=\"cuda\")).cpu().numpy().astype(np.float64)\nref = np.array([float(int(v) - ZERO) for v in q])      # signed (q - zero)\nprint(\"got\", got[:6].tolist())                         # -> [inf, inf, inf, inf, inf, inf]\nprint(\"exp\", ref[:6].tolist())                         # -> [-8.0, -7.0, -6.0, -5.0, -4.0, -3.0]\nassert np.max(np.abs(got - ref)) < 1e-3                # -> AssertionError (max_err = inf)\n\n# CONTROL: zero=0, no field underflows -> correct\nkc   = tilelang.compile(build(0), out_idx=[1])\ngotc = kc(torch.tensor(q, device=\"cuda\")).cpu().numpy().astype(np.float64)\nassert np.max(np.abs(gotc - q.astype(np.float64))) < 1e-3   # -> passes\n```\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion; the decoded values are silently wrong (`inf` / wrapped) and deterministic.\n\n### Expected behavior\n\n`q - zero` should be evaluated in the signed/output domain so it can be negative, matching the sibling `_tir_packed_to_signed_convert`, which casts to the output dtype **first** and subtracts a constant **after** ([quantization.py#L305](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dequantize_gemm/quantize/quantization.py#L305)): `(...).astype(dtype) - tirx.const(max_int_value, dtype)`. Decoding nibbles `0..15` with `zero=8` is expected to give `-8.0 … 7.0`.\n\n### Additional context\n\n**Root cause.** The zero-point subtract is performed in the unsigned storage dtype before the widening cast, so an underflow (`q < zero`) wraps modulo the storage width instead of producing a negative number. The whole point of a zero-point is that `q - zero` is signed, so this is the normal input range for the helper.\n\nThe offending line ([quantization.py#L328](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dequantize_gemm/quantize/quantization.py#L328)):\n\n```python\nreturn (((val >> (pos * nbit).astype(storage_dtype)) & mask) - zero).astype(dtype)\n#        ^--------- unsigned storage_dtype -----------------------^  ^ subtract here, then cast\n```\n\n**Suggested fix.** Move the cast inside the subtraction so the difference is computed in the output domain, matching the signed sibling `_tir_packed_to_signed_convert`:\n\n```python\nreturn ((val >> (pos * nbit).astype(storage_dtype)) & mask).astype(dtype) - zero.astype(dtype)\n```\n\nWhether `zero` is cast to `dtype` or to a signed integer first depends on the intended `zero` dtype (not verified end-to-end).\n\n**Provenance.** Introduced with the helper in [#494](https://github.com/tile-ai/tilelang/pull/494) (`cde1886f2`, 2025-05-14, \"Introduce quantize components of TileLang\"); the `- zero).astype(dtype)` form is unchanged since. A related sign-extension fix landed for the *signed* uint32 decode in [#2500](https://github.com/tile-ai/tilelang/pull/2500), but the `with_zeros` path was not touched. The file was relocated into `examples/dequantize_gemm/quantize/` by [#2761](https://github.com/tile-ai/tilelang/pull/2761); the helper and the buggy line are byte-identical there on `main`. The code is adapted from mlc-llm's `quantization.py` per the file header.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect.\n\n**Reach.** `_tir_packed_to_unsigned_convert_with_zeros` is an internal, underscore-prefixed helper whose sole purpose is asymmetric zero-point dequant, where `q - zero` is meant to be signed. Note on scope: since #2761 it lives under `examples/dequantize_gemm/quantize/` and is only re-exported from that example package's own `__init__.py` — it is **not** part of the installed `tilelang` wheel (there is no `tilelang/quantize/` in the pip package on 0.1.13), so it is reached by building the dequantize-GEMM example from a source checkout, not by importing a public `tilelang` API. Before #2761 it lived in `tilelang/quantize/` and was shipped. `grep` of the repo: the only sites naming it are its definition and its re-export in `__init__.py` — no example or test in `examples/`/`testing/` calls it with a non-zero `zero`, which is why CI is green. The `storage_nbit=8` default masks the bug through C integer promotion; the `uint32`/`uint64` storage widths (used for 32-bit-packed weight layouts) hit it whenever any quantized field is below its zero-point.\n\n**Impact.** The trigger is narrow — it needs 32-/64-bit packed storage plus a non-zero zero-point where at least one quantized field falls below it. But when it fires it is a silent wrong-code miscompile: no error is raised, and every field with `q < zero` decodes to a wrapped value (`inf`, a large positive float, or a wrapped integer) across the whole dequantized weight region rather than one bounded value. Because whether it fires is data-dependent (only fields below their zero-point), it can pass small tests and slip into an asymmetric-quantization workload unnoticed. Fixing it closes the unsigned-underflow boundary and aligns the `with_zeros` path with the signed sibling's cast-then-subtract order.", "url": "https://github.com/tile-ai/tilelang/issues/2947"}
{"number": 3030, "title": "[BUG][Fuzzer][diagnostic] T.alloc_barrier(0) is accepted through the whole compile pipeline and emits `Barrier::init(0)` with no diagnostic", "state": "closed", "created_at": "2026-08-17T03:25:58Z", "updated_at": "2026-08-30T06:53:13Z", "closed_at": "2026-08-30T06:53:13Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n`0.1.13` (release tag `v0.1.13`, commit `8001cc4`). The relevant frontend code is unchanged on `main`.\n\n### System information\n\nReproduced on Linux with tilelang `0.1.13`, NVIDIA L40S (sm_89). Two facts, both observed on this machine:\n\n- The frontend accepts `T.alloc_barrier(0)` (and negatives) at trace time with no error — this is **architecture-independent** (it happens in `alloc_barrier`, before any device pass or codegen).\n- Compiling the kernel to a Hopper (`sm_90`) target **succeeds**: the value is carried verbatim into the emitted CUDA as `bar[0].init(0)`. There is no diagnostic anywhere in the pipeline. Executing that kernel needs a Hopper device (not available here), where `Barrier::init(0)` hits the CUTLASS assert `Arrive count must be non-zero`; that runtime face is inferred from the bundled source, not run.\n\n### Problem description\n\n`T.alloc_barrier`'s documented contract for `arrive_count` is only [\"The number of threads that need to arrive at each barrier\"](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/allocate.py#L194-L207) — it says nothing about the valid range. The frontend forwards **any** integer, including `0` and negatives, straight into the barrier-init attribute with no positivity check, so `T.alloc_barrier(0)` compiles cleanly all the way to emitted CUDA. A `mbarrier.init` arrive count of `0` has no defined meaning (it must be at least 1), so the value should be rejected at build time with a clear Python-level error rather than reaching device code.\n\nThe frontend accepts every non-positive count identically to a valid one (run on `v0.1.13`, L40S; `128` is the working control):\n\n<details><summary>Frontend accepts <code>arrive_count</code> in {0, -5, 128} with no error (arch-independent)</summary>\n\n```\n$ python frontend_only.py\nalloc_barrier(0): frontend built prim_func with NO error\nalloc_barrier(-5): frontend built prim_func with NO error\nalloc_barrier(128): frontend built prim_func with NO error\n```\n</details>\n\nCompiling to a Hopper target lowers `arrive_count` into `bar[0].init(<count>)` verbatim, so the bad value survives the entire compile pipeline (frontend → `LowerSharedBarrier` → CUDA codegen) with no diagnostic:\n\n<details><summary>Emitted CUDA (compiled to <code>sm_90</code> on L40S) — <code>0</code> and <code>-5</code> reach <code>Barrier::init</code> unchecked; <code>128</code> is the control</summary>\n\n```cpp\n// alloc_barrier(0)\n__shared__ __align__(16) uint64_t bar_mem[1];\nauto bar = reinterpret_cast<Barrier*>(bar_mem);   // Barrier = cutlass::arch::ClusterTransactionBarrier\nbar[0].init(0);                                    // <-- arrive_count 0, no guard\n\n// alloc_barrier(-5)\nbar[0].init(-5);                                   // <-- negative count, no guard\n\n// alloc_barrier(128)  (control)\nbar[0].init(128);                                  // valid\n```\n(The three outputs differ only in the `init` argument. On a Hopper device, `bar[0].init(0)` executes `Barrier::init` and trips the CUTLASS assert below. The negative case is worse: the assert only checks `!= 0`, so `-5` — passed as `uint32_t` — slips past it and initializes the mbarrier with a garbage 20-bit count.)\n</details>\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport tvm\n\n\ndef kernel(arrive_count):\n    @T.prim_func\n    def main(A: T.Tensor((128,), \"float32\"), B: T.Tensor((128,), \"float32\")):\n        with T.Kernel(1, threads=128):\n            tx = T.get_thread_binding(0)\n            bar = T.alloc_barrier(arrive_count)     # only this value changes\n            T.mbarrier_arrive(bar)                  # all 128 threads arrive\n            T.mbarrier_wait_parity(bar, 0)          # then wait for phase 0\n            B[tx] = A[tx]\n    return main\n\n\n# Frontend accepts every value with no error (architecture-independent):\nkernel(0)\nkernel(-5)\nkernel(128)\n\n# Compile to a Hopper target -> succeeds; grep the emitted source for the init call:\ntgt = tvm.target.Target({\"kind\": \"cuda\", \"arch\": \"sm_90\"})\nfor c in (0, -5, 128):\n    src = tilelang.compile(kernel(c), out_idx=[1], target=tgt).get_kernel_source()\n    print(c, \"->\", [ln.strip() for ln in src.splitlines() if \".init(\" in ln])\n# 0   -> ['bar[0].init(0);']      <- no diagnostic\n# -5  -> ['bar[0].init(-5);']     <- no diagnostic\n# 128 -> ['bar[0].init(128);']    <- control\n\n# On a Hopper (sm_90) device, launching the arrive_count=0 kernel aborts with a\n# device-side assert (CUTLASS \"Arrive count must be non-zero\"); 128 runs correctly.\n```\n\n### Traceback\n\nNo traceback at build time — the frontend accepts `arrive_count = 0` (and negative) with no diagnostic and the pipeline emits `bar[0].init(0);` through CUDA codegen, so there is nothing to trace at compile. The failure surfaces only later, on a Hopper (sm_90) device, as a CUTLASS device-side assert (`Arrive count must be non-zero`) in debug builds, or as undefined behavior in release builds where `CUTLASS_ASSERT` compiles away.\n\n### Expected behavior\n\nThe valid domain of `arrive_count` should be documented and enforced. `mbarrier.init` requires an arrive count of at least 1, and the bundled CUTLASS barrier asserts this ([`ClusterBarrier::init`, `arch/barrier.h#L390-L391`](https://github.com/NVIDIA/cutlass/blob/b2dd65dc864e09688245b316ac46c4a6cd07e15c/include/cutlass/arch/barrier.h#L390-L391): `CUTLASS_ASSERT(arrive_count != 0 && \"Arrive count must be non-zero\")`), so a `0` (or negative) count has no legal target. The natural expectation is that `T.alloc_barrier` raises a clear Python-level error at build time (e.g. \"arrive_count must be a positive integer, got 0\") — as the frontend already does for other malformed allocation arguments — rather than emitting a barrier that asserts on the device in debug builds and is undefined behavior in release builds (where `CUTLASS_ASSERT` compiles away and the raw `mbarrier.init [addr], 0` runs). A negative count is not even caught by the device assert (it checks `!= 0`, and the arg is `uint32_t`).\n\n### Additional context\n\n**Root cause.** `alloc_barrier` does not validate `arrive_count` before forwarding it to codegen, so a non-positive count flows through unchecked into a barrier init that requires it to be positive. At [`allocate.py#L209-L217`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/allocate.py#L209-L217) the count is normalized to a list and each element is wrapped as `IntImm(\"int32\", c)` and stored in the `barrier_init` block attribute, with no check that `c >= 1`:\n\n<details><summary>The unvalidated forwarding (allocate.py L209–L217)</summary>\n\n```python\nif isinstance(arrive_count, int):\n    arrive_count = [arrive_count]\nelse:\n    arrive_count = list(arrive_count)\nbuffer = _with_span(T.sblock_alloc_buffer((len(arrive_count),), _dtypes.uint64, scope=\"shared.barrier\"))\n# Convert to TIR IntImm expressions for C++ pass to consume as Map<Var, Array<PrimExpr>>\narrive_count_exprs = [IntImm(\"int32\", c) for c in arrive_count]\nsblock_attr({\"barrier_init\": {buffer.data: arrive_count_exprs}})\n```\n</details>\n\n`LowerSharedBarrier` then turns each count into [`ptx_init_barrier_thread_count(mbar[i], count)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/transform/lower_shared_barrier.cc#L121-L127) (no positivity check), which the CUDA backend emits verbatim as [`bar[i].init(count)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L2568-L2574) on `cutlass::arch::ClusterTransactionBarrier` — reaching the `arrive_count != 0` assert with `count == 0`. `alloc_cluster_barrier` ([`allocate.py#L222-L240`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/allocate.py#L222-L240)) has the identical unvalidated forwarding.\n\n**Suggested fix.** Validate each element of `arrive_count` in `alloc_barrier` (and `alloc_cluster_barrier`) before building the `IntImm`s — reject a value `< 1` with a clear message, and document the `>= 1` requirement in the docstring's `arrive_count` line:\n\n```python\narrive_count = [arrive_count] if isinstance(arrive_count, int) else list(arrive_count)\nfor c in arrive_count:\n    if not isinstance(c, int) or c < 1:\n        raise ValueError(f\"alloc_barrier: arrive_count must be a positive integer, got {c}\")\n```\n\nAn upper bound (the `mbarrier.init` count is a 20-bit field) could also be checked; the `< 1` case is the one that currently reaches an assert / UB.\n\n**Provenance.** The current `barrier_init`-attribute form of `alloc_barrier` was introduced by [#1733](https://github.com/tile-ai/tilelang/pull/1733) (\"Enhance `T.alloc_barrier` ... and deprecate legacy mbarrier related intrinsics\"); the pre-#1733 form (`return T.alloc_buffer([arrive_count], ...)`) was equally unvalidated, so the missing positivity check has been present since `alloc_barrier` first shipped ([#605](https://github.com/tile-ai/tilelang/issues/605)) — not a regression. Still live on `main`.\n\n**Dedup.** Distinct from [#2344](https://github.com/tile-ai/tilelang/issues/2344) / [#2345](https://github.com/tile-ai/tilelang/pull/2345) (`alloc_barrier` emitting sm_90-only code on a pre-Hopper *architecture* → nvcc failure, fixed for the arch axis by #2345 — that guard IS present in `v0.1.13`: on a real sm_89 target both `arrive_count=0` and `128` are now rejected identically with \"requires sm_90 (Hopper) or later\") and [#2602](https://github.com/tile-ai/tilelang/issues/2602) (cluster builtins on a too-low *architecture* trapping at runtime). Those are all gated on the target *architecture* and fire for any `arrive_count`; this one is gated on the `arrive_count` *value* and fires on a fully supported target. I searched the open and closed tracker and found no report of the `arrive_count` value domain being unvalidated.\n\n**Impact.** A missing frontend diagnostic. A non-positive `arrive_count` is accepted silently and carried verbatim into device code: `0` aborts the kernel with a device-side assert in debug builds and is undefined behavior in release builds (where `CUTLASS_ASSERT` compiles away and `mbarrier.init [addr], 0` runs — a barrier that expects 0 arrivals, so waiters can deadlock or a phase flips with no arrivals); a negative count slips past even the device assert and initializes the mbarrier with a garbage 20-bit count. The failure surfaces only at device link/run time (and only on Hopper), far from the `T.alloc_barrier(...)` call site, where a compile-time `ValueError` was due.\n\n**Reach.** Triggering requires a non-positive `arrive_count`. Across `examples/` and `testing/` at `v0.1.13` (`git grep`: 156 `alloc_barrier(` call-site lines) every count is a positive literal (`1`, `[1] * n`, `[32] * num_stages`, `[128] * 2`, `arrive_count=128/256/384`, `[warp_size] * num_warps`, …), so none trip it and CI stays green; the exposure is a kernel that computes the count at build time (e.g. from a stage/warp-group count) and lands on `0` for some config.\n\nThe one shipped test that directly exercises `alloc_barrier` is [`testing/python/target/test_tilelang_alloc_barrier_target_guard.py`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/testing/python/target/test_tilelang_alloc_barrier_target_guard.py). **Run verbatim on `v0.1.13` (L40S): `2 passed, 1 skipped`** (the `sm_90`-allowed case is skipped — no Hopper here). It **dodges this bug**: its kernel factory hard-codes `T.alloc_barrier(1)` (a valid positive count), and it tests the *architecture* guard (#2345, rejects the primitive on `sm_86`), never the count *value*. No shipped example passes a non-positive count.\n\n**§17 Generalization.**\n\n*Root, two levels:*\n- **SOURCE-level root** — the count is forwarded unchecked from frontend to device. `alloc_barrier` / `alloc_cluster_barrier` ([`allocate.py#L209-L217`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/allocate.py#L209-L217) / [`#L222-L240`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/allocate.py#L222-L240)) wrap each element as `IntImm(\"int32\", c)` with no `c >= 1` guard → `LowerSharedBarrier` ([`#L121-L127`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/transform/lower_shared_barrier.cc#L121-L127), no check) → codegen `.init(count)`. Positivity is enforced *only* by the hardware/CUTLASS side (`ClusterBarrier::init` assert), off the frontend's reach.\n- **OPERATOR-level root** — *barrier participant-count* arguments in general. TileLang has no shared validator for \"a thread/arrive count is positive\"; every barrier primitive that carries a count forwards it raw. This spans `alloc_barrier`, `alloc_cluster_barrier`, and the named-barrier family (`named_barrier_arrive`, `sync_threads(id, n)`), which route through *different* codegen paths but share the same missing-check.\n\n**4-axis sweep** — every cell run in isolation on `v0.1.13` (fresh process/cache; frontend on L40S, codegen via an `sm_90` target; observed, not asserted):\n\n| Axis | Cell tested | Observed result | Same-root? |\n|---|---|---|---|\n| (core) | `alloc_barrier(0)` | frontend OK; codegen `bar[0].init(0);` | **bug** |\n| related-type | `alloc_barrier(-5)` | frontend OK; codegen `bar[0].init(-5);` | **same** — negative also unchecked; `uint32_t` arg slips past the `!= 0` device assert |\n| (control) | `alloc_barrier(128)` | frontend OK; codegen `bar[0].init(128);` | control (correct) |\n| related-type | `alloc_barrier([0, 128])` | codegen `bar[0].init(0); bar[1].init(128);` | **same** — per-element, `0` not caught even mixed with a valid count |\n| related-type | `alloc_barrier(1<<20)` (2²⁰, one past the 20-bit `mbarrier.init` field max) | frontend OK; codegen `bar[0].init(1048576);` | **same** — no upper-bound check either (out-of-field-range silently emitted) |\n| related-type | `alloc_barrier(1.5)` | raw `TypeError: 'float' object is not iterable` at the `list()` normalize ([`allocate.py#L209-L217`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/allocate.py#L209-L217)) | related — no typed check; crashes with an opaque message instead of a clear one |\n| related-type | `alloc_barrier([])` | codegen emits no barrier at all (0-length buffer) | related, benign — empty allocation is silently a no-op (distinct, not part of the value-domain bug) |\n| related-source | `alloc_cluster_barrier(0)` / `alloc_cluster_barrier(-5)` | codegen `bar[0].init(0);` / `bar[0].init(-5);` | **same** — sibling primitive, identical unvalidated forwarding; the one fix covers it |\n| related-operator | `named_barrier_arrive(1, 0)` | codegen `tl::__named_barrier_arrive(1, 0);` → PTX `bar.arrive 1, 0;` | same *mechanism-of-omission*, **distinct code path** (frontend `builtin.py`, not `alloc_barrier`; `bar.arrive` not `mbarrier.init`) — a non-zero thread count is required here too; **distinct-adjacent, not filed** |\n| related-operator | `sync_threads(1, 0)` | no `bar.sync` line emitted (count lowered away in this config) | null result — not exercised by this cell |\n\n**Verdict.** For `alloc_barrier`/`alloc_cluster_barrier` the boundary is exactly \"no positivity/upper-bound/type check on the count between frontend and `Barrier::init`\": the core `0`, negative, per-element `0`, out-of-range, cluster-sibling and non-int cells all share this one root and the suggested per-element `>= 1` + `int` guard closes all of them in one place. Kept as the specific `alloc_barrier` `arrive_count`-value diagnostic gap. The named-barrier `bar.arrive`/`bar.sync` count is the same *kind* of missing check but a genuinely different primitive and lowering path — recorded as **distinct-adjacent, not filed**.", "url": "https://github.com/tile-ai/tilelang/issues/3030"}
{"number": 2992, "title": "[BUG][Fuzzer][ice-on-valid-code] Public builtin T.sync_global() aborts CUDA codegen with an internal LOG(FATAL)", "state": "open", "created_at": "2026-08-15T20:11:05Z", "updated_at": "2026-08-27T17:00:31Z", "closed_at": null, "labels": [], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\n```\n3.13.13 linux\ntilelang 0.1.13\ntorch 2.13.0+cu130\ncuda 13.0\ngpu NVIDIA L40S (sm_89)\n```\nInstalled via pip into an isolated venv.\n\n### Problem description\n\n`T.sync_global()` is a public language builtin — exported through `tilelang.language` in `__all__` and carrying the docstring *\"Synchronize all threads in the entire grid.\"* Any valid kernel that calls it aborts compilation during CUDA device codegen with an internal fatal error:\n\n```\ntvm.error.InternalError: Global storage sync is no longer supported\n```\n\nThe frontend lowers `T.sync_global()` to a `\"global\"`-scope `tvm_storage_sync`, and the `\"global\"` branch of TileLang's CUDA `PrintStorageSync` is now a bare `LOG(FATAL)` — the global-barrier codegen that once backed it was removed, but the public builtin was left in place. So the backend unconditionally kills any program that reaches this API. This is a compiler abort on a valid, exported API call, not a Python-level error the user can catch at trace time.\n\nThe documented sibling `T.sync_grid()` compiles and runs correctly (control below), so grid-wide synchronization *is* available — just not through `sync_global`.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nN = 32\n\n@T.prim_func\ndef main(A: T.Tensor((N,), \"float32\"), B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(1, threads=N) as bx:\n        for i in T.Parallel(N):\n            B[i] = A[i]\n        T.sync_global()          # public builtin, exported in __all__\n\nk = tilelang.compile(main, out_idx=[1])   # aborts here\na = torch.arange(N, dtype=torch.float32, device=\"cuda\")\nprint(k(a).cpu())\n```\n\nOutput (0.1.13, L40S sm_89):\n```\ntx ty tz 1 32 1\n...\n  File \"<unknown>\", line 0, in tvm::codegen::CodeGenTileLangCUDA::PrintStorageSync(...)\n  File \"<unknown>\", line 0, in tvm::runtime::detail::LogFatal::~LogFatal()\ntvm.error.InternalError: Global storage sync is no longer supported\n```\n\n<details>\n<summary>Control: identical kernel with <code>T.sync_grid()</code> compiles + runs correctly</summary>\n\n```python\nimport tilelang, tilelang.language as T, torch\nN = 32\n@T.prim_func\ndef main(A: T.Tensor((N,), \"float32\"), B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(1, threads=N) as bx:\n        for i in T.Parallel(N):\n            B[i] = A[i]\n        T.sync_grid()            # documented sibling\nk = tilelang.compile(main, out_idx=[1])\na = torch.arange(N, dtype=torch.float32, device=\"cuda\")\nassert torch.allclose(k(a).cpu(), a.cpu())   # passes\n# emitted CUDA contains: cooperative_groups::this_grid().sync();\n```\nRemoving the sync call entirely also compiles and runs correctly, so only the `T.sync_global()` call triggers the abort.\n</details>\n\n### Traceback\n\n```\ntx ty tz 1 32 1\n2026-... [TileLang:tilelang.jit.kernel:INFO] TileLang begins to compile kernel `main` with `out_idx=[1]`\n  File \".../tilelang/backend/device_codegen.py\", line 22, in build\n    return tvm.ffi.get_global_func(global_func_name)(mod, target)\n  File \"<unknown>\", line 0, in tvm::codegen::BuildTileLangCUDA(tvm::IRModule, tvm::Target)\n  File \"<unknown>\", line 0, in tvm::codegen::CodeGenTileLangCUDA::AddFunction(...)\n  File \"<unknown>\", line 0, in tvm::codegen::CodeGenTileLangCUDA::PrintStorageSync(tvm::tirx::CallNode const*)\n  File \"<unknown>\", line 0, in tvm::runtime::detail::LogFatal::~LogFatal()\ntvm.error.InternalError: Global storage sync is no longer supported\n```\n\n### Expected behavior\n\nA public, exported builtin should not abort the compiler with an internal `LOG(FATAL)` on a valid kernel. Since `T.sync_grid()` already provides working grid-wide synchronization (it compiles to `cooperative_groups::this_grid().sync();`) and the codegen message states global storage sync is intentionally gone, the evidence suggests one of: route `sync_global()` to the same lowering as `sync_grid()`; or, if it is meant to be retired, remove it from the public API / have it raise a normal `NotImplementedError` at trace time (pointing users at `sync_grid()`) rather than reaching the backend fatal. Which is appropriate depends on whether `sync_global` is intended to stay — a call for the maintainers.\n\n### Additional context\n\n**Root cause.** `T.sync_global()` lowers to a `\"global\"`-scope `tvm_storage_sync`, and TileLang's CUDA codegen handles that scope with an unconditional `LOG(FATAL)` while the builtin is still public and exported. `PrintStorageSync` handles `\"warp\"`, `\"shared\"`/`\"shared.dyn\"`, and `\"cluster\"` normally (verified in source at `codegen_cuda.cc#L1509-L1542`: `warp`→`__syncwarp`, `shared`→`__syncthreads`, `cluster`→`tl::cluster_sync()`); only `\"global\"` is a dead fatal, and nothing else in codegen ever emits the `\"global\"` scope, so the branch is reachable only through `sync_global`. Class = a public API wired to a removed/dead backend branch (the sibling storage-sync scopes all lower correctly; only this one aborts).\n\n- Frontend: [`tilelang/language/builtin.py#L1182-L1188`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/builtin.py#L1182-L1188)\n- Public export (`__all__`): [`tilelang/language/common.py#L273`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/common.py#L273)\n- Backend fatal: [`src/cuda/codegen/codegen_cuda.cc#L1541-L1542`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L1541-L1542)\n  ```cpp\n  } else if (sync == \"global\") {\n    LOG(FATAL) << \"Global storage sync is no longer supported\";\n  }\n  ```\n\n<details>\n<summary>Two secondary frontend defects in <code>sync_global</code> (latent today because codegen aborts first)</summary>\n\nBoth are in the builtin body and only matter if the `LOG(FATAL)` is ever lifted, but they are worth cleaning up with the fix:\n\n1. **Leftover debug `print`** ([`builtin.py#L1186`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/builtin.py#L1186)): `print(tx, ty, tz, ex, ey, ez)` fires on every call — verified this session, it prints `tx ty tz 1 32 1` to stdout during tracing.\n2. **`tx == 0 and ty == 0 and tz == 0` uses Python `and` over `PrimExpr`s** ([`builtin.py#L1187`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/builtin.py#L1187)). Python `and` over comparison-proxy expressions is unsafe: on 0.1.13, calling `bool()` on `tx == 0` raises `ValueError: Cannot use and / or / not operator to Expr, hint: use tvm.tirx.all / tvm.tirx.any` (verified this session in a traced kernel), so this line is fragile — it should use `tirx.all(tx == 0, ty == 0, tz == 0)` rather than Python `and`. (I could not deterministically reproduce a *silent* collapse to just `tx == 0` on 0.1.13; the `and`-over-`Expr` idiom is simply the wrong construct.)\n</details>\n\n<details>\n<summary>Generalization tested this session (v0.1.13, L40S) — adjacent storage-sync scopes</summary>\n\nThe \"adjacent cells\" for this defect are the other storage-sync scopes and the sibling grid-sync API:\n\n| API / scope | observed |\n|---|---|\n| `T.sync_global()` → `\"global\"` scope | crash: `InternalError: Global storage sync is no longer supported` (+ leftover `tx ty tz 1 32 1` print) |\n| `T.sync_grid()` (documented sibling) | COMPILE_OK, emits `cooperative_groups::this_grid().sync();`, numerically matches input |\n| no sync call | COMPILE_OK |\n| `\"warp\"`/`\"shared\"`/`\"cluster\"` scopes (source-verified in `PrintStorageSync`) | handled normally (`__syncwarp`/`__syncthreads`/`tl::cluster_sync()`) |\n\nOnly the `\"global\"` scope — reachable solely through the public `sync_global` builtin — is a dead `LOG(FATAL)`. Distinct root from the frontend `__pos__` gap and the missing half-precision math lowering; kept as its own specific report.\n</details>\n\n**Impact.** The trigger is narrow (a single call to the public `sync_global` builtin — no exotic ingredient — and no example/test exercises it, so CI stays green) but the API is exported in `__all__` and documented \"synchronize all threads in the entire grid.\" When it fires the failure is an internal `LOG(FATAL)` at CUDA codegen: it aborts compile, produces no wrong data, and blocks that one kernel from building. It cannot reach production as a silent miscompile, but any user who reaches for grid-wide sync and picks `sync_global` (rather than `sync_grid`) hits an uncatchable backend abort on first compile instead of a working lowering or a clean trace-time diagnostic.\n\n**Suggested fix.** Either route `sync_global()` through the same lowering as `sync_grid()` (in `builtin.py`, emitting the `cooperative_groups::this_grid().sync()` path), or, if the builtin is retired, drop it from `__all__` and have the frontend raise `NotImplementedError` at trace time pointing at `sync_grid()` — instead of letting the call reach the backend `LOG(FATAL)`. The two secondary frontend defects noted above should be cleaned up alongside either choice.\n\n**Provenance.** `sync_global` and the `\"global\"` codegen branch were introduced in [#2166](https://github.com/tile-ai/tilelang/pull/2166) (merged 2026-05-12, `431c85f1`). The `LOG(FATAL) << \"Global storage sync is no longer supported\"` line was set in [#2216](https://github.com/tile-ai/tilelang/pull/2216) *\"[TIR][IR] Update to use tirx\"* (merged 2026-05-20, `b939fa01`) — that refactor replaced the branch's prior body with the fatal but left the public `sync_global` builtin in place. Present in every release after #2216; not a recent regression.\n\n**Dedup.** I searched the live tracker this session (open and closed issues and PRs, plus the local filed ledger): zero issues for `sync_global`, `\"Global storage sync\"`, or `barrier_expect`. The only PR hits are false positives (#811 changed only the `sync_global` docstring; #741 merged the ThreadPartialSync/ThreadStorageSync passes with `def sync_global()` unchanged context; #1421 adds a `\"global\"` branch in a different backend with a different message). Not in `bugs/FILED_ISSUES.md` / `bug_catalog_ext.json`.\n\n**Reach.** The trigger is a single call to a public, exported builtin — no exotic ingredient. At `v0.1.13` `sync_global` appears in exactly two places, its definition and its `__all__` re-export; `grep -rn sync_global` finds zero uses in `examples/`, `testing/`, `benchmark/`, or `docs/`, which is why no test exercises it and CI stays green — but the symbol is public, so any user who reaches for grid-wide sync and picks `sync_global` (rather than `sync_grid`) hits the abort on the first compile.", "url": "https://github.com/tile-ai/tilelang/issues/2992"}
{"number": 2973, "title": "[BUG][Fuzzer][wrong-code] CuTeDSL codegen emits Python floor `//` for integer truncdiv (DivNode), giving wrong results for negative operands", "state": "open", "created_at": "2026-08-14T23:16:46Z", "updated_at": "2026-08-27T14:41:17Z", "closed_at": null, "labels": [], "comments_count": 2, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the Issue Tracker that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (reproduced on 0.1.13 with nvidia-cutlass-dsl 4.3.1).\n\n### System information\n\nNVIDIA L40S (`sm_89`), CUDA 12.8, Python 3.13, tilelang 0.1.13, nvidia-cutlass-dsl 4.3.1. The defect is in the target-independent CuTeDSL codegen `DivNode` emit, so it is not specific to one GPU.\n\n### Problem description\n\nOn the **CuTeDSL backend**, TIR's `DivNode` (the truncating integer division used by `T.truncdiv` and the `/` operator on integers) is emitted as Python floor-division `//`. Python `//` rounds toward negative infinity; TIR `truncdiv` (and CUDA C `/`) round toward zero. For negative operands the two differ silently.\n\nWorse, the CuTeDSL `%` operator (emitted for `truncmod`) rounds **toward zero** (C semantics), so `//` and `%` use *inconsistent* conventions and the identity `a == (a // b) * b + (a % b)` is broken.\n\nMeasured operator semantics on `cutlass.Int32` (isolated, `a = -7, b = 3`):\n\n| operator (emitted) | result | convention |\n|---|---|---|\n| `//` (DivNode / truncdiv) | -3 | Python **floor** |\n| `%`  (ModNode / truncmod) | -1 | C **truncate** |\n\n`(-7 // 3) * 3 + (-7 % 3) = -3*3 + -1 = -10 != -7`. Any code that relies on `q*b + r == a` (index math, tiling, un-flattening) silently corrupts.\n\n### Reproducible example code\n\n```python\nimport torch, tilelang, tilelang.language as T\n\n@T.prim_func\ndef tdiv(A: T.Tensor((8,), \"int32\"), C: T.Tensor((8,), \"int32\")):\n    with T.Kernel(1, threads=8) as bx:\n        i = T.get_thread_binding()\n        C[i] = T.truncdiv(A[i], 3)\n\nk = tilelang.compile(tdiv, target=\"cutedsl\", out_idx=-1)\na = torch.tensor([-7,-5,-1,2,5,7,3,4], dtype=torch.int32, device=\"cuda\")\nprint(k(a).tolist())                                   # [-3,-2,-1,0,1,2,1,1]  (floor -- WRONG)\nprint(torch.div(a,3,rounding_mode=\"trunc\").tolist())   # [-2,-1, 0,0,1,2,1,1]  (trunc -- CORRECT)\n```\n\nEmitted CuTeDSL source: `C.iterator[tid] = A.iterator[tid] // 3`.\n\nExpected vs actual:\n\n| a | `truncdiv(a,3)` expected | CuTeDSL emit `a // 3` |\n|---|---|---|\n| -7 | -2 | **-3** |\n| -5 | -1 | **-2** |\n| -1 |  0 | **-1** |\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion; the result is silently wrong and deterministic.\n\n### Expected behavior\n\n`T.truncdiv(a, b)` (TIR `DivNode`) should round toward zero: `truncdiv(-7, 3) == -2`, not `-3`. The same `DivNode` on the CUDA backend already does this — its inherited `CodeGenC::VisitExpr_(DivNode)` prints C `/`, which truncates — so a correct target already exists; only CuTeDSL diverges. This also matches `torch.div(..., rounding_mode=\"trunc\")`. The result should stay consistent with the `%` operator the backend emits, so that `(a // b) * b + (a % b) == a` holds.\n\n### Additional context\n\n**Root cause.** [`src/cuda/codegen/codegen_cutedsl.cc#L667-L670`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cutedsl.cc#L667-L670):\n\n```cpp\nif (op->dtype.is_int() || op->dtype.is_uint()) {\n    PrintBinaryExpr_(\"//\", op->dtype, op->a, op->b, os);   // Python floor, but DivNode means truncdiv\n```\n\n(Identical hazard in the base class [`src/cuda/codegen/codegen_py.cc#L271-L276`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_py.cc#L271-L276) — note the base class emits `/` for the float branch but `//` for the int branch.)\n\nTIR's `Div`/`DivNode` is **truncating** division; `FloorDiv` is a separate node. Emitting `//` for `DivNode` is a semantic mismatch. The CUDA backend emits C `/` here, which truncates correctly. Because CuTeDSL `%` already truncates, `//`-for-DivNode disagrees with it and breaks the div/mod identity.\n\nTVM's own IR contract fixes the expected result — `DivNode` is truncating, not flooring — and the CuTeDSL emit contradicts it in two independent places:\n\n<details>\n<summary>TVM defines DivNode as truncating (doc + constant folder), distinct from FloorDiv</summary>\n\n- **Type doc**, [`tvm/include/tvm/tirx/op.h#L336-L347`](https://github.com/apache/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/include/tvm/tirx/op.h#L336-L347): `truncdiv` is documented as `compute trunc(a / b)` / \"the default integer division behavior in C\"; `div` (the `/` operator) \"directly corresponds to truncdiv\" for integers. TVM deliberately keeps `truncdiv`/`floordiv` as *separate* nodes and emits a compile error for a bare `/` to force the caller to disambiguate — so `DivNode` and `FloorDiv` are not interchangeable by design.\n- **Constant folder**, `tvm/src/arith/const_fold.h`: the two nodes fold with *different* algorithms — [`Div`](https://github.com/apache/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L218-L232) does `pa->value / pb->value` with the comment `// NOTE: this will assumes trunc div.`, while [`FloorDiv`](https://github.com/apache/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L274-L280) does `arith::floordiv(...)`. So at compile time TVM folds `truncdiv(-7,3)` to **-2**.\n\nThe consequence: a `DivNode` with **constant** operands folds to the truncating value (-2), but the **same node with runtime operands** is emitted as `//` and yields the flooring value (-3). One kernel gives two different answers for the same expression depending only on whether the operand is a literal — a self-contradiction that pins -2 as the intended result.\n\n</details>\n\n**Suggested fix.** Emit a truncating form for `DivNode` on integers instead of `//`. Options: emit an explicit truncation (`cutlass` integer division intrinsic if one exists), or `int(a / b)`-style trunc, or a corrected `//` with the standard sign fix-up. Because CuTeDSL `%` already truncates, the fix must make `//`-for-DivNode agree with it so `q*b + r == a` holds. Note the `floordiv`/`ceildiv` lowerings (see \"Same root, wider scope\" below) currently *assume* `//` truncates and add a floor fix-up — whichever convention `//` is fixed to, those fix-ups must be reconciled with it so all of truncdiv/floordiv/ceildiv agree.\n\n**Provenance.** The int arm of `DivNode` emitting `//` is present in `codegen_cutedsl.cc:667-670` and the base `codegen_py.cc:273-274` (0.1.13); reproduced at runtime on 0.1.13 this session (`[-3,-2,-1,…]` vs torch trunc `[-2,-1,0,…]`); origin not bisected below 0.1.13.\n\n**Dedup.** This is not a report that `//`/`>>` floors — that is intended. In #995 (closed) a maintainer set the project's contract explicitly: the `/` operator lowers to floor division (\"`>>` represents floor division, consistent with Python's `//`\"), and **users who want truncation are told to call `T.truncdiv`**. This report is that the escape hatch itself is broken *on the CuTeDSL backend*: `T.truncdiv` builds a TIR `DivNode` (documented truncating), and CuTeDSL emits `//` for it, so the one op the maintainer pointed users to still floors. It is distinct from the CuTeDSL fast-math #2597 and slow-math #2565 families (floating-point math intrinsics, not integer rounding).\n\n**Same root, wider scope.** The `//`-convention inconsistency is not confined to `truncdiv` — I enumerated every op whose correctness depends on the backend's division-rounding convention. `floordiv` and `ceildiv` are corrupted too, in the *opposite* direction: their lowerings assume `//` truncates and add a `+ ((a % b) >> 31)` fix-up to reach floor, but since `//` already floors, they over-correct.\n\n<details>\n<summary>Full rounding-scope on 0.1.13 (int32, operand -7)</summary>\n\n| op | expected | CuTeDSL | emitted |\n|---|---|---|---|\n| `T.truncdiv(-7,3)` | -2 | **-3** | `a // 3` |\n| `T.floordiv(-7,3)` | -3 | **-4** | `(a // 3) + Int32((a % 3) >> 31)` |\n| `T.ceildiv(-7,3)`  | -2 | **-3** | `((a + 2) // 3) + Int32(((a+2) % 3) >> 31)` |\n| `T.truncmod(-7,3)` | -1 | -1 (correct) | `a % 3` |\n| `T.floormod(-7,3)` |  2 |  2 (correct) | `a % 3` |\n| `a >> 1` (a=-8) | -4 | -4 (correct) | `a >> 1` |\n\nSo `truncdiv` floors when it should truncate, while `floordiv`/`ceildiv` double-floor. `truncmod`/`floormod` (both emit `%`, truncating) and arithmetic `>>` are correct. All reproduced this session on 0.1.13.\n\n</details>\n\n**Reach.** `T.truncdiv` is a documented frontend op and is the exact op the maintainer directs truncation-needing users to (#995), so the trigger is a supported, recommended call — not an exotic one. The failing condition is any integer division with a possibly-negative operand (index math, tiling, un-flattening on the CuTeDSL backend); the div/mod identity break additionally reaches code that assumes `q*b + r == a`. Non-negative operands are unaffected. The `floordiv`/`ceildiv` faces above share this one root.\n\n**Impact.** The trigger is narrow in one ingredient only — it needs a negative operand — but integer division is pervasive in index and tiling math, so a possibly-negative operand is not rare. When it fires there is no error: the kernel compiles and runs, and the result is deterministically off by one. Worse, because `%` and `//` round under inconsistent conventions the div/mod identity `q*b+r==a` breaks, so any downstream index math built on the pair (un-flattening, tiling) corrupts silently rather than failing loudly.\n\n", "url": "https://github.com/tile-ai/tilelang/issues/2973"}
{"number": 3027, "title": "[BUG][Fuzzer][ice-on-valid-code] LoopVectorize's internal index-scale product overflows int32 and aborts compile for a large-stride affine access whose every real index fits int32", "state": "closed", "created_at": "2026-08-17T03:16:54Z", "updated_at": "2026-08-27T11:53:49Z", "closed_at": "2026-08-27T11:53:49Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\n- TileLang `0.1.13` (source tag `v0.1.13`, commit `8001cc4ccf6149382d2019654a19f59c1d4d0482`).\n- Python 3.12, Linux.\n- GPU: NVIDIA L40S (sm_89). The crash is in a host-side transform pass (`LoopVectorize`), before any GPU codegen, so it is architecture-independent — no GPU is needed to hit it.\n\n### Problem description\n\nA parallel loop containing a large-stride affine global access — a strided load `O[i] = A[i * stride]`, the mirror store `O[i * stride] = A[i]`, or an implicit large buffer row-stride `A[i, 0]` (all confirmed, see §17) — aborts compilation with\n\n```\nInternalError: Check failed: value < 1LL << (dtype.bits() - 1) (2400000000 vs. 2147483648)\n  : Literal value 2400000000 exceeds maximum of int32\n```\n\neven when the kernel is entirely representable in int32 — in the repro below the largest logical index is `3 * 600_000_000 = 1.8e9 < 2^31` and the buffer has `2e9 < 2^31` elements, so no index or extent is out of int32 range. There is no way to compile this kernel as written; a slightly smaller `stride` compiles and gathers the correct values.\n\nThe crashing literal (`2400000000`) is **not** any index the kernel computes: it is an intermediate the vectorization pass forms internally while checking whether the index is invariant across a vector boundary. The trigger tracks that intermediate, not the real index range — e.g. `stride=500_000_000, extent=8` (max index `3.5e9`, genuinely above `2^31`) compiles fine, while `stride=600_000_000, extent=4` (max index `1.8e9`, well below `2^31`) crashes. So the crash is decoupled from actual int32-index overflow.\n\n<details>\n<summary>trigger boundary — the crash follows the internal scale, not the real max index</summary>\n\n`float32` gather `O[i] = A[i*stride]`, buffer `5e9` elems (same across cells):\n\n| extent | stride | max logical index | result |\n|--------|--------|--------------------|--------|\n| 4 | 500M | 1.5e9 | COMPILED-OK |\n| 4 | 600M | 1.8e9 (< 2^31) | **CRASH** (`2400000000`) |\n| 8 | 300M | 2.1e9 | **CRASH** (`2400000000`) |\n| 8 | 500M | 3.5e9 (> 2^31) | COMPILED-OK |\n| 8 | 600M | 4.2e9 | **CRASH** (`2400000000`) |\n\nThe crash literal is constant at `2400000000` regardless of the true max index, and cases with a genuinely out-of-int32 index (3.5e9) compile.\n</details>\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\ndef gather(stride, extent, nel):\n    @tilelang.jit(out_idx=[-1])\n    def build():\n        @T.prim_func\n        def main(A: T.Tensor((nel,), \"float32\"), O: T.Tensor((extent,), \"float32\")):\n            with T.Kernel(1, threads=extent):\n                for i in T.Parallel(extent):\n                    O[i] = A[i * stride]          # strided gather\n        return main\n    return build()\n\n# FAIL: every index is a legal int32 (max = 3*600_000_000 = 1.8e9 < 2^31),\n# buffer 2e9 elems (< 2^31) -> all values fit int32, yet compilation crashes.\ntry:\n    gather(600_000_000, 4, 2_000_000_000)\n    print(\"FAIL-CASE: COMPILED-OK (unexpected)\")\nexcept Exception as e:\n    print(\"FAIL-CASE crash:\", str(e).splitlines()[0])\n# -> FAIL-CASE crash: ... Literal value 2400000000 exceeds maximum of int32\n\n# CONTROL: identical kernel, stride 500_000_000 -> compiles and gathers correctly.\nk = gather(500_000_000, 4, 2_000_000_000)\nA = torch.arange(2_000_000_000, dtype=torch.float32, device=\"cuda\")   # A[j] = j\nO = k(A)\nref = [float(i * 500_000_000) for i in range(4)]\nprint(\"CONTROL:\", O.tolist(), \"==\", ref, \"->\", O.tolist() == ref)\n# -> CONTROL: [0.0, 500000000.0, 1000000000.0, 1500000000.0] == [...] -> True\n```\n\n### Traceback\n\n```\nInternalError: Check failed: value < 1LL << (dtype.bits() - 1) (2400000000 vs. 2147483648)\n  : Literal value 2400000000 exceeds maximum of int32\n```\n\n<details>\n<summary>abridged C++ frames</summary>\n\n```\ntvm::tl::GetVectorizeSize(...)\ntvm::tl::VectorizePlanner::ComputeBufferVectorSize(...)\ntvm::tl::IsExprInvariantInVectorBoundary(...)\ntvm::arith::Analyzer::CanProveEqual(...)\ntvm::arith::Analyzer::Simplify(...)\ntvm::arith::SumExprNode::Normalize_(...)\ntvm::arith::SplitExprNode::NormalizeWithScale(long)\ntvm::tir::MakeConstScalar<long>(int32, 2400000000)\ntvm::IntImm::IntImm(...)   <- Check failed\n```\n</details>\n\n### Expected behavior\n\nA strided gather `A[i * stride]` whose every index and buffer extent is within int32 should compile (and, as the control shows, it computes the correct values on a nearby stride). One direction is to perform the vector-boundary invariance arithmetic in a width that cannot overflow (int64) so the internal `stride × scale` intermediate is representable; another is to have the invariance check bail out of vectorization (return \"not invariant\" / fall back to the minimum vector size) when the simplifier would overflow, rather than aborting the whole compilation. Which is appropriate depends on how the pass is meant to treat wide-index kernels — a call for the maintainers.\n\n### Additional context\n\n**Root cause.** `LoopVectorize`'s vector-boundary invariance check does its index arithmetic in int32 and overflows on a large stride, so it aborts compilation instead of returning a vectorization decision. In [`ComputeBufferVectorSize`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/loop_vectorize.cc#L801) the element offset is built as `elem_offset += transformed_indices[i] * strides[i]` (int32, [L814-L816](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/loop_vectorize.cc#L814-L816)), then passed to [`IsExprInvariantInVectorBoundary`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/loop_vectorize.cc#L830) at each trial vector size. That function substitutes `floordiv(var, vecsize)*vecsize` for the loop var and calls [`analyzer->CanProveEqual`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/loop_vectorize.cc#L1010); canonical simplification then normalizes the split expression via `SumExprNode::NormalizeWithScale`, which multiplies the `stride` coefficient by the boundary scale and constructs an `int32` `IntImm` of the product (`2400000000` here) — tripping the `value < 2^31` guard in the `IntImm` constructor. Because the overflow is in the *internal* scaled coefficient, it fires for indices that are themselves valid int32 (repro: max index `1.8e9`) and does not fire for some genuinely out-of-range indices (`3.5e9`).\n\n**Suggested fix.** In `IsExprInvariantInVectorBoundary`/`ComputeBufferVectorSize` (`loop_vectorize.cc`), either build the `elem_offset` invariance expression in int64 so the `stride × scale` intermediate cannot overflow, or have the invariance check fall back to the minimum vector size when the simplifier would overflow, instead of aborting compilation.\n\n**Provenance.** Present on 0.1.13 as shipped (and on 0.1.9). The invariance-check-based boundary analysis that constructs and simplifies `elem_offset` here was introduced in [#2050](https://github.com/tile-ai/tilelang/pull/2050) (\"Replace syntactic loop-var checks with invariance checks\", merged 2026-04-17); whether the same input crashed via the prior syntactic check is unverified. On `main` after the #2935 refactor the same `elem_offset += indices[i]*strides[i]` construction feeds `IsExprInvariantInVectorBoundary` → `CanProveEqual`, unchanged, so the path is still present (inspected, not run — only 0.1.13 was executed this session).\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this compile crash. #2560 (int32 wrap of a compile-time-constant index) is a different defect: it is a *silent wrong address* in `FlattenBuffer`, triggered by an index genuinely above `2^31`, whereas this is a *compile-time crash* in `LoopVectorize` that fires for indices within int32.\n\n**Reach.** The trigger is an affine global index `A[i * stride]` (or an implicit large buffer row-stride) — an ordinary kernel idiom (a competent user writes exactly this for a strided read; it is not a fuzzer-only shape). The specific ingredient needed to trip it is a coefficient large enough that `coeff × (vector-boundary scale)` crosses `2^31`; with the default `float32` vectorization the crossing point in the repro is around `stride ≈ 6e8`, so the affected region is large strides into large buffers rather than typical tile-local access. In the v0.1.13 tree `119` example files use `T.Parallel` loops with strided/affine global indices, so the idiom is pervasive, but the shipped examples all use tile-sized strides far below this boundary; none trip it and no test exercises a stride near `2^31` — which is why CI is green. Verified by running the shipped `examples/elementwise/example_elementwise_add.py` verbatim on 0.1.13: it uses this exact `T.Parallel` + affine-global-index idiom (`A[by*block_M, bx*block_N]`, `block_M=block_N=32` into a 1024×1024 buffer) and compiled + passed its own `assert_close` (exit 0) — it dodges because tile-sized strides keep the internal scale far below `2^31`.\n\n**Impact.** The trigger is narrow: it needs a stride large enough that `stride × vector-boundary-scale` crosses `2^31` (≈`6e8` for `float32`), so it is confined to large-stride gathers into large buffers, not tile-local access. When it fires it is a loud compile-time `InternalError` that aborts the build of that one kernel — caught immediately, never a silent wrong result, and it cannot reach a running workload or corrupt any output; the deterministic literal in the message also does not correspond to a real index. Fixing it closes a valid-input class (a strided gather whose every index and extent fits int32) that currently cannot be built.\n\n<details>\n<summary>§17 Generalization record</summary>\n\n**Two-level root.**\n- *SOURCE-level (which implementation is fragile):* bundled TVM's canonical simplifier, [`SplitExprNode::NormalizeWithScale`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/canonical_simplify.cc#L129) — it builds the accumulated coefficient as `res = res * make_const(dtype, sscale)` where `dtype` is the index's `int32` and `sscale = stride × boundary-scale`; the `IntImm` constructor's `value < 2^31` guard aborts on the product. The TileLang sink that feeds it is `LoopVectorize`'s `elem_offset += transformed_indices[i]*strides[i]` (int32, `loop_vectorize.cc` L814-816) → `IsExprInvariantInVectorBoundary` (L830) → `CanProveEqual` (L1010) → `Analyzer::Simplify`.\n- *OPERATOR-level (what correlates):* any op that hands `LoopVectorize` a large-coefficient affine int32 index (explicit `i*stride` OR an implicit large buffer row-stride) and routes it through the vector-boundary invariance check — load, store, and multi-dim indexing all share the same `elem_offset` accumulation and sink.\n\n**Found first.** `float32` gather `O[i] = A[i*stride]`, `extent=4`, `stride=600_000_000`: largest logical index `3 * 600_000_000 = 1.8e9 < 2^31`, buffer `< 2^31` elements, yet compilation aborts with `Literal value 2400000000 exceeds maximum of int32`. The crashing literal `2400000000 = 4 * 600_000_000` is the internal `stride × vector-boundary-scale` product, not any index the kernel computes.\n\n**Class hypothesis.** *Un-narrowed construct*: an int32 `IntImm` is built from a scaled coefficient that is never reduced to a width that can hold it, so the `IntImm` guard aborts on an intermediate whose result would be fine at 64-bit. Same-family (do NOT merge) as filed **#2982** (`T.fill(frag_f32, -3.4028235e38)`), where the same range-check aborts on an un-narrowed literal — different sink (frontend fill constant vs. an internal scaled coefficient inside the vectorizer), different pass, same underlying guard; recorded here only as shared root.\n\n**4-axis sweep (each cell RUN on L40S, 0.1.13, one kernel per fresh process):**\n\n| axis | cell tested | observed result | same-root? |\n|------|-------------|-----------------|------------|\n| related-type | `int32` buffer, `stride=600M` | **CRASH** — `... 2400000000 exceeds maximum of int32` | same-root (32-bit element → same scale → same product; numeric semantics irrelevant) |\n| related-type | `float16` buffer, `stride=600M` | **CRASH** — same literal `2400000000` | same-root (element width changes the scale but a stride still exists that crosses `2^31`) |\n| related-type | `float16` buffer, `stride=300M` | **CRASH** — `Can't fetch the lanes of a scalable vector at a compile time` | **distinct-adjacent** — different sink/symptom (scalable-vector lane query), not the int32 `IntImm` guard; not filed |\n| related-type | `float32`, `extent=8`, `stride=500M` (real index genuinely `> 2^31`, internal scale stays under) | **COMPILED-OK** (max index 3.5e9) | same-root — proves the crash tracks the *internal scale*, not the real index |\n| related-operator | strided **store** `O[i*stride] = A[i]`, `stride=600M` | **CRASH** — same literal `2400000000` | same-root (store path L580/593 shares the `elem_offset` accumulation + sink) |\n| related-operator | 2D **implicit row-stride** `A[i, 0]` with row width `600M` | **CRASH** — same literal `2400000000` | same-root — the coefficient can come from a buffer stride, not just an explicit `*`; broadens class beyond the `A[i*stride]` idiom |\n| similar-logic | condition-path `if i*600M < N` (sibling `IsExprInvariantInVectorBoundary` site, `CheckConditionVectorized` L628-635) | **COMPILED-OK** | same source function reached, but this shape did not force the split-normalize through the overflowing scale — sibling site, not triggered by this input |\n\n**Reframe.** Class confirmed for load, store, and implicit-row-stride affine indexing through `LoopVectorize` (all same root, same literal); title/Problem generalized from \"strided global load\" to \"large-stride affine index\". The `float16`+`stride=300M` \"scalable vector\" error is a **distinct-adjacent** bug (different sink, same pass) — recorded, not filed. No NEW independently-fileable bug found while sweeping. The condition-path sibling site shares the fragile source but did not reproduce on the shapes tried.\n\n**Example-run (PART 1).** `examples/elementwise/example_elementwise_add.py` (v0.1.13) uses this idiom (`T.Parallel` + affine global index `A[by*block_M, bx*block_N]`) and was run verbatim: compiled and passed `assert_close`, exit 0 — dodges because `block_M=block_N=32` into a 1024×1024 buffer keeps the internal scale far below `2^31`.\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3027"}
{"number": 3035, "title": "[BUG][Fuzzer][ice-on-invalid-code] `T.gemm` `k_pack` outside `{1, 2}` aborts with an internal `ICHECK` assert deep in a compiler pass instead of a clean frontend error", "state": "closed", "created_at": "2026-08-17T03:26:37Z", "updated_at": "2026-08-27T11:33:28Z", "closed_at": "2026-08-27T11:33:28Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (comment there if it has.)\n- [x] I have read the [documentation](https://tilelang.tile-ai.com) and [FAQ](https://tilelang.tile-ai.com) for this project.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 · CUDA 12.8 · NVIDIA L40S (sm_89) · Linux x86_64. The defect is in target-independent op construction (`Gemm::Gemm`), so it is not specific to this GPU — see Additional context.\n\n### Problem description\n\n`T.gemm(..., k_pack=k)` accepts any integer `k` at the frontend, but only `k_pack ∈ {1, 2}` is valid. Passing any other value (`0`, `3`, `4`, `8`, `16`, …) is not caught at the Python layer; the invalid value flows into the IR and later trips an internal `ICHECK(false)` inside a compiler pass (`LayoutInference`), surfacing to the user as a `tvm.error.InternalError: Check failed: (false) is false: kPack must be 1 or 2` with a stack trace that descends into TVM internals rather than a clean, early diagnostic that names the argument and its allowed range.\n\n`k_pack` is documented as `Numbers of packed matrix cores, for ROCm only`, and the valid domain `{1, 2}` is fixed by the backend. Every other argument of `T.gemm` is validated at the frontend with a descriptive `assert` (shape/K/N checks, `mbar` type) — `k_pack` is the one argument passed straight through to a C++ `ICHECK`.\n\n<details><summary>Trigger boundary (this session, one process per value)</summary>\n\n| `k_pack` | result |\n|---|---|\n| `1` | compiles OK |\n| `2` | compiles OK |\n| `0` | `InternalError: … kPack must be 1 or 2` |\n| `-1` | `InternalError: … kPack must be 1 or 2` |\n| `3` | `InternalError: … kPack must be 1 or 2` |\n| `4` | `InternalError: … kPack must be 1 or 2` |\n| `8` | `InternalError: … kPack must be 1 or 2` |\n| `16` | `InternalError: … kPack must be 1 or 2` |\n\nEvery value outside `{1, 2}` — including `0` and negatives — hits the same `ICHECK(false)` at `gemm.cc:111` (one process per value, this session, sm_89/0.1.13).\n\n</details>\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\ndef make(k_pack):\n    @T.prim_func\n    def main(\n        A: T.Tensor((128, 128), \"float16\"),\n        B: T.Tensor((128, 128), \"float16\"),\n        C: T.Tensor((128, 128), \"float16\"),\n    ):\n        with T.Kernel(1, 1, threads=128) as (bx, by):\n            A_s = T.alloc_shared((128, 128), \"float16\")\n            B_s = T.alloc_shared((128, 128), \"float16\")\n            C_l = T.alloc_fragment((128, 128), \"float16\")\n            T.copy(A, A_s)\n            T.copy(B, B_s)\n            T.clear(C_l)\n            T.gemm(A_s, B_s, C_l, k_pack=k_pack)   # only k_pack in {1, 2} is valid\n            T.copy(C_l, C)\n    return main\n\ntilelang.compile(make(2), target=\"cuda\")   # control: valid -> compiles OK\ntilelang.compile(make(3), target=\"cuda\")   # k_pack=3 -> tvm.error.InternalError: kPack must be 1 or 2\n```\n\n### Traceback\n\n<details><summary>k_pack=3, target=\"cuda\" (0.1.13)</summary>\n\n```\n  File \".../tilelang/engine/lower.py\", line 289, in lower_to_host_device_ir\n    mod = pipeline.lower(mod, target)\n  File \".../tilelang/cuda/pipeline.py\", line 117, in CUDAPassPipelineBodyPrologue\n    mod = tilelang.transform.LayoutInference()(mod)\n  ...\n  File \"<unknown>\", line 0, in tvm::tl::ParseOperator(tvm::tirx::Call const&, ...)\n  File \"/project/src/op/gemm.cc\", line 111, in tvm::tl::Gemm::Gemm(...)\ntvm.error.InternalError: Check failed: (false) is false: kPack must be 1 or 2\n```\n\nThe failure occurs during `LayoutInference`, not at the `T.gemm(...)` call site — the invalid value is admitted, embedded in the IR, and only rejected when a later pass re-parses the op.\n\n</details>\n\n### Expected behavior\n\n`T.gemm` rejects an out-of-range `k_pack` at the frontend with a clear `ValueError`/`AssertionError` that names the argument and its allowed values (e.g. `k_pack must be 1 or 2, got 3`), the same way the frontend already validates every other GEMM argument ([`gemm_op.py` L82–L107](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/gemm_op.py#L82-L107): shape, K, N, and `mbar`-type checks all raise descriptive `AssertionError`s). An out-of-domain scalar argument with a fixed, documented range is a user error that should be diagnosed before IR construction, not converted into an internal compiler assert.\n\n### Additional context\n\n**Root cause.** The `T.gemm` frontend does not validate the `k_pack` argument against its allowed domain, so an out-of-range value is passed unchecked into op construction, where an internal `ICHECK(false)` — meant as a defensive backstop — fires as the *only* line of defense. The check lives at [`src/op/gemm.cc` L108–L113](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/gemm.cc#L108-L113):\n\n```cpp\nif (args.size() > 14) {\n  node->kPack_ = args[14].as<IntImm>().value()->value;\n  if (node->kPack_ != 1 && node->kPack_ != 2) {\n    ICHECK(false) << \"kPack must be 1 or 2\";   // internal assert, not a user diagnostic\n  }\n}\n```\n\n`ICHECK` is an internal invariant assert; when it fires on user-controllable input it produces the opaque `InternalError` + TVM stack trace above rather than a message that points the user at their `k_pack` argument.\n\n**Impact.** A user-facing usability defect, not a miscompile: a legal-looking call with an out-of-range `k_pack` is accepted at the API boundary and then blows up deep in `LayoutInference` with an internal-invariant abort that names no argument. The most likely way to hit it is an autotuner or a hand-written config sweep that includes a `k_pack` value outside `{1, 2}` — the resulting `InternalError` + TVM stack trace gives no hint that the user's own `k_pack` argument is the cause, making it hard to diagnose. No wrong results are produced (the compile aborts).\n\n<details><summary>Note on the CUDA target</summary>\n\n`k_pack` is documented `for ROCm only` ([`gemm_op.py` L175](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/gemm_op.py#L175)). On the CUDA path the parsed `kPack_` is only stored ([`gemm.h` L118](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/gemm.h#L118)) and exposed as a read-only node property ([`gemm.h` L150](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/gemm.h#L150), `def_ro`); it is not consumed by CUDA codegen, so `k_pack=2` compiles but is effectively inert for CUDA lowering. The `{1, 2}` guard still runs unconditionally at op construction regardless of target, which is why the crash reproduces on CUDA. Rejecting `k_pack` that is meaningless for the selected target could be a second, separate diagnostic; the report here is only about the out-of-domain `ICHECK`.\n\n</details>\n\n**Suggested fix.** Validate `k_pack` in the `T.gemm` frontend alongside the existing argument checks (an `assert isinstance(k_pack, int) and k_pack in (1, 2), f\"k_pack must be 1 or 2, got {k_pack!r}\"` in `_gemm_impl`, in the same style as the shape/`mbar` asserts already there), so the error is raised at the call site with a clear message. This also covers the type facet: passing a float (even `2.0`) or `None` currently aborts with an opaque `bad_optional_access` / `arg 14 is not defined()` from the `args[14].as<IntImm>().value()` unwrap at `gemm.cc` L108–L109, before the range check ever runs. The C++ `ICHECK` can stay as a backstop.\n\n**Provenance.** The `kPack must be 1 or 2` `ICHECK` dates to the initial codebase migration (`git log -S \"kPack must be 1 or 2\"`: first appears in commit `57ab687c` / `af643935`, both titled \"Migration of Codebase from Dev Branch into Main\", [#10](https://github.com/tile-ai/tilelang/pull/10), 2025-01-11, in the original `src/op/gemm.cc`). The assert then followed the python-GEMM refactor path into today's canonical file: [#793](https://github.com/tile-ai/tilelang/pull/793) carried the same check into the then-new python-defined `gemm_py.cc`, and [#2033](https://github.com/tile-ai/tilelang/pull/2033) promoted `gemm_py` to be the canonical `src/op/gemm.cc`. The missing frontend validation has therefore shipped since `k_pack` first existed; no fix PR touches it.\n\n**Dedup.** I searched the open and closed issue tracker and PRs and found no existing report of unvalidated `k_pack` (the many `k_pack` PR hits are the AMD/MFMA feature itself and its refactors).\n\n**Reach.** Triggering requires passing `k_pack` explicitly to `T.gemm` (default is `1`, so most kernels never touch it) with a value outside `{1, 2}`. Verified this session on 0.1.13/sm_89: every value in `{0, -1, 3, 4, 8, 16}` hits the same `gemm.cc:111` `ICHECK` during `LayoutInference`, while `1` and `2` compile — so the boundary is exactly \"any integer not in `{1, 2}`\" (see the trigger-boundary table). In the shipped tree (`grep k_pack` at the pinned SHA), the real `T.gemm(..., k_pack=...)` argument is used in only 4 files, all under `examples/amd/` and `examples/gemm_fp8/` (AMD/MFMA and fp8), and every use passes `1` or `2` — one (`examples/amd/example_amd_flash_attn_fwd.py` L70–72) even comments that `k_pack=2` is itself broken on RDNA WMMA and pins `k_pack=1`. (Two further `grep` hits, in `examples/blockscaled_gemm_sm100/` and `examples/deepseek_v4/`, are the unrelated variable name `k_packed`/`sf_k_packed`, not the argument.) In `testing/`, `k_pack` appears only in AMD test files and autotune, again always in `{1, 2}`. So no shipped example or test passes an out-of-range value; the crash is reachable when a user autotunes or hand-writes a `k_pack` sweep that includes a value outside `{1, 2}` (e.g. `k_pack ∈ [1, 2, 4]`).\n\nThe closest shipped example to this trigger, `examples/gemm_fp8/example_tilelang_gemm_amd.py`, autotunes a real `k_packs = [1, 2]` sweep through `T.gemm(..., k_pack=k_pack)` — exactly the config-sweep pattern that would hit this bug. Run verbatim on 0.1.13/sm_89 (`python example_tilelang_gemm_amd.py`), it compiles all 72 configs and passes (`Tuned Latency 0.0114... k_pack=2 ...`; `passed~`); it does **not** hit the bug precisely because its config list is pinned to `{1, 2}` — the shipped example dodges the defect by construction, it does not exercise it. It also confirms that on CUDA both `k_pack=1` and `k_pack=2` compile and give numerically-correct fp8 GEMM (consistent with the \"Note on the CUDA target\" that `kPack_` is inert for CUDA lowering).\n\n<details><summary>§17 Generalization — two-level root + 4-axis sweep (this session, one process per cell, 0.1.13/sm_89)</summary>\n\n**Two-level root.**\n- *SOURCE-level root:* `Gemm::Gemm(...)` op construction in `src/op/gemm.cc` L108–L111 unwraps and range-narrows a user-controllable scalar (`args[14].as<IntImm>().value()`, then `if (kPack_ != 1 && kPack_ != 2) ICHECK(false)`) with **no frontend guard** in `tilelang/language/gemm_op.py::_gemm_impl` — construct-first, narrow-with-an-internal-assert-later. The C++ `ICHECK` is the only line of defense for both the *domain* and the *type* of the argument.\n- *OPERATOR-level root:* the frontend `T.gemm` (and its `_gemm_impl` common path) forwards every scalar knob straight into op construction; only shape/`K`/`N`/`mbar`-type get a descriptive frontend `assert`. The scalar knobs a user can set on `T.gemm` — `k_pack` and `policy` — are the ones with no frontend domain check.\n\n**4-axis findings.**\n\n| axis | cell tested | input → observed result | same-root? |\n|---|---|---|---|\n| similar-logic / related-operator | out-of-domain `policy` int on `T.gemm` | `policy=99` → `tvm.error.InternalError: Check failed: (0) is false: Unknown GemmWarpPolicy` (from `src/cuda/op/gemm.cc:226/310`, during warp-partition, NOT `LayoutInference`); `policy=2` → `COMPILED_OK` | **same CLASS, distinct location** — identical logical shape (unvalidated user-facing GEMM scalar → deep `ICHECK` → opaque `InternalError`), but a different source line and a different pass. Distinct bug, not filed. |\n| related-source | `wg_wait` scalar (`args[15]`, same construction site, no guard) | not user-reachable via `T.gemm`: `_gemm_impl` hardwires `wg_wait=0` for `T.gemm`; only `tcgen05_gemm_blockscaled` exposes it. Not tested as a live crash (no out-of-domain path from `T.gemm`). | N/A — same fragile site, but no user-facing scalar reaches it from `T.gemm`, so no reachable sibling here. |\n| related-operator | other public GEMM entrypoints that take `k_pack` | source read (pinned SHA): `wgmma_gemm`, `tcgen05_gemm`, `tcgen05_gemm_blockscaled`, `mma_gemm_blockscaled` all hardcode `k_pack=1` at the call into `_gemm_impl`; **only `T.gemm` exposes `k_pack`**. So `k_pack` itself has no sibling operator that can trip it. | narrow — `k_pack` reach is `T.gemm`-only. |\n| related-type | `k_pack` non-int / None on `T.gemm` | `k_pack=2.0` (a *valid* value, but float) → `tvm.error.InternalError: bad_optional_access while parsing tile op call` (the `args[14].as<IntImm>().value()` unwrap fails BEFORE the `{1,2}` check, so no \"kPack must be…\" message at all); `k_pack=3.0` → same `bad_optional_access`; `k_pack=None` → `Check failed: (args[i].defined()) is false: arg 14 is not defined()` | **same SOURCE root, additional facet** — the same two lines (L108–L109) also fail to validate the argument's *type*, and produce an even more opaque abort than the range check. Folds into this same report as a second facet of the missing frontend guard. |\n\n**Framing decision.** Kept the report scoped to `k_pack` (the value-domain guard), but expanded it to note the type facet on the SAME two lines (a frontend `k_pack` guard should check `k_pack in (1, 2)` AND that it is an int — the suggested fix already places the guard at the right spot). The `policy` sibling is the same *class* of missing-frontend-validation defect at a *distinct* source location (`src/cuda/op/gemm.cc`, warp-partition) — recorded here as **distinct, not filed**, so this issue stays a single clean root rather than being reframed into a broad \"validate all GEMM scalars\" class.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3035"}
{"number": 3088, "title": "Bug:  Py_LIMITED_API=0x03080000 incompatible with Cython 3.3+", "state": "closed", "created_at": "2026-08-25T21:30:48Z", "updated_at": "2026-08-26T19:24:50Z", "closed_at": "2026-08-26T19:24:50Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "\n\n## Description\nThe CMake build sets `-DPy_LIMITED_API=0x03080000` (Python 3.8 Limited API) for the Cython wrapper target (`tilelang_cython_wrapper`). Cython 3.3.0 introduced a strict compile-time check that requires `Py_LIMITED_API` to be at least `0x03090000` (Python 3.9):\ntilelang_cython_wrapper.cpp:70:8: \n\nerror: #error \"Cython 3.3 requires the Python Limited API version to be 3.9 or greater.\"\n\n\nThis affects all current releases (tested 0.1.10, 0.1.12, 0.1.13).\n## Steps to reproduce\n1. Install Cython >= 3.3.0\n2. Build tilelang from source (any recent tag)\n3. Observe the `#error` from the generated Cython wrapper\n## Suggested fix\nBump `Py_LIMITED_API` from `0x03080000` to `0x03090000` in the CMakeLists.txt where the Cython wrapper target is defined. Since tilelang already requires Python 3.9+, this should be a safe change.\n## Workaround\nPin `cython<3.3.0` in build requirements.\n## Environment\n- tilelang: 0.1.10 / 0.1.12 / 0.1.13\n- Cython: 3.3.0\n- Python: 3.12\n- GCC: 14 (gcc-toolset-14)\n- OS: RHEL 9 (UBI9)", "url": "https://github.com/tile-ai/tilelang/issues/3088"}
{"number": 2942, "title": "[BUG][Fuzzer][wrong-code] `T.fill` of a row-offset fragment sub-region silently fills the wrong rows instead of the sliced region", "state": "open", "created_at": "2026-08-12T06:05:34Z", "updated_at": "2026-08-26T10:14:52Z", "closed_at": null, "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (+1 or comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13; CUDA 12.8, PyTorch 2.8; NVIDIA L40S (sm_89).\n\n### Problem description\n\n`T.fill(f[off:off+h, :], v)` on a `T.alloc_fragment` sub-region whose row offset `off` is **not** a multiple of the fragment's per-thread row atom silently fills the *wrong rows* — the kernel compiles and runs with no diagnostic. For a `(32, 64)` fragment (128 threads, row atom 8), `T.fill(f[3:11, :], 7.0)` writes rows `[0,1,2,3,4,13,14,15]` instead of `[3..10]`. Offsets that are multiples of the atom (`off=0`, `off=8`) fill the intended rows correctly, so the region is computable and the compiler mishandles only the misaligned offset.\n\nThe same misalignment can also abort compilation with `InternalError: Loop layout is not injective: Fragment(... forward_thread: (_i + 5) % 8 * 16 ...)` for some `(off, h)` (e.g. `off=5, h=10`); the silent-wrong-rows face and the ICE face are two manifestations of the same layout mis-map. Not a regression — wrong since the feature was added (see Provenance).\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nM, N = 32, 64\n\ndef build(off, h):\n    @tilelang.jit(out_idx=[-1])\n    def prog():\n        @T.prim_func\n        def main(A: T.Tensor((M, N), \"float32\"), C: T.Tensor((M, N), \"float32\")):\n            with T.Kernel(1, threads=128) as _:\n                f = T.alloc_fragment((M, N), \"float32\")\n                T.copy(A, f)\n                T.fill(f[off:off + h, :], 7.0)     # fill rows off..off+h of the fragment\n                T.copy(f, C)\n        return main\n    return prog\n\ntorch.manual_seed(0)\na = torch.randn(M, N, device=\"cuda\", dtype=torch.float32)\n\ndef check(off, h):\n    ref = a.clone(); ref[off:off + h, :] = 7.0\n    c = build(off, h)()(a); torch.cuda.synchronize()\n    rows = [r for r in range(M) if (c[r] == 7.0).all()]\n    print(f\"off={off}: {'PASS' if torch.allclose(c, ref, atol=1e-4) else 'WRONG'} \"\n          f\" filled rows {rows}  (wanted {list(range(off, off + h))})\")\n\ncheck(0, 8)   # aligned   -> PASS  filled rows [0..7]\ncheck(8, 8)   # aligned   -> PASS  filled rows [8..15]\ncheck(3, 8)   # unaligned -> WRONG filled rows [0,1,2,3,4,13,14,15]\n```\n\nOutput (deterministic, TileLang 0.1.13, L40S sm_89):\n\n```\noff=0: PASS  filled rows [0, 1, 2, 3, 4, 5, 6, 7]      (wanted [0, 1, 2, 3, 4, 5, 6, 7])\noff=8: PASS  filled rows [8, 9, 10, 11, 12, 13, 14, 15](wanted [8, 9, 10, 11, 12, 13, 14, 15])\noff=3: WRONG filled rows [0, 1, 2, 3, 4, 13, 14, 15]   (wanted [3, 4, 5, 6, 7, 8, 9, 10])\n```\n\n### Traceback\n\nNo traceback — for the silent face the kernel compiles and runs to completion; the result is silently wrong and deterministic. (For some `(off, h)` the same mis-map instead throws `InternalError: Loop layout is not injective: Fragment(...)` at compile time.)\n\n### Expected behavior\n\n`T.fill(f[off:off+h, :], v)` should fill exactly rows `off..off+h`, matching the atom-aligned offsets `off=0` and `off=8` which already produce the correct rows over the same fragment. `T.fill`'s documented contract is to fill \"a buffer or buffer region\", and the aligned sibling offsets demonstrate the misaligned region is computable, so the natural expectation is that a misaligned offset fills the same intended rows (rather than silently writing different rows, or aborting with a non-injective-layout error).\n\n### Additional context\n\n**Root cause.** The fill loop applies the region's row offset to the store address but not to the thread→row layout, so the two disagree for any offset that isn't a whole multiple of the fragment's per-thread row atom. The fill loop is built in [`FillNode::MakeSIMTLoop`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/fill.cc#L156) with the store index `region[i]->min + var` ([fill.cc:164](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/fill.cc#L164)), `var` iterating the region extent `h`. The parallel lowering then derives the loop's fragment layout from the *destination buffer* (unshifted) via [`ComputeLoopLayoutFromBuffer`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L703) ([called at parallel.cc:487](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L487)) — so the offset ends up folded into the *thread selector* of a loop whose induction range is only `[0, h)`, instead of selecting the target rows. When `off` is a multiple of the row atom the shift is a whole number of thread groups and the mapping still lands right; otherwise threads land on the wrong physical rows (silent), or the map is non-injective and trips the [injectivity check](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L536) (the ICE face). Put differently: the forward layout splits a row into a slot component (`row // 8`) and a thread-group component (`row % 8`), but the fill inverts the offset back without that split — it pushes the whole `off` onto the slot selector and drops the `off % 8` part — so the recovered rows are wrong unless `off % 8 == 0`.\n\n<details>\n<summary>Generated CUDA for the <code>off=3</code> fill store, and the exact wrong rows it produces</summary>\n\nThe store the fill lowers to (0.1.13, this repro):\n\n```cuda\nfloat broadcast_var = 7.0f;\n*(float4*)(f + (((((int)threadIdx.x) + 48) >> 7) * 4)) = make_float4(broadcast_var, ...);\n```\n\nThe fragment layout (read off the surrounding `T.copy`, which lowers to `f[i] = A[i*512 + threadIdx.x*4]`) puts logical row `r` in **register slot `r // 8`**, thread group `(r % 8)` (16 threads per row, 4 columns each). So slot 0 holds rows 0–7, slot 1 holds rows 8–15.\n\nThe fill index `((threadIdx.x + 48) >> 7)` (where `48 = off*16 = 3*16`, and `>> 7` is `/128`) evaluates to **0 for threadIdx < 80** and **1 for threadIdx ≥ 80**, i.e. a *thread-dependent slot selector* rather than a per-row predicate:\n- threads 0–79 write **slot 0** → rows `threadIdx//16` = rows **0,1,2,3,4**\n- threads 80–127 write **slot 1** → rows `8 + threadIdx//16` = rows **13,14,15**\n\ngiving the observed `[0,1,2,3,4,13,14,15]`. A correct lowering would instead, per thread, test whether each of its slots maps to a logical row in `[off, off+h)` and write only those — rows 3–10 legitimately span slot 0 (rows 3–7) and slot 1 (rows 8–10). The aligned `off=8` case works precisely because `8*16 = 128` makes the selector the constant `1` (all threads → slot 1 → rows 8–15).\n</details>\n\n<details>\n<summary>The mis-placement is in the fill's layout, not a dropped barrier (control with no preceding write)</summary>\n\nFilling the offset region of a fragment that has no cross-thread write to synchronize against still lands in the wrong rows, so the cause is the fill loop's own layout, not a missing sync:\n\n```python\n@tilelang.jit(out_idx=[-1])\ndef frag():\n    @T.prim_func\n    def main(C: T.Tensor((32, 64), \"float32\")):\n        with T.Kernel(1, threads=128) as _:\n            f = T.alloc_fragment((32, 64), \"float32\")\n            T.fill(f, 0.0)              # whole fragment, aligned -> correct\n            T.fill(f[3:11, :], 7.0)     # offset sub-region\n            T.copy(f, C)\n    return main\n# -> rows filled with 7.0: [0,1,2,3,4,13,14,15]  (wanted [3..10]); 512 sevens, all in wrong rows\n```\n</details>\n\n**Suggested fix.** Invert the row offset the same way the forward layout builds it — split `region[i]->min` into its slot (`// 8`) and thread-group (`% 8`) components so the fill's layout counts rows from the same origin as the store (`fill.cc:164`), rather than folding the whole offset onto the slot selector. (I haven't built or verified a patch; a non-atom-aligned offset makes the target rows span two atom blocks, so this may be more than a one-line change.) Alternatively, if non-atom-aligned fragment fills aren't meant to be supported, the injectivity check at `parallel.cc:536` should reject them — it currently misses the silent-wrong-rows cases — rather than miscompiling.\n\n**Provenance.** The offset line `region[i]->min + var` in `MakeSIMTLoop` was added in [#2166](https://github.com/tile-ai/tilelang/pull/2166) (merged 2026-05-12), which introduced sliced-region fill without a corresponding shift in the loop-layout derivation; the misalignment has been present since, no later than 0.1.9 (the earliest wheel I ran). No review bot flagged it on that PR.\n\n**Dedup.** I searched the open and closed tracker and found no report of a fragment fill mis-mapping a row offset. [#2698](https://github.com/tile-ai/tilelang/issues/2698) is a distinct bug with a different root cause — a **shared**-buffer nonzero-offset fill whose `__syncthreads()` is dropped by `tl.ThreadSync` (a missing WAW barrier). This report is about a **fragment** fill whose layout partition itself mis-maps the offset, reproducing with no cross-thread hazard to synchronize (see the no-preceding-write control above).\n\n**Reach.** The trigger is documented: `T.fill`'s docstring defines \"a buffer or buffer **region**\" as valid input, and slicing a fragment sub-region is the natural way to fill part of a tile. Sliced fills appear in `testing/`: `test_tilelang_issue_1008.py` fills `x[0:128]` and `x[a:b]`, and `test_tilelang_transform_plan_update_buffer_allocation_location.py` fills `x[a:b]` — but all of these fill a **global/argument** tensor (no thread-distributed fragment layout), and the static case starts at offset 0; none exercise a fragment sub-region at a non-atom-aligned offset, and the dynamic-region test only checks compilation, not the filled values. Given a fragment target, the boundary is concrete: with a `(32,64)` fragment at 128 threads the row atom is 8, so `off ∈ {0, 8, 16, 24}` fill correctly and every other row offset is wrong; the crashing vs silent face depends on the extent `h`.\n", "url": "https://github.com/tile-ai/tilelang/issues/2942"}
{"number": 3015, "title": "[BUG][Fuzzer][ice-on-valid-code] Rank-0 `T.Tensor((), dtype)` crashes `FlattenBuffer` instead of compiling like `T.Buffer((), dtype)`", "state": "closed", "created_at": "2026-08-16T07:39:22Z", "updated_at": "2026-08-26T05:53:45Z", "closed_at": "2026-08-26T05:53:45Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (+1 or comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13, CUDA 12.8, PyTorch 2.8.0; reproduced on an NVIDIA L40S (sm_89). The crash is in a target-independent frontend/lowering path (buffer construction + `FlattenBuffer`), so it is not arch-specific.\n\n### Problem description\n\nA rank-0 (scalar) buffer declared with `T.Tensor((), dtype)` aborts compilation in `FlattenBuffer` with\n\n```\nInternalError: Check failed: this->strides.size() == input_indices.size() (1 vs. 0) :\nIf strides are defined, the index's dimensionality must match the dimensionality of the index given.\n```\n\nThe same scalar declared with `T.Buffer((), dtype)` compiles and runs correctly, so the shape itself is supported — only the `T.Tensor` spelling crashes.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\n# One scalar (rank-0) accumulator, written once per kernel.\ndef build(ScalarDecl):\n    @tilelang.jit\n    def kern(N):\n        @T.prim_func\n        def main(A: T.Tensor((N,), \"float32\"), S: ScalarDecl((), \"float32\")):\n            with T.Kernel(1, threads=1):\n                acc = T.alloc_local((1,), \"float32\")\n                acc[0] = T.float32(0)\n                for i in T.serial(N):\n                    acc[0] += A[i]\n                S[()] = acc[0]         # 0-dim index, matches the 0-dim shape\n        return main\n    return kern\n\n# CONTROL: T.Buffer((), ...) -> strides=[] (rank-consistent) -> compiles & runs\nkc = build(T.Buffer)(8)\nA = torch.arange(8, dtype=torch.float32, device=\"cuda\")\nS = torch.zeros((), dtype=torch.float32, device=\"cuda\")\nkc(A, S)\nprint(\"control:\", S.item(), \"== ref\", A.sum().item())   # -> 28.0 == 28.0\n\n# BUG: T.Tensor((), ...) -> _construct_strides(()) returns strides=[1] (rank 1 != shape rank 0)\nbuild(T.Tensor)(8)                                       # -> InternalError in FlattenBuffer\n```\n\n### Traceback\n\n```\nmod = tilelang.transform.FlattenBuffer()(mod)\n  tvm::tl::BufferFlattener::VisitStmt_(tvm::tirx::BufferStoreNode const*)\n  tvm::tl::BufferFlattener::GetSimplifiedElemOffset(tvm::tirx::Buffer const&, ...)\n  3rdparty/tvm/src/tirx/ir/buffer.cc, line 269, in tvm::tirx::BufferNode::ElemOffset(...)\ntvm.error.InternalError: Check failed: this->strides.size() == input_indices.size() (1 vs. 0) :\nIf strides are defined, the index's dimensionality must match the dimensionality of the index given.\n```\n\nThe `FlattenBuffer` pass invokes `BufferNode::ElemOffset` on the store `S[()]`, which asserts `strides.size() == index.size()`. The synthesized rank-1 strides (`[1]`) do not match the rank-0 index `()`.\n\n### Expected behavior\n\nA rank-0 `T.Tensor((), dtype)` should compile to a valid scalar buffer, the same way `T.Buffer((), dtype)` does — the empty tuple is a valid `ShapeType`, and the control shows the identical kernel compiles and returns the correct value (`28.0`) when the scalar is declared via `T.Buffer`. The buffer's strides rank should track its shape rank (empty strides for an empty shape).\n\n### Additional context\n\n<details>\n<summary>Root cause, provenance, dedup, suggested fix</summary>\n\n**Root cause.** `TensorProxy` synthesizes a strides tuple whose length does not match a rank-0 shape, so it hands `FlattenBuffer` a buffer with `shape=[]` but `strides=[1]`. [`TensorProxy._construct_strides`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/proxy.py#L158) seeds the accumulator with `strides = [1]` at [L159](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/proxy.py#L159) and then extends it by iterating `shape[:0:-1]` at [L160](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/proxy.py#L160) (every dim except the first). For an empty shape `()` that slice is empty, so the loop never runs and the seed `(1,)` is returned unchanged (returned at [L163](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/proxy.py#L163)); [`TensorProxy.__call__`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/proxy.py#L165-L168) then passes those strides straight to the buffer. The resulting rank-1 strides on a rank-0 buffer later fail the `strides.size() == input_indices.size()` invariant on a matching 0-dim index. `T.Buffer` leaves `strides=None` (contiguous) and never hits this.\n\nDirectly: `TensorProxy._construct_strides(())` returns `(1,)`, and `T.Tensor((), \"float32\").strides == [1]` while `T.Buffer((), \"float32\").strides == []`.\n\n**Suggested fix.** Make `_construct_strides` return an empty tuple for an empty shape. The codebase already contains the correct idiom in [`tilelang/language/eager/utils.py:construct_strides`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/eager/utils.py#L93-L103), which appends inside a `for s in shape[::-1]` loop (empty seed) and therefore returns `()` for `()` while agreeing with `_construct_strides` on every rank ≥ 1 (verified: `()`→`()`, `(2,3)`→`(3,1)`, `(2,3,4)`→`(12,4,1)`). Rewriting `_construct_strides` to that shape (or handling `len(shape) == 0` up front) keeps the strides rank in sync with the shape rank and lets the scalar compile as it does through `T.Buffer`.\n\n**Provenance.** The `strides = [1]` seed in `TensorProxy._construct_strides` is present at the 0.1.13 release; I did not pin the introducing PR. Not a regression of previously-correct behavior — the seed makes the empty-shape case rank-inconsistent from the moment the helper produces it.\n\n**Dedup.** Searched the open and closed tracker (`0-dimensional`, `strides dimensionality`, `_construct_strides`, `FlattenBuffer strides`, scalar-tensor titles) and found no existing report of this defect. #820 is an unrelated feature request (accepting a PyTorch scalar *dtype*).\n\n</details>\n\n**Generalization (root levels + 4-axis sweep, all cells RUN on 0.1.13).**\n\n**Two-level root.**\n- **SOURCE-level root (fragile implementation):** [`TensorProxy._construct_strides`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/proxy.py#L157-L163) seeds the accumulator with `s, strides = 1, [1]` (L159) *before* narrowing by iterating `shape[:0:-1]` (L160, \"every dim except the first\"). For rank-0 that slice is empty, the loop never runs, and the seed `(1,)` survives — a rank-1 strides list on a rank-0 shape. The downstream victim is the invariant `strides.size() == input_indices.size()` in `BufferNode::ElemOffset` (`3rdparty/tvm/src/tirx/ir/buffer.cc:269`), reached via `FlattenBuffer`/`BufferFlattener`. This is a *construct-then-narrow with a nonempty seed* idiom; a *construct-by-appending-in-loop* idiom (the sibling in `eager/utils.py`, below) has no such off-by-one.\n- **OPERATOR-level root (what correlates):** only proxies that route through `TensorProxy._construct_strides` and pass explicit synthesized strides. `_construct_strides` is overridden **solely in `TensorProxy`** (global-scope `T.Tensor`). `T.Buffer`, `T.StridedTensor`, and the scope proxies (`T.FragmentBuffer`/`SharedBuffer`/`LocalBuffer`, all inheriting `BaseTensorProxy`) leave `strides=None` or guard the length, so none synthesize a rank-mismatched strides list.\n\n**4-axis sweep (every cell run on `/tmp/tl113_venv`, v0.1.13; values observed, not asserted):**\n\n| Axis | Cell tested | Observed result | Same-root? |\n|---|---|---|---|\n| input-family | `_construct_strides` over `()`,`(1,)`,`(4,)`,`(2,3)`,`(2,3,4)` | `()`→`(1,)` (rank1≠0, **bug**); all rank≥1 match shape rank | boundary is exactly `()` |\n| related-source | `StridedTensorProxy` (same file): `T.StridedTensor((),())` | strides=`[]` (rank-consistent); `T.StridedTensor((),(1,))`→`ValueError \"Invalid shape/strides' dimensions\"` | **distinct — uses correct `len(shape)==len(strides)` guard**, does NOT reproduce |\n| related-source | scope proxies `T.FragmentBuffer/SharedBuffer/LocalBuffer(())` | all → strides=`[]`; a rank-0 `T.alloc_shared((),...)` scratch **compiles and runs** end-to-end | distinct — inherit `BaseTensorProxy` (no stride synth), do NOT reproduce |\n| related-operator | `T.Buffer((), \"float32\")` full compile+run (control) | strides=`[]`; kernel runs, `S.item()==28.0` | distinct — no stride synth, dodges |\n| related-type | rank-0 `T.Tensor((), \"int8\")` full compile | **same `InternalError`** | **same root** (stride synth is dtype-agnostic) |\n| similar-logic | `eager/utils.py:construct_strides` (same job, different idiom) | `()`→`()`, `(1,)`→`(1,)`, `(2,3)`→`(3,1)`, `(2,3,4)`→`(12,4,1)` | **the correct sibling**: agrees on rank≥1, diverges only at `()`; already ships the fix pattern |\n\n**Boundary.** The class is a single missing empty-shape guard on the `strides=[1]` seed, unique to `T.Tensor` (`TensorProxy`). Every rank ≥ 1 is already correct; every sibling proxy (`Buffer`, `StridedTensor`, `Fragment/Shared/LocalBuffer`) dodges it; only the global `T.Tensor(())` spelling fails the downstream `strides.size() == input_indices.size()` check. The bug is dtype-agnostic (fp32 and int8 both crash). Kept **specific** to `T.Tensor((), dtype)` — no adjacent operator or type widens the class.\n\n**Example-run result (PART 1).** The `@T.macro` docstring at `parser/entry.py:133-140` documents the exact crashing signature `B: T.Tensor((), \"int32\")` with `B[()] = ...`. Compiling that signature verbatim (in a `T.Kernel(1, threads=1)`, `B[()] = A[128]`) → `tvm.error.InternalError: Check failed: this->strides.size() == input_indices.size() (1 vs. 0)`. So the *documented* idiom crashes; the caveat is that this lives in a prose docstring (illustrating macro hygiene, shown without `T.Kernel`), not in an executed CI test — hence green CI. The one runnable scalar example in the tree (`test_tilelang_issue_sm120_tma_smem_alignment.py:66`) uses `T.Tensor((1,), ...)` and correctly dodges the empty-shape path (verified verbatim in the v0.1.13 tree).\n\n**Distinct-adjacent (not filed).** `StridedTensorProxy`'s length-mismatch path raises a clean `ValueError(\"Invalid shape/strides' dimensions\")` rather than crashing in the backend — this is the *correct* behavior and confirms the fix template: `TensorProxy._construct_strides` should produce empty strides for `()` (as `eager/utils.py:construct_strides` already does). No new bug was found while sweeping.\n\n**Reach.** The trigger is a rank-0 shape `()` passed to `T.Tensor`. It is a type-legal argument: `ShapeType = tuple[Union[PrimExpr, int], ...]`, whose variadic tuple admits length 0. No *runnable* shipped example exercises it: `grep -rn \"T.Tensor(()\" examples/ testing/` returns 0 sites (verified on the v0.1.13 tree, exit-clean, 0 matches) — runnable code that wants a scalar-like buffer uses `(1,)` (e.g. `testing/python/issue/test_tilelang_issue_sm120_tma_smem_alignment.py:66` declares `c_scalar: T.Tensor((1,), \"float32\")`, verified verbatim), which dodges the empty-shape path, so CI stays green.\n\nHowever, TileLang's **own documentation teaches the crashing idiom**: the `@T.macro` docstring in [`tilelang/language/parser/entry.py` L133-L140](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/parser/entry.py#L133-L140) presents rank-0 scalar tensors as the canonical macro-hygiene example — `def use1(A: T.Tensor((1024,), \"int32\"), B: T.Tensor((), \"int32\"))` with `B[()] = A[...]` writes. Compiling that exact signature (rank-0 `T.Tensor((), \"int32\")` written via `B[()]`) reproduces the `InternalError` (run result recorded in §17). The idiom is documented, not merely type-reachable; it just happens to sit in a docstring rather than in an executed test, so nothing in CI catches it.\n\nThe defect is arch-independent (frontend buffer construction + `FlattenBuffer`/`BufferNode::ElemOffset`); reproduced on L40S (sm_89), not run on other arches but no arch gate is involved.\n\n**Impact.** The trigger is narrow: a rank-0 `()` shape spelled specifically via `T.Tensor` (the `T.Buffer` spelling of the same scalar compiles fine, and shipped code sidesteps it with `(1,)`). When it does fire it is a compile-time `InternalError` in `FlattenBuffer` — loud, caught immediately at build, deterministic, and corrupts no output; the only cost is that one legal scalar declaration refuses to compile through the `T.Tensor` path. Fixing it closes the empty-shape boundary class so `T.Tensor((), ...)` behaves identically to `T.Buffer((), ...)`.", "url": "https://github.com/tile-ai/tilelang/issues/3015"}
{"number": 2955, "title": "[BUG][Fuzzer][wrong-code] `B[j % K]` in a 2-D `T.Parallel(M, N)` loop silently reads the wrong element when `K ∤ N` instead of computing `j % K`", "state": "closed", "created_at": "2026-08-13T00:45:32Z", "updated_at": "2026-08-25T11:25:53Z", "closed_at": "2026-08-25T11:25:53Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and did not find a report of this defect.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 (release commit `8001cc4`, bundled TVM `0e15b27`); Linux, NVIDIA L40S (sm_89), CUDA 12.8. Reproduced on sm_89; the defect is in an architecture-independent lowering pass (`FlattenBuffer` → TVM iter-map simplification), so it is not GPU-specific.\n\n### Problem description\n\nA plain per-column table lookup `B[j % K]` written inside a 2-D `T.Parallel(M, N)` loop returns the wrong `B` element for every row `i > 0` when `K` does not divide `N`. The kernel compiles and runs with no error; the result is silently wrong and deterministic. When `K` divides `N`, or when the inner loop is serial (not part of the 2-D parallel fusion), the same code is correct.\n\nFor `for i, j in T.Parallel(16, 64): C[i,j] = A[i,j] + B[j % 7]`, 512 of 1024 outputs are wrong (all rows `i >= 1`). The emitted CUDA indexes `B` as `B[threadIdx.x % 7]` — the flattening maps the 1024 iterations onto `8 x 128` (`i * 128 + threadIdx.x`), so the column is `j = (i*128 + threadIdx.x) % 64 = threadIdx.x % 64`, and the intended index is `(threadIdx.x % 64) % 7`. The inner `% 64` is dropped, so for `threadIdx.x >= 64` the wrong `B` slot is read.\n\nNot a regression — see Provenance.\n\n<details><summary>Emitted CUDA (the dropped inner modulo)</summary>\n\n```cuda\nextern \"C\" __global__ void __launch_bounds__(128, 1)\nmain_kernel(const float* A, const float* B, float* C) {\n  #pragma unroll\n  for (int i = 0; i < 8; ++i) {\n    C[((i * 128) + ((int)threadIdx.x))] =\n        (A[((i * 128) + ((int)threadIdx.x))] + B[(((int)threadIdx.x) % 7)]);\n    //                                          ^^^^^^^^^^^^^^^^^^^^^^^^^ should be ((threadIdx.x % 64) % 7)\n  }\n}\n```\n</details>\n\n### Reproducible example code\n\n```python\nimport numpy as np, torch, tilelang, tilelang.language as T\n\nM, N = 16, 64\n\ndef build(K):\n    @T.prim_func\n    def main(A: T.Tensor((M, N), \"float32\"),\n             B: T.Tensor((K,), \"float32\"),\n             C: T.Tensor((M, N), \"float32\")):\n        with T.Kernel(1, threads=128) as bx:\n            for i, j in T.Parallel(M, N):\n                C[i, j] = A[i, j] + B[j % K]\n    return main\n\nA = torch.arange(M * N, dtype=torch.float32, device=\"cuda\").reshape(M, N)\n\ndef run(K):\n    B = (torch.arange(K, dtype=torch.float32, device=\"cuda\") + 1) * 100\n    out = tilelang.compile(build(K), out_idx=[2])(A, B).cpu().numpy()\n    ref = A.cpu().numpy() + B.cpu().numpy()[np.arange(N) % K][None, :]   # B[j % K] per column\n    return int((out != ref).sum())\n\nprint(\"K=7 (7 does NOT divide N=64):\", run(7), \"/ 1024 wrong\")  # -> 512 / 1024 wrong (all rows i>=1)\nprint(\"K=8 (8 divides N=64):       \", run(8), \"/ 1024 wrong\")  # -> 0            (control)\n```\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion; the result is silently wrong and deterministic.\n\n### Expected behavior\n\n`B[j % K]` should read `B[j % K]` for every `(i, j)`, independent of whether `K` divides `N` — as it already does when `K = 8` (divides `N = 64`) and when the inner `j` loop is serial rather than part of the 2-D parallel fusion (both verified correct over the same inputs). Computing `B[j % 7]` correctly seems the natural expectation, since the identical kernel computes the right answer on those sibling configurations.\n\n### Additional context\n\n**Root cause.** TVM's iterator-affine-map simplifier rewrites a *nested* modulo `floormod(floormod(x, c_inner), c_outer)` to `floormod(x, c_outer)`. That identity is sound only when `c_outer | c_inner` — but the rewrite does not check divisibility; instead it gates the fold on whether `PadDividendToDivisor` can *pad* the inner extent up to a multiple of `c_outer`. Padding always succeeds (any extent can be rounded up to a multiple), so it is not a real precondition: it rounds `c_inner=64` up to `70` (a multiple of `7`) and treats that as license to fold. Padding is the wrong test here — it is a mechanism for *splitting* an iterator whose range isn't divisible by a tile (extend the range, mask the extra iterations with a predicate), and it is sound for a monotone quantity (a plain iterator, a `floordiv`) whose range is merely truncated. But `x % 64` is not truncated — it *wraps*: at `x = 64` it returns to `0`, it never takes the padded values `64..69`. Folding on a padded-but-wrapping quantity rewrites the index for the *real* iterations too, so the collapsed `x % 7` is wrong for every `x ≥ 64`. The correct precondition is simply `c_outer | c_inner` (`CanProveDivisible(c_inner, c_outer)`), with no padding — and the same function's `scale != 1` branch already does exactly that (it uses `CanProveDivisible` and bails when it can't prove divisibility); the `scale == 1` branch drops that check and routes through padding instead. `FlattenBuffer` passes every access index through this simplifier at the `Surjective` level (which enables padding), so the legitimate column index `(threadIdx.x % 64) % 7` collapses to `threadIdx.x % 7`.\n\n<details><summary>Where the inner modulo is dropped (pass-by-pass + the folding rule)</summary>\n\nRunning the lowering pipeline stage by stage, the `B` subscript is correct as `B[tx % 64 % 7]` up to and including `NarrowDataType`, and becomes `B[tx % 7]` immediately after [`FlattenBuffer`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/flatten_buffer.cc#L246-L258):\n\n```\n[NarrowDataType]  C[i*2 + tx//64, tx%64] = A[...] + B[tx % 64 % 7]\n[FlattenBuffer]   C_1[i*128 + tx]        = A_1[...] + B_1[tx % 7]\n```\n\n`FlattenBuffer` routes each access index through `IterMapSimplify` at the `Surjective` level. The offending rewrite is in `SplitFloorModConst`, whose own comment states the identity it applies — [iter_affine_map.cc#L2018-L2023](https://github.com/tile-ai/tvm/blob/0e15b274bce8b46f971abf5ac390e844aa6acee5/src/arith/iter_affine_map.cc#L2018-L2023):\n\n```cpp\n// floormod(floormod(floordiv(iter, lower_factor), c1c2), c1)\n// = floormod(floordiv(iter, lower_factor), c1), where c1=rhs\nreturn IterSplitExpr(padded->source,\n                     /* lower_factor = */ padded->lower_factor,\n                     /* extent = */ rhs,\n                     /* scale = */ padded->scale);\n```\n\nThe dividend is *padded* to a multiple of the divisor (`PadDividendToDivisor`) rather than gated on `c1 | c1c2`. Isolating the simplifier confirms the level dependence (with `tx` an iterator of extent 128):\n\n```\niter_map_simplify([tx % 64 % 7], {tx: [0,128)], check_level=Bijective)   -> [tx % 64 % 7]   # correct\niter_map_simplify([tx % 64 % 7], {tx: [0,128)], check_level=Surjective)  -> [tx % 7]        # drops % 64\n```\n\n`Bijective` (level 0) keeps the inner modulo; `Surjective` (level 1, used by `FlattenBuffer`) drops it. The value divergence is 512/1024 (`(tx % 64) % 7 != tx % 7` for `tx >= 64`), matching the runtime wrong-count exactly.\n</details>\n\n<details><summary>Scope of the unsound fold: which nested div/mod shapes it affects (numerically ground-truthed)</summary>\n\nI swept the neighboring nested div/mod fold shapes through `iter_map_simplify`, comparing each simplified form against a re-evaluation of the original over the whole domain (a nonzero mismatch count = an unsound fold). One root cause — the divisibility-blind `floormod(floormod(·), ·)` fold — shows three faces; the sibling shapes with correct handling are unaffected:\n\n| nested shape | result | why |\n|---|---|---|\n| `(x % a) % b` | **unsound** when `b ∤ a` and the domain lets the inner mod wrap (`ext > a`) | the fold under test; `a`, `b` need not be powers of two — `40%7`, `12%5`, `100%9`, `28%6` all misfold |\n| `(i·BN + tx) % N % K` | **unsound** when `K ∤ N` | same fold, the real post-fusion flatten shape (this bug) |\n| `((x % a) % b) % c` | **unsound**, and it *compounds* | same fold applied recursively drops *both* inner bounds, e.g. `x%64%7%2 → x%2` |\n| `(x / a) / b` | sound | `(x/a)/b = x/(ab)` is unconditional for `x ≥ 0` — no divisibility needed |\n| `(x % a) / b` | sound | folds only when the domain proves the mod inactive (`x < a`) |\n| `(x / a) % b` | sound | folds to `x % (ab) // a`, a correct identity that keeps the structure |\n| `(x·s) % m` (scale ≠ 1) | sound | takes the `CanProveDivisible`-guarded branch |\n\nSo the defect is precisely the outer-`floormod`-collapsed-to-single-mod fold on the `scale == 1` path; the divisor (`divdiv`), mixed (`moddiv`/`divmod`), and scaled shapes each either need no divisibility precondition or already enforce one, and stay correct — bounding the fix to that one branch.\n</details>\n\n**Suggested fix.** The `scale == 1` branch of `SplitFloorModConst` should not decide this fold via `PadDividendToDivisor`. The legality of `floormod(floormod(x, c_inner), c_outer) → floormod(x, c_outer)` depends only on `c_outer | c_inner`, so the branch should gate on `CanProveDivisible(c_inner, c_outer)` and keep the nested modulo when it can't prove divisibility — mirroring what the same function's `scale != 1` branch already does. This is a change to the vendored TVM iter-map simplifier (not verified end-to-end); it is confined to that one branch, and the swept sibling shapes above (div/mixed/scaled) show that branch is the only one that folds without a sound divisibility check, so the fix does not disturb the other folds.\n\n**Provenance.** The rewrite lives in the iter-affine-map padding support inherited from upstream TVM (the non-divisible floordiv/floormod iter-map work, 2022-2023 era); the current line in the bundled fork was last touched by an NFC FFI refactor (#18312, 2025-09-13), not introduced by it. Present since TileLang first vendored this TVM; origin PR in TVM not pinned here.\n\n**Dedup.** I searched the open and closed tracker (`floormod`, `IterMap`, `modulo`, `Parallel index`, `FlattenBuffer`) and found no existing report of this nested-modulo miscompile.\n\n**Reach.** The trigger is `B[j % K]` on the inner index of a 2-D `T.Parallel(M, N)` with `K ∤ N` — a common-idiom lookup: per-column bias/table/rotation indexed by a modulus is a standard kernel pattern and the natural way to express it. The 2-D `T.Parallel` fusion is the default for elementwise/epilogue loops, so the fusion+flatten path is ubiquitous; the extra ingredient is `K ∤ N` (whenever `K` divides `N` the fold is legitimate and the result is correct — e.g. `K=8, N=64` above). The dividing case is common because power-of-two tile widths and power-of-two moduli divide each other, so the bug hides behind the frequent all-powers-of-two configuration — but `K`/`N` need not be powers of two for the bug (any non-dividing pair with the inner modulus actually wrapping misfolds, per the sweep above). `grep -rn \"% .*]\" examples/ tests/` finds modulo-indexed reads, but the shipped examples use moduli/extents that divide (so they dodge the boundary); no test exercises a non-dividing modulus inside a 2-D `T.Parallel`, which is why CI is green.\n\n**Impact.** The narrow ingredient is a nested modulo whose divisors are non-nesting — `K ∤ N` inside the fused 2-D parallel loop; power-of-two tile widths and moduli divide and stay correct, so the boundary is only crossed by odd/non-dividing table sizes. When it fires it is a silent wrong-code: the kernel compiles and runs with no error, corrupts a whole output region (512/1024 elements here, every row `i >= 1`), and the values are data-dependent on the `B` table contents rather than one noticeable constant — so it can pass a smoke run and slip into a workload undetected. Gating the fold on `c_outer | c_inner` closes this nested-modulo miscompile class in the iter-map simplifier; it does not otherwise change any correctly-folding case.", "url": "https://github.com/tile-ai/tilelang/issues/2955"}
{"number": 2603, "title": "[BUG][Fuzzer][wrong-code] Sparse WGMMA `T.gemm_sp` with `block_N ≡ 8 (mod 16)` silently miscomputes the whole output tile on sm_90 (and hangs under warp specialization)", "state": "closed", "created_at": "2026-07-10T23:30:32Z", "updated_at": "2026-08-25T09:09:22Z", "closed_at": "2026-08-25T09:09:22Z", "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.11\n\n### System information\n\n\n- Installed: pip (dev build reporting `0.1.11`)\n- GPU: NVIDIA H100 (sm_90a)\n- Python 3.12, `torch 2.12.1+cu130`, CUDA 13\n\n\n### Problem description\n\n\nOn sm_90, a 2:4-structured-sparse WGMMA `T.gemm_sp` whose N tile is congruent to 8 mod 16 (`block_N ∈ {24, 40, 56, 72, …}`) **compiles clean, runs with no error or warning, and returns a silently wrong result** — about 96–97% of the output-tile elements are incorrect. There is no exception and no diagnostic, and the result is deterministic (bit-identical across two runs at the same seed, and reproduced at independent seeds), so this is a compile/layout miscompute, not a race.\n\nThe trigger is `block_N ≡ 8 (mod 16)` **with more than one N column-atom** on the sparse-WGMMA path with warp specialization disabled (`pass_configs={\"tl.disable_warp_specialized\": True}`). `block_N` a multiple of 16 is correct; a single `block_N = 8` (one `n=8` sparse atom, `num_inst_n = 1`) is also correct.\n\nThe same `block_N ≡ 8 (mod 16)` config additionally **hangs at launch** under the default (warp-specialized) pipeline — the kernel launches, the GPU pins at 100%, and it never returns (see the WS-on row below).\n\nA few things suggest `block_N ≡ 8 (mod 16)` is a shape this path is meant to handle rather than a misuse: the admission guard accepts any `N % 8 == 0` (`ComputeWgmmaWarpPartition`, `kNPerWarp = 8`), the sparse macro generator selects an `inst_n = gcd(warp_col_tiles, 256) = 8` atom for it, Hopper sparse WGMMA has native `m64n{24,40,56,72}k32` atoms, and the single-atom `block_N = 8` case already computes bit-exact — so the shape itself is computable and the wrong result is confined to the multi-column `n_dim = 8` unswizzled descriptor. (By contrast the M axis is guarded at `% 16` and `block_M = 8` is cleanly rejected; the N axis is guarded only at `% 8`.)\n\nReproduced on H100 (sm_90a). This is the sparse-WGMMA path only; sm_89 and earlier cannot reach this code.\n\n<details>\n<summary>N sweep, controls, the WS-on hang, and the emitted sparse-WGMMA atom (v0.1.11, H100, fp16 A / int16 metadata / fp32 accum, integer-valued inputs so exact equality is a valid check, each cell run x2)</summary>\n\nKernel: `M=64, K=128, block_M=64, block_K=64, threads=128`, one N-block (`block_N=N`), `Square` policy, `num_stages=2`, `transpose_B=False` (MN-major B).\n\nWS **disabled** (`tl.disable_warp_specialized: True`):\n\n```\nN%16 | N   | mismatches / total   | emitted atom      | verdict\n  0  |  16 |     0 / 1024         | n=16              | CORRECT (bit-exact)\n  0  |  32 |     0 / 2048         | n=32              | CORRECT (bit-exact)\n  0  |  64 |     0 / 4096         | n=64              | CORRECT (bit-exact)\n  0  | 128 |     0 / 8192         | n=128             | CORRECT (bit-exact)\n  8  |   8 |     0 / 512          | n=8, num_inst_n=1 | CORRECT (single atom)\n  8  |  24 |  1491 / 1536         | n=8, num_inst_n=3 | WRONG (det=True)\n  8  |  40 |  2491 / 2560         | n=8, num_inst_n=5 | WRONG (det=True)\n  8  |  56 |  3478 / 3584         | n=8, num_inst_n=7 | WRONG (det=True)\n  8  |  72 |  4450 / 4608         | n=8, num_inst_n=9 | WRONG (det=True)\n```\n\nDeterminism: `N=24` gives 1491/1536 at seed 1234 and 1490/1536 at seed 42; `N=72` gives 4450/4608 at seed 1234 and 4485/4608 at seed 9999 — bit-identical across the two runs at each seed. `num_stages=0` and `num_stages=2` give the identical wrong result (4450/4608 for N=72), so this is stage-independent (a layout/descriptor error, not a pipeline-versioning one). The wrong rows are whole-row wrong, not a tail-drop — e.g. `N=24` row 0: `ref=[96,89,92,77,97,98,105,93]` vs `got=[99,103,95,103,95,74,73,92]`.\n\nControls (bit-exact, `nmis=0`, det): every `N % 16 == 0` (16/32/64/128) and `N = 8` (single `n=8` atom). So the trigger is specifically `n_dim = 8` **with `num_inst_n > 1`**.\n\nA plain `global → shared → global` `T.copy` of a `64×24` and a `64×72` tile is EXACT (`0/3072`, `0/9216`) over the same inputs — so the fault is in the `T.gemm_sp` compute/layout, not in copying B into shared memory.\n\nWS **enabled** (default pipeline, `num_stages=2`), same kernel, exit code of a 75 s-bounded run:\n\n```\nN   | exit | result\n 64 |   0  | mism=0/4096, no hang (CORRECT)\n128 |   0  | mism=0/8192, no hang (CORRECT)\n  8 | 124  | HANG (no result)\n 24 | 124  | HANG (no result)\n 40 | 124  | HANG (no result)\n 56 | 124  | HANG (no result)\n 72 | 124  | HANG (no result)\n```\n\n`N=8` is EXACT under WS-off but HANGS under WS-on, so the deadlock is a distinct face from the compute miscompute — it is a warp-specialized TMA-arrive deadlock on the small, non-swizzleable B tile (the `block_N ≡ 8 (mod 16)` B shared tile has an inner width that can't take a clean swizzle and is routed to the 1-D bulk TMA copy).\n\nEmitted CUDA (the wrong-vs-right atom is directly visible in the kernel source):\n```\nN=24 (block_N ≡ 8 mod 16): tl::wgmma_sp_ss<..., 64, 8, 32, ...>    # n_dim=8 atom, num_inst_n=3\nN=64 (control):            tl::wgmma_sp_ss<..., 64, 64, 32, ...>   # n_dim=64, correct\n```\n</details>\n\n### Reproducible example code\n\n\n```python\nimport tilelang, tilelang.language as T, torch\nfrom tilelang.utils.sparse import compress, get_e_factor\n\nE_FACTOR = get_e_factor(T.float16, T.int16)\n\ndef make_sp_gemm(M, N, K, bM, bN, bK):\n    @T.prim_func\n    def main(\n        A_sparse: T.Tensor((M, K // 2), \"float16\"),      # 2:4-compressed A\n        E: T.Tensor((M, K // E_FACTOR), \"int16\"),        # 2:4 metadata\n        B: T.Tensor((K, N), \"float16\"),                  # dense, MN-major\n        C: T.Tensor((M, N), \"float32\"),\n    ):\n        with T.Kernel(T.ceildiv(N, bN), T.ceildiv(M, bM), threads=128) as (bx, by):\n            As = T.alloc_shared((bM, bK // 2), \"float16\")\n            Es = T.alloc_shared((bM, bK // E_FACTOR), \"int16\")\n            Bs = T.alloc_shared((bK, bN), \"float16\")\n            Cf = T.alloc_fragment((bM, bN), \"float32\")\n            T.clear(Cf)\n            for ko in T.Pipelined(T.ceildiv(K, bK), num_stages=2):\n                T.copy(E[by * bM, ko * bK // E_FACTOR], Es)\n                T.copy(A_sparse[by * bM, ko * bK // 2], As)\n                T.copy(B[ko * bK, bx * bN], Bs)\n                T.gemm_sp(As, Es, Bs, Cf, transpose_A=False, transpose_E=False,\n                          transpose_B=False, policy=T.GemmWarpPolicy.Square)\n            T.copy(Cf, C[by * bM, bx * bN])\n    return main\n\ndef run(N):\n    M, K = 64, 128\n    g = torch.Generator(device=\"cuda\").manual_seed(1234)\n    # exact 2:4 pattern (keep 2 of every 4 K-cols via randn topk, no int ties),\n    # then set the kept values to ints in {1,2} so fp32 accum is exact.\n    r = torch.randn((M, K // 4, 4), generator=g, device=\"cuda\")\n    mask = torch.zeros_like(r, dtype=torch.bool)\n    mask.scatter_(-1, r.topk(2, dim=-1).indices, True)\n    vals = torch.randint(1, 3, (M, K // 4, 4), generator=g, device=\"cuda\").float()\n    A = (vals * mask).view(M, K).half()\n    B = torch.randint(0, 3, (K, N), generator=g, device=\"cuda\").float().half()\n\n    A_sparse, Emeta = compress(A.contiguous(), meta_dtype=torch.int16)\n    kernel = tilelang.compile(\n        make_sp_gemm(M, N, K, 64, N, 64), out_idx=[3],\n        pass_configs={\"tl.disable_warp_specialized\": True})\n    out = kernel(A_sparse, Emeta, B).float()\n    ref = A.float() @ B.float()                 # dense reference over densified A\n    return (out != ref).sum().item(), out.numel()\n\nprint(\"N=64 (mult of 16):\", run(64))    # -> (0, 4096)     CORRECT\nprint(\"N=24 (8 mod 16): \", run(24))     # -> (1491, 1536)  WRONG, silent\n```\n\n### Traceback\n\n```pytb\nNo traceback — with warp specialization disabled the kernel compiles and runs to completion; the result is silently wrong and deterministic, with no error or warning. Under the default (warp-specialized) pipeline the same config hangs at launch and never returns (no diagnostic).\n```\n\n### Expected behavior\n\n\nA `block_N ≡ 8 (mod 16)` sparse GEMM shouldn't silently return a wrong tile (or hang). The shape appears to be in scope — it's admitted by the `N % 8` guard, selects a dedicated `n = 8` sparse atom, maps to native sparse WGMMA `m64n{24,…}k32` atoms, and the single-atom `block_N = 8` case already computes bit-exact — so computing the correct result for the multi-column case as well seems the natural expectation. If that descriptor is hard to fix, a clean compile-time rejection would at least be preferable to a silent wrong result / hang. Either way, running to completion and returning silently-wrong numbers (or deadlocking with no diagnostic) is the problem.\n\n\n### Additional context\n\n\n**Root cause.** The wrong result is a B shared-memory descriptor field that fails to scale with N on the unswizzled (`SWIZZLE_NONE`) layout of the sparse macro generator — structurally the same shape of bug as on the dense path, but in the separately-authored sparse file. Two things line up for `block_N ≡ 8 (mod 16)` on fp16:\n\n1. **A dedicated `n = 8` atom is selected and tiled `num_inst_n > 1` times.** [`_initialize_wgmma_prefix`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/tilelang/cuda/intrinsics/macro/wgmma_sp_macro_generator.py#L89-L102) sets `inst_n = gcd(warp_col_tiles, 256)`, which is `8` when `warp_col_tiles ≡ 8 (mod 16)`; the emit then tiles `num_inst_n = warp_col_tiles // 8` such atoms along N (e.g. 3 atoms for N=24).\n\n2. **The `SWIZZLE_NONE` B-descriptor puts the N-scaled value in the wrong field.** For the MN-major (`b_is_k_major = False`) unswizzled case, [`compute_wgmma_b_desc_params`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/tilelang/cuda/intrinsics/macro/wgmma_sp_macro_generator.py#L183-L211) emits `b_leading_byte_offset = 8 * n_dim * elems_in_bytes` (the correctly N-scaled `8·N`, but deposited in **LBO**) and `b_stride_byte_offset = (0 if n_dim == 8 else 8 * 8 * elems_in_bytes)` — a **constant, N-independent** value in **SBO**. The SBO for a row-major `(K,N)` tile is the stride from the first 8 K-rows to the next 8 K-rows, which should scale with N; hardwiring it to `8*8*elems` is correct only at `n_dim == 8` (the single-atom case, where SBO is `0`). For every wider `N ≡ 8 (mod 16)` the multi-atom emit walks the wrong K-row pitch and each WGMMA reads the wrong shared-memory bytes — a uniform, whole-tile corruption (each output is a well-formed dot over the wrong set of B elements, not a partial sum or a dropped column band). Verified on H100: the emitted descriptor takes the `SWIZZLE_NONE` branch for these N, and the single-atom `block_N = 8` is bit-exact while `block_N = 24/40/56/72` is not.\n\nThe warp-partition helper [`ComputeWgmmaWarpPartition`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/gemm_sp.cc#L182-L194) guards only [`N % 8 == 0`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/op/gemm_sp.cc#L188) (M uses `% 16`), so `N ≡ 8 (mod 16)` is admitted with no reject.\n\nThe **WS-on hang** is a separate face on the same trigger: the `block_N ≡ 8 (mod 16)` B shared tile is not cleanly swizzleable, so under warp specialization its TMA load is routed to the 1-D bulk copy path; the forward-barrier arrival is not emitted on that route and the consumer wait never flips phase, so the kernel deadlocks (this matches the mbarrier-arrive-drop mechanism, reached here via the sparse path and the `block_N` trigger rather than a single-iteration loop).\n\n<details>\n<summary>Possible fixes (proposed, not built) and disposition</summary>\n\nSince `block_N ≡ 8 (mod 16)` is admitted by the `N % 8` guard and the single-atom `block_N = 8` already computes correctly, one direction is to make the multi-column MN-major unswizzled path compute correctly too: the `SWIZZLE_NONE` B-descriptor's SBO would need to scale its K-row pitch with N (reconciling the LBO/SBO roles against the PTX MN-major core-matrix definition). Alternatively, if that path is not intended to carry these shapes, tightening the `N % 8` guard in `ComputeWgmmaWarpPartition` (`gemm_sp.cc`) to `N % 16` would turn the silent wrong answer (and the hang) into a clean compile-time error. Which is appropriate depends on whether the multi-column `n = 8` MN-major sparse path is meant to support `N ≡ 8 (mod 16)` — a call for the maintainers.\n</details>\n\n**Provenance.** The sparse WGMMA macro generator, including the `b_stride_byte_offset = (0 if n_dim == 8 else 8*8*elems)` descriptor line, was added in [#2048](https://github.com/tile-ai/tilelang/pull/2048) (\"[Backend] Refactor gemm_sp\", merged 2026-05-19); `ComputeWgmmaWarpPartition`'s `N % 8` guard moved into `src/cuda/op/gemm_sp.cc` in [#2297](https://github.com/tile-ai/tilelang/pull/2297) (a namespace refactor). Not a regression — the multi-column `n = 8` sparse descriptor has been wrong since the refactored sparse WGMMA path first shipped. The project's own sparse-GEMM tests and the `examples/gemm_sp` example use N tiles that are multiples of 16 (or the single `N = 8`), so the `≡ 8 (mod 16)` multi-atom case is never exercised.\n\n**Related.** The dense `T.gemm` path has the analogous N-axis `n_dim = 8` miscompute filed as [#2593](https://github.com/tile-ai/tilelang/issues/2593) (in the separate `wgmma_macro_generator.py`); this report is the sparse `T.gemm_sp` path, a distinct file and lowering (`src/cuda/op/gemm_sp.cc`, `tilelang/cuda/intrinsics/macro/wgmma_sp_macro_generator.py`, `tilelang/tileop/gemm_sp/`). It is distinct from the dense N-warp-partition band-drop [#2537](https://github.com/tile-ai/tilelang/issues/2537) (there `warp_col_tiles = N // n_warp` floors and leaves a trailing column band unwritten; here `n_warp = 1`, `warp_col_tiles = N` exactly, no floor loss, and the whole tile is corrupted — a layout error, not a coverage gap), from the dense M-axis row-swap [#2522](https://github.com/tile-ai/tilelang/issues/2522) (that is the M axis; this is the N axis), and from the single-iteration WS deadlock [#2566](https://github.com/tile-ai/tilelang/issues/2566) (that fires at trip count 1; the WS-on hang here fires on `block_N ≡ 8 (mod 16)` at any trip count). I searched the open and closed tracker and found no existing report of this silent miscompute (or hang) on the sparse `T.gemm_sp` N-axis `n = 8` path.\n", "url": "https://github.com/tile-ai/tilelang/issues/2603"}
{"number": 3040, "title": "[BUG][Fuzzer][ice-on-valid-code] A width-changing `T.view` of a fragment aborts with an internal layout-inference `ICHECK` instead of a clear diagnostic", "state": "closed", "created_at": "2026-08-17T05:34:08Z", "updated_at": "2026-08-25T07:46:32Z", "closed_at": "2026-08-25T07:46:32Z", "labels": [], "comments_count": 3, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nLinux; NVIDIA L40S (sm_89); CUDA 12.8; PyTorch 2.8.0; tilelang 0.1.13.\n\n### Problem description\n\nReusing one buffer at a different element width is a documented, shipped pattern — `T.view(buf, shape, dtype)` reinterprets the same storage so a kernel can, e.g., read a bf16 input buffer back as an fp32 accumulator without paying for a second allocation (`examples/dsa_sparse_finetune/sparse_mla_bwd.py:167` does exactly this on a shared buffer). `T.view`'s only stated precondition is that the total bit count is preserved.\n\nWhen the same width-changing view is taken on a **fragment** (`T.alloc_fragment`), compilation aborts with an internal `ICHECK`:\n\n```\nInternalError: Check failed: (analyzer_->CanProveEqual(abs(source->scale), 1)) is false\n```\n\nThe view is in-contract: `256 * 32 == 512 * 16 == 8192` bits, so it passes `T.view`'s own `bits_product` assertion. The failure is fragment-specific and count-change-specific:\n\n| target | view | result |\n|---|---|---|\n| `alloc_shared` fp32(256) → fp16(512) | count changes | **compiles, correct** (TESTED, L40S) |\n| `alloc_fragment` fp32(256) → int32(256) | same count, dtype change | **compiles, correct** (TESTED, L40S) |\n| `alloc_fragment` fp32(256) → fp16(512) | count changes | **aborts** (TESTED, L40S) |\n\nThe shared-buffer row is the byte-identical operation on contiguous storage and it compiles and returns the correct bit-reinterpretation. On a fragment, however, the same view is not well-defined: a fragment is a thread-distributed register layout, and (as the layout-axis sweep in Additional context shows) width-changing it produces a **non-injective** map on a real MMA layout — two logical elements collide on one physical register slot. So the right outcome is a clear front-end rejection of a width-changing `T.view` on a fragment, not the internal `ICHECK` it currently aborts with (nor a silent miscompile). The shared path's computability does not carry over to the distributed register layout.\n\nThe pattern is Hopper-independent (a fragment layout-inference step, no arch gate around it); not re-run on other arches.\n\n### Reproducible example code\n\n```python\nimport tilelang as tl, tilelang.language as T, torch\n\ndef build(scope):                       # scope: T.alloc_fragment or T.alloc_shared\n    @T.prim_func\n    def main(A: T.Tensor((256,), \"float32\"), B: T.Tensor((512,), \"float16\")):\n        with T.Kernel(1, threads=32) as _:\n            buf = scope((256,), \"float32\")\n            T.copy(A, buf)\n            v = T.view(buf, (512,), dtype=\"float16\")   # 256*32 == 512*16 bits: passes bits_product\n            T.copy(v, B)\n    return main\n\ndef check(scope):\n    k = tl.compile(build(scope), out_idx=-1)\n    A = torch.randn(256, device=\"cuda\", dtype=torch.float32)\n    B = k(A)\n    ok = torch.equal(B.view(torch.int16), A.view(torch.float16).view(torch.int16))\n    return \"PASS\" if ok else \"FAIL\"\n\nprint(\"shared  :\", check(T.alloc_shared))     # -> PASS\nprint(\"fragment:\", check(T.alloc_fragment))   # -> aborts: CanProveEqual(abs(source->scale), 1) is false\n```\n\n### Traceback\n\n```\ntilelang.transform.LayoutInference()(mod)\n  ...\n  tvm::tl::ParallelOpNode::InferLayout(...)\n  tvm::tl::ParallelOpNode::ValidateCandidateAgainstFragments(...)   # src/op/parallel.cc\n  tvm::tl::ProveFragmentContains(...)                               # src/layout/utils.cc\n  tvm::tl::FragmentNode::Inverse() -> LayoutNode::InverseWithLevel()\n  tvm::arith::InverseAffineIterMap(...)\n  InverseAffineIterMapTransformer::Visit_(const IterSumExpr&)       # src/arith/iter_affine_map.cc:2568\ntvm.error.InternalError: Check failed: (analyzer_->CanProveEqual(abs(source->scale), 1)) is false:\n```\n\n### Expected behavior\n\nA clear front-end error that names `T.view` and the fragment scope, rather than an internal `ICHECK` deep in layout inference.\n\nThe preferable outcome is a **clean rejection**, not a compile. A width-changing view is well-defined on a shared/global buffer because storage there is a plain contiguous byte layout, but a fragment is a *thread-distributed* register layout, and splitting/merging an element across a width change has no well-defined mapping under a non-trivial fragment layout. The layout-axis sweep below makes this concrete: on an **MMA-laid-out** fragment (a `T.gemm` accumulator), the width-changed view produces a **non-injective** loop layout — two distinct logical elements map to the *same* physical register slot (`logical [0,0,0] and [0,1,0] both map to physical [0,0]`). That is not a limitation the compiler could compute around; the operation genuinely has no correct answer on that layout. So the shared path being byte-computable does **not** imply the fragment path should compile — the earlier framing of \"make the fragment path compile like shared\" is withdrawn in favor of a clean front-end rejection of a width-changing `T.view` on a fragment. (`T.view`'s docstring states the only requirement is bit-count preservation, which this input meets syntactically — so the rejection should be an explicit, documented scope restriction, not the bit-count assert.)\n\n### Additional context\n\n**Root cause.** A width-changing `T.view` of a fragment makes the SIMT loop's index map carry a non-unit element stride, and layout inference's affine-map inverter aborts because it only accepts unit strides. This is a two-pass interaction, not a single faulty check.\n\n- *Pass 1 — the `T.view` front-end op* (`tilelang/language/customize.py`) rebuilds a fresh `Tensor` over the source's `.data` and validates only that the total bit count is preserved. It records nothing about the width change: to everything downstream the view is simply a differently-shaped, differently-typed buffer aliasing the same storage. There is no place in a `T.view` for the element-count reindexing (256 fp32 lanes ↔ 512 fp16 lanes) to be captured.\n- *Pass 2 — `LayoutInference`* (`src/transform/layout_inference.cc`, run as `tilelang.transform.LayoutInference()`) then has to assign a fragment (register) layout to the viewed buffer and prove the SIMT copy-out loop is consistent with it. `ParallelOpNode::ValidateCandidateAgainstFragments` calls `ProveFragmentContains`, which inverts the fragment layout via `large_frag->Inverse()`. Because the width-changed view makes the loop→fragment index map carry a **non-unit element stride**, the affine-map inverter's single-component back-propagation — which asserts the source scale is `±1` — fails.\n\nNeither pass is independently wrong: `T.view` has no mechanism to carry the reindexing, and layout inference has no notion that this fragment is a width-changed alias whose access map is non-unit-stride. The abort is the collision of the two. A shared/global buffer never enters fragment layout inference / this inversion step, which is why the byte-identical view compiles there.\n\n<details><summary>where it happens</summary>\n\n- Pass 1: `T.view` builds a new `Tensor` over the source's `.data` guarded only by a bit-count assert — [`tilelang/language/customize.py#L77-L90`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/customize.py#L77-L90).\n- Pass 2 (`LayoutInference`): the SIMT copy-out candidate is validated against the fragment layout in [`src/op/parallel.cc#L650`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L650) (`ParallelOpNode::ValidateCandidateAgainstFragments`), which calls `ProveFragmentContains` — [`src/layout/utils.cc#L464`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/layout/utils.cc#L464) — which inverts the fragment layout at [`src/layout/utils.cc#L506`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/layout/utils.cc#L506) (`large_frag->Inverse()`).\n- The affine-map inverter's single-component (`args.size()==1`) path asserts unit scale — [`3rdparty/tvm` `iter_affine_map.cc#L2568`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/iter_affine_map.cc#L2568): `TVM_FFI_ICHECK(analyzer_->CanProveEqual(abs(source->scale), 1));`. The width-changed view makes that scale non-unit, so the `ICHECK` aborts.\n\n</details>\n\n**Suggested fix.** Reject a width-changing `T.view` of a fragment up front, at the pass boundary, with a front-end diagnostic that names `T.view` and the fragment scope — not the internal `iter_affine_map.cc#L2568` `ICHECK`. The layout-axis sweep below shows the operation is not merely hard to lower but *ill-defined* on a non-trivial fragment layout (an MMA-laid-out accumulator gives a **non-injective** map — two logical elements collide on one physical register slot), so \"teach the inverter to handle the non-unit stride\" is not a viable direction for the layout that matters: there is no correct mapping to compute. The clean rejection is the appropriate outcome; the bit-count assert (which this input passes) is not the place to enforce it, so the check should be an explicit fragment-scope restriction on width-changing views.\n\n**Provenance.** Present since `T.view` first shipped in [#212](https://github.com/tile-ai/tilelang/pull/212) (merged 2025-03-14); not a regression. No review-bot flag found on that PR.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect. [#2609](https://github.com/tile-ai/tilelang/issues/2609) is the nearest neighbor but is a different bug: a cross-dtype view of a **shared** buffer carrying a swizzle that silently reads the wrong slots (`LayoutNode::Reshape` rescale) — a silent wrong-value on shared memory, whereas this is a hard compile abort specific to a **fragment** view via iter-map inversion.\n\n**Impact.** The trigger is narrow: a width-changing (element count-changing) `T.view` whose target is a fragment rather than a shared/global buffer. When it fires it is a compile-time abort surfaced as an internal `ICHECK`, so it is loud and caught immediately at build time — nothing is miscompiled and no wrong value can reach a running kernel. Fixing it either lets this documented, byte-computable reinterpretation compile on the fragment path (as the shared path already does) or replaces the internal assert with a clear front-end diagnostic; the concrete gain is closing that fragment-vs-shared compile disagreement, not unblocking a workload that ships today.\n\n**Reach.** This is a confirmed two-pass interaction (front-end `T.view` × `LayoutInference` fragment inversion), not a single bad check; the honest boundary was mapped with cells run in isolation on L40S / sm_89 / 0.1.13.\n\n*Root, two levels.* (a) **Source-level:** the fragile implementation is the affine-map inverter's single-component path in `3rdparty/tvm` `src/arith/iter_affine_map.cc:2568` — `TVM_FFI_ICHECK(analyzer_->CanProveEqual(abs(source->scale), 1))` — reached only through the fragment layout inversion in `src/layout/utils.cc` (`ProveFragmentContains` → `large_frag->Inverse()`), which in turn is reached only when a **fragment** (register) buffer carrying a **non-unit element stride** must have its layout inferred. `T.view` (`tilelang/language/customize.py:77-90`) is upstream fragile in a different way: it rebuilds a fresh `T.Tensor(shape, dtype, src.data)` guarded *only* by a bit-count assert and carries no record of the width change. The abort is the collision. (b) **Operator-level:** any op that (i) produces an element-count-changing alias over the same storage and (ii) feeds a fragment into SIMT layout inference. `T.view(dtype=...)` is the only front-end op that changes element count on a fragment; `T.reshape` (the byte-identical sibling in the same file, same rebuild + same guard) cannot change dtype so cannot produce the non-unit stride.\n\n*4-axis sweep (all cells RUN on L40S / sm_89 / 0.1.13, one kernel per fresh process):*\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| baseline | `alloc_shared` fp32(256)→fp16(512), copy-out | **compiles, bit-exact PASS** | — (distinct scope, no failure) |\n| baseline | `alloc_fragment` fp32(256)→int32(256) same-count | **compiles, PASS** | — (no count change → no non-unit stride) |\n| baseline | `alloc_fragment` fp32(256)→fp16(512) | **aborts** `iter_affine_map.cc:2568` | yes |\n| baseline | `alloc_fragment` fp16(512)→fp32(256) | **aborts** `iter_affine_map.cc:2568` | yes (symmetric in count direction) |\n| related-type | `alloc_fragment` int32(256)→int16(512) | **aborts**, same ICHECK | yes |\n| related-type | `alloc_fragment` int32(256)→int8(1024) (4:1) | **aborts**, same ICHECK | yes |\n| related-type | `alloc_fragment` fp32(256)→bfloat16(512) | **aborts**, same ICHECK | yes |\n| related-type | `alloc_fragment` fp16(512)→float8_e4m3(1024) | **aborts**, same ICHECK | yes |\n| related-type | `alloc_fragment` int16(512)→int8(1024) | **aborts**, same ICHECK | yes |\n| related-type | `alloc_fragment` 2-D fp32(16×16)→fp16(16×32) | **aborts**, same ICHECK | yes (rank/shape irrelevant) |\n| related-operator | fragment fp32→fp16 view, then **`T.Parallel` compute** on the view (no copy-out) | **aborts**, same ICHECK | yes (not copy-out-specific — any SIMT use of the viewed fragment) |\n| similar-logic | `T.reshape` fragment fp32(16×16)→fp32(256) (same-count rank change, same rebuild code) | **compiles OK** | related but does not fire (reshape can't change element width → no non-unit stride) |\n| related-source | `alloc_shared` fp32→fp16 view + `T.Parallel` compute on view | **compiles OK** | shared scope never enters fragment inversion, even under a compute loop |\n| **related-layout** | fragment carrying an **MMA layout** (a `T.gemm` fp32 accumulator) fp32→fp16 view + copy-out | **aborts — DIFFERENT site**: `Loop layout is not injective … logical [0,0,0] and [0,1,0] both map to physical [0,0]` (not the `scale==1` assert) | same class, **second face** — proves the op is *ill-defined* (non-injective) on a real distributed layout, not just un-invertible by the default-layout path |\n| **related-layout** | fragment with an explicit `T.annotate_layout(T.Fragment(...))` + width view | aborts (LayoutInference) | same class — a non-trivial fragment layout has no well-defined width-split |\n\n*What the sweep pins down.* The trigger is exactly **{fragment scope} × {element-count-changing view} × {any SIMT/`T.Parallel` use}**; remove any one and it compiles. It is **dtype-family-independent** (fp16/bf16/fp8, int8/16/32 all abort), **direction-independent** (widen or narrow), **rank-independent** (1-D and 2-D both abort), and **not tied to copy-out** (an in-place compute loop on the view aborts the same way).\n\nThe **layout axis** is what settles the disposition. The default-layout fragment aborts at the `CanProveEqual(abs(source->scale), 1)` inverter assert; an **MMA-laid-out** fragment (a `T.gemm` accumulator) aborts at a *different* site — `Loop layout is not injective`, reporting two distinct logical elements mapping to one physical register slot. These are two faces of one class (fragment × width-view unsupported), but the second face is the important one: it shows the width-changing view is **ill-defined**, not merely un-invertible, on a genuine thread-distributed layout — there is no correct physical mapping to compute. This is why the disposition is a **clean rejection**, not \"make the fragment path compile like shared\": the shared path's byte-computability does not transfer to a distributed register layout. `T.reshape` (the byte-identical sibling) cannot change element width, so it cannot reach this. **No separately-fileable neighbor was found** — every failing cell is the same class (default-layout and MMA-layout are two faces of it), every passing cell lacks the fragment-width-view ingredient. Kept a single, specific defect with a same-class reach across dtypes/ranks/consumers/layouts.\n\n*Example run (verified this session, not inherited).* The width-changing `T.view` idiom ships in `examples/dsa_sparse_finetune/sparse_mla_bwd.py:167` — `acc_dkv_shared = T.view(KV_shared, shape=[BS // split_store, D], dtype=accum_dtype)` — a bf16(16-bit)→fp32(32-bit) count-changing view on the **`T.alloc_shared`** buffer `KV_shared` (declared line 151). I ran the shipped example verbatim on 0.1.13 (`test_sparse_mla_bwd(...)`, its `__main__` default config). The kernel that contains this view (`bwd`, a `@tilelang.jit` function) **compiles successfully** (`TileLang completes to compile kernel bwd`); the example then fails only at *runtime* on shared-memory allocation (`Failed to set the allowed dynamic shared memory size to 221040` — ~216 KB, over the L40S per-block limit), a hardware-config limit unrelated to this bug and unrelated to the view. So the shipped example exercises cell 1's shared-scope path, which compiles, and **dodges the fragment-specific abort precisely because the view is on a shared buffer, not a fragment**. This is the same conclusion the draft asserted, now confirmed by running it. Searching the v0.1.13 tree, **no shipped example or test takes a width-changing view of a fragment**, so CI stays green. The trigger uses documented API (`T.view`'s `dtype` parameter and its bit-count-only precondition).", "url": "https://github.com/tile-ai/tilelang/issues/3040"}
{"number": 3031, "title": "[Enhancement] Pipeline planner `LOG(FATAL)`s on a legally reused shared buffer (region-overlap check has no liveness); add liveness or at least a clean diagnostic", "state": "closed", "created_at": "2026-08-17T03:26:01Z", "updated_at": "2026-08-25T07:34:14Z", "closed_at": "2026-08-25T07:34:14Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the Issue Tracker that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (reproduced this session on the released `0.1.13` wheel; source cited at the `v0.1.13` tag).\n\n### System information\n\nNVIDIA L40S (`sm_89`), CUDA 12.8, Python 3.13, tilelang 0.1.13. The defect is in the target-independent pipeline-planning pass, so it is not specific to one GPU.\n\n### Problem description\n\nA loop body that loads a shared scratch buffer with a global→shared copy and then overwrites that buffer's region again later in the same iteration — the first value fully consumed before the second write — aborts compilation with `LOG(FATAL) \"Pipeline planning error: Multiple writes to overlapping buffer regions detected\"` as soon as the loop is pipelined (`num_stages >= 1`). The canonical case is reusing one shared scratch buffer for two sequential `T.copy`s, but the second overwriting write need not be a copy (a plain store trips it too). The identical kernel compiles and runs to a correct result with no pipelining (`num_stages = 0`), and the same pattern with two separate buffers (or with disjoint regions) pipelines fine — so the kernel is valid and the buffer reuse is legal. The write-write conflict check (`MayConflict`) is a deliberate over-approximation — it flags any two writes whose regions intersect — but it is purely geometric with no liveness: in this repro the two writes are separated by a `reduce_sum(sh, …)` that reads the *whole* buffer, so the first write's value is provably dead before the second write (straight-line code, no data-dependent control flow). That is a case an over-approximation could resolve, not one that is genuinely undecidable. The concrete ask here is narrower than \"make the planner schedule it\": an internal `LOG(FATAL)` on a legal kernel is the wrong failure mode regardless — at minimum it should be a clean, actionable diagnostic.\n\nTrigger boundary (all re-run this session): `num_stages = 0` → compiles + exact result; `num_stages = 1` and `num_stages = 2` → the `LOG(FATAL)`.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nM, K, block_M, block_K, dtype = 128, 256, 64, 32, \"int32\"\n\n@tilelang.jit(out_idx=[-1])\ndef kern():\n    @T.prim_func\n    def main(A: T.Tensor((M, K), dtype), B: T.Tensor((M, K), dtype), Out: T.Tensor((M,), dtype)):\n        with T.Kernel(T.ceildiv(M, block_M), threads=128) as bx:\n            sh = T.alloc_shared((block_M, block_K), dtype)     # ONE scratch buffer, reused\n            acc = T.alloc_fragment((block_M,), dtype)\n            partial = T.alloc_fragment((block_M,), dtype)\n            T.clear(acc)\n            for k in T.Pipelined(T.ceildiv(K, block_K), num_stages=2):\n                T.copy(A[bx * block_M, k * block_K], sh)       # copy #1 into sh\n                T.reduce_sum(sh, partial, dim=1)\n                for i in T.Parallel(block_M):\n                    acc[i] += partial[i]\n                T.copy(B[bx * block_M, k * block_K], sh)       # copy #2 into the SAME sh\n                T.reduce_sum(sh, partial, dim=1)\n                for i in T.Parallel(block_M):\n                    acc[i] += partial[i]\n            for i in T.Parallel(block_M):\n                Out[bx * block_M + i] = acc[i]\n    return main\n\nA = torch.randint(-5, 6, (M, K), dtype=torch.int32, device=\"cuda\")\nB = torch.randint(-5, 6, (M, K), dtype=torch.int32, device=\"cuda\")\nkern()(A, B)      # InternalError: Multiple writes to overlapping buffer regions detected\n\n# CONTROL — identical body, num_stages=0: compiles and matches the int reference exactly.\n```\n\n### Traceback\n\n```\nInternalError: Pipeline planning error: Multiple writes to overlapping buffer regions\ndetected. Stage 0 and stage 4 are both writing to buffer 'sh' with overlapping regions.\nThis is not supported in pipeline planning.\n```\n\n### Expected behavior\n\nReusing a shared scratch buffer for two copies whose lifetimes don't overlap (each is fully read before the next write) is a normal, memory-saving pattern; it compiles and runs correctly without pipelining. Enabling `T.Pipelined` should either schedule it (treating the two writes as sequential, single-buffer uses) or, if the planner genuinely cannot, reject it with an actionable message — not abort with an internal `LOG(FATAL)`. The `num_stages = 0` run is the reference that a correct target exists.\n\n### Additional context\n\n**Root cause.** The write-vs-write scan in `AnalyzeCopyLastUse` treats *any* geometric region overlap between a copy stage's shared destination and a later stage's write to the same buffer as an unschedulable conflict, and aborts with `LOG(FATAL)` instead of serializing the two writes (or multi-buffering). The overlap test `MayConflict` ([`#L73-L98`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/pipeline_planning.cc#L73-L98)) is a pure `arith::IntSet` intersection with **no lifetime awareness**: it does not account for the first write's value being fully read before the second write, so serially-reused single-buffer patterns look identical to a real hazard.\n\n- SOURCE-level root: the write-write `MayConflict` branch [`src/transform/pipeline_planning.cc#L673-L682`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/pipeline_planning.cc#L673-L682) inside `AnalyzeCopyLastUse` ([`#L644`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/pipeline_planning.cc#L644)). It only runs when the producer `pinfo.IsCopyStage()` ([`#L663`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/pipeline_planning.cc#L663)); a stage is a copy stage only when `ClassifyCopyLikeStage` sees a *global-like source into a shared destination* ([`#L627-L632`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/pipeline_planning.cc#L627-L632)).\n- OPERATOR-level root: any op that classifies as a copy stage (a `T.copy` global→shared, an equivalent `T.Parallel` load-from-global loop into shared, or an im2col/TMA copy) whose shared destination region is later overwritten — by *any* write (a second copy, or a plain store) — with an overlapping region, inside a `T.Pipelined` loop with `num_stages >= 1`.\n\n**Suggested fix.** Two tiers. The full fix is an enhancement, not a one-line guard change: teaching `AnalyzeCopyLastUse` a *liveness* check — when every consumer of the first write precedes the second write (no live read spans it), treat the buffer as a serially-reused single buffer rather than an overlapping-write conflict. That is new analysis capability the pass does not have today, and in the general case (data-dependent control flow, partial reads) it cannot always decide, so the over-approximation would still fall back to conservative in the undecidable cases. **Independent of that enhancement**, the immediate defect is the failure mode: aborting a legal kernel with an internal `LOG(FATAL)` is wrong regardless of whether liveness is added — it should be a clean, actionable frontend error that names the reused buffer and suggests distinct buffers or a higher `num_stages`. So: floor = don't `LOG(FATAL)` (turn it into a diagnostic); ideal = add the straight-line liveness case so this repro (and the common \"load-use-reload same scratch\" pattern) schedules.\n\n**Provenance.** This overlapping-write check was **added by [#2444](https://github.com/tile-ai/tilelang/pull/2444) (\"[Pipeline] Simplify async copy lowering\", 2026-06-24)** — the same file at the parent commit contains no such check (verified locally: the fatal message appears in #2444's diff and not in its parent). So a kernel with this shape may have compiled before #2444; I have **not** build-verified the pre-#2444 behavior, so I grade this a *possible regression introduced at #2444*, not a confirmed one. First shipped in a release at `v0.1.12` (tag dated 2026-07-08, which post-dates #2444) and still live at `v0.1.13`.\n\n**Dedup.** I searched the open and closed Issue Tracker and found no existing report of this pipeline-planning overlapping-write fatal on a serially-reused single buffer; the other pipeline issues concern distinct mechanisms.\n\n**Impact.** When it fires it is a compile-time `LOG(FATAL)` in the target-independent pipeline-planning pass — loud, deterministic, and caught before any code is generated, so it corrupts no output and cannot reach a running workload silently; the cost is that a valid memory-saving kernel refuses to build and the message is an internal fatal rather than an actionable diagnostic. Fixing it either schedules the serial reuse (the `num_stages = 0` result proves a correct target exists) or replaces the fatal with a clean error naming the reused buffer.\n\n**Generalization (root + 4-axis sweep, all cells re-run on 0.1.13 / L40S, one kernel per fresh process).** Two-level root — as in Root cause above: SOURCE = the lifetime-blind write-vs-write `MayConflict` branch in `AnalyzeCopyLastUse`; OPERATOR = a global-like→shared copy stage whose destination is later overwritten with an overlapping region inside a `T.Pipelined` loop.\n\n**Shipped-example check (Part 1).** The draft cites no specific `examples/X`; its only external claim is the #2444 provenance (verified below). To ground the \"distinct buffers avoid it\" boundary against real code, I ran the canonical `examples/gemm/example_gemm.py` **verbatim** (`num_stages = 3`): it compiles and passes (`All check passed.`, latency ~0.032 ms). It **dodges** the bug precisely because it uses *two distinct* shared buffers (`A_shared`, `B_shared`), each written by exactly one `T.copy` — i.e. the shipped double-buffering idiom never reuses one shared buffer across two copies. No shipped example was found that writes the same shared buffer with two overlapping copies in one pipelined loop, so the trigger is not exercised by the example suite; the \"common memory-saving optimization\" framing is a plausible user pattern, not a demonstrated example-suite pattern.\n\n<details><summary><b>4-axis sweep (8 cells)</b></summary>\n\n| Axis | Cell tested | Result | Same-root? |\n|---|---|---|---|\n| related-type (dtype) | reused `sh`, `dtype=float16`, `num_stages=2` | `LOG(FATAL)` \"Stage 0 and stage 4 … buffer 'sh'\" | yes — check is geometric, dtype-agnostic |\n| related-type (region) | two **adjacent** copies into partially-overlapping sub-slices `sh[:,0:32]`/`sh[:,16:48]` | `LOG(FATAL)` \"Stage 0 and stage 1\" | yes — fires with no consumer between the two writes |\n| similar-logic (num_stages) | reused `sh`, `num_stages=1` | `LOG(FATAL)` | yes — boundary is `num_stages >= 1` |\n| related-operator (2nd write) | copy #1 global→shared, then a **non-copy** `T.Parallel` store overwrites the same `sh` region | `LOG(FATAL)` \"Stage 0 and stage 4\" | yes — broadens class: the *second* write need not be a copy |\n| related-operator (1st write) | write #1 = `T.Parallel` load-**from-global** into `sh`, write #2 likewise | `LOG(FATAL)` | yes — a parallel load-from-global loop is also classified as a copy stage |\n| related-type (region, disjoint) | two copies into **disjoint** halves `sh[:,0:32]` / `sh[:,32:64]` | COMPILES OK | boundary — `MayConflict` sees no overlap; not \"same buffer\" but \"overlapping region\" |\n| related-operator (non-copy 1st) | both writes **computed** (`sh[i,j]=acc[i]±k`, not derived from global) | COMPILES OK | boundary — neither stage `IsCopyStage`, so the write-write branch is skipped |\n| related-type (scope) | two copies into the same **fragment** buffer (dst not shared) | COMPILES OK | boundary — `ClassifyCopyLikeStage` requires a *shared* dst |\n\n</details>\n\n**Class boundary (reframed).** The fatal is triggered by three ingredients together: (1) a *producer* stage that classifies as a global-like→shared copy (`T.copy` **or** an equivalent parallel load-from-global loop into a shared buffer), (2) a later *overlapping* write to that same shared region — the later write can be *any* write, not necessarily a copy, (3) inside a `T.Pipelined` loop with `num_stages >= 1`. Dropping any one avoids it: `num_stages = 0` (reference: compiles + exact result), distinct buffers (shipped-gemm idiom), disjoint sub-regions, a non-shared destination, or a producer that isn't a global→shared copy. This is broader than the title's \"two `T.copy`s\" — the second write being non-copy and the first write being a parallel-load loop are the same root. Title and Problem updated to the class.\n\n**Related-source not reached.** The im2col/TMA copy path (`Im2ColOpNode`, [`#L635-L640`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/pipeline_planning.cc#L635-L640)) also sets `copy_stage = true` and would route through the same `MayConflict` fatal, but constructing a reused-buffer conv/im2col repro was not attempted this session — noted as the same root, untested. The sibling read-vs-write branch ([`#L654-L660`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/pipeline_planning.cc#L654-L660)) only bumps `last_use_stmt_index` and does not fatal, so it is a distinct, benign path.\n\n**Reach.** Reusing one shared buffer for successive stage loads is a plausible shared-memory-budget optimization, and `T.Pipelined` with `num_stages >= 2` is the standard way to overlap load/compute in a main loop. The three ingredients together (a global→shared producer, a later overlapping write to that buffer, in a pipelined loop) are what trip it; distinct buffers, disjoint regions, or `num_stages = 0` all avoid it. The shipped example suite avoids it by always using distinct buffers, so the impact is on user kernels that hand-reuse one scratch buffer, not on the examples themselves.", "url": "https://github.com/tile-ai/tilelang/issues/3031"}
{"number": 3033, "title": "[BUG][Fuzzer][ice-on-invalid-code] `T.finalize_reducer` on a reducer never reduced in a `T.Parallel` loop aborts with an internal `bad_optional_access` in `LayoutInference`", "state": "closed", "created_at": "2026-08-17T03:26:31Z", "updated_at": "2026-08-25T07:29:38Z", "closed_at": "2026-08-25T07:25:34Z", "labels": [], "comments_count": 2, "is_pr": false, "body": "\n## Required prerequisites\n\n- [x] Make sure you've read the [documentation](https://tilelang.com). Your issue may be addressed there.\n- [x] Search the [issue tracker](https://github.com/tile-ai/tilelang/issues) to verify that this hasn't already been reported. No related report was found.\n- [x] I have provided a minimal, reproducible example.\n\n## What version of TileLang are you using?\n\n0.1.13 (tag `v0.1.13`)\n\n## System information\n\n- GPU: NVIDIA L40S (sm_89)\n- CUDA: 13.0 (nvcc V13.0.88)\n- PyTorch: 2.13.0+cu130\n- OS: Linux x86_64\n\n## Problem description\n\nA kernel that allocates a reducer with `T.alloc_reducer`, initializes it (`T.clear`/`T.fill`), and then calls `T.finalize_reducer` **without an enclosing `T.Parallel` reduction loop over that reducer** aborts compilation with a raw C++ optional null-dereference in the `LayoutInference` pass:\n\n```\ntvm.error.InternalError: bad_optional_access while inferring layout for op 9 (tl.FinalizeReducerOp) at level Strict\n```\n\nIt surfaces both in a degenerate form (fill + finalize, no reduction at all) and in the realistic form where the reduction into the reducer is performed with `T.reduce` (a fragment reduce op) instead of an explicit `T.Parallel` accumulate loop. The op index (`op 9` / `op 3`) is the only variable part; the `std::optional` access failure gives the user no indication of what is wrong or where in their source.\n\n## Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\nN = 16\n\n@T.prim_func\ndef reduce_kernel(\n    A: T.Buffer((N,), 'float32'),\n    Out: T.Buffer((1,), 'float32'),\n):\n    with T.Kernel(1, threads=32) as (_,):\n        a = T.alloc_fragment((N,), 'float32')\n        r = T.alloc_reducer((1,), 'float32', op='sum')\n        T.copy(A, a)\n        T.clear(r)\n        # reduce into the reducer via T.reduce (a fragment reduce), not a T.Parallel += loop\n        T.reduce(a, r, reduce_type='sum', dim=0, clear=False)\n        T.finalize_reducer(r)\n        Out[0] = r[0]\n\ntilelang.compile(reduce_kernel, out_idx=[1], target='cuda')\n```\n\n<details>\n<summary>Minimal trigger (deletion-tested) — <code>T.reduce</code> is not required</summary>\n\nRemoving `T.reduce` still crashes with the identical `bad optional access`. The minimal trigger is just: a reducer that is opened by `T.fill`/`T.clear` and closed by `T.finalize_reducer`, with no `T.Parallel` loop reducing it in between.\n\n```python\nimport tilelang\nimport tilelang.language as T\n\nN = 16\n\n@T.prim_func\ndef k(A: T.Buffer((N,), 'float32'), Out: T.Buffer((1,), 'float32')):\n    with T.Kernel(1, threads=32) as (_,):\n        r = T.alloc_reducer((1,), 'float32', op='sum')\n        T.fill(r, 0)\n        T.finalize_reducer(r)\n        Out[0] = r[0]\n\ntilelang.compile(k, out_idx=[1], target='cuda')   # -> InternalError: bad optional access\n```\n\nThe intended reducer workflow — `T.alloc_reducer` + `T.clear` + a `T.Parallel` accumulate loop + `T.finalize_reducer` (as in `examples/gemv/example_gemv.py::gemv_alloc_reducer`) — compiles and runs correctly, so the presence of the `T.Parallel` reduction loop is exactly the difference:\n\n```python\n@T.prim_func\ndef ok(A: T.Buffer((128, 64), 'float32'), Out: T.Buffer((128,), 'float32')):\n    with T.Kernel(1, threads=128) as (_,):\n        a = T.alloc_fragment((128, 64), 'float32')\n        r = T.alloc_reducer((128,), 'float32', op='sum', replication='all')\n        T.copy(A, a)\n        T.clear(r)\n        for i, j in T.Parallel(128, 64):\n            r[i] += a[i, j]\n        T.finalize_reducer(r)\n        T.copy(r, Out)\n# compiles; result == 64.0 (correct)\n```\n</details>\n\n## Traceback\n\n<details>\n<summary>Full traceback</summary>\n\n```\nFile \".../tilelang/cuda/pipeline.py\", line 117, in CUDAPassPipelineBodyPrologue\n    mod = tilelang.transform.LayoutInference()(mod)\nFile \".../tvm/ir/transform.py\", line 171, in __call__\n    return _ffi_transform_api.RunPass(self, mod)\n  ...\n  in tvm::tl::LayoutInferencer::Substitute(tvm::tirx::PrimFunc)\n  in tvm::tl::BufferUseDefCollector::Run()\n  in tvm::tl::BufferUseDefCollector::RunInferStep(...)\ntvm.error.InternalError: bad_optional_access while inferring layout for op 9 (tl.FinalizeReducerOp) at level Strict\nthread_bounds=I.Range(0, 32)\nstmt=T.finalize_reducer(T.region(r[0], 2, 1), 0)\n```\n</details>\n\n## Expected behavior\n\nEither a clear frontend/pass diagnostic (e.g. \"`T.finalize_reducer` on a reducer that is never reduced inside an enclosing `T.Parallel` loop\"), or — if reducing into a reducer via `T.reduce` is intended to be supported — a valid layout for the reducer so compilation succeeds. In no case should the compiler abort with an internal `bad_optional_access` null-dereference on frontend-accepted input.\n\n## Additional context\n\n**Root cause.** `T.finalize_reducer` dereferences the reducer's layout unconditionally, but that layout is only ever produced by a `T.Parallel` reduction loop, so with no such loop the dereference is on an empty `Optional` and aborts.\n\n<details>\n<summary>Mechanism</summary>\n\nThe reducer's `Fragment` layout is assigned only inside the layout annotator's `VisitStmt_(ForNode)`, gated on the loop being a parallel loop:\n\n- [`src/transform/layout_reducer.cc#L189-L191`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/layout_reducer.cc#L189-L191) — `should_annotate` requires `!inside_reducer_range_.empty() && !already_annotated_ && op->kind == ForKind::kParallel`. Only then does it build the `Fragment` and record it at [`layout_reducer.cc#L233`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/layout_reducer.cc#L233) (`new_layout_map_.Set(buffer->data, f)`).\n\nWhen the reducer range is opened (by `T.fill`/`T.clear`) and closed (by `T.finalize_reducer`) with no `ForKind::kParallel` loop in between, `new_layout_map_` never gets an entry for the reducer, so no layout reaches the layout map. Later:\n\n- [`src/op/finalize_reducer.cc#L115`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/finalize_reducer.cc#L115) — `FinalizeReducerOpNode::InferLayout` does `layout_map.Set(reducer, layout_args.layout_map.Get(reducer).value());`. `Get(reducer)` returns an empty `Optional`, and the unconditional `.value()` throws `bad_optional_access`. (Sibling `InferLayout` ops using the same idiom — `ReduceOp` at `reduce.cc:209`, `Scan` at `scan.cc:94` — guard it with a `count()` check first; this call site does not.)\n</details>\n\n**Suggested fix.** In `FinalizeReducerOpNode::InferLayout`, check the `Optional` before dereferencing and raise a contextful error (naming the reducer buffer and pointing at the missing `T.Parallel` reduction loop) instead of `.value()`. Equivalently, the layout annotator in `layout_reducer.cc` could detect a reducer range that is opened and finalized without any enclosing `ForKind::kParallel` loop and reject it up front with the same message. This turns an opaque internal crash into an actionable diagnostic.\n\n**Impact.** The reducer allocated but never reduced in a `T.Parallel` loop is an easy shape for a user to write while building up or refactoring a reduction kernel (e.g. temporarily switching from a `T.Parallel` accumulate loop to `T.reduce`, or a not-yet-finished kernel). Instead of a diagnostic that names the missing reduction loop, the compiler aborts with a raw `bad_optional_access` that gives no hint of which primitive or which buffer is at fault, so the failure is hard to localize. It is a compile-time abort, not a silent miscompile, so no wrong results reach silicon.\n\n**Provenance.** The `.value()` dereference in `FinalizeReducerOpNode::InferLayout` and the parallel-loop-gated layout assignment in `ReducerLayoutAnnotator` are both present at the pinned `v0.1.13` SHA; not a recent regression.\n\n**Dedup.** Distinct from the other reducer/LayoutInference reports: `#2408` and `#2623` are silent *wrong-value* bugs on the correct `T.Parallel` reducer path (not a crash); `#2530`/`#2399`/`#2398` are different `LayoutInference` failures (\"vars.size() 2 vs 1\" keep-dim, \"Inconsistent layouts\", \"no available layout found\"); `#2547` is a Hopper warp-specialization pass-ordering regression. None reports `FinalizeReducerOpNode::InferLayout` dereferencing an empty `Optional`.\n\n**Reach.** Root cause is host-side layout inference, independent of GPU architecture (fails before any codegen). Reproduced on L40S (sm_89); expected to reproduce on all targets. Confirmed dtype- and op-independent (sum/max/min, fp32/fp16/int32, `replication=all`/`none` all abort with the identical `bad_optional_access ... (tl.FinalizeReducerOp) at level Strict`; see Generalization).\n\nThe shipped example `examples/gemv/example_gemv.py::gemv_alloc_reducer` (v0.1.13) is the *correct* reducer path — it puts the accumulation in a `T.Parallel(block_M, block_N)` loop (`o_reducer[i1_m] += ...`) before `T.finalize_reducer`, so it does **not** exercise this bug; it is the control for it. Ran verbatim on 0.1.13: `gemv_alloc_reducer.compile(M=1024, N=1024, block_M=128, block_N=128)` compiles (~5s) and `profiler.assert_allclose` passes. The bug appears only when that `T.Parallel` accumulate loop is absent (reduction done via `T.reduce`, or no reduction at all).\n\n<details>\n<summary>Generalization — two-level root + 4-axis sweep, all cells run in isolation on v0.1.13 / L40S</summary>\n\n**Two-level root.**\n- **Source-level root (fragile implementation):** `FinalizeReducerOpNode::InferLayout` at [`src/op/finalize_reducer.cc#L115`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/finalize_reducer.cc#L115) does `layout_args.layout_map.Get(reducer).value()` with **no `count()`/`Optional` guard**. The layout it depends on is written only by `ReducerLayoutAnnotator::VisitStmt_(ForNode)` under the `op->kind == ForKind::kParallel` gate ([`layout_reducer.cc#L189-L191`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/layout_reducer.cc#L189-L191), set at [L233](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/layout_reducer.cc#L233)). No parallel loop → no layout entry → the unguarded `.value()` throws `bad_optional_access`.\n- **Operator-level root (what correlates):** the `tl.FinalizeReducerOp` op itself. Deletion control (minimal case with `T.finalize_reducer` removed) does **not** hit `bad_optional_access` — it falls through to the free-mode `no available layout found` instead — so the empty-`Optional` dereference is specifically finalize_reducer's, not shared by the reducer's other consumers.\n\n**4-axis sweep** (every cell was compiled; `bad_optional_access` = `... while inferring layout for op N (tl.FinalizeReducerOp) at level Strict`):\n\n| Axis | Cell tested | Result | Same-root? |\n|---|---|---|---|\n| (main) | `T.reduce` fragment-reduce into reducer, `op='sum'` fp32 | `bad_optional_access` (op 9) | yes |\n| (minimal) | `T.fill` + `T.finalize_reducer`, no reduction, `sum` fp32 | `bad_optional_access` (op 3) | yes |\n| related-operator | `op='max'` (via `T.reduce`; and minimal) | `bad_optional_access` (op 9 / op 3) | yes |\n| related-operator | `op='min'`, minimal | `bad_optional_access` (op 3) | yes — exhausts `ReducerOp = {sum,max,min}` |\n| related-type (dtype) | `float16`, minimal | `bad_optional_access` (op 3) | yes |\n| related-type (dtype) | `int32`, minimal | `bad_optional_access` (op 3) | yes |\n| related-type (replication) | `replication='none'`, shape `(32,)`, minimal | `bad_optional_access` (op 3) | yes — both `ReducerRepType::ALL` and `NONE` annotator branches |\n| related-source (sibling `.value()`) | `ReduceOp::InferLayout` `reduce.cc:209` `.Get(dst).value()` | guarded by `if (layout_map.count(dst))` above it — **not** the bug | distinct (correctly guarded) |\n| related-source (sibling `.value()`) | `Scan::InferLayout` `scan.cc:94` `.Get(buf).value()` | guarded by `if (layout_map.count(buf))` — **not** the bug | distinct (correctly guarded) |\n| similar-logic (control, OK) | `T.Parallel` accumulate loop, `sum` | compiles, result `64.0` (correct) | n/a — the intended path |\n| similar-logic (distinct) | `T.serial` accumulate loop instead of `T.Parallel` | `Check failed: (min_reg_num < ...) is false: no available layout found` (`layout_inference.cc:1160`, `InferInFreeMode`) | **distinct root** — separate layout-selection failure, not `bad_optional_access` |\n\n**Findings.** The bug is a single mechanism across all three reducer ops (`sum`/`max`/`min`), both float and int dtypes, and both replication modes — the root is host-side and value/dtype-agnostic, so those neighbors are the *same* bug (kept as one report, not filed separately). The two sibling `InferLayout` ops that use the identical `layout_map.Get(x).value()` idiom (`ReduceOp`, `Scan`) both guard it with a `count()` check first, so `FinalizeReducerOpNode::InferLayout` is the *only* unguarded instance — the boundary is narrow and specific to that one call site. The `T.serial` variant is a **distinct, not-filed** adjacent bug (different pass, different message).\n\n**Example run (PART 1).** The only reducer idiom shipped in examples/tests at v0.1.13 is `examples/gemv/example_gemv.py::gemv_alloc_reducer`, which uses the correct `T.Parallel` accumulate loop and therefore **dodges** this bug by construction — ran verbatim on 0.1.13: it compiles and passes `assert_allclose`. It is the control, not a trigger; no shipped example exercises the missing-loop form. Honest note: this means the buggy shape is one a user reaches while editing/refactoring a reducer kernel, not one demonstrated by an existing example.\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3033"}
{"number": 2995, "title": "[BUG][Fuzzer][ice-on-valid-code] A 4-bit (`int4`/`uint4`) access resolving to `lanes == 2` crashes codegen with \"Cannot convert type int4x2 to CUDA type\"", "state": "closed", "created_at": "2026-08-16T06:20:00Z", "updated_at": "2026-08-25T06:48:55Z", "closed_at": "2026-08-25T06:48:55Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13; CUDA 12.8; NVIDIA L40S (sm_89).\n\n### Problem description\n\nA plain element-wise `T.copy` of a 4-bit buffer (`int4` **or** `uint4`) aborts compilation at the CUDA codegen step with `Fatal: Cannot convert type int4x2 to CUDA type!`, at the ordinary default `threads=128`. `int4` is a first-class dtype with a dedicated scalar packed load/store path, and the same copy compiles and round-trips correctly at other thread counts — so this is a codegen gap, not an unsupported operation.\n\nThe vectorizer distributes the 256 elements over 128 threads as 2 `int4` per thread, producing an `int4x2` (8-bit) vector access. `CodeGenTileLangCUDA::PrintType`'s 4-bit `case 4:` has branches for `lanes == 4, 8, 16, 32, 64` but none for `lanes == 2`, so the 2-lane access falls through to the `LOG(FATAL)` else-arm — for **both** the signed (`int4`) and unsigned (`uint4`) arms of that case. Only `lanes == 2` is affected: `threads=64` (4 int4/thread → `int4x4`) and `threads=32` (8 int4/thread → `int4x8`) both compile; the trigger is independent of `N` (any `N / threads == 2` fires it). Not a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nN = 256\n\n# Trigger: element-wise copy of an int4 buffer at the common default threads=128.\n# The vectorizer packs 2 int4 per thread -> an int4x2 access, which\n# CodeGenTileLangCUDA::PrintType has no branch for -> LOG(FATAL).\n@T.prim_func\ndef trigger(A: T.Tensor((N,), \"int4\"), C: T.Tensor((N,), \"int4\")):\n    with T.Kernel(1, threads=128) as bx:\n        T.copy(A, C)\n\n# Control: same copy, threads=32 -> 8 int4/thread -> int4x8, which PrintType\n# DOES handle. Compiles and round-trips (any lane count except 2 works).\n@T.prim_func\ndef control(A: T.Tensor((N,), \"int4\"), C: T.Tensor((N,), \"int4\")):\n    with T.Kernel(1, threads=32) as bx:\n        T.copy(A, C)\n\ntry:\n    tilelang.compile(trigger, out_idx=[1])\n    print(\"trigger: COMPILED\")\nexcept Exception as e:\n    print(\"trigger CRASH:\", str(e).splitlines()[-1])   # -> Cannot convert type int4x2 to CUDA type!\n\nk = tilelang.compile(control, out_idx=[1])\npacked = torch.randint(-128, 128, (N // 2,), dtype=torch.int32).to(torch.int8).cuda()\nout = k(packed)\nob = out.view(torch.int8) if out.dtype != torch.int8 else out\nprint(\"control:\", \"COMPILED + round-trips\" if torch.equal(ob, packed) else \"WRONG VALUE\")\n```\n\nOutput:\n\n```\ntrigger CRASH: Cannot convert type int4x2 to CUDA type!\ncontrol: COMPILED + round-trips\n```\n\n### Traceback\n\n```\n[..] : Fatal: Cannot convert type int4x2 to CUDA type!\ntilelang error: InternalError: Cannot convert type int4x2 to CUDA type!\n```\n\n### Expected behavior\n\nThe copy should compile, like it already does at every other lane count. An `int4x2` is exactly 8 bits, i.e. one byte — the same width `PrintType` already emits `signed char`/`char` for scalar `int4`. Adding a `lanes == 2` branch to the 4-bit case (emitting the 1-byte type the scalar path uses) would let the 2-lane access codegen the same way the 4/8/16/32/64-lane accesses already do.\n\n### Additional context\n\n**Root cause.** `CodeGenTileLangCUDA::PrintType` does not handle a 2-lane 4-bit vector: its 4-bit [`case 4:`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L952) enumerates `lanes == 4, 8, 16, 32, 64` (starting at [L961](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L961)) but has no `lanes == 2`, so an `int4x2` produced by the vectorizer falls through to the [`LOG(FATAL)` else-arm](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L978).\n\n<details><summary>Boundary: which thread count → which lane count, and which compile</summary>\n\nOver 256 `int4` elements, the vectorizer packs `256 / threads` elements per thread:\n\n| threads | int4 / thread | vector type | result |\n|---|---|---|---|\n| 128 | 2 | `int4x2` | **crash** (`Cannot convert type int4x2`) |\n| 64 | 4 | `int4x4` | compiles |\n| 32 | 8 | `int4x8` | compiles |\n| 1 (serial) | 1 | scalar | compiles, round-trips |\n\nOnly `lanes == 2` is missing from the enumeration; every neighbor is handled.\n\n</details>\n\n**Suggested fix.** Add a `lanes == 2` arm to the 4-bit `case 4:` at `codegen_cuda.cc:961` alongside the existing `lanes == 4`..`64` arms, emitting the 1-byte type the scalar branch uses (`signed char`/`char`) (not verified end-to-end).\n\n**Provenance.** The 4-bit vector `PrintType` branches (and the missing `lanes == 2`) predate 0.1.9 — present since the initial codebase migration (`#10`); origin before that is unverified.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this `int4x2` codegen gap.\n\n**Reach.** `int4` is a documented, first-class dtype and int4 quantization is a common idiom — the `dequantize_gemm` example family targets 4-bit weights (`examples/dequantize_gemm/`: `example_dequant_gemm_w4a8.py`, `example_dequant_gemv_fp16xint4.py`, `example_dequant_gemm_fine_grained.py`; the `int4` value appears only as an `out_dtype` *literal* in `examples/dequantize_gemm/quantize/lop3.py:1112`, not as a buffer dtype). **Ran** `example_dequant_gemm_fine_grained.py` verbatim on 0.1.13 (default `test_run_dequantize_gemm()` config) — it **compiles and passes** (`profiler.assert_allclose` OK). It dodges this path because it declares `storage_dtype = T.int8` (verified: line 34) and unpacks 4-bit values arithmetically; `example_dequant_gemv_fp16xint4.py` likewise uses `storage_dtype = T.int8` (line 20). Neither declares an `int4` buffer, so none emits a raw `int4` vector access and CI stays green. The trigger is a raw `int4` buffer copied at `threads=128`; the crash fires whenever the per-thread lane count resolves to exactly 2 (here, `N / threads = 2`). No shipped example exercises the raw-`int4` path.\n\n**Impact.** The trigger is narrow: only a raw `int4` vector access that resolves to exactly `lanes == 2` (every other lane count, and the scalar path, compile), which the packed-int8/lop3 examples never emit. When it fires it is a compile-time `LOG(FATAL)` at codegen — loud and caught immediately, blocking that one kernel from building while corrupting no output and never reaching a running workload silently. Adding the `lanes == 2` arm closes this boundary in the 4-bit `PrintType` enumeration so a legal `int4` copy is no longer refused.\n\n<details><summary>§17 Generalization record</summary>\n\n**Two-level root.**\n- **SOURCE-level.** `CodeGenTileLangCUDA::PrintType`, integer `case 4:` at [codegen_cuda.cc:952](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L952). The narrow-integer cases (`case 1`, `case 4`) enumerate lane counts with an explicit `if/else if` chain and a `LOG(FATAL)` else-arm ([L978](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L978)), rather than a range predicate (`lanes <= N`) the way `case 16`/`case 32` do. Any legal lane count the vectorizer emits that is not in the enumeration falls through to FATAL. `case 4` enumerates `{4,8,16,32,64}` — the hole is `lanes == 2`.\n- **OPERATOR-level.** Op-independent: the `int4x2` vector type is produced by the *vectorizer* from `N / threads`, not by `T.copy`. Any data-movement / elementwise op over a raw `int4` buffer that resolves to 2 elems/thread routes through the same `PrintType` FATAL (confirmed with `T.Parallel` below).\n\n**4-axis sweep (each a real cell, fresh python process, fresh cache, run on 0.1.13 / L40S):**\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| found-first | `int4` copy `threads=128`, `N=256` → `int4x2` | **crash** `Cannot convert type int4x2` | — |\n| related-type | `uint4` copy `threads=128` → `uint4x2` | **crash** `Cannot convert type uint4x2` | **SAME** — `case 4` uint arm, identical missing `lanes==2` hole |\n| related-type | `int8` copy `threads=128` → `int8x2` | compiles + round-trips OK | **distinct** — `case 8` else-arm emits `char` (a fallback, not `LOG(FATAL)`) |\n| related-type | `bool` copy `threads=128` → 2-lane bool | compiles | **distinct** — routes via `is_vector_bool()` → `ushort2` (L919), never reaches integer `case 1`; the `case 1` `{8,16,32}` hole is unreachable this way |\n| related-source | `int4` copy `N=64 threads=32` → `int4x2` | **crash** `Cannot convert type int4x2` | **SAME** — proves root is `lanes==2`, not the specific `N` |\n| related-operator | `int4` `T.Parallel` elementwise write `threads=128` | **crash** `Cannot convert type int4x2` | **SAME** — op-independent; root is `PrintType` codegen, not `T.copy` |\n\nOriginal adjacent-lane cells (all **distinct**, the enumeration handles them): `threads=64`→`int4x4` compiles (`lanes==4` present); `threads=32`→`int4x8` compiles+round-trips (`lanes==8` present); `threads=1`→scalar compiles+round-trips.\n\n**Class.** The class is \"narrow-integer `PrintType` lane-enumeration with a FATAL fall-through\": it fires for **both `int4` and `uint4`** at `lanes==2` (same case, both arms), so the title/problem is reframed from the single `int4` face to the 4-bit face. It does **not** generalize to `int8` (fallback else-arm, no FATAL) or to `bool` (different code path). The 1-bit `case 1` shares the same fragile idiom (enumerates `{8,16,32}`, FATAL else, missing `lanes ∈ {2,4}`) but is **unreachable via a `bool` buffer** (bool vectors are diverted to `is_vector_bool`), so it is recorded as a latent same-shape sibling, **not** a confirmed reproducer — **distinct, not filed**.\n\n**Same-root sibling draft.** B045 is a near-duplicate face of the **same** 4-bit `int4x2`/`case 4` gap. Recorded as same-root; **not merged** (kept as distinct drafts).\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/2995"}
{"number": 2994, "title": "[BUG][Fuzzer][ice-on-valid-code] Width-2 auto-vectorized `int4`/`uint4` store crashes with `Cannot convert type int4x2 to CUDA type!` instead of compiling", "state": "closed", "created_at": "2026-08-16T06:19:59Z", "updated_at": "2026-08-25T06:48:55Z", "closed_at": "2026-08-25T06:48:55Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\n- TileLang 0.1.13, Python 3.13, Linux.\n- GPU: NVIDIA L40S (compute capability 8.9, sm_89). The defect is in the CUDA type printer (`src/cuda/codegen/codegen_cuda.cc`), which is architecture-independent — the crash is at code-generation time, before any arch-specific lowering.\n\n### Problem description\n\nA plain `T.copy` between two `int4` (4-bit) buffers aborts at compile time with\n\n```\nInternalError: Cannot convert type int4x2 to CUDA type!\n```\n\nThe kernel is legal: `int4` is a supported element dtype, and `T.copy` auto-vectorizes the transfer (here to a width-2 vector, `int4x2`). The CUDA type printer's `int4` branch has cases for vector widths 4, 8, 16, 32, and 64, but no case for width 2, so a width-2 vectorized `int4` transfer falls through to a fatal `LOG(FATAL)` instead of printing a type.\n\nThe width chosen is a function of the loop shape, so the crash is data-shape-dependent: 256 elements over 128 threads vectorizes to width 2 and crashes; the same copy over 64 or 32 threads vectorizes to width 4 (`int16_t`) or 8 (`int`) and compiles. A scalar (non-vectorized) `int4` copy also compiles and round-trips correctly, and the identical vectorized `T.copy` on the sibling `int8` dtype compiles — so this is specific to the missing width-2 `int4` type mapping, not to `T.copy` or to sub-word dtypes in general. The crash is **not `T.copy`-specific**: an element-wise `T.Parallel` assignment over the same `int4` buffer at the same thread count also aborts with the identical `int4x2` error, and the unsigned sibling `uint4` crashes the same way — see §17. It is the auto-vectorizer folding an `int4` store loop to width 2, times the missing `int4x2` case in the CUDA type printer.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nN = 256\n\n# int4 (4-bit) buffers. T.copy auto-vectorizes; at 128 threads over 256\n# elements the transfer becomes width-2 (int4x2), which the CUDA type\n# printer has no case for -> compile-time crash.\n@T.prim_func\ndef crash(A: T.Tensor((N,), \"int4\"), C: T.Tensor((N,), \"int4\")):\n    with T.Kernel(1, threads=128) as bx:\n        T.copy(A, C)\n\n# Control: identical copy, scalar (threads=1, serial loop) -> not vectorized,\n# uses the scalar int4 load/store helpers, compiles and round-trips.\n@T.prim_func\ndef ok(A: T.Tensor((N,), \"int4\"), C: T.Tensor((N,), \"int4\")):\n    with T.Kernel(1, threads=1) as bx:\n        for i in T.serial(N):\n            C[i] = A[i]\n\npacked = torch.randint(-128, 128, (N // 2,), dtype=torch.int32).to(torch.int8).cuda()\n\nk = tilelang.compile(ok, out_idx=[1])\nout = k(packed)\nprint(\"control (scalar) round-trips:\", torch.equal(out.view(torch.int8), packed))  # -> True\n\nk = tilelang.compile(crash, out_idx=[1])   # -> InternalError: Cannot convert type int4x2 to CUDA type!\n```\n\n<details><summary>Boundary: which vector widths compile vs crash (same kernel, thread count varied)</summary>\n\n| threads | int4 elems/thread → vector | CUDA type printed | result |\n|---|---|---|---|\n| 32  | `int4x8`  | `int`      | compiles |\n| 64  | `int4x4`  | `int16_t`  | compiles |\n| 128 | `int4x2`  | (no case)  | **crash** |\n\nThe width-2 case is the only one in the 2–64 range with no mapping. The `int8` sibling at the same shape compiles and round-trips.\n\n</details>\n\n### Traceback\n\n```\n[..] : Fatal: Cannot convert type int4x2 to CUDA type!\nInternalError: Cannot convert type int4x2 to CUDA type!\n```\n\n### Expected behavior\n\nThe width-2 `int4` copy should compile, the same as widths 4/8/16/32/64 already do. `int4x2` is 8 bits and can be printed the same way width-4 (`int4x4` → `int16_t`) and width-8 (`int4x8` → `int`) are printed as a wider integer holding the packed nibbles; the width-4 path already round-trips correctly on the same kernel. Alternatively, if width-2 vectorization of `int4` is not intended to be supported, the copy vectorizer could avoid choosing width 2 for `int4`, so the type printer is never asked to emit `int4x2`.\n\n### Additional context\n\n**Root cause.** `PrintType` in the CUDA code generator does not handle a width-2 vector of the 4-bit `int4` dtype, so it emits a fatal error instead of a type. In [`CodeGenTileLangCUDA::PrintType`, `case 4:`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L952-L978) the `int4` branch enumerates `lanes()` 4, 8, 16, 32, and 64 and otherwise hits [`LOG(FATAL) << \"Cannot convert type ... to CUDA type!\"`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L978); there is no `lanes() == 2` case. The other sub-word integer branch (`case 8:` for `int8`) instead `break`s and falls through to the generic width-2..4 tail (`os << lanes`), which is why `int8` at the same shape compiles while `int4` aborts.\n\n**Suggested fix.** Add a `lanes() == 2` case to the `int4` branch of `PrintType` (a width-2 `int4` is a single byte; print it alongside the [existing width-4 case](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L961)). If width-2 `int4` vectorization is not intended to be supported, the alternative is to gate the copy vectorizer away from width 2 for `int4`.\n\n**Provenance.** The `int4` branch's missing width-2 case predates the [#2297](https://github.com/tile-ai/tilelang/pull/2297) refactor (merged 2026-06-02) that relocated this printer into TileLang's own `codegen_cuda.cc`; #2297 only moved the code, it did not write the width enumeration. The gap has been present since the `int4` type mapping was added; reproduced on 0.1.13, and the branch is unchanged on `main`.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect. Distinct from [#2526](https://github.com/tile-ai/tilelang/issues/2526): that is a runtime `misaligned address` crash from an unconditional 16-byte load emitted for the CUDA `int4` **vector type** (four `int32`s) over a `make_tensor` view, rooted in `loop_vectorize.cc`. This is a compile-time type-printer crash for the 4-bit `int4` **dtype** vectorized to width 2.\n\n**Reach.** `int4` is an accepted element dtype (`tilelang/language/dtypes.py`: `\"int4\": \"Int4\"`, `int4 = dtype(\"int4\")`) and `T.copy` is a core op documented to move whole buffers; the crash is also reachable via a plain `T.Parallel` element-wise store (§17), so it is not gated on `T.copy`. In this repo's shipped code the actual `int4` *dtype* is otherwise unused as a buffer element type: `grep -rn '\"int4\"' examples testing` at v0.1.13 returns a single hit — a docstring mention in `examples/dequantize_gemm/quantize/lop3.py` (`\"int4\"` as a *quantization format string*, decoded via lop3 into fp16, not a `T.Tensor(..., \"int4\")` buffer). The other `int4` sites (`examples/bitnet-1.58b/...`, `examples/dequantize_gemm/example_dequant_gemm_fine_grained.py`) store int4 *values packed inside int8 buffers* and never instantiate an `int4`-dtype buffer or copy one. So **no shipped example or test exercises a vectorized `int4`-dtype copy** — verified by running (PART-1, §17): there is no example to run that hits this path, which is why CI is green. Given an actual `int4` buffer, the crashing width-2 vectorization is selected by the common default of one element more than a byte per thread (256 elements / 128 threads); other thread counts dodge it by landing on width 4 or 8.\n\n**Impact.** The trigger is narrow: an `int4` buffer whose copy loop shape happens to vectorize to exactly width 2 (e.g. 256 elements over 128 threads), while nearby thread counts land on the mapped widths 4/8. When it fires it is a compile-time `LOG(FATAL)` — the kernel fails to build loudly and immediately, corrupting no data and never reaching a running workload silently; a data-shape change (thread count) makes it appear or disappear but the failure is always a hard abort, never a wrong value. Fixing it closes the width-2 hole in the `int4` type printer so that the full 2–64 vectorization range compiles as widths 4/8/16/32/64 already do.\n\n<details><summary>§17 Generalization record</summary>\n\n**Two-level root.**\n- **SOURCE-level root:** `CodeGenTileLangCUDA::PrintType`, `case 4:` (the 4-bit `int` dtype-code branch) in `src/cuda/codegen/codegen_cuda.cc`. It enumerates `lanes()` ∈ {4, 8, 16, 32, 64} and any other lane count falls through to `LOG(FATAL) << \"Cannot convert type ... to CUDA type!\"`. There is no `lanes()==2` case and (unlike `case 8:`/`case 16:`) no `break` into the generic width-2..4 tail (`os << lanes`). The sign bit is not consulted, so signed and unsigned share the hole.\n- **OPERATOR-level root:** the auto-**vectorizer** (`loop_vectorize.cc`) folding an `int4` store loop to width 2 whenever the loop shape (elems/thread) lands on 2. Any op whose store loop the vectorizer folds to `int4x2` reaches the printer hole — this is not tied to `T.copy`.\n\n**PART 1 — example/doc-claim run result.** The draft's only Reach claim is a *negative* one (no shipped example exercises a vectorized `int4`-dtype copy → CI green). Verified by running: `grep -rn '\"int4\"' examples testing` at v0.1.13 returns exactly one hit, a docstring in `examples/dequantize_gemm/quantize/lop3.py` where `\"int4\"` is a **quantization format string** (decoded via lop3 into fp16), not a `T.Tensor(..., \"int4\")` buffer. The other `int4` mentions (`examples/bitnet-1.58b/...`, `examples/dequantize_gemm/example_dequant_gemm_fine_grained.py`) pack int4 values inside `int8` buffers and never instantiate an `int4`-dtype buffer. There is therefore **no shipped example that runs this path** — nothing to execute that would hit or dodge the bug; the negative Reach claim is confirmed by the source search, not by a passing example. (The draft's own repro and its scalar control were re-run at 0.1.13, below.)\n\n**Found-first cell (re-run at v0.1.13).** `int4` buffer, `T.copy(A, C)`, `threads=128`, `N=256` → `int4x2` → `InternalError: Cannot convert type int4x2 to CUDA type!`. Still reproduces.\n\n**4-axis sweep (each a fresh process, fresh cache, at v0.1.13; observed, not asserted):**\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| baseline | `int4` `T.copy`, threads=128, N=256 | `raised: Cannot convert type int4x2 to CUDA type!` | — (found-first) |\n| related-type (sign) | `uint4` `T.copy`, threads=128, N=256 | `raised: Cannot convert type uint4x2 to CUDA type!` | **same** — `case 4:` ignores sign at width 2 |\n| related-operator | `int4` **`T.Parallel`** element-wise store, threads=128, N=256 | `raised: Cannot convert type int4x2 to CUDA type!` | **same** — same vectorizer→printer path, not `T.copy`-specific |\n| related-type (scope) | `int4` `T.copy` **via `alloc_shared`**, threads=128, N=256 | `raised: Cannot convert type int4x2 to CUDA type!` | **same** — scope is not a mitigating knob |\n| related-source (sibling case) | `int8` `T.copy`, threads=128, N=256 | `compiled`; round-trips `True`; generated source emits `char2` | **distinct** — `case 8:` `break`s into the `os << lanes` tail (→ `char2`); the hole is `case 4:`-only |\n| related-type (width up) | `int4` `T.copy`, threads=64, N=256 (→ `int4x4`) | `compiled` | N/A — mapped branch (`lanes()==4`→`int16_t`); width-2-specific hole |\n| related-type (width up) | `int4` `T.copy`, threads=32, N=256 (→ `int4x8`) | `compiled` | N/A — mapped branch (`lanes()==8`→`int`) |\n| vectorization off | `int4`, threads=1, serial loop | `compiled` + round-trips `True` | N/A — scalar path never asks `PrintType` for a vector `int4` |\n| similar-logic (latent) | `bool`/`int1` `T.copy`, threads=64 N=256 and threads=128 N=512 | `compiled` (both) — source shows scalar `bool`/`char`, no wide-lane vector | **latent sibling, unreached** — see below |\n\n**Reframe.** Broadened from \"`T.copy` of `int4`\" toward the class **\"any auto-vectorized store of an `int4`/`uint4` buffer that folds to width 2\"**: signed and unsigned both crash, and `T.Parallel` (not just `T.copy`) reaches it, so the trigger is the *vectorizer picking width 2 for a 4-bit dtype*, not the op. Kept the width-2 boundary specific (widths 4/8/16/32/64 all compile). One fix (add `lanes()==2` to `case 4:`) closes it for all these faces.\n\n**Similar-logic sibling `case 1:` (int1/bool) — same-shaped source hole, could NOT reach via this surface.** [`case 1:`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L935-L951) has the identical construct: it enumerates `lanes()` ∈ {8, 16, 32} and every other lane count (incl. 2 and 4) falls through to the same `LOG(FATAL)` with no `break`. It is a real source-level twin of the `int4` hole. But I could not reach it through the copy/parallel vectorizer at v0.1.13: bool copies at shapes that would demand width 2/4 compiled, and the generated source used scalar `bool`/`char` (the vectorizer does not select an unmapped bool lane count here). Recorded as a **latent same-shaped hole, not independently reproduced** — honest: it shares the source pattern but I have no live trigger for it via `T.copy`/`T.Parallel`.\n\n**Same root as sibling draft B045↔B113.** B113 is a near-duplicate FACE of this same `int4x2` `PrintType` gap (same `case 4:` missing `lanes()==2`, same `LOG(FATAL)`), reached via a different surface. Same root cause — NOT merged here; recorded per project policy so the two faces are traceable to one fix.\n\n**Distinct from #2526** (per Dedup above): #2526 is a runtime misaligned-address crash from a 16-byte load over the CUDA `int4` vector type (four `int32`s), rooted in `loop_vectorize.cc` — a different mechanism (runtime, wrong address) from this compile-time type-printer fall-through. Distinct, not filed here.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/2994"}
{"number": 2913, "title": "[Feature Request] Add TileLang support to Compiler Explorer (Godbolt)", "state": "closed", "created_at": "2026-08-07T05:55:05Z", "updated_at": "2026-08-25T06:11:22Z", "closed_at": "2026-08-25T06:11:22Z", "labels": ["enhancement", "help wanted"], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and did not find an existing issue requesting TileLang support in Compiler Explorer.\n\n### Motivation\n\n[Compiler Explorer](https://godbolt.org/) provides an interactive way to experiment with code and inspect compiler output in the browser. It already supports GPU programming systems such as Triton and CuTe DSL.\n\nAdding TileLang support would make it easier to:\n\n- experiment with small TileLang kernels without setting up a local environment;\n- inspect useful compiler outputs and transformations;\n- compare TileLang versions or supported targets;\n- share reproducible examples through Compiler Explorer links;\n- help new users learn TileLang and help contributors investigate compiler behavior.\n\nIssue #1895 discusses pass/IR dumping that could also be useful in a Compiler Explorer context, but this request is broader: making TileLang available as a supported language/compiler on the platform.\n\n### Solution\n\nAdd TileLang support to Compiler Explorer so that users can enter a self-contained TileLang example and inspect useful compilation results in the browser.\n\nThe implementation approach is intentionally left open. Contributors and maintainers familiar with TileLang, TVM, and Compiler Explorer may be able to identify a better design than any solution proposed upfront.\n\n### Alternatives\n\nUsers can currently run TileLang locally and inspect generated output using its existing debugging and inspection facilities. However, that does not provide the same discoverability, convenience, version comparison, or shareable browser-based experience.\n\n### Additional context\n\n- Compiler Explorer: https://godbolt.org/\n- Compiler Explorer repository: https://github.com/compiler-explorer/compiler-explorer\n- Related TileLang IR-dump request: #1895\n\nContributions and design suggestions from the community would be very welcome.", "url": "https://github.com/tile-ai/tilelang/issues/2913"}
{"number": 3001, "title": "[BUG][Fuzzer][ice-on-valid-code] Constant int32 `1 << 31` aborts compile in const-fold instead of wrapping to INT32_MIN", "state": "closed", "created_at": "2026-08-16T06:20:21Z", "updated_at": "2026-08-25T05:43:57Z", "closed_at": "2026-08-25T05:39:12Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar issue was found.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nLinux, NVIDIA L40S (sm_89), CUDA 13.0, PyTorch 2.13.0. The defect is in constant folding (host-side, `target=\"cuda\"`) and is architecture-independent — it aborts before any device code is generated.\n\n### Problem description\n\nA left shift by a compile-time-constant amount whose folded result lands in the top bit of a signed integer aborts compilation with an internal `IntImm` range assert, instead of producing the wrapped constant. `(T.int32(1) << T.int32(31))` — a compile-time-constant `0x80000000` / `INT32_MIN` — crashes the compiler:\n\n```\nCheck failed: value < 1LL << (dtype.bits() - 1) (2147483648 vs. 2147483648)\n  : Literal value 2147483648 exceeds maximum of int32\n```\n\nThe same operation is well-defined and works on every non-folding path:\n\n| form | `1 << 31`, `int32` | result |\n|---|---|---|\n| **constant amount** `T.int32(1) << T.int32(31)` | **compile abort** (range check) | — |\n| runtime amount `T.int32(1) << S[i]` (`S[i]==31`) | compiles | `-2147483648` (`0x80000000`) ✓ |\n| constant amount, `uint32` | compiles | `2147483648` ✓ |\n| constant amount `1 << 30`, `int32` | compiles | `1073741824` ✓ |\n\nThe controls agree with NumPy and PyTorch, both of which return `-2147483648` for `int32(1) << 31` (they wrap, they don't reject). Note the `uint32` row compiles because `uint32` is *not* an index type, so its shift is never const-folded (a `shift_left` intrinsic is emitted); only signed `int32` both folds *and* overflows the constructor. So only the *constant-folded signed `int32`* path crashes; the value is computable and its wrapped result is well-defined.\n\nThis is not a regression — the folding logic predates the vendored TVM fork (see Provenance).\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\nimport torch\n\nN = 256\n\ndef build(shift):\n    @T.prim_func\n    def p(A: T.Tensor((N,), \"int32\"), C: T.Tensor((N,), \"int32\")):\n        with T.Kernel(1, threads=N) as _:\n            i = T.get_thread_binding()\n            C[i] = A[i] + (T.int32(1) << T.int32(shift))   # shift amount is a compile-time constant\n    return tilelang.compile(p, out_idx=[1], target=\"cuda\")\n\na = torch.zeros(N, dtype=torch.int32, device=\"cuda\")\n\n# control: shift = 30 -> folds to 1073741824, fits int32 -> compiles\nk = build(30); print(\"shift=30:\", int(k(a).cpu()[0]))   # -> 1073741824\n\n# bug: shift = 31 -> const-fold builds IntImm(int32, 1<<31 = 2147483648) -> range check aborts compile\nk = build(31); print(\"shift=31:\", int(k(a).cpu()[0]))   # want INT32_MIN = -2147483648; instead: compile aborts\n```\n\n### Traceback\n\n```\nFile \"repro.py\", line 9, in p\n    C[i] = A[i] + (T.int32(1) << T.int32(shift))\nFile \".../tvm/python/tvm/tirx/expr.py\", line 128, in __lshift__\n    return _ffi_api.left_shift(self, other, None)\nFile \"<unknown>\", line 0, in tvm::left_shift(tvm::PrimExpr, tvm::PrimExpr, tvm::Span)\nFile \".../3rdparty/tvm/src/ir/expr.cc\", line 73, in tvm::IntImm::IntImm(tvm::DataType, int64_t, tvm::Span)\nValueError: Check failed: value < 1LL << (dtype.bits() - 1) (2147483648 vs. 2147483648) : Literal value 2147483648 exceeds maximum of int32\n```\n\n### Expected behavior\n\nCompile and produce the wrapped constant — `int32(1) << 31 == -2147483648` (`0x80000000`), matching TileLang's own runtime-shift path, its `uint32` path, and NumPy/PyTorch. The wrapped low `bits` of the fold are the well-defined result.\n\n### Additional context\n\n**Root cause.** `left_shift`'s constant-folding path builds the result `IntImm` directly from the raw wide product, without narrowing it to the result dtype first. When both operands are constants, [`op.cc:792`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L792) does `return IntImm(rtype, (pa->value << pb->value), span)`. For `rtype=int32`, `1LL << 31 == 2147483648`, which is outside the signed-int32 range, so the `IntImm` constructor's range check [`expr.cc:73`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/ir/expr.cc#L73) (`TVM_FFI_CHECK_LT(value, 1LL << (dtype.bits() - 1), ValueError)`) fires. The preceding `pb->value < rtype.bits()` guard (shift `31 < 32`) passes, so the shift amount itself is legal; it is the folded *value* that overflows the dtype at construction. Note that this range branch is gated on `dtype.bits() < 64`, so a 64-bit result dtype takes a different path (see §17).\n\n<details><summary>Why the sibling fold paths don't crash</summary>\n\nThe arith constant-fold helper [`GetFoldResultInt64Repr` (`const_fold.h:81`)](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L81) already narrows a fold result to the dtype before constructing the `IntImm` — it masks the low `bits` (`x &= (1LL << dtype.bits()) - 1`) and sign-extends for signed types (`x = (x ^ m) - m`). That helper is what `Add`/`Mul`/etc. route their folds through. `left_shift` in `op.cc` bypasses it and hands the raw `int64_t` product straight to `IntImm`, so it is the one integer fold that does not narrow.\n\n- `uint32`: the fold is never entered — the fold body is gated on `arith::IsIndexType`, which is `type.is_int()` (signed only) `&& bits ∈ {32,64}`, so `uint32` takes the non-fold path and a `shift_left` intrinsic is emitted; the device computes `2147483648`. (Even if it did fold, the unsigned `IntImm` range `[0, 2^32)` would accept the value.)\n- `int8`/`int16`: also never fold — `IsIndexType` requires bits 32 or 64, so these emit a `shift_left` intrinsic (verified in §17).\n- runtime shift amount: `pa && pb` is false, so the fold is skipped entirely and a `shift_left` intrinsic is emitted; the device computes the wrapped value.\n</details>\n\n**Suggested fix.** Narrow the fold result to the result dtype before building the `IntImm` — e.g. route the `left_shift` constant fold through the same `GetFoldResultInt64Repr` masking/sign-extension that the other integer folds use, so `int32(1) << 31` yields `IntImm(int32, -2147483648)`. (Not built end-to-end; the mask helper already exists in the same tree.)\n\n**Provenance.** The fold lives in the vendored TVM fork (`3rdparty/tvm`, submodule `tile-ai/tvm` at `8df8ebd6`). The `left_shift` fold predates the fork (the file carries apache/tvm history back to 2020), so this is inherited-upstream, not a TileLang regression.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of the left-shift fold overflow. It shares its general root with #2982 (`FloatImm`/`IntImm` range-check on an un-narrowed value in the same `expr.cc`) — same mechanism, different sink (`FloatImm` in `T.fill` there, the `left_shift` `IntImm` fold here). These are distinct sinks in different operators and are not a merge; the shared root is recorded in §17.\n\n**Reach.** The trigger is a compile-time-constant left shift into the sign bit of **signed `int32`** — `T.int32(1) << 31`. The fold only runs for *index* dtypes (`arith::IsIndexType` = signed int of exactly 32 or 64 bits, [`const_fold.h:81`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L81)), and the `expr.cc:73` signed range check only fires for `bits < 64`; so of the index dtypes only `int32` both folds *and* overflows the constructor. `int8`/`int16` do **not** fold at all (non-index width → a `shift_left` intrinsic is emitted, verified in §17), and `int64` folds but skips the range check (`bits==64`), so the boundary is exactly `int32`. Building `INT32_MIN`, a top-bit mask, or `0x80000000` as a literal is ordinary integer code, and the shift amount `31` is exactly on the boundary of the op's own accepted range (`< bits`).\n\n**Shipped-example run (0.1.13, L40S).** The only shipped `examples/` kernels that route a constant `<<` through this exact fold are the dequant-GEMM FP4 converters (`example_dequant_gemm_fp4_hopper.py`, `example_dequant_gemm_bf16_mxfp4_hopper.py`), which build FP16/BF16 bit patterns with `s << tirx.const(5, uint16)`, `... << tirx.const(10, uint16)`, `m << tirx.const(9, uint16)`. I ran `example_dequant_gemm_fp4_hopper.py::test_fp4_fp16_convert_close()` verbatim on 0.1.13 — it **compiles and passes** (`Pass`). It **dodges the bug** for two independent reasons: (1) the dtype is `uint16`, which is not an index type, so the fold is never entered (a `shift_left` intrinsic is emitted); and (2) even the largest shift (`1 << 15`) is in range. All other `<<` uses in `examples/` are Python-level `int`/torch shifts (folded by Python/PyTorch, not TVM) or C++ string literals inside inline-PTX asm — none reach the TVM `int32` fold. This is why CI is green. Any kernel that writes a constant `T.int32(1) << 31` in signed `int32` context trips it at compile.\n\n**Impact.** The trigger is narrow: the shift amount and operand must both be compile-time constants of signed `int32`, and the folded result must land exactly on the sign bit (`1 << 31`) — a runtime shift amount, an unsigned dtype, a non-index-width signed dtype (`int8`/`int16`, which do not fold at all), a 64-bit dtype, or any smaller shift all avoid it. When it fires it is a loud compile-time abort, deterministic on the source, so it cannot silently corrupt a result or slip into a running workload — it blocks that one kernel from building and produces nothing wrong. Fixing it closes a boundary case (the top-bit constant-shift fold) so a legal idiom like a literal `INT_MIN`/top-bit mask compiles instead of being refused, matching the runtime-shift and `uint32` paths.\n\n<details><summary>§17 Generalization record</summary>\n\n**Found first.** `int32` constant `T.int32(1) << T.int32(31)` — folds to `IntImm(int32, 2147483648)`, aborts compile at the `expr.cc:73` range check.\n\n**Two-level root.**\n- **SOURCE-level (fragile implementation):** `left_shift`'s constant-fold arm in `3rdparty/tvm/src/tirx/op/op.cc:792` — `return IntImm(rtype, (pa->value << pb->value), span)` — builds the result `IntImm` from the *raw* wide `int64_t` product without narrowing to `rtype`. It bypasses `arith::GetFoldResultInt64Repr` (`const_fold.h:81`), the mask-and-sign-extend helper that the arith `TryConstFold` folds (`Add`/`Sub`/`Mul`/`Div`/`Mod`) all route through. The same *bypass* pattern (`IntImm(rtype, raw)` inline, no `GetFoldResultInt64Repr`) appears in the sibling ops in the same file: `right_shift` (`op.cc:771`), `bitwise_and` (`:805`), `bitwise_or` (`:817`), `bitwise_xor` (`:829`), and the C++ unary `neg` (`:524`, `IntImm(dtype, -pa->value)`).\n- **OPERATOR-level (what could correlate):** any const-foldable integer op whose folded value can land outside the *signed 32-bit* range while its wrapped result is well-defined. The only such op reachable at the fold gate is a top-bit `<<`; the bitwise ops can raise no bit their (already-valid) operands don't already have, so from valid `int32` `IntImm`s they cannot produce an out-of-range value; `right_shift` shrinks; and the C++ `neg` overflow point (`-INT32_MIN`) is not reachable from the Python frontend (see below).\n\n**Fold gate (why only `int32`).** The fold body is wrapped in `TVM_INDEX_CONST_PROPAGATION`, which runs the fold *only* when both operands satisfy `arith::IsIndexType` = `type.is_int()` (signed, `kInt` — `data_type.h:210`) `&& bits ∈ {32, 64}`. So the fold is entered *only* for signed `int32`/`int64`. Combined with the `expr.cc:73` range check being gated on `bits < 64`, the crash window is exactly signed `int32`.\n\n**Four-axis cells tested (observed on 0.1.13, L40S, each in a fresh process):**\n\n| axis | cell tested | input → observed result | same root? |\n|---|---|---|---|\n| — (finding) | `int32` `1 << 31` | **compile abort**, `expr.cc:73` `(2147483648 vs. 2147483648)` | — |\n| related-type | `int64` `1 << 63` | **folds, compiles** → `IntImm(int64, -9223372036854775808)` (INT64_MIN) | distinct — same fold arm, but range check skipped (`bits==64`); no overflow |\n| related-type | `uint32` `1 << 31` | **not folded** → `T.shift_left(...)` Call; kernel compiles, yields `2147483648` | distinct — `uint32` is not an index type (`is_int()` false), fold never entered |\n| related-type | `int8` `1 << 7` | **not folded** → `T.shift_left(int8(1), int8(7))` Call | distinct — non-index width (bits≠32/64), fold never entered |\n| related-type | `int16` `1 << 15` | **not folded** → `T.shift_left(int16(1), int16(15))` Call | distinct — same as int8 |\n| related-operator | `neg(INT32_MIN)` via Python `-x` | **folds, compiles** → `IntImm(int32, -2147483648)` (correct wrap) | distinct — Python `__neg__` is `x * const(-1)` → routes through `Mul` `TryConstFold` → `GetFoldResultInt64Repr` narrows. The un-narrowed C++ `neg` at `op.cc:524` is not reached from the frontend |\n| related-operator | `bitwise_or` `0x40000000 \\| 0x1` (`int32`) | **folds, compiles** → `IntImm(int32, 1073741825)` | distinct — OR/XOR/AND of valid `int32` `IntImm`s can never set a bit above what an operand already holds, so cannot reach out-of-range |\n| related-source | `right_shift` `(1<<30) >> 1` (`int32`) | **folds, compiles** → `IntImm(int32, 536870912)` | distinct — shares the same inline-`IntImm` bypass at `op.cc:771`, but right-shift shrinks the value so it can never overflow |\n\n**Boundary verdict — kept specific.** The crash is narrow to signed `int32` top-bit `<<`. The *source* fragility (raw-`IntImm` fold without `GetFoldResultInt64Repr`) is shared across `right_shift` + the three bitwise ops + C++ `neg`, but none of those sinks can be driven out of range from valid frontend inputs: right-shift/bitwise cannot grow a value past its operands, and frontend `neg` narrows via `Mul`. So the fix (route the `<<` fold through `GetFoldResultInt64Repr`, ideally applied uniformly to all five inline-`IntImm` sites for consistency) is the right shape, but only the `<<` site is currently a live crash. Title/Problem kept specific to the `int32 1<<31` crash.\n\n**Distinct-adjacent, not filed.** The `int8`/`int16` \"not folded → intrinsic emitted\" behavior is a *non-fold* (correct on device), not a bug — the shift is left as a runtime `shift_left`, so `int8`/`int16` never crash (they never fold; the crash window is `int32` only). The C++ `neg` un-narrowed sink (`op.cc:524`) is latent but frontend-unreachable. No new bug was found while sweeping.\n\n**Shared general root with #2982.** #2982 is the `FloatImm` range-check-on-un-narrowed-value defect (`T.fill(f32, -3.4028235e38)` rejected though it rounds onto `float32::lowest()`), same `expr.cc` at the same tvm pin `8df8ebd6`, different sink (the `FloatImm` branch, reached via `T.fill`). This left-shift `IntImm` fold is the same general mechanism at a different sink. **Do not merge** — they are separate operators/sinks; the shared root is recorded here for tracking, not as a duplicate.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3001"}
{"number": 3012, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.gemm` on a `T.reshape`d 1D→2D operand aborts (\"shape should be at least 2\") instead of compiling", "state": "closed", "created_at": "2026-08-16T07:20:50Z", "updated_at": "2026-08-24T07:51:24Z", "closed_at": "2026-08-24T07:51:24Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (+1 or comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 / CUDA 12.8 / PyTorch 2.8.0, NVIDIA L40S (sm_89). The defect is in the target-independent `LowerTileOp` pass, so it is not arch-gated.\n\n### Problem description\n\nPassing a `T.reshape`d (or `T.view`ed) shared buffer as a `T.gemm` operand aborts compilation with an internal check:\n\n```\ntvm.error.InternalError: Check failed: (buffer->shape.size() >= 2) is false:\nThe dimension of Buffer \"As\" with shape (1024,) should be at least 2\n```\n\nThe reshaped operand *is* 2D — e.g. `T.reshape(As, (M, K))` — but the check reports its shape as the original 1D `(1024,)`. `T.reshape` returns a view that shares the source buffer's data var (`return T.Tensor(shape, src.dtype, src.data)`), and the GEMM lowering resolves the operand's access pointer back to the *underlying allocation* keyed by that data var, recovering the pre-reshape 1D buffer rather than the 2D view. The same 1D→2D reshape works fine as a `T.copy` source/target; only the GEMM operand path trips.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\n# reshape a 1D shared buffer to 2D, then use it as a T.gemm operand -> compile abort\n@tilelang.jit(out_idx=[-1])\ndef make(M, K, N):\n    @T.prim_func\n    def main(A: T.Tensor((M * K,), \"float16\"), B: T.Tensor((K, N), \"float16\"),\n             C: T.Tensor((M, N), \"float32\")):\n        with T.Kernel(1, threads=128) as bx:\n            As = T.alloc_shared((M * K,), \"float16\")   # 1D shared\n            T.copy(A, As)\n            Am = T.reshape(As, (M, K))                 # documented 2D view over As\n            Bs = T.alloc_shared((K, N), \"float16\")\n            T.copy(B, Bs)\n            Cf = T.alloc_fragment((M, N), \"float32\")\n            T.clear(Cf)\n            T.gemm(Am, Bs, Cf)                         # <-- aborts: \"As with shape [1024] should be at least 2\"\n            T.copy(Cf, C)\n    return main\n\nM, K, N = 32, 32, 32\na = torch.randn(M * K, device=\"cuda\", dtype=torch.float16)\nb = torch.randn(K, N, device=\"cuda\", dtype=torch.float16)\nmake(M, K, N)(a, b)   # InternalError at compile time\n```\n\n<details>\n<summary>Two controls that compile and return the correct result (isolate the defect)</summary>\n\n```python\n# CONTROL A: allocate the operand directly as 2D (no reshape) -> compiles, correct.\nAs = T.alloc_shared((M, K), \"float16\")\nT.copy(A2d, As); T.gemm(As, Bs, Cf)            # ORACLE: PASS\n\n# CONTROL B: the SAME 1D->2D reshape used as a T.copy target -> compiles, correct.\nAm = T.reshape(As, (M, K))\nT.copy(Am, C2d)                                 # ORACLE: PASS\n```\n\nControl A shows a 2D shared operand GEMMs correctly; Control B shows the 1D→2D reshape view is otherwise usable. Reshaping either operand (A or B) into GEMM aborts identically (`\"Bs\" with shape (1024,) should be at least 2`).\n</details>\n\n### Traceback\n\n```\ntvm.error.InternalError: Check failed: (buffer->shape.size() >= 2) is false:\nThe dimension of Buffer \"As\" with shape (1024,) should be at least 2\n  File \".../src/transform/lower_tile_op.cc\", line 409, in tvm::tl::LowerTileOpPass::CheckAndGetBufferRowSize(...)\n  ... in tvm::tl::LowerTileOpPass::VisitExpr_(tvm::tirx::CallNode const*)\n```\n\n### Expected behavior\n\n`T.gemm` should compile with a reshaped operand and compute the same result as the equivalent kernel that allocates the operand as 2D directly (Control A above), since `T.reshape` is documented to return \"a new buffer view with the specified shape\" and the reshaped operand is genuinely 2D. At minimum, if a reshaped operand is not meant to be supported here, a clear frontend error naming `T.gemm`/`T.reshape` would be preferable to an internal `buffer->shape.size() >= 2` abort that reports the wrong (pre-reshape) shape.\n\n### Additional context\n\n**Root cause.** The GEMM operand lowering measures the operand's rank on the *underlying 1D allocation* instead of on the 2D reshaped view it was actually given — so a legitimate 2D view (rank 2) is rejected because its backing buffer is rank 1.\n\n<details><summary>Mechanism (how the view's rank is bypassed)</summary>\n\n`T.reshape`/`T.view` build the 2D operand over the source buffer's existing data var ([`customize.py:60-74`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/customize.py#L60-L74), `return T.Tensor(shape, src.dtype, src.data)`). When lowering the operand's `tvm_access_ptr`, the pass looks the buffer up by that data var in `buffer_map_` ([`lower_tile_op.cc:437-438`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/lower_tile_op.cc#L437-L438)), which returns the original 1D `alloc_buffer` (shape `[1024]`), and hands it to `CheckAndGetBufferRowSize`, whose `ICHECK(buffer->shape.size() >= 2)` fails ([`lower_tile_op.cc:408-411`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/transform/lower_tile_op.cc#L408-L411)). It is reached only via the swizzled-`ptx_ldmatrix` access-pointer rewrite, so it is that path — not GEMM lowering in general — that trips.\n\n</details>\n\n**Suggested fix.** In the GEMM operand lowering (`lower_tile_op.cc`), resolve the operand shape/row-size from the reshaped view the GEMM was actually passed (the 2D buffer) rather than from `buffer_map_[data_var]`, so a legitimate view is measured with its own shape. This likely requires threading the view's shape through the access-pointer resolution, not a one-line change.\n\n**Provenance.** Not a regression. Both `src/transform/lower_tile_op.cc` and the `CheckAndGetBufferRowSize` guard (with its `should be at least 2` message) have existed since the initial codebase migration ([#10](https://github.com/tile-ai/tilelang/pull/10), `git log -S`/`--diff-filter=A` both point at that commit), so the check predates any recent change. Still present on `main` (checked `origin/main` @ `27f68b4e` this session): the `CheckAndGetBufferRowSize` helper was refactored away, but the identical `ICHECK(buffer->shape.size() >= 2)` on the operand's underlying (reshape-aliased) allocation now lives in `GetSwizzleShapeInfoChecked` ([`src/layout/gemm_layouts.cc#L405-L407`](https://github.com/tile-ai/tilelang/blob/27f68b4e95f8aa21320a47ebc95e170c2f8aa7c3/src/layout/gemm_layouts.cc#L405-L407), message reworded to \"Swizzle layout expects rank >= 2 buffer\"). Same guard, same rank-of-underlying-alloc check, same swizzled-`ptx_ldmatrix` path — so the crash is live on `main`, not just the `0.1.13` release.\n\n**Class (operator-level root).** The assert sits on the swizzled-`ptx_ldmatrix` access-pointer rewrite (source detail in the Root-cause `<details>` above), so it affects *any* tile op whose shared-memory operand goes through that permuted-layout rewrite when the operand is a `data`-var-sharing view (`T.reshape`/`T.view`) over a genuinely 1D shared allocation — not just `T.gemm`.\n\n**4-axis generalization (0.1.13, L40S; every cell run this session, one kernel per fresh process).**\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| — (baseline) | `T.reshape` 1D→2D **shared** A operand → `T.gemm` | abort `\"As\" (1024,) should be at least 2` (line 409) | — |\n| baseline | `T.reshape` 1D→2D shared **B** operand → `T.gemm` | abort identically `\"Bs\" (1024,)` | yes |\n| similar-logic | `T.view` (sibling, same `return T.Tensor(shape,…,src.data)`) shared A → `T.gemm` | abort identically `\"As\" (1024,)` (line 409) | yes |\n| related-type (scope) | `T.reshape` 1D→2D **fragment** operand → `T.gemm` | **compiles, correct** (err 3.8e-6) — no ldmatrix-over-shared, so `CheckAndGetBufferRowSize` never reached | boundary (distinct path) |\n| related-type (rank) | `T.reshape` **3D→2D** shared operand → `T.gemm` (underlying alloc rank ≥ 2) | **compiles**; result correct when 3D storage is laid out row-major identical to the view (err 3.8e-6) | boundary (rank check passes; row-size value is discarded `(void)`, not load-bearing for correctness) |\n| related-source | `CheckAndGetBufferRowSize`'s only callers (`:576`,`:680`) both live in the `ptx_ldmatrix` permuted-layout rewrite | source read: confirms the abort is the ldmatrix path, shared by any op emitting `ptx_ldmatrix` over a reshaped shared buffer | yes (shared fragile code) |\n| control | directly-allocated 2D shared operand (no view) → `T.gemm` | compiles, correct (err 3.8e-6) | — |\n| control | same 1D→2D `T.reshape` view fed to `T.copy` instead of `T.gemm` | compiles, matches reference (this is the shipped test path) | — |\n\n**Class boundary.** The class is *a `data`-var-sharing view (`T.reshape`/`T.view`) over a genuinely 1D **shared** allocation, passed as a `T.gemm` operand* — same root across the reshape/view siblings and both operand positions. The boundary is sharp on two axes the draft previously did not pin: (a) **scope** — a 1D *fragment* reshape into `T.gemm` compiles correctly (it does not route through `ptx_ldmatrix`-over-shared); (b) **rank** — a ≥2D underlying alloc passes the `>= 2` check and computes correctly, so the defect is exclusively the loud abort on a 1D-underlying shared alloc, not a silent miscompile. An earlier candidate \"3D→2D reshape gives wrong values\" was **refuted**: the wrong result came from a fill that placed data at different offsets than the row-major view implies, not from the reshape/gemm path (verified by re-running with storage laid out identical to the view → correct).\n\n**Distinct-adjacent, not filed.** `T.gemm_sp`/`wgmma_gemm`/`tcgen05_gemm` are sibling GEMM operators in the same pass, but each needs extra operands (sparse metadata) or specific hardware to set up a valid reshaped-shared-operand kernel; not exercised this session. No new bug found while sweeping.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect.\n\n**Reach.** (Example verified by run, 0.1.13.) The trigger is a documented view op (`T.reshape`'s docstring returns \"a new buffer view with the specified shape\") fed into `T.gemm`. The 1D→2D shared-buffer reshape itself is exercised by the shipped test `test_reshape_smem_1d_2_2d` (`testing/python/language/test_tilelang_language_reshape.py`). I ran that test **verbatim** on 0.1.13 (`git show v0.1.13:...` → `pytest ...::test_reshape_smem_1d_2_2d`): it **passes** (`1 passed`). It dodges this bug because its kernel body feeds the reshaped view to `T.copy` (`T.copy(A_smem_reshaped, B)`), not `T.gemm` — the copy path never resolves the operand back to the underlying alloc through the `ptx_ldmatrix` permuted-layout rewrite. So the shipped 1D→2D-reshape coverage exists but stops short of the GEMM-operand path; no test feeds a reshaped operand into `T.gemm`, and CI is green.\n\n**Impact.** The trigger is narrow: a `T.reshape`/`T.view` view (rather than a directly-allocated 2D buffer) passed specifically as a `T.gemm` operand. When it fires it is a compile-time `InternalError` abort — loud and caught immediately at build, so nothing is silently miscompiled and no wrong values can reach a running workload; the cost is that this valid kernel refuses to build and the abort reports the wrong (pre-reshape) shape, which is misleading to diagnose. Fixing it closes this view-as-GEMM-operand boundary (measure the operand by the view's own shape) and replaces a wrong-shape internal assert with either a compile or a clear frontend error.", "url": "https://github.com/tile-ai/tilelang/issues/3012"}
{"number": 3022, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.reduce_*` on a sliced buffer crashes with a Python `AttributeError` — the input contract is undefined for a `BufferRegion`", "state": "closed", "created_at": "2026-08-17T03:06:14Z", "updated_at": "2026-08-24T07:47:53Z", "closed_at": "2026-08-24T07:47:53Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar issue has been filed.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nL40S (sm_89), CUDA 12.8, Python 3.13, TileLang 0.1.13. The failure is in the Python eager builder (frontend), so it is architecture-independent — it aborts before any codegen.\n\n### Problem description\n\nPassing a **slice** of a buffer to `T.reduce_sum` / `reduce_max` / `reduce_min` / etc. (e.g. `T.reduce_sum(a[0:64], out, dim=0)`) crashes the compiler with an uncaught Python `AttributeError: 'BufferRegion' object has no attribute 'shape'`, instead of either reducing the slice or reporting a clear error. Reducing the **whole** buffer over the same inputs compiles fine.\n\nThe reduce docstrings type the input as `tirx.Buffer` (\"Input buffer to reduce\") and say nothing about slices/regions, so the contract for a sliced input is undefined. But slicing a buffer is a first-class idiom in TileLang, and the sibling scan op `T.cumsum` *does* document and accept exactly this input — its docstring says [\"Supports Buffer, BufferRegion, and BufferLoad inputs, allowing operations on buffer slices/regions\"](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/scan_op.py#L84-L95) and it works on the same slice (see the example below). So a user reasonably expects `T.reduce_*` to accept a slice too; today it neither supports it nor rejects it cleanly.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\n\n@tilelang.jit(out_idx=[1])              # CONTROL: reduce the whole fragment\ndef reduce_full(N=128):\n    @T.prim_func\n    def main(A: T.Tensor((N,), \"float32\"), B: T.Tensor((1,), \"float32\")):\n        with T.Kernel(1, threads=N):\n            a = T.alloc_fragment((N,), \"float32\")\n            o = T.alloc_fragment((1,), \"float32\")\n            T.copy(A, a)\n            T.reduce_sum(a, o, dim=0)           # whole Buffer -> OK\n            T.copy(o, B)\n    return main\n\n\n@tilelang.jit(out_idx=[1])              # BUG: reduce a slice (a BufferRegion)\ndef reduce_slice(N=128):\n    @T.prim_func\n    def main(A: T.Tensor((N,), \"float32\"), B: T.Tensor((1,), \"float32\")):\n        with T.Kernel(1, threads=N):\n            a = T.alloc_fragment((N,), \"float32\")\n            o = T.alloc_fragment((1,), \"float32\")\n            T.copy(A, a)\n            T.reduce_sum(a[0:64], o, dim=0)    # slice -> BufferRegion -> crash\n            T.copy(o, B)\n    return main\n\n\nfor name, fn in [(\"control (whole buffer)\", reduce_full),\n                 (\"slice   (a[0:64])     \", reduce_slice)]:\n    try:\n        fn(); print(f\"{name}: compiles OK\")\n    except Exception as e:\n        print(f\"{name}: {type(e).__name__}: {e}\")\n\n# control (whole buffer): compiles OK\n# slice   (a[0:64])     : AttributeError: 'BufferRegion' object has no attribute 'shape'\n```\n\n### Traceback\n\n```\n  File \".../tilelang/language/reduce_op.py\", line 232, in reduce_sum\n    reduce(buffer, out, \"sum\", dim, clear, batch=batch)\n  File \".../tilelang/language/reduce_op.py\", line 51, in reduce\n    expected_shapes = [buffer.shape[:dim] + buffer.shape[dim + 1 :], buffer.shape[:dim] + [1] + buffer.shape[dim + 1 :]]\n                       ^^^^^^^^^^^^\nAttributeError: 'BufferRegion' object has no attribute 'shape'\n```\n\n### Expected behavior\n\n`T.reduce_*` most plausibly should **accept a sliced input and reduce it**, matching the sibling `T.cumsum`, which already documents and supports `Buffer`/`BufferRegion`/`BufferLoad` and reduces the same slice correctly on 0.1.13. On that reading this is a valid input the compiler nearly handles already — the *output* side of the same function is already region-aware — so the fix is to route the input shape read through the same region-aware helper `cumsum` uses. If the maintainer instead intends sliced reduce to be out of scope, a clear frontend error naming the unsupported input would still be far better than the current uncaught `AttributeError` from deep inside the builder. The contract direction is the maintainer's to set; either way the present outcome — a raw Python attribute error and a dumped transpiler source — is the defect.\n\n### Additional context\n\n**Root cause.** `reduce()` reads `.shape` directly off its input argument, but a sliced input is a `BufferRegion`, which has no `.shape` — so the shape-validation line raises before the reduction is ever built. Concretely, [`reduce()`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/reduce_op.py#L24-L25) is typed `buffer: tirx.Buffer` and computes `expected_shapes` from [`buffer.shape[:dim] + buffer.shape[dim + 1:]`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/reduce_op.py#L51); when `buffer` is a `BufferRegion` (a slice like `a[0:64]`), `.shape` does not exist. `reduce_max`/`reduce_min` hit the same wall one step earlier for a negative `dim` via [`_legalize_dim`'s `len(buffer.shape)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/reduce_op.py#L12-L14). The sibling `cumsum` avoids this by taking shapes through the region-aware helper [`retrieve_shape`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/utils/language.py#L233-L251) ([used at `cumsum` line 64](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/scan_op.py#L64)), which returns `[r.extent for r in region.region]` for a `BufferRegion`.\n\n**Suggested fix.** The primary suggestion, given the `cumsum` precedent, is to **support the slice**: route the shape read through the same `retrieve_shape` helper `cumsum` uses (and drop the `.shape` access in `_legalize_dim` similarly), so a `BufferRegion` extent list is used for the shape check. If instead sliced reduce is meant to be out of scope, the alternative is an explicit type check at the top of `reduce()` that raises a clear error naming the input. Which direction is right is the maintainer's contract call; both replace the current uncaught `AttributeError`.\n\n**Class tested (this session, 0.1.13, L40S/A10G).** Two-level root: **(a) source-level** — `reduce()` and its helper `_legalize_dim` in `tilelang/language/reduce_op.py` read shape off the *input* argument directly (`buffer.shape` at [L51](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/reduce_op.py#L51), `len(buffer.shape)` at [L14](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/reduce_op.py#L12-L14)) instead of routing it through the region-aware `retrieve_shape` helper the sibling `cumsum` uses; the *output* argument in the same function IS routed through a region-aware read ([`to_buffer_region(out).buffer` at L47](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/reduce_op.py#L47)), so the fragility is input-side only. **(b) operator-level** — the whole `T.reduce_*` family (`sum`/`max`/`min`/`abssum`/`absmax`/`bitand`/`bitor`/`bitxor`), which all funnel through the one `reduce()`/`_legalize_dim` pair.\n\n<details>\n<summary>4-axis generalization sweep (each cell run on 0.1.13, observed result recorded — not asserted)</summary>\n\n| axis | cell tested | observed result | same root? |\n|---|---|---|---|\n| (op family) | `reduce_sum(a[0:64], o, dim=0)` | `AttributeError: 'BufferRegion' object has no attribute 'shape'` @ `reduce_op.py:51` | yes |\n| related-operator | `reduce_max` / `reduce_min` / `reduce_abssum` / `reduce_absmax` `(a[0:64], o, dim=0)` | same `AttributeError` @ `reduce_op.py:51` | yes |\n| related-operator + related-type | `reduce_bitxor(a[0:64], o, dim=0)`, `int32` | same `AttributeError` @ `reduce_op.py:51` | yes |\n| similar-logic | `reduce_max(a[0:64], o, dim=-1)` — negative dim | same `AttributeError`, one step earlier @ `_legalize_dim` (`reduce_op.py:14`, `len(buffer.shape)`) | yes (2nd site) |\n| related-source | `reduce_sum(a, o[0:4], dim=0)` — whole input, **sliced output** | clean `ValueError` @ `reduce_op.py:54` (no crash; out routed through `to_buffer_region`) | **no — out-side is region-aware** |\n| related-type (dtype control) | `reduce_bitxor(a, o, dim=0)`, `int32` whole buffer | compiles OK | control (dtype not the cause) |\n| (input-shape control) | `reduce_sum(a, o, dim=0)` — whole `Buffer` | compiles OK | control |\n| related-operator (sibling scan) | `cumsum(a[0:64], o[0:64], dim=0)` — same slice | **compiles OK** (documented to accept `BufferRegion`) | control (sibling handles it) |\n\nThe defect is the sliced (`BufferRegion`) *input* reaching a `Buffer`-only shape read: shared across the entire `reduce_*` family and across both the `dim>=0` (`reduce_op.py:51`) and negative-`dim` (`_legalize_dim`, `reduce_op.py:14`) sites, and dtype-independent (int32 slice crashes identically, int32 whole buffer is fine). It is NOT shared by the output argument (region-aware via `to_buffer_region`) — the sliced-output cell gave a clean `ValueError`, isolating the fragility to the input-side read. The region-aware sibling `cumsum` accepts the same slice, confirming the intended-idiom framing.\n\n</details>\n\n**Provenance.** The `expected_shapes = [buffer.shape[...]]` read at `reduce_op.py:51` — the crash on the `dim >= 0` path — was added by [#748 \"Add shape checking for reduce options\"](https://github.com/tile-ai/tilelang/pull/748) (merged 2025-08-24). The `len(buffer.shape)` read in `_legalize_dim` predates #748, so the negative-`dim` variant of the crash is older; both are region-unaware for the same reason. The file was later moved from `tilelang/language/reduce.py` to `reduce_op.py` and the `cumsum` scan op split out into `scan_op.py`. Not a regression either way — no version accepted a sliced reduce input.\n\n**Related.** Same family, different sites, both closed: [#2550](https://github.com/tile-ai/tilelang/issues/2550) (`T.atomic_addx4` reads `.dtype` off a sliced destination) and [#2631](https://github.com/tile-ai/tilelang/issues/2631) (`increase_descriptor_offset` reads `.shape` off a `BufferLoad`). Those were the eager builder reading a `Buffer`-only attribute off a `BufferRegion`/`BufferLoad`; this is the same shape but in `reduce()`, and it is not fixed.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of the reduce case; the related-family issues (#2550, #2631) are different sites and both already closed.\n\n**Reach.** Triggering requires passing a *slice* (a `BufferRegion`) as the reduce input. I audited the shipped `T.reduce_*` call sites in `examples/` (flash-attn, layernorm, rms_norm, the fp8-cast kernels, the sparse-MLA kernels): every one passes a *whole* fragment buffer (`acc_s`, `A_local`, `y_local`, …), never a slice — so none trip this and CI stays green; there is no test exercising a sliced reduce input. To make the \"dodges by whole-buffer input\" point concrete I ran the shipped `examples/norm/rms_norm.py` verbatim on 0.1.13 (A10G): it prints `All checks pass.` — its two `T.reduce_sum(A_pow_local, A_powsum, dim=1)` calls take whole buffers, so it compiles and matches the torch reference. Slicing itself is common, and the sibling `T.cumsum` explicitly documents slice input — and I confirmed on 0.1.13 that `T.cumsum(a[0:64], o[0:64], dim=0)` compiles fine while the identical reduce slice crashes — so a user moving between the two ops hits this immediately. The failure is a hard compile-time abort (a raw `AttributeError` from the eager builder) — it corrupts no data and cannot slip into a running workload; it just fails loudly on that kernel build.", "url": "https://github.com/tile-ai/tilelang/issues/3022"}
{"number": 3025, "title": "[BUG][Fuzzer][wrong-code] `T.ldg*`/`T.stg*`/`T.lds*` on a `T.StridedTensor` address the wrong element — offset uses buffer shape, not the declared strides", "state": "closed", "created_at": "2026-08-17T03:16:49Z", "updated_at": "2026-08-23T17:16:11Z", "closed_at": "2026-08-23T17:16:11Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\n- TileLang 0.1.13, Python 3.13, Linux.\n- GPU: NVIDIA L40S (compute capability 8.9, sm_89). The defect is in the pointer-offset helper `tilelang/utils/language.py` (Python frontend), which is architecture-independent; the wrong offset is baked into the emitted address arithmetic on every target.\n\n### Problem description\n\n`T.StridedTensor((M, N), (PITCH, 1))` declares a pitched/padded global buffer whose physical row stride `PITCH` is larger than the logical width `N`. When such a buffer feeds any of the explicit `retrieve_ptr`-backed load/store primitives (`T.ldg32/64/128/256`, `T.stg32/64/128/256`, `T.lds32/64/128`, `T.ds_read_*`), the flat element offset for `[row, col]` is computed from the buffer's **shape** (row-major `row*N`) rather than its **declared strides** (`row*PITCH`), so every non-first row is accessed at the wrong address. The kernel compiles and runs with no error; the result is silently wrong (and, when the physical buffer is exactly the logical size, the accesses land out of bounds).\n\nWith `M,N,PITCH = 4,3,8`, a `T.stg32(dst[r,0], r+1)` loop over a `StridedTensor` writes value `r+1` to flat element `r*3` instead of `r*8`; reading back column 0 gives `[1, 0, 0, 0]` (rows 1–3 scattered into the padding gap) instead of `[1, 2, 3, 4]`. The identical kernel on a contiguous `T.Tensor` gives `[1, 2, 3, 4]`, so this is specific to the strided buffer, not to `T.stg32`. The symmetric load path `T.ldg32` is wrong the same way (on a pitched source whose col-0 rows hold `[10, 18, 26, 34]`, `T.ldg32(src[r,0])` reads `[10, 13, 16, 19]` — i.e. `src[r*3]` — instead).\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport torch, tilelang, tilelang.language as T\n\nM, N, PITCH = 4, 3, 8  # logical (M,N); physical row pitch PITCH > N (padded rows)\n\ndef kernel(strided):\n    dt = (T.StridedTensor((M, N), (PITCH, 1), \"int32\") if strided\n          else T.Tensor((M, N), \"int32\"))\n    @T.prim_func\n    def main(dst: dt):\n        with T.Kernel(1, threads=1):\n            for r in T.serial(M):\n                T.stg32(dst[r, 0], T.Cast(\"uint32\", r + 1))   # write r+1 into element [r, 0]\n    return main\n\ndef run(strided):\n    k = tilelang.compile(kernel(strided), target=\"cuda\")\n    if strided:\n        phys = torch.zeros(M * PITCH, dtype=torch.int32, device=\"cuda\")\n        dst = phys.as_strided((M, N), (PITCH, 1))   # pitched view, stride[0] = PITCH\n    else:\n        dst = torch.zeros(M, N, dtype=torch.int32, device=\"cuda\")\n    k(dst)\n    return dst[:, 0].tolist()\n\nprint(\"contiguous  col0 =\", run(False))   # -> [1, 2, 3, 4]  (correct control)\nprint(\"strided     col0 =\", run(True))    # -> [1, 0, 0, 0]  (wrong: rows 1-3 lost)\n```\n\n<details><summary>Emitted CUDA and the offset math (the address uses <code>r*3</code>, i.e. shape, not the declared stride <code>8</code>)</summary>\n\nThe strided kernel emits the store address with the shape-derived stride `3`, not the declared `PITCH = 8`:\n\n```\ntl::store_global_32((&(dst[(r * 3)])), ...);   // should be dst[(r * 8)]\n```\n\nThe offset comes straight from `retrieve_ptr`: for `dst[2, 0]` on this buffer it accumulates `offset = 2*3 = 6`, where the correct offset is `2*PITCH = 16`. `buffer.access_ptr(offset=...)` treats `offset` as a flat element offset, so a stride computed from shape produces a wrong address directly.\n\n</details>\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion; the result is silently wrong and deterministic. (When the physical allocation equals the logical `M*N`, the wrong offsets also read/write out of bounds.)\n\n### Expected behavior\n\nFor a `StridedTensor`, the element offset should use the buffer's declared strides (`row*PITCH`), so that `T.stg32`/`T.ldg32` address the same physical layout the buffer describes — matching the contiguous case, which is correct, and matching the region-copy path (`T.copy`, TMA), which threads the buffer's real strides through the TIR buffer rather than recomputing them.\n\n### Additional context\n\n**Root cause.** `retrieve_stride` computes row-major strides from the buffer's `shape` and never reads the buffer's declared `strides`, so any buffer whose real strides differ from its shape (a `StridedTensor`) gets wrong offsets. [`retrieve_stride`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/utils/language.py#L253-L271) loops `for s in reversed(shape): strides.insert(0, stride); stride *= s` — the docstring even states it derives strides \"based on its buffer.shape\". Its result feeds the offset accumulation in [`retrieve_ptr`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/utils/language.py#L303-L332) (`offset += region[i].min * strides[i]`), which backs `T.ldg32/64/128` and `T.stg32/64/128` (`builtin.py`, `ptr = retrieve_ptr(dst/src, ...)`). `retrieve_stride` is also called in [`gemm_op.py`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/gemm_op.py#L96-L97) (`A_stride = retrieve_stride(A_region)`), but the in-tree comment there states that value is deprecated and \"no longer read in-tree\" (GEMM consumes the complete operand `BufferRegion`s), so GEMM is not a demonstrated wrong-code path here — the confirmed consumers are the explicit `ldg*`/`stg*` primitives.\n\n**Suggested fix.** Have `retrieve_stride` return the buffer's declared `strides` when present (`obj.strides` / `obj.buffer.strides`), falling back to the row-major-from-shape computation only when strides are unset. The same `buffer.strides` field is already carried on the TIR buffer that `StridedTensor` builds, so the offset accumulation in `retrieve_ptr` would then match the buffer's actual layout.\n\n**Provenance.** Present since `retrieve_stride` first shipped in [#701](https://github.com/tile-ai/tilelang/pull/701) (\"Support mma gemm with stride\", merged 2025-08-11) — the original definition already recomputed strides from `reversed(object.shape)` and ignored the buffer's declared strides; [#1200](https://github.com/tile-ai/tilelang/pull/1200) (merged 2025-11-12) only promoted it to the shared `utils/language.py` helper without changing that logic. Not a regression. Reproduced on 0.1.13 (`8001cc4`); the helper is byte-identical on the current `main`, so it is still live there.\n\n**Dedup.** I searched the open and closed tracker (`retrieve_stride`, `StridedTensor`, stride) and found no existing report of this offset defect. Distinct from [#2755](https://github.com/tile-ai/tilelang/issues/2755): that is the NVRTC **host adapter** dropping dynamic-stride parameters at the launch-marshalling boundary, not the device-side offset arithmetic; this is `retrieve_stride` computing the wrong in-kernel address from a static declared stride.\n\n**Reach.** Verified by running the shipped tests this session (0.1.13, L40S sm_89). `StridedTensor` is a documented public buffer type (`tilelang/language/proxy.py`, shown in the `examples/eager_jit` notebooks) and appears in 10 test/example files at v0.1.13 (`test_tilelang_language_subtype.py`, `..._copy.py`, `..._tma_copy.py`, `..._vectorize.py`, `..._eager_jit.py`, `test_tilelang_issue_2292.py`, the two `eager_jit` notebooks, and the NVRTC/cutedsl host tests). I ran the ones that exercise strides:\n- `test_tilelang_language_subtype.py -k \"stride or noncontig\"` → **12 passed** (0.1.13). These store to a `StridedTensor` via the ordinary indexed store `dst[i,j,k] = ...` (e.g. `test_subtype_fp4_dynamic_stride_store`), which lowers through the normal TIR index path that DOES honor the declared strides — so they pass and never touch `retrieve_ptr`. They dodge the bug because they use ordinary `BufferStore`, not `T.stg*`.\n- `test_tilelang_language_ldst.py` + `test_tilelang_language_ldg.py` → **15 passed, 4 skipped**. These are the only shipped `stg*`/`ldg*` tests and they dodge the bug for the other reason: every buffer is a contiguous 1-D `T.Tensor` (`T.Tensor[[N], f32]`), so the shape-derived stride equals the real stride and `retrieve_stride` is accidentally correct.\n\nSo no shipped test or example ever crosses a `StridedTensor` (declared stride ≠ shape) with a `retrieve_ptr`-backed primitive — the two conditions that jointly trigger the bug — which is why CI is green. Both ingredients are individually common (pitched allocations for alignment; explicit `ldg`/`stg`); the bug fires at their intersection.\n\n**Two-level root.**\n- *Source-level root:* `retrieve_stride` (`tilelang/utils/language.py:253-271`) derives strides from `buffer.shape` and never reads `buffer.strides`; its result feeds the offset accumulation in `retrieve_ptr` (L303-347). The dead-but-exported twin `retrive_ptr_from_buffer_region` (L274-301) has the identical `for i, shape in enumerate(reversed(buffer.shape))` logic (same latent defect, no in-tree caller).\n- *Operator-level root:* every primitive whose address is built by `retrieve_ptr` — `T.ldg32/64/128/256`, `T.stg32/64/128/256`, `T.lds32/64/128`, `T.ds_read_tr8/tr16_b64` (all in `builtin.py`), plus the mbar pointers in `cluster.py` / `gemm_tcgen05.py` (which carry no meaningful stride, so benign).\n\n**Generalization sweep — every cell RUN this session on 0.1.13 (sm_89), value/emitted-address recorded:**\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| (repro) | `T.stg32` store, pitch `(8,1)` shape `(4,3)` | col0 = `[1,0,0,0]`; emits `dst[(r * 3)]` (should be `r*8`) | yes |\n| related-operator | `T.ldg32` load, same pitched buffer | reads `[10,13,16,19]`, correct `[10,18,26,34]`; emits `src[(r * 3)]` | yes (symmetric `retrieve_ptr`) |\n| related-operator | `T.stg64`/`T.ldg64` (64-bit width) | emits `dst[(r * 4)]` for shape `(4,4)` pitch `(8,1)` (should be `r*8`) | yes, width-independent |\n| related-source | 3-D `StridedTensor` `(2,3,4)` strides `(40,10,1)` → `T.stg32` | emits `dst[((a * 12) + (b * 4))]` (shape-derived; should be `a*40+b*10`) | yes, multi-dim accumulation |\n| related-operator/scope | `T.lds32` from shared `alloc_shared((M,PITCH))` | reads correct `[0,3,6,9]`; emits `smem[(r_1 * 8)]` | shares code, NOT triggerable — `alloc_shared` gives a buffer whose shape == layout, so shape-derived stride is correct; no frontend makes a shared buffer with declared stride ≠ shape |\n| similar-logic (CONTROL) | ordinary indexed store `dst[r,0]=r+1` to the SAME pitched buffer | col0 = `[1,2,3,4]` (correct); emits `dst[(r * 8)]` | **distinct path** — ordinary `BufferStore` honors the declared stride; isolates the defect to `retrieve_ptr` |\n| related-source (CONTROL) | `T.copy(strided_src, shared)` region path | matches the pitched source exactly | **distinct path** — threads the buffer's real strides through the TIR `BufferRegion`, does not recompute via `retrieve_stride` |\n\nThe bug is a **class over the `retrieve_ptr`-backed primitive family** (`ldg*`/`stg*`/`lds*`/`ds_read_*`), not a single-primitive defect: load and store, all widths, and any rank are wrong identically, because they share the one `retrieve_stride` call. The two CONTROL rows (ordinary store, `T.copy`) are the *correct* strided paths and localize the defect precisely to `retrieve_ptr`. `T.lds*` shares the fragile code but cannot be independently triggered because shared allocations always have shape == layout. Feeding a global `StridedTensor` directly as a `T.gemm` operand is rejected by this build (`Unsupported gemm combination, A: global`) and the `retrieve_stride` call in `gemm_op.py` is dead (see Root cause), so GEMM is neither reachable nor a wrong-code path here.\n\n**Impact.** Silent-wrong when it fires and deterministic (the wrong address is fixed at compile time, so a kernel that hits it is wrong the same way every run). The trigger is narrow — it needs a buffer whose declared stride differs from its shape reaching an explicit `ldg*`/`stg*` primitive — but the consequence is broad: every element whose declared stride differs from the shape-derived one is misaddressed, corrupting the whole output region rather than one value, and when the physical allocation is not over-sized the misaddressing is an out-of-bounds access. Fixing it makes `StridedTensor` — the type whose purpose is to describe a non-contiguous layout — actually honor its declared strides across the `retrieve_ptr`-backed consumers.", "url": "https://github.com/tile-ai/tilelang/issues/3025"}
{"number": 2623, "title": "[BUG][Fuzzer][wrong-code] `T.finalize_reducer(reducer, batch>1)` silently reduces only the first `batch` elements", "state": "open", "created_at": "2026-07-12T06:04:36Z", "updated_at": "2026-08-22T21:03:39Z", "closed_at": null, "labels": [], "comments_count": 1, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.11 (source pinned to tag `v0.1.11`, commit `cd37ed5fc35ae7a60a1277c8eb49028174ac51e6`).\n\n### System information\n\nReproduced on two GPUs: NVIDIA H100 80GB HBM3 (`sm_90`) and NVIDIA L40S (`sm_89`), Python 3.12/3.13, torch cu13, tilelang 0.1.11. Bit-identical mismatch counts on both — the defect is in the target-independent finalize-reducer lowering, not arch-specific codegen.\n\n```python\nimport sys, tilelang, torch\nprint(sys.version, sys.platform)   # 3.13.x linux\nprint(tilelang.__version__)        # 0.1.11\nprint(torch.__version__)           # 2.11.x (cu13)\n```\n\n### Problem description\n\n`T.finalize_reducer(reducer, batch=B)` with `B > 1` **silently miscomputes**: it correctly reduces only the *first `B`* of the reducer's per-thread output elements and leaves every remaining element as its un-reduced per-thread partial (mostly the cleared value, i.e. `0` for `sum`, the identity clamp for `max`/`min`). `batch=1` on the exact same kernel is correct. The **first `batch` output elements are correctly reduced and the rest are dropped** — so the number of matching elements is `batch` plus any coincidental matches (a dropped row whose leftover partial happens to equal the reduced value); with random exact-int inputs I measured e.g. batch=4 → 6/128 match (rows 0–3 correct + 2 coincidental), batch=8 → 9/128, batch=16 → 17/128, batch=1 → 128/128. The structural defect is unambiguous regardless of the coincidental count: elements `[batch, OutputDim)` are never reduced.\n\nThis is a gross value error, not float rounding — the repro uses integer-valued inputs (any reassociation is still exact) and is bit-identical across repeated launches (deterministic, not a race).\n\n<details>\n<summary>Trigger boundary (replication=\"all\", threads=256, fp32 integer inputs, row-sum reducer)</summary>\n\nThe reducer output has `block_M` elements; with `replication=\"all\"` each thread holds all `block_M`. The batched path processes exactly `batch` of them, so the count of correct rows is exactly `batch`:\n\n| `block_M` (output elems) | `batch` | # correct rows | correct indices |\n|---|---|---|---|\n| 16 | 1  | 16/16 | all (scalar path) |\n| 16 | 2  | 2/16  | `[0, 1]` |\n| 16 | 4  | 4/16  | `[0, 1, 2, 3]` |\n| 16 | 8  | 8/16  | `[0..7]` |\n| 16 | 16 | 16/16 | all (`batch == block_M`, single call covers everything) |\n| 128 | 1 | 128/128 | all |\n| 128 | 4 | 4/128  | `[0, 1, 2, 3]` (the other 124 are wrong, mostly `0`) |\n\nThe defect fires **iff** the batched path is taken (`batch > 1` **and** `reducing_threads > warpSize`, so `MakeBatchAllReduce`/`run_batch` is emitted) **and** `batch < block_M` (the reducer's per-thread output count). It is accidentally correct when `batch == block_M` (the single `run_batch` happens to cover all elements) or `batch == 1` (scalar path). `max`/`min` reproduce identically.\n\n</details>\n\n#### Root cause\n\nThe finalize-reducer lowering does not emit the per-element outer loop on its batched path, so only the first `batch` output elements are reduced and the rest are left un-reduced. Concretely, `FinalizeReducerLowerer::Lower` ([`src/backend/common/op/finalize_reducer.h`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/backend/common/op/finalize_reducer.h#L81-L112)) has two paths:\n\n- **Scalar path** (`batch == 1` or `reducing_threads <= warpSize`, [lines 93–111](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/backend/common/op/finalize_reducer.h#L93-L111)): builds a `BufferStore` and **wraps it in a `For` loop over every `layout->OutputShape()[i]`** (lines 106–109), so all output elements are reduced. Emits, per element:\n  ```cuda\n  for (int __finred_0 = 0; __finred_0 < 128; ++__finred_0)\n    o_reducer[__finred_0] = tl::AllReduce<SumOp,256,1,0>::run(o_reducer[__finred_0], workspace);\n  ```\n\n- **Batched path** (`use_batch`, [lines 81–91](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/backend/common/op/finalize_reducer.h#L81-L91)): emits a **single `Evaluate(Call(... run_batch ...))` with NO enclosing loop**:\n  ```cuda\n  tl::AllReduce<SumOp,256,1,0,SyncThreadsBarrier,4,256>::run_batch(o_reducer, workspace);\n  ```\n  `run_batch` covers only `batch` elements starting at `o_reducer[0]`. The outer `ceil(OutputDim / batch)` loop that the [`T.reduce` docstring promises](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/tilelang/language/reduce_op.py#L35-L39) (\"the compiler emits `ceil(N/batch)` batched AllReduce calls\") is simply missing from the finalize-reducer lowering. So elements `[batch, OutputDim)` are never reduced.\n\nThe scalar path is immune because it iterates the full `OutputDim`; the batched path drops the iteration entirely.\n\n### Reproducible example code\n\n`replication=\"all\"`, `threads=256`, so `reducing_threads = 256 > warpSize` and the batched path is taken. Integer inputs make any reassociated sum exact — any difference is a real miscompile. Only `BATCH` changes between the correct control (`1`) and the wrong run (`4`).\n\n```python\nimport torch, tilelang as tl\nimport tilelang.language as T\n\ndef make_kernel(BATCH, block_M=128, block_N=64):\n    @T.prim_func\n    def kernel(A: T.Tensor((block_M, block_N), \"float32\"), B: T.Tensor((block_M,), \"float32\")):\n        with T.Kernel(1, threads=256):\n            o = T.alloc_reducer(block_M, \"float32\", op=\"sum\", replication=\"all\")\n            T.clear(o)\n            A_s = T.alloc_shared((block_M, block_N), \"float32\"); T.copy(A, A_s)\n            A_f = T.alloc_fragment((block_M, block_N), \"float32\"); T.copy(A_s, A_f)\n            for i, j in T.Parallel(block_M, block_N):\n                o[i] += A_f[i, j]\n            T.finalize_reducer(o, batch=BATCH)     # flip BATCH 1 -> 4\n            T.copy(o, B)\n    return kernel\n\nFLAGS = {tl.PassConfigKey.TL_DISABLE_WARP_SPECIALIZED: True,\n         tl.PassConfigKey.TL_DISABLE_TMA_LOWER: True}\n\ntorch.manual_seed(0)\nA = torch.randint(-8, 9, (128, 64), dtype=torch.int32).float().cuda()  # exact integers\nref = A.sum(dim=1)\n\nfor BATCH in (1, 4):\n    B = tl.compile(make_kernel(BATCH), out_idx=-1, pass_configs=FLAGS)(A)\n    ndiff = int((B != ref).sum())\n    print(f\"BATCH={BATCH}: ndiff={ndiff}/128\")\n\n# -> BATCH=1: ndiff=0/128    (control: scalar path, correct)\n# -> BATCH=4: ndiff=122/128  (batched path: only first 4 rows correct, rest dropped to 0)\n```\n\n### Traceback\n\nNo traceback — silent, no crash. The kernel compiles and runs cleanly; the output tensor is just wrong for elements `[batch, OutputDim)`.\n\n### Expected behavior\n\n`T.finalize_reducer(reducer, batch=B)` should reduce **all** `OutputDim` per-thread output elements, exactly matching the `batch=1` scalar path, per the [`T.reduce` docstring](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/tilelang/language/reduce_op.py#L35-L39): \"the compiler emits `ceil(N/batch)` batched AllReduce calls.\" Concretely, on the repro above, `BATCH=4` would then give `ndiff=0/128` (identical to `BATCH=1`); currently only the first `batch` elements are reduced and the remaining `OutputDim - batch` are silently left as their un-reduced per-thread partials.\n\n### Additional context\n\nNote: the maintainers' own test `test_finalize_reducer_correctness` ([`testing/python/language/test_tilelang_language_reduce.py`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/testing/python/language/test_tilelang_language_reduce.py#L260)) only parametrizes `batch == 1` cases (the parametrize filters `c[4] == 1`) and its docstring notes *\"batch>1 blocked by fragment layout bug\"*; the separate `batch>1` codegen test only asserts the `run_batch` string appears, never that the result is correct. So `batch>1` is a known-unfinished path. That docstring attributes it to a *fragment layout* bug; this report identifies a concrete, independently-reproducible cause on the finalize-reducer lowering — the missing outer loop — which may be the same issue or a distinct one (I have not confirmed the \"fragment layout\" attribution refers to this loop). Either way, the exact-int oracle here shows the observable failure and its lowering site.\n\nAlso note this is a **performance opt-in with narrow reach**: `batch` defaults to `1` everywhere and no in-tree example, library path, or autotuner passes `batch>1` (checked this session) — so this only affects a user who manually enables the documented barrier-reduction knob. It is still a silent wrong result for that user.\n\n**Suggested fix:** Wrap the batched `run_batch` call in the same outer loop the scalar path uses — emit `ceil(OutputDim / batch)` `run_batch` calls, each covering a `batch`-element slice of the reducer, advancing the base pointer by `batch` each iteration (matching the documented \"ceil(N/batch) batched AllReduce calls\" contract). Alternatively, until the batched finalize-reducer path is correct, fall back to the scalar path for `finalize_reducer` (ignore `batch`), which is already numerically correct.\n\n**Provenance.** Not a regression — the batched finalize-reducer path was born without the loop. It was introduced by [#1976](https://github.com/tile-ai/tilelang/pull/1976) (\"Batched AllReduce for better T.reduce performance\", commit `6548c05f`): in that commit's `src/op/finalize_reducer.cc`, the `use_batch` branch already `return`s a single `Evaluate(Call(... ::run_batch ...))` with no enclosing loop, while the scalar path directly below it builds the `For` over `layout->OutputDim()`. (#1976's own message says it \"emits `ceil(N/batch)` calls to `run_batch`\", but the finalize-reducer branch does not — it emits one.) The [#2163](https://github.com/tile-ai/tilelang/pull/2163) refactor (\"Share common GPU tile op lowerers\", commit `38983368f`) relocated the same branch to `src/backend/common/op/finalize_reducer.h` unchanged. Still present on `main`.\n\n**Dedup.** Searched the tracker (`gh` + `finalize_reducer` / `reducer batch`) this session. Distinct from three nearby reducer issues:\n- **#2408** (`alloc_reducer(replication=\"all\")` over-count): a `batch=1` defect (over-counts by `floor(threads/block_N)×`); here `batch=1` is *correct* and only `batch>1` fails.\n- **#2542** (`reduce_sum`/`max` non-power-of-two lane drop): independent of `batch`; a lane-count issue in the AllReduce butterfly, not a missing outer loop.\n- **#2524** (`reduce_sum`/`max` with `batch>1` workspace-stride vs block-index aliasing): the closest neighbor, but a *different op and file* (`T.reduce` via `reduce.h`, not `T.finalize_reducer` via `finalize_reducer.h`) and a *different mechanism* — #2524 is workspace aliasing and is correct when `reducing_threads == total_threads`, whereas this bug fails *exactly* at `reducing_threads(256) == total(256)` because the cause is a missing outer loop, not workspace aliasing. Fixing #2524 would not fix this.\n", "url": "https://github.com/tile-ai/tilelang/issues/2623"}
{"number": 2569, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.shfl_*` on `float16`/`bfloat16` fails to compile instead of shuffling the value", "state": "closed", "created_at": "2026-07-09T01:11:36Z", "updated_at": "2026-08-22T18:37:37Z", "closed_at": "2026-08-22T18:37:37Z", "labels": ["bug"], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n`0.1.11` (source pinned to tag `v0.1.11`, commit `cd37ed5fc35ae7a60a1277c8eb49028174ac51e6`).\n\n### System information\n\n\n- Installed from the `0.1.11` wheel; source pinned to tag `v0.1.11` (commit `cd37ed5fc35ae7a60a1277c8eb49028174ac51e6`).\n- Python 3.12.3, linux; PyTorch 2.12.1+cu130 (CUDA 13.0).\n- GPU: NVIDIA H100 80GB HBM3 (`sm_90`). The defect is in the CUDA codegen emit of `__shfl_*_sync` — a shared, target-independent lowering path (not arch-gated), so the same compile failure is expected on any CUDA target that reaches it; only `sm_90` was run this session.\n\n```python\nimport sys, tilelang, torch\nprint(sys.version.split()[0], sys.platform)                              # 3.12.3 linux\nprint(tilelang.__version__)                                              # 0.1.11\nprint(torch.__version__, torch.version.cuda)                             # 2.12.1+cu130 13.0\nprint(torch.cuda.get_device_name(), torch.cuda.get_device_capability())  # NVIDIA H100 80GB HBM3 (9, 0)\n```\n\n### Problem description\n\n\n`T.shfl_xor` / `T.shfl_down` / `T.shfl_up` / `T.shfl_sync` on `float16`/`bfloat16` fail to compile (`nvcc` error) when the shuffle result is assigned to a materialized temporary — because the CUDA codegen emits the raw `__shfl_*_sync` builtin directly ([codegen_cuda.cc#L3986-L4009](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/codegen/codegen_cuda.cc#L3986-L4009)) instead of routing through the `tl::shfl_*_sync` C++ wrappers ([common.h#L955-L973](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/tl_templates/cuda/common.h#L955-L973)) whose 16-bit-float specializations exist precisely to make this legal.\n\nFor a 16-bit-float operand the raw builtin binds to the `float` overload and **returns `float`**, so `bfloat16_t r = __shfl_xor_sync(...)` (a copy-initialization) hits `cutlass::bfloat16_t`'s missing `float` constructor:\n\n```\ntvm_kernels.cu(22): error: no suitable constructor exists to convert from \"float\" to \"cutlass::bfloat16_t\"\n    bfloat16_t r = __shfl_xor_sync((unsigned int)4294967295, v, 1, 32);\n```\n\nThe `tl::shfl_*_sync` wrappers ([`half_t`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/tl_templates/cuda/common.h#L977-L1009), [`bfloat16_t`](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/tl_templates/cuda/common.h#L1013-L1047)) already do the correct `raw16 → uint32 → __shfl_*_sync → raw16` round-trip and return the 16-bit-float type — but the codegen path never calls them, so those specializations are dead code on the `T.shfl_*` path. The 16-bit float types are the only ones that break: `int32`/`int8`/`float32` were tested and compile with the correct lane permutation (see matrix below), and the other native/32-bit-plus types (`int16`/`int64`/`uint32`/`float64`) are expected to compile for the same reason — the raw builtin has native or safe-promoting overloads for them, and only fp16/bf16 lack a lossless `float`→type conversion.\n\nThe failure is code-shape dependent, and differently so for the two 16-bit float types:\n\n<details><summary>Trigger / control matrix (TESTED on H100 sm_90 this session)</summary>\n\n| form | `int32`/`int8`/`fp32` | `float16` | `bfloat16` |\n|---|---|---|---|\n| materialized temp (`r = T.shfl_xor(v,1); O[tx]=r`) | MATCH | **fails to compile** | **fails to compile** |\n| inline store (`O[tx] = T.shfl_xor(A[tx],1)`) | MATCH | MATCH | **fails to compile** |\n\n- **`bfloat16`**: fails in **both** forms — there is no `float`↔`bfloat16_t` conversion at all, so even the inline store fails (`error: no operator \"=\" matches these operands`).\n- **`float16`**: fails only in the temp form. The inline `O[tx] = __shfl_xor_sync(...)` compiles because assigning a `float` result into a `half_t` element is an *implicit* conversion (`half_t` has a `float` constructor), whereas the temp `half_t r = __shfl_xor_sync(...)` is copy-initialization from a `float` that here does not resolve. **The inline-`float16` path that compiles is numerically correct** — verified as an exact lane permutation (lane `L` receives lane `L^1`, 32/32 exact, matching the `float32` control), so this is a **compile-time failure only, not a silent miscompute**; the value round-trips losslessly for exactly-representable inputs.\n\nThe matrix above was run with `shfl_xor`. The other three ops (`shfl_down`, `shfl_up`, `shfl_sync`) share the identical raw-emit branch in the same codegen block ([L3986-L4009](https://github.com/tile-ai/tilelang/blob/cd37ed5fc35ae7a60a1277c8eb49028174ac51e6/src/cuda/codegen/codegen_cuda.cc#L3986-L4009) — each emits `__shfl_*_sync` directly with no wrapper routing), so they are expected to fail identically for `fp16`/`bf16` in the temp form for the same reason.\n\n</details>\n\n### Reproducible example code\n\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\n\ndef make_temp(dt):     # materialized-temp form: r = shfl(...); O[tx] = r\n    @T.prim_func\n    def k(A: T.Tensor((32,), dt), O: T.Tensor((32,), dt)):\n        with T.Kernel(1, threads=32):\n            tx = T.get_thread_binding()\n            v = A[tx]\n            r = T.shfl_xor(v, 1, 32)\n            O[tx] = r\n    return k\n\n\ndef make_inline(dt):   # inline-store form: O[tx] = shfl(A[tx], ...)\n    @T.prim_func\n    def k(A: T.Tensor((32,), dt), O: T.Tensor((32,), dt)):\n        with T.Kernel(1, threads=32):\n            tx = T.get_thread_binding()\n            O[tx] = T.shfl_xor(A[tx], 1, 32)\n    return k\n\n\ndef run(kernel_fn, dt):\n    try:\n        kk = tilelang.compile(kernel_fn(dt), target=\"cuda\")\n    except Exception as e:\n        line = next((l.strip() for l in str(e).splitlines() if \"error:\" in l), type(e).__name__)\n        return f\"COMPILE ERROR :: {line[:110]}\"\n    A = (torch.arange(32, dtype=torch.float64).to(getattr(torch, dt)).cuda()\n         if \"float\" in dt else torch.arange(32, dtype=getattr(torch, dt), device=\"cuda\"))\n    O = torch.full((32,), -99, dtype=torch.int64).to(getattr(torch, dt)).cuda()\n    kk(A, O); torch.cuda.synchronize()\n    got = O.to(torch.int64).tolist()\n    ref = [L ^ 1 for L in range(32)]         # shfl_xor(v, 1): lane L receives lane L^1\n    return \"MATCH\" if got == ref else f\"MISMATCH got={got[:6]} ref={ref[:6]}\"\n\n\nprint(\"temp  int32   :\", run(make_temp,   \"int32\"))     # -> MATCH   (control)\nprint(\"temp  float16 :\", run(make_temp,   \"float16\"))   # -> COMPILE ERROR\nprint(\"temp  bfloat16:\", run(make_temp,   \"bfloat16\"))  # -> COMPILE ERROR\nprint(\"inline float16:\", run(make_inline, \"float16\"))   # -> MATCH   (control)\nprint(\"inline bfloat16:\", run(make_inline, \"bfloat16\")) # -> COMPILE ERROR\n```\n\n### Traceback\n\n```pytb\n<details><summary>nvcc error (bfloat16, temp form) — the emitted line and the diagnostic</summary>\n\n\n// emitted CUDA (codegen_cuda.cc), materialized temp:\nbfloat16_t r = __shfl_xor_sync((unsigned int)4294967295, v, 1, 32);\n\ntvm_kernels.cu(22): error: no suitable constructor exists to convert from \"float\" to \"cutlass::bfloat16_t\"\n    bfloat16_t r = __shfl_xor_sync((unsigned int)4294967295, v, 1, 32);\n1 error detected in the compilation of \"tvm_kernels.cu\".\n\n\nFor `bfloat16` inline it is instead `error: no operator \"=\" matches these operands` on\n`O[...] = __shfl_xor_sync(...)`. For the working controls the emit is the native path,\ne.g. `int r = __shfl_xor_sync((uint)4294967295, v, 1, 32);` (compiles).\n\n</details>\n```\n\n### Expected behavior\n\n`T.shfl_*` should compile and return the shuffled 16-bit-float value for `float16`/`bfloat16`, regardless of whether the result is stored inline or through a temporary — the same as it already does for the integer and `float32`/`float64` dtypes. The project already ships the correct machinery for this: the `tl::shfl_*_sync` `half_t`/`bfloat16_t` specializations in `common.h` (added by #1169 specifically to support 16-bit shuffle) do the bit-round-trip and return the right type; the codegen simply does not call them.\n\n\n### Additional context\n\n\n**Suggested fix.** Have the `shfl_*` emit call the `tl::shfl_*_sync` wrapper (which already handles the 16-bit-float packing and matches how `reduce.h` uses it) instead of the raw `__shfl_*_sync` builtin — e.g. emit `tl::shfl_xor_sync(mask, value, lane, width)`. Verified in a standalone `nvcc` probe on sm_90 against the shipped cutlass headers: the temp assignment `bfloat16_t r = tl::shfl_xor_sync(mask, v, 1);` compiles cleanly, whereas the raw-builtin form gives the `no suitable constructor` error above. (Codegen is compiled C++, so a shipped fix needs a rebuild; this is a proposed fix probed at the nvcc level, not built end-to-end.)\n\n**Provenance.** The `T.shfl_*` TIR builtins and this raw-`__shfl_*_sync` codegen emit were both introduced together in [#1858](https://github.com/tile-ai/tilelang/pull/1858) (\"Add TIR builtins for warp-level vote and block-level predicate sync\", merged 2026-04-13). The `tl::shfl_*_sync` 16-bit-float wrappers that would fix it predate it: they were added in [#1169](https://github.com/tile-ai/tilelang/pull/1169) (\"Support 16bits shfl_sync\", merged 2025-10-31) and wired into `reduce.h`, but #1858's new codegen path did not route through them. The codegen file was later moved (`src/target/` → `src/cuda/codegen/`) by refactors #2121/#2297 — those only relocated the emit, they did not introduce it.\n\n**Relationship to #1162 / #1169 (same root, different site).** This is the *same* `no suitable constructor exists to convert from \"float\" to \"cutlass::bfloat16_t\"` failure that [#1162](https://github.com/tile-ai/tilelang/issues/1162) reported for `T.reduce_max` on `bfloat16` — there it fired on the `__shfl_down_sync` inside `reduce.h`. #1169 fixed *that* site (and closed #1162) by adding the `tl::shfl_*_sync` 16-bit-float wrappers and routing `reduce.h` through them. This report is a **fresh occurrence of the same root cause on a new, un-routed path**: #1858's `T.shfl_*` codegen emits the raw builtin directly and never calls those wrappers, so `reduce.h` is fixed while the user-facing `T.shfl_*` builtins are not. So this is not a re-report of #1162 (which is fixed) — it is the identical defect resurfacing where #1169's fix was never applied.\n\nI searched the open and closed tracker (`shfl`, `shuffle`, `bfloat16 shuffle`, `__shfl_xor_sync`, `warp shuffle half`, `no suitable constructor bfloat16`) and found no existing report of this defect on the `T.shfl_*` path. It is distinct from #2478 (`T.atomic_addx4` on fp16/bf16 — a different op and codegen site).\n", "url": "https://github.com/tile-ai/tilelang/issues/2569"}
{"number": 1729, "title": "[Feature Request] Improve layout when small fragments cause excessive thread replication", "state": "closed", "created_at": "2026-01-26T06:25:53Z", "updated_at": "2026-08-21T06:47:04Z", "closed_at": "2026-08-21T06:47:04Z", "labels": ["enhancement"], "comments_count": 7, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### Motivation\n\nTileLang code:\n```python\nimport tilelang\nimport tilelang.language as T\n\n\n@tilelang.jit\ndef get_qwq():\n    @T.prim_func\n    def main(\n        A: T.Tensor[(2, 2560), T.float32],\n        B: T.Tensor[(2, 2560), T.float32],\n        C: T.Tensor[(2, ), T.float32]\n    ):\n        with T.Kernel(1, threads=256):\n            tx = T.get_thread_binding(0)\n\n            C_local = T.alloc_fragment((2, ), T.float32)\n            T.copy(C, C_local)\n\n            for i, j in T.Parallel(2, 2560):\n                if C_local[i] >= 0:\n                    B[i, j] = A[i, j]\n\n    return main\n\n\nkernel = get_qwq()\nprint(kernel.get_kernel_source())\n```\n\ngenerated code:\n```cuda\n#include <tl_templates/cuda/gemm.h>\n#include <tl_templates/cuda/copy.h>\n#include <tl_templates/cuda/reduce.h>\n#include <tl_templates/cuda/ldsm.h>\n#include <tl_templates/cuda/threadblock_swizzle.h>\n#include <tl_templates/cuda/debug.h>\n#ifdef ENABLE_BF16\n#include <tl_templates/cuda/cuda_bf16_fallbacks.cuh>\n#endif\n\nextern \"C\" __global__ void main_kernel(const float* __restrict__ A, float* __restrict__ B, const float* __restrict__ C);\nextern \"C\" __global__ void __launch_bounds__(256, 1) main_kernel(const float* __restrict__ A, float* __restrict__ B, const float* __restrict__ C) {\n  float C_local[1];\n  C_local[0] = C[(((int)threadIdx.x) & 1)];\n  if ((((int)threadIdx.x) >> 1) == 0) {\n    #pragma unroll\n    for (int i = 0; i < 640; ++i) {\n      float4 v_ = *(float4*)(A + (((((int)threadIdx.x) & 1) * 2560) + (i * 4)));\n      tl::store_global_128_conditional((&(B[(((((int)threadIdx.x) & 1) * 2560) + (i * 4))])), (*(uint4 *)(&(v_))), (0x0p+0f/*0.000000e+00*/ <= C_local[0]));\n    }\n  }\n}\n```\n\nIn the generated kernel, only threads 0 and 1 perform the global memory read/write, leaving most threads idle. This indicates a suboptimal layout and thread mapping, and there should be room to improve the layout.\n\n### Solution\n\n_No response_\n\n### Alternatives\n\n_No response_\n\n### Additional context\n\n_No response_", "url": "https://github.com/tile-ai/tilelang/issues/1729"}
{"number": 2688, "title": "[BUG][Fuzzer][wrong-code] Vectorized `T.atomic_add` silently downgrades the requested `memory_order` to relaxed", "state": "closed", "created_at": "2026-07-17T04:28:11Z", "updated_at": "2026-08-20T05:34:01Z", "closed_at": "2026-08-20T05:34:01Z", "labels": [], "comments_count": 1, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the Issue Tracker that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.12 (reproduced this session on the released `0.1.12` wheel). Source on `main` re-checked at `d36ec37c596ff8e587fc67fcf1e88bcb90cc60d6` (and the offending line is unchanged at current `main` HEAD `c5e53076`).\n\n### System information\n\nNVIDIA L40S (`sm_89`), CUDA 12.8, Python 3.13, tilelang 0.1.12. The defect is in target-independent transform/codegen (the atomic-add vectorizer and its CUDA codegen), so it is not specific to one GPU.\n\n### Problem description\n\nA tile-region `T.atomic_add(Out, Src, memory_order=\"acq_rel\")` on `float16`/`bfloat16` (or `float32`) silently produces a **relaxed** atomic whenever the add auto-vectorizes to `AtomicAddx2`/`AtomicAddx4`, even though the *identical* call lowers scalar-wise — and correctly emits the requested ordering — when it does not vectorize. Whether it vectorizes is decided by whether the per-thread inner-loop extent shares a factor ≥2 with the atomic vector width (`GCD(vector_width, extent) ≥ 2`, i.e. the extent is divisible by 2 for the fp16/bf16 `x2` path or by 2/4 for the fp32 `x4` path) — a tiling detail unrelated to memory ordering. No error or warning is raised at any stage; the kernel compiles and runs, and the requested memory ordering is just gone.\n\nConcretely, the same `T.atomic_add(Out[i], X[i], memory_order=\"acq_rel\")` in a `T.Parallel` loop emits (both verified this session, fp16, `threads=32`):\n\n| per-thread extent | lowering | emitted CUDA | ordering |\n|---|---|---|---|\n| 3 (`N=96`) — `GCD(2,3)=1`, no vectorize | scalar | `AtomicAdd(&Out[...], Src[...], 4)` | acq_rel (id 4) — **honored** |\n| 2 (`N=64`) — `GCD(2,2)=2`, vectorizes | `AtomicAddx2` | `AtomicAddx2(&Out[tid*2], *(uint1*)(Src+tid*2))` | (no arg) → **relaxed** |\n\nSo identical public-API input yields different memory ordering depending purely on an invisible vectorization choice driven by the tile shape. This is not a regression — the vectorized atomic path has reconstructed the wide op from two operands since it first shipped.\n\n<details><summary>Emitted atomic lines, both paths (this session, 0.1.12)</summary>\n\n```\nextent 3 (no vectorize): AtomicAdd((&(Out[((int)threadIdx.x)])), Src[((int)threadIdx.x)], 4);\n   -> honors acq_rel (trailing \", 4)\"): True\nextent 2 (vectorizes)  : AtomicAddx2((&(Out[(((int)threadIdx.x) * 2)])), *(uint1*)(Src + (((int)threadIdx.x) * 2)));\n   -> honors acq_rel (trailing \", 4)\"): False   (silently relaxed)\n```\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\n\ndef mk(N, threads):\n    @T.prim_func\n    def k(X: T.Tensor((N,), \"float16\"), Out: T.Tensor((N,), \"float16\")):\n        with T.Kernel(1, threads=threads):\n            # identical tile-region atomic_add on both paths; only N/threads differ\n            T.atomic_add(Out[0:N], X[0:N], memory_order=\"acq_rel\")   # acq_rel == id 4\n    return k\n\ndef emitted_atomic(N, threads):\n    src = tilelang.compile(mk(N, threads), out_idx=-1, target=\"cuda\").get_kernel_source()\n    return [l.strip() for l in src.splitlines() if \"AtomicAdd\" in l][0]\n\n# SCALAR lowering (1 elem/thread): honors acq_rel -> \"AtomicAdd(..., 4)\"\nprint(emitted_atomic(130, 130))   # -> AtomicAdd(&Out[...], X[...], 4);   (\"...4)\" present)\n\n# VECTORIZED lowering (4 elem/thread, SAME call): drops it -> \"AtomicAddx2(...)\" no order arg\nprint(emitted_atomic(256, 64))    # -> AtomicAddx2(&Out[...], *(uint1*)(X+...));   (order gone -> relaxed)\n```\n\n### Traceback\n\nNo traceback — the kernel compiles and runs to completion on both paths; the vectorized path silently emits a weaker (relaxed) atomic than requested, with no error or warning.\n\n### Expected behavior\n\nA vectorized `T.atomic_add` should carry the same requested `memory_order` its scalar sibling does — the wide device helpers already accept and honor a `memory_order` argument (they branch to explicit ordered PTX for a non-relaxed order), so the requested `acq_rel` seems computable on the vectorized path too, not just the scalar one. Grounding: the compiler already emits `AtomicAdd(..., 4)` for the identical tile-region call when it lowers scalar-wise, so honoring the order is the behavior the code already exhibits on one of its two lowerings of the same input.\n\n(One doc note for the maintainers: the `atomic_add` docstring currently says \"the tile-region path ignores `memory_order`\", but the scalar tile-region lowering demonstrably *does* honor it, so that line reads as stale — the intent appears to be to honor the order, which makes the vectorized drop the defect. Reconciling the docstring either way would remove the ambiguity.)\n\n### Additional context\n\n**Root cause.** The atomic-add vectorizer does not carry the operation's memory-order operand when it rewrites a tile-region atomic add into its wide (x2/x4) form, so the wide atomic is generated without any ordering and silently falls back to relaxed. In detail: the tile-region lowering attaches the requested order as the third operand of the scalar `atomic_add_elem_op` ([`atomic_add.cc#L185-L192`](https://github.com/tile-ai/tilelang/blob/d36ec37c596ff8e587fc67fcf1e88bcb90cc60d6/src/cuda/op/atomic_add.cc#L185-L192), pushing `op.GetMemoryOrder()` — [`atomic_reduce.h#L47-L54`](https://github.com/tile-ai/tilelang/blob/d36ec37c596ff8e587fc67fcf1e88bcb90cc60d6/src/op/atomic_reduce.h#L47-L54)); the vectorizer then reconstructs the wide op from **only** the destination and source and discards that operand ([`vectorize_loop.cc#L629-L664`](https://github.com/tile-ai/tilelang/blob/d36ec37c596ff8e587fc67fcf1e88bcb90cc60d6/src/transform/vectorize_loop.cc#L629-L664) — the return is `Call(op->dtype, GetVectorizedAtomicOp(vector_size), {dst, src})`, two operands only).\n\n<details><summary>Why the dropped operand changes the emitted ordering</summary>\n\nThe CUDA codegen for both the scalar and the wide atomics appends the order only when it is present as a third operand:\n\n- scalar [`AtomicAdd` — codegen_cuda.cc#L4597-L4608](https://github.com/tile-ai/tilelang/blob/d36ec37c596ff8e587fc67fcf1e88bcb90cc60d6/src/cuda/codegen/codegen_cuda.cc#L4597-L4608): `AtomicAdd(dst, src)` then `if (op->args.size() > 2) stream << \", \" << args[2];`\n- wide [`AtomicAddx2`/`AtomicAddx4` — codegen_cuda.cc#L4620-L4643](https://github.com/tile-ai/tilelang/blob/d36ec37c596ff8e587fc67fcf1e88bcb90cc60d6/src/cuda/codegen/codegen_cuda.cc#L4620-L4643): identical `if (op->args.size() > 2)` guard.\n\nAfter the vectorizer drops the operand, `op->args.size()` is 2, so the guard is false and no order is emitted. The wide device helper then takes its default: [`AtomicAddx2(half_t*, ValType, int memory_order = relaxed)` — atomic.h#L557-L577](https://github.com/tile-ai/tilelang/blob/d36ec37c596ff8e587fc67fcf1e88bcb90cc60d6/src/tl_templates/cuda/atomic.h#L557-L577) branches on the order — relaxed → a plain `atomicAdd`; non-relaxed → explicit ordered PTX (`tl_atomic_add_v2_f16` with fences). So the requested `acq_rel` (id 4) becomes relaxed (id 0), silently. `AtomicAddx4` and the `float32` wide wrappers share the same default-relaxed / branch shape, so `fp32 → AtomicAddx4` is the same mechanism (I exhibited the `fp16 → AtomicAddx2` face; the x4 face is inferred from the identical guard and helper, not separately run).\n\n</details>\n\n**Suggested fix (proposed, not built).** One direction that fits the existing wiring is to carry the memory-order operand through the wide reconstruction — append the original op's third operand (when present) to the `{dst, src}` argument list in the vectorizer — so the wide `AtomicAddx2`/`AtomicAddx4` codegen and device helpers receive the order exactly as the scalar path does. The codegen and device helpers already accept it (the `if (op->args.size() > 2)` guard and the `memory_order` parameter are in place); only the vectorizer's two-operand reconstruction appears to drop it.\n\n**Distinct from #2382/#2574/#2573.** #2382 (closed, fixed by #2414) is the *same* vectorizer function but a different defect — the wide helper bit-reinterprets instead of converting when the source dtype differs from the destination; its fix touched only `atomic.h` and never the operand reconstruction, so the memory-order drop is untouched (`{dst, src}` still stands at `main`). #2574 is an *illegal* memory order on `atomic_load`/`atomic_store` reaching a device assert (a missing frontend legality check; the order is carried, not dropped). #2573 is an `int64` `atomic_max`/`atomic_min` compile failure with no memory-order involvement. I searched the open and closed tracker and found no existing report of the memory-order drop on the vectorized `atomic_add` path.\n\n**Provenance.** Not a regression — the vectorized atomic path has reconstructed the wide op from two operands (`{addr_dst, addr_src}` / `{dst, src}`) since it first shipped: it existed in the pre-refactor `atomicadd_vectorize.cc`, was relocated into `vectorize_loop.cc` (with `MutateAtomicAddExpr_`) by [#1677](https://github.com/tile-ai/tilelang/pull/1677) (`5feb2253`, merged 2026-01-16), and the line was last moved by the #2166 refactor (`431c85f1`, 2026-05-12). The `memory_order` argument for atomics arrived alongside it in [#1676](https://github.com/tile-ai/tilelang/pull/1676) (`b27fb928`, merged 2026-01-16, an ancestor of #1677), so from the moment a non-relaxed order could be requested the vectorized path has never carried it. (Origins verified via `git log`/`git blame` and `gh pr view`; the exact PR that first added the two-operand form pre-refactor is not pinned beyond \"no later than #1677\".)\n\n**Reach.** Triggering requires a tile-region `T.atomic_add` (a region argument such as `Out[0:N]`, which is what routes to the wide helpers) with a non-relaxed `memory_order` *and* a per-thread inner-loop extent divisible by the dtype's atomic vector width (2 for fp16/bf16, 2-or-4 for fp32), so the add auto-vectorizes. A non-relaxed order is the rare ingredient — `relaxed` is the default, so a program only hits this if it explicitly asks for ordering; the divisible extent is common (the 128-multiple tiles typical of reduction/scatter kernels split to an even per-thread extent), so once a kernel does request ordering on a region it will usually take the dropping path. `grep -rn` over `examples/` and `testing/` this session: the shipped examples that pass `memory_order=` all pass `\"relaxed\"` (`examples/norm/layernorm.py:127-128`, `examples/flash_attention/example_gqa_bwd_tma_reduce_varlen.py:476`), so the downgrade is a no-op for them. The atomic tests that use a non-relaxed order (`testing/python/language/test_tilelang_language_atomic.py:104`, `memory_order=\"release\"`; and `:46`) index a single element (`B[idx_i, idx_j]`), which routes to the scalar/extern `AtomicAdd` path — the one that honors the order — never the region/vectorized path; and `test_atomic_addx2` calls `T.atomic_addx2` directly, which has no `memory_order` parameter. So no example or test exercises the region + non-relaxed-order + even-count intersection, and the value-comparison tests would not catch an ordering downgrade regardless (they check a sum, which has no cross-thread ordering dependency).\n", "url": "https://github.com/tile-ai/tilelang/issues/2688"}
{"number": 2985, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.max`/`T.min`/`T.clamp` on `float8_e4m3`/`float8_e5m2` fail to compile instead of computing the value", "state": "closed", "created_at": "2026-08-15T20:10:49Z", "updated_at": "2026-08-18T09:50:29Z", "closed_at": "2026-08-18T09:50:29Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\n- Installed from the `0.1.13` wheel.\n- Python 3.13, linux; PyTorch with CUDA 12.8.\n- GPU: NVIDIA L40S (`sm_89`) — where this session's run was done. The defect is in the target-independent binary-op codegen (`MaxNode`/`MinNode` → bare `max(...)`/`min(...)`) plus the `float8` template type, so it is architecture-independent (not arch-gated); only `sm_89` was run this session.\n\n### Problem description\n\n`T.max`, `T.min`, and `T.clamp` applied to `float8`-typed operands fail to compile — nvcc rejects the store with `no operator \"=\" matches these operands / operand types are: fp8_e4_t = float`. The same kernel with `float16` or `bfloat16` operands compiles and returns the correct value, so this is a `float8` coverage gap, not a \"this dtype can't be compared\" limitation.\n\nThe codegen emits a bare `max(A[i], B[i])` for an fp8-typed `MaxNode`. There is no `max(fp8, fp8) -> fp8` overload: the arguments promote to `float`, so `max(...)` returns `float`, and `tl::float_e4m3_t` / `tl::float_e5m2_t` have no `operator=(float)` (their only converting constructor is from `__nv_bfloat16`, and it is `explicit`), so the assignment `C[i] = max(...)` does not compile.\n\nBoth `float8` variants fail identically (`fp8_e4_t = float` for `e4m3`, `fp8_e5_t = float` for `e5m2`), and all three ops share the mechanism: `T.clamp` lowers to nested `T.max`/`T.min`, so it hits the same missing overload. All six (op × dtype) pairs were run this session and all six fail with the identical `no operator \"=\" matches` store error. Not a regression — see Provenance.\n\n<details>\n<summary>Emitted CUDA and the per-op / per-dtype result</summary>\n\nThe generated kernel for the `float8_e4m3` `T.max` case:\n\n```cuda\nextern \"C\" __global__ void main_kernel(const fp8_e4_t* A, const fp8_e4_t* B, fp8_e4_t* C) {\n  C[((int)threadIdx.x)] = max(A[((int)threadIdx.x)], B[((int)threadIdx.x)]);\n}\n```\n\nCompile result of `C[i] = T.<op>(A[i], B[i])` (and `T.clamp(A[i], lo, hi)`) per dtype, this session:\n\n| op | float16 | bfloat16 | float8_e4m3 | float8_e5m2 |\n|---|---|---|---|---|\n| `T.max`   | ok | ok | fail (`fp8_e4_t = float`) | fail (`fp8_e5_t = float`) |\n| `T.min`   | ok | ok | fail | fail |\n| `T.clamp` | ok | ok | fail | fail |\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\nN = 128\n\n@tilelang.jit(out_idx=[2])\ndef maxk(dtype):\n    @T.prim_func\n    def main(A: T.Tensor((N,), dtype), B: T.Tensor((N,), dtype), C: T.Tensor((N,), dtype)):\n        with T.Kernel(1, threads=N):\n            i = T.get_thread_binding()\n            C[i] = T.max(A[i], B[i])\n    return main\n\n# control: float16 compiles and matches torch.maximum\na16 = torch.randn(N, device=\"cuda\").to(torch.float16)\nb16 = torch.randn(N, device=\"cuda\").to(torch.float16)\nc16 = maxk(\"float16\")(a16, b16)\nprint((c16.float() - torch.maximum(a16, b16).float()).abs().max().item())   # -> 0.0\n\n# bug: float8_e4m3 fails to compile\na8 = torch.randn(N, device=\"cuda\").to(torch.float8_e4m3fn)\nb8 = torch.randn(N, device=\"cuda\").to(torch.float8_e4m3fn)\nmaxk(\"float8_e4m3\")(a8, b8)   # -> nvcc error: no operator \"=\" matches these operands (fp8_e4_t = float)\n```\n\n`T.min` and `T.clamp` fail the same way; `float8_e5m2` behaves identically to `float8_e4m3`.\n\n### Traceback\n\n```pytb\ntvm_kernels.cu(14): error: no operator \"=\" matches these operands\n            operand types are: fp8_e4_t = float\n    C[((int)threadIdx.x)] = max(A[((int)threadIdx.x)], B[((int)threadIdx.x)]);\n```\n\n### Expected behavior\n\n`T.max`/`T.min`/`T.clamp` on `float8` should compile and return the elementwise result, as they already do for `float16`/`bfloat16` (verified: `float16` returns `max_err = 0.0` on the same kernel). The operation is computable in `float8`: writing the max in `float` and converting back — `C[i] = T.Cast(dtype, T.max(T.Cast(\"float32\", A[i]), T.Cast(\"float32\", B[i])))` — compiles and returns the exact result (`max_err = 0.0`, verified this session), which is the same route TileLang already uses to close comparable `float8` coverage gaps.\n\n### Additional context\n\n**Root cause.** An fp8 `max`/`min` codegens to a bare `max(...)`/`min(...)` call whose `float` result has no way back into the fp8 wrapper type — the wrapper has no `operator=(float)` — so the store fails to compile.\n\n<details><summary>Mechanism</summary>\n\nThe CUDA codegen prints an fp8-typed `MaxNode`/`MinNode` as a bare `max(...)`/`min(...)` call ([`codegen_c.cc` `PrintBinaryExpr`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L514-L534), reached from the [Max/Min visitors](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L587-L591)), but no `max`/`min` overload returns `float8`: the fp8 operands promote to `float`, so `max(...)` is `float`, and the [`tl::float_e4m3_t` / `float_e5m2_t` wrapper types](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/common.h#L808) have no `operator=(float)` — their [only converting constructor from a scalar float type is `explicit float_e4m3_t(__nv_bfloat16)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/common.h#L814-L817) — so the store `C[i] = max(...)` fails to compile. `T.clamp` lowers to nested `T.max`/`T.min`, so it inherits the same failure.\n\n</details>\n\n**Generalization (tested this session, 0.1.13 / sm_89 / L40S).** This is a *missing-overload* class, not a per-op quirk. All six (`max`/`min`/`clamp`) × (`float8_e4m3`/`float8_e5m2`) pairs fail with the identical store error (`fp8_e4_t = float` / `fp8_e5_t = float`); the `float16`/`bfloat16` controls all compile and match torch; and the `float`-cast route (`T.Cast(dtype, T.max(T.Cast(\"float32\", …), …))`) compiles and returns `max_err = 0.0`. The neighbors share one root — the codegen emits a bare `max`/`min` whose `float` result has no `operator=` on the fp8 wrapper — so the title and description are framed to the whole (op × fp8) class with `max` as the worked example.\n\n**Suggested fix.** One option that matches how the `float16`/`bfloat16` cases already work is a `float8` overload of the emitted symbols that computes in `float` and converts back — `TL_DEVICE tl::float_e4m3_t max(tl::float_e4m3_t a, tl::float_e4m3_t b) { return tl::float_e4m3_t(max(float(a), float(b))); }` (and `min`, for both `e4m3` and `e5m2`) — so the result type matches the store target instead of resolving to `float`. This is the same shape as the `float` cast the manual sibling path above already uses successfully.\n\n**Provenance.** Present since the `float8` wrapper types were wired: the `tl::float_e4m3_t`/`float_e5m2_t` structs (common.h) carry no `operator=(float)` and no `max`/`min` overload, and the codegen has always emitted a bare `max(...)`/`min(...)` for `MaxNode`/`MinNode`. Not a regression — the (`max`/`min`/`clamp`, `float8`) pairs have never compiled on this path.\n\n**Dedup.** I searched the open and closed tracker (and PRs) and found no existing report of the `max`/`min`/`clamp` on `float8` compile failure. Distinct from [#2565](https://github.com/tile-ai/tilelang/issues/2565), which covers the same *class* of gap for the unary math intrinsics (`exp`/`log`/`sqrt`/`erf`/…) — but its `float8` face is a *frontend* failure (`Unresolved call ir.Op ... name=\"tirx.exp\"`; the op never reaches codegen), and its op set is disjoint from the binary `max`/`min`/`clamp` here, which *do* reach codegen and fail at nvcc.\n\n**Reach.** The trigger is `T.max`/`T.min`/`T.clamp` with `float8`-typed operands. The op docstrings type the operands as generic `PrimExpr` with no dtype disclaimer, and `float8_e4m3`/`float8_e5m2` are first-class TileLang dtypes; clamping a value into the fp8 representable range is a standard fp8-quantization idiom. `grep`: the shipped fp8-cast examples that clamp (`examples/cast/example_per_token_cast_to_fp8.py:35`, `example_group_per_split_token_cast_to_fp8.py`, `example_triton_cast_to_fp8.py`) all `T.clamp` a value that is still in the *input* dtype (`float16`/`bfloat16`) and only then `T.copy` into a separate `float8` buffer — so they clamp in the wide type and never apply `max`/`min`/`clamp` to an fp8-typed value, which is why CI is green. The bug fires the moment the clamped/compared value is itself `float8` (e.g. clamping an already-`float8` accumulator, or writing `T.max` directly on `float8` operands).\n\n**Impact.** The trigger is narrow — `T.max`/`T.min`/`T.clamp` applied to an operand whose dtype is itself `float8` (the shipped fp8-cast idiom clamps in the wide type first, so this misses CI). When it fires the failure is a compile-time nvcc error, loud and caught immediately: it blocks that one kernel from building but corrupts no data and cannot reach a running workload silently. Fixing it closes an fp8 coverage gap so a legal `float8` kernel compiles, matching the `float16`/`bfloat16` behavior; it removes a build-time refusal, not a silent wrong result.", "url": "https://github.com/tile-ai/tilelang/issues/2985"}
{"number": 2989, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.__ldg` on `float16`/`bfloat16` fails to compile instead of loading the value", "state": "closed", "created_at": "2026-08-15T20:11:03Z", "updated_at": "2026-08-17T06:12:54Z", "closed_at": "2026-08-17T06:12:54Z", "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar issue was found.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 · CUDA 13.0 (nvcc V13.0.88) · PyTorch 2.13.0 · GPU: NVIDIA L40S (sm_89)\n\n### Problem description\n\n`T.__ldg(x[i])` on a `float16` or `bfloat16` buffer produces CUDA source that nvcc rejects, so the kernel never compiles:\n\n```\ntvm_kernels.cu(13): error: no instance of overloaded function \"__ldg\" matches the argument list\n  B[((int)threadIdx.x)] = __ldg(&(A[((int)threadIdx.x)]));\n```\n\nThe same kernel on `float32` and `int32` compiles and runs correctly (see the control in the repro). The `__ldg` docstring documents `T.__ldg(x[i])` as emitting `__ldg(&x[i])` on CUDA with no dtype restriction, and `float16`/`bfloat16` are core TileLang tensor dtypes, so this is a supported input that fails to build.\n\nNot a regression — present since `T.__ldg` was introduced (see Provenance).\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\ndef build(dtype):\n    @T.prim_func\n    def main(A: T.Tensor((128,), dtype), B: T.Tensor((128,), dtype)):\n        with T.Kernel(1, threads=128) as bx:\n            i = T.get_thread_binding(0)\n            B[i] = T.__ldg(A[i])\n    return main\n\ndef run(dtype):\n    try:\n        k = tilelang.compile(build(dtype), out_idx=[1], target=\"cuda\")\n    except Exception as e:\n        print(f\"{dtype:>9}: COMPILE-FAIL (no instance of overloaded __ldg)\")\n        return\n    a = torch.randn(128, device=\"cuda\").to(getattr(torch, dtype))\n    print(f\"{dtype:>9}: ok, matches input = {torch.equal(k(a), a)}\")\n\nrun(\"float32\")   # -> ok, matches input = True   (control)\nrun(\"int32\")     # -> ok, matches input = True   (control)\nrun(\"float16\")   # -> COMPILE-FAIL\nrun(\"bfloat16\")  # -> COMPILE-FAIL\n```\n\n### Traceback\n\n<details><summary>nvcc error (float16; bfloat16 is identical)</summary>\n\n```\ntvm_kernels.cu(13): error: no instance of overloaded function \"__ldg\" matches the argument list\n  B[((int)threadIdx.x)] = __ldg(&(A[((int)threadIdx.x)]));\ncuda_fp16.hpp(1896): note: function \"__ldg(const __half *)\" does not match because argument #1 does not match parameter\ncuda_bf16.hpp(1825): note: function \"__ldg(const __nv_bfloat16 *)\" does not match because argument #1 does not match parameter\n...\n```\n\nThe emitted kernel signature is `const half_t* __restrict__ A` (for bf16: `const bfloat16_t*`), and the call is `__ldg(&(A[i]))`.\n\n</details>\n\n### Expected behavior\n\n`T.__ldg` on `float16`/`bfloat16` should compile and load the element through the read-only cache, matching what it already does for `float32`/`int32`. CUDA provides the overloads that make this computable — `__ldg(const __half*)` and `__ldg(const __nv_bfloat16*)` (the nvcc notes above list them) — so the value is loadable; only the pointer type reaching `__ldg` is wrong.\n\n### Additional context\n\n**Root cause.** The `tl.__ldg` codegen emits `__ldg(&(<ref>))` unconditionally, but for `float16`/`bfloat16` the buffer element type is TileLang's `cutlass::half_t` / `cutlass::bfloat16_t` wrapper, not CUDA's `__half` / `__nv_bfloat16`. So the argument is `const half_t*`, and none of CUDA's `__ldg` overloads (which take `const __half*` / `const __nv_bfloat16*` / built-in types) match.\n\n<details><summary>Where</summary>\n\n- Emit site: [`src/cuda/codegen/codegen_cuda.cc#L4101`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L4101) — `os << \"__ldg(&(\" << buffer_ref << \"))\";`, with no dtype-dependent cast of `buffer_ref`.\n- Frontend: [`tilelang/language/builtin.py#L50`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/builtin.py#L50) — accepts any-dtype `BufferLoad`.\n- The fp16/bf16 element types are [`cutlass::half_t` / `cutlass::bfloat16_t`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/common.h#L49-L50), which are layout-compatible with `__half`/`__nv_bfloat16` but are distinct C++ types for overload resolution.\n\n</details>\n\n**Generalization (tested this session, 0.1.13 / sm_89 / L40S).** This is a *missing-cast* class keyed on whether the buffer element type is a TileLang C++ *wrapper* or a native C type. Every dtype whose element type is a wrapper fails at the same emit with the identical error (`no instance of overloaded function \"__ldg\" matches the argument list`, emitted as `__ldg(&(A[i]))`): tested `float16` (`half_t`), `bfloat16` (`bfloat16_t`), and `float8_e4m3` (`fp8_e4_t`) all FAIL. Every dtype that maps to a native C type compiles and round-trips: tested `float32`, `int32`, `int16`, `int64`, `float64` all OK. So the boundary is wrapper-vs-native at the shared emit site, not any one dtype. The title stays on `float16`/`bfloat16` (the dominant half-precision compute dtypes) as the worked example; `float8_e4m3` is recorded here as a same-root neighbor a fix at `codegen_cuda.cc#L4101` should also cover.\n\nAlso turned the *operator* knob (sibling load intrinsics): `T.ldg32` on an `fp16` buffer **compiles** — the width-tagged `ldg32`/`ldg64`/`ldg128` take a different codegen path that does not pass the raw wrapper pointer to `__ldg`. So the defect is specific to the `T.__ldg` emit site (`codegen_cuda.cc#L4101`), not to load intrinsics in general — which localizes the fix to that one site rather than the whole load family. (`ldg64`/`ldg128` return `uint32x2`/`x4` and can't be stored into a scalar `fp16` slot — a harness type-mismatch, not part of this bug.)\n\n**Suggested fix.** At the emit site, when the buffer dtype is a wrapper type (`float16`/`bfloat16`, and by the same mechanism `float8_*`), reinterpret the pointer to CUDA's native type for the `__ldg` call (and wrap the result back), e.g. emit `half_t(__ldg(reinterpret_cast<const __half*>(&(<ref>))))` for fp16 and the `__nv_bfloat16` analogue for bf16. nvcc-probe verified: reinterpreting a `const half_t*` to `const __half*` at the `__ldg` call compiles (`nvcc -arch=sm_89`). Not built into the library end-to-end.\n\n**Provenance.** Introduced by [#1414](https://github.com/tile-ai/tilelang/pull/1414) (\"[Enhancement] Introduce `T.__ldg`\", merged 2025-12-12), which added both the frontend and this codegen branch; the fp16/bf16 case was never wired for the wrapper-vs-native type mismatch. Confirmed via `git log -S'tl::__ldg()'` on the emit site.\n\n**Dedup.** I searched the open and closed tracker (`__ldg`, `ldg half`) and found no existing report of this defect; the `__ldg` matches are unrelated feature requests. Distinct from #2569 (`T.shfl_*` on fp16/bf16 fails to compile): same underlying family — a codegen path passes the `cutlass::half_t`/`bfloat16_t` wrapper to a raw CUDA intrinsic that has no matching overload — but a different intrinsic (`__ldg`, pointer form) and a different emit site, so it needs its own cast at `codegen_cuda.cc#L4101`.\n\n**Reach.** `T.__ldg` is documented at [`builtin.py`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/builtin.py#L50) with no dtype restriction, and `float16`/`bfloat16` are the dominant compute dtypes for the GEMM/attention kernels TileLang targets, so a read-only-cache load of a half-precision operand is a natural use. The one shipped test, [`testing/python/language/test_tilelang_language_intrinsics_codegen.py`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/testing/python/language/test_tilelang_language_intrinsics_codegen.py), exercises `T.__ldg` only on `float32` and asserts on the generated string without recompiling for other dtypes (grepped this session: 1 site, all `float32`), so the fp16/bf16 path is never built in CI.\n\n**Impact.** The trigger is narrow — it needs `T.__ldg` applied to a `float16`/`bfloat16` operand specifically (the `float32`/`int32` paths compile). When it fires it is a compile-time nvcc error: the kernel refuses to build, loud and immediate, and it cannot silently corrupt a result or reach a running workload. Fixing it closes the wrapper-vs-native-type gap for the half-precision `__ldg` path so those valid kernels compile like the `float32` case already does.", "url": "https://github.com/tile-ai/tilelang/issues/2989"}
{"number": 3032, "title": "[BUG][Fuzzer][ice-on-invalid-code] `T.gemm` on a narrow dtype (`int8`/`fp8`/`bfloat16`) with any sub-atom `block_K` crashes nvcc instead of cleanly rejecting", "state": "open", "created_at": "2026-08-17T03:26:29Z", "updated_at": "2026-08-17T03:26:29Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nNVIDIA L40S (sm_89), CUDA 12.8 (nvcc), PyTorch 2.8.0. The defect is in the shared `mma.sync` macro generator and is not arch-gated within the sm_75–sm_89 tensor-core range (see Additional context).\n\n### Problem description\n\nA `T.gemm` whose reduction tile `block_K` is smaller than the operand dtype's MMA K-atom fails to compile: nvcc aborts with a `static_assert` `\"tl::mma_sync: unsupported configuration\"`. This affects the whole class of sub-atom tiles on the narrow dtypes whose atom exceeds a common `block_K` — verified this session on `int8` (atom 32, crashes at `block_K∈{8,16}`), `float8_e4m3` (atom 32, crashes at `block_K=16`), and `bfloat16` (atom 16, crashes at `block_K=8`). The identical kernel with `block_K` at or above the atom compiles and is correct in every case (see §17). `float16` at a sub-atom `block_K` does NOT crash — it silently miscomputes instead (distinct symptom, same upstream clamp; see §17).\n\nThe emitted call is `tl::mma_sync<AType, AType, CType, M=16, N=8, K=block_K, …>` with `K` below the dtype's atom. For `int8`/`fp8`/`bf16` no `MmaDispatcher` specialization matches that under-atom `K`, so the `static_assert` in the unspecialized `MmaDispatcher::exec` at [`mma.h#L167`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/instruction/mma.h#L167) fires. (`float16` differs only because mma.h *does* ship a real `m16n8k8` fp16 dispatcher at [`mma.h#L198`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/instruction/mma.h#L198), so the under-selected fp16 call binds to a valid-but-wrong-K instruction and miscomputes rather than crashing.)\n\nA sub-atom `block_K` has no legal lowering for these dtypes (one int8/fp8 MMA consumes 32 K-elements, one bf16 MMA consumes 16). The expected outcome is a clean compile-time rejection, not an nvcc back-end `static_assert`. Regression introduced by a change to `_initialize_k_dim` — see Provenance.\n\n<details><summary>nvcc error (int8, block_K=16)</summary>\n\n```\ntl::mma_sync<tl::DataType::kInt8, tl::DataType::kInt8, tl::DataType::kInt32, 16, 8, 16, false, true>(...);\nmma.h(282): error: static assertion failed with \"tl::mma_sync: unsupported configuration\"\n  static_assert(!std::is_void_v<typename Dispatcher::CRegType>, ...)\nmma.h(167): error: static assertion failed with \"tl::mma_sync: unsupported configuration\"\n  static_assert(always_false_v<std::integral_constant<int, M>>, ...)\n  [with AType=kInt8, BType=kInt8, CType=kInt32, M=16, N=8, K=16, ...]\n2 errors detected in the compilation of \"tvm_kernels.cu\".\n```\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\ndef gemm(M, N, K, dt, acc):\n    @T.prim_func\n    def main(A: T.Tensor((M, K), dt), B: T.Tensor((N, K), dt), C: T.Tensor((M, N), acc)):\n        with T.Kernel(1, threads=32):\n            sa = T.alloc_shared((M, K), dt); sb = T.alloc_shared((N, K), dt)\n            c = T.alloc_fragment((M, N), acc)\n            T.copy(A, sa); T.copy(B, sb); T.clear(c)\n            T.gemm(sa, sb, c, transpose_B=True)   # int8 tensor-core requires B transposed\n            T.copy(c, C)\n    return main\n\ndef run(tag, M, N, K, dt, acc, tdt):\n    try:\n        k = tilelang.compile(gemm(M, N, K, dt, acc), out_idx=[2])\n        a = torch.randint(-3, 3, (M, K), device=\"cuda\").to(tdt)\n        b = torch.randint(-3, 3, (N, K), device=\"cuda\").to(tdt)\n        out = k(a, b); exp = a.float() @ b.float().T\n        print(f\"{tag}: compiled, maxerr={(out.cpu().float()-exp.cpu()).abs().max().item()}\")\n    except Exception as e:\n        print(f\"{tag}: CRASH {type(e).__name__}\")\n\nrun(\"int8  block_K=16 (bug)    \", 16, 16, 16, \"int8\",    \"int32\",   torch.int8)      # -> CRASH (nvcc static_assert, K=16)\nrun(\"int8  block_K=8  (bug)    \", 16, 16,  8, \"int8\",    \"int32\",   torch.int8)      # -> CRASH (nvcc static_assert, K=8, same root)\nrun(\"int8  block_K=32 (control)\", 16, 16, 32, \"int8\",    \"int32\",   torch.int8)      # -> compiled, maxerr=0.0\nrun(\"fp16  block_K=16 (control)\", 16, 16, 16, \"float16\", \"float32\", torch.float16)   # -> compiled, maxerr~0\n```\n\n### Traceback\n\nThe failure surfaces as an nvcc back-end `static_assert` (shown collapsed above), not a TileLang-level diagnostic — the front-end accepts the kernel and emits an int8 MMA call that has no valid instruction.\n\n### Expected behavior\n\nA clean compile-time rejection at the TileLang level (e.g. \"`block_K` must be a multiple of the int8 MMA K-atom (32)\"). TileLang already has this exact guard on the same path — [`gemm_mma.py#L104`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/op/gemm/gemm_mma.py#L104): `assert block_K >= micro_size_k` — but it is defeated because `micro_size_k` is shrunk to `block_K` first (see Root cause). Restoring the true atom makes this existing assert fire cleanly (verified below).\n\n### Additional context\n\n**Root cause.** `_initialize_k_dim` picks an MMA K smaller than the operand dtype's real atom when `block_K` is small. It computes [`self.k_dim = min(256 // a_dtype.bits, self.chunk)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_macro_generator.py#L131), where `chunk` is `block_K`. For `int8` (8 bits) with `block_K=16`: `min(32, 16) = 16`. [`_initialize_mma_prefix`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_macro_generator.py#L167) then maps `k_dim==16` to `m16n8k16` — the `float16` atom — and `_initialize_micro_size` sets `micro_size_k = k_dim = 16`, so the guard at `gemm_mma.py#L104` sees `block_K(16) >= micro_size_k(16)` and passes. The `min(…)` clamp both selects a nonexistent int8 instruction and neutralizes the check that would have caught it.\n\n<details><summary>Why this reads as reject (no legal int8 target) and a fix sketch, verified before→after</summary>\n\nThe `int8` tensor-core MMA has no atom with `K<32`; `block_K=16` provides fewer K-elements than one int8 MMA consumes, so there is no legal lowering — the natural outcome is rejection. The Hopper WGMMA sibling on the same file never clamps: [`self.k_dim = 256 // DataType(a_dtype).bits`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_macro_generator.py#L952) (int8 → 32 regardless of `block_K`).\n\nOne direction that matches the WGMMA sibling: pin `k_dim` to the dtype atom rather than clamping it below the atom, so `micro_size_k` stays 32 for int8 and the existing `block_K >= micro_size_k` assert rejects `block_K=16` with a clear message. Whether the clamp is instead meant to enable a smaller-`block_K` int4 packed path (its introducing PR was int4-focused) is a maintainer call.\n\nVerified this session (0.1.13, sm_89): with `_initialize_k_dim` pinned to `256 // bits` (no clamp), `int8`/`block_K=16` now raises the clean `AssertionError: block_K (16) must be >= micro_size_k (32)` instead of the nvcc `static_assert`, and `int8`/`block_K=32` still compiles with `maxerr=0.0`.\n\n</details>\n\n**Suggested fix.** Pin `k_dim` to the dtype atom rather than clamping it below the atom (matching the WGMMA sibling's `256 // bits`), so `micro_size_k` stays 32 for int8 and the existing `block_K >= micro_size_k` assert at `gemm_mma.py#L104` rejects `block_K=16` with a clear message. Verified this session: with `_initialize_k_dim` pinned to `256 // bits` (no clamp), `int8`/`block_K=16` raises the clean `AssertionError: block_K (16) must be >= micro_size_k (32)` and `int8`/`block_K=32` still compiles with `maxerr=0.0`. Whether the clamp is instead meant to enable a smaller-`block_K` int4 packed path is a maintainer call.\n\n**Provenance.** Introduced by [#2073](https://github.com/tile-ai/tilelang/pull/2073) (\"[CUDA] Improve int4 GEMM lowering and packed codegen support\", merged 2026-04-21), commit `174c4f5d`, which changed `self.k_dim = 256 // a_dtype.bits` → `self.k_dim = min(256 // a_dtype.bits, self.chunk)`. Before that change, `int8` `k_dim` was pinned to 32 and `block_K=16` was rejected by the `block_K >= micro_size_k` guard (a correct clean reject) — so this is a regression of the reject behavior, not a never-worked case. Still present on `main` (the clamp is at `tilelang/cuda/intrinsics/macro/mma_macro_generator.py:131`; the WGMMA sibling at `:952` still uses `256 // bits`).\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this crash. Distinct from [#2381](https://github.com/tile-ai/tilelang/issues/2381) (`block_K` *not a multiple* of the atom → silent K-tail miscompile): that is a supported tile size with a silent wrong result; this is a *sub-atom* tile with a compile-time crash and a different root (atom under-selection vs a floored K-loop). The `float16` sub-atom *silent miscompile* twin (see §17) is a distinct symptom of the same clamp and is not filed separately.\n\n**Reach.** The trigger is a narrow-dtype `T.gemm` (documented dtypes) at any `block_K` below that dtype's MMA K-atom. `block_K=16` is the common `float16` default and a natural first choice a user carries over to `int8`/`fp8`; `block_K=8` breaks even `bfloat16` (atom 16). No shipped example or test exercises a sub-atom `block_K` — that is why CI is green (see example-run below).\n\n**Generalization (§17).**\n\n_Two-level root._\n- **SOURCE-level (fragile implementation):** the clamp `self.k_dim = min(256 // a_dtype.bits, self.chunk)` in `TensorCoreIntrinEmitter._initialize_k_dim` ([`mma_macro_generator.py#L131`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_macro_generator.py#L131)) lets `k_dim` fall below the operand dtype's real K-atom when `block_K < atom`. `_initialize_micro_size` then sets `micro_size_k = k_dim` ([`#L221`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_macro_generator.py#L221)), which neutralizes the `block_K >= micro_size_k` guard at [`gemm_mma.py#L104`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/op/gemm/gemm_mma.py#L104). The `min(…)` both selects a nonexistent instruction and disarms the check that would catch it.\n- **OPERATOR-level (what correlates):** any operand dtype routed through this SIMT `TensorCoreIntrinEmitter` whose K-atom exceeds the requested `block_K`. Whether the under-selection *crashes* or *silently miscomputes* depends only on whether `mma.h` happens to ship a `MmaDispatcher` at the under-selected `K` for that dtype.\n\n**4-axis findings** (each cell run in its own process, fresh `TILELANG_CACHE_DIR`, 0.1.13 / sm_89; \"emit-K\" = the `K` in the generated `mma_sync<…>` call):\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| related-type | `float8_e4m3`, `block_K=16` | **CRASH** — emits `mma_sync<kFloat8_e4m3,…,K=16>`, same `mma.h(282)`/`mma.h(167)` static_assert | **YES** — broadens the class |\n| related-type | `bfloat16`, `block_K=8` | **CRASH** — emits `mma_sync<kBFloat16,…,K=8>`, same `mma.h(282)`/`mma.h(167)` static_assert (bf16 atom is 16, so `min(16,8)=8`) | **YES** — broadens the class |\n| related-type | `int8`, `block_K=8` | **CRASH** — emits `mma_sync<kInt8,…,K=8>` (`min(32,8)=8`), same static_assert | **YES** — same dtype, other sub-atom |\n| related-type | `float16`, `block_K=8` | compiled, **maxerr=31.0** (silent wrong result) — same clamp under-selects to K=8, but `mma.h#L198` ships a real `m16n8k8` fp16 dispatcher so it binds a valid-but-wrong-K instruction | **DISTINCT symptom**, same upstream clamp — not claimed here |\n| related-type | `int4`, `block_K=32` | compiled, emits `mma_sync<…,K=32>` (int4 atom is 64, but `min(64,32)=32` maps to the valid `m16n8k32` shape → no static_assert); fails later at a runtime packed-ABI check | **DISTINCT** — clamp lands on an existing instruction, not this bug |\n| related-source | `int8`/`fp8`/`bf16` at/above atom (controls) | compiled, `maxerr=0.0` in every case | confirms the boundary is exactly `block_K < atom` |\n| related-operator | Hopper WGMMA sibling `_initialize_k_dim` at [`#L952`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/cuda/intrinsics/macro/mma_macro_generator.py#L952) | `self.k_dim = 256 // DataType(a_dtype).bits` — **no clamp** (int8 → 32 regardless of `block_K`) | correct-behavior contrast; the fix should match it |\n\nAxes skipped: **similar-logic** beyond the WGMMA sibling — the `min(atom, chunk)` idiom appears only in this one emitter's `_initialize_k_dim`; the WGMMA copy is the sole sibling and it does not clamp (already tested above).\n\n**Class boundary.** The crash class is `{int8, float8_e4m3, bfloat16}` (and any other dtype with no `MmaDispatcher` at its under-selected `K`) at `block_K < atom`. `float16` sub-atom is a *silent-miscompile* twin (distinct symptom, flagged distinct, not filed separately here — same fix closes it since pinning `k_dim` to the atom makes the `block_K >= micro_size_k` guard reject it too). `int4` sub-atom does not reach this failure because its clamp lands on a real `m16n8k32` shape.\n\n**Example-run (PART 1, MANDATORY).** The prior Reach cited `int8` GEMM shipping in `examples/bitnet-1.58b/kernel_benchmark/tl_int8xint8.py`. I located it in the v0.1.13 tree and inspected/ran it:\n- It **cannot run on the 0.1.13 `tilelang` install** — it imports `bitblas` and the old `tvm.tl` API (`from tvm import tl as TL`, `import tvm.tl.language`), neither of which is present (`ModuleNotFoundError: No module named 'bitblas'` / `'tvm'`). It is a legacy BitBLAS-era example, not a modern-API kernel.\n- Even structurally it would **NOT exercise this bug**: its int8 config uses `chunk = 64` (i.e. `block_K=64`), a multiple of the int8 atom of 32, so it is a supported tile.\n- The cited test `testing/python/kernel/test_tilelang_kernel_gemm_sm75.py` likewise builds its int8 GEMM with a `block_K` of 64 (a multiple of 32) and is sm_75-gated, so it neither exercises nor could run the sub-atom case on this host.\n\nCorrected conclusion: **no shipped example or test exercises a sub-atom `block_K`**, so the example does not demonstrate the bug — the Reach rests on the tested cells above (which reproduce cleanly), not on any shipped example. The presence of `int8`/`fp8`/`bf16` `T.gemm` as documented, supported dtypes is what makes the sub-atom tile a reachable user mistake.\n\n**Impact.** Trigger is a narrow-dtype `T.gemm` (`int8`/`fp8`/`bfloat16`) at a sub-atom `block_K` — already-invalid tiles, since one MMA consumes a full atom of K-elements. When it fires the failure is a loud compile-time crash (an nvcc back-end `static_assert` on every affected build) — deterministic, corrupts no output, and cannot reach a running workload silently; the cost is that a smaller tile size fails to build with a confusing back-end assert instead of a TileLang-level message. Fixing it (pin `k_dim` to the atom, matching the WGMMA sibling) moves the failure to a clear front-end rejection, closes the sub-atom `block_K` boundary class for all three crash dtypes, and — as a bonus — turns the `float16` sub-atom *silent miscompile* into the same clean rejection.", "url": "https://github.com/tile-ai/tilelang/issues/3032"}
{"number": 3023, "title": "[BUG][Fuzzer][ice-on-valid-code] Region-form tile atomic with a higher-rank source than destination crashes at compile with a raw `IndexError`", "state": "open", "created_at": "2026-08-17T03:06:17Z", "updated_at": "2026-08-17T03:06:17Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 / Python 3.13 / torch 2.8.0, CUDA 12.8 / NVIDIA L40S (sm_89). The failure is at kernel-build (tile-op lowering, before any codegen), so it is architecture-independent; reproduced on sm_89, other arches inferred.\n\n### Problem description\n\nA region-form tile atomic — `T.atomic_add(dst_region, src_region)` (and the sibling `T.atomic_max`/`atomic_min`) — where the source region has one or more extra non-unit dimensions relative to the destination (e.g. a `(K, M, N)` source reduced into an `(M, N)` destination) aborts kernel-build with a raw `IndexError: Index 2 out of bounds 2` thrown deep inside the tile-op lowering (`MakeIndices`), with no diagnostic naming the shapes or the op. The same call with rank-matched regions compiles and computes correctly (control below).\n\nThe docstring for `T.atomic_add` states that region inputs of unequal rank are handled: *\"If at least one side exposes extents, extents are aligned (missing dimensions are treated as size 1); an assertion is raised if extents cannot be deduced.\"* So passing a rank-3 source and a rank-2 destination is an in-contract input per the documentation — the destination's missing leading dimension should be treated as size 1. Instead of aligning (or raising the documented assertion), the lowering crashes on an out-of-bounds container access.\n\n<details>\n<summary>Trigger boundary, the passing control, and the reverse-direction asymmetry (tested this session, 0.1.13, L40S)</summary>\n\nThe crash fires when the source region exposes **more** non-unit dimensions than the destination region, and at least one operand is a slice/`BufferRegion` (so the tile-region path is taken rather than the two-plain-Buffer path). It is not specific to `atomic_add` or to a single-dimension gap:\n\n| variant | result |\n|---|---|\n| `atomic_add(B_s[:,:], A_s[:,:,:])` — src rank 3, dst rank 2 | **`IndexError: Index 2 out of bounds 2`** |\n| `atomic_max(B_s[:,:], A_s[:,:,:])` — src rank 3, dst rank 2 | **`IndexError: Index 2 out of bounds 2`** (same crash, sibling op via shared lowering) |\n| `atomic_add(B_s[:,:], A_s[:,:,:,:])` — src rank 4, dst rank 2 (two-dim gap) | **`IndexError: Index 2 out of bounds 2`** (same crash) |\n| `atomic_add(B_s[:,:], A_s[:,:])` — matched rank 2 / rank 2 | compiles, correct (control) |\n| K-loop of `atomic_add(B_s[:,:], A_s[k,:,:])` — matched, reduces over K | compiles, `A.sum(0)` exact (control) |\n| `atomic_add(B_s[:,:,:], A_s[:,:])` — src rank 2, dst rank 3 (reverse) | `InternalError: Check failed: (loop_vars.size() <= op.src_range.size()) is false: loop_vars.size() = 3, src_range.size() = 2, src = A_s, dst = B_s` — a *labeled* ICHECK |\n| `atomic_add(B_s, A_s)` — two plain Buffers, rank 3 / rank 2 | `ValueError: StructuralEqual check failed ... (8, 16) and (4, 8, 16)` (a different, earlier guard) |\n\nThree things this isolates: (1) the defect is the rank mismatch, not the atomic op, the slice form, or the harness — matched-rank slices are correct; (2) it is a **shared** defect — `atomic_max` (which lowers through a separate but identical copy of the same logic) crashes the same way, and the gap can be wider than one dimension; (3) the **reverse** rank mismatch (destination has more dims) is caught by a labeled `ICHECK` that names the shapes, while the **forward** mismatch (source has more dims — this report) slips past that same guard and dies in a raw container-bounds `IndexError` with no context.\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\nK, M, N = 4, 8, 16\n\n# BUG: rank-3 src region (K,M,N) reduced into a rank-2 dst region (M,N)\n@T.prim_func\ndef rank_mismatch(A: T.Tensor((K, M, N), \"float32\"), B: T.Tensor((M, N), \"float32\")):\n    with T.Kernel(1, threads=128):\n        A_s = T.alloc_shared((K, M, N), \"float32\")\n        B_s = T.alloc_shared((M, N), \"float32\")\n        T.atomic_add(B_s[:, :], A_s[:, :, :])   # -> IndexError at compile\n\n# CONTROL: the same reduction written as a K-loop of rank-matched atomic_add\n@T.prim_func\ndef kloop(A: T.Tensor((K, M, N), \"float32\"), B: T.Tensor((M, N), \"float32\")):\n    with T.Kernel(1, threads=128):\n        A_s = T.alloc_shared((K, M, N), \"float32\")\n        B_s = T.alloc_shared((M, N), \"float32\")\n        T.copy(A, A_s); T.clear(B_s)\n        for k in range(K):\n            T.atomic_add(B_s[:, :], A_s[k, :, :])   # rank-matched -> compiles + correct\n        T.copy(B_s, B)\n\ntry:\n    tilelang.compile(rank_mismatch); print(\"BUG : COMPILED (no crash)\")\nexcept Exception as e:\n    print(\"BUG :\", type(e).__name__, \"->\", str(e).splitlines()[-1].strip())\n\nf = tilelang.compile(kloop)\nA = torch.randn(K, M, N, dtype=torch.float32, device=\"cuda\")\nB = torch.zeros(M, N, dtype=torch.float32, device=\"cuda\")\nf(A, B)\nprint(\"CTRL: COMPILED, max_err vs A.sum(0) =\", float((B - A.sum(0)).abs().max()))\n```\n\nOutput (0.1.13, L40S):\n\n```\nBUG : IndexError -> Index 2 out of bounds 2\nCTRL: COMPILED, max_err vs A.sum(0) = 0.0\n```\n\n### Traceback\n\nThe kernel builds fine; the crash is raised later during the `LowerTileOp` pass:\n\n```\nFile \"<unknown>\", in tvm::tl::LowerTileOpPass::VisitStmt_(tvm::tirx::EvaluateNode const*)\nFile \"<unknown>\", in tvm::tl::AtomicAddNode::Lower(tvm::tl::LowerArgs const&, tvm::arith::Analyzer*) const\nFile \"<unknown>\", in tvm::tl::cuda::AtomicAdd::Lower(...)\nFile \"<unknown>\", in tvm::tl::cuda::AtomicAdd::LowerSIMT(...)\nFile \".../tvm-ffi/include/tvm/ffi/container/seq_base.h\", line 86, in\n    tvm::ffi::SeqBaseObj::operator[](int64_t) const\nIndexError: Index 2 out of bounds 2\n```\n\nFor `atomic_max`/`atomic_min` the same crash arrives through the shared base: `tvm::tl::backend::AtomicReduce::Lower` -> `tvm::tl::backend::atomic_reduce::MakeSIMTLoop` -> `seq_base.h:86` -> `IndexError: Index 2 out of bounds 2`.\n\n### Expected behavior\n\nThe immediate, low-risk expectation is a **clear compile-time diagnostic that names the operand shapes**, rather than the opaque `IndexError` from a container-bounds check. The reverse-direction mismatch already produces exactly such a labeled `ICHECK` naming the shapes (`loop_vars.size()=3, src_range.size()=2, src=..., dst=...`), so a comparable diagnostic for the forward direction would simply match the compiler's own existing behavior on the mirror case — the smallest fix that removes the raw crash. (Whether the rank-mismatched form should additionally be *aligned and computed* rather than rejected is a separate semantics question, below.)\n\nWhat the *aligned* result should be is genuinely ambiguous and would benefit from being pinned down: the docstring's \"missing dimensions are treated as size 1\" reads as a **broadcast** (the destination gains a leading size-1 axis that is broadcast to K, i.e. K copies written), whereas a rank-3-into-rank-2 shape reads naturally as a **reduction over the leading axis**. The reduction is computable — the rank-matched K-loop control returns `A.sum(0)` exactly — so if reduction is the intended semantics the operation has a working target; if neither is intended, a clean reject is the alternative. Defining and enforcing one of these (rather than leaving it to crash) is the underlying ask.\n\n### Additional context\n\n**Root cause.** The tile-atomic SIMT lowering builds the loop variables from the **destination** range only (comment: \"Make IterVars according to dst, not src, since src may be a scalar Expr\"), so a rank-2 destination yields 2 loop vars; the subsequent `MakeIndices` call for the **source** then walks the rank-3 source range and indexes `ivs[2]` — past the end of the 2-element loop-var array — throwing the raw `IndexError`. `atomic_add` and `atomic_max`/`atomic_min` each carry an identical copy of this logic, which is why both crash.\n\n<details>\n<summary>Where it happens (SHA-pinned, v0.1.13 tree) and why the two ICHECKs don't catch it</summary>\n\nThe logic exists in two identical copies:\n\n- `atomic_add` — free functions in [`src/cuda/op/atomic_add.cc`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/atomic_add.cc#L67-L86):\n  - [`MakeIterVars`, `atomic_add.cc#L67-L86`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/atomic_add.cc#L67-L86) iterates `dst_range` and emits one `IterVar` per non-unit destination dim → 2 vars for an `(M,N)` destination.\n  - [`MakeSIMTLoop`, `atomic_add.cc#L139-L170`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/atomic_add.cc#L139-L170) guards with `ICHECK(loop_vars.size() <= op.dst_range.size())` (2 ≤ 2, passes) and [`ICHECK(loop_vars.size() <= op.src_range.size())` at `atomic_add.cc#L162-L167`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/atomic_add.cc#L162-L167) (2 ≤ 3, passes), so **both** guards are satisfied — they bound `loop_vars` against each range, but never check that `loop_vars` has *enough* entries for the range with the most non-unit dims.\n  - [`MakeIndices`, `atomic_add.cc#L88-L104`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/op/atomic_add.cc#L88-L104) then does `indices.push_back(ranges[i]->min + ivs[idx]->var)` for each non-unit dim of the **source** range (called with `src_dst=0`); with 3 non-unit source dims and only 2 loop vars, `ivs[2]` is out of bounds. The trailing `ICHECK(idx == ivs.size() || ...)` that would flag the mismatch is *after* the indexing, so the container bounds-check fires first.\n\n- `atomic_max`/`atomic_min` — the same three functions in the shared base [`src/backend/common/op/atomic_reduce.h`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/backend/common/op/atomic_reduce.h#L34-L52): `MakeIterVars` at [`atomic_reduce.h#L34-L52`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/backend/common/op/atomic_reduce.h#L34-L52) (dst-only), `MakeIndices`/`MakeSIMTLoop` at [`atomic_reduce.h#L75-L100`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/backend/common/op/atomic_reduce.h#L75-L100) with the identical two-ICHECK guard and post-indexing check.\n\nThe reverse case (destination rank 3, source rank 2) builds 3 loop vars from the destination and trips the `ICHECK(loop_vars.size() <= op.src_range.size())` (`3 <= 2` is false) with a labeled message — which is why only the forward direction produces the raw `IndexError`.\n\n</details>\n\n**Reaching the tile-region path.** The crash requires at least one operand to be a slice/`BufferRegion`; two plain `Buffer`s take an earlier `ir.assert_structural_equal(dst.shape, value.shape)` branch ([`atomic.py#L271`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/atomic.py#L271)) that rejects the pair with a `ValueError` before lowering. The slice form (`A_s[:,:,:]`, `B_s[:,:]`) is the documented `BufferRegion`/tensor-to-tensor form, and [`legalize_pairwise_extents`, `utils/language.py#L469`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/utils/language.py#L469) leaves the ranks mismatched: its non-1-count early-exit does not fire (3 vs 2 differ), it right-aligns only the shared tail dimensions (`(M,N)` against `(M,N)`) and keeps the unmatched leading source dim `K` as-is — it never left-pads the destination with a size-1 axis — so `legalize_pairwise_extents([K,M,N],[M,N])` returns `([K,M,N],[M,N])` and the rank-3/rank-2 pair reaches `MakeIterVars`/`MakeIndices` unaligned (verified this session).\n\n**Suggested fix.** At minimum, turn the immediate crash into a labeled diagnostic by checking in `MakeSIMTLoop` that the loop-var count covers the larger of the source/destination non-unit-dim counts (symmetric with the existing ICHECK that already catches the reverse direction), so a rank mismatch reports the operand shapes instead of an out-of-bounds container access. Apply it in both copies (`atomic_add.cc` and `atomic_reduce.h`). Whether the aligned form should then *compute* (broadcast or reduce) rather than reject depends on the intended semantics of an unequal-rank region atomic, per the ambiguity noted under Expected behavior.\n\n**Provenance.** Rank-mismatched region atomics have never computed a correct result, so this is not a functionality regression — no released version aligned or reduced an unequal-rank region atomic. `MakeIterVars` currently builds loop vars from the **destination** range and `MakeIndices` guards the mismatch only *after* the out-of-bounds index, which is what produces the opaque `IndexError` rather than a labeled diagnostic on the forward direction.\n\n**Dedup.** I searched the open and closed tracker (`atomic_add rank`, `atomic_add IndexError`, `MakeIndices atomic`, `Index out of bounds`, `atomic tile region crash`) and found no existing report of this defect. The closest tracker neighbors are distinct: #2583 is a TMA reduce-dtype legality crash at *launch* on rank-matched operands (`use_tma=True`), not a rank-mismatch build crash; and the general \"region-like input read as a plain `Buffer`\" frontend crashes (#2550 `atomic_addx4` reading `.dtype` off a sliced destination, #2631 `.shape` off a `BufferLoad`) raise a Python `AttributeError` in the eager builder for *any* slice, whereas here a matched-rank slice compiles and runs correctly and the crash is a C++ container-bounds `IndexError` inside the `LowerTileOp` loop-var construction that fires only on the *rank mismatch*. The other atomic_add issues (memory-order and dtype-reinterpret value bugs) are rank-matched and silent/value, a different symptom class.\n\n**Reach.** Triggering requires a region-form atomic whose source exposes more non-unit dimensions than its destination (a whole-tensor reduction over a leading axis expressed as `atomic_add(dst[...], src[...])`). The shipped reduction pattern dodges it: the canonical `atomic_add_program` in `testing/python/language/test_tilelang_language_atomic.py` reduces a `(K,M,N)` input into an `(M,N)` output but does so with **per-element scalar indices** inside a `T.Parallel(block_M, block_N)` loop, which takes the scalar-extern path, not the tile-region path. I ran that shipped example verbatim on 0.1.13 (`run_atomic_add(4, 64, 64, 32, 32)`) and it compiles and matches `A.sum(0)` — it dodges the crash because the scalar indices never build a tile region. I also ran the two region-form atomic examples that ship in the same file: `atomic_addx4_sliced_dst_program(8, 16)` (region slices `dst[idx[t], 0:4]` / `val[t, 0:4]`, rank-matched) and the whole-buffer `tma_atomic_add` form (`(16,16)`/`(16,16)`, rank-matched) — both compile; neither exercises a rank-mismatched region, so CI stays green. When the bug fires it is a hard compile-time abort (a raw `IndexError` during `LowerTileOp`) — it corrupts no data and cannot slip into a running workload; the kernel simply fails to build.\n\n<details>\n<summary>Generalization: two-level root + 4-axis sweep (all cells RUN this session, 0.1.13, L40S)</summary>\n\n**Root, two levels.** (a) *Source-level:* the fragile implementation is the tile-atomic SIMT lowering `MakeIterVars` → `MakeIndices` → `MakeSIMTLoop`, which builds loop vars from the **destination** range only and then indexes `ivs[idx]` while walking the **source** range in `MakeIndices(op, loop_vars, /*src_dst=*/0)`. Two copies exist: `src/cuda/op/atomic_add.cc` and the shared base `src/backend/common/op/atomic_reduce.h`. (b) *Operator-level:* every region-form atomic that routes through this SIMT loop-builder — `atomic_add`, `atomic_max`, `atomic_min` — correlates; the vectorized `atomic_addx4` and the scalar/`T.Parallel` atomic path do not.\n\n| axis | cell tested (rank-3 src → rank-2 dst region unless noted) | observed result | same root? |\n|---|---|---|---|\n| related-operator | `atomic_max(B_s[:,:], A_s[:,:,:])` | `IndexError: Index 2 out of bounds 2` | yes (shared `atomic_reduce.h`) |\n| related-operator | `atomic_min(B_s[:,:], A_s[:,:,:])` (re-run alone) | `IndexError: Index 2 out of bounds 2` | yes (shared `atomic_reduce.h`) |\n| related-operator | `atomic_addx4(dst[0:4], val[:,0:4])` (src rank-2, vectorized path) | **COMPILED** — separate entry point (`atomic.py#L338`), does not build the SIMT loop | no — distinct path |\n| related-type | `atomic_add` int32, rank-3 src → rank-2 dst | `IndexError: Index 2 out of bounds 2` | yes (dtype-independent; the crash is in index construction, before any dtype/codegen) |\n| related-type | rank-4 src → rank-2 dst (two-dim gap, `atomic_add`) | `IndexError: Index 2 out of bounds 2` (already in trigger table) | yes |\n| similar-logic | `T.copy(A_s[:,:,:], B_s[:,:])` — sibling region data-movement with its own `MakeIterVars`/`MakeIndices` | `InternalError: Check failed: (loop_vars.size() <= dst_range.size()) ... loop_vars.size() = 3, dst_range.size() = 2` | **distinct** — `copy.cc`'s `MakeIterVars` selects a `base_is_src` range and carries an explicit \"src_range has extra non-1 extent\" sanity check (`copy.cc#L405-L425`), so it aborts with a *labeled* diagnostic instead of a raw `IndexError`. Same family, different (safer) implementation — this is exactly the guard the atomic path is missing. |\n| related-source | `atomic_reduce.h` copy of the three functions (backs max/min above) | crashes identically via `MakeSIMTLoop` → `MakeIndices` | yes (this IS the second copy) |\n\nTakeaways: the class is **region-form `atomic_add`/`atomic_max`/`atomic_min` with a source that has more non-unit dims than the destination** — three operators, two source copies, dtype- and gap-width-independent, all sharing one root. The vectorized `atomic_addx4` is a distinct path that does not crash (not filed). `T.copy` is the sibling that already has the guard the atomic path lacks — evidence the fix (a labeled ICHECK covering the larger non-unit-dim count) is the compiler's own established pattern, not distinct.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3023"}
{"number": 3020, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.import_source` prelude with no trailing newline fuses with the next `#include`, so nvcc rejects the CUDA instead of compiling", "state": "open", "created_at": "2026-08-17T03:06:08Z", "updated_at": "2026-08-17T03:06:08Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\n- TileLang 0.1.13 (pip), TVM submodule `tile-ai/tvm@8df8ebd6`\n- CUDA 12.8, PyTorch 2.8.0\n- Reproduced on an NVIDIA L40S (sm_89); the defect is in the arch-independent C codegen base, so it is not GPU-specific.\n\n### Problem description\n\nA C source string passed to `T.import_source(...)` is emitted verbatim into the generated CUDA file with no separator after it, so if the prelude does not itself end in a newline, its last line is fused with whatever preprocessor line the code generator emits next. On 0.1.13 that next line is the prelude guard `#if defined(_MSC_VER)`, so the emitted line becomes `...my_helper(int x){return x+1;}#if defined(_MSC_VER)...` — the fused `#if` is no longer parsed as a preprocessor directive, so its matching `#endif` later in the file has no opener and the compiler rejects the file (on 0.1.9 the fused line was the first `#include <tl_templates/cuda/gemm.h>` instead — the emitted-next line moved with the codegen preamble, the missing separator did not):\n\n```\n__device__ int my_helper(int x) { return x + 1; }#if defined(_MSC_VER) && !defined(__clang__) && _MSC_VER < 1940\ntvm_kernels.cu:7:2: error: #endif without #if\n```\n\nThe prelude is a plain C string; whether it ends in a newline is not documented as significant, and the same prelude with a trailing `\\n` compiles and runs correctly (control below). Not a regression — see Provenance.\n\n<details><summary>Prelude last-line variants (tested this session on sm_89)</summary>\n\nThe fused next line on 0.1.13 is the `#if defined(_MSC_VER)` prelude guard, so the symptom depends on the prelude's last line:\n\n- Last line is a **declaration**, no trailing newline → it fuses with `#if defined(_MSC_VER) ...`, so that `#if` is no longer parsed as a directive and its matching `#endif` is unbalanced → nvcc `#endif without #if` (the case above). Reproduced.\n- Last line is a **`//` line comment**, no trailing newline → the comment swallows the fused `#if` onto its own line, so the guard block's opener is gone while its `#endif` remains → still an nvcc `#endif without #if`. Reproduced.\n- Prelude ends in **`\\n`** → separate lines, compiles, `B == A + 1`, `max_err = 0`. Control.\n\nOn 0.1.13 the first thing the generator writes after the prelude is a `#if…#endif` guard *block*, so both non-newline last lines fail loudly at compile — I did not observe a silent-drop face on this release. A silent-drop face is possible only if the fused next line were a standalone directive (e.g. a bare `#include`) that a `//` comment could swallow without unbalancing a later `#endif`; that is not the emitted order here.\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\n# A prelude whose last line is a declaration and does NOT end in \"\\n\".\nPRELUDE = \"__device__ int my_helper(int x) { return x + 1; }\"      # FAILS: no trailing newline\n# PRELUDE = \"__device__ int my_helper(int x) { return x + 1; }\\n\"  # CONTROL: trailing newline -> compiles & runs\n\n@tilelang.jit(out_idx=[1])\ndef make(prelude):\n    @T.prim_func\n    def main(A: T.Tensor((128,), \"int32\"), B: T.Tensor((128,), \"int32\")):\n        with T.Kernel(1, threads=128) as bx:\n            T.import_source(prelude)\n            tid = T.get_thread_binding(0)\n            B[tid] = A[tid] + 1\n    return main\n\nmake(PRELUDE)   # RuntimeError: Compilation error: #endif without #if  (the prelude fused with the next preprocessor line)\n```\n\nThe prelude with a trailing `\\n` is a correct control over the same kernel: it compiles and `B == A + 1` (verified `max_err = 0`).\n\n### Traceback\n\n```\nRuntimeError: Compilation error:\n__device__ int my_helper(int x) { return x + 1; }#if defined(_MSC_VER) && !defined(__clang__) && _MSC_VER < 1940\ntvm_kernels.cu:7:2: error: #endif without #if\n```\n(the exact compiler diagnostic depends on the nvcc/host-compiler version — the constant is that the prelude's last line and the generator's next `#if` land on the same physical line; here the fused `#if` is not seen as a directive, so the later `#endif` is unmatched.)\n\n### Expected behavior\n\nThe imported source and the code generator's own declarations/includes should be emitted on separate lines regardless of whether the user's string ends in a newline — i.e. append a `\\n` after the prelude. `T.import_source` accepts an arbitrary C string with no documented requirement that it be newline-terminated, so a prelude ending mid-line should still produce compilable output.\n\n### Additional context\n\n**Root cause.** The C code generator emits the `pragma_import_c` payload without a trailing separator, so it runs into whatever the generator writes to the declaration stream next. `T.import_source(src)` attaches `src` as a `pragma_import_c` block attribute ([language/common.py#L157-L161](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/common.py#L157-L161)); TileLang's CUDA generator does not special-case that attribute and [falls through to the base `CodeGenC::VisitStmt_`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L5034), which does [`decl_stream << value->value;`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L1123) with no following newline. Whatever preprocessor line the CUDA generator writes to the same stream next then lands on the prelude's last line — on 0.1.13 that is the `#if defined(_MSC_VER)` prelude guard that precedes the `tl_templates` `#include`s.\n\n**Suggested fix.** Terminate the emitted block with a newline, e.g. `decl_stream << value->value << \"\\n\";` at the cited line (or prepend a newline before the header block); it is a C++ source change in the TVM submodule. The Python-side control above — the identical prelude with a trailing `\\n` compiling and returning the correct result — demonstrates the separator is exactly what is missing.\n\n**Provenance.** The `pragma_import_c` handler has emitted the source with no trailing newline since the token was introduced in TVM [`#5417`](https://github.com/apache/tvm/pull/5417) (2020-04-30); the recent `tirx`-namespace refactor only relocated the line. Not a regression.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect.\n\n**Reach.** `T.import_source` is used in exactly 5 shipped examples at v0.1.13 (`git grep -l import_source v0.1.13 -- 'examples/*'`: `examples/bitnet-1.58b/kernel_benchmark/tilelang_bitnet_158_int8xint2_decode.py`, and four `examples/dequantize_gemm/example_dequant_gemm_bf16_{fp4,mxfp4_cdna4,mxfp4}_hopper.py` + `example_dequant_gemv_fp16xint4.py`). All feed it a source string that ends in `\\n`, so every shipped caller dodges the bug and CI stays green:\n\n- **Ran** `example_dequant_gemv_fp16xint4.py` verbatim at v0.1.13 (default config): it **compiles and runs** (`kernel(A, qB, C)` prints `C` and `Ref C`; dequant-gemv is approximate so the reference differs slightly — not a compile failure). Its `import_source` payload is `lop3_intrin_info[\"c_source\"]`; I read that string back through the example's own `quantize.lop3.get_lop3_intrin_group(...)` and it ends `...B_local_decode, N);\\n}\\n` (`endswith(\"\\n\") == True`) — that trailing `\\n` is exactly why it dodges the bug.\n- The bitnet example's `decode_i2s_to_i8s` is a hand-written `\"\"\"...\"\"\"` string; parsed out of the file it ends `...immLut));\\n    }\\n}\\n` (`endswith(\"\\n\") == True`) — same dodge.\n- The `mxfp` examples feed `mxfp_intrin_info[\"c_source\"]`, the same generator family as `lop3`.\n\nSo the shipped examples exercise the API but all terminate their strings; no test exercises the non-newline-terminated path. The trigger is a prelude whose last line has no `\\n` — the natural way to write a one-line `__device__` helper or a source string assembled without a trailing blank line.\n\n**Impact.** The trigger is narrow: a prelude string whose last line lacks a trailing newline, which the shipped `\\n`-terminated blocks never hit. On 0.1.13 the effect is a loud nvcc compile error — caught immediately at build time, blocking that one kernel, corrupting nothing and unable to reach production silently — for both a declaration and a `//`-comment last line, because the fused next line is a `#if…#endif` guard block whose balance the fusion breaks either way. A silent-drop (dropped-directive) face would require the fused next line to be a standalone directive a comment could swallow without unbalancing an `#endif`; that is not the emitted order on this release, so I do not claim it here. Fixing it (append `\\n` after the block) closes the newline-termination boundary class for `T.import_source` so a prelude ending mid-line compiles regardless.\n\n**Generalization (root + 4-axis sweep, all cells RUN on sm_89 / 0.1.13).**\n\n*Two-level root.*\n- **Source-level root:** `CodeGenC::VisitStmt_(const AttrStmtNode*)` in the TVM submodule, `pragma_import_c` branch: [`decl_stream << value->value;`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/source/codegen_c.cc#L1123) writes the user payload into `decl_stream` with **no trailing separator**. This is the *only* raw-payload write into `decl_stream` in that file (the other, `fwd_decl_stream << \";\\n\"` at line 160, correctly self-terminates). The TileLang CUDA backend does not override this branch (falls through via `CodeGenC::VisitStmt_(op)` at [codegen_cuda.cc#L5034](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L5034)). The payload is emitted *before* the include-preamble header ([codegen_cuda.cc#L651-L742](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L651)), whose first line on 0.1.13 is `#if defined(_MSC_VER)...` — so a non-`\\n`-terminated payload fuses with that `#if`.\n- **Operator-level root:** the token is `pragma_import_c`, reachable from Python only through `T.import_source` ([common.py#L157-L161](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/common.py#L157-L161)). Its sibling `pragma_import_llvm` exists but is consumed only in the LLVM CPU backend ([codegen_cpu.cc#L1151](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/llvm/codegen_cpu.cc#L1151)) — LLVM IR, not concatenated CUDA text, so it cannot fuse. There is no other CUDA-side operator that routes raw text into `decl_stream`.\n\n*4-axis findings* (each cell run in its own fresh process/cache; input → observed):\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| baseline | decl last line, no `\\n` | `#endif without #if` (fused `...x+1;}#if defined(_MSC_VER)...`) | — |\n| control | same + trailing `\\n` | compiles, `B==A+1`, `max_err=0` | — |\n| similar-logic | last line is `//` comment, no `\\n` | `#endif without #if` (comment swallows `#if`) | yes |\n| similar-logic | prelude ends in `\\` (line-continuation), no `\\n` | `#endif without #if` (`\\` splices the `#if` line) | yes |\n| similar-logic | prelude *is* `#define MY_HELPER 1`, no `\\n` | `#endif without #if` (fuses `#define...1#if...`) | yes |\n| related-source | other `decl_stream << value->value` sites | none exist (unique in codegen_c.cc) — nothing to run | n/a |\n| related-operator | `pragma_import_llvm` / other CUDA raw-text attrs | none reach CUDA decl stream — nothing to run | n/a |\n| related-operator | helper actually invoked via `T.call_extern` (shipped idiom), no `\\n` | `#endif without #if` (identical) | yes |\n| related-type | fp16 kernel + non-`\\n` prelude | `#endif without #if` (identical) | yes — dtype/scope-independent |\n| related-type | empty-string prelude `\"\"` | **compiles OK** (emits nothing → `#if` at col 0, no fusion) | boundary: only *non-empty* last line triggers |\n| related-type | prelude ends in trailing spaces, no `\\n` | `#endif without #if` (`...}   #if...`) | yes — boundary is missing NEWLINE, not missing whitespace |\n\n*Reframe decision.* The defect is a single-site, single-operator textual-separator bug that is **orthogonal to dtype/scope/rank** (fp16 == int32) and **orthogonal to whether the helper is used** (declared-only == `call_extern`-invoked, the shipped idiom). The boundary is precise: *any non-empty prelude whose last line lacks a trailing `\\n`* fuses with the emitted `#if`; an empty string or a `\\n`-terminated string is fine. Every last-line shape tested (declaration, `//` comment, `#define`, backslash-continuation, trailing-space) hits the *same* root and the *same* `#endif without #if`. Kept **specific to `T.import_source` / `pragma_import_c`** because that is the only reachable entry point; no distinct-adjacent bug found; no new bug found while sweeping. The correct scope is \"the non-newline-terminated-prelude class of `T.import_source`\", which the suggested fix (`decl_stream << value->value << \"\\n\";`) closes for all these last-line shapes at once.", "url": "https://github.com/tile-ai/tilelang/issues/3020"}
{"number": 3018, "title": "[BUG][Fuzzer][ice-on-valid-code] `interleave_weight` raises `TypeError: 'torch.dtype' object is not callable` on its `nbits=1`/`nbits=2` branches", "state": "open", "created_at": "2026-08-17T03:05:55Z", "updated_at": "2026-08-17T03:05:55Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "\n### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no existing report covers this.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release; the function lives at `examples/dequantize_gemm/quantize/utils.py`, relocated from `tilelang/quantize/utils.py`).\n\n### System information\n\nTileLang 0.1.13, PyTorch 2.13.0. This is a host-side (CPU/`torch`) tensor utility; no GPU is involved and it is architecture-independent.\n\n### Problem description\n\n`tilelang.quantize.interleave_weight(qweight, nbits, target_dtype)` crashes with `TypeError: 'torch.dtype' object is not callable` for three of its documented input combinations — `(nbits=1, \"int8\")`, `(nbits=2, \"float16\")`, and `(nbits=1, \"float16\")` — and never returns. The three affected branches each build a bit mask by calling `torch.int32(0x....)`, but `torch.int32` is a dtype object, not a constructor, so the first such call aborts the function.\n\n`target_dtype=\"float16\"` is one of only two values the function's own `assert` admits (utils.py:88), and `nbits=1`/`nbits=2` each have dedicated, hand-written interleave logic in the function, so these are inputs the function is written to handle. The generic `nbits=4` path (which never reaches these lines) works, which is why the default-config example and CI don't catch it. Not a regression — see Provenance.\n\n<details><summary>All input combinations (run on 0.1.13)</summary>\n\n```\nnbits=1 int8     -> CRASH TypeError: 'torch.dtype' object is not callable\nnbits=2 float16  -> CRASH TypeError: 'torch.dtype' object is not callable\nnbits=1 float16  -> CRASH TypeError: 'torch.dtype' object is not callable\nnbits=4 float16  -> PASS shape=(4, 4) dtype=torch.int8    (generic path)\nnbits=4 int8     -> PASS shape=(4, 4) dtype=torch.int8    (generic path)\n```\n</details>\n\n### Reproducible example code\n\n```python\nimport sys, torch\n\n# 0.1.13: interleave_weight moved from `tilelang.quantize` into the\n# dequantize-gemm example. The pip wheel does not ship examples/, so point\n# at a source checkout of the tag (adjust the path to your clone).\nsys.path.insert(0, \"/path/to/tilelang/examples/dequantize_gemm/quantize\")\nfrom utils import interleave_weight\n\nqw = torch.randint(0, 15, (4, 4), dtype=torch.int8)\n\n# Fails: takes the nbits=2 / float16 special branch (utils.py:110-114).\ninterleave_weight(qw, nbits=2, target_dtype=\"float16\")   # -> TypeError\n\n# Control (same inputs, generic nbits=4 path) returns normally:\nout = interleave_weight(qw, nbits=4, target_dtype=\"float16\")\nprint(out.shape, out.dtype)   # -> torch.Size([4, 4]) torch.int8\n```\n\n### Traceback\n\n```\nTraceback (most recent call last):\n  File \".../_tb.py\", line 4, in <module>\n    interleave_weight(qw, nbits=2, target_dtype=\"float16\")\n  File \".../examples/dequantize_gemm/quantize/utils.py\", line 111, in interleave_weight\n    n8_weight = new_qweight & torch.int32(0xFF0000FF)\n                              ~~~~~~~~~~~^^^^^^^^^^^^\nTypeError: 'torch.dtype' object is not callable\n```\n\n### Expected behavior\n\nThe call should return the interleaved `int8` tensor, as the `nbits=4` path already does. The three special branches are computable — replacing each `torch.int32(0xMASK)` with a plain Python integer mask (`0xMASK`), which `torch`'s bitwise `&`/`|` accept directly, produces a result of the expected shape and dtype with no other change.\n\n<details><summary>Fix verified (nbits=2 / float16 branch, plain-int masks)</summary>\n\nRunning the function's own core loop followed by the `nbits=2/float16` branch rewritten with plain-int masks returns a `torch.Size([4, 4]) torch.int8` tensor instead of raising:\n\n```python\nn8_weight  =  new_qweight & 0xFF0000FF\nn8_weight |= ((new_qweight & 0x0000FF00) >> 8)  << 16\nn8_weight |= ((new_qweight & 0x00FF0000) >> 16) << 8\nreturn n8_weight.view(torch.int8)\n```\n</details>\n\n### Additional context\n\n**Root cause.** `interleave_weight` uses the dtype object `torch.int32` as if it were an integer constructor when building its bit masks, so any code path that reaches one of those masks aborts. The masks live only in the `nbits`/`target_dtype` special branches: [`nbits=1, \"int8\"` at utils.py:104-108](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dequantize_gemm/quantize/utils.py#L104), [`nbits=2, \"float16\"` at :111-113](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dequantize_gemm/quantize/utils.py#L111), and [`nbits=1, \"float16\"` at :116-122](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dequantize_gemm/quantize/utils.py#L116). The generic tail (`return new_qweight.view(torch.int8)`) never touches them, so `nbits=4` is unaffected.\n\n**Suggested fix.** In the three special branches (`utils.py:104-122`), drop the `torch.int32(...)` wrapper on each mask and pass the hex integer directly (`new_qweight & 0xFF0000FF`, …); `torch` widens a Python int to the tensor dtype for `&`/`|`. This matches the working `nbits=4` path, which already uses a plain-int mask (`mask = (1 << nbits) - 1`).\n\n**Provenance.** Introduced with the quantize components in [#494](https://github.com/tile-ai/tilelang/pull/494) (2025-05-14), which added `tilelang/quantize/utils.py` with these lines already in their current form; the branches have never returned. The function was relocated verbatim into `examples/dequantize_gemm/quantize/utils.py` (it was `tilelang.quantize.interleave_weight` up to 0.1.9; on 0.1.13 it is re-exported from the example's `quantize/__init__.py`).\n\n**Adjacent inputs (tested this session).** Ran all six `(nbits, target_dtype)` combinations the `assert` admits (0.1.13, v0.1.13 source extracted from the checkout — the wheel does not ship `examples/`); the three that reach a `torch.int32(...)` mask crash, the three that take the generic tail return normally:\n\n| nbits | target_dtype | result |\n|---|---|---|\n| 1 | int8 | CRASH `TypeError: 'torch.dtype' object is not callable` (utils.py:104) |\n| 2 | float16 | CRASH (utils.py:111) |\n| 1 | float16 | CRASH (utils.py:116) |\n| 2 | int8 | PASS `shape=(4,4) dtype=torch.int8` (generic tail) |\n| 4 | float16 | PASS `shape=(4,4) dtype=torch.int8` (generic tail) |\n| 4 | int8 | PASS `shape=(4,4) dtype=torch.int8` (generic tail) |\n\n**Generalization (roots + 4-axis sweep, run this session).**\n\n*Root, two levels.* (a) SOURCE-level: a `torch.dtype` object is used as an integer constructor — `torch.int32(0xMASK)` — in the three special branches of `interleave_weight` (`utils.py:104–122`); `torch.dtype.__call__` does not exist, so the first mask aborts before any tensor op. (b) OPERATOR-level: the \"dtype-name-as-mask-constructor\" idiom for building a bit mask. The correct spellings in the same function (`.view(torch.int32)`, `mask = (1<<nbits)-1`) pass the dtype as an *argument* / use a plain int, and work.\n\n| axis | cell tested (run) | observed result | same-root? |\n|---|---|---|---|\n| similar-logic | plain-int fix on all three branches (`& 0xFF0000FF` … , `torch.int32(…)`→hex) | all three now PASS `(4,4) int8` — one fix clears the whole branch-family | confirms root |\n| related-source | `general_compress` (sibling fn, same `utils.py`) | PASS `(4,4) int8` — it uses `dtype` as an argument (`torch.zeros(dtype=…)`, `.to(torch.int8)`), never calls it | distinct (unaffected) |\n| related-operator | bitnet-1.58b local `interleave_weight` copies (`np.int32(0x…)` masks), `nbits=2`/`float16` branch | CRASH `OverflowError: Python integer 4278190335 out of bounds for int32` (numpy 2.x) — same idiom, different failure | distinct-adjacent, not filed |\n| related-type | `torch.<dt>(0x0F)` for dt ∈ {int8,int16,int32,int64,uint8,float16,bfloat16,float32,bool} | every one `TypeError: 'torch.dtype' object is not callable` | root is dtype-agnostic (not int32-specific) |\n\nClass: **one root, three affected branches** — every special branch spells its mask `torch.int32(0x…)` (dtype-object-as-constructor); the generic tail uses a plain-int mask and is unaffected. Title/Problem already framed as the branch-family, kept. Distinct-adjacent (not filed): (1) the bitnet `np.int32(0x…)` copies break the same way on numpy 2.x via `OverflowError`; (2) the docstring's own `(10,10)` example crashes at the `.view(torch.int32)` on a non-divisible shape (a shape defect in the example, unrelated to the mask). Neither is this bug.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect (#2477 concerns `decode_i4s_to_i8s` int4 decode, a different function and mechanism).\n\n**Reach.** `target_dtype` is typed and `assert`-restricted to `{\"float16\",\"int8\"}` at [utils.py:88](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dequantize_gemm/quantize/utils.py#L88), and `nbits=1`/`nbits=2` each have their own dedicated branch — the function is written to accept exactly these. The only shipped callers of *this* copy are the dequantize-gemm gemv example ([`example_dequant_gemv_fp16xint4.py:204,266`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/examples/dequantize_gemm/example_dequant_gemv_fp16xint4.py#L204), which calls `interleave_weight(qB, num_bits, in_dtype)` with `num_bits=4, in_dtype=\"float16\"` → generic tail, no mask) — verified this session by reading the example's `main()`. The docstring's own example `interleave_weight(qweight, 4, \"float16\")` reaches the generic tail, but **run verbatim it does not even reach a mask — it raises earlier** (`RuntimeError: self.size(-1) must be divisible by 4 to view Char as Int … got 10`, because the docstring's `(10,10)` int8 shape is not `.view(torch.int32)`-able); with a divisible last dim (e.g. `(10,12)`) the generic tail returns normally. So no shipped caller or test of this copy exercises the three special branches — that is why CI is green. The `nbits=2` interleave is itself a real idiom — the bitnet-1.58b examples carry their own local `interleave_weight` and call it — but both bitnet `__main__`s pass `in_dtype=T.int8` → `interleave_weight(qw, 2, \"int8\")`, which is the `nbits=2`/`int8` generic tail (no mask), so they dodge the mask branches regardless of copy. Those copies also spell their masks as a dtype-name-as-constructor (`np.int32(0x…)`); on modern numpy (2.x) that path is *also* broken but with a different failure (`OverflowError: Python integer 4278190335 out of bounds for int32`, since `np.int32(0xFF0000FF)` overflows) — a distinct-adjacent defect, not this one. The defect reported here is specific to the `torch.int32(...)` spelling in `examples/dequantize_gemm/quantize/utils.py`.\n\n**Impact.** The trigger is narrow: only the three special branches `(nbits=1,\"int8\")`, `(nbits=2,\"float16\")`, `(nbits=1,\"float16\")` reach a mask, so it needs a non-default `nbits`/`target_dtype` combination no shipped caller uses. When it fires it is a loud, deterministic `TypeError` at call time — the function aborts before returning, so nothing is silently miscomputed and no output tensor is corrupted; the cost is that these three otherwise-legal input combinations cannot run at all. Fixing it closes that boundary class (makes the documented `nbits=1`/`nbits=2` branches usable), rather than correcting any wrong result in a running workload.", "url": "https://github.com/tile-ai/tilelang/issues/3018"}
{"number": 3004, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.gemm` accepts bf16/int8/tf32 input with `float16` accum (no MMA specialization) then aborts nvcc with a template `static_assert`", "state": "open", "created_at": "2026-08-16T07:20:34Z", "updated_at": "2026-08-16T07:36:40Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nLinux; TileLang 0.1.13; CUDA 12.8 (nvcc), PyTorch 2.8; reproduced on an NVIDIA L40S (sm_89), classic MMA path.\n\n### Problem description\n\n`T.gemm` does not validate the accumulator dtype against the input dtypes. Any A/B/accumulator dtype triple whose dtypes each individually pass the frontend's global dtype gate is accepted, generates otherwise well-formed CUDA (correct `m16n8k16` shape), and then — if the `MmaDispatcher` template has no specialization for that exact triple — aborts `nvcc` deep in template instantiation with `static_assert failed \"tl::mma_sync: unsupported configuration\"`. The compile dies instead of either computing the GEMM or reporting a clean frontend error.\n\nThe class covers every input dtype whose *only* dispatcher accumulator is wider than `float16` — `bfloat16`, `int8`, and `tf32` inputs each have only a `→float32`/`→int32` MMA atom in the table — paired with a `float16` accumulator (which passes the global dtype gate). Verified crashes (§17): `bfloat16`/`float16`, `int8`/`float16`, `float32`(tf32)/`float16`. Inputs whose narrow-accumulator entry *does* exist escape: `float16`/`float16` and `float8_e4m3`/`float16` both compile.\n\nThe accumulator dtype is a normal, documented knob — `example_gemm_sp.py` exposes `--accum_dtype` with `choices=[T.float, T.float16]`. That example pairs the `float16` accumulator with `float16` inputs, which has a dispatcher specialization and compiles+runs (verified, §17), so it dodges the bug.\n\n<details><summary>Trigger boundary — which A/B × accumulator dtype combinations crash vs compile (tested, sm_89)</summary>\n\n| A/B input dtype | accumulator dtype | result |\n|---|---|---|\n| `float16` | `float16` | compiles, correct (`maxdiff 2.6e-4`) |\n| `float16` | `float32` | compiles, correct |\n| `bfloat16` | `float32` | compiles, correct (`maxdiff 6e-8`) |\n| `float8_e4m3` | `float16` | compiles (dispatcher entry exists) |\n| `bfloat16` | `float16` | **nvcc `static_assert` abort** |\n| `int8` | `float16` | **nvcc `static_assert` abort** (same root) |\n| `float32` (tf32) | `float16` | **nvcc `static_assert` abort** (same root) |\n\nThe crash fires whenever the input dtype has no `float16`-accumulator MMA atom in the dispatcher table (`bfloat16`, `int8`, `tf32`), yet the `float16` accumulator passes the frontend's global dtype gate. Inputs whose `float16`-accumulator entry *does* exist (`float16`, `float8_e4m3`) compile. All of the above reproduced on sm_89 (classic MMA path); the WGMMA path (`wgmma.h`) has the same missing-`bfloat16`→`float16` structure but is not runnable on sm_89 (see §17).\n\n</details>\n\n### Reproducible example code\n\n```python\nimport torch, tilelang, tilelang.language as T\n\n# REPRO: bfloat16 GEMM accumulating into a float16 accumulator aborts nvcc.\nM = N = K = 64\n@T.prim_func\ndef main(A: T.Tensor((M, K), \"bfloat16\"),\n         B: T.Tensor((K, N), \"bfloat16\"),\n         C: T.Tensor((M, N), \"float16\")):\n    with T.Kernel(1, 1, threads=32):\n        As = T.alloc_shared((M, K), \"bfloat16\")\n        Bs = T.alloc_shared((K, N), \"bfloat16\")\n        Cl = T.alloc_fragment((M, N), \"float16\")\n        T.clear(Cl)\n        T.copy(A, As); T.copy(B, Bs)\n        T.gemm(As, Bs, Cl)          # -> nvcc: static_assert \"tl::mma_sync: unsupported configuration\"\n        T.copy(Cl, C)\n\nk = tilelang.compile(main, out_idx=[2], target=\"cuda\")   # aborts here\n\n# CONTROL (ORACLE: PASS): identical kernel, float16 inputs instead of bfloat16.\n# A float16 accumulator is a valid target -> compiles and is correct.\n@T.prim_func\ndef ctrl(A: T.Tensor((M, K), \"float16\"),\n         B: T.Tensor((K, N), \"float16\"),\n         C: T.Tensor((M, N), \"float16\")):\n    with T.Kernel(1, 1, threads=32):\n        As = T.alloc_shared((M, K), \"float16\")\n        Bs = T.alloc_shared((K, N), \"float16\")\n        Cl = T.alloc_fragment((M, N), \"float16\")\n        T.clear(Cl)\n        T.copy(A, As); T.copy(B, Bs)\n        T.gemm(As, Bs, Cl)\n        T.copy(Cl, C)\n\nkc = tilelang.compile(ctrl, out_idx=[2], target=\"cuda\")\nA = torch.randn(M, K, device=\"cuda\", dtype=torch.float16) * 0.1\nB = torch.randn(K, N, device=\"cuda\", dtype=torch.float16) * 0.1\nout = kc(A, B).cpu().float(); ref = (A.float() @ B.float()).cpu()\nprint(\"maxdiff\", (out - ref).abs().max().item())          # -> ~2.6e-4\nprint(\"OK\" if torch.allclose(out, ref, atol=2e-2, rtol=2e-2) else \"BAD\")   # -> OK\n```\n\n### Traceback\n\n```\nsrc/tl_templates/cuda/instruction/mma.h(282): error: static assertion failed with \"tl::mma_sync: unsupported configuration\"\n    static_assert(!std::is_void_v<typename Dispatcher::CRegType>, ...\n      detected during instantiation of \"void tl::mma_sync<...>(...)\"\n      [with AType=tl::DataType::kBFloat16, BType=tl::DataType::kBFloat16,\n       CType=tl::DataType::kFloat16, M=16, N=8, K=16, TransA=false, TransB=true, Saturate=false]\n\nsrc/tl_templates/cuda/instruction/mma.h(167): error: static assertion failed with \"tl::mma_sync: unsupported configuration\"\n    static_assert(always_false_v<std::integral_constant<int, M>>, ...\n2 errors detected in the compilation of \".../tvm_kernels.cu\".\n```\n\n### Expected behavior\n\nEither compile and compute the GEMM (accumulating in a supported width and narrowing to the requested `float16` accumulator, the way the `float16`-input path already produces a correct `float16` result), or reject the unsupported dtype combination in the frontend with a clear message — rather than emitting CUDA that a `static_assert` rejects after the frontend has already accepted it.\n\n### Additional context\n\n**Root cause.** The GEMM frontend accepts an accumulator dtype the MMA dispatcher has no atom for, so codegen builds an `mma_sync` template that has no matching specialization and detonates a `static_assert` at nvcc time. (Two levels.)\n\n<details><summary>Mechanism</summary>\n\n*Source-level:* the GEMM frontend does not validate the accumulator dtype against the input dtypes: [`GemmBase.accum_dtype`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/tileop/gemm/gemm_base.py#L104-L105) is simply `self.C.dtype`, so any `C` dtype that passes the global dtype gate is passed straight through to codegen; there is no accum-vs-input dtype check anywhere in the gemm lowering (`gemm_base.py` is the only per-op source at v0.1.13; `src/op/gemm.cc` has no dtype validation). The generated `mma_sync<Ain, Bin, kFloat16, 16, 8, K, ...>` then falls through to the primary-template [`MmaDispatcher` `static_assert(always_false_v<…>)`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/instruction/mma.h#L167) (and the paired [`is_void_v<CRegType>` assert](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/instruction/mma.h#L282)) whenever the input/accumulator triple has no entry in the [dispatcher specialization table](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/tl_templates/cuda/instruction/mma.h#L194-L217). The table defines `kFloat16→kFloat16` (L194) and `kFloat16→kFloat32` (L196), but for `kBFloat16` inputs only `kBFloat16→kFloat32` (L205), for `kInt8` only `→kInt32` (L209), and for `kTensorFloat32` only `→kFloat32` (L256). *Operator-level:* the fragile mechanism is the \"frontend accepts, dispatcher table decides\" split shared by every dtype pair routed through `mma_sync` — so the same crash reproduces across input dtypes (bf16 / int8 / tf32) and across the sibling WGMMA dispatcher (`wgmma.h`), not just the single bf16/f16 case.\n\n</details>\n\n**Suggested fix.** The two Expected-behavior options are not equivalent, and which is right depends on intent: PTX `mma` has a native `f16.f16.f16` accumulate atom (so the `float16`-input path can accumulate directly in `float16`), but `bf16` inputs only have an `f32`-accumulate atom — so a `bfloat16`-input / `float16`-accumulator GEMM would have to accumulate in `float32` and narrow, not map to a native instruction. Adding that specialization vs. rejecting the combination in the frontend is a design choice; the frontend-reject path at minimum turns the late `static_assert` into an early, legible error.\n\n**Provenance.** Reproduced on 0.1.13; the `accum_dtype = self.C.dtype` passthrough (`gemm_base.py`) and the `MmaDispatcher` table (`mma.h`) are both present at the v0.1.9 tag and unchanged since. Not verified against earlier versions, so not characterized as a regression.\n\n**Dedup.** I searched the open and closed tracker (`MmaDispatcher`, `mma_sync unsupported configuration`, gemm accumulator-dtype terms) and found no existing report of this defect. The only keyword hit, #1441, is an unrelated closed report about compiling a specific bf16 FlashAttention kernel.\n\n**Impact.** When it fires the failure is a compile-time `nvcc` `static_assert` abort — loud and caught immediately, after the frontend has already accepted the kernel; it corrupts no values and cannot reach a running workload silently, but it blocks that kernel from building. The trigger is not a single dtype triple but a *family*: any input dtype with no `float16`-accumulator MMA atom (`bfloat16`, `int8`, `tf32`) paired with a `float16` accumulator (§17). No shipped example pairs those. Fixing it either adds the accumulate-in-`f32`-and-narrow specializations or moves the rejection into the frontend, turning a late template-instantiation abort into an early legible error.\n\n**Generalization.** 4-axis sweep, all cells run on 0.1.13 / L40S sm_89 unless noted. Two-level root as under **Root cause** above: (a) source = the `GemmBase.accum_dtype` passthrough with no accum-vs-input validation; (b) operator = the \"frontend accepts / dispatcher table decides\" split shared by every dtype pair routed through `mma_sync`.\n\n| axis | cell tested (input → accum) | observed result | same root? |\n|---|---|---|---|\n| related-type (wider-only-atom neighbor) | `int8`/`int8` → `float16` | nvcc `static_assert \"unsupported configuration\"`, `mma_sync<kInt8,kInt8,kFloat16,16,8,32,...>` | **same root** |\n| related-type (fp32/tf32 input) | `float32`/`float32` → `float16` | nvcc `static_assert`, `mma_sync<kFloat32,kFloat32,kFloat16,16,8,8,...>` | **same root** |\n| related-type (covered-atom contrast) | `float8_e4m3`/`float8_e4m3` → `float16` | **compiles OK** — table has `kFloat8_e4m3→kFloat16` (mma.h L237) | same mechanism, cell covered → no crash (boundary) |\n| related-type (narrow-int accum) | `int8`/`int8` → `int16` | frontend `ValueError: Unsupported dtype: int16` (caught early by a different, global dtype gate) | **distinct** — rejected before codegen, not a dispatcher fall-through |\n| related-source (sibling dispatcher) | WGMMA `wgmma.h` `bfloat16` → `float16` | source-confirmed: `wgmma.h` has `F16_F16_F16` + `F16_F16_F32` + `BF16_BF16_F32` (L274-282) but **no** `BF16_BF16_F16`, same `always_false_v` primary template (L95/L106) | **same root** (Hopper WGMMA path; not runnable on sm_89) |\n\n- **similar-logic / related-operator:** the identical \"construct-then-narrow, validate late\" shape recurs across every `mma_sync` input dtype — the int8, tf32, and bf16 cells above are the meaningful operator siblings and all share the root.\n- **related-type (covered):** `float16` and `float8_e4m3` inputs escape only because their `→float16` entries exist; this pins the root to the *specific missing table cell*, not to fp16 accumulators in general.\n- **Example run (Part 1):** `examples/gemm_sp/example_gemm_sp.py --accum_dtype float16` (sparse gemm, fp16 inputs) compiled and ran on 0.1.13 → dodges the bug (fp16/fp16 has a dispatcher entry); the numeric mismatch it prints is fp16-accumulate tolerance, not a compile failure. `--accum_dtype float32` passed cleanly.\n- **Distinct-adjacent (not filed):** the `int16`-accumulator rejection (`ValueError: Unsupported dtype: int16`) is a *different* mechanism (global dtype gate, fires before codegen) — not this bug.\n- **No new bug** was found while sweeping; every crashing cell reduces to the same missing-`MmaDispatcher`-entry root.\n\n**Reach.** (Verified by running the shipped example.) A `float16` accumulator is a documented, user-facing config: `examples/gemm_sp/example_gemm_sp.py` exposes `--accum_dtype` with `choices=[T.float, T.float16]`. I ran that shipped example verbatim on 0.1.13/sm_89: with `--accum_dtype float16` it compiled and ran (only a fp16-accumulate tolerance miss, not a compile error); with `--accum_dtype float32` it passed cleanly. It dodges this bug because it is the *sparse* gemm path with `float16` inputs (`torch.half`) — the `float16`/`float16` pairing has a dispatcher entry. The other cited sites also use `float16` (not `bfloat16`) inputs (`example_gemm_intrinsics.py` even asserts `in_dtype in [T.float16, ...]`), so none pairs a `float16` accumulator with `bfloat16`/`int8`/`tf32` inputs, and CI stays green. The crash needs one of those non-`float16` inputs combined with the `float16` accumulator; no shipped example uses that pairing.", "url": "https://github.com/tile-ai/tilelang/issues/3004"}
{"number": 3014, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.q_multiply_shift` crashes on a runtime (non-constant) multiplier instead of computing the result", "state": "open", "created_at": "2026-08-16T07:21:08Z", "updated_at": "2026-08-16T07:21:08Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "## Required prerequisites\n\n- [x] I have searched the [issue tracker](https://github.com/tile-ai/tilelang/issues) and did not find an existing report for this defect.\n- [x] I have tested with the latest release.\n\n## What version of TileLang are you using?\n\n0.1.13 (submodule `3rdparty/tvm` at `8df8ebd6`)\n\n## System information\n\nTileLang 0.1.13 / CUDA 12.8 / PyTorch 2.8.0, single NVIDIA A10G (Ampere, sm_86). The crash is in a target-independent TIR legalization pass, so it is not arch-specific.\n\n## Problem description\n\n`T.q_multiply_shift(x, y, q, s)` crashes the compiler whenever the multiplier `y` is a runtime value (e.g. a `BufferLoad`) rather than a compile-time integer constant. The op is a documented public primitive computing the rounded fixed-point product `round(x*y*2^-s)` on Q-format integers, and its signature declares both operands as `PrimExpr` with no constant requirement. Every invocation with a per-element `y` — thread-indexed, in `T.Parallel`, or in `T.serial` — aborts during legalization with:\n\n```\nInternalError: Check failed: (broadcast_node != nullptr) is false:\n```\n\nThe multiplier is the only load-bearing ingredient: a runtime `x` with a constant `y` compiles fine, while a constant `x` with a runtime `y` crashes.\n\n<details><summary>Isolation: which operand triggers it (ran this session)</summary>\n\n| `x` | `y` | `s` | result |\n|---|---|---|---|\n| runtime `BufferLoad` | constant `12345` | const `1` | compiles OK |\n| constant `12345` | runtime `BufferLoad` | const `1` | crashes (`broadcast_node != nullptr`) |\n| runtime `BufferLoad` | constant `12345` (non-pow2) | runtime `BufferLoad` | compiles OK |\n| runtime `BufferLoad` | constant `1<<30` (pow2) | runtime `BufferLoad` | crashes (`broadcast_node != nullptr`) |\n\nSame crash inside `T.serial` with no thread binding, so it is the op legalization, not the launch/threading path.\n\n`get_int_value` is a **missing-guard** helper (it asserts constant instead of returning an optional), and it is called at *two* sites: on the multiplier `y` unconditionally ([L283](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L283)), and on the shift `s` inside the power-of-2 branch ([L285](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L285)). So a runtime `y` always crashes; a runtime `s` crashes too, but only once a constant `y == 1<<30` has steered execution into the power-of-2 branch (last two rows) — with a non-power-of-2 constant `y` the general path is taken and the runtime `s` compiles. Same root (the constant-asserting helper), two reachable call sites; fixing the helper to return a sentinel closes both. (Both rows re-run this session on 0.1.13 — see §17.)\n</details>\n\nNot a regression — see Provenance.\n\n## Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch\n\nN = 16\n\n@T.prim_func\ndef main(X: T.Tensor((N,), \"int32\"), Y: T.Tensor((N,), \"int32\"), Out: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=N):\n        tx = T.get_thread_binding(0)\n        # Multiplier Y[tx] is a runtime value -> crashes in legalization.\n        Out[tx] = T.q_multiply_shift(X[tx], Y[tx], 31, 1)\n\nk = tilelang.compile(main, out_idx=[2])   # InternalError: Check failed: (broadcast_node != nullptr)\n\n# --- Working control: the SAME rounded fixed-point multiply, done by hand in int64.\n#     Compiles and returns the correct value, proving the crash is the op, not the harness.\n@T.prim_func\ndef control(X: T.Tensor((N,), \"int32\"), Y: T.Tensor((N,), \"int32\"), Out: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=N):\n        tx = T.get_thread_binding(0)\n        s = 32                                   # total right shift = q(31) + s(1)\n        prod = T.cast(X[tx], \"int64\") * T.cast(Y[tx], \"int64\")\n        Out[tx] = T.cast((prod + (T.cast(1, \"int64\") << (s - 1))) >> s, \"int32\")\n\nkc = tilelang.compile(control, out_idx=[2])\nx = torch.full((N,), 2**30, dtype=torch.int32).cuda()\ny = torch.full((N,), 2**30, dtype=torch.int32).cuda()\nprint(kc(x, y)[0].item())   # -> 268435456  == 2**28 == round(2**30 * 2**30 * 2**-32)  (correct)\n```\n\n## Traceback\n\n<details><summary>InternalError from the intrinsic legalizer</summary>\n\n```\n[TileLang] TileLang begins to compile kernel `main`\nFatal: Check failed: (broadcast_node != nullptr) is false:\n  ...\n  tvm::tl::IntrinInjecter::VisitExpr_(tvm::tir::CallNode const*)\n  tvm::runtime::detail::LogFatal::~LogFatal()\ntvm.error.InternalError: Check failed: (broadcast_node != nullptr) is false:\n```\n</details>\n\n## Expected behavior\n\nCompilation should succeed and the kernel should compute `round(x*y*2^-s)` for a runtime multiplier, the same value the manual int64 control returns (`2**28` for the inputs above). A runtime `y` is computable: the op's own general lowering path treats `y` as an arbitrary `PrimExpr` (it casts it to int64 and multiplies), so nothing about a non-constant multiplier is unsupported — only the compile-time power-of-2 shortcut needs a constant, and when the value is unknown the shortcut should simply be skipped. If a constant multiplier were genuinely required, the op should reject a non-constant `y` with a clear frontend error rather than aborting deep in legalization; the evidence suggests computing is the natural outcome, but which is appropriate is a call for the maintainers.\n\n## Additional context\n\n**Root cause.** The `tir.q_multiply_shift` `FLegalize` lowering assumes the multiplier `y` is always a compile-time constant, so it fails on any runtime `y`. Before choosing between the power-of-2 fast path and the general path, the lowering unconditionally calls a helper to read `y`'s integer value, and that helper hard-asserts the node is an `IntImm` or a `Broadcast(IntImm)` — a `BufferLoad` is neither, so the assert fires.\n\n<details><summary>Mechanism (pinned)</summary>\n\nThe lowering computes [`if (get_int_value(y) == (1 << 30))`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L283) to detect the power-of-2 multiplier special case. The [`get_int_value` helper](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L270-L279) only recognizes `IntImmNode` or `Broadcast(IntImmNode)`; on any other node it hits [`ICHECK(broadcast_node != nullptr)`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L275) and aborts. A per-element multiplier lowers to a `BufferLoad`, so the guard crashes before the general path is reached.\n\nThe general path — [`QMultiplyShift`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L224-L255) — casts `y` to int64 and multiplies with no constant requirement, which is why a runtime multiplier is computable once the guard stops probing it.\n</details>\n\n**Suggested fix.** Make the power-of-2 detection tolerate a non-constant `y`: attempt the constant extraction and, when `y` is not a compile-time integer, skip the fast path and fall through to the general `QMultiplyShift` lowering (which already handles a symbolic `y`). Concretely, `get_int_value` could return an optional / sentinel instead of asserting, and the `== (1 << 30)` test would then be false for a non-constant `y`.\n\n**Provenance.** The offending guard and helper are present in the TileLang TVM fork at the pinned submodule commit `8df8ebd6` shipped with 0.1.13. The pattern derives from upstream Apache TVM's `q_multiply_shift` legalization and is long-standing; the exact introducing commit is not pinned here. Not a recent regression.\n\n**Dedup.** No existing report covers a `q_multiply_shift` legalization crash; the mechanism (a constant-only guard on the multiplier) is distinct from the other intrinsic-lowering issues on record (math-intrinsic dtype coverage, `T.Broadcast` codegen arity, LayoutInference divide-by-zero).\n\nThis is one of three distinct defects in the same `tirx.q_multiply_shift` legalization (`intrin_rule.cc`): (1) this constant-asserting `get_int_value` helper (runtime `y` → `broadcast_node != nullptr`); (2) the power-of-2 fast path off-by-one at `s == 1` (`1 << -1` shift-range ICHECK); (3) the per-axis op forwarding an integer `is_lshift_required` into a boolean `Select` (`is_bool()` assert). Same file and op family, three separate root causes on different lines — this report covers only (1).\n\n**Reach.** Triggering requires only that the multiplier `y` be a runtime value — the natural way to use the op for per-element quantized rescaling (a rescale factor loaded from a tensor). A compile-time-constant `y` avoids the crash, so a program that hard-codes the multiplier is unaffected. `q_multiply_shift` is a public exported op (`tilelang/language/tir/ir.py`) but is used in no shipped `examples/` and exercised by no test in the repo, which is why CI does not catch it.\n\n*No-example verification (ran this session on 0.1.13):* `git grep q_multiply_shift` over the v0.1.13 tree (excluding the bundled `3rdparty/tvm`) returns **only** the op definition (`op.py:3215`), the `.pyi` stub, and the `_op_wrapper` re-exports in `ast/ir.py` + `tir/ir.py` — **zero** call sites under `examples/`, `testing/`, or `docs/`. There is therefore no shipped example or test to run for this op; the \"no example exercises it\" claim is confirmed by direct grep, not inherited. The op's docstring (`op.py`) documents it as `out = round(x*y*2^-s)` with both operands typed `PrimExpr` and no constant requirement, matching the Problem statement.\n\n**Generalization (§17) — root + 4-axis sweep, all cells re-run on 0.1.13.**\n\n*Two-level root.*\n- *SOURCE-level:* the constant-asserting `get_int_value` lambda in the `tirx.q_multiply_shift` `FLegalize` ([intrin_rule.cc L270-279](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L270-L279)): it reads an integer value to steer the power-of-2 fast path, but `TVM_FFI_ICHECK`s that the node is an `IntImm` or `Broadcast(IntImm)` instead of returning an optional. Any other node (a `BufferLoad`) aborts. It is called at **two** sites: on `y` unconditionally (L283) and on `s` inside the pow2 branch (L285).\n- *OPERATOR-level:* the fixed-point-rescale op family `q_multiply_shift` / `q_multiply_shift_per_axis`, both legalized in this same file and both ultimately delegating to `QMultiplyShift` (L224-255).\n\n*4-axis findings* (input → observed → same-root?):\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| related-source | runtime `s` (`S[tx]`) with **pow2 const** `y=1<<30` → forces the L285 `get_int_value(s)` site | `InternalError: broadcast_node != nullptr` | **YES** — 2nd call site of the same helper |\n| related-source (control) | runtime `s` with **non-pow2 const** `y=12345` → general path, L285 not reached | `COMPILED_OK` | n/a — proves branch-steering, not a bug |\n| related-operator | `q_multiply_shift_per_axis(X[tx], Y[tx], 0,1,31,True,True)` (runtime multiplier) | `COMPILED_OK` | **NO** — its `FLegalize` (L311-325) forwards straight to `QMultiplyShift` with no `get_int_value` guard, so a runtime `y` is fine |\n| related-type / scope | runtime int32 `y` in `T.serial` (no thread binding) | `InternalError: broadcast_node != nullptr` | **YES** — confirms it is op legalization, not the launch/threading path |\n| related-type (dtype) | **const** non-pow2 `y=12345`, all buffers **int16**, `q=15` → general path | `InternalError: (y.dtype().bits() == 32) is false` | **NO — distinct adjacent bug** (see below) |\n| similar-logic | scan of `intrin_rule.cc` for a sibling \"assert-constant-to-steer-a-fast-path\" idiom | none found | n/a — the only other node-`Downcast` (L135, `tvm_access_ptr` buffer-var) is a structurally-required Var arg, not a value probe |\n\n*Boundary / reframe decision.* The root is **narrow and specific**: it is exactly the `get_int_value` helper in the scalar `q_multiply_shift` legalizer. The sibling op `q_multiply_shift_per_axis` does **not** share it (compiles with a runtime multiplier), and no other legalizer in the file uses the idiom. So the report is **kept specific** to `q_multiply_shift`, not reframed to a class. Within that op the root spans **two call sites** (`y` at L283, `s` at L285) — both verified, both closed by one sentinel fix — which the report already covers as a single root.\n\n*Distinct adjacent bug found while sweeping (not filed).* With a constant non-pow2 multiplier the general path is reached, and `QMultiplyShift` hard-asserts `y.dtype().bits() == 32` ([L227](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L224-L255)); an int16 program crashes with `Check failed: (y.dtype().code()==kDLInt && y.dtype().bits()==32)`. This is the documented \"int32-only\" limitation surfacing as a deep legalization `ICHECK` rather than a frontend dtype error — a **distinct root** (dtype restriction, not the constant guard). Recorded here, **not filed**.\n\n*Example run (PART 1).* No shipped example/test exercises `q_multiply_shift` at v0.1.13 (grep result above), so there was no example to run; the crash is demonstrated only by the minimal repro in this report, which reproduces on 0.1.13.", "url": "https://github.com/tile-ai/tilelang/issues/3014"}
{"number": 3013, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.Parallel(coalesced_width=<int>)` aborts compilation instead of accepting the documented int", "state": "open", "created_at": "2026-08-16T07:20:50Z", "updated_at": "2026-08-16T07:20:50Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 / CUDA 12.8 / PyTorch 2.8 ; run on NVIDIA L40S (sm_89). The failure is in `LayoutInference` (host-side layout planning), before any device code is generated, so it is architecture-independent.\n\n### Problem description\n\nPassing an `int` to `T.Parallel`'s documented `coalesced_width` keyword aborts compilation:\n\n```\ntvm.error.InternalError: coalesced_width should be an IntImmNode.\n```\n\n`coalesced_width` is a public keyword of `T.Parallel`, typed `int | None` in the signature ([loop.py#L15](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/loop.py#L13-L15)) and documented as `Optional[int]` ([loop.py#L28](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/loop.py#L28-L29)), so a plain Python int is exactly what the signature promises. It never reaches codegen — the crash fires during `LayoutInference`. The same int works everywhere else it is accepted: passing it via `T.copy(..., coalesced_width=<int>)` compiles fine, and wrapping the int in `tir.IntImm` before handing it to `T.Parallel` also compiles fine (both verified below), so the value itself is legal.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n# 0.1.13: IntImm moved from tvm.tir to tvm.tirx; tilelang re-exports it as T.IntImm.\n\nN, blk = 4096, 256\nA = torch.arange(N, device=\"cuda\", dtype=torch.float32)\n\n# --- FAIL: documented int coalesced_width aborts compilation ---\n@T.prim_func\ndef bug(A: T.Tensor((N,), \"float32\"), B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(N // blk, threads=64) as bx:\n        for i in T.Parallel(blk, coalesced_width=4):   # int, per the signature\n            B[bx * blk + i] = A[bx * blk + i]\n\ntilelang.compile(bug, out_idx=[1], target=\"cuda\")\n# -> tvm.error.InternalError: coalesced_width should be an IntImmNode.\n\n# --- CONTROL: identical kernel, coalesced_width wrapped in IntImm -> compiles & correct ---\n@T.prim_func\ndef ok(A: T.Tensor((N,), \"float32\"), B: T.Tensor((N,), \"float32\")):\n    with T.Kernel(N // blk, threads=64) as bx:\n        for i in T.Parallel(blk, coalesced_width=T.IntImm(\"int32\", 4)):\n            B[bx * blk + i] = A[bx * blk + i]\n\nk = tilelang.compile(ok, out_idx=[1], target=\"cuda\")\nprint(\"control match:\", torch.equal(k(A), A))   # -> True\n```\n\n### Traceback\n\n```\nFile \".../tilelang/cuda/pipeline.py\", line 117, in CUDAPassPipelineBodyPrologue\n    mod = tilelang.transform.LayoutInference()(mod)\n  ...\n  tvm::tl::ParallelOpNode::ComputePlanCandidate(...) const\ntvm.error.InternalError: coalesced_width should be an IntImmNode.\n```\n\n### Expected behavior\n\n`T.Parallel(..., coalesced_width=4)` should compile and use the requested coalesced width, matching its own `int | None` signature and the sibling `T.copy(..., coalesced_width=<int>)` path, which accepts a bare int over the same value. At minimum a documented, correctly-typed argument should not abort compilation.\n\n### Additional context\n\n<details><summary>Guard boundary tested (this session, 0.1.13, L40S) — the bare-int path aborts unconditionally</summary>\n\nEach a separate compile of the same kernel, varying only how `coalesced_width` is passed to `T.Parallel`:\n\n| `coalesced_width=` | result |\n|---|---|\n| `1` (bare int, smallest legal) | CRASH `coalesced_width should be an IntImmNode.` |\n| `2` (bare int) | CRASH `coalesced_width should be an IntImmNode.` |\n| `T.IntImm(\"int32\", 2)` (wrapped) | compiles, output matches |\n| `T.IntImm(\"int32\", 4)` (wrapped, CONTROL) | compiles, output matches |\n\nSo the abort is unconditional on the bare-`int` path (independent of the value) and disappears the moment the same value is wrapped in `IntImm` — a **missing-case / type-coercion gap**, not a value problem. It is a *distinct* branch from the sibling divisibility abort (\"Vector size N is not divisible by coalesced width M\", `parallel.cc:804`): that one honors the int type and rejects on geometry; this one rejects the int type itself (`parallel.cc:808`). Same function `ComputePlanCandidate`, adjacent code, different root — reported separately.\n</details>\n\n**Root cause.** `T.Parallel` attaches the `coalesced_width` argument to the loop-annotation map as a raw Python int, but the C++ layout planner reads it back as an `IntImmNode` and hard-fails when it is not one. In [`loop.py#L79-L87`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/loop.py#L79-L87) the value is placed in `merged_annotations` unconverted (`merged_annotations[\"coalesced_width\"] = coalesced_width`) and handed straight to `_ffi_api.Parallel(extents, merged_annotations)`, which attaches the dict to the `For` node's annotation Map; the value arrives as a runtime int, not a `tir.IntImm`. Then in [`parallel.cc#L798-L809`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L798-L809), `ParallelOpNode::ComputePlanCandidate` does `coalesced_width->as<IntImmNode>()`, which returns null, and takes the `LOG(FATAL)` branch (`parallel.cc:808`).\n\nThe `T.copy` family stores the argument the same raw way ([`copy_op.py#L124-L125`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/copy_op.py#L124-L125)), and its lowering ([`copy.cc#L538-L540`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/copy.cc#L538-L540)) *does* re-attach `kCoalescedWidth` onto a `ForKind::kParallel` loop that reaches the **same** `ComputePlanCandidate` / `as<IntImmNode>()` read. The reason copy does not crash is not that it skips that read — it is that copy passes its annotations through `call_intrin(..., annotations=ann)`, which builds a TVM `Map` and coerces the Python int to a `tir.IntImm` (a proper `IntImmNode`) as part of node construction, whereas `_ffi_api.Parallel` attaches the raw Python dict directly and no such coercion happens. So the divergence is a coercion gap at the FFI boundary of `T.Parallel`, not a difference in what the C++ planner reads. (Verified this session: `T.copy(..., coalesced_width=4)` compiles and returns the correct result on CUDA; see Reach.)\n\n**Suggested fix.** Wrapping the value at the Python boundary — e.g. `merged_annotations[\"coalesced_width\"] = IntImm(\"int32\", coalesced_width)` in `T.Parallel` — makes the annotation an `IntImmNode` and lets the existing `parallel.cc` branch run; the CONTROL above (which does exactly this wrap at the call site) compiles and returns the correct result, so this is the minimal change. Alternatively `ComputePlanCandidate` could coerce the annotation via `Downcast`/`arith` before the `as<IntImmNode>()` read.\n\n**Provenance.** The `coalesced_width` keyword on `T.Parallel` was added by [#1887](https://github.com/tile-ai/tilelang/pull/1887) (merged 2026-03-06), which introduced the raw-int passthrough at `loop.py:80`; that is when the crash first became reachable. The `as<IntImmNode>()` FATAL in `parallel.cc` predates it (present since the codebase migration #10, 2025-01-11), so the two halves never agreed on the type — the argument crashes on every release that exposes the keyword, not a regression.\n\n**Dedup.** Searched the open and closed tracker (`coalesced_width`, `IntImmNode`, `should be an IntImmNode`) and found no existing report of this defect.\n\n**Reach.** The trigger is documented and typed: `coalesced_width` is declared `int | None` and documented `Optional[int]`, so an int is the type the API asks for. `coalesced_width` appears in shipped code (`git grep coalesced_width examples/ testing/` at v0.1.13): every site routes it through `T.copy` / `T.async_copy`, e.g. `examples/amd/example_amd_flash_attn_bwd.py:163,172-173` and `examples/amd/example_amd_flash_attn_fwd.py:183,192-193` (`coalesced_width=vec_size`), and `testing/python/amd/test_tilelang_gfx950_copy_async.py`. No example or test passes it to `T.Parallel` (`git grep 'Parallel(' examples/ testing/ | grep coalesced_width` at v0.1.13 is empty), so CI never exercises the crashing path. These shipped sites are all AMD gfx950-targeted and were not runnable on this session's hardware (L40S, sm_89), but the load-bearing fact they establish — that a **bare int** through the copy family is a legal `coalesced_width` — was verified directly on CUDA this session: `T.copy(dst, src, coalesced_width=4)` compiled and returned `torch.equal(...) == True`. Given the keyword on `T.Parallel`, any value trips the crash — the failure is unconditional on that int path (the CONTROL only differs by wrapping the same value in `IntImm`), and it also fires when the int is supplied via the generic `annotations={\"coalesced_width\": <int>}` dict, so it is the FFI-attach path of `T.Parallel`, not the specific keyword.\n\n**Impact.** The narrow ingredient is passing `coalesced_width` as a bare int through `T.Parallel` specifically (the sibling `T.copy` path and the `IntImm`-wrapped form both compile). When it fires it is a loud compile-time abort during `LayoutInference` — no device code is produced, so nothing is silently miscompiled and no output is corrupted; the failure is deterministic and caught immediately at build time. Fixing it removes a type disagreement between the Python signature (`int | None`) and the C++ `as<IntImmNode>()` read, letting a documented, correctly-typed argument build the kernel it should.\n\n<details><summary><b>Generalization (root analysis + 4-axis sweep, all cells run this session, 0.1.13 / L40S)</b></summary>\n\n**Two-level root.**\n- *Source-level:* `_ffi_api.Parallel(extents, merged_annotations)` ([loop.py#L79-L87](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/loop.py#L79-L87)) attaches the raw Python annotation dict to the `For` node with no int→`IntImm` coercion, while `ParallelOpNode::ComputePlanCandidate` reads the value through `as<IntImmNode>()` and `LOG(FATAL)`s on a miss ([parallel.cc#L798-L809](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L798-L809)). The fragile code is this uncoerced FFI-attach on the `T.Parallel` frame.\n- *Operator-level:* the `coalesced_width` (`kCoalescedWidth`) annotation is consumed only in `ComputePlanCandidate`. Every op that produces a parallel loop carrying that annotation — `T.Parallel`, and via re-attach `T.copy` / `T.async_copy` ([copy.cc#L538-L540](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/copy.cc#L538-L540)) and the atomic reductions (`atomic_add.cc:200-202`, `atomic_reduce.h:129-131`) — funnels through that single `as<IntImmNode>()` read. The copy/atomic ops are safe only because their annotations are laundered into an `IntImm` by `call_intrin`'s `Map` construction; `T.Parallel` is the one entry point that attaches raw.\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| repro | `T.Parallel(..., coalesced_width=4)` (bare int) | CRASH `coalesced_width should be an IntImmNode.` | — (the bug) |\n| control | `T.Parallel(..., coalesced_width=T.IntImm(\"int32\",4))` | COMPILES, `torch.equal==True` | — (control) |\n| similar-logic | same int via generic `T.Parallel(..., annotations={\"coalesced_width\":4})` (bypass the typed kwarg) | CRASH `... should be an IntImmNode.` | **same root** — proves it is the FFI-attach path, not the keyword |\n| related-operator | `T.copy(dst, src, coalesced_width=4)` (bare int) | COMPILES, `torch.equal==True` | distinct outcome — same `as<IntImmNode>()` read, but `call_intrin` coerces int→`IntImm`, so no crash (documents WHY sibling dodges) |\n| related-operator | `T.copy(dst, src, annotations={\"coalesced_width\":4})` (bare int via dict) | COMPILES | same as above — copy launders the dict through `Map` construction |\n| related-source | divisibility sibling branch: `coalesced_width=T.IntImm(\"int32\",3)` (vector_size=4) | CRASH `Vector size 4 is not divisible by coalesced width 3` (parallel.cc:804) | **distinct** — honors the int type, rejects on geometry; not a type-coercion gap. Not filed. |\n| related-type | `coalesced_width=T.IntImm(\"int64\",4)` (width neighbor) | COMPILES, `torch.equal==True` | any `IntImmNode` width passes — confirms the check is type-tag only |\n| related-type | `coalesced_width=np.int32(4)` (common user int, non-`IntImm` runtime scalar) | CRASH `... should be an IntImmNode.` | **same root** — any non-`IntImm` scalar on the `T.Parallel` path aborts |\n\n**PART-1 example run.** The shipped `coalesced_width` sites (AMD flash-attn fwd/bwd examples, gfx950 copy-async test) were located in the v0.1.13 tree; all route the value through `T.copy`/`T.async_copy`, none through `T.Parallel`, so none exercise this crash — the draft's Reach is confirmed by source, not merely inherited. They are AMD-targeted and were not run on this L40S session; the sibling-path claim they support (bare int is a legal `coalesced_width`) was instead verified directly on CUDA (row 3/4 above).\n\n**Framing decision.** The two crashing cells (typed kwarg, generic annotations dict) share one root, so the write-up is kept at the `T.Parallel`-int-path level (title/Problem unchanged) — the class is exactly \"a bare/non-`IntImm` `coalesced_width` reaching `ComputePlanCandidate` via the raw `T.Parallel` FFI attach.\" The divisibility branch (parallel.cc:804) is a **distinct** adjacent check and is *not filed*. No new bug was found while sweeping.\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3013"}
{"number": 3011, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.cumsum`/`T.cummax` shared `src` + fragment `dst` aborts with a message-less internal `ICHECK(IsSharedBuffer(dst))` instead of scanning", "state": "open", "created_at": "2026-08-16T07:20:49Z", "updated_at": "2026-08-16T07:20:49Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13 / CUDA 12.8 / NVIDIA L40S (sm_89).\n\n### Problem description\n\n`T.cumsum(src=shared_buffer, dst=fragment_buffer, dim=0)` aborts compilation with a bare internal check `Check failed: (IsSharedBuffer(op.dst)) is false:` (no message), even though the call passed the frontend's own validation (matching shapes, in-bounds `dim`). The two same-scope forms of the same scan compute the correct answer, so this is a specific `src`-shared / `dst`-fragment combination that ICEs rather than a limitation of the scan itself.\n\nThe frontend's dispatch keys only on `src`'s scope: when `src` is shared it emits the `tl.cumsum` intrinsic regardless of where `dst` lives, and the lowering then asserts `dst` is also shared. There is no user-facing message — the compile just aborts. `T.cummax` shares the identical lowering (`LowerCumSum` and `LowerCumMax` both call the template `LowerSharedScan`), so it aborts on the same input — this is a scan-lowering class bug, not a cumsum-only one (both faces run this session; see §Generalization).\n\nNot a regression — see Provenance.\n\n<details><summary>Trigger vs. the two working sibling forms (all three ran this session)</summary>\n\n| `src` scope | `dst` scope | result |\n|---|---|---|\n| shared | shared | compiles, `max_err=9.5e-07` vs `torch.cumsum` |\n| fragment | fragment | compiles, matches torch (delegates to `cumsum_fragment`, which stages through a shared scratch) |\n| **shared** | **fragment** | **`ICHECK(IsSharedBuffer(op.dst))` abort** |\n\nSo the scan is computable into a fragment — the fragment→fragment path already does exactly that internally (copy to shared scratch, scan, copy back). Only the shared→fragment spelling has no path.\n\n</details>\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\n\ndef build(dst_is_fragment):\n    @T.prim_func\n    def kernel(A: T.Tensor((128,), \"float32\"), B: T.Tensor((128,), \"float32\")):\n        with T.Kernel(1, threads=128):\n            A_shared = T.alloc_shared((128,), \"float32\")\n            T.copy(A, A_shared)\n            if dst_is_fragment:\n                B_dst = T.alloc_fragment((128,), \"float32\")   # TRIGGER: shared src -> fragment dst\n            else:\n                B_dst = T.alloc_shared((128,), \"float32\")      # CONTROL: shared src -> shared dst\n            T.cumsum(src=A_shared, dst=B_dst, dim=0)\n            T.copy(B_dst, B)\n    return kernel\n\ntilelang.compile(build(False), out_idx=[1])  # control: compiles fine\ntilelang.compile(build(True),  out_idx=[1])  # trigger: aborts with the ICHECK below\n```\n\n### Traceback\n\n```\n[..] : Fatal: InternalError: Check failed: (IsSharedBuffer(op.dst)) is false:\n```\n\nThe check fires with no message — the reader gets neither the offending scopes nor a hint that a fragment `dst` is the problem.\n\n### Expected behavior\n\nA call the frontend accepts should not abort in the C++ lowering with a message-less internal check. Since the same scan into a fragment already works when `src` is a fragment (the `cumsum_fragment` macro stages through a shared scratch), one direction is to route a shared-`src`/fragment-`dst` scan the same way (scan in shared, copy the result to the fragment `dst`). If mixing scopes is intentionally out of scope, a clear frontend error naming the constraint — as the docstring documents `dst` as a general output buffer — would at least turn the abort into a diagnostic. Which is appropriate depends on whether the path is meant to support a differently-scoped `dst`; the current message-less `ICHECK` is the part that seems wrong either way.\n\n### Additional context\n\n**Root cause.** The frontend selects the lowering path from `src`'s scope alone, so a shared `src` with a fragment `dst` reaches the shared-scan intrinsic, whose lowering then asserts the `dst` is also shared and aborts. The Python `cumsum` validates that `dst`'s *shape* matches `src` but never checks its *scope* ([`tilelang/language/scan_op.py#L135-L146`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/scan_op.py#L135-L146) — the `is_fragment(src)` fork, else emit the intrinsic), and the shared branch of the C++ template `LowerSharedScan` has the unconditional [`ICHECK(IsSharedBuffer(op.dst))`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/backend/common/op/scan.h#L37) (on 0.1.9 this is `src/op/reduce.cc:626`). Because `LowerCumSum` and `LowerCumMax` both instantiate this one template, `T.cummax` inherits the identical abort. The assert fires before the ndim (1D/2D) split and before `op.dst->dtype` is touched, so the crash is independent of rank and dtype (all confirmed this session).\n\n**Suggested fix.** Route a shared-`src`/fragment-`dst` scan the same way the fragment-`src` path already does (scan in a shared scratch, copy the result to the fragment `dst`) — the `cumsum_fragment` macro already implements exactly that staging. If mixing scopes is intentionally out of scope, at minimum replace the message-less `ICHECK` in the shared branch with a frontend error naming the constraint.\n\n**Provenance.** The `ICHECK(IsSharedBuffer(op.dst))` was added to the CumSum lowering in [#1909](https://github.com/tile-ai/tilelang/pull/1909) (merged 2026-03-18); the surrounding shared/fragment branch structure predates it, so the shared-`src`/fragment-`dst` input has aborted since. Relocated to `src/backend/common/op/scan.h` by the backend split (#2156), unchanged in logic.\n\n**Dedup.** I searched the open and closed tracker for `cumsum`. The other cumsum reports are different mechanisms — shared-`dst` sub-region wrong values (#2523, #2536) and a device-template `static_assert` on unsupported thread counts (#2576); none concern the `src`-shared / `dst`-fragment scope combination or this scope `ICHECK`.\n\n**Reach.** The `dst` parameter is documented and shape-validated at [`scan_op.py#L73-L78`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/scan_op.py#L73-L78) (the `_prepare_scan_args` rank/dim check — it validates `dst`'s shape but never its scope), so passing an explicit `dst` is a supported spelling; a fragment output is a natural choice when the scan result is consumed by later fragment-scoped compute. Every shipped caller dodges the crash by keeping `src` and `dst` the same scope: `git grep 'T.cumsum\\|T.cummax'` over `examples/ testing/` at v0.1.13 finds 22 call lines across 5 files (`examples/gdn/example_cumsum.py`, `testing/python/issue/test_tilelang_issue_1001.py`, `testing/python/language/test_tilelang_language_rand.py`, `testing/python/language/test_tilelang_language_scan.py`, `testing/python/target/test_tilelang_codegen_cutedsl_scan.py`) — all are in-place (`dst=None`), shared→shared, or fragment→fragment (via the `cumsum_fragment` macro); none mix `src`/`dst` scope, which is why CI is green.\n\nRan the shipped example this session (0.1.13, L40S) to confirm it dodges. `examples/gdn/example_cumsum.py`'s `main()` uses `use_fragment=False`. Its `main()` cannot run standalone here (its torch reference `chunk_local_cumsum_scalar` is only defined when the optional `fla` package imports; without `fla` it raises `NameError` in the reference path — a pre-existing example dependency, unrelated to this bug). Compiling the shipped builder `tilelang_chunk_local_cumsum_scalar` directly, both shipped branches build and match torch: `use_fragment=False` (shared, in-place `T.cumsum(G_shared, dim=1)`) → `max_err 9.5e-07`; `use_fragment=True` → `max_err 2.1e-06`. The `use_fragment=True` branch does **not** exercise the bug either: it allocates `T.alloc_fragment((1,block_S), scope=\"shared\")` (an override that makes `is_fragment` False) and calls `T.cumsum(G_fragment, dim=1)` in-place (`dst=None`), so it stays shared→shared. No shipped caller reaches the shared-`src`/fragment-`dst` combination.\n\n**Generalization (this session, 0.1.13, L40S — every cell run, one kernel per fresh process).**\n\nTwo-level root:\n- **Source-level root:** the template `LowerSharedScan` in [`src/backend/common/op/scan.h`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/backend/common/op/scan.h#L28-L76). It dispatches on `op.src` scope: `IsFragmentBuffer(src)&&IsFragmentBuffer(dst)` → `LOG(FATAL)` \"not implemented\"; `else if (IsSharedBuffer(op.src))` → **`ICHECK(IsSharedBuffer(op.dst))`** (L37, message-less); `else` → `ICHECK(false) << \"Cannot lower … for <src> and <dst>\"` (L71). The Python frontend (`scan_op.py`) mirrors this — it branches only on `is_fragment(src)`.\n- **Operator-level root:** every shared-scan op instantiates this one template. `LowerCumSum` and `LowerCumMax` (scan.h L78/L84) both call `LowerSharedScan`, so any op reachable through it correlates.\n\n4-axis sweep:\n\n| axis | cell tested (all shared `src`, unless noted) | observed result | same root? |\n|---|---|---|---|\n| baseline | `T.cumsum` shared→fragment, 1D fp32 | `ICHECK(IsSharedBuffer(op.dst))` abort (message-less) | — |\n| control | `T.cumsum` shared→shared, 1D fp32 | compiles, `max_err 9.5e-07` vs `torch.cumsum` | — |\n| control | `T.cumsum` fragment→fragment (in-place), 2D fp32 | compiles, matches torch (routes via `cumsum_fragment` shared-scratch staging) | — |\n| related-operator | `T.cummax` shared→fragment, 1D fp32 | **same** `ICHECK(IsSharedBuffer(op.dst))` abort | yes (shares `LowerSharedScan`) |\n| related-type (dtype+rank) | `T.cumsum` shared→fragment, **2D fp16** | **same** `ICHECK(IsSharedBuffer(op.dst))` abort | yes (assert precedes ndim split & dtype use) |\n| related-source | `T.cumsum` **global** `src` → shared `dst` | `ICHECK(false) … \"Cannot lower cumsum for global and shared.dyn\"` | same root (src-only dispatch), **distinct symptom** — this branch *does* name the scopes |\n\nAxes skipped: **similar-logic** — the sibling scan.h fragment→fragment branch (L32, `LOG(FATAL)` \"not implemented\") is unreachable from the frontend, because Python intercepts `is_fragment(src)` and routes to the `cumsum_fragment` macro before the intrinsic is emitted; so no separate cell exists. No random cells added.\n\n**Findings.** The bug is a **scan-lowering class**, not cumsum-specific: `T.cumsum` and `T.cummax` both abort identically on shared-`src`/fragment-`dst`, at all ranks and dtypes (the `ICHECK` fires before any rank/dtype-specific code). The scan into a fragment is computable — the fragment→fragment control already does exactly that staging (copy to shared scratch, scan, copy back); only the shared→fragment *spelling* is unrouted. A distinct-adjacent sibling in the same function is the **global-`src`** input, which reaches the `else` branch (scan.h L71) with a *descriptive* message; that is a separate missing-path in the same src-only dispatch and is **not filed** here.\n\n**Impact.** The trigger is narrow — it needs the specific `src`-shared / `dst`-fragment scope pairing, which every shipped caller currently avoids — but it spans the whole shared-scan family (`T.cumsum` and `T.cummax`, all ranks and dtypes) because they share one lowering template. When it fires the failure is a compile-time `ICHECK` abort: loud, deterministic, and caught the moment the kernel is built, so it corrupts no output and cannot slip into a running workload silently — the only cost is that this valid, frontend-accepted spelling won't compile (and does so via a message-less internal check rather than a diagnostic). Fixing it closes a scope-combination hole in the scan lowering and turns the bare `ICHECK` into either a working path or a clear frontend error.", "url": "https://github.com/tile-ai/tilelang/issues/3011"}
{"number": 3009, "title": "[BUG][Fuzzer][ice-on-valid-code] a non-`bool` buffer element as a bare `if`/`assert` condition aborts with an internal `is_bool()` assert instead of testing `!= 0`", "state": "open", "created_at": "2026-08-16T07:20:48Z", "updated_at": "2026-08-16T07:20:48Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and no similar report exists.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13 / L40S (sm_89), CUDA 12.8. The abort happens during frontend compilation (TIR analysis), before any codegen, so it is GPU-independent.\n\n### Problem description\n\nGuarding work with `if <mask>[i]:` — the block-sparse idiom the shipped examples use (`examples/blocksparse_gemm`, `examples/blocksparse_attention`) — aborts compilation as soon as the mask buffer is a non-`bool` type. `if A[i]:` on an int buffer is the ordinary Python spelling of \"if this element is non-zero\", but it aborts with an internal TVM assert:\n\n```\ntvm.error.InternalError: Check failed: (a.dtype().is_bool()) is false:\n```\n\nThe identical kernel written with the explicit comparison `if A[i] != 0:` compiles and runs (control below). So the integer condition is silently accepted at parse time and only detonates later, in a downstream pass that composes the raw int condition with a boolean operator, with a message that never mentions the user's `if`.\n\nThe abort is the same on the `@T.prim_func` path and the `@tilelang.jit` eager path (both go through the same `if` visitor). Where it detonates depends on the branch shape: with an `else` branch the `LegalizeNegativeIndex` pass builds `!condition` and trips the bool-only `Not` constructor (bare `Check failed: (a.dtype().is_bool())`); with only a `then` branch the buffer-index flattener composes the condition with `&&` and trips the `logical_and` RHS check (`Check failed: (rhs.dtype().is_bool()) ... Expected boolean argument as RHS of && operator (logical AND), but received A[i] of type int32`). Same root — the raw `int32` condition is never coerced to `bool` — two detonation sites.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T\n\n# FAIL: integer truthiness in an `if`. `if A[i]:` on an int buffer is the normal\n# Python spelling of `A[i] != 0`.\n@T.prim_func\ndef k(A: T.Tensor((8,), \"int32\"), B: T.Tensor((8,), \"int32\")):\n    with T.Kernel(1, threads=8) as bx:\n        i = T.get_thread_binding()\n        if A[i]:            # <-- int32 condition\n            B[i] = 1\n        else:\n            B[i] = 0\ntilelang.compile(k, out_idx=[1]).get_kernel_source()   # InternalError: is_bool() ...\n\n# PASS control: explicit `!= 0` compiles and runs — isolates the defect to the\n# bare integer condition, not the surrounding kernel.\n@T.prim_func\ndef k_ok(A: T.Tensor((8,), \"int32\"), B: T.Tensor((8,), \"int32\")):\n    with T.Kernel(1, threads=8) as bx:\n        i = T.get_thread_binding()\n        if A[i] != 0:\n            B[i] = 1\n        else:\n            B[i] = 0\ntilelang.compile(k_ok, out_idx=[1]).get_kernel_source()  # -> compiles OK\n```\n\n### Traceback\n\n```\n# with an `else` branch (LegalizeNegativeIndex builds `!condition`):\n  in tvm::tl::LegalizeNegativeIndex(tvm::tirx::PrimFunc)\n  in tvm::arith::IRVisitorWithAnalyzer::VisitStmt_(tvm::tirx::IfThenElseNode const*)\n  File \".../3rdparty/tvm/src/tirx/ir/expr.cc\", line 471, in tvm::tirx::Not::Not(tvm::PrimExpr, tvm::Span)\ntvm.error.InternalError: Check failed: (a.dtype().is_bool()) is false:\n\n# with a `then`-only branch (buffer-index flattener composes `condition && ...`):\n  in tvm::tl::BufferFlattener::GetSimplifiedElemOffset(...)\n  in tvm::operator&&(tvm::PrimExpr, tvm::PrimExpr)\n  in tvm::logical_and(tvm::PrimExpr, tvm::PrimExpr, tvm::Span)\ntvm.error.InternalError: Check failed: (rhs.dtype().is_bool()) is false:\nExpected boolean argument as RHS of && operator (logical AND), but received A[i] of type int32\n```\n\n### Expected behavior\n\n`if A[i]:` on a non-boolean condition should be treated as `if A[i] != 0:` (Python truthiness), which the compiler already handles correctly — the `!= 0` control produces a working kernel. Failing that, a parse-time diagnostic naming the `if` and the non-boolean condition would at least point the user at the real line, instead of an internal assert in a region-analysis pass.\n\n### Additional context\n\n**Root cause.** The `if` visitor accepts a non-boolean condition without coercing it to `bool`, so an integer `PrimExpr` becomes the `IfThenElse` condition; a later pass applies `!`/`&&` to that condition and asserts it must be boolean. In [`visit_if`, `tilelang/language/parser/parser.py:475-504`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/parser/parser.py#L475-L504) the evaluated predicate is passed straight into `T.If(...)` whenever it is a `PrimExpr`/`ExprOp` — the `else` clause only rejects a predicate that is neither `PrimExpr` nor `bool`, so an `int32` element slips through unchecked and un-cast.\n\n<details>\n<summary>where it detonates</summary>\n\nThe raw `int32` condition survives to a downstream pass that composes it with a boolean operator, and the bundled `tile-ai/tvm` boolean ops assert their arguments are `bool`:\n\n- **With an `else` branch:** `LegalizeNegativeIndex` builds `!condition`, and the `Not` constructor asserts [`a.dtype().is_bool()`, `src/tirx/ir/expr.cc:471`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/ir/expr.cc#L471) (bare `TVM_FFI_ICHECK`, no message).\n- **With a `then`-only branch:** the buffer-index flattener composes the condition with `&&`, and `logical_and` trips the RHS-must-be-bool check [`type_check_boolean_args`, `src/tirx/op/op.cc:705-708`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L705-L708) (`Expected boolean argument as RHS of && operator ...`).\n\nDifferent passes, same root: neither `visit_if` nor anything between it and these ops coerces the `int32` predicate to `bool`.\n\n</details>\n\n**Suggested fix.** In `visit_if` (and the sibling `visit_assert`), when the predicate is a `PrimExpr` that is not already boolean, coerce it to `bool` (`cond != 0`) before entering the `T.If`/`T.Assert` frame — matching Python truthiness, what the user's explicit `!= 0` already does, and what `visit_while` effectively already does (its lowering emits `if (!(cond)) break;`, so an int `while` compiles). Alternatively, reject a non-boolean condition at parse time with a diagnostic naming the statement — as `T.if_then_else` already does (`if_then_else only accept the condition to be boolean type`), which is a far better message than the `if` path's internal `is_bool()` assert.\n\n**Provenance.** The unchecked-condition handling is in TileLang's own `if` visitor (`tilelang/language/parser/parser.py`), and the bool-only asserts it flows into are in the bundled `tile-ai/tvm` op/expr layer. It aborts on 0.1.13 and is not a regression introduced by a recent change. I did not pin an introducing PR.\n\n**Root, two levels.** (a) SOURCE-level: [`visit_if`, `parser.py:474-504`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/parser/parser.py#L474-L504) passes the evaluated predicate straight into `T.If(...)` whenever it is a `PrimExpr`/`ExprOp`, with no coercion to `bool`; the sibling condition-takers [`visit_assert`, `parser.py:508-524`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/parser/parser.py#L508-L524) and [`visit_while`, `parser.py:201-216`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/tilelang/language/parser/parser.py#L201-L216) share the same uncoerced-condition shape. (b) OPERATOR-level: the raw non-`bool` `PrimExpr` reaches a bundled-tvm op/stmt that asserts `is_bool()`/`is_predicate_dtype()`/`==Bool()` — `Not` ([`expr.cc:471`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/ir/expr.cc#L471)), `logical_and` ([`op.cc:705-708`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L705-L708)), `AssertStmt` ([`stmt.cc:107`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/ir/stmt.cc#L107)), `if_then_else` ([`op.cc:634`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L634)). Any bool-only op the uncoerced condition later flows into is a detonation site; which one fires depends on the construct and branch shape.\n\n**Generalization (0.1.13, L40S). All cells run this session, one kernel per fresh process — observed, not asserted.**\n\n| axis | cell tested | result | same-root? |\n|---|---|---|---|\n| base | `if A[i]:` int32, `if`/`else` | abort `LegalizeNegativeIndex`→`Not::Not` `expr.cc:471` (`is_bool()`) | — |\n| base | `if A[i]:` int32, `then`-only | abort `BufferFlattener`→`logical_and` `op.cc:707` (`... RHS of && ... of type int32`) | yes (raw int reaches a diff bool-only op) |\n| control | `if A[i] != 0:` int32 | **compiles + runs** | boundary |\n| control | `if A[i]:` `bool` (shipped-mask spelling) | **compiles** | boundary |\n| related-type | `if A[i]:` `int8` | abort `Not::Not` `expr.cc:471` (`is_bool()`) | yes |\n| related-type | `if A[i]:` `uint8` | abort `Not::Not` `expr.cc:471` (`is_bool()`) | yes |\n| related-type | `if A[i]:` `float32` | abort `Not::Not` `expr.cc:471` (`is_bool()`) | yes — not int-specific, any non-`bool` element |\n| related-operator | `if not A[i]:` int32 | abort `Not::Not` `expr.cc:471`, EAGER at parse (the `not` builds `Not(int)` directly) | yes, same site, distinct trigger path |\n| related-operator | `T.if_then_else(A[i],1,0)` int32 selector | abort `op.cc:634` `if_then_else only accept the condition to be boolean type` (clear msg, eager) | yes — same class (non-`bool`→bool-only op), distinct op/site |\n| similar-logic | `assert A[i]` int32 (via `visit_assert`) | abort `AssertStmt::AssertStmt` `stmt.cc:107` `AssertStmt should have boolean condition ... dtype int32` | yes — sibling parser visitor, same uncoerced-condition root |\n| similar-logic | `while n[0]:` int32 (via `visit_while`) | **compiles + CORRECT** — codegens `while(1){ if(!(n[0])) break; ...}` | **boundary: distinct path, no bug** — `While` lowering handles the int condition via C `!` |\n\nClass = *a non-`bool` buffer element (any width, signed/unsigned, or float) used as a bare condition in `if` / `assert` / `not` / `if_then_else`*; all abort with a bool-only op assert. Boundary: an explicit `!= 0`, a `bool` condition, and a `while` loop all compile (the `while` path does the coercion the `if` path omits — this is the fix precedent). `if_then_else` and `assert` reach the abort through their own tvm ops with clearer messages; they are the same defect class, worth the same one-line coercion fix, not separate bugs.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of an integer `if` condition aborting on the `is_bool()` assert. (Sibling-in-spirit but distinct root from the float-`%`/`//` abort: that is the *int*-only op-assert on `floormod`/`floordiv`; this is the *bool*-only op-assert on `Not`/`&&`. Different operators, asserts, and fix sites, so filed separately.)\n\n**Impact.** The trigger is a bare non-`bool` buffer element used as a condition (`if` / `assert` / `not` / `if_then_else`), which the shipped idiom currently sidesteps by declaring every mask `bool`. When it fires it is a compile-time abort — loud and immediate, caught before any code is generated, so it corrupts no output and cannot reach a workload silently; the cost is that a valid `if M[i]:` on an integer mask won't build, and (for the `if`/`not` paths) the internal `is_bool()` assert names a downstream pass (`LegalizeNegativeIndex` or the index flattener) rather than the user's line. Fixing it closes the whole class — non-`bool` truthiness compiles like the explicit `!= 0` — and replaces the internal assert with either a working kernel or a diagnostic that points at the real line. The `while` path already does this coercion correctly and is the fix precedent.\n\n**Reach.** Shipped examples located and run this session (0.1.13). The `if <mask>[i]:` idiom is shipped and I verified each cited mask is `bool` in the v0.1.13 tree: `examples/blocksparse_gemm/example_blocksparse_gemm.py:82` guards on `BlockMask`, declared `T.Tensor(..., \"bool\")` at line 69; `examples/blocksparse_attention/.../sparse_gqa_decode_varlen_mask.py:78` guards on `block_mask`, declared `T.Tensor(shape_mask, T.bool)` at line 42; `examples/deepseek_v32/sparse_mla_fwd_pipelined.py:258` guards on `is_kv_valid`, allocated `T.alloc_shared([BI], \"bool\", ...)` at line 92 (the seesaw variant lines 219/251 the same). I RAN the shipped blocksparse kernel (the `test_pipeline_order_stage` body from `testing/python/language/test_tilelang_language_pipeline.py`, `BlockMask: T.Tensor(..., \"bool\")`) verbatim: it compiles (`get_kernel_source()` → 8700-char kernel). So every shipped site DODGES — solely because the mask is `bool`. To confirm the crash is one dtype away in the real kernel, I re-ran that exact shipped kernel with `BlockMask` changed to `int8` and the `if` directly on the buffer element: it aborts `logical_and` RHS `op.cc:707` — `Expected boolean argument as RHS of && ... but received BlockMask[(by*2+bx)*8] of type int8`. Storing a 0/1 mask as `int8`/`uint8` (a common flag encoding) trips the bug in an otherwise-shipped kernel. CI stays green because every shipped mask is `bool`.", "url": "https://github.com/tile-ai/tilelang/issues/3009"}
{"number": 3008, "title": "[BUG][Fuzzer][ice-on-valid-code] Float operands to the integer-only div/mod ops (`%`, `//`, `T.floormod`/`floordiv`/`ceildiv`/`truncdiv`) abort with an internal assert", "state": "open", "created_at": "2026-08-16T07:20:37Z", "updated_at": "2026-08-16T07:20:37Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and this has not been filed.\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\nTileLang 0.1.13; CUDA 12.8; NVIDIA L40S (sm_89). The crash is in host-side IR construction, so it is architecture-independent (it fires before any codegen).\n\n### Problem description\n\nWriting `A[i] % B[i]` or `A[i] // B[i]` in a kernel where `A`/`B` are `float32` (or any float dtype) aborts kernel construction with an internal fatal assert:\n\n```\nInternalError: Check failed: (a.dtype().is_int() || a.dtype().is_uint()) is false: A[i]\n```\n\nThe `%` and `//` Python operators are overloaded on tensor loads and accept float operands silently; the failure only surfaces deep inside `tvm::floormod`/`tvm::floordiv` as a `LogFatal`, i.e. an internal check firing rather than a diagnostic the frontend raises. Float modulo and floor-division are well-defined (`5.3 % 3.0 == 2.3`, as in Python/NumPy/`torch`), so a natural kernel expression crashes the compiler.\n\nThe same abort happens through every spelling that reaches these ops. The class is the **integer-only division/modulo family in `src/tirx/op/op.cc`**, all of which open with the identical `TVM_FFI_ICHECK(a.dtype().is_int() || a.dtype().is_uint())` guard and have no float branch: `floormod` (via `%` / `T.floormod`), `floordiv` (via `//` / `T.floordiv`), `ceildiv` (via `T.ceildiv`), and `truncdiv` (via `T.truncdiv`) — all four abort identically on a float operand (verified this session, see §Generalization). This is reachable in both the eager `@tilelang.jit` DSL and the classic `T.prim_func` path. `int32`/`uint` operands compile and compute correctly; only float operands trip it. The float-native siblings that carry no such guard — `T.fmod`, `T.truncmod`, and float `/` (`div`) — compile and compute. Not a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\nN = 128\n\n@tilelang.jit\ndef mod_kernel(A, B):\n    A: T.Tensor[[N], T.float32]\n    B: T.Tensor[[N], T.float32]\n    C = T.empty((N,), T.float32)\n    with T.Kernel(1, threads=N) as bx:\n        for i in T.Parallel(N):\n            C[i] = A[i] % B[i]            # float floor-mod -> internal abort\n    return C\n\n@tilelang.jit\ndef mod_int(A, B):                        # control: identical kernel, int32\n    A: T.Tensor[[N], T.int32]\n    B: T.Tensor[[N], T.int32]\n    C = T.empty((N,), T.int32)\n    with T.Kernel(1, threads=N) as bx:\n        for i in T.Parallel(N):\n            C[i] = A[i] % B[i]\n    return C\n\n# control PASSes: int32 % compiles and matches torch\nci = mod_int(torch.arange(N, dtype=torch.int32, device=\"cuda\"),\n             torch.full((N,), 7, dtype=torch.int32, device=\"cuda\")).cpu()\nassert bool((ci == (torch.arange(N) % 7)).all())          # -> True\n\n# the bug: float % aborts kernel construction\na = torch.full((N,), 5.3, dtype=torch.float32, device=\"cuda\")\nb = torch.full((N,), 3.0, dtype=torch.float32, device=\"cuda\")\nmod_kernel(a, b)                          # InternalError: is_int() || is_uint() is false: A[i]\n```\n\n`//`, `T.floormod`, `T.floordiv`, `T.ceildiv`, and `T.truncdiv` on float abort identically (all four ops share one int-only guard); fp16/bf16 operands abort the same way.\n\n### Traceback\n\n```\nFile \".../tilelang/language/eager/builder.py\", line 1233, in get_tir\n    self.ir_gen.gen(builder)(**tensor_args, **kwargs)\nFile \".../mod_kernel\", line 14, in mod_kernel\n    C[i] = A[i] % B[i]\nFile \".../tilelang/3rdparty/tvm/python/tvm/tirx/expr.py\", line 118, in __mod__\n    return _ffi_api._OpFloorMod(self, other, None)\nFile \"python/tvm_ffi/cython/function.pxi\", line 968, in tvm_ffi.core.Function.__call__\nFile \".../3rdparty/tvm/src/tirx/op/op.cc\", line 599, in tvm::PrimExpr tvm::floormod(PrimExpr, PrimExpr, Span)\ntvm.error.InternalError: Check failed: (a.dtype().is_int() || a.dtype().is_uint()) is false: A[i]\n```\n\n(This is the traceback on the eager `@tilelang.jit` path; the underlying `tvm::floormod` `ICHECK` is the same on the classic `T.prim_func` path. For `//` the abort is identical at `tvm::floordiv`, `src/tirx/op/op.cc:573`.)\n\n### Expected behavior\n\nEither of two behaviors would remove the internal abort, depending on whether float floor-mod/floor-div is meant to be supported:\n\n- **Compute it.** The operation is well-defined and already computable from primitives the compiler emits: `a // b == floor(a / b)` and `a % b == a - floor(a/b) * b` (Python/NumPy/`torch` floor semantics). Note this differs from `T.fmod`, which is C `fmod` (truncated toward zero) — it matches on positive operands but not on negatives (`-5.3 % 3.0` is `0.7` under floor semantics vs `-2.3` under `fmod`), so `T.fmod` is not a drop-in substitute for `%`.\n- **Reject at the frontend with a clear message.** If float floor-mod is intentionally unsupported (upstream TVM restricts `floormod`/`floordiv` to integers), a Python-level `TypeError`/`ValueError` at the `%`/`//` overload naming the unsupported dtype would be far better than a `LogFatal` internal assert with no source location.\n\nThe evidence leans toward *compute*, for two reasons: (1) it is cheaply computable from primitives the compiler already emits (`floor(a/b)`), and (2) TileLang **already supports float division/modulo elsewhere** — float `/` (`div`), `T.fmod`, and `T.truncmod` carry no int-only guard and compile fine — so rejecting float `%`/`//` would be internally inconsistent with those siblings (a user reasonably expects `//`/`%` to work wherever `/` does). Either way, the `%`/`//` overloads currently accept float operands and then abort internally; surfacing this at the frontend (compute, or at least a clean diagnostic) is the fix.\n\n### Additional context\n\n**Root cause.** The `%`/`//` operators and the `T.floormod`/`T.floordiv`/`T.ceildiv`/`T.truncdiv` named ops route to `tvm::floormod`/`floordiv`/`ceildiv`/`truncdiv`, each of which opens with the same `is_int() || is_uint()` assert and has no float path. Nothing between the Python operator/`T.*` overload and that assert rejects or handles float operands — so a float operand reaches the int-only check and aborts.\n\n<details>\n<summary>Mechanism: operator overload → int-only C++ op</summary>\n\n`PrimExpr.__mod__` calls `_OpFloorMod` and `PrimExpr.__floordiv__` routes through the generic `floordiv`:\n[`expr.py:118` (`__mod__`)](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/tirx/expr.py#L118),\n[`generic.py:127` (`floordiv`)](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/tirx/generic.py#L127).\n\nBoth land in the C++ ops whose first statements assert the operands are int/uint:\n[`floordiv`, op.cc:573](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L573) and\n[`floormod`, op.cc:598](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L598):\n\n```cpp\nPrimExpr floormod(PrimExpr a, PrimExpr b, Span span) {\n  TVM_FFI_ICHECK(a.dtype().is_int() || a.dtype().is_uint()) << a;   // <- aborts on float\n  TVM_FFI_ICHECK(b.dtype().is_int() || b.dtype().is_uint()) << b;\n  ...\n}\n```\n\nThere is no float branch and no earlier guard, so a float operand reaches the `TVM_FFI_ICHECK` and aborts.\n\n</details>\n\n**Suggested fix.** Either add a float branch to `floormod`/`floordiv` (`src/tirx/op/op.cc:598/573`) that lowers to the primitives already emitted (`a // b == floor(a / b)`, `a % b == a - floor(a/b) * b`, floor semantics — not C `fmod`), or reject float operands at the `%`/`//` Python overload with a `TypeError`/`ValueError` naming the unsupported dtype, so the failure surfaces at the frontend rather than as an internal `LogFatal` (not verified end-to-end).\n\n**Provenance.** Long-standing; the int-only `TVM_FFI_ICHECK` on `floormod`/`floordiv`/`ceildiv`/`truncdiv` lives in the bundled `tile-ai/tvm` op layer (`src/tirx/op/op.cc`, pinned at `8df8ebd6` for 0.1.13) and is inherited from the upstream TVM `tir` floor-op semantics, not introduced by a recent TileLang change. Not a regression.\n\n**Generalization (0.1.13, L40S, all cells re-run this session, one kernel per fresh process).**\n\n*Two-level root.*\n- **Source-level root:** `src/tirx/op/op.cc`. Four ops in the div/mod family open with the identical `TVM_FFI_ICHECK(a.dtype().is_int() || a.dtype().is_uint()) << a;` guard and have no float branch — `truncdiv` (L550), `floordiv` (L573), `ceildiv` (L590), `floormod` (L598). Nothing between the Python operator/`T.*` overload and this guard rejects or handles float operands, so a float operand reaches the `ICHECK` and aborts. The fragile idiom is the guard itself, shared verbatim across these four functions.\n- **Operator-level root:** the integer floor/ceil/trunc division-and-modulo family. Their float-native counterparts — `truncmod` (op.cc:556, *no* guard), `fmod` (own float lowering), and `div`/`/` — do not carry the guard and compile.\n\n*4-axis sweep* (input `5.3, 3.0` float32 unless noted; `RESULT` = observed):\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| similar-logic | `T.truncdiv(A[i],B[i])` float32 (same guard idiom, op.cc:550) | `InternalError: is_int()||is_uint() is false: A[i]` | **same-root** |\n| related-source | `T.ceildiv(A[i],B[i])` float32 (same-file sibling guard, op.cc:590; also used internally in eager negstep-range builder.py:462–470) | `InternalError: is_int()||is_uint() is false: A[i]` | **same-root** |\n| related-operator | `A[i] // B[i]` float32 (`__floordiv__`→`generic.floordiv`→op.cc:573) | `InternalError: is_int()||is_uint() is false: A[i]` | **same-root** |\n| related-operator | `T.floormod(A[i],B[i])` float32 (named-op spelling, op.cc:598) | `InternalError: is_int()||is_uint() is false: A[i]` | **same-root** |\n| related-operator (boundary) | `T.truncmod(A[i],B[i])` float32 (op.cc:556, **no** guard) | `NO ERROR`, val0 = `2.3000` | distinct — no guard, compiles |\n| related-operator (boundary) | `T.fmod(A[i],B[i])` float32 (own float path) | `NO ERROR`, val0 = `2.3000` (C `fmod`, truncated) | distinct — correct |\n| related-type | `A[i] % B[i]` **float16** | `InternalError: is_int()||is_uint() is false: A[i]` | **same-root** |\n| related-type | `A[i] // B[i]` **bfloat16** | `InternalError: is_int()||is_uint() is false: A[i]` | **same-root** |\n| related-type (control) | `A[i] % B[i]` **int32** | `NO ERROR`, matches `torch (arange % 7)` | boundary — int path is correct |\n\n*Reframe.* The class is **float operand to the integer-only div/mod ops `floormod`/`floordiv`/`ceildiv`/`truncdiv` (op.cc), reachable via `%`, `//`, `T.floormod`, `T.floordiv`, `T.ceildiv`, `T.truncdiv`** — all four share one guard and abort identically for every float width (fp32/fp16/bf16). The boundary is sharp: integer operands compile and compute; and the guard-less float siblings `T.truncmod`, `T.fmod`, and float `/` compile. Note one internal inconsistency surfaced by the sweep (recorded, not separately filed): `T.truncdiv` aborts on float while its own modulo partner `T.truncmod` compiles — the guard was added to three of the four family members but omitted from `truncmod`.\n\n**Example-run (PART 1, verbatim on 0.1.13).** The shipped test `testing/python/transform/test_tilelang_transform_simplify.py::test_conditional_floor_mod` uses `T.floormod(0 - i, 2)` and `T.floormod(i, 2)` where `i: T.int32` — run verbatim: **`1 passed`**. It is green precisely because it dodges the bug: both `T.floormod` operands are `int32` scalars, never float. Every `T.floordiv`/`T.floormod` use grep'd across `examples/` (e.g. `example_tilelang_sparse_gqa_decode_paged.py`: `T.floormod(num_blocks, num_split)`; the flash/attention bwd examples: `T.floordiv(by * block_M, block_N)`) is likewise on integer block/thread-index scalars. No shipped example or test applies `%`/`//`/`T.floormod`/`T.floordiv`/`T.ceildiv` to a float tensor value, which is why the suite is green and the bug is untested.\n\n**Dedup.** I searched the open and closed tracker (`floormod`/`floordiv`/`mod float`/`is_int`) and found no existing report of `%`/`//` on floats crashing; the nearest float-related issue (#859) is an unrelated fp32-GEMM problem. (Sibling-in-spirit but distinct root: the integer-`if`-condition abort is the *bool*-only op-assert analogue of this *int*-only op-assert; different operators, asserts, and fix sites, so filed separately.)\n\n**Reach.** The trigger is any of `%`, `//`, `T.floormod`, `T.floordiv`, `T.ceildiv`, `T.truncdiv` applied to float tensor elements: these are standard arithmetic idioms and float modulo/floor-division is well-defined in Python, NumPy, and `torch`, so a user expressing it on float data writes exactly this. Confirmed by running the shipped tests (see Example-run above): `grep`ing `examples/` and `testing/` finds every `T.floordiv`/`T.floormod`/`T.ceildiv` use is on integer block/thread-index scalars, not float tensor values — the shipped `test_conditional_floor_mod` passes verbatim on 0.1.13 precisely because its `T.floormod` operands are `int32`. No shipped test exercises any of these ops on float.\n\n**Impact.** The trigger is a float operand to any of `%`/`//`/`T.floormod`/`T.floordiv`/`T.ceildiv`/`T.truncdiv` (integer operands compile and compute correctly). When it fires it is a compile-time `LogFatal` during host-side IR construction: loud, deterministic, and caught immediately, so it corrupts no output and cannot reach a running workload silently — the only cost is that this valid float expression fails to build with an internal assert instead of a clear frontend error. Fixing it either closes that dtype class (float div/mod compiles) or converts the internal abort into a located diagnostic.", "url": "https://github.com/tile-ai/tilelang/issues/3008"}
{"number": 3006, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.q_multiply_shift_per_axis` aborts with an internal `is_bool()` check when `is_lshift_required` is a plain integer (its documented type)", "state": "open", "created_at": "2026-08-16T07:20:35Z", "updated_at": "2026-08-16T07:20:35Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported. (+1 or comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nLinux / NVIDIA L40S (sm_89), CUDA 12.8, PyTorch 2.8.0. The failure is in TIR legalization (target-independent), before any codegen.\n\n### Problem description\n\n`T.q_multiply_shift_per_axis(x, y, ls, rs, q, is_lshift_required, is_rshift_required)` aborts compilation with an internal error when `is_lshift_required` is passed as an ordinary integer (`0`/`1`), which is the type its own docstring documents (`is_lshift_required : IntImm`):\n\n```\nInternalError: Check failed: (condition.dtype().is_bool()) is false:\n```\n\nThe kernel never compiles. The scalar sibling `T.q_multiply_shift` (same underlying `QMultiplyShift` helper) compiles and runs correctly on the same inputs, and the per-axis op itself compiles and computes the right values if the flag is passed as `bool` — so the operation is fully supported; only the documented integer-flag form is rejected by an internal assert.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport numpy as np, tilelang, tilelang.language as T, torch\n\nN = 64; Q, LS, RS = 15, 0, 7\n\n# is_lshift_required / is_rshift_required as plain integers — the documented `IntImm` form.\n@T.prim_func\ndef fail(X: T.Tensor((N,), \"int32\"), Y: T.Tensor((N,), \"int32\"), O: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=N):\n        t = T.get_thread_binding()\n        O[t] = T.q_multiply_shift_per_axis(X[t], Y[t], LS, RS, Q, 0, 1)  # int flags\n\n# CONTROL: identical call, flags as bool -> compiles and is numerically correct.\n@T.prim_func\ndef ok(X: T.Tensor((N,), \"int32\"), Y: T.Tensor((N,), \"int32\"), O: T.Tensor((N,), \"int32\")):\n    with T.Kernel(1, threads=N):\n        t = T.get_thread_binding()\n        O[t] = T.q_multiply_shift_per_axis(X[t], Y[t], LS, RS, Q, T.bool(False), T.bool(True))\n\nx = np.random.randint(-1000, 1000, N).astype(np.int32)\ny = np.random.randint(1, 1 << 20, N).astype(np.int32)\ntot = RS + Q\nref = ((x.astype(np.int64) * y.astype(np.int64) + (1 << (tot - 1))) >> tot).astype(np.int32)\n\ntry:\n    tilelang.compile(fail, out_idx=[2])\n    print(\"int flag: compiled\")\nexcept Exception as e:\n    print(\"int flag:\", type(e).__name__, str(e).splitlines()[0])   # -> Check failed: is_bool()\n\nk = tilelang.compile(ok, out_idx=[2])\no = k(torch.tensor(x, device=\"cuda\"), torch.tensor(y, device=\"cuda\")).cpu().numpy()\nprint(\"bool flag maxerr:\", int(np.max(np.abs(o.astype(np.int64) - ref))))  # -> 0\n```\n\n### Traceback\n\n```\nInternalError: Check failed: (condition.dtype().is_bool()) is false:\n```\n\n<details><summary>Full abort line</summary>\n\n```\n[.. ] : Fatal: InternalError: Check failed: (condition.dtype().is_bool()) is false:\n```\n\n</details>\n\n### Expected behavior\n\nCompile and compute the requantize result, as the `bool`-flag form and the scalar `T.q_multiply_shift` already do. An integer `is_lshift_required` (the type in the op's own docstring) should be treated as the boolean it denotes, not rejected by an internal assert.\n\n### Additional context\n\n**Root cause.** The per-axis legalization forwards the caller's `is_lshift_required` argument unchanged into a `Select` whose condition must be boolean, whereas the scalar path constructs a genuine boolean before doing so. In [`tirx.q_multiply_shift_per_axis`'s `FLegalize`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L312), the flag is read as `call->args[5]` and [handed straight to `QMultiplyShift`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L322-L326), which uses it as [`Select(is_left_shift_required, x << left_shift, x)`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L240). `Select` asserts its condition is `bool`, so an `int32` argument trips the internal check. The scalar `q_multiply_shift` never hits this because it derives the flag as a comparison — [`(left_shift != zero)`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L306) — which is already boolean.\n\n**Suggested fix.** Coerce the flag to boolean in the per-axis `FLegalize` before calling `QMultiplyShift` (e.g. compare against a zero constant, `is_lshift_required != 0`, matching the scalar path), or have `QMultiplyShift` cast it. The `bool`-flag form is verified numerically exact (maxerr 0); the coercion itself is not verified end-to-end.\n\n**Provenance.** The raw pass-through derives from upstream `apache/tvm`, whose `intrin_rule.cc` has the same `is_lshift_required = call->args[5]` → `QMultiplyShift` path, so the defect is not TileLang-introduced. Present in the TileLang TVM fork (`3rdparty/tvm` pinned at `8df8ebd6` for 0.1.13); exact introducing upstream commit not verified. Not a recent regression.\n\n**Dedup.** I searched the open and closed tracker (`q_multiply_shift`, `q_multiply_shift_per_axis`, `is_lshift_required`) and found no existing report of this op.\n\n**Boundary & generalization (tested this session, sm_89 / 0.1.13).** This is a **missing-coercion** bug.\n\n*Two-level root.*\n- **Source-level:** `QMultiplyShift` ([`intrin_rule.cc:240`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L240)) forwards its `is_left_shift_required` parameter straight into `tirx::Select(cond, ...)`, and `Select` asserts `cond.dtype().is_bool()`. The per-axis `FLegalize` ([L322](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L322)) hands it `call->args[5]` verbatim; the scalar `FLegalize` derives a genuine bool (`left_shift != zero`, [L306](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L306)) so its callers can never reach the assert with a non-bool. It is the **only** one of the four `tirx::Select` sites in the file (L176 tanh, L240, L304, L305) whose condition is a forwarded caller argument rather than a self-derived comparison.\n- **Operator-level:** the two ops routing through `QMultiplyShift` — scalar `tirx.q_multiply_shift` and `tirx.q_multiply_shift_per_axis`. Only the per-axis op exposes a user-supplied flag argument; the scalar op has no flag argument at all (4 args), so it self-derives the bool and is immune.\n\n*4-axis sweep (each cell run, result observed):*\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| related-type (flag dtype, arg 5) | `int8` / `int16` / `int32` / `int64` / `uint32` / `float32` flag | all ICE `is_bool()` | yes — dtype-of-condition, width/signedness/float all irrelevant |\n| related-type (flag value, arg 5) | int `0` vs int `1` | both ICE `is_bool()` | yes — value irrelevant |\n| related-type (arg 6, `is_rshift_required`) | int `int8`/`int32` flag, arg 5 bool | compiles | n/a — arg 6 is dead in per-axis `FLegalize` (dropped per the `// 7th argument … we don't need that here` comment), never reaches a `Select` |\n| related-type (bool control) | `bool` flag arg 5 | compiles, maxerr 0 | — (the intended form) |\n| related-operator | scalar `tirx.q_multiply_shift`, const multiplier, `s=3` | compiles | distinct — self-derives bool flag (`left_shift != zero`), no user flag surface → immune |\n| related-operator | scalar `tirx.q_multiply_shift`, **runtime** multiplier `Y[t]` | ICE `broadcast_node != nullptr` (**distinct bug**, see below) | distinct root |\n| similar-logic | other `tirx::Select` sites (L176 tanh, L304/L305 scalar shifts) | not run — condition is a self-derived comparison, not a forwarded arg, so structurally cannot expose the bug | distinct (safe by construction) |\n| related-source | per-axis `FLegalize` is the sole raw-forwarding path; no sibling pass forwards a user arg into `Select` in this file | — | boundary is this one line |\n\nThe class is **narrow and fully bounded**: exactly one Select condition in `intrin_rule.cc` is a forwarded caller argument, and exactly one op (`q_multiply_shift_per_axis`) surfaces it. Any non-bool dtype for arg 5 trips it; nothing else in the file shares the fragile shape. Kept **specific** (title unchanged).\n\n**Distinct adjacent bugs (same file/op family, NOT filed here, separate roots).** (1) the shared `get_int_value` helper asserts a compile-time-constant multiplier — a **runtime** `y` (e.g. `Y[t]`) trips `broadcast_node != nullptr` on both the scalar and per-axis paths (reproduced this session: scalar with `Y[t]` → `Check failed: (broadcast_node != nullptr)`); (2) the power-of-2 fast path off-by-one at `s == 1` (`1 << -1`). Same file, different lines and root causes — this report covers only the `is_bool()` coercion defect.\n\n**Reach.** The trigger is the documented flag type: both the TIR op docstring (`is_lshift_required : IntImm`) and TVM's own `topi.math` requantize helper docstring (`is_lshift_required : int`) type the flag as an integer. The only in-tree caller, `topi.math`'s [`fixed_point_multiply_per_axis`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/topi/math.py#L686) (whose own signature and docstring type the flag `is_lshift_required : int`, [L691/L708](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/topi/math.py#L708)), dodges the crash by [wrapping the flag in `tvm.tirx.const(is_lshift_required, \"bool\")`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/python/tvm/topi/math.py#L735) itself before the call — so any user who reads the op's documented `int` signature and passes `0`/`1` directly hits the assert, while the one shipped caller happens to pre-cast. Verified this session: `fixed_point_multiply_per_axis(x,y,lsh,rsh,1,1,axes=(0,))` (int flags, as its signature types them) builds the compute without error, because it internally pre-casts. No TileLang `examples/` or `testing/` exercises the op directly (`git grep q_multiply_shift` over `v0.1.13` examples+testing: 0 sites; the only in-repo references are the TileLang language-binding wrappers in `tilelang/language/**/ir.py` + `op.py` and the two `topi/math.py` definitions — none passes an int flag to the per-axis op), which is why CI is green.\n\n**Impact.** Narrow trigger: only the per-axis op called with an integer `is_lshift_required` (the documented `IntImm`/`int` form) rather than a pre-cast `bool`. When it fires it is a loud compile-time abort (`is_bool()` internal check) — the kernel fails to build, caught immediately, and nothing is silently miscomputed or reaches runtime with a wrong value. Fixing it closes the gap between the op's documented integer signature and what legalization accepts, letting a kernel written to the docstring compile instead of being refused.", "url": "https://github.com/tile-ai/tilelang/issues/3006"}
{"number": 3005, "title": "[BUG][Fuzzer][ice-on-invalid-code] Reading a `local.fragment` at two different indices in `T.Parallel` aborts with an internal `StructuralEqual` `ICHECK` instead of a clear \"unsupported\" diagnostic", "state": "open", "created_at": "2026-08-16T07:20:35Z", "updated_at": "2026-08-16T07:20:35Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and found no similar report.\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nTileLang 0.1.13; CUDA 12.8; NVIDIA L40S (sm_89), single GPU.\n\n### Problem description\n\nA `T.Parallel` loop body that reads the same `local.fragment` buffer at two different\nindices — e.g. a neighbor/stencil access `fa[i] + fa[(i + 1) % N]` — aborts compilation with\nan internal assert:\n\n```\nInternalError: Check failed: (StructuralEqual()(it->second.indices, indices)) is false:\nbuf: ((i + 1) % 128,) and (i,)\n```\n\nThe abort fires in `LayoutInference`, before any code is generated. The identical kernel over a\n`shared` buffer instead of a fragment compiles and returns the correct result (control below), so\nthe pattern itself is not rejected — only the fragment path aborts, and it does so via an internal\n`ICHECK` rather than either compiling it or emitting a clean diagnostic.\n\nA single non-identity index on the fragment (`fa[(i + 1) % N]` with no second index) does *not*\ntrip this assert — it compiles but silently drops the offset and reads the thread's own local\nelement (a separate silent-miscompile face; see Additional context). The crash here is specific to\n**two or more structurally-different index expressions on the same fragment** in one parallel body.\n\nIntroduced by #1713 — see Provenance.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch, numpy as np\n\nN = 128\n\ndef build(scope):  # scope: \"fragment\" (crashes) or \"shared\" (correct control)\n    @T.prim_func\n    def main(A: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n        with T.Kernel(1, threads=128):\n            buf = T.alloc_fragment((N,), \"int32\") if scope == \"fragment\" \\\n                  else T.alloc_shared((N,), \"int32\")\n            T.copy(A, buf)\n            for i in T.Parallel(N):\n                B[i] = buf[i] + buf[(i + 1) % N]   # same buffer, two different indices\n    return main\n\na   = torch.arange(N, dtype=torch.int32, device=\"cuda\")\nexp = a.cpu().numpy() + a.cpu().numpy()[(np.arange(N) + 1) % N]\n\nfor scope in (\"fragment\", \"shared\"):\n    try:\n        out = tilelang.compile(build(scope), out_idx=[1])(a).cpu().numpy()\n        print(scope, \"->\", \"PASS\" if np.array_equal(out, exp) else f\"FAIL ({(out!=exp).sum()}/{N})\")\n    except Exception as e:\n        print(scope, \"-> CRASH:\", str(e).splitlines()[-1][:80])\n\n# fragment -> CRASH: Check failed: (StructuralEqual()(it->second.indices, indices)) ...\n# shared   -> PASS\n```\n\n### Traceback\n\n```\nFile \".../tilelang/cuda/pipeline.py\", line 148, in CUDAPassPipelineBody\n    mod = tilelang.transform.LayoutInference()(mod)\n  ...\n  in tvm::tl::LayoutInferencer::Substitute(tvm::tirx::PrimFunc)\n  in tvm::tl::ParallelOpNode::ParallelOpNode(tvm::tirx::For)\n  File \"/project/src/op/parallel.cc\", line 255, in tvm::tl::ParallelOpNode::RecordBufferAccess(...)\ntvm.error.InternalError: Check failed: (StructuralEqual()(it->second.indices, indices)) is false:\nbuf: ((i + 1) % 128,) and (i,)\n```\n\n### Expected behavior\n\nReading a `local.fragment` at two distinct indices inside one `T.Parallel` body is genuinely\nunsupported on this path: a fragment is register-distributed across threads, so element `i+1` may\nlive in a different thread's registers than element `i`, and resolving `fa[i] + fa[i+1]` requires a\ncross-thread exchange (shuffle) that the `T.Parallel` fragment lowering does not emit. (The `shared`\ncontrol compiles and returns the correct `l + r` only because a shared buffer is visible to all\nthreads and needs no exchange.) So the right outcome is a **clean, user-facing rejection** — a clear\nmessage that a fragment accessed at multiple distinct indices inside `T.Parallel` is not supported\n(and to use a `shared` buffer for cross-lane stencils) — not an internal `StructuralEqual` `ICHECK`\nthat points at compiler internals. TileLang already raises clear errors for other unsupported\nfragment access patterns, so this would match that precedent. (If the maintainers instead intend to\nsupport cross-lane fragment stencils, that is a larger feature — emitting the missing shuffle — but\nthe immediate defect is the internal-assert failure mode.)\n\n### Additional context\n\n**Root cause.** `ParallelOp`'s fragment layout inference assumes each fragment is accessed at a\nsingle index expression across the whole parallel body: it records the first access's index and\nasserts every later access to that fragment is structurally equal, so a second, different index on\nthe same buffer is treated as impossible rather than handled.\n\n<details><summary>Where</summary>\n\nThe parallel-loop visitor calls\n[`RecordBufferAccess`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L250-L269)\non every fragment load/store. That routine keeps one `BufferAccessInfo` per fragment; the first\ncall stores the index array, and every subsequent call runs\n[`ICHECK(StructuralEqual()(it->second.indices, indices))`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L255).\n`buf[i]` records `(i,)`; the following `buf[(i + 1) % N]` then fails the check with\n`((i + 1) % 128,)` vs `(i,)`, aborting the pass. Because the map is keyed only on the buffer, any two\nstructurally-distinct index expressions on the same fragment collide, regardless of the offset. The\n`shared` path is not recorded here and takes ordinary indexed lowering, so it compiles and is\ncorrect.\n\n</details>\n\n**Suggested fix.** `RecordBufferAccess` (`parallel.cc`) should not treat a second, structurally-different index on the same fragment as impossible: either support multiple distinct index expressions per fragment in the layout inference (so a stencil lowers to the required cross-thread exchange), or, if that pattern is unsupported on the fragment path, replace the `ICHECK` with a clear user-facing diagnostic — matching the clean errors TileLang already raises for other unsupported fragment access patterns. The first option is a substantial layout-inference change; the diagnostic option is small.\n\n**Provenance.** `RecordBufferAccess` and its `StructuralEqual` assert were introduced by\n[#1713 \"[Layout] Fix Layout Bugs in Parallel and Reduce\"](https://github.com/tile-ai/tilelang/pull/1713)\n(merged 2026-01-28; `git log -S` on the assert text in `src/op/parallel.cc` returns only that\ncommit). The permalinks above pin v0.1.13 (`8001cc4`).\n\n**Related.** The same fragment-layout-inference limitation shows a second, silent face: a *single*\nnon-identity index such as `fa[(i + 1) % N]` compiles but drops the offset and reads the local\nelement, returning wrong data instead of crashing (it never reaches the two-index assert). That\nsilent-miscompile face is distinct from this crash and is being reported separately.\n\n**Guard boundary tested (this session, 0.1.13, L40S).** The abort is a **tight structural-equality guard**: it keys on whether two index expressions are structurally *distinct*, not on the number of accesses. Three neighboring inputs, each a separate compile:\n\n| fragment access in `T.Parallel` body | result |\n|---|---|\n| `buf[i]` (single, identity) | compiles, correct |\n| `buf[i] + buf[i]` (two accesses, structurally *equal* indices) | compiles, correct — `StructuralEqual` passes, guard not tripped |\n| `buf[(i + 1) % N]` (single, non-identity) | compiles but returns wrong data (out `[0,1,2]` vs expected `[1,2,3]` — offset dropped, reads local element; the silent face above) |\n| `buf[i] + buf[(i + 1) % N]` (two, structurally *different* indices) | **`StructuralEqual` `ICHECK` abort** (this bug) |\n\nSo the guard fires exactly on two-or-more structurally-different index expressions on one fragment; equal repeated indices pass, and a lone non-identity index escapes into the silent-miscompile face. The crash face and the silent face share the one root (single-index-per-fragment assumption in `RecordBufferAccess`); this report is the crash face and keeps its specific trigger.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this `StructuralEqual` abort.\n\n**Reach.** The triggering ingredient is reading (or writing) one `local.fragment` at two\nstructurally-different indices inside a single `T.Parallel` body — the shape of any stencil /\nneighbor access (`fa[i-1] + fa[i] + fa[i+1]`, `fa[i] + fa[(i+1)%N]`); this is a common kernel idiom.\nIt is not documented as unsupported: `alloc_fragment` and `T.Parallel` docstrings describe fragments\nand element-wise parallel loops without disclaiming multi-offset fragment reads.\n\nGrepping `examples/` and `testing/` this session (at 0.1.13): no shipped example reads a\n`local.fragment` at two distinct offsets inside one `T.Parallel` body. The shipped\n`examples/convolution/example_convolution.py` (verified this session) stages the input through\n`data_shared`/`kernel_shared` and only ever touches its `out_local` fragment at the single GEMM\nindex — exactly the passing `shared`-staging control — so it does not exercise this path and CI is\ngreen. TileLang *does* ship a related diagnostic: `tilelang/analysis/fragment_loop_checker.py`'s\n`FragmentLoopChecker` raises a clean `ValueError` when a fragment is indexed by the loop var of a\n**symbolic-extent** `T.Parallel` loop, and its test\n`testing/python/analysis/test_tilelang_fragment_loop_checker.py` passes verbatim on 0.1.13\n(2 passed; the `pytest.raises(ValueError)` cases fire from `fragment_loop_checker.py:90`, not from\n`parallel.cc`). That checker is scoped to symbolic-extent loops and cross-loop layout mismatch; the\ntwo-distinct-*static*-indices-in-one-body case tested here is outside its scope and falls straight\ninto the raw `ICHECK` — so the precedent for a clean diagnostic exists but has a gap exactly where\nthis crash lives. Any fragment stencil written directly (without a shared staging buffer) trips it,\nindependent of `threads`, tile size, dtype, rank, read-vs-write, or the specific offset (§17).\n\n**Impact.** The trigger is narrow — two structurally-different index expressions on one\n`local.fragment` inside a single `T.Parallel` body — but it is a common stencil idiom, and when it\nfires it aborts at compile time through an internal `ICHECK`, deterministically, before any code is\ngenerated: nothing miscompiles and no wrong value can reach a running workload, but that one kernel\ncannot build (or is refused via an internal assert rather than a clear message). Fixing it either\ncloses this fragment-stencil class on the layout-inference path or replaces the internal abort with\na user-facing diagnostic.\n\n**Generalization (tested this session, 0.1.13, L40S sm_89).**\n\n_Two-level root._\n- **SOURCE-level root:** `ParallelOpNode::RecordBufferAccess` at\n  [`src/op/parallel.cc:250-269`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L250-L269)\n  keeps one `BufferAccessInfo` per fragment keyed on the `Buffer` alone and asserts\n  `ICHECK(StructuralEqual()(it->second.indices, indices))` on every subsequent access\n  ([L255](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L255)).\n  The single-index-per-fragment assumption is baked into the whole `ParallelOpNode` layout path:\n  `GetAccessInfo`, `CompleteBufferFragment`, and `IsBufferCompletelyReplicated` all read back the\n  one recorded `indices`. It is called from *both* the load visitor\n  ([L156-158](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L156-L158),\n  `is_write=false`) and the store visitor\n  ([L149-151](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/op/parallel.cc#L149-L151),\n  `is_write=true`), so the guard is symmetric across reads and writes.\n- **OPERATOR-level root:** any construct whose body is lowered as a `ParallelOp` and touches a\n  `local.fragment` at more than one static index — element-wise `T.Parallel`, in either the\n  read or write direction, at any dtype/rank. The `shared`/`global` scope is never recorded here\n  (`IsFragmentBuffer` gate) and takes ordinary indexed lowering, so it is immune.\n\n_4-axis sweep_ (each cell a fresh process; observed, not asserted):\n\n| axis | cell tested | input | observed result | same-root? |\n|---|---|---|---|---|\n| baseline | `buf[i] + buf[(i+1)%N]` read (int32) | draft repro | `ICHECK` abort `((i+1)%128,) and (i,)` at parallel.cc:255 | — |\n| related-operator | two distinct **write** indices `buf[i]=…; buf[(i+1)%N]=…` | int32 | same `ICHECK` abort | yes |\n| related-source | mixed **read+write** distinct indices (`B[i]=buf[(i+1)%N]; buf[i]=A[i]`) | int32 | same `ICHECK` abort `(i,) and ((i+1)%128,)` | yes |\n| related-operator | negative-offset neighbor `buf[i] - buf[(i-1+N)%N]` | int32 | same `ICHECK` abort `((i+127)%128,) and (i,)` | yes |\n| related-type (dtype) | `buf[i]+buf[(i+1)%N]` in **fp16** | fp16 | same `ICHECK` abort | yes |\n| related-type (dtype) | **bool** fragment `buf[i] & buf[(i+1)%N]` | bool | same `ICHECK` abort | yes |\n| related-type (rank) | **2D** fragment `buf[i,j]+buf[i,(j+1)%N]` | int32 2D | same `ICHECK` abort `(i,(j+1)%32) and (i,j)` | yes |\n| related-type (scope) | same idiom over **`shared`**, fp16 | fp16 | compiles, **correct value** (`l+r`) | distinct (not recorded here) |\n| similar-logic (cross-loop) | two distinct indices in **separate** `T.Parallel` bodies (static extent) | int32 | compiles, no crash — each `ParallelOp` sees one index | boundary |\n| similar-logic (checker) | symbolic-extent loop indexing fragment (shipped test case) | dynamic `A` | clean `ValueError` from `fragment_loop_checker.py:90` (2 passed) | distinct (separate checker) |\n| guard boundary | single offset `buf[(i+1)%N]` | int32 | compiles, **silently wrong** (`out[:3]=[0,1,2]` vs `[1,2,3]`) | same root, silent face |\n| guard boundary | `buf[i]+buf[i]` (structurally equal) | int32 | compiles, correct — `StructuralEqual` passes | same root, guard not tripped |\n\n_Findings._\n- The bug is a *class*, not a one-off: read/write direction, offset sign, dtype (int32/fp16/bool),\n  and rank (1D/2D) all reduce to the identical `RecordBufferAccess` assert. The trigger is precisely\n  \"≥2 structurally-distinct index expressions on one fragment inside one `ParallelOp`\". Title and\n  Problem already describe the class; kept the concrete stencil repro as the representative.\n- **Boundary (kept specific):** the crash needs the two distinct indices in the *same* `T.Parallel`\n  body. Splitting them across separate parallel loops does not trip parallel.cc (each `ParallelOp`\n  has its own `indice_map_`); a symbolic-extent loop is instead caught earlier by the clean\n  `FragmentLoopChecker` `ValueError`. So this ICHECK is the gap between \"static, one body,\n  multi-index\" and the two paths that are handled cleanly.\n- **Silent sibling (distinct face, not filed here):** a *single* non-identity index compiles and\n  returns wrong data (offset dropped, reads the thread-local element) — reproduced this session\n  (`out=[0,1,2]`, expected `[1,2,3]`). Shares the single-index assumption but is a silent-miscompile\n  face, reported separately.\n- **No NEW distinct bug** surfaced while sweeping. The `shared`-scope and cross-loop cases are\n  correct-by-design (immune), not defects; the `ValueError` path is the existing intended diagnostic.\n\n_Shipped-example run._ The claimed shipped example precedent was verified by running, not inherited:\n`test_tilelang_fragment_loop_checker.py` runs `2 passed` on 0.1.13, and its `ValueError` fires from\n`fragment_loop_checker.py:90` (symbolic-extent path), never reaching parallel.cc — confirming the\nclean-diagnostic precedent exists but does not cover this crash's static-multi-index trigger. The\n`example_convolution.py` fragment (`out_local`) is touched only at its single GEMM index and stages\ninputs through `shared`, so it dodges the bug — matching the passing control.", "url": "https://github.com/tile-ai/tilelang/issues/3005"}
{"number": 3003, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.q_multiply_shift` with `s=1` and a power-of-2 multiplier aborts with an internal shift-range ICHECK instead of computing the result", "state": "open", "created_at": "2026-08-16T07:20:34Z", "updated_at": "2026-08-16T07:20:34Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (latest release)\n\n### System information\n\nLinux, NVIDIA L40S (sm_89), CUDA 12.8, PyTorch 2.8.0. The abort happens during host-side `LowerIntrin` (before any GPU code is generated), so it is target-independent — no specific GPU is needed to reproduce.\n\n### Problem description\n\n`T.q_multiply_shift(x, y, q, s)` aborts the compiler with an internal shift-range check when the shift `s == 1` and the multiplier `y` is exactly `1 << 30`:\n\n```\nInternalError: Check failed: (pb->value >= 0 && pb->value < rtype.bits()) is false:\nShift amount must be non-negative and less than 32 for type int32\n```\n\n`s = 1` is a documented, ordinary shift value: the op's docstring types `s` as an unrestricted \"Integer shift\" (only `q > 0` is required), and the same call with any other multiplier of the same magnitude compiles and runs. The failure is specific to the power-of-2 multiplier value `1 << 30` combined with `s == 1`; nearby shifts (`s = 0, 2, 3, -1`) all compile and return correct results.\n\nNot a regression — see Provenance.\n\n### Reproducible example code\n\n```python\nimport torch, tilelang, tilelang.language as T\n\nN, Q = 128, 31\nPOW2 = 1 << 30      # power-of-2 multiplier (represents 0.5)\n\ndef build(y, s):\n    @T.prim_func\n    def qm(A: T.Tensor((N,), \"int32\"), O: T.Tensor((N,), \"int32\")):\n        with T.Kernel(1, threads=N) as bx:\n            t = T.get_thread_binding()\n            O[t] = T.q_multiply_shift(A[t], y, Q, s)   # out = round(x*y*2^-s)\n    return qm\n\na = torch.randint(-(1 << 20), 1 << 20, (N,), dtype=torch.int32, device=\"cuda\")\n\n# CONTROL: same s=1, a non-power-of-2 multiplier -> compiles and computes correctly\ntilelang.compile(build((1 << 30) + 1, 1), out_idx=[1])(a)   # -> OK\n\n# TRIGGER: s=1 with the power-of-2 multiplier -> aborts at legalize time\ntilelang.compile(build(POW2, 1), out_idx=[1])(a)            # -> InternalError (below)\n```\n\n### Traceback\n\n```\ntvm::left_shift(tvm::PrimExpr, tvm::PrimExpr, tvm::Span)\n...\nInternalError: Check failed: (pb->value >= 0 && pb->value < rtype.bits()) is false:\nShift amount must be non-negative and less than 32 for type int32\n```\n\n(raised from `tilelang.transform.LowerIntrin()` while legalizing the `tir.q_multiply_shift` call.)\n\n### Expected behavior\n\nCompile and return the same value the general (non-power-of-2) path already returns for the same `s`. `s = 1` is a legal shift and the result is computable — the control above, and the same op at `s ∈ {-1, 0, 2, 3}`, all produce correct output; only the `1 << 30, s = 1` combination aborts.\n\n### Additional context\n\n**Root cause.** The `q_multiply_shift` legalization rule mishandles the boundary case `s == 1` in its power-of-2 fast path: it selects a rounding-and-right-shift branch that assumes the shift amount is at least 1, but at `s == 1` that amount is 0, so it emits a left shift by `-1` that the TIR shift builder rejects.\n\n<details>\n<summary>Mechanism (the off-by-one)</summary>\n\nIn the `tirx.q_multiply_shift` `FLegalize` rule ([intrin_rule.cc L283-L297](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L283-L297)), the multiplier `y == 1 << 30` takes the power-of-2 special case:\n\n```cpp\nPrimExpr exp = s - 1;\nint exp_val = get_int_value(s) - 1;\nif (exp_val > 0) {\n  // power of 2 is greater than 0, apply left shift.\n  return x << exp;                              // s >= 2\n} else {\n  // power of 2 is less than 0, round and then apply right shift.\n  PrimExpr one = make_const(lp_dtype, 1);\n  exp = -exp;                                   // s == 1  ->  exp == 0\n  PrimExpr rounding_factor = one << (exp - 1);  // 1 << -1  ->  ICHECK abort\n  PrimExpr rounded_t = x + rounding_factor;\n  return rounded_t >> exp;\n}\n```\n\nThe branch guard is `exp_val > 0` (i.e. `s >= 2`), so the else-branch is entered for every `s <= 1`. Its comment says \"power of 2 is less than 0\", but that only holds for `s < 1`; at the boundary `s == 1` the exponent `exp` is exactly `0`, and `one << (exp - 1)` is `1 << -1`. The [`<<` shift builder](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/target/intrin_rule.cc#L294) const-folds that and its shift-range ICHECK aborts (`Shift amount must be non-negative and less than 32 for type int32`). For `s == 1` the mathematically correct result is `x` (0.5 · 2¹ · x = x), i.e. `x >> 0` with no rounding term; the `exp == 0` case needs no rounding factor.\n</details>\n\n<details>\n<summary>Trigger boundary (which s trip vs dodge, and the correct-sibling control)</summary>\n\n`y = 1<<30, Q = 31`, output compared against the general (non-power-of-2) semantics computed in int64 (`ls=max(s,0)`, `rs=max(-s,0)`, `((x<<ls)*y + 2^(rs+q-1)) >> (rs+q)`), all cells run this session on sm_89 / 0.1.13:\n\n| `s` | branch | result |\n|---|---|---|\n| 100 | `x << exp` (exp=99) | **ICE (left shift ≥ 32)** — distinct boundary, see §Generalization |\n| 33 | `x << exp` (exp=32) | **ICE (left shift ≥ 32)** — distinct boundary |\n| 32 | `x << exp` (exp=31) | compiles, maxerr 0 |\n| 3 | `x << exp` | compiles, maxerr 0 |\n| 2 | `x << exp` | compiles, maxerr 0 |\n| **1** | else, `exp==0` | **ICE (`1 << -1`)** ← this report |\n| 0 | else, `exp==1` | compiles, maxerr 0 |\n| -1 | else, `exp==2` | compiles, maxerr 0 |\n| -5 | else, `exp==6` | compiles, maxerr 0 |\n\nIn the `else` (round-and-right-shift) branch, only `s == 1` aborts; every other `s ≤ 0` compiles and matches the int64 reference exactly. This is a **tight-guard / off-by-one boundary** bug: the guard `exp_val > 0` is off by one at the single boundary value `s == 1`. (The `s ≥ 33` aborts are a *separate* unclamped-large-left-shift defect in the sibling `x << exp` branch — same source region, distinct root; recorded in §Generalization.) The control in the repro (same `s == 1`, multiplier `(1<<30)+1`) takes the general path and returns the correct value (maxerr 0 vs the int64 reference), which proves `s == 1` is a legal, computable input rather than a harness artifact.\n\nThis is one of three distinct defects in the same `tirx.q_multiply_shift` legalization (`intrin_rule.cc`): (1) this power-of-2 `s == 1` off-by-one; (2) the shared `get_int_value` helper asserts a compile-time-constant multiplier, so a runtime `y` crashes with `broadcast_node != nullptr`; (3) the per-axis op passes an integer `is_lshift_required` straight into a boolean `Select`, tripping an `is_bool()` check. Same file and op family, three separate root causes on different lines — this report covers only (1).\n</details>\n\n**Suggested fix.** In the `q_multiply_shift` power-of-2 branch (`intrin_rule.cc` L283-L297), handle `exp == 0` so it returns `x` directly (no rounding factor, no shift) — equivalently, widen the left-shift guard from `exp_val > 0` to `exp_val >= 0` so `s == 1` takes `x << exp` with `exp == 0`. Both are small, localized changes.\n\n<details>\n<summary>§17 Generalization (two-level root + 4-axis sweep, all cells run this session on sm_89 / 0.1.13)</summary>\n\n**Root, two levels.**\n- **Source-level:** the `tirx.q_multiply_shift` `FLegalize` rule, `intrin_rule.cc` L283–L297. The fragile idiom is an unguarded `one << (exp - 1)` (L294) where `exp` can be `0`, producing a shift by `-1` that the TIR `<<` builder's range ICHECK rejects. More broadly the whole rule emits shift amounts with **no range clamp** — neither the `exp==0` underflow (this report) nor a `> 31` overflow is checked.\n- **Operator-level:** the `QMultiplyShift` op family that all route through `intrin_rule.cc` — `tirx.q_multiply_shift`, `tirx.q_multiply_shift_per_axis`, and the shared static `QMultiplyShift` helper (L224). The helper computes the same `one << (total_right_shift - 1)` rounding factor (L247), so the same underflow class exists there.\n\n**4-axis findings.**\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| similar-logic (guard boundary) | pow2 path, `s = 33` and `s = 100` | `InternalError: Shift amount must be … less than 32` — from the *sibling* `x << exp` branch (L288) with `exp = s-1 ≥ 32`; `s = 32` (exp 31) compiles | **distinct-adjacent** (over-large left-shift, no clamp; L288 not L294). Same source region, different defect — recorded, not filed here |\n| related-source | shared helper `one << (total_right_shift - 1)` (L247) forced to `total_right_shift == 0` via per_axis `rs=0, q=0` | `InternalError: Shift amount must be … less than 64` (int64 here) — same underflow class | **same root**, but only reachable **off-contract** (docstring requires `q > 0`); B029's face is reachable *in-contract* (`s = 1` is documented-legal), which is why B029 is the fileable instance |\n| related-operator | `T.q_multiply_shift_per_axis(x, 1<<30, 0, 0, 31, 1, 0)` (normal int flags) | `InternalError: (condition.dtype().is_bool()) is false` — integer `is_lshift_required` fed into a boolean `Select` | **distinct** root (bool-typed `Select` misuse); this is the draft's noted defect (3). Passing bool-cast flags makes per_axis compile — so it is a separate bug, not this one |\n| related-type / scope (vectorized) | pow2 `s = 1` under `T.vectorized(4)` (lanes > 1) | **compiles (dodges the ICE) but silently returns `0` for all 128 inputs** — reference (int64) is in ±10⁶ range: `o[:3]=[0,0,0]` vs `ref[:3]=[-116505,-620886,-956886]`; the identical kernel at `s = 0` gives maxerr 0 (control) | **SAME root**, **silent face** — see below |\n| related-type (extra shifts) | pow2 `s = -5` | compiles, maxerr 0 | control — confirms only the `exp==0` boundary is affected in the `else` branch |\n\n**New same-root finding — silent-value face under vectorization.** The same `exp==0` off-by-one that *loudly* aborts in scalar form does **not** abort when the `q_multiply_shift(s=1)` expression is vectorized (`T.vectorized`, lanes > 1): the `1 << -1` const-folds to `0` on the vector/broadcast path instead of tripping the scalar ICHECK, so the rounding factor is poisoned and every lane's output collapses to `0`. Verified: the identical vectorized kernel at `s = 0` matches the int64 reference exactly (maxerr 0), while at `s = 1` all 128 outputs are `0` against a reference spanning ±10⁶. This means the L294 off-by-one is not merely a loud compile abort — in vectorized code it becomes a **silent wrong-value**. This strengthens the report's impact and belongs to the same root cause; folded here rather than filed separately.\n\n**Example-run (PART 1).** There is **no shipped example or test to run** for this op: `git grep q_multiply_shift v0.1.13 -- examples testing` returns no match; the identifier exists only in the language-definition files. The docstring in `tilelang/language/tir/op.py` (v0.1.13) was read directly and types `s` as an unrestricted \"Integer shift\" (`q` \"Needs to be > 0\"), confirming `s = 1` is a documented-legal input. Reach is therefore grounded in the public op surface, not in any shipped call site.\n\n</details>\n\n**Provenance.** The QMultiplyShift power-of-2 lowering is vendored TVM code in the TileLang TVM fork (`3rdparty/tvm` pinned at `8df8ebd6` for 0.1.13); the boundary bug is present as long as this rule has shipped. Origin in upstream Apache TVM is unverified.\n\n**Dedup.** I searched the open and closed tracker and found no existing report of this defect.\n\n**Reach.** `T.q_multiply_shift` is a documented, publicly exported op (its docstring — verified in `tilelang/language/tir/op.py` at v0.1.13 — types `s` as an unrestricted \"Integer shift\" and only requires `q > 0`); `s = 1` and a power-of-2 multiplier are ordinary requantize parameters. `git grep q_multiply_shift v0.1.13 -- examples testing` returns **no match** (verified this session): the identifier appears only in the language-definition files (`language/tir/op.py`, `language/tir/ir.py{,i}`, `language/ast/ir.py`), never in a shipped example or test. So no test exercises this legalization path at any `s` — which is why CI is green, and there is no shipped example to run for this op. The trigger is the intersection of the power-of-2 multiplier value (`y == 1<<30`, the only value that enters the fast path) and the single shift `s == 1`; every other `s` on that path compiles (see §Generalization for the exact boundaries).\n\n**Impact.** The trigger is narrow: a power-of-2 multiplier entering the fast path combined with the single boundary shift `s == 1`. In **scalar** form it is a compile-time abort during host-side `LowerIntrin` — loud, caught immediately, blocks that one kernel from building. But the same off-by-one is **not always loud**: when the `q_multiply_shift(s=1)` expression is **vectorized** (lanes > 1), the `1 << -1` const-folds to `0` instead of aborting, and the kernel compiles but silently returns `0` for every element (verified this session against an int64 reference — see §Generalization). So the same root cause has both a loud (scalar) and a silent-wrong-value (vectorized) face. Fixing the `exp == 0` case closes both: a legal `s == 1` requantize input then computes the correct value instead of aborting or producing zeros.", "url": "https://github.com/tile-ai/tilelang/issues/3003"}
{"number": 3002, "title": "[BUG][Fuzzer][ice-on-valid-code] `int32` expression whose constant terms fold past `INT32_MAX` aborts compilation instead of wrapping", "state": "open", "created_at": "2026-08-16T06:20:22Z", "updated_at": "2026-08-16T06:20:22Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13\n\n### System information\n\n- TileLang `0.1.13` (installed wheel; the affected code is the vendored TVM `arith` simplifier, `3rdparty/tvm` pinned at commit `8df8ebd61659505dd7005f4820b030e983e2c005`).\n- Python 3.13, Linux.\n- GPU: NVIDIA L40S (sm_89). The defect is in the host-side IR simplifier and is architecture-independent — no GPU is needed to hit the compile-time crash.\n\n### Problem description\n\nA valid `int32` kernel fails to **compile** when the simplifier reassociates two (or more) compile-time-constant addends whose exact integer sum exceeds `INT32_MAX`. `B[i] = (A[i] + 2000000000) + 2000000000` aborts at `tilelang.compile()` with:\n\n```\nInternalError: Check failed: value < 1LL << (dtype.bits() - 1) (4000000000 vs. 2147483648)\n  : Literal value 4000000000 exceeds maximum of int32\n```\n\nThe two constants (`2_000_000_000 + 2_000_000_000 = 4_000_000_000`) are combined into a single `int32` literal, which the `IntImm` constructor rejects. Under C/CUDA two's-complement `int32` semantics the sum should wrap (`4_000_000_000 mod 2^32 = -294_967_296`), so this is a legal kernel that never gets to run.\n\nThe trigger is narrow: the crash only fires when the constant fold happens **inside a sum that also has a runtime term** (the `+ A[i]`). Two controls bound it (both run below):\n\n- **Runtime addend, no fold → compiles + wraps correctly.** Replacing the second constant with a runtime buffer value (`(A[i] + 2000000000) + C[i]`, `C[i]==2000000000`) never triggers the fold; it compiles and returns the correctly-wrapped `[-294967296, …]`, matching numpy `int32`.\n- **Pure-constant, no runtime term → compiles.** `B[0] = T.int32(1073741824) * T.int32(2)` (product `= 2^31`) with no runtime term folds fine — the plain constant-folding path wraps, so nothing out-of-range is materialized.\n\nSo the same arithmetic compiles on either side of the boundary; only the \"constant fold reached through the summation path\" case crashes. The multiplication form (`T.int32(1073741824) * T.int32(2) + A[0]`, constant factor `= 2^31`) crashes identically (`2147483648 vs 2147483648`), confirming the root is the shared constant-accumulator, not a specific operator.\n\nNo traceback beyond the `InternalError` above — the failure is a compile-time `ICHECK` abort, not a runtime error.\n\n### Reproducible example code\n\n```python\nimport tilelang, tilelang.language as T, torch, numpy as np\n\n# --- Trigger: two int32 constant addends whose sum (4_000_000_000) > INT32_MAX,\n#     plus a runtime term A[i]. Legal int32 arithmetic; should wrap. ---\n@tilelang.jit(out_idx=[1])\ndef make(N):\n    @T.prim_func\n    def main(A: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n        with T.Kernel(1, threads=N) as bx:\n            i = T.get_thread_binding(0)\n            B[i] = (A[i] + T.int32(2000000000)) + T.int32(2000000000)\n    return main\n\na = torch.tensor([0, 1, -1, 100], dtype=torch.int32, device=\"cuda\")\ntry:\n    b = make(4)(a).cpu()\n    print(\"TRIGGER got\", b.tolist())          # expected once fixed: wrapped values\nexcept Exception as e:\n    line = [l for l in str(e).splitlines() if \"exceeds maximum\" in l]\n    print(\"TRIGGER CRASH:\", (line[0] if line else str(e).splitlines()[-1]).strip())\n    # -> Check failed: value < 1LL << (dtype.bits()-1) (4000000000 vs. 2147483648) : Literal value 4000000000 exceeds maximum of int32\n\n# --- Control: same arithmetic, second addend from a runtime buffer -> no fold ->\n#     compiles and wraps correctly (int32 two's-complement). ---\n@tilelang.jit(out_idx=[2])\ndef make_ctl(N):\n    @T.prim_func\n    def main(A: T.Tensor((N,), \"int32\"), C: T.Tensor((N,), \"int32\"), B: T.Tensor((N,), \"int32\")):\n        with T.Kernel(1, threads=N) as bx:\n            i = T.get_thread_binding(0)\n            B[i] = (A[i] + T.int32(2000000000)) + C[i]\n    return main\n\nc = torch.full((4,), 2000000000, dtype=torch.int32, device=\"cuda\")\nb = make_ctl(4)(a, c).cpu()\naa = a.cpu().numpy().astype(np.int64)\ns = (aa + 2000000000 + 2000000000) % (2**32)\nexp = torch.from_numpy(np.where(s >= 2**31, s - 2**32, s).astype(np.int32))\nprint(\"CONTROL got\", b.tolist(), \"exp\", exp.tolist(),\n      \"->\", \"PASS\" if torch.equal(b, exp) else \"FAIL\")\n# -> CONTROL got [-294967296, -294967295, -294967297, -294967196] exp [...] -> PASS\n```\n\n### Traceback\n\n```\nNo traceback beyond the InternalError — the kernel never finishes compiling:\n\n  [Fatal] InternalError: Check failed: value < 1LL << (dtype.bits() - 1)\n          (4000000000 vs. 2147483648) : Literal value 4000000000\n          exceeds maximum of int32\n```\n\n### Expected behavior\n\nThe kernel should compile and the constant sum should be folded modulo `2^32` (`4_000_000_000 → -294_967_296`), matching the two's-complement `int32` result the hardware produces when the fold is avoided (the runtime-addend control returns exactly that). TileLang/TVM already wraps `int32` constants elsewhere: the plain constant-folder wraps the same `2^31` product without complaint (pure-constant control above), and the runtime path wraps correctly at execution. Compiling `(x + 2000000000) + 2000000000` should not depend on whether an addend happens to be a compile-time constant.\n\n### Additional context\n\n**Root cause.** The `arith` canonical simplifier accumulates a summation's constant part in a `int64_t` field without ever reducing it modulo the expression's `int32` dtype, then rematerializes it as an `int32` literal, which is out of range. `SumExprNode` stores the constant term in [`int64_t base`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/canonical_simplify.cc#L245); [`AddToSelf`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/canonical_simplify.cc#L296) does `base += value` and [`MulToSelf`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/canonical_simplify.cc#L274) does `base *= scale`, neither masked to the dtype. When the sum has a non-constant arg it goes through `Normalize_`, which emits [`res + make_const(dtype, base)`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/canonical_simplify.cc#L510) with `base` still `4_000_000_000` (or `2^31`); `make_const(int32, …)` builds an `IntImm` whose [range check](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/ir/expr.cc#L73) then aborts.\n\nThe contrast with the *scalar* constant-folder is the key. The plain folder, [`TryConstFold<Add>`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L81), routes its result through [`GetFoldResultInt64Repr`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L81) which masks to `dtype.bits()` and sign-extends — so it *wraps* and never materializes an out-of-range literal. The `SumExpr` accumulator applies no such mask. That is why the all-constant expression (no runtime arg, or a constant sub-expression isolated inside a `min`/`max` — both verified below) is fine, and why only \"the fold reached through the summation path with a runtime term\" crashes.\n\n**Only signed `int32` crashes (verified — dtype boundary is sharp).** The `SumExpr` canonical path is entered **only for index-typed dtypes**, [`IsIndexType`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L75) `= is_int() && (bits==32 || bits==64)`. `int8`/`int16`/`uint32` are *not* index types, so they never enter the un-masked accumulator — they fold through `GetFoldResultInt64Repr` and **wrap** (verified: `int8` sum→`-56`, `int16` sum→`-25536`, `uint32` sum→`1705032704`, all compile). `int64` *is* an index type and *does* enter the accumulator, but the accumulator itself wraps in C++ `int64_t` and the `IntImm` range check is [gated on `dtype.bits() < 64`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/ir/expr.cc#L70), so it never fires (verified: `int64` sum→wraps, compiles). Signed `int32` is the *only* dtype that is both an index type (enters the un-masked path) and narrow enough that the range check is armed — so it is the sole crash face.\n\n**Suggested fix.** Apply `GetFoldResultInt64Repr`-style masking to `base` before materializing it — wrap at the `make_const(dtype, base)` sites in `Normalize`/`Normalize_` (and correspondingly in `AddToSelf`/`MulToSelf`), so an in-`int32`-range literal is emitted, consistent with the wrapping the scalar const-folder and the hardware already perform.\n\n**Provenance.** The unmasked `int64_t base` accumulator and `make_const(dtype, base)` rematerialization are original to the TVM CanonicalSimplifier ([apache/tvm #2891](https://github.com/apache/tvm/pull/2891), 2019; relocated into the `arith/` subfolder by #4722), inherited unchanged by the TileLang TVM fork — present since the simplifier first shipped, not a regression.\n\n**Dedup.** I searched the open and closed tracker; the nearest report is [#2560](https://github.com/tile-ai/tilelang/issues/2560), the opposite face — a `>2^31` *constant* index that is **silently** truncated to `int32` (wrong address, no crash), where adding a runtime term *fixes* it via int64 promotion. Here a runtime term is what *triggers* the crash, and the value is in `int32` range once wrapped; different mechanism, different symptom.\n\n**Reach.** The trigger is: a **signed `int32`** expression (verified: NOT narrower ints — `int8`/`int16`/`uint32`/`int64` all wrap, see §17) where the simplifier folds two or more compile-time-constant terms whose exact sum/product leaves `[-2^31, 2^31)`, mixed with at least one runtime term so the summation path (not the scalar folder) is taken. `int32` is the default index/offset dtype, and the fold also fires through **index arithmetic** — `B[i] = A[(i + 2000000000) + 2000000000]` crashes identically (verified §17), which is the more idiomatic `base_off + stride*K` route.\n\nLarge-constant `int32` code appears in the tree — `examples/dsa_hisa/pool_mqa_fp8.py:76-77` seeds reduction bounds with `cu_k_s_min = 2147483647` / `cu_k_e_max = -2147483648` (also `examples/deepseek_v32/fp8_lighting_indexer.py:140-141`; `git grep` of ≥10-digit decimal literals under `examples/`+`testing/` hits ~5 non-mask/non-hash sites). **Ran the example** (`pool_mqa_fp8.py`, smallest shipped config `(32768,64,128,128,256,1)`, tilelang 0.1.13, L40S): the kernel **compiles cleanly past the IR simplifier** (\"TileLang completes to compile kernel\") with **no fold crash** — then fails at an unrelated launch-time step (`InternalError: Failed to set the allowed dynamic shared memory size to 116736`, i.e. the L40S ~99 KB smem cap, a hardware-capacity limit, not this bug). It dodges the fold because each sentinel is a **single** constant fed to `T.min`/`T.max` (`cu_k_s_min = T.min(cu_k_s_min, ...)`), never two constants summed together with a runtime term — the isolated-constant / max-arg case that §17 confirms wraps rather than crashes. So no shipped example currently exercises the crash; ordinary offset accumulation with large literals would.\n\n**Impact.** The trigger is narrow: a compile-time fold of two or more constant terms whose exact sum/product leaves `[-2^31, 2^31)`, mixed with a runtime term. When it fires it is a compile-time `ICHECK` abort — loud and immediate, refusing to build that one legal kernel; nothing is silently miscomputed and no wrong value can reach a running workload. Fixing it removes a compile-time disagreement between the summation path and the plain constant-folder/hardware (both of which already wrap `int32`), closing the \"constant fold reached through the summation path\" boundary class.\n\n<details><summary>§17 Generalization record</summary>\n\n**Two-level root.**\n- **SOURCE-level (fragile implementation):** the `arith` canonical simplifier's `SumExprNode` constant accumulator — `int64_t base` ([canonical_simplify.cc:245](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/canonical_simplify.cc#L245)) mutated by `AddToSelf`/`MulToSelf` and rematerialized by `make_const(dtype, base)` in `Normalize`/`Normalize_` ([L510](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/canonical_simplify.cc#L510)) **without** the dtype-masking (`GetFoldResultInt64Repr`) that the scalar folder [`TryConstFold`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L81) applies. The `IntImm` range check ([expr.cc:73](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/ir/expr.cc#L73)) then aborts.\n- **OPERATOR-level (what correlates):** any construct whose two-or-more constant terms are canonicalized into one `SumExpr` *together with a runtime term* — value stores, index/address arithmetic, `+`/`*`/`-` mixes. The single-constant idioms (`min`/`max` sentinels) and all-constant sub-expressions route through the scalar folder instead and wrap.\n\n**All cells run in their own fresh process, fresh `TILELANG_CACHE_DIR`, tilelang 0.1.13, L40S. Observed, not asserted.**\n\n| Axis | Cell tested (input) | Observed result | Same root? |\n|---|---|---|---|\n| trigger | `(A[i] + int32(2e9)) + int32(2e9)` | CRASH `4000000000 vs 2147483648` | — |\n| related-operator | mult: `int32(1073741824) * int32(2) + A[i]` | CRASH `2147483648 vs 2147483648` | **same** (base via `MulToSelf`) |\n| related-operator | negative: `(A[i] + int32(-2e9)) + int32(-2e9)` | CRASH `4000000000 vs 2147483648` (materialized as positive operand of a sub) | **same** |\n| related-operator | isolated const in max-arg: `T.max(A[i], int32(2e9)+int32(2e9))` | COMPILES, `[0,1,-1,100]` (const sub-expr wraps via scalar folder → `-294967296`) | **same root, dodged** — needs the runtime term *inside* the sum |\n| related-source | index arithmetic: `A[(i + 2e9) + 2e9]` (big src buffer) | CRASH `4000000000 vs 2147483648` | **same** — fold fires through address expr too (idiomatic offset accumulation) |\n| related-type | int8: `(A[i] + int8(100)) + int8(100)` | COMPILES, `[-56,-55,-57,-46]` (wraps) | **distinct** — int8 is not an `IsIndexType`, never enters `SumExpr`; scalar folder masks |\n| related-type | int16: `(A[i] + int16(20000)) + int16(20000)` | COMPILES, `[-25536,…]` (wraps); mult form `int16(16384)*int16(4)` → wraps too | **distinct** — same reason as int8 |\n| related-type | uint32: `(A[i] + uint32(3e9)) + uint32(3e9)` | COMPILES, `[1705032704,…]` (wraps) | **distinct** — `uint` is not `is_int()`, not an index type |\n| related-type | int64: `(A[i] + int64(5e18)) + int64(5e18)` | COMPILES, `[-8446744073709551616,…]` (wraps) | **distinct boundary** — index type, but base wraps in C++ int64 AND range check gated off at `bits==64` |\n| related-type (boundary) | int32 to exactly 2^31: `(A[i] + int32(2147483647)) + int32(1)` | CRASH `2147483648 vs 2147483648` | **same** — crash starts one past `INT32_MAX` |\n\n**Reframed finding — the dtype boundary is much sharper than first stated.** The earlier \"int32 or narrower-int\" reach was **refuted by running it**: the crash is **signed-`int32`-ONLY**. `int8`/`int16` are excluded because `SumExpr` is entered only for [`IsIndexType`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/arith/const_fold.h#L75) (`is_int() && bits∈{32,64}`); `uint32` is excluded (not `is_int()`); `int64` enters the path but is masked off by the C++ int64 wrap + the `bits<64` range-check gate. `int32` is the unique dtype that (a) enters the un-masked accumulator and (b) has the range check armed. The class is therefore narrow and specific — kept specific in the title, not broadened.\n\n**Same-root neighbors folded into this report:** the multiplication form, the negative-overflow form, and the index-arithmetic form all share the identical `SumExpr.base`→`make_const` sink and are the same bug (documented above, not separate reports).\n\n**Distinct-adjacent, not filed:** the `int8`/`int16`/`uint32`/`int64` wrapping behaviors are *correct* (not bugs) and delimit the boundary. No NEW bug was found while sweeping.\n\n**Example-run result (PART 1).** `examples/dsa_hisa/pool_mqa_fp8.py` (the cited large-`int32`-literal site) was fetched at v0.1.13 with its local deps and run at its smallest shipped config on 0.1.13/L40S: it **compiles past the IR simplifier with no fold crash** and then fails at an unrelated launch step (`Failed to set the allowed dynamic shared memory size to 116736` — L40S smem cap, not this bug). It dodges the fold because the `2147483647`/`-2147483648` sentinels are each a **single** constant inside `T.min`/`T.max`, matching the \"isolated const in max-arg\" cell above which wraps rather than crashes. Confirms: no shipped example exercises this crash.\n\n**SAME MECHANISM as filed [#2982](https://github.com/tile-ai/tilelang/issues/2982), different sink — do NOT merge.** #2982 is the same general root — an `IntImm`/`FloatImm` range check aborting on a value not first narrowed to the result dtype — at a different construction site. Here the sink is specifically the `SumExpr` `int64_t base` accumulator rematerialized via `make_const(dtype, base)`; #2982's is a distinct materialization point. Shared general root, distinct sinks: keep as separate reports.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3002"}
{"number": 3000, "title": "[BUG][Fuzzer][ice-on-valid-code] Casting an out-of-range integer constant to a narrow int crashes the compiler instead of wrapping", "state": "open", "created_at": "2026-08-16T06:20:21Z", "updated_at": "2026-08-16T06:20:21Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) that this hasn't already been reported. (comment there if it has.)\n\n### What version of TileLang are you using?\n\n0.1.13 (tag `v0.1.13` = `8001cc4ccf6149382d2019654a19f59c1d4d0482`, bundling the `tile-ai/tvm` submodule at `8df8ebd61659505dd7005f4820b030e983e2c005`).\n\n### System information\n\n- NVIDIA L40S (`sm_89`), CUDA 12.8, Python 3.13, torch 2.8, tilelang 0.1.13.\n- **Architecture-independent — no GPU required.** The crash fires host-side while the arithmetic simplifier folds the store's value expression at compile time, before any target codegen. It was reproduced with `target=\"cuda\"`, but the abort happens while lowering the `@T.prim_func`, before a device is touched.\n\n### Problem description\n\nWriting a compile-time integer constant that does not fit the destination's narrow integer type crashes compilation with an internal `Check failed` abort. `T.Cast(\"int8\", 300)` aborts with `Check failed: value < 1LL << (dtype.bits() - 1) (300 vs. 128) : Literal value 300 exceeds maximum of int8`, even though narrowing `300` to `int8` is a defined conversion that wraps to `44` (`300 - 256`). The same narrowing computed at runtime returns `44` — only the compile-time-constant path crashes.\n\nThe trigger is a constant whose value exceeds the target's range, cast to any integer type narrower than 64 bits (int8/int16/int32/uint*; only 64-bit types skip the range check):\n\n<details>\n<summary>Trigger / control matrix (run this session, L40S, tilelang 0.1.13)</summary>\n\n| cast | expected (C narrowing / numpy) | outcome |\n|---|---|---|\n| `T.Cast(\"int8\", 300)` | `44` | **CRASH** `value < 1LL << (dtype.bits() - 1) (300 vs. 128) : Literal value 300 exceeds maximum of int8` |\n| `T.Cast(\"int8\", 200)` | `-56` | **CRASH** `(200 vs. 128) : ... exceeds maximum of int8` |\n| `T.Cast(\"int8\", -200)` | `56` | **CRASH** `value >= -(1LL << (dtype.bits() - 1)) (-200 vs. -128) : ... exceeds minimum of int8` (GE check) |\n| `T.Cast(\"int8\", 128)` | `-128` | **CRASH** `(128 vs. 128) : ... exceeds maximum of int8` (128 is one past `int8` max) |\n| `T.Cast(\"int16\", 70000)` | `4464` | **CRASH** `(70000 vs. 32768) : ... exceeds maximum of int16` |\n| `T.Cast(\"uint8\", 300)` | `44` | **CRASH** `value < 1LL << dtype.bits() (300 vs. 256) : ... exceeds maximum of uint8` (uint LT check) |\n| `T.Cast(\"int32\", 3000000000)` | `-1294967296` | **CRASH** `(3000000000 vs. 2147483648) : ... exceeds maximum of int32` — int32 is NOT wide enough to be safe |\n| `T.Cast(\"int64\", 5000000000)` (bits==64) | `5000000000` | OK — 64-bit types skip the range check, so no crash |\n| `T.Cast(\"int8\", 44)` (in range) | `44` | OK — compiles and returns `44` |\n| runtime `int32 -> int8` of `300` (value from a buffer) | `44` | OK — wraps, returns `44` |\n\nThe runtime narrowing cast and the in-range constant both work; only an **out-of-range constant** cast to an integer type narrower than 64 bits aborts. This is not a regression — the fold shortcut is long-standing.\n\n</details>\n\n### Reproducible example code\n\n```python\nimport torch, numpy as np, tilelang, tilelang.language as T\nN = 8\n\n# (A) BUG: a compile-time integer constant that overflows the narrow target type\n#     crashes at compile time. 300 as int8 is a defined narrowing conversion\n#     (300 - 256 = 44), yet the fold aborts.\n@tilelang.jit(out_idx=[0])\ndef const_cast(dd, val):\n    @T.prim_func\n    def main(B: T.Tensor((N,), dd)):\n        with T.Kernel(1, threads=1):\n            for i in T.serial(N):\n                B[i] = T.Cast(dd, val)     # val folded at compile time\n    return main\n\n# (B) CONTROL: the SAME narrowing, value supplied at runtime -> computes 44 (wraps).\n@tilelang.jit(out_idx=[1])\ndef runtime_cast(sd, dd):\n    @T.prim_func\n    def main(A: T.Tensor((N,), sd), B: T.Tensor((N,), dd)):\n        with T.Kernel(1, threads=1):\n            for i in T.serial(N):\n                B[i] = T.Cast(dd, A[i])\n    return main\n\nfor dd, val in [(\"int8\", 300), (\"int16\", 70000), (\"uint8\", 300)]:\n    ref = int(np.array([val]).astype({\"int8\":np.int8,\"int16\":np.int16,\"uint8\":np.uint8}[dd])[0])\n    try:\n        got = int(const_cast(dd, val)()[0].item())\n        print(f\"{dd} <- {val}: got={got} expected={ref}\")\n    except Exception as e:\n        print(f\"{dd} <- {val}: CRASH -> {str(e).splitlines()[-1][:70]} (expected {ref})\")\n# int8  <- 300  : CRASH -> ... 300 exceeds maximum of int8   (expected 44)\n# int16 <- 70000: CRASH -> ... 70000 exceeds maximum of int16 (expected 4464)\n# uint8 <- 300  : CRASH -> ... 300 exceeds maximum of uint8   (expected 44)\n\na = torch.tensor([300, 208, 200, 128, 44, -1, 127, 255], dtype=torch.int32, device=\"cuda\")\nb = runtime_cast(\"int32\", \"int8\")(a).cpu().numpy()\nprint(\"runtime int32->int8:\", b, \"(wraps: 300 -> 44)\")   # -> [44 -48 -56 -128 44 -1 127 -1]\n```\n\n### Traceback\n\n```\ntvm.error.InternalError: Check failed: value < 1LL << (dtype.bits() - 1) (300 vs. 128) :\n    Literal value 300 exceeds maximum of int8\n  ...\n  in tvm::arith::RewriteSimplifier::Impl::VisitExpr_(tvm::tir::CastNode const*)\n  in tvm::cast(tvm::runtime::DataType const&, tvm::PrimExpr, tvm::Span)\n  in tvm::PrimExpr tvm::tir::MakeConstScalar<long>(tvm::runtime::DataType, long, tvm::Span)\n  in tvm::IntImm::IntImm(tvm::runtime::DataType, long, tvm::Span)\n```\n\n### Expected behavior\n\nCasting an integer constant to a narrower integer type should produce the same value the runtime cast produces — the low `bits()` of the source, interpreted in the target type (`300 -> int8 == 44`, matching C 2's-complement narrowing and `numpy.int8(300)`). The runtime path already computes exactly this over the same inputs, so the constant path returning a different result — a hard compiler abort — is the inconsistency.\n\n### Additional context\n\n**Root cause.** The arithmetic simplifier's constant-cast shortcut narrows an integer constant by re-using the source value unmasked, so it hands an out-of-range value to `IntImm`, whose range assert aborts. When `tvm::cast` sees a `Cast` of an `IntImm`, it folds via [`make_const(t, op->value, op->span)`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L445) ([comment \"const fold IntImm as they are used in index computations\"](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/tirx/op/op.cc#L442)). That routes to [`MakeConstScalar`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/include/tvm/tirx/op.h#L978), which for an int/bool type does `IntImm(t, static_cast<int64_t>(value), span)` with no truncation to `t.bits()`. `IntImm`'s constructor then hits its [range check](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/ir/expr.cc#L73) and aborts. A wrapping `value & ((1 << bits) - 1)` (sign-extended for signed types) before constructing the `IntImm` — the operation the emitted device code already performs — would fold to the correct in-range constant.\n\n**Suggested fix.** In the integer branch of `MakeConstScalar` (or in the `cast`/`make_const` fold shortcut), truncate the value to the destination width and sign/zero-extend per signedness before building the `IntImm`, matching the runtime narrowing. That keeps the constant fold and returns the same value the non-folded path computes, instead of aborting.\n\n**Provenance.** The `tvm::cast` IntImm const-fold shortcut is long-standing upstream TVM code inherited by the `tile-ai/tvm` fork; it is present at the v0.1.13 submodule pin (`8df8ebd6`) with the \"const fold IntImm as they are used in index computations\" comment. Origin predates the fork; the exact upstream commit is unverified.\n\n**Dedup.** I searched the open and closed tracker (keywords \"constant fold\", \"cast\", \"exceeds maximum\", \"narrowing\", \"IntImm\") and found no report of this narrowing-cast crash. It shares its general mechanism with the already-filed #2982 — that report is the same \"range-check on an un-narrowed constant\" defect, but on the **FloatImm** branch reached via `T.fill(float32, ...)` (`expr.cc:100-107`, the float bounds checked before rounding). This one is the **IntImm** branch reached via `T.Cast` to a narrow int (`expr.cc:71-73`, the int bounds checked before truncation), fed by a different sink (`tvm::cast` -> `MakeConstScalar`, which does no mask step). Same root family, distinct sink — do NOT merge. Also distinct from #2582, a `uint64` `Add`/`Mul` fold whose *result* lands in `[2^63, 2^64)`: that path (`GetFoldResultInt64Repr`) masks to `bits()` for narrow types, so it is a separate mechanism.\n\n**Reach.** The trigger is typed and documented (`T.Cast` accepts any dtype and value pair, and the runtime path defines the result), and it is natural usage: writing a constant clamp/bias/fill into a narrow-int buffer is the natural way to express a quantization step; 32 example files under `examples/` use `int8`/`int16` dtypes. The **fragile narrowing-cast path IS exercised by shipped examples** — the dequantize kernels build narrow-int constants through this exact fold via `tirx.const(..., T.uint8/uint16)` and `expr.astype(T.uint8/uint16)` (e.g. `examples/dequantize_gemm/example_dequant_gemm_w4a8.py`, `..._bf16_mxfp4_hopper.py`). Those examples **dodge the crash only because every constant they narrow is in range** (masks `(1<<4)-1=15`, bit-shift amounts `1..8`, bias `126` — all `< 256`). I ran the shipped `example_dequant_gemm_w4a8.py` verbatim on 0.1.13 (`--m 128 --n 256 --k 256`) and it prints `All checks pass.` — it uses the same `tirx.const(..., T.uint8)` narrowing fold this bug lives in, but every folded constant is in-range so the range check never trips. No shipped example uses an *out-of-range* narrow-int constant, which is why CI is green. The one ingredient that fires it is a constant outside the target's `[min, max]`; any narrow-int destination trips it, an in-range one does not.\n\n**Impact.** The trigger is narrow: a compile-time integer constant, statically outside the destination integer type's `[min, max]` (a runtime value or an in-range constant does not fire). When it fires it is a hard host-side compile abort — loud, deterministic, caught immediately, and it corrupts no data and cannot slip silently into a running workload; the cost is that a valid kernel writing an out-of-range constant into a narrow-int buffer refuses to build. Fixing it removes the const-fold-vs-runtime disagreement (the folded path would then return the same wrapped value the runtime cast already produces) and closes this out-of-range narrowing boundary.\n\n<details>\n<summary>§17 Generalization record</summary>\n\n**Two-level root.**\n- **SOURCE-level (fragile implementation):** the `IntImm` constructor's range check (`src/ir/expr.cc`, the `is_uint` LT check ~L64, the signed lower-bound GE ~L71 and upper-bound LT ~L73, and the `bits()==1`/bool `value==0||value==1` branch ~L69) is fed by `tvm::cast`'s IntImm const-fold shortcut (`src/tirx/op/op.cc:445`, `make_const(t, op->value, ...)`) → `MakeConstScalar` (`include/tvm/tirx/op.h:979`, `IntImm(t, static_cast<int64_t>(value), span)`) which passes the source value through with **no mask/truncate to `t.bits()`**. The abort is the range check firing on a value the destination could legally hold post-narrowing.\n- **OPERATOR-level (what correlates):** ANY frontend path that folds an out-of-range integer constant into a narrow-int `IntImm` — every alias of \"cast/construct a constant into a narrow integer\" routes through the same `cast`→`MakeConstScalar`→`IntImm` sink and shares this root.\n\n**Class.** \"Un-narrowed constant construct\" family: a fold builds an `IntImm`/`FloatImm` from the source value WITHOUT masking/rounding to the destination's `bits()`. General root shared with the already-filed **#2982**, which is the **FloatImm** sibling sink (`T.fill(float32,...)`, float bounds checked before rounding); this draft is the **IntImm** sink (`cast`→`MakeConstScalar`, no mask step). Same family, distinct sink — do NOT merge. Also distinct from **#2582** (`uint64` `Add`/`Mul` fold whose *result* is masked to `bits()` in `GetFoldResultInt64Repr` — separate mechanism; confirmed below `int64<-5e9` does not fire).\n\n**4-axis sweep (every cell RUN, fresh process + fresh cache, L40S, 0.1.13):**\n\n| axis | cell tested | input → observed result | same-root? |\n|---|---|---|---|\n| found-first | `T.Cast(\"int8\", 300)` | CRASH `value < 1LL<<(bits-1) (300 vs. 128) : exceeds maximum of int8` (wants `44`) | — (root) |\n| related-operator | `tirx.const(300, \"int8\")` (the helper examples use) | CRASH `(300 vs. 128) : exceeds maximum of int8` | SAME |\n| related-operator | `T.Cast(\"int32\", 300).astype(\"int8\")` (the `.astype` example idiom) | CRASH `(300 vs. 128) : exceeds maximum of int8` | SAME |\n| related-operator | `T.Cast(\"int8\", 100+250)` (constant *arithmetic* folds to 350) | CRASH `(350 vs. 128) : exceeds maximum of int8` | SAME — need not be a literal |\n| related-source | (FloatImm sink, `T.fill(float32,...)`) = **#2982** | filed separately | same family, distinct sink |\n| related-source | (`uint64` result-mask fold) = **#2582** | masks, no crash | distinct mechanism |\n| related-operator | `T.Cast(\"uint8\", 300)` | CRASH `value < 1LL<<bits (300 vs. 256) : exceeds maximum of uint8` (wants `44`) | SAME, uint LT sub-check (expr.cc~L64) |\n| related-type (bool) | `T.Cast(\"bool\", 5)` | CRASH `(value==0||value==1) is false: 5 exceeds range of bool` | SAME sink/root, the `bits()==1` sub-check — no narrowing-to-`1` |\n| related-type (sign) | `T.Cast(\"int8\", -200)` | CRASH `value >= -(1LL<<(bits-1)) (-200 vs. -128) : exceeds minimum of int8` (wants `56`) | SAME, signed lower-bound GE (expr.cc~L71) |\n| related-type (boundary) | `T.Cast(\"int8\", 128)` | CRASH `(128 vs. 128)` (wants `-128`) | SAME — max is exclusive |\n| related-type (width) | `T.Cast(\"int16\", 70000)` | CRASH `(70000 vs. 32768) : exceeds maximum of int16` (wants `4464`) | SAME |\n| related-type (width) | `T.Cast(\"int32\", 3000000000)` | CRASH `(3000000000 vs. 2147483648) : exceeds maximum of int32` (wants `-1294967296`) | SAME — int32 NOT safe |\n| related-type (width) | `T.Cast(\"int64\", 5000000000)` | OK, returns `5000000000` | DISTINCT boundary — `bits()==64` skips the range check |\n| control | `T.Cast(\"int8\", 44)` (in range) | OK, returns `44` | — proves boundary |\n| control | runtime `int32→int8` of `300` (buffer value) | OK, returns `44` (wraps) | — the non-fold path is correct |\n\n**What the sweep establishes:**\n- The class is **\"any integer type narrower than 64 bits, out-of-range constant, via any cast/const-construct idiom\"** — not \"sub-int32\" (int32 fires) and not \"only `T.Cast`\" (`tirx.const`, `.astype`, and constant *arithmetic* all fire the same sink).\n- `bool` (`bits()==1`) hits a *different* assert inside the same `IntImm` ctor — same root, one more sub-check, so it belongs to the class.\n- 64-bit is the sole safe integer width (range check gated on `bits() < 64`).\n\n**PART 1 — shipped-example run.** No `examples/` file spells `T.Cast(\"int8\"|\"int16\"|\"uint8\"` literally (`grep`: 0 sites), but the narrowing-cast fold **is** exercised by the dequantize examples via `tirx.const(..., T.uint8/uint16)` and `.astype(T.uint8/uint16)`. I ran `examples/dequantize_gemm/example_dequant_gemm_w4a8.py --m 128 --n 256 --k 256` verbatim on 0.1.13: it prints `All checks pass.` It **dodges** the crash because every constant it narrows is in range (`(1<<4)-1=15`, shift amounts, `T.int8` reinterpret masks — all `< 256`). So a shipped example touches the exact fragile path and passes only by never handing it an out-of-range value. The bug is real (all matrix cells above reproduce), the example does not trigger it, and that is why CI is green.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/3000"}
{"number": 2999, "title": "[BUG][Fuzzer][ice-on-valid-code] A finite fp32 constant above a narrow float dtype's max (fp16/fp8/fp4) aborts in `FloatImm` instead of rounding like the runtime cast", "state": "open", "created_at": "2026-08-16T06:20:20Z", "updated_at": "2026-08-16T06:20:20Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and found no existing report of this defect.\n\n### What version of TileLang are you using?\n\n0.1.13 (tilelang `8001cc4`, bundled tvm `8df8ebd`). The same check is present on `main`; on this pin the assertion macro is `TVM_FFI_CHECK_LE` and the whole range check is now wrapped in an `if (!std::isinf(value) && !std::isnan(value))` guard, so only finite literals are checked.\n\n### System information\n\nTileLang 0.1.13 (tvm `8df8ebd`); the crash is at compile time (host-side constant folding), so it is architecture-independent. Reproduced on an NVIDIA L40S (sm_89), CUDA 12.\n\n### Problem description\n\nCasting a compile-time `float32` constant whose magnitude is above the fp16 *maximum finite value* (65504) but still finite as fp32 aborts compilation with an internal check, e.g. `T.cast(T.float32(65510.0), \"float16\")` raises `Check failed: value <= support::kMaxFloat16 (65510 vs. 65504)`. The identical cast performed at runtime on a device value compiles and returns the correctly-rounded result — `65510.0 → 65504.0` (the fp16 max), and any value `≥ 65520.0 → inf`, both matching `torch`/`numpy`. So a legal fp32→fp16 conversion crashes the compiler on the constant-folding path while succeeding on the runtime path.\n\nThe affected region is every *finite fp32* constant above `65504`: `(65504, 65520)` rounds to the finite fp16 max `65504`, and `[65520, ∞)` (still finite as fp32) rounds to `inf` — both are defined fp16 conversions that the compiler refuses to fold. The only value in that magnitude range that compiles is an actual `float('inf')` literal, because the check is now skipped for non-finite inputs (`!std::isinf`). This is a hard compile abort (`ValueError` from the FFI check), not a silent miscompile.\n\nThe same `FloatImm` check governs the *other narrow float dtypes* and *other constant sinks*, all verified below: fp8 (`e5m2` max `57344`, `e4m3`/`e4m3fn` max `448`) and fp4 (`e2m1fn` max `6`) abort identically on a finite fp32 constant above their max; the sink need not be a `T.cast` — a direct `T.float16(65510.0)` construction or a `T.fill(buf, T.float16(65510.0))` aborts at the same line. The negative side is symmetric (`T.cast(T.float32(-65510.0),\"float16\")` trips the `GE` check against `-65504`). `bfloat16` is the one narrow dtype with *no* analogous edge, because its max (`≈3.895e38`) exceeds `float32::max` (`≈3.403e38`), so a finite fp32 constant can never reach the bf16 bound (it overflows the fp32 literal check first).\n\n### Reproducible example code\n\n```python\nimport os, tempfile\nos.environ[\"TILELANG_CACHE_DIR\"] = tempfile.mkdtemp(prefix=\"tl_\")\nimport tilelang, tilelang.language as T, torch\n\nC = 65510.0  # in (65504, 65520): IEEE round-to-nearest maps it to the finite fp16 max 65504.0\n\n# CONTROL: cast the SAME value from a device tensor at runtime\n@tilelang.jit(out_idx=[1])\ndef krun(n):\n    @T.prim_func\n    def main(A: T.Tensor((n,), \"float32\"), B: T.Tensor((n,), \"float16\")):\n        with T.Kernel(1, threads=n) as bx:\n            for i in T.Parallel(n):\n                B[i] = T.cast(A[i], \"float16\")\n    return main\n\nA = torch.full((2,), C, dtype=torch.float32, device=\"cuda\")\nref = torch.full((2,), C, dtype=torch.float32).to(torch.float16)[0].item()  # torch RNE -> 65504.0\nprint(\"CONTROL runtime cast :\", krun(2)(A)[0].item(), \"  ref\", ref)   # -> 65504.0   ref 65504.0\n\n# TRIGGER: cast the SAME value as a compile-time float32 CONSTANT (const-folded)\n@tilelang.jit(out_idx=[0])\ndef kfold(n):\n    @T.prim_func\n    def main(B: T.Tensor((n,), \"float16\")):\n        with T.Kernel(1, threads=n) as bx:\n            for i in T.Parallel(n):\n                B[i] = T.cast(T.float32(C), \"float16\")\n    return main\n\nprint(\"TRIGGER const-fold   :\", kfold(2)()[0].item())   # -> InternalError before this prints\n```\n\n<details><summary>Boundary: runtime cast is correct across the whole region; const-fold aborts on every value &gt; 65504</summary>\n\n| fp32 constant | torch/numpy fp16 (RNE) | runtime `T.cast` (control) | const-fold `T.cast(T.float32(C),\"float16\")` |\n|---|---|---|---|\n| 65505.0 | 65504.0 | 65504.0 | `Check failed: value <= support::kMaxFloat16 (65505 vs. 65504)` |\n| 65510.0 | 65504.0 | 65504.0 | `... (65510 vs. 65504)` |\n| 65519.0 | 65504.0 | 65504.0 | `... (65519 vs. 65504)` |\n| 65520.0 | inf | inf | `... (65520 vs. 65504)` |\n| 66000.0 | inf | inf | `... (66000 vs. 65504)` |\n| 1e30 | inf | inf | `... (1e+30 vs. 65504)` |\n| `float('inf')` | inf | inf | **compiles → inf** (non-finite input skips the check) |\n\n</details>\n\n### Traceback\n\n```\nValueError: Check failed: value <= support::kMaxFloat16 (65510 vs. 65504) : Literal value 65510 exceeds maximum of float16\n  File \".../3rdparty/tvm/src/ir/expr.cc\", line 108, in tvm::FloatImm::FloatImm(tvm::DataType, double, tvm::Span)\n  <- tvm::tirx::MakeConstScalar<double>(...) <- tvm::tirx::make_const<double>(...) <- tvm::cast(...)\n  <- T.cast(T.float32(65510.0), \"float16\")\n```\n\n(The `TVM_FFI_CHECK_LE(..., ValueError)` macro raises a `ValueError`, surfaced through the FFI as a hard compile-time abort; it is not caught or rounded.)\n\n### Expected behavior\n\nThe constant-fold path should perform the same IEEE-754 round-to-nearest conversion the runtime cast already performs: fold a constant in `(65504, 65520)` to the fp16 max `65504.0`, and a constant `≥ 65520` to `inf` — matching `torch`/`numpy` and the compiler's own device cast. A finite fp32 value should not make a `float16` cast uncompilable when the runtime path converts it without error.\n\n### Additional context\n\n**Root cause.** The `FloatImm` constructor treats the fp16 *maximum finite value* as the *maximum admissible* literal, so it rejects any finite value above it instead of allowing round-to-nearest to reach `65504` or `inf`. In [`src/ir/expr.cc`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/ir/expr.cc#L99-L109), the range check is entered only for finite values (`if (!std::isinf(value) && !std::isnan(value))`, L99); the `is_float16()` branch (L105) then does `TVM_FFI_CHECK_LE(value, support::kMaxFloat16)` (L108) against [`kMaxFloat16 = 65504.0`](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/support/limits.h#L32). Any *finite* constant in `(65504, ∞)` fed to the fp16 `FloatImm` — as happens when `T.cast(<fp32 const>, \"float16\")` is constant-folded — trips the check and aborts. The runtime cast lowers to a device `float2half` conversion that is unaffected, which is why the two paths disagree. (An already-`inf` literal skips the check via the `!std::isinf` guard and compiles.)\n\n**Suggested fix.** In the `FloatImm` constructor (`src/ir/expr.cc`), round the incoming finite value into the target dtype's representable set rather than erroring. The same fix covers every narrow-float branch (fp16 / fp8 / fp4), since all use the identical *check-max-before-round* shape: map a finite value in `(dtype_max, overflow_threshold)` to `dtype_max` and a finite value `≥ overflow_threshold` to `inf` (or the dtype's saturating max for the `fn`/no-inf formats), matching the device cast and the already-accepted `inf` literal. (bf16 already can't reach its bound from an fp32 constant, so it needs no change but shares the same corrected logic.)\n\n**Provenance.** The fp16 range check in `FloatImm` is inherited from the Apache TVM base the TileLang fork tracks; on the 0.1.13-bundled tvm (`8df8ebd`) it uses the `TVM_FFI_CHECK_LE` macro under the `!isinf && !isnan` guard. Exact introducing commit not verified — it predates the fork's own history for this file. Not a regression: the same abort occurs on 0.1.13 (and on 0.1.9, the earliest version tested).\n\n**Dedup.** I searched the open and closed tracker and found no existing report of the fp16 const-fold *cast* defect. #2982 (`T.fill` of a boundary float32/float16 literal aborting on the same `FloatImm` range check) is the same root mechanism — a `FloatImm` built from a value not first rounded into the target dtype's representable set — at a **different sink** (`T.fill` materialization vs. constant-folded `T.cast`). Not merged: distinct trigger and repro path; see the generalization record below.\n\n**Reach.** The trigger is a `float16` cast (or direct construction) of an fp32 constant in `(65504, 65520)` — an in-contract, round-to-nearest-defined conversion (fp32→fp16 casting is a core documented op, with the runtime path as the reference behavior). fp16 constants near the type's maximum arise wherever a kernel materializes a large fp16 literal (a saturation/clip ceiling, an initializer, a max-value sentinel).\n\n*Shipped-example check (run on 0.1.13).* `git grep` over `examples/` and `testing/` in the v0.1.13 tree finds **no** site that casts or constructs a constant `> 65504` at fp16. The near-neighbours all dodge the region for a concrete reason: the large-magnitude literal casts in the flash-attention backward examples (`examples/flash_attention/example_gqa_bwd_tma_reduce.py:50`, `T.fill(scores_max, T.cast(-1e30, accum_dtype))`) target `accum_dtype = T.float32` (verified: `-1e30` fits fp32, so it builds a fp32 `FloatImm`, not fp16); the fp16 literal in `examples/dequantize_gemm/example_dequant_gemm_fine_grained.py:325` is `T.float16(0)` (in range); and every fp16 *cast* in the examples (`examples/deepseek_v32/topk_selector.py:11` `T.cast(x, T.float16)`, the `acc_s.to(torch.float16)` reductions) casts a **runtime** value, which lowers to the unaffected device `float2half` path — this is why CI is green. I confirmed the runtime path directly: `T.cast(A[i],\"float16\")` on a device tensor of `65510.0` returns `65504.0` (matches torch), and `65520.0`/`66000.0` return `inf` (match torch), while the const-fold path aborts on all three (table above).\n\n**Impact.** The trigger is narrow: it needs a compile-time float *constant* (not a runtime value) whose magnitude falls above the target narrow dtype's max and reaches `FloatImm` via cast const-fold / direct construction / `T.fill` / reducer init. When it fires it is a hard compile-time abort (`ValueError` surfaced through the FFI), so it is caught immediately, blocks that one kernel from building, and cannot silently corrupt a result or reach production — no wrong value is ever produced. One fix in the `FloatImm` constructor closes the whole class (fp16 + fp8 e5m2/e4m3/e4m3fn + fp4 e2m1fn, both signs), removing a compile-vs-runtime disagreement on documented, round-to-nearest-defined narrowing conversions.\n\n<details><summary>§17 Generalization record</summary>\n\n**Two-level root.**\n- **SOURCE-level root:** `tvm::FloatImm::FloatImm(DataType, double, Span)` in the bundled tvm [`src/ir/expr.cc` L99–L109](https://github.com/tile-ai/tvm/blob/8df8ebd61659505dd7005f4820b030e983e2c005/src/ir/expr.cc#L99-L109). Under the `if (!isinf && !isnan)` guard (L99), every narrow-float branch does `TVM_FFI_CHECK_LE(value, <dtype_max>)` (fp16 L108, bf16 L113, fp8 L161, fp6 L169, fp4 L176) — a range check against the dtype's *exact finite max*, performed **before** the value is rounded into the dtype. Any finite value in `(dtype_max, ∞)` is rejected instead of allowed to round to the dtype max / `inf`. This is the fragile code shape.\n- **OPERATOR-level root:** any front-end path that materialises a compile-time float constant at a narrow dtype builds this `FloatImm`. Confirmed sinks: `T.cast(<fp32 const>, narrow)` (const-fold), direct `T.<narrow>(const)` construction, `T.fill(buf, T.<narrow>(const))`, and the `comm_reducer` init literal. The *runtime* cast path (`T.cast(<runtime value>, narrow)`) does **not** go through `FloatImm`; it lowers to a device conversion and is unaffected — this is the compile-vs-runtime disagreement.\n\n**Class hypothesis (confirmed):** the **\"check-max-before-round\"** family — a `FloatImm` is range-checked against the target dtype's exact bounds *before* the value is rounded into that dtype's representable set, so a value that would legally round to the dtype max / to `inf` is rejected. Same root as #2982 (`T.fill` boundary literal) at a different sink.\n\n**4-axis sweep** (each cell = fresh `/tmp/gen_29_*` dir, fresh cache, one kernel/process, `/tmp/tl113_venv` = 0.1.13):\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| found-first | `T.cast(T.float32(65510.0),\"float16\")` | CRASH `value <= support::kMaxFloat16 (65510 vs. 65504)`; runtime cast → `65504.0` | — |\n| similar-logic | negative side: `T.cast(T.float32(-65510.0),\"float16\")` | CRASH `value >= -support::kMaxFloat16 (-65510 vs. -65504)`; torch → `-65504.0` | **same root** (mirror `GE` check, same line pair) |\n| related-source | `≥65520` (→inf) rows: `65520.0`, `66000.0`, `1e30` cast to fp16 | all CRASH (`(65520 vs. 65504)` … `(1e+30 vs. 65504)`); runtime cast of `65520`/`66000` → `inf` (= torch) | **same root** — finite value hits LE check before rounding to inf |\n| related-operator | direct `T.float16(65510.0)` (no cast) | CRASH at same `expr.cc:108`; this is the **minimal** form — bug is not cast-specific | **same root, broader** |\n| related-operator | `T.fill(buf, T.float16(65510.0))` | CRASH `(65510 vs. 65504)` at same line | **same root** = #2982 sink |\n| related-type | `T.cast(T.float32(60000.0),\"float8_e5m2\")` (max 57344) | CRASH `value <= bound (60000 vs. 57344)` | **same root** — fp8 branch, reachable from fp32 |\n| related-type | `T.cast(T.float32(500.0),\"float8_e4m3\"/\"float8_e4m3fn\")` (max 448) | CRASH `(500 vs. 448)` | **same root** — fp8 branch |\n| related-type | `T.cast(T.float32(10.0),\"float4_e2m1fn\")` (max 6) | CRASH `(10 vs. 6)` | **same root** — fp4 branch |\n| related-type | `T.cast(T.float32(3.0e38),\"bfloat16\")` | **compiles → `3.004e38`** (in range) | n/a |\n| related-type | `T.cast(T.float32(3.5e38),\"bfloat16\")` | CRASH but on the **fp32** check `(3.5e+38 vs. 3.40282e+38)` — overflows fp32 literal first | **NOT reachable** — bf16 max `≈3.895e38 > float32::max`, no analogous edge via fp32-const cast |\n| boundary | `T.cast(T.float32(float('inf')),\"float16\")` | **compiles → `inf`** — `!isinf` guard skips the check | distinct (marks the exact defect boundary: only *finite* > max crashes) |\n\n**Reframe.** The defect is a **class** across the narrow-float branches of `FloatImm` (fp16 + fp8 e5m2/e4m3/e4m3fn + fp4 e2m1fn) and across the constant sinks (cast / direct construction / `T.fill` / reducer init), all one fix in one function. Title/Problem widened to the class; fp16 kept as the lead example (most common in practice). bf16 is explicitly out-of-class (bound unreachable from fp32). The negative side and the `≥ overflow_threshold`(→inf) sub-region are the same root, not separate bugs. #2982 is the same root at the `T.fill` sink — **not merged** (distinct trigger/repro), noted as the same class here.\n\n**No new distinct bug found while sweeping.** All crashing cells trace to the single `check-max-before-round` root; the two clean cells (bf16 in-range, `inf` literal) are correct behaviour, not new defects.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/2999"}
{"number": 2998, "title": "[BUG][Fuzzer][ice-on-valid-code] `T.reduce_max`/`reduce_min` into a `float8_e4m3`/`float4` fragment crashes nvcc with undefined `inff` instead of compiling", "state": "open", "created_at": "2026-08-16T06:20:02Z", "updated_at": "2026-08-16T06:20:02Z", "closed_at": null, "labels": [], "comments_count": 0, "is_pr": false, "body": "### Required prerequisites\n\n- [x] I have read the documentation <https://tilelang.com>.\n- [x] I have searched the [Issue Tracker](https://github.com/tile-ai/tilelang/issues) and [Discussions](https://github.com/tile-ai/tilelang/discussions) that this hasn't already been reported.\n\n### What version of TileLang are you using?\n\n0.1.13 (still present on `main`, inspected, for the `float8_e4m3`/`float4` sub-case — see Additional context).\n\n### System information\n\nTileLang 0.1.13 / CUDA 12.8 / NVIDIA L40S (sm_89).\n\n### Problem description\n\n`T.reduce_max` (or `T.reduce_min`) whose destination fragment is a `float8_e4m3` type fails to compile: nvcc aborts with `identifier \"inff\" is undefined`. The reduction lowers correctly and torch reduces the same fp8 inputs fine; the failure is purely in the emitted CUDA source.\n\nThe reduce lowers an init constant of `-INFINITY` (for max) or `+INFINITY` (for min) into the fp8 accumulator, and codegen prints that infinity as the literal `-inff`:\n\n```\nCf[0] = fp8_e4_t(-inff/*-inf*/);   // nvcc: identifier \"inff\" is undefined\n```\n\nSwapping only the destination fragment to `float32` (same reduce, same fp8 inputs) compiles and returns the correct result, so the crash is specific to printing the fp8-typed infinity init, not to the reduction. `float4_e2m1fn` hits the identical `inff` emission; `float8_e5m2` no longer emits `inff` on 0.1.13 (its inf/nan cases were guarded by #2671) — see Additional context.\n\nNot a regression — the fp8 codegen path has emitted this since it was added (see Provenance).\n\n### Reproducible example code\n\n```python\nimport tilelang\nimport tilelang.language as T\nimport torch\n\nM, N = 32, 64\n\n@tilelang.jit(out_idx=[1])\ndef kernel(dt):\n    @T.prim_func\n    def main(A: T.Tensor((M, N), dt), C: T.Tensor((M,), dt)):\n        with T.Kernel(1, threads=128):\n            Af = T.alloc_fragment((M, N), dt)\n            Cf = T.alloc_fragment((M,), dt)\n            T.copy(A, Af)\n            T.reduce_max(Af, Cf, dim=1)   # init = make_const(dt, -INFINITY)\n            T.copy(Cf, C)\n    return main\n\na = torch.rand(M, N, device=\"cuda\")\nref = a.max(1).values\n\n# BUG: fp8 destination -> nvcc \"identifier \\\"inff\\\" is undefined\"\nkernel(\"float8_e4m3\")(a.to(torch.float8_e4m3fn))   # -> CUDA compile error\n\n# CONTROL: identical reduce, float32 destination -> compiles, correct\nc = kernel(\"float32\")(a)\nprint((c - ref).abs().max().item())                # -> 0.0\n```\n\n### Traceback\n\n```\nCompilation error:\ntvm_kernels.cu(17): error: identifier \"inff\" is undefined\n    Cf[0] = fp8_e4_t(-inff/*-inf*/);\ntvm_kernels.cu(20): error: no operator \"=\" matches these operands\n```\n\n### Expected behavior\n\nThe kernel should compile — a max/min reduction over an fp8 fragment is a legal reduction. `float16`/`bfloat16` and `float32`/`float64` destinations already compile the same reduction correctly, because their branches special-case the infinity init (`std::numeric_limits<...>::infinity()` and `CUDART_INF_F`). The fp8/fp4 branch is the one path that emits a raw hexfloat with no inf/nan spelling.\n\nThe precedent for the fix direction is in-repo: [#2671](https://github.com/tile-ai/tilelang/pull/2671) added exactly this guard to the fp8 branch for `float8_e5m2` (emitting the inf/nan bit pattern via `::bitcast`). The same guard is still missing for `float8_e4m3`/`float4`, which fall through to the unguarded hexfloat path.\n\n### Additional context\n\n**Root cause.** The CUDA codegen's float-constant printer does not special-case infinity/NaN for the `float8_e4m3`/`float4` sub-branch, so it prints them as an invalid literal. In [`PrintConst`, the fp8/fp4 branch](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L5776-L5792) the `float8_e5m2` inf/NaN cases are now guarded (`::bitcast(0x7c/0xfc/0x7e)`, the #2671 fix), but any other fp8/fp4 value falls through to `os << '(' << FlexibleHexFormat(op->value) << 'f' ...` unconditionally ([L5789-L5791](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L5789)); for a non-finite value that spelling yields `inf`/`nan`, and the trailing `'f'` produces the undefined identifier `inff`/`nanf`. The sibling [fp16/bf16 branch](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/cuda/codegen/codegen_cuda.cc#L5730-L5752) and the fp32/fp64 branch both guard `std::isinf`/`std::isnan` first.\n\nThe infinity reaches that branch from the reduce lowering: [`MakeInitValue`](https://github.com/tile-ai/tilelang/blob/8001cc4ccf6149382d2019654a19f59c1d4d0482/src/backend/common/op/reduce.h#L207-L231) returns `make_const(dst->dtype, -INFINITY)` for max (L222) and `+INFINITY` for min (L230), in the fp8 destination's dtype. Note `float8_e4m3fn` has no infinity encoding, so for e4m3 a valid init also needs a finite sentinel; e5m2 does have inf and #2671's `::bitcast` approach applies directly.\n\n<details><summary>Reproduced this session (0.1.13, L40S) — got vs control</summary>\n\n```\n# dt=\"float8_e4m3\", input a.to(torch.float8_e4m3fn):\nCOMPILE/RUN EXCEPTION:\n   tvm_kernels.cu(23): error: identifier \"inff\" is undefined\n   emitted:  Cf[0] = fp8_e4_t(-inff/*-inf*/);\n   ORACLE: FAIL\n\n# dt=\"float4_e2m1fn\" (the accepted float4 spelling), same fp8 inputs:\nCOMPILE/RUN EXCEPTION:\n   tvm_kernels.cu(23): error: identifier \"inff\" is undefined\n   emitted:  tl_fp4_packed_store(Cf_packed, 0, fp4_e2_t(-inff/*-inf*/));\n   ORACLE: FAIL\n\n# dt=\"float32\" (control), same fp8 inputs:\nmaxerr 0.0\n   ORACLE: PASS\n\n# dt=\"float8_e5m2\", input a.to(torch.float8_e5m2): the inff crash is FIXED here\n#   (#2671 guard applies) — PrintConst now emits  Cf[0] = fp8_e5_t::bitcast(0xfc);\n#   but the e5m2 reduce still fails to compile with a DISTINCT error\n#   (no operator \"=\" for `Cf[0] = max(...)`), not the inff path.\n# So on 0.1.13 the inff codegen crash is float8_e4m3 and float4_e2m1fn only.\n```\n</details>\n\n**Suggested fix.** Extend the `std::isinf`/`std::isnan` guard that the fp16/bf16 and fp32/fp64 branches already have to the fp8/fp4 branch of `PrintConst` (`codegen_cuda.cc`). #2671 does this for `float8_e5m2` via `::bitcast` to the inf/nan bit pattern; the remaining `float8_e4m3`/`float4` fall-through still needs it. Because e4m3 has no inf encoding, the reduce init/emitted constant would need to be a representable finite sentinel rather than infinity, so this is more than copying the e5m2 branch verbatim.\n\n**Provenance.** The unguarded fp8/fp4 branch of `PrintConst` was introduced with `[Enhancement] Enhance FP8/FP4 type handling in CUDA codegen` ([#323](https://github.com/tile-ai/tilelang/pull/323), 2025-04-05) and has never guarded inf/nan; the current line positions come from the cache-refactor [#780](https://github.com/tile-ai/tilelang/pull/780) (2025-09-02), which relocated but did not change the logic. Present since #323; not a regression.\n\n**Dedup.** Closely related to [#2670](https://github.com/tile-ai/tilelang/issues/2670) (`T.infinity(\"float8_e5m2\")` internal fatal abort), fixed by [#2671](https://github.com/tile-ai/tilelang/pull/2671). That report/fix is about `T.infinity()`, whose frontend `infinity_op` aborts before codegen, and #2671's codegen change only covers `float8_e5m2`. This report is the codegen `PrintConst` path reached by an ordinary `reduce_max`/`reduce_min` init on `float8_e4m3`/`float4`, which #2671 does not fix and which still crashes on current `main`. I found no other open or closed report of the `float8_e4m3`/`float4` codegen crash.\n\n**Reach.** The trigger is `reduce_max`/`reduce_min` (a common reduction idiom) with the reduce *destination fragment* declared as an fp8 dtype. fp8 reductions appear in the shipped fp8 examples (`examples/deepseek_mla/experimental/example_mla_decode_kv_fp8.py:69` does `T.reduce_max`), but there the destination `scores_max` is the fp32 `accum_dtype` (`:43`), so it takes the guarded fp32 branch and dodges the crash — that is why CI is green. **Verified this session (0.1.13):** built that example's kernel verbatim (default `dtype=float16`, `q_dtype=float8_e4m3fn`, `accum_dtype=float32`); it **compiles cleanly** and its infinity init prints as `-CUDART_INF_F` (guarded fp32 branch) — `inff` does not appear anywhere in the emitted source. The example does *not* exercise the bug precisely because the reduce destination is fp32. The bug fires only when the reduce output fragment is itself `float8_e4m3`/`float4` (or, on 0.1.9, `float8_e5m2`); no shipped example or test declares an fp8 reduce destination.\n\n**Impact.** The narrow trigger is a `reduce_max`/`reduce_min` whose destination fragment is declared as an fp8/fp4 dtype (so the infinity init hits the unguarded `PrintConst` sub-branch). When it fires the failure is a loud, deterministic nvcc compile error (`identifier \"inff\" is undefined`) caught at build time — it blocks that one kernel from compiling but corrupts no value and cannot slip into a running workload silently. Fixing it closes the fp8/fp4 non-finite-constant printing class so an fp8-typed reduction compiles like the fp16/fp32 destinations already do.\n\n<details><summary>§17 Generalization record</summary>\n\n**Two-level root.**\n- **SOURCE-level:** the fp8/fp4 sub-branch of `PrintConst` in `src/cuda/codegen/codegen_cuda.cc` (v0.1.13 L5776–L5792) lacks the `std::isinf`/`std::isnan` special-case that every sibling branch (fp16/bf16 at L5730, tfloat32, fp32/fp64) has. After #2671 added an inf/nan `::bitcast` guard *only* for `float8_e5m2`, any *other* fp8/fp4 non-finite value falls through to `os << '(' << FlexibleHexFormat(op->value) << 'f' ...` (L5789), which spells a non-finite value as `inf`/`nan` + trailing `f` = the undefined C++ identifier `inff`/`nanf`.\n- **OPERATOR-level:** any op whose lowering plants a non-finite constant *in an fp8/fp4 dtype* reaches this branch. The only in-tree producer is `MakeInitValue` in `src/backend/common/op/reduce.h` (L207–L231): `reduce_max` → `make_const(dst, -INFINITY)` (L222), `reduce_min` → `+INFINITY` (L230). This `make_const` bypasses the frontend `T.infinity` guard, so reduce is (near-)uniquely the path that gets a non-finite fp8 constant to codegen.\n\n**Found-first:** `reduce_max` into a `float8_e4m3` destination fragment → nvcc `identifier \"inff\" is undefined`. Class: **missing inf/nan guard in a `PrintConst` dtype sub-branch** (codegen missing-case family).\n\n**4-axis sweep** (fresh process each, 0.1.13 `/tmp/tl113_venv`, L40S sm_89):\n\n| axis | cell tested | observed result | same-root? |\n|---|---|---|---|\n| found-first | `reduce_max`, e4m3 dst | `Cf[0] = fp8_e4_t(-inff/*-inf*/);` → `identifier \"inff\" is undefined` | — (the bug) |\n| related-operator | `reduce_min`, e4m3 dst (+INF init) | `Cf[0] = fp8_e4_t(inff/*inf*/);` → `identifier \"inff\" is undefined` | **SAME-ROOT** |\n| related-operator | `reduce_sum`, e4m3 dst (zero init) | compile fails but **NO `inff`** (fails on unrelated fp8 `shfl`/`operator<` infra); zero init dodges the non-finite branch | distinct (discriminator: root is the *infinity init*, not fp8-reduce generally) |\n| related-type | `float4_e2m1fn`, `reduce_max` dst | `tl_fp4_packed_store(..., fp4_e2_t(-inff ))` → `identifier \"inff\" is undefined` | **SAME-ROOT** (L5789 fall-through covers `is_float8() \\|\\| is_float4()`) |\n| related-type | `float8_e5m2`, `reduce_max` dst | `Cf[0] = fp8_e5_t::bitcast(0xfc);` (no `inff`), then a **distinct** `no operator \"=\" matches these operands` | distinct sink (guarded by #2671) |\n| related-type | `float32`, `reduce_max` dst (control) | compiles, `maxerr 0.0` | distinct (guarded fp32 branch) |\n| similar-logic / related-source | `T.fill(Cf, -T.infinity(\"float8_e4m3\"))` (direct non-finite fp8 const, no reduce) | frontend **fatal**: `Cannot decide infinity for type float8_e4m3` — aborts in `infinity_op` *before* codegen | distinct: this is the #2670 frontend face; it does **not** reach `PrintConst`, confirming reduce's internal `make_const` is the entry point that bypasses the frontend guard |\n| related-source (control) | `T.fill(Cf, Cast(e4m3, 1.5))` (finite fp8 const via same fall-through) | compiles cleanly, no error | confirms the L5789 fall-through is correct for *finite* values — only non-finite spelling is broken |\n\n**Example run (Reach).** `examples/deepseek_mla/experimental/example_mla_decode_kv_fp8.py` (the only shipped fp8-reduce example) built verbatim on 0.1.13 → **compiles clean, no `inff`**; its infinity init prints `-CUDART_INF_F` because `scores_max` is fp32 `accum_dtype`. The shipped example genuinely dodges the bug (fp32 reduce destination). No shipped example/test uses an fp8 reduce destination.\n\n**Reframe decision:** kept specific to `reduce_max`/`reduce_min` with fp8/fp4 destination. The class is \"non-finite fp8/fp4 constant reaching `PrintConst`\", but reduce is the *only* reachable producer (the direct `T.infinity` path is walled off by the frontend #2670 abort), so the trigger boundary is genuinely narrow. e4m3 and float4_e2m1fn are the same bug (one fix). e5m2 (distinct, guarded) and the reduce_sum/fp8-`shfl` infra failure are **distinct, not filed**.\n\n**Distinguished from #2670/#2671:** those are the `T.infinity()` frontend `infinity_op` abort plus the e5m2 codegen `::bitcast` guard; this is the e4m3/float4 codegen `PrintConst` fall-through that #2671 left unguarded.\n\n</details>", "url": "https://github.com/tile-ai/tilelang/issues/2998"}
