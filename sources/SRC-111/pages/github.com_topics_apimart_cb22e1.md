source: https://github.com/topics/apimart

You signed in with another tab or window. Reload to refresh your session.You signed out in another tab or window. Reload to refresh your session.You switched accounts on another tab or window. Reload to refresh your session.Dismiss alert

Grok Image API (Grok Imagine 1.5, grok-imagine-1.5-apimart): model ids, per-image pricing, text-to-image and edit routes, moderation behaviour and runnable API examples.

OpenAI-compatible API migration kit: drop-in /v1/images/generations and chat examples, a compatibility matrix, rollback switches and a checklist for moving workloads between AI API gateways.

Seedance 2.5 API (seedance-2.5): per-second pricing by resolution, up to 30-second text-to-video and reference-media workflows, with cURL and Python examples and measured costs.

Grok Imagine API guide: image generation and editing on the grok-imagine routes, per-image pricing, prompt recipes with example renders and async task polling examples.

AI API pricing comparison: per-image, per-second and per-million-token costs for image2.5, Nano Banana 2/Pro, Grok Image, Seedance, GPT-5.5, Claude Opus 5 and DeepSeek, refreshed daily from the public pricing payload.

LLM gateway comparison: self-hosted and managed AI API gateways compared on protocol compatibility, billing units, timeout and retry semantics, idempotency, quotas and observability.

Nano Banana Pro prompts: tested prompt recipes with example renders, aspect ratios, the model id (gemini-3-pro-image-preview) and the cost each render reported.

Midjourney API access: how to generate Midjourney-style images through an API key, with per-call pricing, request examples, queue behaviour and current limits.

Nano Banana Pro API (gemini-3-pro-image-preview): model id, per-image pricing, aspect ratios, resolution tiers, curl, Python and JavaScript examples with async task polling.

AI API proxy SDK for Python: retry with idempotency keys, rate-limit backoff, task polling, cost accounting and typed errors for OpenAI-compatible AI API gateways.

AI API aggregator companion: machine-readable catalog of 300+ models behind one key and one base URL, with routing patterns, cost accounting, OpenAI SDK and curl examples.

Seedance 2.0 API (seedance-2.0 / seedance-2.0-mini / seedance-2.0-fast): per-second pricing by resolution, text-to-video and image-to-video examples, parameters and limits.

DeepSeek 4 API (deepseek-v4-flash / deepseek-v4-pro): model ids, per-million-token pricing, cached-input rates, context limits and OpenAI-compatible API examples.

Claude Opus 5 API (claude-opus-5): model id, per-million-token pricing, cache write and cache read rates, streaming and Anthropic-style requests through an OpenAI-compatible gateway.

OpenRouter alternatives: AI API aggregator and gateway options compared on model coverage, billing units, free tiers, OpenAI compatibility, rate limits and migration effort.

Unified AI API model catalog: machine-readable JSON for 300+ models behind one OpenAI-compatible API, with modality, billing unit, per-image, per-second and per-token pricing and doc links.

DeepSeek 4 API pricing: per-million-token input, cached input and output rates for deepseek-v4-pro and deepseek-v4-flash with worked cost examples for real workloads.

AI API gateway and LLM gateway field notes: routing image2.5 (GPT-Image-2.5) through an OpenAI-compatible base URL, with retry, idempotency, timeout, polling and failover patterns.

GPT-5.5 API (gpt-5.5 / gpt-5.5-pro): model ids, per-million-token pricing, context limits, streaming, tool calling and OpenAI-compatible base URL examples.