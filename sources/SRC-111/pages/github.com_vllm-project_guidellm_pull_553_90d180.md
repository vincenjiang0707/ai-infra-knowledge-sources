source: https://github.com/vllm-project/guidellm/pull/553

# Fixup pylock after dependabot PRs - #553

## Conversation

Signed-off-by: Samuel Monson <smonson@redhat.com>


**approved these changes**

[dbutenhof](https://github.com/dbutenhof)Jan 23, 2026

###
**
**[dbutenhof](https://github.com/dbutenhof)
left a comment

**left a comment**

[dbutenhof](https://github.com/dbutenhof)

There was a problem hiding this comment.

Intriguing ... from what I can tell this looks good.

So what exactly is the GuideLLM `pylock.toml`

story, anyway? I don't see it referenced anywhere in the code base except where it's generated. The format is effectively a stripped-down `uv.lock`

, and, for example, the `Containerfile`

uses `uv sync`

to install dependencies, having mapped `uv.lock`

and `pyproject.toml`

. As I understand it, `pylock.toml`

is an early "evolving" Python standard that *will* replace `requirements.txt`

(and perhaps `uv.lock`

and `poetry.lock`

) over time, but which apparently nobody but `uv`

yet does much of anything with, and which `uv`

really supports only as an export format.

Obviously, since we have it, keeping it up-to-date with `uv.lock`

makes sense, and as it becomes more standard I'd hope that dependabot would support that automatically... but if GuideLLM is actually *doing* anything with it, I didn't find it.

|
Pylock is the PEP standard so we originally added it thinking that more tools would adopt it. The previous package manager ( |

[sjmonson](https://github.com/sjmonson)added a commit that referenced this pull request

Jan 23, 2026

## Summary The workflow in[#553]is failing due to lack of permissions. Rather than spend more time on this fragile hack. Drop the pylock for now. If we see a need later we can revive support. ## Details See[#553 (comment)]--- - [x] "I certify that all code in this PR is my own, except as noted below." ## Use of AI - [ ] Includes AI-assisted code completion - [ ] Includes code generated by an AI application - [ ] Includes AI-generated tests (NOTE: AI written tests should have a docstring that includes `## WRITTEN BY AI ##`)

[Learn more about bidirectional Unicode characters](https://github.co/hiddenchars)

## Summary

Dependabot seems to have recently added support for updating

`uv`

packages with CVEs but it does not update the pylock along with it. This PR adds a CI job to automatically commit a pylock update to dependabot PRs.## Test Plan

Can only be tested once merged

## Use of AI

`## WRITTEN BY AI ##`

)