source: https://github.com/vllm-project/guidellm/discussions/941

You signed in with another tab or window. Reload to refresh your session.You signed out in another tab or window. Reload to refresh your session.You switched accounts on another tab or window. Reload to refresh your session.Dismiss alert

Providing an api key breaks the csv renderer, the provided podman/docker command requires user permissions that shoudl be set via -e user=guidellm, the HTML output is ALWAYS broken, the console-table is lacking width and showing almost all info with an ellipsis...

And I sort of expected it to benchmark ALL models after it queries 'models', but instead if only benchmarks the first.

I do not really see the point of this utility.

I wanted a simple time-to-first-token, tps, prefix benchmark for all my models behind a local endpoint.

guidlellm is probably the completely wrong tool for this...?

reacted with thumbs up emoji reacted with thumbs down emoji reacted with laugh emoji reacted with hooray emoji reacted with confused emoji reacted with heart emoji reacted with rocket emoji reacted with eyes emoji

## Uh oh!

There was an error while loading. Please reload this page.

Providing an api key breaks the csv renderer, the provided podman/docker command requires user permissions that shoudl be set via -e user=guidellm, the HTML output is ALWAYS broken, the console-table is lacking width and showing almost all info with an ellipsis...

And I sort of expected it to benchmark ALL models after it queries 'models', but instead if only benchmarks the first.

I do not really see the point of this utility.

I wanted a simple time-to-first-token, tps, prefix benchmark for all my models behind a local endpoint.

guidlellm is probably the completely wrong tool for this...?

## All reactions