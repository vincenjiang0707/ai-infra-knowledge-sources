# [PR #79532] [AP] Add conv2d + epilogue fusion pattern.

source: https://github.com/PaddlePaddle/Paddle/pull/79532
state: closed | updated: 2026-09-11T09:40:04Z
labels: 

## 正文

<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->
<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->

### PR Category
<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->
CINN

### PR Types
<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->
New features

### Description
<!-- Describe what you’ve done -->
新增`Conv2dEpilogueFusion`，初步支持 conv2d + trivial_fusion 的子图匹配，打通子图匹配和`ap_variadic` 算子替换流程。
本 PR 提交的单测主要测试 pattern 能否成功匹配和替换成 ap_variadic 算子，后端计算直接调用 cutlass 的 conv2d+relu 接口，自动生成 varidic epilogue 由后续 PR 支持。

当前生成的代码如下：
```cpp

// auto generated codes
#include "kernels.h"
#include <vector>

namespace ap {

template <typename T>
struct VariadicEpilogueFunctor {
  struct Arguments {
    // Extents of the output spatial dimensions. The epilogue of an implicit
    // GEMM only knows the GEMM_M index `coord.row`, these are what let the
    // generated statements decompose it back into (n, p, q).
    int P;
    int Q;
    int64_t input0_dim1;
    int64_t input0_dim3;
    const half* in_ptr_0;
  };

  // Note: need to support vectorized operation
  __forceinline__ __host__ __device__
  T operator()(T x, const Arguments& args, const MatrixCoord& coord) const {
    T out;
    float op2_out0 = static_cast<float>(args.in_ptr_0[((coord.row / (args.P * args.Q)) * args.input0_dim1 * args.input0_dim1 * args.input0_dim3 + ((coord.row / args.Q) % args.P) * args.input0_dim1 * args.input0_dim3 + (coord.row % args.Q) * args.input0_dim3 + coord.column)]);
    float op3_out0 = static_cast<float>(x + op2_out0);
    float op4_out0 = static_cast<float>((op3_out0 > 0 ? op3_out0 : 0) );
    out = op4_out0;
    return out;
  }
};

template <int TuningConfigId>
static void RunConv2dWithVariadicKernel(const Conv2dEpilogueParams &params, const half* input0, const half* input1, half* output, int64_t input0_dim0, int64_t input0_dim1, int64_t input0_dim3, int64_t input1_dim1, const half* in_ptr_0) {
  using ElementT = half;
  using ElementComputeT = float;

  typename VariadicEpilogueFunctor<ElementComputeT>::Arguments epilogue_args;
  epilogue_args.P = params.P;
  epilogue_args.Q = params.Q;

  epilogue_args.input0_dim1 = input0_dim1;
  epilogue_args.input0_dim3 = input0_dim3;
  epilogue_args.in_ptr_0 = in_ptr_0;

  // The activation and the filter are both indexed along the channel dimension,
  // while the output is indexed along the output channel dimension.
  constexpr int AlignA = Alignment<ElementT, 16>::kValue;
  constexpr int AlignB = AlignA;
  constexpr int AlignC = Alignment<ElementT, 16>::kValue;

  Conv2dVariadicFusion<ElementT, ElementComputeT, VariadicEpilogueFunctor,
                       AlignA, AlignB, AlignC, TuningConfigId>(params,
                                                              epilogue_args);
}

} // namespace ap

extern "C" {

void Conv2dVariadicKernel(void* stream_ptr, const half* input0, const half* input1, half* output, int64_t input0_dim0, int64_t input0_dim1, int64_t input0_dim3, int64_t input1_dim1, const half* in_ptr_0) {
  // The activation is NHWC (data_format) and the filter is KRSC.
  std::vector<int64_t> input0_shape;
  input0_shape.resize(4);
  input0_shape[0] = input0_dim0;
  input0_shape[1] = input0_dim1;
  input0_shape[2] = input0_dim1;
  input0_shape[3] = input0_dim3;

  std::vector<int64_t> input1_shape;
  input1_shape.resize(4);
  input1_shape[0] = input0_dim3;
  input1_shape[1] = input1_dim1;
  input1_shape[2] = input1_dim1;
  input1_shape[3] = input0_dim3;

  ap::Conv2dEpilogueParams params(stream_ptr,
                                  input0,
                                  input1,
                                  /*bias=*/nullptr,
                                  output,
                                  input0_shape,
                                  input1_shape,
                                  std::vector<int64_t>{},
                                  std::vector<int>{1, 1},
                                  std::vector<int>{1, 1},
                                  std::vector<int>{1, 1},
                                  1);

  ap::RunConv2dWithVariadicKernel<ap::DefaultConfig::kConfigId>(params, input0, input1, output, input0_dim0, input0_dim1, input0_dim3, input1_dim1, in_ptr_0);
}
}

```

#### 支持范围
| 维度 | 首版支持 | 拦截位置 |
|---|---|---|
| 算子 | pd_op.conv2d fprop | source_pattern |
| 设备 | CUDA（`-arch=sm_80`）；DCU/HIP 不支持 | `constraint()` → `compile_command_util.supports("conv2d")` |
| dtype | fp16（已验证）；bf16 代码路径存在但未验证 | 无检查 |
| activation 布局 | `data_format == "NHWC"` | `constraint()` |
| filter 布局 | `KRSC`，依赖 `FLAGS_manually_trans_conv_filter=1` | 运行期 `Conv2dEpilogueParams` 断言（见已知限制）|
| groups | `== 1` | `constraint()` |
| padding | `padding_algorithm == "EXPLICIT"` 且上下/左右对称 | `constraint()` |
| kernel / stride / dilation | 任意（走 `IteratorAlgorithm::kOptimized` + `StrideSupport::kStrided`，`R/S ≤ 32` 时最优） | — |                                            
| epilogue | `ap_trivial_fusion_op`，输入 2 ~ 11、输出；1 ~ 10（`register_conv2d_epilogue_class` 注册），可融性由 `constraint()` 的访存拓扑化简判定 | `constraint()` |

实现上固定的选择：`split_k_slices = 1`；tile 固定 `DefaultConfig::kConfigId`，autotune 未开启；epilogue functor为标量版本，未向量化；AlignA/AlignB 按输入通道 C 取、AlignC 按输出通道 K 取。

#### 验证环境
- GPU / 工具链：A100（sm_80）、CUDA 12.8 + nvcc、gcc 12.3.0、Python 3.10；构建需开启 CINN。
- 端到端单测：`test/ap/test_conv2d_epilogue.py`，NHWC fp16、3×3、padding=1、groups=1，子图为 `conv2d + add + relu`。断言融合发生（PIR 中出现`pd_op.ap_variadic`）并与 Eager 对数值（atol=1e-1）。已覆盖 add 的两种索引形态：per-channel bias（沿 N/P/Q 广播，索引化简为coord.column）和与输出同形的逐元素加（coord.row 反解为 (n,p,q) 后线性化）。
- 运行前置：`FLAGS_manually_trans_conv_filter=True`（filter 走 KRSC）、`FLAGS_deny_cinn_ops="transpose"`（transpose 留在 phi kernel）。

未验证 / 不在本 PR 声明范围：
- DCU/HIP（无环境，已在 pass 层拒绝并有单测）；
- sm_80 以外架构；
- fp32；
- 性能对比与 autotune。

### 是否引起精度变化
<!-- one of the following [ 是 | 否 ]-->
否

@Paddle-Bot 请review。

## 评论 (2)

### paddle-bot[bot] · 2026-07-22

你的PR提交成功，感谢你对开源项目的贡献!
请关注后续CI自动化测试结果，详情请参考[Paddle-CI手册](https://www.paddlepaddle.org.cn/documentation/docs/zh/develop/dev_guides/git_guides/paddle_ci_manual_cn.html)。
Your PR has been submitted. Thanks for your contribution!
Please wait for the result of CI firstly. See [Paddle CI Manual](https://www.paddlepaddle.org.cn/documentation/docs/zh/develop/dev_guides/git_guides/paddle_ci_manual_cn.html) for details.

### Xreki · 2026-09-08

@Paddle-Bot   请重新Review最新提交。

## Review (13)

### risemeup1111 · 2026-07-22 · CHANGES_REQUESTED

我在这轮审查里发现了会阻断 conv2d epilogue fusion 实际生成或运行的问题，具体已放在行级评论里。请先修复这些 P1 问题并补充相应验证后再复查；CI 仍有部分检查未完成，我这轮主要依据代码路径和新增测试审查。

- ![P3](https://img.shields.io/badge/P3-low-blue) **优先级：P3** 非行级：PR 描述目前只有 `RT`，缺少支持范围和验证信息。请补充：支持的 dtype/device、conv2d 参数范围（padding/stride/dilation/groups/data_format）、已运行的测试和实际设备环境。
  **处理要求：请针对该评论进行回复（同意并已修改请回复 Done，不同意请说明理由）。**

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.5 xhigh, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-07-22 · COMMENTED

(no text)

### risemeup1111 · 2026-07-22 · CHANGES_REQUESTED

已复查当前提交。P1 阻断项还没有全部修复：conv2d kernel 入口和编译命令分支的旧线程仍适用，输入数映射问题这次移动到了共享 helper，我已在原线程补充了当前位置。请先修复 P1 项并提交新 commit 后再复查。

- ![P3](https://img.shields.io/badge/P3-low-blue) **优先级：P3** 非行级：PR 描述仍只有 `RT`，缺少支持范围和验证信息。请补充支持的 dtype/device、conv2d 参数范围（padding/stride/dilation/groups/data_format）、已运行的测试和实际设备环境。
  **处理要求：请针对该评论进行回复（同意并已修改请回复 Done，不同意请说明理由）。**

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.5 xhigh, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-07-23 · CHANGES_REQUESTED

已复查新提交。输入数映射问题已经修正；但 conv2d kernel 入口和 conv2d 编译命令分支的 P1 仍未修，本轮还新增了一个 constraint 相关的行级 P1。请先修复这些 P1 并提交新 commit 后再复查，详细内容见行级评论和既有线程。

- ![P3](https://img.shields.io/badge/P3-low-blue) **优先级：P3** 非行级：PR 描述仍只说明部分重构拆到 `#79535`，缺少当前 PR 的实际支持范围和验证信息。请补充支持的 dtype/device、conv2d 参数范围（padding/stride/dilation/groups/data_format）、已运行的测试和实际设备环境。
  **处理要求：请针对该评论进行回复（同意并已修改请回复 Done，不同意请说明理由）。**

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.5 xhigh, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-07-23 · CHANGES_REQUESTED

已复查新提交。输入数映射问题看起来已经修正；但 conv2d kernel 入口、conv2d 编译命令分支和 constraint 放行问题仍未解决，本轮新增的 `__main__.py` import 还会默认加载尚不可运行的 conv2d pass，我已补充新的行级 P1 评论。请先修复这些 P1 并提交新 commit 后再复查。

- ![P3](https://img.shields.io/badge/P3-low-blue) **优先级：P3** 非行级：PR 描述仍只说明部分重构拆到 `#79535`，缺少当前 PR 的实际支持范围和验证信息。请补充支持的 dtype/device、conv2d 参数范围（padding/stride/dilation/groups/data_format）、已运行的测试和实际设备环境。
  **处理要求：请针对该评论进行回复（同意并已修改请回复 Done，不同意请说明理由）。**

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.5 xhigh, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-07-29 · COMMENTED

已复查这次编辑。代码侧的阻断问题仍在既有行级评论里，我这轮没有重复开新线程。

- ![P3](https://img.shields.io/badge/P3-low-blue) **优先级：P3** 非行级：当前描述比之前更具体了，但还是缺少支持范围和验证信息。建议补充支持的 dtype/device、conv2d 参数范围（padding/stride/dilation/groups/data_format）、已运行测试和实际设备环境，避免“初步支持”被理解为已覆盖完整场景。
  **处理要求：请针对该评论进行回复（同意并已修改请回复 Done，不同意请说明理由）。**

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.5 xhigh, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-08-04 · CHANGES_REQUESTED

<!-- nyanpasu-review-board:v1 -->
| 序号 | 位置 | 优先级 | 规则来源 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | [conv2d launcher](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3628147034) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：算子与设备](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |
| 2 | [conv2d 编译命令](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3628149651) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：构建与代码生成](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |
| 3 | [epilogue constraint](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3635754685) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：功能正确性](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |
| 4 | [默认加载 conv2d pass](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3636109361) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：功能正确性](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |
| 5 | [PR 描述与验证范围](https://github.com/PaddlePaddle/Paddle/pull/79532#pullrequestreview-4804390088) | ![P3](https://img.shields.io/badge/P3-low-blue) | [仓库规则：PR 信息与评审评论](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | 🚧 |
| 6 | [conv2d pass 注册](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3746167939) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：功能正确性](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |
| 7 | [DCU backend 能力保护](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3841747126) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：算子与设备](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | 🚧 |
| 8 | [filter 物理布局](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3841752302) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：跨模块契约](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |
| 9 | [padding_algorithm 转发](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3841755457) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：功能正确性](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |
| 10 | [分组卷积能力保护](https://github.com/PaddlePaddle/Paddle/pull/79532#discussion_r3853799705) | ![P1](https://img.shields.io/badge/P1-high-orange) | [仓库规则：算子与设备](https://github.com/PaddlePaddle/Paddle/blob/ae4a6b6370decedd1dd95ca51cb66a647999e886/.agents/skills/ai-review/references/base-rules.md) | ✅ |

当前 head 已修复 launcher、CUDA/DCU 编译映射与头文件路径、epilogue constraint、默认加载、pass 注册、filter 物理布局、`padding_algorithm` 放行约束和分组卷积能力保护；仍需修复 DCU/HIP backend 的可用性保护问题。PR 描述仍缺少明确的 dtype/device、conv2d 参数支持范围和验证环境。

本轮依据当前 head 的精确 diff、既有线程/调用链、`git diff --check` 和 Python 静态编译检查完成静态复核；本地没有可用的 Paddle/GPU runtime，因此未声称运行时数值验证通过。

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.6-sol 默认推理级别, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-08-10 · CHANGES_REQUESTED

(no text)

### risemeup1111 · 2026-08-10 · CHANGES_REQUESTED

当前 head 仅合并了 develop，上一轮列出的未解决问题仍存在：conv2d launcher 为空、conv2d 编译头文件/路径不完整，且 pass 未注册。请按现有行级评论和 Review Board 修复后再复查。


### risemeup1111 · 2026-08-24 · CHANGES_REQUESTED

已复查当前提交。上一轮的 launcher、编译命令/头文件路径、epilogue constraint、默认加载和 pass 注册问题已解决；本轮仍有 3 个新的 P1 阻断项：

- DCU/HIP 分支未提供 `Conv2dAddRelu` 所需的 conv2d backend，命中该 pass 会编译失败。
- filter 仍以 KCRS 物理布局传入按 KRSC 读取的 backend，非 1x1 filter 会读取错误权重。
- `padding_algorithm` 未传入 codegen/backend，`SAME`、`VALID` 和非对称 padding 的输出几何可能错误。

请修复上述问题并补充对应的设备、layout、padding 覆盖后再复查。PR 描述中的支持范围和验证环境说明仍是未解决的 P3。

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.6-sol 默认推理级别, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-08-25 · CHANGES_REQUESTED

已复查当前提交。上一轮的 filter 物理布局和 `padding_algorithm` 问题已通过 channel-last filter 路径与显式对称 padding 约束解决；本轮仍有 2 个 P1 阻断项：

- DCU/HIP 分支仍未提供 `Conv2dAddRelu` 所需的 conv2d backend，命中该 pass 会编译失败。
- `groups > 1` 未被 pattern constraint 拦截，但 `Conv2dEpilogueParams` 明确断言只支持 `groups == 1`，分组卷积命中后会运行时中止。

请修复上述设备与分组卷积能力保护，并补充对应测试后再复查。PR 描述中的支持范围和验证环境说明仍是未解决的 P3。

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.6-sol 默认推理级别, please check the suggestions carefully.</sup>
</div>


### risemeup1111 · 2026-08-25 · COMMENTED

(no text)

### risemeup1111 · 2026-08-25 · CHANGES_REQUESTED

已复查当前提交。`groups == 1` 约束已补齐，分组卷积 P1 已解决；但 DCU/HIP backend 问题仍未解决：当前提交删除了 DCU 的 `conv2d` 编译映射，却没有增加 capability guard，命中该 pass 时 `CompileCommandGenerator("conv2d", ...)` 会直接抛 `KeyError`。

请补齐 HIP conv2d backend，或在 pass/codegen 入口明确拒绝 DCU 后再复查。PR 描述中的支持范围和验证环境说明仍是未解决的 P3。

<div align="right">
   <sup>Powered by Nyanpasu with gpt-5.6-sol 默认推理级别, please check the suggestions carefully.</sup>
</div>

