# [Issue #79618] paddle.i0 prematurely overflows to Inf for finite float64 result

source: https://github.com/PaddlePaddle/Paddle/issues/79618
state: open | updated: 2026-08-17T10:52:30Z
labels: status/new-issue, type/bug-report

## 正文

### bug描述 Describe the Bug

`paddle.i0` prematurely overflows to `Inf` for a finite `float64` input.

For input `713.0`, the modified Bessel function `i0(713.0)` is approximately `6.705128263670996e+307`, which is finite and still within the `float64` range. However, Paddle returns `Inf`.

### Environment

- PaddlePaddle version: 3.3.1
- NumPy version: 2.2.6
- dtype: `float64`
- Device: CPU

### Minimal reproducible code

```python
import numpy as np
import paddle

x = paddle.to_tensor(
    np.asarray([713.0], dtype=np.float64)
)

wrong = paddle.i0(x).numpy()

expected = np.asarray(
    [6.705128263670996e+307],
    dtype=np.float64,
)

print("wrong:", wrong.tolist())
print("expected:", expected.tolist())
```

### Actual behavior

```shell
wrong: [inf]
```

### Expected behavior

```shell
expected: [6.705128263670996e+307]
```

`paddle.i0(713.0)` should return a finite `float64` value close to `6.705128263670996e+307`.

Instead, Paddle prematurely overflows the result to `Inf`.

### 其他补充信息 Additional Supplementary Information

_No response_

## 评论 (1)

### hariomlohardev · 2026-08-17

this is fixed in this pull request https://github.com/numpy/numpy/pull/32223 but not yet merged let the numpy owners know its fixed 
