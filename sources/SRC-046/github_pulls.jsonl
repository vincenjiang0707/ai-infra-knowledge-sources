{"number": 64017, "title": "[ROCm]fix build with Rocm57", "state": "closed", "created_at": "2024-05-01T08:05:31Z", "updated_at": "2026-09-25T02:45:11Z", "closed_at": "2024-05-02T02:27:03Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n Environment Adaptation\r\n\r\n\r\n### PR Types\r\n Bug fixes\r\n\r\n\r\n### Description\r\n适配rocm5.7,修复编译错误\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/64017"}
{"number": 79773, "title": "[Cherry-Pick] Fix 0-D index bug and add deterministic path in gather grad kernel", "state": "open", "created_at": "2026-09-15T09:12:08Z", "updated_at": "2026-09-24T10:45:57Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\nOperator Mechanism\r\n\r\n\r\n### PR Types\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79754\r\n\r\n### 是否引起精度变化\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79773"}
{"number": 79811, "title": "[Cherry-Pick] Support iteration space beyond INT32_MAX in GPU index_elementwise_put kernels(#79810)", "state": "open", "created_at": "2026-09-22T16:30:15Z", "updated_at": "2026-09-24T09:13:26Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\n\nOperator Mechanism\n\n### PR Types\n\nBug fixes\n\n### Description\n\ndev PR: https://github.com/PaddlePaddle/Paddle/pull/79810\n\n`x[index] = value` 这类 advanced indexing 的 setitem 在 GPU 上走 `index_elementwise_put` / `index_elementwise_put_with_tensor` 及其反向。当被索引区域的迭代空间 `numel` 超过 `INT32_MAX` 时：\n\n- 前向对 `N = numel` 做了无条件的 `PADDLE_ENFORCE`，与实际使用的 offset 位宽无关，即使调度端已选 64 位 offset 也会直接报 `the value of N should be in [0, std::numeric_limits<int32_t>::max()]`。实际触发场景是大 padding 率样本下 `input_embeds[indices] = value`，`len(indices) * hidden_size` 越界导致训练中断。\n- 反向没有这个检查，32 位实例化下 `offset_calc.get()` 的形参就是 `uint32_t`，线性下标被静默截断，写入落到错误地址。\n\n改动内容：\n\n- 32 位实例化里发现 `numel > INT32_MAX` 时重新派发到 64 位实例化，位置在构造 offset calculator 之前（32 位的构造函数本身会对越界偏移抛错）。\n- 删掉前向的 `numel` 守卫，改为检查 grid 维度——线性下标放宽到 64 位后真正的硬上限是 `gridDim.x`，且 `dim3` 构造存在 `int64_t` 到 `unsigned int` 的隐式窄化。\n- 反向删掉两处冗余的 `value_grad->numel()` 守卫：`OffsetCalculator` 构造函数（`signed_strides = true` 下）的 `CheckOffsetRange` 已经逐操作数校验偏移是否装得进 offset 类型，且更精确。\n\n本 PR 是 #79810 到 release/3.4 的等价 cherry-pick。#79731 合入后 release/3.4 的 `index_elementwise` 代码已与 develop 对齐（`MakeOffsetCalculatorPut<3, true>`、`CheckOffsetRange`、`IsInInt32Range(..., IndexOperandByteSpan, StridedOperandByteSpan)` 判据均在），因此三个 commit 无冲突原样应用，与 #79810 逐字节一致（仅 `phi::SizeOf` 命名空间限定符按各分支既有风格保留）。\n\n#### 复现\n\n`buf` 取 `[2, 1000000]` float32（8 MiB，字节跨度远在 uint32 内）、索引取长度 `rep` 的 int64 张量、行 1 只出现在索引尾部时：\n\n- `rep = 4075`（迭代空间 4.075e9）正常；\n- `rep = 4100`（4.1e9）在未修复版本上直接 `CUDA error(700) illegal memory access`。\n\n阈值低于 `2**32`：32 位实例化的线性下标走 `IntDivider<unsigned int>::div`（`t = __umulhi(n, m1); return (t + n) >> shift;`），其中 `t + n` 是 32 位加法，对最大 extent `d` 可用的下标上界是 `2**32 * d / 2**ceil(log2 d)`，最坏情况接近 `2**31`。这也是重派发阈值取 `INT32_MAX` 的依据。\n\n### 是否引起精度变化\n\n否。改动只影响下标与偏移的计算位宽，以及守卫条件的作用范围，不改变任何计算逻辑。\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79811"}
{"number": 79810, "title": "[Big Tensor] Support iteration space beyond INT32_MAX in GPU index_elementwise_put kernels", "state": "closed", "created_at": "2026-09-22T16:27:01Z", "updated_at": "2026-09-24T08:42:57Z", "closed_at": "2026-09-24T08:16:24Z", "labels": ["skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T08:16:24Z", "body": "### PR Category\r\n\r\nOperator Mechanism\r\n\r\n### PR Types\r\n\r\nBug fixes\r\n\r\n### Description\r\n\r\n`x[index] = value` 这类 advanced indexing 的 setitem 在 GPU 上走 `index_elementwise_put` / `index_elementwise_put_with_tensor` 及其反向。当被索引区域的迭代空间 `numel` 超过 `INT32_MAX` 时，当前实现有两个问题：\r\n\r\n#### 前向：无条件拒绝\r\n\r\n`index_elementwise_put_kernel.cu` 里对 `N = numel` 做了无条件的 `PADDLE_ENFORCE`，要求 `N <= INT32_MAX`，与实际使用的 offset 位宽无关。因此即使调度端已经选择了 64 位 offset 的实例化，调用一样会失败：\r\n\r\n```\r\nPreconditionNotMetError: the value of N should be in [0, std::numeric_limits<int32_t>::max()]\r\n```\r\n\r\n实际触发场景：大 padding 率样本下 `input_embeds[indices] = value`，`len(indices) * hidden_size` 越过 `INT32_MAX` 直接报错中断训练。\r\n\r\n#### 反向：静默截断\r\n\r\n`index_elementwise_put_grad_kernel.cu` 连这个检查都没有。32 位实例化下 `OffsetCalculator::get()` 的形参类型就是 `uint32_t`，线性下标会被截断且不报错，写入落到错误地址。\r\n\r\n#### 本 PR 的改动\r\n\r\noffset 位宽是调度端按各操作数的字节跨度选的，而字节跨度并不约束迭代空间大小：一个很小的 index 张量广播到大 input 上，跨度全在 int32 以内，但 `numel` 可以远超 `INT32_MAX`。所以：\r\n\r\n- 在 32 位实例化里发现 `numel > INT32_MAX` 时，重新派发到 64 位实例化，而不是报错或截断。放在构造 `OffsetCalculator` 之前，因为 32 位的构造函数本身会对越界偏移抛错。\r\n- 删掉前向的 `numel` 守卫，改为对 grid 维度做检查：线性下标放宽到 64 位后，真正的硬上限是 `gridDim.x`，而且 `dim3` 的构造存在从 `int64_t` 到 `unsigned int` 的隐式窄化，需要显式挡一层。\r\n- 删除反向里两处 `IsInInt32Range(value_grad->numel())` enforce守卫：MakeOffsetCalculatorPut里已经对其逐维检查过\r\n\r\nCPU / XPU 后端的同名守卫本 PR 未改动。\r\n\r\n**精度测试**\r\n```\r\npaddle.Tensor.__setitem__(Tensor([100000, 1024],\"float32\"), Tensor([100000],\"int64\"), Tensor([1024],\"float32\"), )\r\npaddle.Tensor.__setitem__(Tensor([2, 1000000],\"float32\"), Tensor([4100],\"int64\"), Tensor([1000000],\"float32\"), )\r\npaddle.Tensor.__setitem__(Tensor([600000, 4096],\"float32\"), Tensor([600000],\"int64\"), Tensor([4096],\"float32\"), )\r\npaddle.Tensor.__setitem__(Tensor([100000, 1024],\"float32\"), Tensor([100000],\"int64\"), 1, )\r\npaddle.Tensor.__setitem__(Tensor([2, 1000000],\"float32\"), Tensor([5000],\"int64\"), 1, )\r\npaddle.Tensor.__setitem__(Tensor([600000, 4096],\"float32\"), Tensor([600000],\"int64\"), 1, )\r\n```\r\n可以直接复制到cases.txt中，在PaddleAPITest中通过--api_conig_file=\"cases.txt\" + --accuract_stable=true测试精度\r\n前三条覆盖index_elementwise_put_with_tensor，，后三条覆盖index_elementwise_put，内部依次覆盖`纯int32索引`/`跨度小→launch GPUxxxKernel时offsetT=uint32，但 N>INT32_MAX` / `跨度>INT32_MAX`\r\n\r\n\r\n\r\n\r\n### 是否引起精度变化\r\n否\r\n改动只影响下标与偏移的计算位宽，以及守卫条件的作用范围，不改变任何计算逻辑。", "url": "https://github.com/PaddlePaddle/Paddle/pull/79810"}
{"number": 79807, "title": "[FlexCheckpoint] Avoid the full metadata load in the local-resume check", "state": "open", "created_at": "2026-09-22T08:49:16Z", "updated_at": "2026-09-24T08:35:03Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\n\nDistributed Strategy\n\n### PR Types\n\nPerformance\n\n### Description\n\n`dist.load_state_dict` 在 reshard 之前先判断“每张卡能否直接从自己的 `.distcp` 恢复”，只有答案为否才需要 reshard。这个布尔值目前的代价是对 `*.metadata` 做一次完整 `paddle.load` 再构建 `MetadataManager`，因为 `check_resumable_locally` 需要 `storage_metadata`——而它是 `Metadata` 里最大的字段，完全副本下有 `num_keys * world_size` 条记录，真实 154GB checkpoint 上是 227MiB 文件、十几秒 unpickle，全部只为换一个 bool。\n\n改为由两个只读极少字节的读取器回答同一个问题：\n\n- `metadata_reader.load_state_dict_metadata`：`state_dict_metadata` 恰好紧邻 `storage_metadata` 之前被 pickle，在后者的 key opcode 处植入 `STOP`，只读文件前缀即可拿到需要的字段，其后的字节一个都不从磁盘读。不符合该假设时退回完整 `paddle.load`，因此调用永远安全。该函数同时导出为 `paddle.distributed.load_state_dict_metadata`。\n- `distcp_reader.scan_tensor_shapes`：只走 pickle 头部得到本卡文件真实的 `{key: shape}`；payload 长度显式写在 opcode 里，故可直接 `seek` 跳过。\n\n`fast_resumable.check_resumable_locally_fast` 组合两者，是三态的：只有当 checkpoint 里每个张量都整块存放时，张量名才唯一确定其 `LocalTensorIndex`，`storage_metadata` 不携带额外信息，结论可只由本卡文件头得出；一旦有张量被切分，shard 身份依赖仅存在于 `storage_metadata` 的 `global_offset` / `flattened_range`，此时返回 `None`，调用方原样走原检查。\n\n两点使它成为 drop-in 替换：是否适用只取决于共享的 metadata 文件，所有卡在任何通信之前就得到相同判断；适用时恰好发一次 `all_gather_object`、不适用时零次，与原实现的集合通信次数一致。它也不比原检查宽松——校验文件的真实内容而非 metadata 的声明，因此没写完或被截断的 `.distcp`（原检查只做 `os.path.isfile`）会退回 reshard。\n\n由 `load_state_dict(..., fast_resumable_check: bool = True)` 控制，置 `False` 强制走原路径，无需环境变量即可按调用点回退。\n\nrelease/3.4 对应 PR 为 #79787。\n\n### 是否引起精度变化\n\n否。快速路径只替换 `check_resumable_locally` 的布尔判断，张量数据通路完全未动。\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79807"}
{"number": 79787, "title": "[Cherry-Pick][Performance] Add num_workers to paddle.load for parallel payload reading (#79786)", "state": "open", "created_at": "2026-09-17T16:43:25Z", "updated_at": "2026-09-24T08:31:14Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\n\r\nPerformance Optimization\r\n\r\n### PR Types\r\n\r\nPerformance\r\n\r\n### Description\r\n\r\ndevPR:  https://github.com/PaddlePaddle/Paddle/pull/79786\r\n\r\n\r\n本 PR 含两处改动，针对 `dist.load_state_dict` 加载路径上两个不同的瓶颈：\r\n\r\n**一、`paddle.load(num_workers=...)`：并行读取张量数据**（cherry-pick of #79786）\r\n\r\n`paddle.load` 全程单线程搬运张量数据。把 8GB checkpoint 放到 `/dev/shm` 后仍需 6.7s，说明瓶颈不是 IO 而是单线程拷贝：pickle 对张量数据不做任何变换，元信息只占极小比例，耗时几乎全在“把 GB 级字节搬一遍”。\r\n\r\n`protocol >= 4` 下 >= 64KB 的 payload 写在 pickle frame 之外，可以先只读元信息建好完整对象图（此时 numpy 数组已是空缓冲区上的零拷贝视图），再用线程池的 `os.preadv` 把这些 payload 直接读进最终内存，`preadv` 期间释放 GIL 所以能真正并行。pickle 格式与解析逻辑均不变，安全反序列化照常生效。\r\n\r\n默认 `num_workers=1` 即原串行行为。`BytesIO`、macOS、无 `os.preadv`、`protocol == 2`、文件中没有大 payload，以及并行过程中的任何异常，都自动回退串行。\r\n\r\n**二、跳过 local-resume 检查中的全量 metadata 加载**\r\n\r\n`dist.load_state_dict` 在 reshard 前先判断“每张卡能否直接从自己的 `.distcp` 恢复”。这个布尔值目前的代价是对 `*.metadata` 做完整 `paddle.load` 再构建 `MetadataManager`，因为 `check_resumable_locally` 需要 `storage_metadata`——而它是 `Metadata` 里最大的字段，完全副本下有 `num_keys * world_size` 条记录，真实 154GB checkpoint 上是 227MiB 文件、十几秒 unpickle，全部只为换一个 bool。\r\n\r\n改为由两个只读极少字节的读取器回答同一个问题：\r\n\r\n- `metadata_reader.load_state_dict_metadata`：`state_dict_metadata` 恰好紧邻 `storage_metadata` 之前被 pickle，在后者的 key opcode 处植入 `STOP`，只读文件前缀即可拿到需要的字段，其后的字节一个都不从磁盘读。不符合该假设时退回完整 `paddle.load`，因此调用永远安全。该函数同时导出为 `paddle.distributed.load_state_dict_metadata`。\r\n- `distcp_reader.scan_tensor_shapes`：只走 pickle 头部得到本卡文件真实的 `{key: shape}`；payload 长度显式写在 opcode 里，故可直接 `seek` 跳过。\r\n\r\n`fast_resumable.check_resumable_locally_fast` 组合两者，是三态的：只有当 checkpoint 里每个张量都整块存放时，张量名才唯一确定其 `LocalTensorIndex`，`storage_metadata` 不携带额外信息，结论可只由本卡文件头得出；一旦有张量被切分，shard 身份依赖仅存在于 `storage_metadata` 的 `global_offset` / `flattened_range`，此时返回 `None`，调用方原样走原检查。\r\n\r\n两点使它成为 drop-in 替换：是否适用只取决于共享的 metadata 文件，所有卡在任何通信之前就得到相同判断；适用时恰好发一次 `all_gather_object`、不适用时零次，与原实现的集合通信次数一致。它也不比原检查宽松——校验文件的真实内容而非 metadata 的声明，因此没写完或被截断的 `.distcp`（原检查只做 `os.path.isfile`）会退回 reshard。\r\n\r\n由 `load_state_dict(..., fast_resumable_check: bool = True)` 控制，置 `False` 强制走原路径。\r\n\r\n\r\n### 是否引起精度变化\r\n\r\n否。改动一的并行路径读的是同一文件的同一偏移，与串行结果逐位一致；改动二只替换 `check_resumable_locally` 的布尔判断，张量数据通路完全未动。\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79787"}
{"number": 79731, "title": "[Cherry-Pick][Operator Mechanism]Add CUDA small-stride index gradient kernel", "state": "closed", "created_at": "2026-09-02T06:43:43Z", "updated_at": "2026-09-24T08:16:55Z", "closed_at": "2026-09-24T08:16:55Z", "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T08:16:55Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 修复 GPU 高级索引（advanced indexing）反向的三个问题：\r\n\r\n1. 低精度 dtype 下重复下标的累加逐步舍入，`x[:, idx]` 这类表达式的梯度精度明显低于 `x[idx]`；\r\n2. `None`（newaxis）出现在高级索引之前时，索引被绑定到错误的轴，**前向 shape 就是错的**；\r\n3. `index_elementwise_get_grad` 没有校验 `slice_offset` 和被索引视图的跨度，非法值会变成越界写。\r\n\r\n#### 1. 高级索引反向的低精度累加\r\n\r\ntorch 的 `index_put_(accumulate=True)` CUDA 实现完全没有 atomicAdd，只有排序路径，所以它的结果确定、可复现，且重复下标只舍入一次。本节的三处改动合起来把 Paddle 的所有 accumulate 反向也导向这条路径：1.1 放开路由，1.2 补上排序路径缺失的 kernel，1.3 让排序路径能处理放开后新进来的跨步视图。\r\n\r\n##### 1.1 路由判据用错了维度，`x[:, idx]` 落到逐项舍入的 atomicAdd\r\n\r\n**问题**\r\n\r\n`index_elementwise_get_grad` 原来的路由条件是：\r\n\r\n```cpp\r\nif (accumulate && index.size() == 1 && !is_combined) {\r\n  IndexPutWithSortKernel<T, int64_t>(...);\r\n  return;\r\n}\r\n```\r\n\r\n`is_combined` 来自 pybind：`eager_method.cc` 里 `index_size = PyTuple_GET_SIZE(index_ptr)` 是 `__getitem__` 索引元组的**元素个数**（`slice` / `None` / `Ellipsis` 都算），`slice_utils.h` 里 `is_combined = (index_size == 1) ? false : true`。因此真实判据是「索引元组只有一个元素」：\r\n\r\n- `x[idx]` → 走排序 kernel（`IndexingBackwardKernelStride1` / `IndexingBackwardKernel`），重复下标在 `opmath_t`（fp32）寄存器里归约，只在写回时舍入一次；\r\n- `x[:, idx]`、`x[..., idx]`、`x[idx, None]` → `is_combined=true`，落到 `GPUIndexElementwiseGetGrad`，`IndexEleGetGradAccKernel` 内是 `CudaAtomicAdd(reinterpret_cast<T*>(...), T value)`，即 fp16/bf16 原生 atomicAdd，**每加一次就舍回低精度**，误差随重复次数增长，且累加顺序不确定、run-to-run 不可复现。\r\n\r\n触发条件是同一下标重复 **≥3** 次：N=2 时低精度逐步累加恒等于正确舍入（`0+a` 精确，`a+b` 只舍一次），所以必须重复三次以上才会暴露。`[16, 8]` 配 `x[:, idx]`（索引长 5）200 组随机数据统计：\r\n\r\n| 最大重复次数 | fp16 与 torch 不一致率 | bf16 |\r\n| --- | --- | --- |\r\n| 1 | 0/46 | 0/46 |\r\n| 2 | 0/127 | 0/127 |\r\n| 3 | 26/27 | 27/27 |\r\n\r\n**改动**\r\n\r\n路由门从 `accumulate && index.size() == 1 && !is_combined` 改为 `if (accumulate)`（`index_elementwise_get_grad_kernel.cu:724`）。放开之后有两类输入是排序路径原来处理不了的，分别由 1.2、1.3 补上。\r\n\r\n##### 1.2 排序路径缺 `1 < sliceSize <= 32` 的 kernel\r\n\r\n**问题**\r\n\r\n`sliceSize`（被索引轴之后的元素个数）落在 `1 < sliceSize <= 32` 时，即使走到排序路径，也只有 `IndexingBackwardKernelStride1`（仅 `sliceSize == 1`）和按 feature 展开的通用 `IndexingBackwardKernel` 两个选择，后者对每个重复项都做一次 `grad_weight` 的 read-modify-write，同样是每步舍入。torch 在这里有专门的 `indexing_backward_kernel_small_stride`，且它真正的分派维度就是 `sliceSize` 与 warp size 的比较，而不是索引表达式的写法。\r\n\r\n**改动**\r\n\r\n新增 `IndexingBackwardKernelSmallStride`（`:344`），覆盖 `1 < sliceSize <= WARP_SIZE`：一个线程独占一个 feature 列，该下标的所有重复项在 `opmath_t`（fp32）寄存器内归约，写回时才舍入一次。分派处（`:638`）由此按 `sliceSize` 三分：`== 1` 走 `IndexingBackwardKernelStride1`，`<= WARP_SIZE` 走新 kernel，其余走通用 `IndexingBackwardKernel`。\r\n\r\n##### 1.3 排序 kernel 无法寻址跨步视图\r\n\r\n**问题**\r\n\r\n排序 kernel 把 `grad_weight` 当作一块平坦 buffer 寻址；原 `IndexPutWithSortKernel` 自己做 transpose / inverse-perm 并用 `funcs::makeLinearIndex` 构造线性索引，只在「被索引对象就是整块连续 `x_grad`」时成立。1.1 放开路由后，`x[1::2, idx]` 这类先切片再索引也会进来，此时被索引对象是 `x_grad` 内的一块跨步子区域，直接跑排序 kernel 会写到错误位置。\r\n\r\n**改动**\r\n\r\n`IndexPutWithSortKernel` 改为接收一个描述被索引视图的 `SortedPathLayout`（`:401`），并直接用 `funcs::computeLinearIndex` 在该视图上构造线性索引。被索引轴的位置由 layout 给出，且必然是连续的一段，因此 `transposeToFrontAndInvPerm` 那一步在此路径上不再需要，也省掉了 permute 后的 `Contiguous` 与写回时的反向 transpose。\r\n\r\n视图信息的恢复方式（`DeriveSortedPathLayout`，`:437`）：kernel 参数描述的是 **restride 之后**的视图 —— `restride_src()`（`paddle/fluid/pybind/slice_utils.h`）把被索引轴换成广播后的 index shape 并给这些轴**零 stride**，所以 `input_strides` 里连续的一段 0 就标出了被索引块，起点即 `dims_before`；而 `index_dims`（即 `AdvancedIndex::indexed_sizes`）开头正是这些轴的 extent，于是视图在索引之前的 shape 及其在 `x_grad` 内的 stride 都可以还原。据此分三种情况：\r\n\r\n- 视图恰好是整块连续 `x_grad` 时，直接在 `x_grad` 上跑排序 kernel；\r\n- 视图是跨步子区域时（`x[1::2, idx]`），先分配一个与该视图同形的连续 scratch，在其上归约，再用 `StridedCopyKernel` 按 `view_strides` / `view_offset` 搬回 `x_grad` —— 这与 torch 用 `slice_backward` 组合 `index_backward` 的做法一致。`x_grad` 已经清零，所以搬回时无需累加。注意 `phi::StridedCopyKernel` 会覆写 `out` 的 meta，调用方需自行 save / restore；\r\n- `IsNonOverlapping`（`:411`）拒绝自重叠的 `as_strided` 式视图，这类布局回落到原来的 elementwise 路径（精度更低、run-to-run 不确定），与上游行为保持一致。\r\n\r\n`slice_offset` / `index_strides` 是**字节**偏移（`slice_utils.h` 里 `stride * element_size_bytes`），派生 layout 时统一按 `elesize` 换算回元素单位，不能整除即视为非法并回落。\r\n\r\n#### 2. `None` 出现在高级索引之前导致轴绑定错位\r\n\r\n##### 问题\r\n\r\n`None` 位于高级索引之前时，**前向** shape 就与 numpy / torch 不一致：\r\n\r\n| 表达式（`x` 为 `[16, 8]`，`i` 长 3） | numpy / torch | 修改前 Paddle |\r\n| --- | --- | --- |\r\n| `x[None, :, i]` | `(1, 16, 3)` | `(1, 3, 8)` |\r\n| `x[:, None, i]` | `(16, 1, 3)` | `(16, 3, 8)` |\r\n| `x[None, i]` | `(1, 3, 8)` | `(3, 16, 8)` |\r\n| `x[i, None]` | `(3, 1, 8)` | `(3, 1, 8)`（正确） |\r\n\r\n`ParseIndex` 里的 `estimated_dim` 是**基础索引输出张量的轴号**（`slice` 保留 → `++`，整数 → 不 `++`（会 decrease），`Ellipsis` → `+= rank - specified`，bool tensor → `+= rank`），`advanced_index_dim[estimated_dim] = estimated_dim` 用它记录高级索引落在哪个轴。但 `Py_None` 分支只 push `none_axes` 而**不推进 `estimated_dim`**，而 `getTensorWithBasicIndexing` 结尾的 `unsqueeze_ad_func(out, *none_axes)` 确实在输出里插入了这个轴（`none_axes` 已按 `decrease_axis` 修正，就是基础输出的坐标系）。两个坐标系因此错开 `none_count` 个位置，索引被绑到错误的轴上。\r\n\r\n同分支的单 bool（`True` / 0-D bool tensor）处理是对的：它 push `none_axes` 的同时也 `estimated_dim++`，正好印证 `None` 分支缺这一步。\r\n\r\n静态图 `parse_index` 有同构的 bug，且更早暴露：`replace_none` 先把 `None` 从 `indices` 里剥掉，循环根本看不到 `None`，修改前静态图 `x[None, i]` 直接在 `gather_nd` 报 index out of range。\r\n\r\n##### 改动\r\n\r\n- `slice_utils.h` 的 `ParseIndex` `Py_None` 分支补 `estimated_dim++`；\r\n- `eager_method.cc` 两处 `advanced_index_dim` 容量从 `rank * 2` 改为 `rank * 2 + index_size`（setitem 侧为 `+ size`）。否则 rank 2 张量上 `x[None, None, None, None, None, i]` 会让 `estimated_dim` 到 5，发生越界写；\r\n- `variable_index.py` 的 `parse_index` 改成 `_, none_axes = replace_none(indices)` 保留原 `indices`，循环里遇到 `None` 时 `estimated_dim += 1; continue`，并把 `advanced_index` 扩容 `len(indices)`。\r\n\r\n#### 3. `slice_offset` 与视图跨度的越界校验\r\n\r\n##### 问题\r\n\r\n`slice_offset` 是前向记录的「被索引视图在 `x_grad` buffer 内的字节偏移」。排序路径和 elementwise 回落路径都会把它加到 `x_grad` 的基址上，因此一个非法值（负数，或超出 buffer）会直接变成越界写，而不是报错。\r\n\r\n同样地，跨步视图是**稀疏**的，它触达的最远位置由跨度决定而不是元素个数决定，只检查 `numel <= grad_numel` 不足以保证不越界。\r\n\r\n##### 改动\r\n\r\n- 在 `IndexElementwiseGetGradKernel` 入口校验 `0 <= slice_offset < x_grad 字节数`；\r\n- 在 `DeriveSortedPathLayout` 里校验视图触达的最后一个位置 `slice_offset / elesize + Σ (view_dims[k] - 1) * view_strides[k] < x_grad->numel()`，报错信息带上 offset、view shape 和 strides，便于定位。\r\n\r\n#### 修改后的行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| fp16/bf16 `x[:, idx]` 等重复下标反向 | 每个重复项舍入一次，误差随重复次数增长，顺序不确定 | fp32 寄存器内归约，写回舍入一次，与 torch 逐位一致 |\r\n| `1 < sliceSize <= 32` 的排序路径 | 走按 feature 展开的通用 kernel，逐项 read-modify-write | 新增 small-stride kernel，单线程独占一列 |\r\n| 先切片再高级索引（`x[1::2, idx]`） | 走 elementwise atomicAdd 回落 | 连续 scratch 归约后按视图 stride 搬回 |\r\n| `x[None, :, i]` 等 `None` 在前 | 前向 shape 错、`__setitem__` 多数直接报 broadcast 失败、静态图 `gather_nd` 越界 | 与 torch 一致 |\r\n| 多个连续 `None` | `advanced_index_dim` 可能越界写 | 容量按 `index_size` 扩展 |\r\n| 非法 `slice_offset` / 越界视图 | 静默越界写 | 明确报错 |\r\n\r\n自重叠的 `as_strided` 式视图仍回落到原 elementwise 路径，行为与之前相同。\r\n\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79716\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是", "url": "https://github.com/PaddlePaddle/Paddle/pull/79731"}
{"number": 79809, "title": "[Cherry-Pick] fix illegal blockDim.z in GetGpuLaunchConfig3D (#79808)", "state": "closed", "created_at": "2026-09-22T16:19:55Z", "updated_at": "2026-09-24T06:36:58Z", "closed_at": "2026-09-24T06:36:58Z", "labels": ["skip-ci: api-benchmark"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T06:36:58Z", "body": "### PR Category\r\nOperator Mechanism\r\n\r\n### PR Types\r\nBug fixes\r\n\r\n### Description\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79808\r\n\r\n`GetGpuLaunchConfig3D` 计算 `block_z` 时只受总线程预算 `max_threads`（= 256）约束，没有受硬件单维度上限 `maxThreadsDim[2]`（= 64）约束。当输出空间尺寸退化（`out_h * out_w <= 3`，即 `block_x * block_y < 4`）时，剩余线程预算全部落到 z 维，使 `blockDim.z` 达到 128 或 256，驱动在 launch 时拒绝启动并返回 `cudaErrorInvalidValue`。\r\n\r\n影响 `interpolate` 的 `nearest` / `bilinear` NCHW 前向与 `bilinear` NCHW 反向（`interpolate_kernel.cu`、`interpolate_grad_kernel.cu`）。\r\n\r\n该错误非 sticky，`cudaDeviceSynchronize()` 探不到，只会在下一次 `cudaGetLastError()` 抛出，因此 traceback 会指向紧随其后的无关算子（例如 `rms_norm` 的预检），排查时极具误导性。\r\n\r\n修复：把 `block_z` 钳制到 64。总线程数上限（1024）原本已由 `max_threads` 预算保证，无需额外限制。\r\n\r\n最小复现：\r\n\r\n```python\r\nimport paddle\r\nimport paddle.nn.functional as F\r\n\r\nx = paddle.zeros([1, 768, 52, 52]).astype(\"float32\")\r\ny = F.interpolate(x, size=[1, 3], mode=\"bilinear\", align_corners=False)\r\npaddle.base._check_last_cuda_error()  # 此处抛出 cudaErrorInvalidValue\r\n```\r\n\r\n详细分析见 #79808。\r\n\r\n### 是否引起精度变化\r\n否\r\n\r\n`blockDim.z` 只影响线程数分配，kernel 的 grid-stride 循环仍覆盖全部输出元素，计算结果与修复前一致。\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79809"}
{"number": 79808, "title": "[Operator Mechanism] fix illegal blockDim.z in GetGpuLaunchConfig3D", "state": "closed", "created_at": "2026-09-22T16:18:26Z", "updated_at": "2026-09-24T05:59:46Z", "closed_at": "2026-09-24T05:59:46Z", "labels": ["skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-24T05:59:46Z", "body": "### PR Category\r\nOperator Mechanism\r\n\r\n### PR Types\r\nBug fixes\r\n\r\n### Description\r\n\r\n`GetGpuLaunchConfig3D` 在计算 `block_z` 时，只受「总线程预算」`max_threads`（= 256）约束，没有受硬件「单维度上限」`maxThreadsDim[2]`（= 64）约束。\r\n\r\n当输出空间尺寸退化时（`out_h * out_w <= 3`，即 `block_x * block_y < 4`），整个剩余线程预算会被推到 z 维，使 `blockDim.z` 达到 128 或 256，超过 64。驱动在 launch 时校验 block 维度并直接拒绝启动，返回 `cudaErrorInvalidValue`。\r\n\r\n受影响的调用点：\r\n\r\n- `paddle/phi/kernels/gpu/interpolate_kernel.cu` 的 `nearest` NCHW 前向（`GetGpuLaunchConfig3D` 调用处）\r\n- `paddle/phi/kernels/gpu/interpolate_kernel.cu` 的 `bilinear` NCHW 前向\r\n- `paddle/phi/kernels/gpu/interpolate_grad_kernel.cu` 的 `bilinear` NCHW 反向\r\n\r\n最小复现（纯单算子，无需端到端训练）：\r\n\r\n```python\r\nimport paddle\r\nimport paddle.nn.functional as F\r\n\r\n# NCHW, float32, 输出空间退化为 1x3\r\nx = paddle.zeros([1, 768, 52, 52]).astype(\"float32\")\r\ny = F.interpolate(x, size=[1, 3], mode=\"bilinear\", align_corners=False)\r\npaddle.base._check_last_cuda_error()  # 此处抛出 cudaErrorInvalidValue\r\n```\r\n\r\n实测触发边界（`n * c = 768`）：`out_h * out_w <= 3` 必崩，等价于 `(out_h == 1 && out_w <= 3) || (out_w == 1 && out_h <= 3)`，且要求 `n * c > 64`。例如 `(1,1)`、`(1,2)`、`(1,3)`、`(2,1)`、`(3,1)` 崩溃，而 `(1,4)`、`(4,1)`、`(2,2)`、`(1,63)`、`(42,63)` 正常。\r\n\r\n修复方式：\r\n\r\n把 `block_z` 钳制到硬件 z 维上限 64。\r\n\r\n需要说明的是，**总线程数上限（1024）本来就已经满足**，无需额外限制：\r\n\r\n```\r\nblock_x * block_y * block_z <= block_x * block_y * floor(max_threads / (block_x * block_y))\r\n                             <= max_threads\r\n                             = min(256, maxThreadsPerBlock)\r\n                             <= maxThreadsPerBlock\r\n```\r\n\r\n即 `max_threads` 本身已经用 `GetMaxThreadsPerBlock()` 兜过底，缺的只是`block.z`的**单维度**上限这一条。\r\n\r\n正确性：\r\n\r\nkernel 通过 grid-stride 循环覆盖 `n * c`（`nc_stride = blockDim.z * gridDim.z`），`blockDim.z` 变小只会让 `gridDim.z` 相应变大（`DivUp(num_img, blockDim.z * 4)`），覆盖范围不变。\r\n\r\n### 是否引起精度变化\r\n否\r\n\r\n`blockDim.z` 只影响线程数分配，kernel 的 grid-stride 循环仍覆盖全部输出元素，计算结果与修复前完全一致。\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79808"}
{"number": 79813, "title": "[Fsdp] support fp8  and fused linear", "state": "open", "created_at": "2026-09-24T03:08:55Z", "updated_at": "2026-09-24T03:24:32Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nDistributed Strategy\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n[Fsdp] support fp8  and fused linear\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79813"}
{"number": 79785, "title": "[Cherry-Pick] Add deterministic path for take_along_axis_grad cuda kernel", "state": "open", "created_at": "2026-09-17T12:51:58Z", "updated_at": "2026-09-24T03:19:23Z", "closed_at": null, "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\nOperator Mechanism\r\n\r\n### PR Types\r\nOthers\r\n\r\n### Description\r\n<!-- Describe what you've done -->\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79753\r\n\r\n### 是否引起精度变化\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79785"}
{"number": 79812, "title": "[Cherry-Pick][compat] Resolve a scoped spec once instead of re-entering it", "state": "open", "created_at": "2026-09-23T02:51:43Z", "updated_at": "2026-09-23T10:52:32Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\nPerformance Optimization\n\n### PR Types\nPerformance\n\n### Description\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79798 (merged 2026-09-22, merge commit `7c722e5509`)\n\nCherry-pick of the above onto `release/3.4`.\n\n`TorchProxyMetaFinder._find_spec_for_specific_module` looks up the real spec with `importlib.util.find_spec` while the proxy is disabled, which is what is meant to keep the finder out of its own lookup. That only holds while the finder is registered **once**: `enable_torch_proxy` inserts it into `sys.meta_path` unconditionally, so nested guards leave duplicates behind, while `disable_torch_proxy` drops a single occurrence. With a duplicate left in place the lookup re-enters the finder for the very same name and restarts the whole resolution, and a name that has no spec at all never converges, so the lookups compound.\n\nMeasured with a probe for a missing submodule of a package in the enabled scope:\n\n| finder registrations in `sys.meta_path` | 1 | 2 | 3 | 4 |\n| --- | --- | --- | --- | --- |\n| lookups per probe, before | 1 | 4 | 15 | 64 |\n| lookups per probe, after | 1 | 2 | 3 | 4 |\n\nEvery lookup toggles the proxy, and every toggle walks all of `sys.modules` to move `torch*` in and out of the shadow table, so the cost is `O(lookups * len(sys.modules))` -- it also grows as the program imports more. Importing one package inside a scoped guard with 7 registrations already in place expanded the probes for a missing module into 13699 lookups and ~90M module-name comparisons: **37.9s, of which 0.5s was the actual import work**.\n\n**Fix**: suppress the finder for the name it is currently resolving, so the outer lookup finishes instead of starting over. Each registered copy is now asked exactly once. The proxy is still disabled around the lookup and `exec_module` still runs under its own guard, so the resulting specs, loaders and `__enable_torch_proxy__` marks are unchanged.\n\n### Adaptations for `release/3.4`\n\n- The guard is named `use_torch_proxy_guard` on this branch (not `use_compat_guard`), so the one conflicting line keeps this branch's name; the new test uses `paddle.compat.use_torch_proxy_guard` accordingly.\n- `test/compat/fake_modules/torch_proxy_local_enabled_package/` does not exist on this branch, so it is brought in together with the test that needs it (two small files).\n\nThe fix itself is identical to the develop commit: `import threading`, `_resolving` / `_resolving_names` on the finder, the early `return None` in `find_spec`, and add/discard around `importlib.util.find_spec`.\n\n### Verification\n\nThe `proxy.py` on this branch matches the released 3.x wheel (both use `use_torch_proxy_guard`), so the same patch could be applied to an installed paddle and the new test run against it for real:\n\n```\nnew test, patch on :  2 tests, OK\nnew test, patch off:  2 tests, FAILED   (AssertionError: 3 != 1 / 4 != 2)\n```\n\nFull `test/compat/test_torch_proxy.py`, A/B on the same interpreter:\n\n```\npatch off: run=13  failures=5   <- includes the 2 new ones\npatch on : run=13  failures=3   <- the 2 new ones now pass\n```\n\nThe 3 remaining failures are identical with the patch on and off, i.e. unrelated to this change (they come from running this branch's test file against a released wheel on a machine that also has real PyTorch installed).\n\nEquivalence, measured on a package that loads 51 sonicmoe submodules under a scoped guard: identical submodule set, identical set of modules marked with `__enable_torch_proxy__` (220), same `quack` module object before and after, `quack.gemm_interface.gemm_symmetric` still importable.\n\nEnd to end: 8-node x 8-GPU pretraining ran 216 steps with this patch applied to the venv's paddle, loss 12.2834 -> 6.3311, memory flat, no import-related errors. `import paddlefleet_ops` in the same interpreter: **4.89s -> 3.01s** (A/B by disabling only the reentrancy bookkeeping, so the 1.88s is attributable to it alone).\n\n### 是否引起精度变化\n否\n\nOnly import-time module resolution changes. No kernel, no computation and no reduction order is touched.\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79812"}
{"number": 79777, "title": "Add compatibility for CUB/CCCL 3.0+ API", "state": "open", "created_at": "2026-09-15T09:31:23Z", "updated_at": "2026-09-23T08:45:18Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\nEnvironment Adaptation\r\n\r\n### PR Types\r\nOthers\r\n\r\n### Description\r\n\r\nUpgrade the vendored CUB/CCCL from the 2.2.0-dev snapshot (`1f6e4bca`) to **CCCL v3.1.0** on CUDA >= 12.8, and adapt the tree so it still compiles against CCCL 2.x.\r\n\r\n**Motivation**\r\n\r\n- `cumsum` on the deterministic path (used to match torch bit-wise) reduces with `cub::BlockScan::Sum` / `BlockReduce::InclusiveSum`. torch built with cu130 uses CCCL 3.0, and the reduction order of these primitives differs between CUB 2.2 and 3.0, which surfaces as ULP-level differences against torch.\r\n- CUDA 13 ships CCCL 3.x, and a CUDA 13 toolchain cannot be paired with CCCL 2.2; NVIDIA supports only the latest release.\r\n- CCCL 3.x also carries Blackwell (sm_100+) tuning that 2.2 does not have.\r\n\r\n**Changes**\r\n\r\n1. Version gate — `cmake/external/cccl.cmake`\r\n   - CUDA < 12.8(temporarily) keeps the old fix commit `1f6e4bca` (behaviour unchanged).\r\n   - CUDA >= 12.8(temporarily) checks out `v3.1.0` and applies `patches/cccl/util_device.cuh.v3.1.0.patch`.\r\n   - The tag and its patch are selected together through `CCCL_PATCH_FILE`.\r\n\r\n2. Compatibility shim — `paddle/phi/kernels/funcs/cub.h` (new)\r\n   - The single place that holds the CCCL 2.x/3.x `#if`, keyed on `CUB_VERSION >= 300000`.\r\n   - Restores the symbols removed in CCCL 3.0 into `namespace cub` so all call sites keep the `cub::` spelling (which also lets HIP keep using rocPRIM-compatible iterators):\r\n     - `cub::Sum` / `Equality` / `Max` -> `cuda::std::plus<>` / `equal_to<>` / `cuda::maximum<>`\r\n     - `cub::TransformInputIterator` / `CountingInputIterator` -> thrust iterators\r\n     - `cub::LaneId()` / `LaneMaskLt()` -> `cuda::ptx::get_sreg_*`\r\n   - Also hosts the `phi::float16` / `bfloat16` CUB `NumericTraits` registration and the `cuda::std::numeric_limits` / `cuda::is_floating_point_v` specializations that CCCL 3.x requires, so they are defined once instead of being duplicated per kernel.\r\n\r\n3. Call-site adaptations\r\n   - `block_radix_topk.cuh`: `cub::CTA_SYNC()` -> `__syncthreads()` (16 sites; CUDA builtin, available on old and new, no guard needed).\r\n   - `argsort_kernel.cu` / `argsort_grad_kernel.cu` / `top_k_function_cuda.h`: `cub::BaseTraits` 5-param -> 4-param, guarded by `CUB_VERSION`; the half/bfloat16 traits moved to `funcs/cub.h`.\r\n   - `shuffle_batch.cu.h`: `__thrust_exec_check_disable__` -> `_CCCL_EXEC_CHECK_DISABLE`.\r\n   - `tensorrt/plugin/common/common.cuh`: include `funcs/cub.h` instead of `<cub/cub.cuh>`..\r\n\r\n**Why not use the toolchain's own CUB/CCCL**\r\n\r\n`WITH_SHARED_PHI` compiles multiple .so, and the CUB headers need their `inline` replaced by `__forceinline__` so the same symbol does not end up at different addresses across the .so. That requires patching the headers, and the toolkit headers are read-only, so a patched private copy is needed.\r\n\r\n\r\n### 是否引起精度变化\r\n是", "url": "https://github.com/PaddlePaddle/Paddle/pull/79777"}
{"number": 79799, "title": "[Operator Mechanism] Fix topk returning NaN for an empty reduction axis", "state": "open", "created_at": "2026-09-22T03:17:39Z", "updated_at": "2026-09-23T06:04:07Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 修复 `topk` 在**规约轴长度为 0** 且 `k >= 1` 时静默返回全 `NaN` 的问题，覆盖 CPU / GPU / XPU 三个 `TopkKernel`（`topk` 与 `topk_v1` 共用同一实现）。核心问题和结果可以概括为：\r\n\r\n1. **空规约轴被短路成 NaN 输出**：当输入在 topk 轴上的长度为 0（如 `paddle.empty([1024, 0])` 沿 `axis=-1` 取 `k=10`）时，输出在该轴上的期望长度为 `k >= 1`，输出并非空张量。三个 kernel 在真正的 `k` 越界校验（`PADDLE_ENFORCE_GE(x.numel(), k)`）之前，先用 `x.numel() == 0` 短路，直接把输出填成 `NaN`、索引填成 `0` 并返回，导致非法请求被静默接受、返回无意义结果，而不是报错。\r\n2. **与 torch 行为不一致**：torch 对同样的输入抛出 `selected index k out of range`。Paddle 前向的这类\"轴无法提供 k 个元素\"应当报错，而不是产出 NaN。\r\n3. **合法空张量不受影响**：真正合法的空输出（如 `paddle.empty([0, 5])` 沿 `axis=-1` 取 `k=3`，输出形状 `[0, 3]`、`numel == 0`）走的是更早的 `out->numel() == 0` 空输出分支，本 PR 不触碰该路径。\r\n\r\n#### 1. 空规约轴静默返回 NaN\r\n\r\n##### 问题\r\n\r\n三个 kernel 的开头顺序都是：先处理\"输出为空\"（`out->numel() == 0`）与 0-D 输入，再处理 `x.numel() == 0`，最后才做 `k` 的范围校验。问题出在 `x.numel() == 0` 这一分支：\r\n\r\n```python\r\nimport paddle\r\n# 规约轴长度为 0，k>=1：输出期望形状 [1024, 10]，非空\r\nx = paddle.empty([1024, 0], dtype='float32')\r\nv, i = paddle.topk(x, 10, axis=-1)\r\n# 期望：抛 InvalidArgument（对齐 torch \"selected index k out of range\"）\r\n# 实际（修复前）：v 为形状 [1024, 10] 的全 NaN，无任何报错\r\n```\r\n\r\n能走到 `x.numel() == 0` 这一步，说明输出已通过前面的 `out->numel() == 0` 判断（即输出非空）。输入为空而输出非空，只可能是 topk 轴长度为 0——此时该轴根本无法提供 `k >= 1` 个元素，属于非法请求。旧实现却把它当成\"空输入\"用 `Full(NAN)` 兜底：\r\n\r\n```cpp\r\nif (x.numel() == 0) {\r\n  Full<T, Context>(dev_ctx, out->dims(), NAN, out);\r\n  Full<int64_t, Context>(dev_ctx, indices->dims(), 0, indices);\r\n  return;\r\n}\r\n```\r\n\r\nCPU kernel 的 `FullTopK` 里其实有正确的 `PADDLE_ENFORCE_LE(k, input_width)`，但对空输入这条分支到不了；GPU/XPU 也在该分支之后才有 `k` 的范围校验。三处是同一根因。\r\n\r\n##### 改动\r\n\r\n- 把 CPU / GPU / XPU 三个 `TopkKernel` 中 `x.numel() == 0` 的 NaN 兜底分支替换为显式报错：\r\n\r\n```cpp\r\nif (x.numel() == 0) {\r\n  // Reaching here with a non-empty output implies the topk axis has size 0,\r\n  // which cannot supply the requested k (>=1) elements. Align with the\r\n  // \"selected index k out of range\" semantics instead of returning NaN.\r\n  PADDLE_THROW(errors::InvalidArgument(\r\n      \"topk cannot select k = %d elements from an axis of size 0 \"\r\n      \"(selected index k out of range).\",\r\n      static_cast<int>(k)));\r\n}\r\n```\r\n\r\n- 该分支位于\"输出为空\"早返回之后，因此只拦截\"输入空 + 输出非空\"这一必然非法的组合；合法的空输出请求在更早的分支已经返回，不受影响。\r\n- `topk_v1` 复用 `TopkKernel`，无需单独改动。\r\n\r\n##### 修改后结果\r\n\r\n- 规约轴长度为 0 且 `k >= 1` 的请求在进入计算前明确抛出 `InvalidArgument`，与 torch 的 `selected index k out of range` 语义一致；\r\n- 不再返回全 `NaN` 的\"看似成功\"结果；\r\n- CPU / GPU / XPU 三个后端行为统一。\r\n\r\n### 修改后的总体行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| 规约轴长度为 0 且 k>=1（输出非空） | 静默返回全 NaN，不报错 | 抛 `InvalidArgument`，对齐 torch |\r\n| 合法空输出（如 `[0,5]` 沿 `axis=-1` 取 `k=3`，输出 `[0,3]`） | 返回空张量 | 返回空张量（不变） |\r\n| 0-D 输入 | 原样拷贝、索引置 0 | 原样拷贝、索引置 0（不变） |\r\n| 非空且 k 合法 | 正常 topk | 正常 topk（不变） |\r\n\r\n合法输入的结果不变；修改只影响原本被静默接受、返回 NaN 的非法边界请求。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79799"}
{"number": 79800, "title": "[Operator Mechanism] Add K-dim consistency check to matmul_grad InferMeta", "state": "open", "created_at": "2026-09-22T03:19:08Z", "updated_at": "2026-09-23T05:57:19Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 修复 `matmul_grad` 反向算子**不校验收缩维（K 维）一致性**导致的前后向契约不一致问题。前向 `matmul` 在 `MatmulInferMeta` 中会强制要求 X、Y 的收缩维相等，但反向 `matmul_grad` 复用了通用的 `GeneralBinaryGradInferMeta`，只把 X、Y 的 meta 原样共享给 `dx`、`dy`，跳过了这道校验，于是前向会拦截的 K 维不匹配输入，反向却照单全收。核心问题和结果可以概括为：\r\n\r\n1. **前向校验、反向不校验**：`GeneralBinaryGradInferMeta` 只做 `dx->share_meta(x)`、`dy->share_meta(y)`，没有任何形状约束。前向被 `PADDLE_ENFORCE_EQ(K_lhs, K_rhs)` 拒绝的 K 维不匹配组合，直接单独调用私有反向算子 `paddle._C_ops.matmul_grad` 时不会报错。\r\n2. **新增专用 InferMeta**：新增设备无关的 `MatmulGradInferMeta`，复刻前向的收缩维一致性检查，同时保留原有的 `share_meta` 行为。\r\n3. **覆盖全部 dense 反向路由**：`matmul_grad` 在动态图、PIR 静态图、legacy 静态图三套 yaml 中各有定义，另有同根因的 `legacy_matmul_grad`（前向 `legacy_matmul` 同样走 `MatmulInferMeta`）。四条 dense 路径全部改为路由到 `MatmulGradInferMeta`；稀疏 `matmul_grad` 无转置参数、属独立算子族，保持不动。\r\n\r\n#### 1. matmul_grad 缺失 K 维一致性校验\r\n\r\n##### 问题\r\n\r\n前向 `MatmulInferMeta`（`paddle/phi/infermeta/binary.cc`）按 `transpose_x/transpose_y` 定位 X、Y 的收缩维并强制相等：\r\n\r\n```cpp\r\nconst int64_t K_lhs = dims_x[lhs_reduce_dim];\r\nconst int64_t K_rhs = dims_y[rhs_reduce_dim];\r\nif (config.is_runtime || (K_rhs != -1 && K_lhs != -1)) {\r\n  PADDLE_ENFORCE_EQ(K_lhs, K_rhs, ...);\r\n}\r\n```\r\n\r\n反向 `matmul_grad` 的 `infer_meta` 却指向通用的 `GeneralBinaryGradInferMeta`（`paddle/phi/infermeta/backward.cc`）：\r\n\r\n```cpp\r\nvoid GeneralBinaryGradInferMeta(const MetaTensor& x, const MetaTensor& y,\r\n                                MetaTensor* dx, MetaTensor* dy) {\r\n  if (dx) dx->share_meta(x);\r\n  if (dy) dy->share_meta(y);\r\n}\r\n```\r\n\r\n它只抄形状、不校验收缩维，于是前向会拦的 K 维不匹配输入，反向不拦：\r\n\r\n```python\r\nimport paddle\r\n# 前向 matmul([1024,0], [4096,64])：X 末维 0 != Y 首维 4096 -> 前向抛 InvalidArgument\r\n# 反向直接单调：不报错，照样返回 dx=[1024,0]、dy=[4096,64]\r\npaddle._C_ops.matmul_grad(\r\n    paddle.empty([1024, 0]), paddle.empty([4096, 64]),\r\n    paddle.empty([1024, 64]), False, False)\r\n```\r\n\r\ntorch 对复原出的前向组合同样一律 `RuntimeError: mat1 and mat2 shapes cannot be multiplied`。这是 Paddle 自身前后向契约不一致：前向校验 K 维，反向 `InferMeta` 漏了这道校验。\r\n\r\n> 说明：`matmul_grad` 是私有反向算子，正常训练中它只会拿到已被前向校验过的合法 shape，K 维不匹配不会自然出现；这些用例是测试直接单独调用反向算子构造的合成输入。本 PR 的价值在于\"前后向契约一致\"与对齐 torch，而非修复实际训练路径的 bug。\r\n\r\n##### 改动\r\n\r\n- 在 `paddle/phi/infermeta/backward.cc` 新增设备无关的 `MatmulGradInferMeta`，复刻前向的收缩维定位与一致性检查，并保留原有 `share_meta`：\r\n\r\n```cpp\r\nvoid MatmulGradInferMeta(const MetaTensor& x, const MetaTensor& y,\r\n                         bool transpose_x, bool transpose_y,\r\n                         MetaTensor* dx, MetaTensor* dy, MetaConfig config) {\r\n  std::vector<int64_t> dims_x = common::vectorize(x.dims());\r\n  std::vector<int64_t> dims_y = common::vectorize(y.dims());\r\n  const auto ndims_x = dims_x.size();\r\n  const auto ndims_y = dims_y.size();\r\n  if (ndims_x > 0 && ndims_y > 0) {\r\n    const int64_t lhs_reduce_dim = (ndims_x == 1) ? 0 : ndims_x - 1 - transpose_x;\r\n    const int64_t rhs_reduce_dim = (ndims_y == 1) ? 0 : ndims_y - 2 + transpose_y;\r\n    const int64_t K_lhs = dims_x[lhs_reduce_dim];\r\n    const int64_t K_rhs = dims_y[rhs_reduce_dim];\r\n    if (config.is_runtime || (K_rhs != -1 && K_lhs != -1)) {\r\n      PADDLE_ENFORCE_EQ(K_lhs, K_rhs, common::errors::InvalidArgument(...));\r\n    }\r\n  }\r\n  if (dx) dx->share_meta(x);\r\n  if (dy) dy->share_meta(y);\r\n}\r\n```\r\n\r\n- 在 `paddle/phi/infermeta/backward.h` 补充对应声明（末尾 `MetaConfig config = MetaConfig()`，由代码生成自动追加）。\r\n- 采用 `ndims_x > 0 && ndims_y > 0` 的守卫，避免对空维索引越界；`config.is_runtime` 与 `-1` 判断与前向保持一致，兼容静态图未知维。\r\n- 收缩维一致性检查天然允许 K 维为 0 的合法零尺寸反向（如 `[M,0]·[0,N]`），仅拒绝 K 维不匹配。\r\n\r\n##### 修改后结果\r\n\r\n- 直接调用 `matmul_grad` 时，K 维不匹配输入在进入 kernel 前明确抛出 `InvalidArgument`，与前向 `matmul` 及 torch 一致；\r\n- 校验点在设备无关的 `InferMeta`，一处覆盖 CPU / GPU / oneDNN / XPU 全部后端，静态图编译期即可拦截；\r\n- 合法（K 一致，含 K=0）的反向计算行为不变。\r\n\r\n#### 2. 覆盖全部 dense 反向路由\r\n\r\n##### 问题\r\n\r\n`matmul_grad` 的 `infer_meta` 路由分散在多套 yaml 中，若只改其中一处，其余执行模式仍不拦：\r\n\r\n- `paddle/phi/ops/yaml/inconsistent/dygraph_backward.yaml`（动态图）\r\n- `paddle/phi/ops/yaml/inconsistent/static_backward.yaml`（PIR 静态图）\r\n- `paddle/phi/ops/yaml/legacy/static_backward.yaml`（legacy 静态图）\r\n\r\n此外 `paddle/phi/ops/yaml/inconsistent/static_backward.yaml` 中的 `legacy_matmul_grad` 属同一根因——其前向 `legacy_matmul` 同样走 `MatmulInferMeta` 校验 K 维，反向也用 `GeneralBinaryGradInferMeta`。\r\n\r\n##### 改动\r\n\r\n- 将上述 3 处 `matmul_grad` 与 1 处 `legacy_matmul_grad` 的 `infer_meta` 由 `GeneralBinaryGradInferMeta` / `param : [x, y]` 改为 `MatmulGradInferMeta` / `param : [x, y, transpose_x, transpose_y]`；\r\n- `legacy_matmul_grad` 多一个 `alpha` 参数，但 K 维校验与 `alpha` 无关，复用同一 `MatmulGradInferMeta`（`alpha` 不参与 infer_meta）；\r\n- 稀疏 `matmul_grad`（`paddle/phi/ops/yaml/sparse_backward.yaml`）无 `transpose_x/transpose_y` 参数、走稀疏 kernel、属独立算子族，且本次问题场景均为 dense，保持不动。\r\n\r\n##### 修改后结果\r\n\r\n- 动态图、PIR 静态图、legacy 静态图以及 `legacy_matmul` 的全部 dense 反向路径统一获得 K 维校验；\r\n- 稀疏路径行为不变。\r\n\r\n### 修改后的总体行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| `matmul_grad` K 维不匹配（直调反向） | 不报错，返回 dx/dy | 抛 `InvalidArgument`，对齐前向与 torch |\r\n| `legacy_matmul_grad` K 维不匹配 | 不报错 | 抛 `InvalidArgument` |\r\n| K 维一致（含 K=0 的合法零尺寸反向） | 正常返回 | 正常返回（不变） |\r\n| 稀疏 `matmul_grad` | 走 `GeneralBinaryGradInferMeta` | 不变 |\r\n\r\n合法输入的结果不变；修改只影响原本被静默接受的 K 维不匹配反向请求。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79800"}
{"number": 79803, "title": "[Cherry-Pick][FlexCheckpoint] Simplify AOA name mapping and trim the generation surface (#79791)", "state": "closed", "created_at": "2026-09-22T08:07:43Z", "updated_at": "2026-09-23T04:02:51Z", "closed_at": "2026-09-23T04:02:51Z", "labels": ["skip-ci: api-benchmark", "skip-ci: slice", "skip-ci: Doc-Preview"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T04:02:51Z", "body": "### PR Category\r\nDistributed Strategy\r\n\r\n### PR Types\r\nImprovements\r\n\r\n### Description\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79791 \r\n\r\n主要改动：\r\n\r\n#### 简化 AOA name mapping\r\n- 将 `checkpoint_name_mapping` 的 key/value 都改为绝对语义：key 从 model root 起始，value 是完整的 checkpoint name，命中即为最终结果，不再用 checkpoint root 二次拼前缀。这样才能让某个 tensor 布局到共享 checkpoint root 之外（ForCausalLM 输出头作为 backbone 的顶层同级节点）。\r\n- `validate_checkpoint_name_mapping` 随之调整：root 校验只保留在 key 侧；value 侧去掉校验（value 按原样使用，落在共享 root 之外是合法布局），随之移除不再使用的 `checkpoint_name_prefix` 参数；同时去掉 duplicate-target 规则（两个互斥输出头共享一个 value 是合法声明）。\r\n- 移除 dtype-cast 辅助逻辑：`should_skip` 以及 `AOAContext` 的 `dtype_cast_rules` / `excluded_names` 字段。\r\n\r\n#### 用 checkpoint lookup drop segment 替换 AOA name scope\r\n- 当子树在 live tree 中比 checkpoint 多嵌套一层 module 时，不再用四字段 name scope + 专门的 scoped-resolution 路径，而是在 checkpoint-name 查找前从单个 name 中丢弃多出的路径段，复用同样的 mapping 模板与 identity fallback。\r\n- 移除 `AOANameScope`、`_scoped_checkpoint_name`、`_resolve_scoped_names`；`gen_aoa_statements` / `gen_inv_aoa_statements` 的关键字改为 `checkpoint_lookup_drop_segment`。\r\n\r\n### 是否引起精度变化\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79803"}
{"number": 79802, "title": "Stabilize Code Sync submodule packaging in Clone commit and package job", "state": "open", "created_at": "2026-09-22T07:38:54Z", "updated_at": "2026-09-22T18:05:13Z", "closed_at": null, "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "The `Clone commit and package` Actions job regressed after switching `code_sync.yml` to `actions/checkout` recursive submodule fetching. On the HongKong clone runner, submodule checkout failed during external fetches (`zlib`, `cccl`), causing the packaging stage to never run.\n\n- **Root cause**\n  - `actions/checkout` with `submodules: recursive` pushed all submodule cloning through checkout's built-in path, which proved fragile on this runner/network path.\n  - The first hard failure in the logs was during submodule fetch, with transfer errors such as `curl 28`, `early EOF`, and `invalid index-pack output`.\n\n- **Workflow change**\n  - Remove `submodules: recursive` from the initial checkout in `.github/workflows/code_sync.yml`.\n  - Add a dedicated submodule sync/update step after checkout so the job still packages a repo with populated submodules.\n\n- **Retry behavior**\n  - Follow the same recovery pattern already used in Paddle CI:\n    - `git submodule sync --recursive`\n    - `git submodule update --init --force --recursive --depth 1`\n    - on failure: `git submodule deinit -f --all`, clear `.git/modules`, then retry once\n\n- **Fail-fast hardening**\n  - Add `set -euo pipefail` to the retry step so a failed retry cannot fall through to tarball packaging with incomplete submodules.\n\n```yaml\n- name: Sync submodules with retry\n  working-directory: Paddle\n  run: |\n    set -euo pipefail\n    git submodule sync --recursive\n    if ! git submodule update --init --force --recursive --depth 1; then\n      git submodule deinit -f --all || true\n      rm -rf .git/modules\n      git submodule sync --recursive\n      git submodule update --init --force --recursive --depth 1\n    fi\n```", "url": "https://github.com/PaddlePaddle/Paddle/pull/79802"}
{"number": 79806, "title": "[Cherry-Pick] Fix new[]/delete mismatch in AttributeManager destructor", "state": "open", "created_at": "2026-09-22T08:45:57Z", "updated_at": "2026-09-22T14:48:02Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\nExecute Infrastructure\r\n\r\n### PR Types\r\nBug fixes\r\n\r\n### Description\r\n#### 说明\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79805\r\n\r\n#### 问题背景\r\n\r\n`AttributeManager`（`paddle/fluid/pir/dialect/operator/ir/op_dialect.cc`）是 `OperatorDialect` 内部用来把算子属性名从 `std::string` 转成 C 风格字符串数组、并负责其生命周期的辅助单例，供 `RegisterOpInfo` 注册算子接口时使用。\r\n\r\n它的析构函数用标量 `delete` 释放了由 `new[]` 分配的内存，分配与释放不配对：\r\n\r\n- `char_pointers_[i][j]` 由 `new char[attr_name.size() + 1]` 分配\r\n- `char_pointers_[i]` 由 `new const char*[attr_names.size()]` 分配\r\n\r\n按 C++ 标准（[expr.delete]/2），`new[]` 分配的对象必须用 `delete[]` 释放，两者混用是未定义行为。\r\n\r\n#### 为什么一直没有暴露\r\n\r\n`char` 是平凡析构类型，libstdc++ 中 `operator new[]` / `operator delete[]` 对这类 POD 基本只是转发到 `malloc` / `free`，且不携带数组 cookie，因此在默认分配器（glibc）下运行时“碰巧”正常，这个 UB 长期没有被发现。在 ASan 下则会直接报 `alloc-dealloc-mismatch (operator new [] vs operator delete)`。\r\n\r\n#### 实际暴露场景\r\n\r\n在使用 jemalloc 作为内存分配器时，Paddle 进程运行结束后会在退出阶段崩溃，定位到正是这里的 `new[]` / `delete` 不配对。\r\n\r\n#### 改动内容\r\n\r\n将析构函数中两处 `delete` 改为 `delete[]`，与分配方式配对。不涉及接口、逻辑和数值行为变更。\r\n\r\n### 是否引起精度变化\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79806"}
{"number": 79805, "title": "[Execute Infrastructure] Fix new[]/delete mismatch in AttributeManager destructor", "state": "open", "created_at": "2026-09-22T08:39:37Z", "updated_at": "2026-09-22T14:36:03Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\nExecute Infrastructure\r\n\r\n### PR Types\r\nBug fixes\r\n\r\n### Description\r\n#### 问题背景\r\n\r\n`AttributeManager`（`paddle/fluid/pir/dialect/operator/ir/op_dialect.cc`）是 `OperatorDialect` 内部用来把算子属性名从 `std::string` 转成 C 风格字符串数组、并负责其生命周期的辅助单例，供 `RegisterOpInfo` 注册算子接口时使用。\r\n\r\n它的析构函数用标量 `delete` 释放了由 `new[]` 分配的内存，分配与释放不配对：\r\n\r\n- `char_pointers_[i][j]` 由 `new char[attr_name.size() + 1]` 分配\r\n- `char_pointers_[i]` 由 `new const char*[attr_names.size()]` 分配\r\n\r\n按 C++ 标准（[expr.delete]/2），`new[]` 分配的对象必须用 `delete[]` 释放，两者混用是未定义行为。\r\n\r\n#### 为什么一直没有暴露\r\n\r\n`char` 是平凡析构类型，libstdc++ 中 `operator new[]` / `operator delete[]` 对这类 POD 基本只是转发到 `malloc` / `free`，且不携带数组 cookie，因此在默认分配器（glibc）下运行时“碰巧”正常，这个 UB 长期没有被发现。在 ASan 下则会直接报 `alloc-dealloc-mismatch (operator new [] vs operator delete)`。\r\n\r\n#### 实际暴露场景\r\n\r\n在使用 jemalloc 作为内存分配器时，Paddle 进程运行结束后会在退出阶段崩溃，定位到正是这里的 `new[]` / `delete` 不配对。\r\n#### 改动内容\r\n\r\n将析构函数中两处 `delete` 改为 `delete[]`，与分配方式配对。不涉及接口、逻辑和数值行为变更。\r\n\r\n### 是否引起精度变化\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79805"}
{"number": 79796, "title": "[Operator Mechanism] Pre-reduce broadcast axes in strided view backward", "state": "open", "created_at": "2026-09-21T12:19:48Z", "updated_at": "2026-09-22T11:54:43Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\n### Description\r\n\r\n本 PR 修复 `as_strided` / `tensor_unfold` 等 strided view 反向在**重叠 view** 下的两类问题，并对齐 PyTorch (`at::as_strided_backward` + `use_deterministic_algorithms`) 的行为：\r\n\r\n1. **零 stride 广播 view**：反向卡死与低精度饱和（改在 `StridedTensorAccumulate` 做零 stride 维预归约）；\r\n2. **非零 stride 交叠 view**：反向在 GPU 原子加下 run-to-run 不确定（开启 `FLAGS_cudnn_deterministic` 时改走排序 scatter）。\r\n\r\n两处都源自「重叠 view 反向 → `StridedTensorAccumulate` → 一维 storage + 一条线性 index → `index_put(accumulate=true)`」这条共享路径，分别命中「同 slot 高度碰撞」与「碰撞顺序不确定」两个子问题。\r\n\r\n---\r\n\r\n#### 1. 零 stride 广播维反向的性能卡死与精度饱和\r\n\r\n**修改前的问题**\r\n\r\n`StridedTensorAccumulate`（`paddle/phi/kernels/funcs/strided_utils.h`）把 `value_numel` 个逻辑下标全部展开成 index，交给 `index_put(accumulate=true)` 在 GPU 上用 `CudaAtomicAdd` 累加。当 view 为**全广播**（`stride` 全 0，如 `as_strided(Tensor([1]), (M,N), (0,0))`）时，`stride==0` 使该维所有下标映射到存储中同一个 slot，退化成约 `M*N` 次原子加全部打向同一地址：\r\n\r\n- 性能：同地址原子加被硬件完全串行化，`(24704,4096)` 需约 1.01 亿次串行原子加，反向跑不完（PaddleAPITest harness 下单条超过 30min timeout）；\r\n- 精度：低精度下对同一地址反复 `CudaAtomicAdd`，bf16 累加到超出尾数（~2^8）后再 `+1.0` 被舍掉、fp16 在 ~2^11 饱和，梯度远小于真值。CPU 老路径 `HostAccumulateStridedView` 的 `static_cast<T>(acc+v)` 同样在 bf16/fp16 饱和。\r\n\r\n**修改内容**\r\n\r\n在 `StridedTensorAccumulate` 开头新增「零 stride 维预归约」：\r\n\r\n- 收集 `stride[i]==0 && dims[i]>1` 的广播轴 `reduce_axes`；\r\n- 门控三条件全真才启用：`reducible_dtype`（仅 fp16/bf16/fp32/fp64/complex64/complex128——梯度唯一携带、唯一掉精度、且 `sum` 不 promote 输出位宽的类型）、`!reduce_axes.empty()`、`factory.HasKernel(\"sum\", …)`（GPU sum 仅 12 dtype，STRIDED 反向 17 dtype，缺失则回退原路径）；\r\n- 命中则：densify `out_grad` 为连续并 reshape 到逻辑 `dims`，用 `PD_VISIT_KERNEL(\"sum\")` 在广播轴上归约（`keep_dim=false`、`out_dtype=UNDEFINED` 保持类型 T），再带着去掉广播维的 `reduced_dims/reduced_stride` **递归**调用 `StridedTensorAccumulate`；此时已无广播维，落到原生 scatter 基例，只散射数量大幅缩小后的梯度。全广播时归约成标量，补一个单位维 `{1}/{1}` 让递归终止。\r\n\r\n正确性依据：广播维每个下标映射同一 slot、贡献同构，先 sum 再 scatter 与逐个 scatter 累加在加法结合律/交换律下**完全等价（非近似）**。\r\n\r\n**修改后结果**\r\n\r\n- 性能：`(24704,4096)` 的 1.01 亿次原子加 → 1 次并行 reduce + 写 1 个元素；内存也更省（原路径需 784MB 的 int64 index buffer，新路径无）。\r\n- 精度：`sum` 用 fp32 累加器、末尾单次舍入，得到最近可表示的 bf16/fp16 值，与 PyTorch 逐位一致。\r\n- 确定性：reduce 走固定归约结构，逐次运行一致。\r\n\r\n---\r\n\r\n#### 2. 非零 stride 交叠 view 反向在 `FLAGS_cudnn_deterministic` 下的确定性\r\n\r\n**修改前的问题**\r\n\r\n零 stride 预归约不触发的重叠 view（`stride` 非 0，如 `as_strided(x, (M,N), (1,1))`、`tensor_unfold` 的交叠窗口），仍走 `index_put(accumulate=true)` 的默认 GPU 路径，用 `CudaAtomicAdd` 散射累加。当同一 storage slot 被多个逻辑下标命中、且入梯度非均匀时，**原子加的完成顺序不固定**，浮点加法不满足结合律，导致**同一输入逐次运行结果在低位漂移**（run-to-run 非确定）。这与 PyTorch 在 `torch.use_deterministic_algorithms(True)` 下要求 `as_strided_backward` 确定的行为不一致。\r\n\r\n**根因**\r\n\r\n`index_put` 的 GPU 累加实现 `LaunchIndexPutCudaKernel`（`paddle/phi/kernels/gpu/index_put_kernel.cu`）对所有碰撞下标一律 `CudaAtomicAdd`，不做分组、不固定加序。\r\n\r\n**修改内容**\r\n\r\n在 `IndexPutKernel` 的 `LaunchIndexPutCudaKernel` 之前新增确定性分支（`#ifdef PADDLE_WITH_CUDA`）：\r\n\r\n```cpp\r\nif (accumulate && FLAGS_cudnn_deterministic &&\r\n    int_indices_v.size() == 1 && x.dims().size() == 1) {\r\n  // 复现 LaunchIndexPutCudaKernel 的 prologue：未初始化目标先 copy(x)，\r\n  // 已初始化（如预置零的 strided 梯度 buffer）保持不动\r\n  ...\r\n  funcs::IndexPutWithSortKernel<T, int64_t>(dev_ctx, x, *ptr_value,\r\n      res_indices_v, x_dims_v, x_strides_v, idx_dims_v, idx_strides_v,\r\n      /*slice_offset=*/0, /*accumulate=*/true, out);\r\n  return;\r\n}\r\n```\r\n\r\n`IndexPutWithSortKernel`（`paddle/phi/kernels/funcs/index_put_with_sort.cu.h`）先对 index 排序，把命中同一 slot 的下标聚成连续段、在段内以**固定顺序**累加（stride-1 场景的 `IndexingBackwardKernelStride1` 是 PyTorch `indexing_backward_kernel_stride_1` 的忠实移植：lane 映射、warp shuffle-down 树形规约、尾部串行补加顺序逐一致），结果与原子路径数值相等但**逐位可复现**。其余情况保持原子路径不变。\r\n\r\n**门控条件说明**\r\n\r\n- `int_indices_v.size() == 1 && x.dims().size() == 1`：**精确匹配 strided view 反向发出的调用形状**——`StridedTensorAccumulate`（与 torch `as_strided_backward` 在 `FunctionsManual.cpp` 中的做法一致）总是把梯度摊到一维 storage、用一条线性 index 做 scatter。此处也匹配 `index_elementwise_get_grad` 对同一 helper 的单索引用法。\r\n- 这两个条件**不是 PyTorch 对 index_put 的限制**（torch 的确定性 scatter 对 N-D/多索引通用）；本 PR 有意收窄到 strided 反向这一 flat scatter，避免误伤其他 `index_put` 调用，且当前 helper 调用的 flat 几何（`slice_offset=0`、把 `out` 当线性 buffer）只在 1-D 单索引下验证过。直接调用 `paddle.index_put(accumulate=True)` 的 N-D 场景在本改动下仍走原子路径（本 PR 范围外）。\r\n\r\n**修改后结果**\r\n\r\n- 确定性：交叠 view 反向在 `FLAGS_cudnn_deterministic=True` 下逐次运行逐位一致；\r\n- 与 PyTorch 对齐：开 `torch.use_deterministic_algorithms(True)` 后，Paddle(flag on) 与 torch(det) 的反向梯度**逐位相等**（4 组几何 `(1000,1000)/(1,1)`、`(4000,4000)/(1,1)`、`(500,37)/(3,7)`、`(300,300)/(2,1)` 实测 `array_equal=True`，两者对 fp64 参考 max_abs_diff 完全一致，如 (1000,1000) 均为 1.58e-4）；torch 默认模式（原子加）run-to-run 不一致，作为对照不逐位相等；\r\n- 正确性：与「fp64 逐个累加」参考在 `rtol=1e-5` 内一致；\r\n- 关闭 flag 时保持原子路径，性能不受影响。\r\n\r\n---\r\n\r\n#### 3. 覆盖范围\r\n\r\n一处零 stride 预归约 + 一处确定性 gate，共同覆盖三条入口：`as_strided_grad`、`tensor_unfold_grad`、`StridedTensorAccumulateThroughStorage`。零 stride 广播命中预归约；非零 stride 交叠在开 flag 时命中确定性排序 scatter，不开 flag 时保持原子路径。白名单外 dtype、无广播维、`sum` 未注册、非 1-D/多索引等情况均回退原路径，结果不变。\r\n\r\n#### 4. 测试\r\n\r\n`test/legacy_test/test_as_strided.py` 新增：\r\n\r\n- `TestAsStridedZeroStrideBroadcastBackward`（覆盖问题 1）：\r\n  - bf16/fp16 全广播（`(4096,)/(0,)` 与 `(64,64)/(0,0)`），期望 grad=4096（2^12 在 bf16/fp16 精确）；\r\n  - fp32 全广播 `(1000,1000)/(0,0)` → 1e6；\r\n  - 部分广播 `(4,2048)/(1,0)` → 保留真实轴，grad 全 2048。\r\n- `TestAsStridedNonZeroOverlapDeterministic`（覆盖问题 2，GPU-only）：\r\n  - `test_high_collision_run_to_run_bitwise`：`(1000,1000)/(1,1)`、非均匀入梯度，开 `FLAGS_cudnn_deterministic` 跑两次，断言两次 `array_equal`（逐位可复现）且与 fp64 参考 `assert_allclose`；\r\n  - `test_low_collision_still_reproducible`：`(10,2)/(1,1)` 低碰撞同样确定且正确。\r\n\r\n验证结果：\r\n\r\n- `test_full_broadcast_low_precision` 在**未修复**产物上 FAIL（bf16 饱和~256 / fp16~2048），修复后 PASS；\r\n- `TestAsStridedNonZeroOverlapDeterministic` 在**未修复**产物（开 flag 仍走原子）上 run-to-run 不逐位一致而 FAIL，修复后 PASS；\r\n- 全量 `test/legacy_test/test_as_strided.py`、`test/legacy_test/test_tensor_unfold.py` 修复后全部通过，无回归；\r\n- 真 harness 路径单条 `(24576,4096)/(0,0)`：修改前卡死被上限杀掉，修改后 2.47s pass；bf16 全广播反向逐条与 PyTorch `as_strided_backward` 逐位一致（如 `(44160,2048)` 两侧均为 90177536）。\r\n\r\n\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是", "url": "https://github.com/PaddlePaddle/Paddle/pull/79796"}
{"number": 79798, "title": "[compat] Resolve a scoped spec once instead of re-entering the finder", "state": "closed", "created_at": "2026-09-21T16:22:03Z", "updated_at": "2026-09-22T11:22:23Z", "closed_at": "2026-09-22T09:52:16Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T09:52:16Z", "body": "### PR Category\nPerformance Optimization\n\n### PR Types\nPerformance\n\n### Description\n`TorchProxyMetaFinder._find_spec_for_specific_module` looks up the real spec with `importlib.util.find_spec` while the proxy is disabled, which is what is meant to keep the finder out of its own lookup. That only holds while the finder is registered **once**: `enable_compat` inserts it into `sys.meta_path` unconditionally, so nested guards leave duplicates behind, while `disable_compat` drops a single occurrence. With a duplicate left in place the lookup re-enters the finder for the very same name and restarts the whole resolution, and a name that has no spec at all never converges, so the lookups compound.\n\nMeasured with a probe for a missing submodule of a package in the enabled scope:\n\n| finder registrations in `sys.meta_path` | 1 | 2 | 3 | 4 |\n| --- | --- | --- | --- | --- |\n| lookups per probe, before | 1 | 4 | 15 | 64 |\n| lookups per probe, after | 1 | 2 | 3 | 4 |\n\nEvery lookup toggles the proxy, and every toggle walks all of `sys.modules` three times to move `torch*` in and out of the shadow table, so the cost is `O(lookups * len(sys.modules))` -- it also grows as the program imports more, because that is what makes `sys.modules` bigger. Importing one package inside a scoped guard with 7 registrations already in place expanded 7 probes for a missing module into 13699 lookups and ~90M module-name comparisons: **37.9s, of which 0.5s was the actual import work**.\n\n**Fix**: suppress the finder for the name it is currently resolving, so the outer lookup finishes instead of starting over. Each registered copy is now asked exactly once. The proxy is still disabled around the lookup and `exec_module` still runs under its own guard, so the resulting specs, loaders and `__enable_torch_proxy__` marks are unchanged.\n\nVerified on that same package -- identical submodule set and identical set of modules marked with `__enable_torch_proxy__` before and after:\n\n- imported inside a scoped guard late in the process (`len(sys.modules)` = 4732): **37.9s -> 1.2s**\n- the same package imported eagerly at startup: **6.6s -> 2.3s**\n\nTests: `test/compat/test_torch_proxy.py::TestTorchProxySpecLookupReentrancy`. A missing submodule of a scoped package is now looked up once per registered copy of the finder (2 copies cost 4 lookups before this change), and an existing submodule is still resolved by the first copy and still loaded through the proxy loader.\n\nNote: the duplicate registrations are a defect of their own -- `enable_compat` should not insert a copy that is already registered, and `disable_compat` cannot undo nested enables by removing a single occurrence. That is left alone here because fixing it changes the enable/disable bookkeeping, whereas this change makes the lookup correct regardless of how many copies are registered. Happy to follow up in a separate PR.\n\n### 是否引起精度变化\n否\n\nOnly import-time module resolution changes. No kernel, no computation and no reduction order is touched.\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79798"}
{"number": 79804, "title": "[FlexCheckpoint] Avoid the full metadata load in the local-resume check", "state": "closed", "created_at": "2026-09-22T08:29:10Z", "updated_at": "2026-09-22T10:51:20Z", "closed_at": "2026-09-22T10:51:20Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\n\nDistributed Strategy\n\n### PR Types\n\nPerformance\n\n### Description\n\n`dist.load_state_dict` first asks whether every rank can restore straight from its own `.distcp`, and only reshards if the answer is no. Answering it costs a full `paddle.load` of the `*.metadata` file plus a `MetadataManager` build, because `check_resumable_locally` needs `storage_metadata`. On a fully replicated checkpoint `storage_metadata` holds `num_keys * world_size` entries — on a real 154 GB checkpoint that is a 227 MiB metadata file and tens of seconds of unpickling — all to produce a single boolean.\n\nThis PR adds a fast path that answers the same question from two much cheaper sources.\n\n**1. `flex_checkpoint/dcp/metadata_reader.py` — partial metadata read**\n\n`load_state_dict_metadata(path)` returns `Metadata.state_dict_metadata` alone, without materializing `storage_metadata`. It relies on `state_dict_metadata` being pickled immediately before `storage_metadata`: writing a `STOP` opcode over the opcode that would have pushed the `\"storage_metadata\"` key ends the stream with exactly the wanted value on top of the unpickler's stack, and nothing past that point is read from disk. Whenever the stream does not match that expectation the function falls back to a full `paddle.load`, so calling it is always safe.\n\nIt is also exported as `paddle.distributed.load_state_dict_metadata`, since \"which tensors are in this checkpoint and how is each one shaped\" is useful on its own.\n\n**2. `flex_checkpoint/dcp/distcp_reader.py` — header-only `.distcp` read**\n\n`scan_tensor_shapes(path)` returns `{tensor_key: shape}` by walking only the pickle header. Every weight payload carries an explicit length in its opcode, and pickle writes any payload of at least 64 KiB outside the frame stream, so those bytes can be `seek()`-ed over. The cost is a few hundred bytes per tensor instead of the whole file. It deliberately reports what the file itself claims to hold rather than what the metadata says, so a file that was truncated or never fully written is detected rather than trusted.\n\n**3. `flex_checkpoint/dcp/fast_resumable.py` — the combined check**\n\n`check_resumable_locally_fast(...)` is tri-state. If every tensor of the checkpoint is stored whole — one shard description, not flattened, zero global offset, local shape equal to global shape — then a tensor key identifies its `LocalTensorIndex` uniquely, so `storage_metadata` carries no information the key alone does not, and the verdict follows from the rank's own file header. If any tensor is sharded, shard identity depends on `global_offset` / `flattened_range`, which are recorded nowhere but `storage_metadata`; the function then returns `None` and the caller runs the stock check unchanged.\n\nTwo properties make it a drop-in replacement:\n\n- The applicability verdict is a pure function of the shared metadata file, so every rank reaches the same decision before any communication happens. Only the per-rank file check can differ, and it yields a plain bool folded into the same `all_gather_object` the stock check uses.\n- Exactly one `all_gather_object` is issued when the check applies and none when it declines, matching the stock collective count on either branch.\n\nThe check is never more permissive than the stock one: it validates the file's real contents instead of what the metadata claims about them, so an unfinished or truncated `.distcp` — which the stock check accepts, since it only calls `os.path.isfile` — makes this one fall back to resharding. Like the stock check it compares shapes but not dtypes.\n\n**Enablement**\n\n`load_state_dict(..., fast_resumable_check: bool = True)`. Setting it to `False` forces the original code path, so the change is fully reversible per call site without an environment variable.\n\n**Tests**\n\n`test/flex_checkpoint/test_fast_resumable_check.py` (66 cases) covers the three new modules, mostly differentially against `utils.check_resumable_locally` so agreement is asserted rather than assumed:\n\n- partial metadata read: pickle protocols 2 / 4 / 5, scalar and empty shapes, an empty checkpoint, a guard that `paddle.load` is never called and that only a prefix of the file is read, and the full-load fallback\n- byte-level framing helpers: `_field_after`, `_key_patterns` (including a name too long for `SHORT_BINUNICODE`), `_find_first`, `_scan_for_patterns` across a chunk boundary, all `_bytes_needed` rejection branches, `_as_state_dict_metadata` validation, and `_load_truncated` on unparsable, mis-ordered and marker-less streams\n- header-only `.distcp` read: float32/float16/bfloat16/int64/int32/uint8/bool, scalar and empty shapes, payload sizes bracketing pickle's 64 KiB frame target, `StructuredToParameterName@@` skipped, truncated / corrupt / non-dict files\n- the check itself: unsharded, dedup'ed (`save_replicas=False`) and sharded checkpoints, `ShardedWeight` inputs stored whole / offset / flattened, uninitialized tensors, missing and truncated `.distcp`, missing and unparsable metadata\n- the distributed branch: the verdict under agreeing and dissenting ranks, and that exactly one collective is issued when the check applies and zero when it declines\n\nLine coverage of the three new modules is 98% (`distcp_reader.py` 100%, `metadata_reader.py` 99%, `fast_resumable.py` 95%); what remains uncovered are the `TYPE_CHECKING` import blocks.\n\n### 是否引起精度变化\n\n否\n\nThe fast path only replaces the boolean decision of `check_resumable_locally`; the tensor data path is untouched. Loaded values are asserted equal to the saved ones end to end, and the fast verdict is asserted equal to the stock verdict on every checkpoint layout tested.\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79804"}
{"number": 79801, "title": "代码缓存拉取submodule", "state": "closed", "created_at": "2026-09-22T06:02:15Z", "updated_at": "2026-09-22T06:46:55Z", "closed_at": "2026-09-22T06:46:55Z", "labels": ["skip-ci: all"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T06:46:55Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nEnvironment Adaptation\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nOthers\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nOthers\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79801"}
{"number": 75421, "title": "[Auto Parallel] Add co_shard spmd_rule for elementwise_binary ops and layernorm.", "state": "closed", "created_at": "2025-09-21T02:38:04Z", "updated_at": "2026-09-22T06:39:37Z", "closed_at": "2026-09-22T06:39:37Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nAuto Parallel\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n为LayerNorm 和elementwise_binary（例如：Add）的算子添加切多刀机制的推导规则。\r\n\r\n对于elementwise_binary 类的算子如：Add(x,y) ，切分推导策略为，解决x和y的切分策略冲突，并合并x和y的切分策略，作为算子输出的output的张量的output。相应的，其反向算子的切分策略推导与上面一致。（```ShardingMergeForTensors``` 函数在PR#74829当中，等待该P合入之后，该PR可以合入）\r\n\r\n对于LayerNorm算子，在原来的切分策略不变的基础上，需要保证在做layernorm之前的维度上（即：```dim< begin_norm_axis```），支持多刀切机制。\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/75421"}
{"number": 75371, "title": "max same with amax, min same with amin (#72512)", "state": "closed", "created_at": "2025-09-18T13:28:06Z", "updated_at": "2026-09-22T06:39:35Z", "closed_at": "2026-09-22T06:39:35Z", "labels": ["skip-ci: warning"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOthers\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\n\r\nOthers\r\n### Description\r\n<!-- Describe what you’ve done -->\r\ncherry-pick PR(https://github.com/PaddlePaddle/Paddle/pull/72512）\r\npcard-67164", "url": "https://github.com/PaddlePaddle/Paddle/pull/75371"}
{"number": 75350, "title": "[AutoParallel] Adapt full_like for p_norm dynamic parallel backward", "state": "closed", "created_at": "2025-09-18T02:48:21Z", "updated_at": "2026-09-22T06:39:33Z", "closed_at": "2026-09-22T06:39:33Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nAuto Parallel\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes \r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/75350"}
{"number": 75331, "title": "[Auto-Paralllel] fix dense add dist in acc grad", "state": "closed", "created_at": "2025-09-17T08:41:02Z", "updated_at": "2026-09-22T06:39:31Z", "closed_at": "2026-09-22T06:39:31Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nAuto Parallel\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nPcard-70448", "url": "https://github.com/PaddlePaddle/Paddle/pull/75331"}
{"number": 75320, "title": "[Auto-Paralllel] fix reshard grad", "state": "closed", "created_at": "2025-09-16T09:10:05Z", "updated_at": "2026-09-22T06:39:30Z", "closed_at": "2026-09-22T06:39:30Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nAuto Parallel\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nSkip reshard grad output for non dist_tensor\r\nPcard-70448", "url": "https://github.com/PaddlePaddle/Paddle/pull/75320"}
{"number": 79716, "title": "[Operator Mechanism]Add CUDA small-stride index gradient kernel", "state": "closed", "created_at": "2026-08-28T10:03:26Z", "updated_at": "2026-09-22T06:24:15Z", "closed_at": "2026-09-22T06:24:14Z", "labels": ["skip-ci: slice", "skip-ci: template"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T06:24:14Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 修复 GPU 高级索引（advanced indexing）反向的三个问题：\r\n\r\n1. 低精度 dtype 下重复下标的累加逐步舍入，`x[:, idx]` 这类表达式的梯度精度明显低于 `x[idx]`；\r\n2. `None`（newaxis）出现在高级索引之前时，索引被绑定到错误的轴，**前向 shape 就是错的**；\r\n3. `index_elementwise_get_grad` 没有校验 `slice_offset` 和被索引视图的跨度，非法值会变成越界写。\r\n\r\n#### 1. 高级索引反向的低精度累加\r\n\r\ntorch 的 `index_put_(accumulate=True)` CUDA 实现完全没有 atomicAdd，只有排序路径，所以它的结果确定、可复现，且重复下标只舍入一次。本节的三处改动合起来把 Paddle 的所有 accumulate 反向也导向这条路径：1.1 放开路由，1.2 补上排序路径缺失的 kernel，1.3 让排序路径能处理放开后新进来的跨步视图。\r\n\r\n##### 1.1 路由判据用错了维度，`x[:, idx]` 落到逐项舍入的 atomicAdd\r\n\r\n**问题**\r\n\r\n`index_elementwise_get_grad` 原来的路由条件是：\r\n\r\n```cpp\r\nif (accumulate && index.size() == 1 && !is_combined) {\r\n  IndexPutWithSortKernel<T, int64_t>(...);\r\n  return;\r\n}\r\n```\r\n\r\n`is_combined` 来自 pybind：`eager_method.cc` 里 `index_size = PyTuple_GET_SIZE(index_ptr)` 是 `__getitem__` 索引元组的**元素个数**（`slice` / `None` / `Ellipsis` 都算），`slice_utils.h` 里 `is_combined = (index_size == 1) ? false : true`。因此真实判据是「索引元组只有一个元素」：\r\n\r\n- `x[idx]` → 走排序 kernel（`IndexingBackwardKernelStride1` / `IndexingBackwardKernel`），重复下标在 `opmath_t`（fp32）寄存器里归约，只在写回时舍入一次；\r\n- `x[:, idx]`、`x[..., idx]`、`x[idx, None]` → `is_combined=true`，落到 `GPUIndexElementwiseGetGrad`，`IndexEleGetGradAccKernel` 内是 `CudaAtomicAdd(reinterpret_cast<T*>(...), T value)`，即 fp16/bf16 原生 atomicAdd，**每加一次就舍回低精度**，误差随重复次数增长，且累加顺序不确定、run-to-run 不可复现。\r\n\r\n触发条件是同一下标重复 **≥3** 次：N=2 时低精度逐步累加恒等于正确舍入（`0+a` 精确，`a+b` 只舍一次），所以必须重复三次以上才会暴露。`[16, 8]` 配 `x[:, idx]`（索引长 5）200 组随机数据统计：\r\n\r\n| 最大重复次数 | fp16 与 torch 不一致率 | bf16 |\r\n| --- | --- | --- |\r\n| 1 | 0/46 | 0/46 |\r\n| 2 | 0/127 | 0/127 |\r\n| 3 | 26/27 | 27/27 |\r\n\r\n**改动**\r\n\r\n路由门从 `accumulate && index.size() == 1 && !is_combined` 改为 `if (accumulate)`（`index_elementwise_get_grad_kernel.cu:724`）。放开之后有两类输入是排序路径原来处理不了的，分别由 1.2、1.3 补上。\r\n\r\n##### 1.2 排序路径缺 `1 < sliceSize <= 32` 的 kernel\r\n\r\n**问题**\r\n\r\n`sliceSize`（被索引轴之后的元素个数）落在 `1 < sliceSize <= 32` 时，即使走到排序路径，也只有 `IndexingBackwardKernelStride1`（仅 `sliceSize == 1`）和按 feature 展开的通用 `IndexingBackwardKernel` 两个选择，后者对每个重复项都做一次 `grad_weight` 的 read-modify-write，同样是每步舍入。torch 在这里有专门的 `indexing_backward_kernel_small_stride`，且它真正的分派维度就是 `sliceSize` 与 warp size 的比较，而不是索引表达式的写法。\r\n\r\n**改动**\r\n\r\n新增 `IndexingBackwardKernelSmallStride`（`:344`），覆盖 `1 < sliceSize <= WARP_SIZE`：一个线程独占一个 feature 列，该下标的所有重复项在 `opmath_t`（fp32）寄存器内归约，写回时才舍入一次。分派处（`:638`）由此按 `sliceSize` 三分：`== 1` 走 `IndexingBackwardKernelStride1`，`<= WARP_SIZE` 走新 kernel，其余走通用 `IndexingBackwardKernel`。\r\n\r\n##### 1.3 排序 kernel 无法寻址跨步视图\r\n\r\n**问题**\r\n\r\n排序 kernel 把 `grad_weight` 当作一块平坦 buffer 寻址；原 `IndexPutWithSortKernel` 自己做 transpose / inverse-perm 并用 `funcs::makeLinearIndex` 构造线性索引，只在「被索引对象就是整块连续 `x_grad`」时成立。1.1 放开路由后，`x[1::2, idx]` 这类先切片再索引也会进来，此时被索引对象是 `x_grad` 内的一块跨步子区域，直接跑排序 kernel 会写到错误位置。\r\n\r\n**改动**\r\n\r\n`IndexPutWithSortKernel` 改为接收一个描述被索引视图的 `SortedPathLayout`（`:401`），并直接用 `funcs::computeLinearIndex` 在该视图上构造线性索引。被索引轴的位置由 layout 给出，且必然是连续的一段，因此 `transposeToFrontAndInvPerm` 那一步在此路径上不再需要，也省掉了 permute 后的 `Contiguous` 与写回时的反向 transpose。\r\n\r\n视图信息的恢复方式（`DeriveSortedPathLayout`，`:437`）：kernel 参数描述的是 **restride 之后**的视图 —— `restride_src()`（`paddle/fluid/pybind/slice_utils.h`）把被索引轴换成广播后的 index shape 并给这些轴**零 stride**，所以 `input_strides` 里连续的一段 0 就标出了被索引块，起点即 `dims_before`；而 `index_dims`（即 `AdvancedIndex::indexed_sizes`）开头正是这些轴的 extent，于是视图在索引之前的 shape 及其在 `x_grad` 内的 stride 都可以还原。据此分三种情况：\r\n\r\n- 视图恰好是整块连续 `x_grad` 时，直接在 `x_grad` 上跑排序 kernel；\r\n- 视图是跨步子区域时（`x[1::2, idx]`），先分配一个与该视图同形的连续 scratch，在其上归约，再用 `StridedCopyKernel` 按 `view_strides` / `view_offset` 搬回 `x_grad` —— 这与 torch 用 `slice_backward` 组合 `index_backward` 的做法一致。`x_grad` 已经清零，所以搬回时无需累加。注意 `phi::StridedCopyKernel` 会覆写 `out` 的 meta，调用方需自行 save / restore；\r\n- `IsNonOverlapping`（`:411`）拒绝自重叠的 `as_strided` 式视图，这类布局回落到原来的 elementwise 路径（精度更低、run-to-run 不确定），与上游行为保持一致。\r\n\r\n`slice_offset` / `index_strides` 是**字节**偏移（`slice_utils.h` 里 `stride * element_size_bytes`），派生 layout 时统一按 `elesize` 换算回元素单位，不能整除即视为非法并回落。\r\n\r\n#### 2. `None` 出现在高级索引之前导致轴绑定错位\r\n\r\n##### 问题\r\n\r\n`None` 位于高级索引之前时，**前向** shape 就与 numpy / torch 不一致：\r\n\r\n| 表达式（`x` 为 `[16, 8]`，`i` 长 3） | numpy / torch | 修改前 Paddle |\r\n| --- | --- | --- |\r\n| `x[None, :, i]` | `(1, 16, 3)` | `(1, 3, 8)` |\r\n| `x[:, None, i]` | `(16, 1, 3)` | `(16, 3, 8)` |\r\n| `x[None, i]` | `(1, 3, 8)` | `(3, 16, 8)` |\r\n| `x[i, None]` | `(3, 1, 8)` | `(3, 1, 8)`（正确） |\r\n\r\n`ParseIndex` 里的 `estimated_dim` 是**基础索引输出张量的轴号**（`slice` 保留 → `++`，整数 → 不 `++`（会 decrease），`Ellipsis` → `+= rank - specified`，bool tensor → `+= rank`），`advanced_index_dim[estimated_dim] = estimated_dim` 用它记录高级索引落在哪个轴。但 `Py_None` 分支只 push `none_axes` 而**不推进 `estimated_dim`**，而 `getTensorWithBasicIndexing` 结尾的 `unsqueeze_ad_func(out, *none_axes)` 确实在输出里插入了这个轴（`none_axes` 已按 `decrease_axis` 修正，就是基础输出的坐标系）。两个坐标系因此错开 `none_count` 个位置，索引被绑到错误的轴上。\r\n\r\n同分支的单 bool（`True` / 0-D bool tensor）处理是对的：它 push `none_axes` 的同时也 `estimated_dim++`，正好印证 `None` 分支缺这一步。\r\n\r\n静态图 `parse_index` 有同构的 bug，且更早暴露：`replace_none` 先把 `None` 从 `indices` 里剥掉，循环根本看不到 `None`，修改前静态图 `x[None, i]` 直接在 `gather_nd` 报 index out of range。\r\n\r\n##### 改动\r\n\r\n- `slice_utils.h` 的 `ParseIndex` `Py_None` 分支补 `estimated_dim++`；\r\n- `eager_method.cc` 两处 `advanced_index_dim` 容量从 `rank * 2` 改为 `rank * 2 + index_size`（setitem 侧为 `+ size`）。否则 rank 2 张量上 `x[None, None, None, None, None, i]` 会让 `estimated_dim` 到 5，发生越界写；\r\n- `variable_index.py` 的 `parse_index` 改成 `_, none_axes = replace_none(indices)` 保留原 `indices`，循环里遇到 `None` 时 `estimated_dim += 1; continue`，并把 `advanced_index` 扩容 `len(indices)`。\r\n\r\n#### 3. `slice_offset` 与视图跨度的越界校验\r\n\r\n##### 问题\r\n\r\n`slice_offset` 是前向记录的「被索引视图在 `x_grad` buffer 内的字节偏移」。排序路径和 elementwise 回落路径都会把它加到 `x_grad` 的基址上，因此一个非法值（负数，或超出 buffer）会直接变成越界写，而不是报错。\r\n\r\n同样地，跨步视图是**稀疏**的，它触达的最远位置由跨度决定而不是元素个数决定，只检查 `numel <= grad_numel` 不足以保证不越界。\r\n\r\n##### 改动\r\n\r\n- 在 `IndexElementwiseGetGradKernel` 入口校验 `0 <= slice_offset < x_grad 字节数`；\r\n- 在 `DeriveSortedPathLayout` 里校验视图触达的最后一个位置 `slice_offset / elesize + Σ (view_dims[k] - 1) * view_strides[k] < x_grad->numel()`，报错信息带上 offset、view shape 和 strides，便于定位。\r\n\r\n#### 修改后的行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| fp16/bf16 `x[:, idx]` 等重复下标反向 | 每个重复项舍入一次，误差随重复次数增长，顺序不确定 | fp32 寄存器内归约，写回舍入一次，与 torch 逐位一致 |\r\n| `1 < sliceSize <= 32` 的排序路径 | 走按 feature 展开的通用 kernel，逐项 read-modify-write | 新增 small-stride kernel，单线程独占一列 |\r\n| 先切片再高级索引（`x[1::2, idx]`） | 走 elementwise atomicAdd 回落 | 连续 scratch 归约后按视图 stride 搬回 |\r\n| `x[None, :, i]` 等 `None` 在前 | 前向 shape 错、`__setitem__` 多数直接报 broadcast 失败、静态图 `gather_nd` 越界 | 与 torch 一致 |\r\n| 多个连续 `None` | `advanced_index_dim` 可能越界写 | 容量按 `index_size` 扩展 |\r\n| 非法 `slice_offset` / 越界视图 | 静默越界写 | 明确报错 |\r\n\r\n自重叠的 `as_strided` 式视图仍回落到原 elementwise 路径，行为与之前相同。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是", "url": "https://github.com/PaddlePaddle/Paddle/pull/79716"}
{"number": 79797, "title": "增加Paddle代码缓存", "state": "closed", "created_at": "2026-09-21T13:45:06Z", "updated_at": "2026-09-22T05:13:07Z", "closed_at": "2026-09-22T05:13:07Z", "labels": ["skip-ci: all"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T05:13:07Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\n Environment Adaptation\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\n\r\nNot User Facing\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n增加Paddle源码缓存\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79797"}
{"number": 79791, "title": "[FlexCheckpoint] Simplify AOA name mapping and trim the generation surface", "state": "closed", "created_at": "2026-09-20T12:24:14Z", "updated_at": "2026-09-22T04:23:32Z", "closed_at": "2026-09-22T04:23:32Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T04:23:32Z", "body": "### PR Category\nDistributed Strategy\n\n### PR Types\nImprovements\n\n### Description\n\n本 PR 简化 #79723 引入的模块化 AOA 生成接口，收敛命名协议、去掉两处过度设计。\n\n**1. `checkpoint_name_mapping` 两侧改为绝对路径**\n\n原来 key/value 都是相对根前缀的模板，命中后再拼 checkpoint 根前缀。改为 key 从 model 根写起、value 是完整的 checkpoint 名。\n\n**2. `validate_checkpoint_name_mapping` 相应调整**\n\n- key 侧保留根前缀校验：key 是拿去和 single 名比对的，single 名必带 model 根，所以无根 key 永远匹配不上，会静默退化成 identity 兜底产出一个看似合理的错名字，必须在入口拒掉。\n- value 侧去掉同类校验：value 被原样当作最终 checkpoint 名使用，落在共享根之外是合法布局而非错误。\n- 去掉「重复 checkpoint 目标」校验：mapping 要覆盖模型可能构建的全部布局，而两个互斥的输出头只会实例化一个，两个 key 共享一个 value 是合法声明。\n\n**3. 去掉 dtype cast 机制与 `should_skip`**\n\n删除 `DtypeCastRule`、`resolve_dtype_cast_rule`、`format_dtype_cast_attr`、`format_inv_dtype_cast_attr`、`should_skip`，以及 `AOAContext` 的 `dtype_cast_rules` / `excluded_names` 两个字段。需要 cast 的组件改为直接覆写最小范围类的 AOA 方法；`Layer` 基类递归改为两侧解析结果同名时省略该条语句。\n\n**4. 用 `checkpoint_lookup_drop_segment` 取代 `AOANameScope`**\n\n有的子树在真实模块树里比 checkpoint 里多嵌一层（MTP block 把 transformer layer 当子模块持有，而 checkpoint 把该层的张量直接挂在层下）。原来用一个四字段的 `AOANameScope` 加一条独立的 scoped 解析路径描述这件事，现在改为在查 checkpoint 名之前把这一个多出来的路径段从 single 名里去掉：去掉后子树的名字和普通层同形，`checkpoint_name_mapping` 里为普通层写的模板与 identity 兜底都直接适用，model 侧的名字保留该段。\n\n删除 `AOANameScope`、`_scoped_checkpoint_name`、`_resolve_scoped_names`；`gen_aoa_statements` / `gen_inv_aoa_statements` 的关键字参数由 `aoa_name_scope` 改为 `checkpoint_lookup_drop_segment`。段不存在时按 no-op 处理，这样子树的持有者可以把同一个值无条件传给所有子模块；出现多次则报错，因为该丢哪一个无从判断。\n\n\n### 是否引起精度变化\n否\n\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79791"}
{"number": 79781, "title": "[FSDP] FSDP Support Muon and CSA", "state": "open", "created_at": "2026-09-16T08:12:03Z", "updated_at": "2026-09-21T13:30:42Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nDistributed Strategy\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nFSDP Support Muon and CSA\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79781"}
{"number": 79752, "title": "[Operator Mechanism] Add compatible tile_grad_kernel with torch2.12", "state": "open", "created_at": "2026-09-09T01:07:42Z", "updated_at": "2026-09-21T12:54:57Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\nOperator Mechanism\r\n\r\n### PR Types\r\nOthers\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n为paddle.tile增加了torch2.12对齐的TileGradCompatKernel替换旧Eigen路径\r\n<!-- 1. 为安全删除代码中的旧Eigen路径，为`SumKernel`和`SumRawKernel`在cuda中增加了fp8类型支持；（kernel底层已使用MPType作为计算时的精度类型提升，已包含fp8的处理）\r\n2. CompatKernel补充了tile grad中旧Eigen路径没有的对非连续输入(比如 transpose 视图)的处理 -->\r\n\r\ncuda13.2 dtype=float32的性能测试\r\nCase | Grad numel | torch (ms) | paddle (Eigen) (ms) | paddle (compat) (ms)\r\n-- | -- | -- | -- | --\r\nlead_dim | 524,288 | 0.0480 | 0.0539 | 0.0663\r\nmulti_axis | 1,572,864 | 0.0464 | 0.0598 | 0.0647\r\nlong_reduce_small_grad | 1,048,576 | 0.0501 | **40.0781** | **0.0531**\r\nrow_broadcast_1M | 16,777,216 | 0.1186 | 0.1059 | 0.1582\r\nmid_2d | 33,554,432 | 0.0870 | 0.2197 | 0.1713\r\nbig_5d | 16,777,216 | 0.0559 | 0.1691 | 0.1123\r\n\r\n\r\n### 是否引起精度变化\r\n是\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79752"}
{"number": 79780, "title": "[Cherry-Pick] Remove unbind int32 element limit", "state": "closed", "created_at": "2026-09-15T11:39:04Z", "updated_at": "2026-09-21T10:00:45Z", "closed_at": "2026-09-21T10:00:45Z", "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T10:00:45Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\nOperator Mechanism\r\n\r\n### PR Types\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you've done -->\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79737\r\n\r\n### 是否引起精度变化\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79780"}
{"number": 79779, "title": "[Cherry-Pick] Align cosine_similarity with torch2.12", "state": "open", "created_at": "2026-09-15T11:08:51Z", "updated_at": "2026-09-21T08:37:34Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\nOperator Mechanism\r\n\r\n### PR Types\r\nOthers\r\n\r\n### Description\r\n<!-- Describe what you've done -->\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79710\r\n\r\n### 是否引起精度变化\r\n是\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79779"}
{"number": 79795, "title": "[release/3.4] Translate Chinese comments to English in muon optimizer test", "state": "open", "created_at": "2026-09-21T07:50:25Z", "updated_at": "2026-09-21T07:54:35Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nUser Experience\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nOthers\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n删除中文注释\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79794", "url": "https://github.com/PaddlePaddle/Paddle/pull/79795"}
{"number": 79794, "title": "Translate Chinese comments to English in muon optimizer coverage test", "state": "closed", "created_at": "2026-09-21T06:43:52Z", "updated_at": "2026-09-21T07:53:27Z", "closed_at": "2026-09-21T07:53:27Z", "labels": ["skip-ci: all"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T07:53:27Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nUser Experience\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nOthers\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n删除中文注释\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79794"}
{"number": 79790, "title": "[release/3.4] Fix the bug in CommBuffer when param and grad have different shape", "state": "closed", "created_at": "2026-09-20T08:05:16Z", "updated_at": "2026-09-21T07:32:04Z", "closed_at": "2026-09-21T07:32:04Z", "labels": ["contributor", "skip-ci: api-benchmark"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T07:32:04Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nDistributed Strategy\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes \r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nWhen copying grads to the communication buffer, FusedCommBuffer will set the flattened grad's shape with param shape. This causes an error when param and grad have different shape. \r\n\r\nThis pr fix this bug by setting flattened grad's shape with the original grad shape.\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否\n\n---\n\nCherry-pick of #79788 (authored by @pkuzyc) to `release/3.4`.\n\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79788 <!-- For pass CI -->", "url": "https://github.com/PaddlePaddle/Paddle/pull/79790"}
{"number": 79793, "title": "[release/3.4] Support hyperball optim-method and shardingV1 for adam", "state": "closed", "created_at": "2026-09-20T13:41:11Z", "updated_at": "2026-09-21T07:27:18Z", "closed_at": "2026-09-21T07:27:18Z", "labels": ["skip-ci: api-benchmark", "skip-ci: dcu", "skip-ci: win-gpu"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T07:27:18Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nExecute Infrastructure\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n**What**\r\n\r\nAdd the **Hyperball** constraint to the `Muon` optimizer so each parameter can be trained in one of four modes, plus a decoupled `muon_epsilon`.\r\n\r\n- **`MuonParamInfo.use_hyperball`** (new field, default `False`): combined with `use_muon`, selects the per-parameter mode — **MuonH** (Muon direction + sphere projection), **AdamH** (Adam direction + sphere projection), plain **Muon**,\r\n  plain **AdamW**.\r\n- **`Muon._hyperball_apply`** (new): the shared projection `W_{t+1} = R·Normalize(W_t − η·R·Normalize(u))`, with `R = ‖W‖_F` recomputed every step (per-matrix for 2D, per-expert for 3D). No weight decay; `R` is not stored in the checkpoint.\r\n- **`Muon._adamw_hyperball_update`** (new): the AdamH path — Adam-preconditioned direction fed into `_hyperball_apply`.\r\n- **`muon_epsilon`** (new ctor arg, default `None`): the Newton–Schulz normalization epsilon, decoupled from `adam_epsilon`. When unset it falls back to `adam_epsilon`, so existing behavior is unchanged.\r\n- **`MuonShardingOptimizer`**: classification changed to `keep_whole = use_muon or use_hyperball`. Hyperball params are kept whole on an owner rank (V1) so the global Frobenius norm is computable locally with no extra collective; plain AdamW stays element-wise sharded (V2).\r\n\r\n**Why**\r\n\r\nHyperball fixes each weight matrix on a Frobenius-norm sphere (replacing weight decay), which needs the full matrix to compute `‖W‖_F`. Decoupling `muon_epsilon` lets the NS orthogonalization use a sensible eps (~1e-7) independent of Adam's much smaller `epsilon`.\r\n\r\n**Backward compatibility**\r\n\r\nFully backward compatible: `use_hyperball` defaults to `False` (sharding reads it via `getattr(..., False)`), and `muon_epsilon` defaults to `adam_epsilon`. Existing configs produce identical behavior.\r\n\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79792", "url": "https://github.com/PaddlePaddle/Paddle/pull/79793"}
{"number": 79786, "title": "[Performance] Add num_workers to paddle.load for parallel payload reading", "state": "closed", "created_at": "2026-09-17T16:28:04Z", "updated_at": "2026-09-21T06:37:12Z", "closed_at": "2026-09-21T06:37:12Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T06:37:12Z", "body": "### PR Category\n\nPerformance Optimization\n\n### PR Types\n\nPerformance\n\n### Description\n\n#### 背景\n\n`paddle.load` 加载 checkpoint 时全程单线程搬运张量数据，带宽远低于存储与主机的能力。把 8GB checkpoint 放到 `/dev/shm`（完全排除磁盘）后 `paddle.load` 仍需 6.7s（1.2 GB/s），说明瓶颈不在 IO 而在单线程拷贝：pickle 对张量数据不做任何变换，`protocol >= 3` 存下来的字节与内存逐位相同，元信息只占极小比例（实测一个 2.1GB 的 `.distcp` 文件中 opcode + 名字 + dtype/shape 合计仅 1,106 字节，占 0.00005%），因此耗时几乎全在“把 GB 级字节搬一遍”。\n\n#### 方案\n\n保持 pickle 格式完全不变，只改变“谁来搬”：\n\n1. `_ParallelPayloadFile` 包装 checkpoint 文件。CPython 的 C 版 unpickler 对写在 pickle frame 之外的 payload 会调用文件对象的 `readinto()`（`protocol >= 4` 的 framer 对 >= 64KB 的数据就写在 frame 之外）；\n2. `readinto(b)` 收到 >= 1MB 的请求时，把 `memoryview(b)` 按 32MB 切块交给线程池的 `os.preadv` 并行读，**等所有块写完才返回**，随后把底层文件指针前进 `len(b)`；小于阈值的请求原样转发给串行 `readinto`；\n3. `os.preadv` 是系统调用，执行期间释放 GIL，所以线程能真正并行；切块互不重叠，每个字节只被一个线程写一次，无需加锁。切块的原因是张量数量少时（2.1GB 的 `.distcp` 只有 3 个 payload）“一个 payload 一个线程”用不满线程池。\n\n关键点是**数据在 `readinto` 返回前已经就位**：unpickler 观察到的行为与普通 `readinto` 完全一致，代码对这块内存的生命周期不做任何假设，因此 `bytearray` / `bytes` / `str` 这类在解析期就会拷贝或解码的 payload 同样安全。（初版曾用“先跳过、解析完再回填”的写法，对 protocol 4 的 `bytearray` 会写进已释放的临时缓冲区，属于越界写；已按 review 意见重写，并补了对应单测。）\n\npickle 的解析工作一步没少，安全反序列化（`RestrictedUnpickler`）照常生效，下游 `_pack_loaded_dict` / `StructuredToParameterName@@` / `_parse_load_result` 等后处理无需改动。\n\n#### 接口\n\n默认关闭，行为与改动前完全一致：\n\n```python\npaddle.load(path)                    # 串行，同现状\npaddle.load(path, num_workers=32)    # 并行读取 payload\n\ndist.load_state_dict(sd, path, num_workers=32)   # dcp 侧同名参数，默认 1\n```\n\n以下情况自动回退到原串行实现，只 warn 不报错：传入 `BytesIO`、macOS（走 `_pickle_loads_mac`）、无 `os.preadv`（Windows）、文件中没有 >= 1MB 的 payload（含 `protocol == 2`，payload 以 latin1 文本存储），以及并行读取过程中的任何异常（整体丢弃结果、`seek(0)` 后重新串行加载，不会返回半填充的张量）。`num_workers` 的校验放在 `_parse_load_config`，因此非法参数在所有平台上都会报错。\n\n#### 实测数据\n\n本地 NVMe（ext4），加载真实的 flex_checkpoint `.distcp` 文件：\n\n- 2.10 GB / 10 个张量：冷缓存 2.41s -> 0.92s（2.6x），热缓存 2.14s -> 0.64s（3.3x）\n- tmpfs 上 8GB 合成 checkpoint：6.67s -> 1.08s（6.2x）\n- 单卡 `dist.load_state_dict`（256MB checkpoint）：`num_workers=1` 0.473s -> `num_workers=8` 0.214s（2.2x）\n\n收益上限取决于存储带宽：在冷读带宽约 1.8 GB/s 的网络盘上只有约 1.4x（IO 本身已经是下界），本地盘或 page cache 命中时可达 3~6x。\n\n#### 单测\n\n新增 `test/legacy_test/test_paddle_load_num_workers.py`，29 个 case：\n\n- 精度：多个大张量、大小张量混合、7 种 dtype（fp32/fp64/fp16/bf16/int64/int32/bool）、NaN/±Inf/-0.0、嵌套结构、`Layer.state_dict()`（含参数 name 恢复）、tensor place 与串行一致、不同 `num_workers`、重复加载稳定性。全部使用**逐字节比对**而非 `allclose`；\n- 非张量 payload：`bytearray` / `bytes` / `str` 在 protocol 4/5 下重复加载校验，覆盖上面提到的缓冲区生命周期问题；\n- 参数校验：非 int / 负数分别抛 `TypeError` / `ValueError`，`None`/`0`/`1` 走串行；\n- 回退路径：protocol 2/3/4/5、仅小张量、`BytesIO`、缺少 `os.preadv`、`sys.platform == 'darwin'`、注入 `preadv` 短读；\n- 白盒：payload 覆盖率 > 99%、大 buffer 会降低命中率（解释 `_BUFFER_SIZE = 8192` 的取值）、小 payload 不进快路径、短读抛 `EOFError`。\n\nLinux 29 个全部通过；Windows（无 `os.preadv`）与 macOS（`paddle.load` 走 `_pickle_loads_mac`）下依赖快路径的用例用 `skipUnless` 跳过，其余用例仍校验回退结果与串行逐字节一致。\n\n### 是否引起精度变化\n\n否。\n\n并行路径与串行路径读取同一文件的同一偏移，结果逐位一致：2.10GB 文件 10/10 个张量、7.83GB 文件 161/161 个张量的 `dtype`/`shape` 相同且 `tobytes()` 完全相等；单测中对 NaN/±Inf/-0.0 也做了逐字节校验。并行读取过程中的任何异常都会整体丢弃结果、退回串行加载，不会返回半填充的张量。\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79786"}
{"number": 79792, "title": "Support hyperball optim-method and shardingV1 for adam.", "state": "closed", "created_at": "2026-09-20T13:22:42Z", "updated_at": "2026-09-21T06:10:55Z", "closed_at": "2026-09-21T06:10:55Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-21T06:10:55Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nExecute Infrastructure\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n**What**\r\n\r\nAdd the **Hyperball** constraint to the `Muon` optimizer so each parameter can be trained in one of four modes, plus a decoupled `muon_epsilon`.\r\n\r\n- **`MuonParamInfo.use_hyperball`** (new field, default `False`): combined with `use_muon`, selects the per-parameter mode — **MuonH** (Muon direction + sphere projection), **AdamH** (Adam direction + sphere projection), plain **Muon**,\r\n  plain **AdamW**.\r\n- **`Muon._hyperball_apply`** (new): the shared projection `W_{t+1} = R·Normalize(W_t − η·R·Normalize(u))`, with `R = ‖W‖_F` recomputed every step (per-matrix for 2D, per-expert for 3D). No weight decay; `R` is not stored in the checkpoint.\r\n- **`Muon._adamw_hyperball_update`** (new): the AdamH path — Adam-preconditioned direction fed into `_hyperball_apply`.\r\n- **`muon_epsilon`** (new ctor arg, default `None`): the Newton–Schulz normalization epsilon, decoupled from `adam_epsilon`. When unset it falls back to `adam_epsilon`, so existing behavior is unchanged.\r\n- **`MuonShardingOptimizer`**: classification changed to `keep_whole = use_muon or use_hyperball`. Hyperball params are kept whole on an owner rank (V1) so the global Frobenius norm is computable locally with no extra collective; plain AdamW stays element-wise sharded (V2).\r\n\r\n**Why**\r\n\r\nHyperball fixes each weight matrix on a Frobenius-norm sphere (replacing weight decay), which needs the full matrix to compute `‖W‖_F`. Decoupling `muon_epsilon` lets the NS orthogonalization use a sensible eps (~1e-7) independent of Adam's much smaller `epsilon`.\r\n\r\n**Backward compatibility**\r\n\r\nFully backward compatible: `use_hyperball` defaults to `False` (sharding reads it via `getattr(..., False)`), and `muon_epsilon` defaults to `adam_epsilon`. Existing configs produce identical behavior.\r\n\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79792"}
{"number": 79788, "title": "Fix the bug in CommBuffer when param and grad have different shape", "state": "closed", "created_at": "2026-09-18T07:55:08Z", "updated_at": "2026-09-20T08:05:25Z", "closed_at": "2026-09-20T07:02:19Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T07:02:19Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nDistributed Strategy\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes \r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nWhen copying grads to the communication buffer, FusedCommBuffer will set the flattened grad's shape with param shape. This causes an error when param and grad have different shape. \r\n\r\nThis pr fix this bug by setting flattened grad's shape with the original grad shape.\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79788"}
{"number": 79680, "title": "[RL FIX] Fix nopp rl error", "state": "closed", "created_at": "2026-08-20T10:47:05Z", "updated_at": "2026-09-20T07:47:58Z", "closed_at": "2026-09-20T07:47:57Z", "labels": ["skip-ci: dcu"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T07:47:57Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nDistributed Strategy\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nrl场景下不开pp时报错，为已知报错，pp场景以前修复过，现在迁移到NoPP场景下\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79680"}
{"number": 79784, "title": "[Cherry-Pick][Operator Mechanism] Add int64-safe batched GEMM and align bmm N==1 with Torch", "state": "closed", "created_at": "2026-09-17T11:17:06Z", "updated_at": "2026-09-20T06:52:52Z", "closed_at": "2026-09-20T06:52:52Z", "labels": ["skip-ci: api-benchmark"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T06:52:52Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 将 Phi BLAS 的 GEMM 和 GEMV 接口中的矩阵维度及 leading dimension 参数升级为 `int64_t`。当参数超过 `INT_MAX` 时，调用 CUDA 12.3 及以上版本提供的 64 位 cuBLAS API。\r\n\r\n#### 修改前 Paddle 不同数据类型的 GEMM 调用\r\n\r\n| 数据类型 | 普通尺寸 | 64 位尺寸 |\r\n| --- | --- | --- |\r\n| float | flag 关闭：cublasSgemm；flag 开启：cublasSgemmEx | flag 关闭：不支持；flag 开启：cublasSgemmEx_64 |\r\n| double | cublasDgemm | 不支持 |\r\n| float16 | cublasGemmEx | cublasGemmEx_64 |\r\n| bfloat16 | cublasGemmEx | cublasGemmEx_64 |\r\n| complex64 | cublasGemmEx | cublasGemmEx_64 |\r\n| complex128 | cublasGemmEx | cublasGemmEx_64 |\r\n\r\n#### PyTorch 不同数据类型的 GEMM 调用\r\n\r\n| 数据类型 | 普通尺寸 | 64 位尺寸 |\r\n| --- | --- | --- |\r\n| float | cublasSgemm |不支持，TORCH_CHECK报错 |\r\n| double | cublasDgemm | 不支持，TORCH_CHECK报错 |\r\n| float16 | cublasGemmEx | 不支持，TORCH_CHECK报错 |\r\n| bfloat16 | cublasGemmEx | 不支持，TORCH_CHECK报错 |\r\n| complex64 | cublasCgemm | 不支持，TORCH_CHECK报错 |\r\n| complex128 | cublasZgemm | 不支持，TORCH_CHECK报错 |\r\n\r\n#### 修改后不同数据类型的 GEMM 调用，与Pytorch调用的API对齐\r\n\r\n| 数据类型 | 普通尺寸 | 64 位尺寸 |\r\n| --- | --- | --- |\r\n| float | `cublasSgemm` | `cublasSgemm_v2_64` |\r\n| double | `cublasDgemm` | `cublasDgemm_v2_64` |\r\n| float16 | `cublasGemmEx` | `cublasGemmEx_64` |\r\n| bfloat16 | `cublasGemmEx` | `cublasGemmEx_64` |\r\n| complex64 | `cublasCgemm` | `cublasCgemm_v2_64` |\r\n| complex128 | `cublasZgemm` | `cublasZgemm_v2_64` |\r\n\r\n#### API修改包括：\r\n\r\n- FP32 GEMM 不再根据 Tensor Core flag 在 cublasSgemm 和 cublasSgemmEx 之间切换，统一使用 PyTorch 同类路径采用的 cublasSgemm。\r\n- complex精度下不再调用cublasGemmEx，而是与Pytorch对齐，分别调用cublasCgemm、cublasZgemm。\r\n- 对于所有的数据类型都进行了int64_t支持。\r\n\r\n#### Tensor Core Flag 行为\r\n\r\n**调整前**：FP32 GEMM 在 `FLAGS_enable_cublas_tensor_op_math=1` 时，会从 `cublasSgemm` 切换到 `cublasSgemmEx`。\r\n\r\n**调整后**：`FLAGS_enable_cublas_tensor_op_math` 这个 FLAG **已被删除**（详见下方「BC Breaking」小节）。FP32 GEMM 无论如何都调用：\r\n\r\n- 普通尺寸：`cublasSgemm`\r\n- 64 位尺寸：`cublasSgemm_v2_64`\r\n\r\nTF32 现在统一由 `FLAGS_cublas_allow_tf32` 控制。它不再影响所调用的 API，只决定使用哪个 cuBLAS handle：\r\n\r\n| `FLAGS_cublas_allow_tf32` | FP32 GEMM API | Handle |\r\n| --- | --- | --- |\r\n| 0 | cublasSgemm / cublasSgemm_v2_64 | 默认 cuBLAS handle |\r\n| 1 | cublasSgemm / cublasSgemm_v2_64 | Tensor Core math-mode handle（`gpu_context.cc:727`、`custom_context.cc:327`） |\r\n\r\n#### BC Breaking：随 #79601 回移带入的接口 / FLAG 删除\r\n\r\n为使 release/3.4 的 BLAS 实现与 develop 对齐，本 PR 随 #79601 一并删除了下列 TF32 相关的开关（与 develop 保持一致，属**有意删除**）：\r\n\r\n| 删除项 | 类型 | 迁移方式 |\r\n| --- | --- | --- |\r\n| `paddle.base.core.set_cublas_switch` / `get_cublas_switch` | 公开 Python 接口（v3.2.0 / v3.3.0 / v3.4.0 均已发布） | 改用 `FLAGS_cublas_allow_tf32` |\r\n| `FLAGS_enable_cublas_tensor_op_math` | 环境 FLAG | 改用 `FLAGS_cublas_allow_tf32` |\r\n| `test/legacy_test/test_tf32_cublas.py::TestTF32Switch` | 单测 | 随接口删除 |\r\n\r\n**影响面**：\r\n\r\n- 3.4.x 用户升级到本补丁版本后，调用 `paddle.base.core.set_cublas_switch` / `get_cublas_switch` 会直接抛 `AttributeError`。\r\n- 此前通过 `FLAGS_enable_cublas_tensor_op_math=1` 打开 TF32 的用户，需改为设置 `FLAGS_cublas_allow_tf32=1`，否则将回落到 `FLAGS_cublas_allow_tf32` 的默认行为（可能与原先不同）。\r\n\r\n### CUDA Version Checks and Error Handling\r\n\r\n本 PR 同时整理了 GEMM_EX 的版本判断和错误处理：\r\n\r\n- 抽取公共的 GemmEx/GemmEx64 实现，减少 FP16、BF16 和 complex 等类型中重复的 cuBLAS 调用代码。\r\n- cublasGemmEx 要求 CUDA 8.0 及以上。\r\n- cublasGemmEx_64 以及其他 64 位 cuBLAS API 要求 CUDA 12.3 及以上，并且当前仅在 Linux 下启用。\r\n- BF16 GEMM 继续要求 CUDA 11.0 及以上以及支持 BF16 的 GPU compute capability。\r\n\r\n原有 FP16 路径在 CUDA 8.0 以下会回退到 cublasHgemm。该回退路径与正常的 cublasGemmEx 路径在 API、compute type 和计算语义上并不完全一致。\r\n\r\n本 PR 删除这些 CUDA 8.0 以下的分散 fallback。当构建环境不支持所需 API 时，统一抛出明确错误。\r\n\r\n---\r\n\r\n本 PR 让**批量 GEMM / MatMul** 支持超过 `INT_MAX` 的维度，并把 bmm 在 `N == 1` 时的操作数布局对齐 PyTorch。它是 #79601（单次 GEMM/GEMV 的 int64 化）的后续：批量路径此前要么直接 `PADDLE_THROW` 拒绝，要么在 `static_cast<int>` 处静默截断。\r\n\r\n改动的核心取舍是**「绝大多数 cuBLAS 调用形式保持不变」**：所有既有的 cuBLAS 调用实参文本逐字保留，64 位分派与 N==1 布局都通过调用之前的局部变量实现。\r\n\r\n#### 1. 接口加宽（`blas.h`）\r\n\r\n以下签名由 `int` 改为 `int64_t`：\r\n\r\n- `Blas<DeviceContext>::MatMul(int64_t M, int64_t N, int64_t K, const T* A, const T* B, T* C)`\r\n- 指针数组版 `BatchedGEMM(... int64_t M, int64_t N, int64_t K, ..., int64_t batchCount)`\r\n- `BatchedGEMMWithHead(... int64_t W1, int64_t H1, int64_t W2, int64_t H2, ..., int64_t batchCount, ...)`\r\n- `MatMulWithHead` / `MatMulWithHeadGradImpl` 的 `head_number`\r\n- MKLML 打包 API：`GEMM_ALLOC`、`GEMM_PACK`、`GEMM_COMPUTE`（含其 `lda/ldb/ldc/ld`）\r\n\r\n#### 2. GPU：`> INT_MAX` 走 cuBLAS 64 位 API（`blas_impl.cu.h`、`dynload/cublas.h`）\r\n\r\n每个 strided / 指针数组重载里统一引入\r\n\r\n```cpp\r\nconst bool requires_64_bit_blas = M > INT_MAX_VALUE || N > INT_MAX_VALUE ||\r\n                                  K > INT_MAX_VALUE ||\r\n                                  batchCount > INT_MAX_VALUE;\r\n```\r\n\r\n- 为真 → 走 `_64` 入口；为假 → 走原来的 32 位入口，实参文本不变，只是用 `static_cast<int>` 收窄（由上面的条件保证不会溢出）。\r\n- `batchCount` 也纳入判断：32 位 strided API 的 `strideA/strideB/strideC` 形参本身是 `long long`，原样传递不会溢出，但 `batchCount` 是 `int`。\r\n- 新增 `CUBlas<float/double/float16/complex64/complex128>::GEMM_STRIDED_BATCH_64` 共 5 个成员（bf16 没有 strided 主模板实例化，其批量路径是显式全特化，直接 dynload `cublasGemmStridedBatchedEx(_64)`）。\r\n- 用到的 64 位符号：`cublas{S,D,H,C,Z}gemmStridedBatched_64`、`cublasGemmStridedBatchedEx_64`、`cublasGemmBatchedEx_64`、`cublas{S,D,C,Z}trsm_v2_64`。\r\n- 这些声明与对应实现都包在 `CUDA_VERSION >= 12030 && defined(__linux__)` 里；条件不满足时抛出明确的 `Unimplemented`（而不是截断）。\r\n- 顺带补齐了两处算力门控（HEAD 已存在的不一致）：bf16 strided 重载补 `cc >= 80`，fp16 的 `GEMM(bool transA, ...)` 补 `cc >= 53`。\r\n\r\n#### 3. GPU：`N == 1` 的操作数布局对齐 PyTorch\r\n\r\n行主序的批量 GEMM 交给列主序 cuBLAS 时，通常要交换 A/B。但 PyTorch 在 `N == 1` 时用的是**不交换**的形式（`baddbmm_out_cuda_impl` 按 out 的 stride 选择，`cublasAdjustLdLevel3` 在 `n <= 1` 时把 `ldc` 修正为 `max(m, 1)`）。\r\n\r\nPaddle 侧不新增调用分支，而是在调用前就地改写入参：\r\n\r\n```cpp\r\nif (N == 1 && !FLAGS_use_legacy_gemm) {\r\n  ldc = std::max<int64_t>(M, 1);\r\n  std::swap(M, N);\r\n  std::swap(lda, ldb);\r\n  std::swap(strideA, strideB);\r\n  std::swap(A, B);\r\n  const cublasOperation_t cu_trans_a = cuTransA;\r\n  cuTransA = (cuTransB == CUBLAS_OP_T) ? CUBLAS_OP_N : CUBLAS_OP_T;\r\n  cuTransB = (cu_trans_a == CUBLAS_OP_T) ? CUBLAS_OP_N : CUBLAS_OP_T;\r\n}\r\n```\r\n\r\n同一段插在 6 个 strided 重载里，因此**下面的 cuBLAS 调用一个字都不用动**。`N == 1` 时列主序 `M×1` 与行主序 `M×1` 的内存序相同，交换是安全的；四种 trans 组合下 `lda/ldb/ldc` 都满足列主序最小 ld 要求。\r\n\r\n同时删除了 HEAD 里一个失效的重复分支：它的条件是 `N == 1 && ldc >= std::max<int64_t>(1, M) && ...`，而同函数里 `ldc` 初值就是 `N`，条件退化为 `M <= 1 && N == 1`，实际上只对 `[b,1,K] @ [b,K,1]` 生效；被它包住的调用文本保留（只是去掉了外层 `{}`，整体左移两格）。\r\n\r\n#### 4. CPU：显式收窄 + int64 指针运算（`blas_impl.h`）\r\n\r\n- 删除 `Blas<CPUContext>::BatchedGEMM` 顶部那句 `\"CPU BatchedGEMM only supports ... not larger than INT_MAX\"` 的整块 `PADDLE_THROW`，改为在真正需要 32 位参数时用 `detail::to_blas_int(M, \"BatchedGEMM M\")` 收窄 —— 报错信息会带上具体是哪个维度、期望值、实际值，而不是一句笼统的 `Unimplemented`。\r\n- `const int64_t strideC = M * N;` 取代 `&C[k * M * N]`：原写法是 `int * int`，`M * N` 稍大（例如 `M = N = 50000`，即 2.5e9）就会溢出，与是否支持 int64 无关，**这是 CPU 上的一个真实正确性问题**。\r\n- `MatMulWithHead` / `BatchedGEMMWithHead` 里的 `sub_matA_offset` / `sub_matB_offset` / `sub_matC_offset` / `lda/ldb/ldc` / 循环下标全部提到 `int64_t`（head 切分后每个子 GEMM 的维度仍然是小的，但**偏移量**会落在原张量的大坐标上）。\r\n- `CBlas<T>` 的 `GEMM_ALLOC/GEMM_PACK/GEMM_COMPUTE` 保持 `int`，由外层 `to_blas_int` 收窄。\r\n\r\n#### 5. MatMul 上层的守卫清理（`matmul_kernel_impl.h`）\r\n\r\n- case 9（`GEMV(y_batch_size * N, ...)`）与 case 11（`GEMM(x_batch_size * M, ...)`）删除 `PADDLE_ENFORCE_LE_INT_MAX`。检查没有消失，而是下沉到 BLAS 层：`Blas<GPUContext>::GEMV` 内部 `if (M > INT_MAX || N > INT_MAX)` 走 `GEMV_64`（老 CUDA 干净抛出），`GEMM` 走 `requires_64_bit_blas`，CPU 侧走 `to_blas_int`。这两处从「拒绝」变成「支持」。\r\n- case 14（广播 + 指针数组）的守卫**保留**，只是上移到 `Out->ResizeAndAllocate` 之前，避免超大批次先分配输出再报错。条件是 `is_broadcast_dims && x_batch_size != 1 && y_batch_size != 1`，与 `else` 分支（case 14）的进入条件完全等价（分支链：case 8 → 9/10 → 11/12 → 13 → else）。\r\n- `MatMulFunctionImplWithCublasLt` 的 case 9/11 守卫删除是另一回事：`blaslt::Run` 的形参本来就是 `int64_t`，且 `cublasLtMatrixLayoutCreate` 接收 `uint64_t rows/cols`，原来的 `int` 守卫会把合法的 64 位尺寸拒掉。该函数里 `LE_INT_MAX(out_batch_size)` 与 batch 属性的 `static_cast<int>` 保持原样（`CUBLASLT_MATRIX_LAYOUT_BATCH_COUNT` 本身是 int32）。\r\n\r\n#### 6. HIP（`blas_impl.hip.h`，顺带）\r\n\r\n- fp16 的 `GEMM` 在 `compute_type == rocblas_datatype_f16_r` 时仍然把 `float` 的 alpha/beta 传进去，而 rocBLAS 是按 computeType 宽度读标量的，等于读了 4 字节里的前 2 字节。现在该分支改用转换后的 `phi::float16` 标量（`t_alpha`/`t_beta`），非 fp16 组合行为不变。\r\n- 5 个 `BatchedGEMM` 重载里 `\"Hip BatchedGEMM not supported for large tensor size\"` 的 `PADDLE_THROW` 换成 `detail::to_blas_int` 显式校验，与 CUDA 路径的报错风格一致（HIP 没有 64 位入口，超限仍然会抛，只是信息更明确）。\r\n\r\n#### 7. 测试\r\n\r\n- `test/cpp/phi/kernels/test_math_function.cc`：\r\n  - `batched_gemm_matmul_int64_dimensions`：`MatMul` 与指针数组 `BatchedGEMM` 走 int64 形参，结果都是 `{4, 5, 10, 11}`。\r\n  - `batched_gemm_matmul_reject_invalid_dimensions`：`batchCount`/`M` 取 `INT_MAX + 1` 时抛 `EnforceNotMet`。\r\n- `test/cpp/phi/kernels/test_math_function.cu`：\r\n  - `batched_gemm_strided_column_output`：`BatchedGemmStridedTest` 覆盖 `(batch, m, n, k)` = `(3,5,1,4)`、`(2,1,1,6)`、`(3,5,2,4)` × 四种 `trans_a/trans_b` 组合 × float/double，与 host 参考实现比对。其中 `n == 1` 的配置即第 3 节的未交换布局，`n == 2` 的配置作为交换布局的回归护栏。\r\n\r\n### 验证\r\n\r\n本机 `WITH_TESTING=OFF`、无 gtest、只构建了 libphi（无 `libpaddle.so`），因此两个新增测试文件无法直接 `ninja`，是**把测试体抠出来在真机上跑的**（判据与测试文件逐字一致）：\r\n\r\n| 检查 | 结果 |\r\n| --- | --- |\r\n| `ninja` 编译 `matmul_kernel.cu.o` / `bmm_kernel.cu.o` / `matmul_grad_kernel.cu.o` / `bmm_grad_kernel.cu.o` / `phi_core/.../cpu/matmul_kernel.cc.o` | 全绿 |\r\n| clang-format 13.0.0（仓库 pin 版本，`-style=file`） | `blas_impl.cu.h` 全文零 diff |\r\n| GPU 新增用例（B30Z，cc 10.3，20 个配置） | **368 checks / 0 failures** |\r\n| CPU 新增用例（含两个超限拒绝路径） | **10 checks / 0 failures** |\r\n| `_64` 分派真机冒烟：`M = INT_MAX + 1`、`N = K = 1`、唯一非零元放在数组末端 | `C[M-1] == 2`，即没有静默截断 |\r\n| N==1 对齐：Paddle 实参形式 vs 改动前的交换形式，36 配置（tA×tB×M{1,4,7}×K{1,3,5}） | 逐位相同 |\r\n| N==1 对齐：Paddle 实参形式 vs PyTorch 自己的实参顺序，9 配置 | 逐位相同 |\r\n\r\n关于第 3 节的可测性：**它属于调用形式对齐，不是数值修复**。改动前的交换形式本来就是对的，两种形式在 cuBLAS 12.8.3 / 13.3 + B30Z 上逐位相同，因此不存在一个「修复前 FAIL、修复后 PASS」的数值用例；上表里的逐位比对 + 新增 UT 中 `n == 2` 的回归护栏就是它的回归价值所在。\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79772\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79784"}
{"number": 79772, "title": " [Operator Mechanism] Add int64-safe batched GEMM and align bmm N==1 with Torch", "state": "closed", "created_at": "2026-09-15T08:16:22Z", "updated_at": "2026-09-20T06:43:53Z", "closed_at": "2026-09-20T06:43:53Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T06:43:53Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 让**批量 GEMM / MatMul** 支持超过 `INT_MAX` 的维度，并把 bmm 在 `N == 1` 时的操作数布局对齐 PyTorch。它是 #79601（单次 GEMM/GEMV 的 int64 化）的后续：批量路径此前要么直接 `PADDLE_THROW` 拒绝，要么在 `static_cast<int>` 处静默截断。\r\n\r\n改动的核心取舍是**「绝大多数 cuBLAS 调用形式保持不变」**：所有既有的 cuBLAS 调用实参文本逐字保留，64 位分派与 N==1 布局都通过调用之前的局部变量实现。\r\n\r\n#### 1. 接口加宽（`blas.h`）\r\n\r\n以下签名由 `int` 改为 `int64_t`：\r\n\r\n- `Blas<DeviceContext>::MatMul(int64_t M, int64_t N, int64_t K, const T* A, const T* B, T* C)`\r\n- 指针数组版 `BatchedGEMM(... int64_t M, int64_t N, int64_t K, ..., int64_t batchCount)`\r\n- `BatchedGEMMWithHead(... int64_t W1, int64_t H1, int64_t W2, int64_t H2, ..., int64_t batchCount, ...)`\r\n- `MatMulWithHead` / `MatMulWithHeadGradImpl` 的 `head_number`\r\n- MKLML 打包 API：`GEMM_ALLOC`、`GEMM_PACK`、`GEMM_COMPUTE`（含其 `lda/ldb/ldc/ld`）\r\n\r\n#### 2. GPU：`> INT_MAX` 走 cuBLAS 64 位 API（`blas_impl.cu.h`、`dynload/cublas.h`）\r\n\r\n每个 strided / 指针数组重载里统一引入\r\n\r\n```cpp\r\nconst bool requires_64_bit_blas = M > INT_MAX_VALUE || N > INT_MAX_VALUE ||\r\n                                  K > INT_MAX_VALUE ||\r\n                                  batchCount > INT_MAX_VALUE;\r\n```\r\n\r\n- 为真 → 走 `_64` 入口；为假 → 走原来的 32 位入口，实参文本不变，只是用 `static_cast<int>` 收窄（由上面的条件保证不会溢出）。\r\n- `batchCount` 也纳入判断：32 位 strided API 的 `strideA/strideB/strideC` 形参本身是 `long long`，原样传递不会溢出，但 `batchCount` 是 `int`。\r\n- 新增 `CUBlas<float/double/float16/complex64/complex128>::GEMM_STRIDED_BATCH_64` 共 5 个成员（bf16 没有 strided 主模板实例化，其批量路径是显式全特化，直接 dynload `cublasGemmStridedBatchedEx(_64)`）。\r\n- 用到的 64 位符号：`cublas{S,D,H,C,Z}gemmStridedBatched_64`、`cublasGemmStridedBatchedEx_64`、`cublasGemmBatchedEx_64`、`cublas{S,D,C,Z}trsm_v2_64`。\r\n- 这些声明与对应实现都包在 `CUDA_VERSION >= 12030 && defined(__linux__)` 里；条件不满足时抛出明确的 `Unimplemented`（而不是截断）。\r\n- 顺带补齐了两处算力门控（HEAD 已存在的不一致）：bf16 strided 重载补 `cc >= 80`，fp16 的 `GEMM(bool transA, ...)` 补 `cc >= 53`。\r\n\r\n#### 3. GPU：`N == 1` 的操作数布局对齐 PyTorch\r\n\r\n行主序的批量 GEMM 交给列主序 cuBLAS 时，通常要交换 A/B。但 PyTorch 在 `N == 1` 时用的是**不交换**的形式（`baddbmm_out_cuda_impl` 按 out 的 stride 选择，`cublasAdjustLdLevel3` 在 `n <= 1` 时把 `ldc` 修正为 `max(m, 1)`）。\r\n\r\nPaddle 侧不新增调用分支，而是在调用前就地改写入参：\r\n\r\n```cpp\r\nif (N == 1 && !FLAGS_use_legacy_gemm) {\r\n  ldc = std::max<int64_t>(M, 1);\r\n  std::swap(M, N);\r\n  std::swap(lda, ldb);\r\n  std::swap(strideA, strideB);\r\n  std::swap(A, B);\r\n  const cublasOperation_t cu_trans_a = cuTransA;\r\n  cuTransA = (cuTransB == CUBLAS_OP_T) ? CUBLAS_OP_N : CUBLAS_OP_T;\r\n  cuTransB = (cu_trans_a == CUBLAS_OP_T) ? CUBLAS_OP_N : CUBLAS_OP_T;\r\n}\r\n```\r\n\r\n同一段插在 6 个 strided 重载里，因此**下面的 cuBLAS 调用一个字都不用动**。`N == 1` 时列主序 `M×1` 与行主序 `M×1` 的内存序相同，交换是安全的；四种 trans 组合下 `lda/ldb/ldc` 都满足列主序最小 ld 要求。\r\n\r\n同时删除了 HEAD 里一个失效的重复分支：它的条件是 `N == 1 && ldc >= std::max<int64_t>(1, M) && ...`，而同函数里 `ldc` 初值就是 `N`，条件退化为 `M <= 1 && N == 1`，实际上只对 `[b,1,K] @ [b,K,1]` 生效；被它包住的调用文本保留（只是去掉了外层 `{}`，整体左移两格）。\r\n\r\n#### 4. CPU：显式收窄 + int64 指针运算（`blas_impl.h`）\r\n\r\n- 删除 `Blas<CPUContext>::BatchedGEMM` 顶部那句 `\"CPU BatchedGEMM only supports ... not larger than INT_MAX\"` 的整块 `PADDLE_THROW`，改为在真正需要 32 位参数时用 `detail::to_blas_int(M, \"BatchedGEMM M\")` 收窄 —— 报错信息会带上具体是哪个维度、期望值、实际值，而不是一句笼统的 `Unimplemented`。\r\n- `const int64_t strideC = M * N;` 取代 `&C[k * M * N]`：原写法是 `int * int`，`M * N` 稍大（例如 `M = N = 50000`，即 2.5e9）就会溢出，与是否支持 int64 无关，**这是 CPU 上的一个真实正确性问题**。\r\n- `MatMulWithHead` / `BatchedGEMMWithHead` 里的 `sub_matA_offset` / `sub_matB_offset` / `sub_matC_offset` / `lda/ldb/ldc` / 循环下标全部提到 `int64_t`（head 切分后每个子 GEMM 的维度仍然是小的，但**偏移量**会落在原张量的大坐标上）。\r\n- `CBlas<T>` 的 `GEMM_ALLOC/GEMM_PACK/GEMM_COMPUTE` 保持 `int`，由外层 `to_blas_int` 收窄。\r\n\r\n#### 5. MatMul 上层的守卫清理（`matmul_kernel_impl.h`）\r\n\r\n- case 9（`GEMV(y_batch_size * N, ...)`）与 case 11（`GEMM(x_batch_size * M, ...)`）删除 `PADDLE_ENFORCE_LE_INT_MAX`。检查没有消失，而是下沉到 BLAS 层：`Blas<GPUContext>::GEMV` 内部 `if (M > INT_MAX || N > INT_MAX)` 走 `GEMV_64`（老 CUDA 干净抛出），`GEMM` 走 `requires_64_bit_blas`，CPU 侧走 `to_blas_int`。这两处从「拒绝」变成「支持」。\r\n- case 14（广播 + 指针数组）的守卫**保留**，只是上移到 `Out->ResizeAndAllocate` 之前，避免超大批次先分配输出再报错。条件是 `is_broadcast_dims && x_batch_size != 1 && y_batch_size != 1`，与 `else` 分支（case 14）的进入条件完全等价（分支链：case 8 → 9/10 → 11/12 → 13 → else）。\r\n- `MatMulFunctionImplWithCublasLt` 的 case 9/11 守卫删除是另一回事：`blaslt::Run` 的形参本来就是 `int64_t`，且 `cublasLtMatrixLayoutCreate` 接收 `uint64_t rows/cols`，原来的 `int` 守卫会把合法的 64 位尺寸拒掉。该函数里 `LE_INT_MAX(out_batch_size)` 与 batch 属性的 `static_cast<int>` 保持原样（`CUBLASLT_MATRIX_LAYOUT_BATCH_COUNT` 本身是 int32）。\r\n\r\n#### 6. HIP（`blas_impl.hip.h`，顺带）\r\n\r\n- fp16 的 `GEMM` 在 `compute_type == rocblas_datatype_f16_r` 时仍然把 `float` 的 alpha/beta 传进去，而 rocBLAS 是按 computeType 宽度读标量的，等于读了 4 字节里的前 2 字节。现在该分支改用转换后的 `phi::float16` 标量（`t_alpha`/`t_beta`），非 fp16 组合行为不变。\r\n- 5 个 `BatchedGEMM` 重载里 `\"Hip BatchedGEMM not supported for large tensor size\"` 的 `PADDLE_THROW` 换成 `detail::to_blas_int` 显式校验，与 CUDA 路径的报错风格一致（HIP 没有 64 位入口，超限仍然会抛，只是信息更明确）。\r\n\r\n#### 7. 测试\r\n\r\n- `test/cpp/phi/kernels/test_math_function.cc`：\r\n  - `batched_gemm_matmul_int64_dimensions`：`MatMul` 与指针数组 `BatchedGEMM` 走 int64 形参，结果都是 `{4, 5, 10, 11}`。\r\n  - `batched_gemm_matmul_reject_invalid_dimensions`：`batchCount`/`M` 取 `INT_MAX + 1` 时抛 `EnforceNotMet`。\r\n- `test/cpp/phi/kernels/test_math_function.cu`：\r\n  - `batched_gemm_strided_column_output`：`BatchedGemmStridedTest` 覆盖 `(batch, m, n, k)` = `(3,5,1,4)`、`(2,1,1,6)`、`(3,5,2,4)` × 四种 `trans_a/trans_b` 组合 × float/double，与 host 参考实现比对。其中 `n == 1` 的配置即第 3 节的未交换布局，`n == 2` 的配置作为交换布局的回归护栏。\r\n\r\n### 验证\r\n\r\n本机 `WITH_TESTING=OFF`、无 gtest、只构建了 libphi（无 `libpaddle.so`），因此两个新增测试文件无法直接 `ninja`，是**把测试体抠出来在真机上跑的**（判据与测试文件逐字一致）：\r\n\r\n| 检查 | 结果 |\r\n| --- | --- |\r\n| `ninja` 编译 `matmul_kernel.cu.o` / `bmm_kernel.cu.o` / `matmul_grad_kernel.cu.o` / `bmm_grad_kernel.cu.o` / `phi_core/.../cpu/matmul_kernel.cc.o` | 全绿 |\r\n| clang-format 13.0.0（仓库 pin 版本，`-style=file`） | `blas_impl.cu.h` 全文零 diff |\r\n| GPU 新增用例（B30Z，cc 10.3，20 个配置） | **368 checks / 0 failures** |\r\n| CPU 新增用例（含两个超限拒绝路径） | **10 checks / 0 failures** |\r\n| `_64` 分派真机冒烟：`M = INT_MAX + 1`、`N = K = 1`、唯一非零元放在数组末端 | `C[M-1] == 2`，即没有静默截断 |\r\n| N==1 对齐：Paddle 实参形式 vs 改动前的交换形式，36 配置（tA×tB×M{1,4,7}×K{1,3,5}） | 逐位相同 |\r\n| N==1 对齐：Paddle 实参形式 vs PyTorch 自己的实参顺序，9 配置 | 逐位相同 |\r\n\r\n关于第 3 节的可测性：**它属于调用形式对齐，不是数值修复**。改动前的交换形式本来就是对的，两种形式在 cuBLAS 12.8.3 / 13.3 + B30Z 上逐位相同，因此不存在一个「修复前 FAIL、修复后 PASS」的数值用例；上表里的逐位比对 + 新增 UT 中 `n == 2` 的回归护栏就是它的回归价值所在。\r\n\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79772"}
{"number": 79789, "title": "skip deterministic tests for take_along_axis_grad at ROCm", "state": "closed", "created_at": "2026-09-18T13:07:31Z", "updated_at": "2026-09-20T03:48:19Z", "closed_at": "2026-09-20T03:48:18Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-20T03:48:18Z", "body": "### PR Category\r\nOperator Mechanism\r\n\r\n\r\n### PR Types\r\nOthers\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\ntake_along_axis_grad 的 deterministic 相关单测只在 CUDA 上有确定性反向路径，在 ROCm 上并不适用。\r\n之前这些用例仅用 `not core.is_compiled_with_cuda()` 作为跳过条件，导致 ROCm 环境下仍会执行并失败。\r\n本 PR 将四处 `skipIf` 的条件改为 `not core.is_compiled_with_cuda() or core.is_compiled_with_rocm()`，在 ROCm 下正确跳过。同时删除一段已注释掉的 `test_check_output` 冗余代码。仅涉及测试文件，无框架逻辑改动。\r\n\r\n### 是否引起精度变化\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79789"}
{"number": 51508, "title": "add register of bincount", "state": "closed", "created_at": "2023-03-11T05:35:19Z", "updated_at": "2026-09-19T12:45:40Z", "closed_at": "2023-03-12T12:07:15Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": "2023-03-12T12:07:15Z", "body": "<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n### PR types\r\n Others\r\n\r\n### PR changes\r\n Others \r\n\r\n### Describe\r\n #51292  case11\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/51508"}
{"number": 79778, "title": "[Cherry-Pick] optimize cummax/cummin GPU scan kernels", "state": "open", "created_at": "2026-09-15T11:08:48Z", "updated_at": "2026-09-18T04:13:45Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "\r\n### PR Category\r\nPerformance Optimization\r\n\r\n### PR Types\r\nPerformance\r\n\r\n### Description\r\n<!-- Describe what you've done -->\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79729\r\n\r\n### 是否引起精度变化\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79778"}
{"number": 77770, "title": "Fix DataLoader to support non-Tensor batch data (strings, custom objects)", "state": "closed", "created_at": "2026-02-09T02:38:17Z", "updated_at": "2026-09-17T16:37:14Z", "closed_at": "2026-03-07T10:30:29Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`DataLoader` silently yields nothing when a dataset returns non-Tensor data (e.g., strings, custom objects). The C++ blocking queue treats an empty `DenseTensorArray` as end-of-stream, so batches with zero tensors are dropped.\n\n```python\nfrom paddle.io import Dataset, DataLoader\n\nclass StringDataset(Dataset):\n    def __getitem__(self, idx):\n        return \"a\"\n    def __len__(self):\n        return 20\n\nloader = DataLoader(StringDataset(), batch_size=10, collate_fn=lambda d: d)\nfor batch in loader:\n    print(batch)  # Previously: no output. Now: ['a', 'a', ...]\n```\n\n### Changes\n\n- **`python/paddle/io/dataloader/flat.py`**: When `_flatten_batch` produces an empty `flat_batch` (no tensors extracted), inject a dummy sentinel tensor (`np.array([0])`) and wrap the structure in a `_NonTensorBatch` marker. `_restore_batch` detects this marker and returns the original structure, discarding the dummy.\n- **`test/legacy_test/test_multiprocess_dataloader_dynamic.py`**: Added tests for string and custom-object datasets in both single-process and multi-process (`num_workers=2`) modes.\n\n<!-- START COPILOT ORIGINAL PROMPT -->\n\n\n\n<details>\n\n<summary>Original prompt</summary>\n\n> \n> ----\n> \n> *This section details on the original issue you should resolve*\n> \n> <issue_title>paddle的Dataset, DataLoader不支持返回非Tensor类数据</issue_title>\n> <issue_description>### bug描述 Describe the Bug\n> \n> 如题，如果是使用torch框架运行如下代码：\n> ```Python\n> #from paddle.io import Dataset, DataLoader\n> from torch.utils.data import DataLoader, Dataset\n> # define a random dataset\n> class RandomDataset(Dataset):\n>     def __init__(self, num_samples):\n>         self.num_samples = num_samples\n> \n>     def __getitem__(self, idx):\n>         return \"a\"\n> \n>     def __len__(self):\n>         return self.num_samples\n> \n> def f(d):\n>     return d\n> \n> dataset = RandomDataset(20)\n> \n> loader = DataLoader(dataset,\n>                     batch_size=10,\n>                     shuffle=True,\n>                     drop_last=True,\n>                     collate_fn=f)\n> \n> for e in range(1):\n>     for i, j in enumerate(loader):\n>         print(j)\n> \n> ```\n> 结果为\n> ```Text\n> E:\\code\\paddleaudio补全\\别的贡献计划\\ECD_but_paddle>python C:\\Users\\15895\\Desktop\\functions.py\n> ['a', 'a', 'a', 'a', 'a', 'a', 'a', 'a', 'a', 'a']\n> ['a', 'a', 'a', 'a', 'a', 'a', 'a', 'a', 'a', 'a']\n> \n> E:\\code\\paddleaudio补全\\别的贡献计划\\ECD_but_paddle>\n> ```\n> 如果是把运行框架换成paddle:\n> ```Python\n> from paddle.io import Dataset, DataLoader\n> #from torch.utils.data import DataLoader, Dataset\n> # define a random dataset\n> class RandomDataset(Dataset):\n>     def __init__(self, num_samples):\n>         self.num_samples = num_samples\n> \n>     def __getitem__(self, idx):\n>         return \"a\"\n> \n>     def __len__(self):\n>         return self.num_samples\n> \n> def f(d):\n>     return d\n> \n> dataset = RandomDataset(20)\n> \n> loader = DataLoader(dataset,\n>                     batch_size=10,\n>                     shuffle=True,\n>                     drop_last=True,\n>                     collate_fn=f)\n> \n> for e in range(1):\n>     for i, j in enumerate(loader):\n>         print(j)\n> \n> ```\n> 结果为\n> ```Text\n> E:\\code\\paddleaudio补全\\别的贡献计划\\ECD_but_paddle>python C:\\Users\\15895\\Desktop\\functions.py\n> \n> E:\\code\\paddleaudio补全\\别的贡献计划\\ECD_but_paddle>\n> ```\n> 经过本人两个小时的debug，触发迭代终止的逻辑似乎写在C++里面了，大概是C++那边判断类型不是Tensor就给直接抛出异常了。paddle框架的DataLoader应该支持非Tensor类型数据的返回，否则将会影响众多项目的复现。\n> \n> ### 其他补充信息 Additional Supplementary Information\n> \n> 首当其冲的就是 https://github.com/PFCCLab/paddle_geometric 这个库\n> 因为这个库的加载数据的逻辑是，返回一个包含自定义数据类型的列表\n> 我滴妈呀，反正我用paddle_geometric的时候是不敢用框架内的加载逻辑了\n> 感觉当下想要解决这个问题，最好的办法就是自己写一个DataLoader类，难绷\n> 可惜了我又因此debug了两个小时，复现模型时时间居然主要浪费在和框架斗智斗勇这一块\n> 总之， PaddlePaddle/Paddle#77752 和这个issue如果不能想办法解决，paddle_geometric这个库就别想活了（\n> 毕竟人家那个库的逻辑在那，要么修复框架，要么对库逻辑重构，确实没办法</issue_description>\n> \n> ## Comments on the Issue (you are @copilot in this section)\n> \n> <comments>\n> </comments>\n> \n\n\n</details>\n\n\n\n<!-- START COPILOT CODING AGENT SUFFIX -->\n\n- Fixes PaddlePaddle/Paddle#77754\n\n<!-- START COPILOT CODING AGENT TIPS -->\n---\n\n💬 We'd love your input! Share your thoughts on Copilot coding agent in our [2 minute survey](https://gh.io/copilot-coding-agent-survey).\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/77770"}
{"number": 73482, "title": "Revert \"fix bc bug of big tensor\"", "state": "closed", "created_at": "2025-06-19T13:30:26Z", "updated_at": "2026-09-17T16:36:59Z", "closed_at": "2026-07-07T06:43:09Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Reverts PaddlePaddle/Paddle#73105", "url": "https://github.com/PaddlePaddle/Paddle/pull/73482"}
{"number": 77682, "title": "[SOT] Add `paddle.core.nvprof_*` to `NATIVE_CODE_PURE_FUNCTIONS`", "state": "closed", "created_at": "2026-02-04T08:27:04Z", "updated_at": "2026-09-17T16:36:26Z", "closed_at": "2026-09-17T16:36:20Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nExecute Infrastructure\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nImprovements\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n<!-- pcard-91591 -->\r\nAdd `paddle.core.nvprof_*` to `NATIVE_CODE_PURE_FUNCTIONS`\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/77682"}
{"number": 79442, "title": "Revert \"Revert \"[release/3.4][Operator Mechanism]Align cuBLAS workspace size for SM10 GPUs\"\"", "state": "closed", "created_at": "2026-07-08T08:48:12Z", "updated_at": "2026-09-17T16:35:25Z", "closed_at": "2026-07-10T00:43:35Z", "labels": ["skip-ci: coverage"], "comments_count": null, "is_pr": true, "merged_at": "2026-07-10T00:43:35Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nReverts PaddlePaddle/Paddle#79406\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79443\r\n\r\n调整 SM10 架构 GPU 上的 cuBLAS workspace size 设置，将其与 PyTorch 在 Hopper/Blackwell 架构上的行为对齐。\r\n\r\n  具体修改：\r\n  - 在 `gpu_context.cc` 中更新 `GetCublasWorkspaceSize` 逻辑；\r\n  - 对 SM9 和 SM10 架构使用 32MiB cuBLAS workspace；\r\n  - 其他架构保持原有约 8.125MiB workspace 设置不变。\r\n\r\n  该修改用于减少因 cuBLAS workspace size 不一致导致的算法选择差异，从而改善部分 matmul 场景下与 PyTorch\r\n  的数值对齐表现。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是", "url": "https://github.com/PaddlePaddle/Paddle/pull/79442"}
{"number": 79355, "title": "CI: avoid hard-fail in Linux-CPU tests when quick-disable list fetch is unavailable", "state": "closed", "created_at": "2026-06-22T10:43:32Z", "updated_at": "2026-09-17T16:35:15Z", "closed_at": "2026-06-23T02:26:38Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`Linux-CPU / Build and test` was failing in the test stage with exit code `102` before running `ctest`. The failure path came from quick-disable list retrieval, where transient fetch errors terminated the job instead of using the existing fallback pattern.\n\n- **Root cause**\n  - `ci/utils.sh:get_quickly_disable_ut` called `exit 102` on fetch failure, aborting the whole test script.\n\n- **Change**\n  - Replace hard exit with graceful fallback in `get_quickly_disable_ut`:\n    - keep `disable_ut_quickly='disable_ut'`\n    - return non-zero (`return 1`) instead of exiting the process\n    - emit explicit fallback log message\n\n- **Why this aligns with current call sites**\n  - Callers already use short-circuit fallback style, e.g.:\n    ```bash\n    get_quickly_disable_ut || disable_ut_quickly='disable_ut'\n    ```\n\n- **Example patch shape**\n  ```bash\n  else\n      echo \"Failed to fetch quickly disabled unittests, fallback to local default.\"\n      disable_ut_quickly='disable_ut'\n      return 1\n  fi\n  ```", "url": "https://github.com/PaddlePaddle/Paddle/pull/79355"}
{"number": 79443, "title": "Revert \"Revert \"[Operator Mechanism]Align cuBLAS workspace size for SM10 GPUs\"\"", "state": "closed", "created_at": "2026-07-08T08:48:25Z", "updated_at": "2026-09-17T16:34:57Z", "closed_at": "2026-07-10T01:40:54Z", "labels": ["skip-ci: coverage"], "comments_count": null, "is_pr": true, "merged_at": "2026-07-10T01:40:53Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nReverts PaddlePaddle/Paddle#79405\r\n\r\n调整 SM10 架构 GPU 上的 cuBLAS workspace size 设置，将其与 PyTorch 在 Hopper/Blackwell 架构上的行为对齐。\r\n\r\n  具体修改：\r\n  - 在 `gpu_context.cc` 中更新 `GetCublasWorkspaceSize` 逻辑；\r\n  - 对 SM9 和 SM10 架构使用 32MiB cuBLAS workspace；\r\n  - 其他架构保持原有约 8.125MiB workspace 设置不变。\r\n\r\n  该修改用于减少因 cuBLAS workspace size 不一致导致的算法选择差异，从而改善部分 matmul 场景下与 PyTorch\r\n  的数值对齐表现。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79443"}
{"number": 79745, "title": "Harden Fleet CI yq download path in H-Coverage workflow", "state": "closed", "created_at": "2026-09-07T13:21:50Z", "updated_at": "2026-09-17T16:34:34Z", "closed_at": "2026-09-07T13:55:41Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`Fleet Unit test (single card)` was failing before tests ran because `yq` download intermittently failed with proxy tunneling errors in the `Install PaddleFleet` step. This change makes that external fetch resilient without altering test logic or package selection.\n\n- **Root-cause path addressed**\n  - `H-Coverage.yml` failed on a single-shot `wget` to GitHub release assets (`yq_linux_amd64`), causing job exit on transient proxy/network failure.\n\n- **Workflow hardening (minimal scope)**\n  - Updated `Install PaddleFleet` in both Fleet jobs:\n    - `Fleet Unit test (single card)`\n    - `Fleet Unit test (multi-card)`\n  - Replaced one-shot download with retry/timeout flags and a `--no-proxy` fallback attempt.\n\n- **Representative change**\n  ```bash\n  wget --tries=5 --retry-connrefused --waitretry=2 --timeout=30 --read-timeout=30 \\\n    https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 \\\n    -O /usr/local/bin/yq || \\\n  wget --no-proxy --tries=5 --retry-connrefused --waitretry=2 --timeout=30 --read-timeout=30 \\\n    https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 \\\n    -O /usr/local/bin/yq\n  chmod +x /usr/local/bin/yq\n  ```", "url": "https://github.com/PaddlePaddle/Paddle/pull/79745"}
{"number": 79768, "title": "[Cherry-Pick] Add machine-balanced partitioning for Muon 2D sharding (#79763 )", "state": "closed", "created_at": "2026-09-14T04:01:31Z", "updated_at": "2026-09-17T14:13:01Z", "closed_at": "2026-09-17T14:13:01Z", "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T14:13:01Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nPerformance Optimization\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nPerformance\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79763\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79768"}
{"number": 79763, "title": "Add machine-balanced partitioning for Muon 2D sharding", "state": "closed", "created_at": "2026-09-11T02:12:39Z", "updated_at": "2026-09-17T14:12:18Z", "closed_at": "2026-09-17T14:12:18Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T14:12:18Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nPerformance Optimization \r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nImprovements\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n优化muon 2d参数的切分，增加了machine_balanced_2d_partition开关（默认为关），当开启后参数切分将会尽可能做到机器之间的均衡\r\n\r\n步骤如下：\r\n1. 通信拿到所有rank和机器node的映射\r\n2. 通信是的每个rank可以看到全局的muon 2D参数\r\n3. 贪心的将数据往rank上放，放的顺序如下\r\n    a. 找到压力最小的机器\r\n    b. 找到拥挤程度最低的机器\r\n    c. 找到该机器上压力最小的rank\r\n\r\n【测试结果】：在8机情况下，开启ep=4，pp=0的测试条件下，生成checkpoint\r\n1. **正确性检验：** \r\n    a. 接续热启训练：checkpoint-10、checkpoint-40为老版本代码生成；checkpoint-20、checkpoint-30为新版本代码生成。 四次训练的loss值正常接续\r\n    b. 代码修改前后收敛测试结果验证（4000步误差<0.001）：\r\n<img width=\"2200\" height=\"1320\" alt=\"loss_norm_compare\" src=\"https://github.com/user-attachments/assets/4088a2da-df05-478f-a21a-2e974c9c01fb\" />\r\n\r\n\r\n2. **负载均衡检验：** \r\n  文件大小：\r\n  老版本的所有权重均聚集于排序靠前的rank\r\n  <img width=\"340\" height=\"142\" alt=\"image\" src=\"https://github.com/user-attachments/assets/0d2c4505-625e-49ac-8d1e-5d23f4cc29e5\" />\r\n  \r\n\r\n  新版本权重按照机器均分，高负载机器尽可能远离（1号机器和8号机器）\r\n  <img width=\"350\" height=\"162\" alt=\"image\" src=\"https://github.com/user-attachments/assets/352edfc1-19d4-4fb5-9f82-4c54350a0ccb\" />\r\n\r\nTODO：目前开启machine_balanced_2d_partition的同时开启EMA会产生问题，因为PaddleFormers在判定机器布局，并行策略相同后就不会进行reshard，这是不对的，需要进行修改。待修改后才可同时使用 （fixed by：https://github.com/PaddlePaddle/PaddleFleet/pull/2056 ）\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是\r\n\r\n精度变化仅来源于 nccl reduce 的 dst 不同时累加顺序有变化，理论上和上面实测都是零偏", "url": "https://github.com/PaddlePaddle/Paddle/pull/79763"}
{"number": 75456, "title": "Fleety 12", "state": "closed", "created_at": "2025-09-22T13:15:48Z", "updated_at": "2026-09-17T09:28:43Z", "closed_at": "2026-09-17T09:28:43Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nDistributed Strategy\r\n\r\n### PR Types\r\n<!-- One of [ New features |  | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n当开启流水并行并且在pp_configs中配置sync_param sync_moment时，会在每一个流水步之后，从pp_stage 0向与其共享参数另外的pp_stage 进行参数和优化器状态的同步（broadcast）。\r\n\r\n本PR在\"参数和优化器状态的同步\"过程的多个通信（broadcast）按照global的名称进行排序，避免使用local的名称进行排序导致出现通信交叉而出现hang的问题\r\npcard-73263\r\n\r\ndevelop PR：https://github.com/PaddlePaddle/Paddle/pull/75365\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/75456"}
{"number": 75728, "title": "test ci  no merge", "state": "closed", "created_at": "2025-10-10T13:21:00Z", "updated_at": "2026-09-17T09:28:10Z", "closed_at": "2026-09-17T09:28:10Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\n\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/75728"}
{"number": 74115, "title": "pp shared_comm", "state": "closed", "created_at": "2025-07-18T09:22:16Z", "updated_at": "2026-09-17T09:28:01Z", "closed_at": "2026-09-17T09:28:01Z", "labels": ["skip-ci: coverage"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nUser Experience\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n现有pp共享参数使用不同的通信组来共享不同的参数，比如用户组网如下\r\n\r\n```\r\npp_stage 0：\r\nshared_layer{**w0**, **w1**, **w2**}\r\n\r\npp_stage 1：\r\nshared_layer{**w0**, w1, w2}\r\n\r\npp_stage 2：\r\nshared_layer{w0, **w1**, w2}\r\n\r\npp_stage 3：\r\nshared_layer{w0, w1, **w2**}\r\n```\r\n\r\n\r\n这种情况下对于pp_stage0的参与方，需要建立[0,1] ,[0, 2], [0,3]三个通信组\r\npp_stage0一般处于显存的峰值的位置，建立多个通信组对其显存造成压力\r\n\r\n现在改成建立一个大的通信组[0, 1, 2, 3]这种方式来进行共享参数的广播，相比于上述方式会多一些通信量，但是会少建通信组。\r\n\r\n另外这种方式仍然兼容原始的方式，用户可改写组网如下，与原始方式行为相同\r\n\r\n```\r\npp_stage 0：\r\nshared_layer_0{**w0**}\r\nshared_layer_1{**w1**}\r\nshared_layer_2{**w2**}\r\n\r\npp_stage 1：\r\nshared_layer_0{**w0**}\r\n\r\npp_stage 2：\r\nshared_layer_1{**w1**}\r\n\r\npp_stage 3：\r\nshared_layer_2{**w2**}\r\n```\r\nPcard-76459", "url": "https://github.com/PaddlePaddle/Paddle/pull/74115"}
{"number": 79782, "title": "[release/3.4][Performance] Parallelise moe_permute", "state": "closed", "created_at": "2026-09-16T08:40:31Z", "updated_at": "2026-09-17T08:53:26Z", "closed_at": "2026-09-17T08:53:26Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T08:53:26Z", "body": "### PR Category\nPerformance Optimization, Operator Mechanism\n\n### PR Types\nPerformance, Bug fixes\n\n### Description\n\n`moe_permute` was one of the two largest data-movement kernels in an MoE\ntraining step for us. Two rewrites make it 1.9x faster for bfloat16 and 3.3x\nfor float8 at our production shape, and no shape we measured is slower. A third\nchange fixes two pre-existing misaligned accesses that abort the kernel\noutright.\n\n**The limiter was parallelism, not bandwidth.** This is worth stating because\nthe usual reading of \"a pure movement kernel at low DRAM utilisation\" is \"the\naccesses are not coalesced\", and acting on that reading here would have been\nwasted work. Two independent measurements say otherwise:\n\n| evidence | reading |\n|---|---|\n| bfloat16 vs float8 | 1297.3 vs 1295.8 us, 0.1% apart -- float8 moves half the bytes in the same time, so the time does not track the byte count |\n| `do_gather=false`, i.e. no token movement at all | saves only 18% |\n\n#### 1. Replace the cross-block serial chain in phase 1\n\nPhase 1b needs, for every (block, expert), how many rows the preceding\nsame-parity blocks assigned to that expert. It was computed with a chain: a\nblock spin-waited on `global_expertwise_block_cumsum` until its predecessor\npublished a running offset with `atomicExch`, then published its own for\n`blockIdx + 2`. The chain is `gridDim.x / 2` deep and every hop is an L2 atomic\nround trip, so the cost scaled with the grid rather than with the data -- at our\nproduction shape, 2406 hops at roughly 419 ns each, which is essentially the\nwhole kernel. Spin-waiting on another block also relies on blocks being\nlaunched roughly in order, which the programming model does not guarantee.\n\nIt is replaced by two kernels with no dependency between them:\n`permute_block_count_kernel` (same block-to-row mapping) writes the per-block\nper-expert row counts, and `permute_chain_scan_kernel` (one block per expert per\nparity) turns them in place into the exclusive prefix sum over preceding\nsame-parity blocks. `chain_offset` becomes an ordinary load. Phase 1 goes from\n1068.6 to 61.6 us.\n\nThe offsets are the same integers, so every row lands in the same output slot.\nDropping the chain also drops the sentinel pre-fill of the cumsum buffer and the\ntwo extra rows it carried for publishing to `blockIdx + 2`, and the launcher\nonly runs the two kernels when there is more than one block -- with a single\nblock there are no preceding blocks, so every offset is zero.\n\n#### 2. Move token rows with one warp per row instead of one row at a time\n\nPhase 2 walked the block's 32 rows strictly serially: per row a\n`cuda::pipeline` wait, two block synchronisations, a single\n`cp_async_bulk_shared_to_global` issued by thread 0, and a\n`cp_async_bulk_wait_group_read<0>` drain. Only one row (2 or 4 KB) was ever in\nflight per block, 255 of the 256 threads waited on thread 0, and every row was\nstaged through shared memory on the way from global to global.\n\nEach warp now owns one source row -- so `warp_num` rows are in flight per block\n-- and moves it with 16 B vector loads and stores straight through registers,\nwith two vectors per lane so the stores of one overlap the load of the next. No\nshared-memory staging, no pipeline, no per-row drain, and one load now feeds all\n`topk` destinations. bfloat16 goes from 797.7 to 246.8 us, float8 from 814.2 to\n192.9 us.\n\nThis makes the TMA store path unreachable: it required the same 16 B row\nalignment as the new gather, so `use_tma && do_gather` now always selects the\ngather, and the ping/pong buffers and `cp_async_bulk` loop are removed. TMA\nstill stages `routemap` and `probs`, which every warp reads repeatedly. Not\nstaging token rows also returns `2 * token_dim * sizeof(dtype)` of shared memory\nper block. The remaining phase-2 fallback, taken when a token row is not a whole\nnumber of 16 B vectors, is the block-cooperative element copy that was already\nthere.\n\nNeither rewrite performs any arithmetic on the token data -- one changes how an\ninteger offset is computed, the other is pure movement -- so the output is\nbit-identical rather than merely close.\n\n#### 3. Fix two misaligned 16 B accesses (pre-existing)\n\n`moe_permute` touched token rows with 16 B vectors in two places that never\nchecked the address: the phase-2 fallback that copies a token row, and the\npadding-row fill. A row starts at `token_dim * sizeof(dtype) * row`, so as soon\nas `token_dim` is not a whole number of 16 B every other row is misaligned and\nthe kernel aborts with `cudaErrorMisalignedAddress`. Reachable from the public\nAPI, for instance bfloat16 with `token_dim = 12` and `padding_alignment = 128`.\n`token_dim = 4` does not reproduce it: the row holds no whole 16 B vector, so\nonly the scalar tail runs.\n\nBoth now go through the alignment-checked helpers -- `try_vectorized_memcpy` for\nthe token row, and a new `try_vectorized_memset` for the padding fill. Where the\nrow happens to be aligned the emitted accesses are unchanged.\n\n#### Verification\n\nNo arithmetic on token data changes: item 1 only changes how an integer offset is\ncomputed and produces the same integers, item 2 is pure movement, and item 3 adds\nan address check ahead of accesses that were already being made. So the bar here\nis bit-identical output, not a tolerance.\n\nTwo independent tracks, over 46 shapes covering: a single block\n(`rows <= 32`) and both parities of the block mapping; `token_dim` aligned and\nunaligned to 16 B; `topk` 1/2/6/10/16; `num_experts` 1/8/40/128/256/384 with\nexperts that receive no token; `padding_alignment` 1/4/8/128; `do_gather` and\n`return_expert_indices` in every combination; bfloat16 and float8, with and\nwithout ue8m0-packed scales.\n\n* Against an independently computed reference for all five outputs\n  (`hidden_states_unzipped`, `zipped_expertwise_rowmap`, `token_prob_unzipped`,\n  `scale_unzipped`, `expert_indices`): **46/46 exactly equal**. Since the op\n  performs no arithmetic, exact equality with the reference *is* bit-exactness.\n* Against a build of the previous revision of this branch, which still had the\n  old path available: **40/40 shapes bit-identical on every output tensor**, 0\n  mismatches. The remaining 6 are the shapes the old path aborts on, i.e. the\n  ones item 3 fixes.\n* `test/legacy_test/test_moe_permute_unpermute.py`: 11/11 passing.\n\nA second sweep covers 19 degenerate and large shapes: zero rows; every token\nunrouted, so the output itself has zero rows; one expert receiving everything;\n`token_dim` 1; a `topk` where most slots are -1; and at the other end 4M rows,\n1M rows over 64 experts, 100k rows over 384 experts, `token_dim` 65536, and\n400k rows at the production `token_dim`. All 19 match the reference --\n`zipped_expertwise_rowmap`, `token_prob_unzipped` and `expert_indices` in full,\nthe gathered rows and the padding rows sampled.\n\nThat sweep turned up one more shape the old path cannot run: at `token_dim`\n65536 in bfloat16 the TMA path asks for `2 * token_dim * sizeof(dtype)` = 256 KB\nof shared memory for its ping/pong buffers, past the per-block limit, so\n`cudaFuncSetAttribute` fails with `cudaErrorInvalidValue`. Measured, the old\npath tops out at `token_dim` 57344 for bfloat16 (224 KB) and fails from 65536\non. The new one has no such ceiling, because it does not stage token rows in\nshared memory at all -- checked up to `token_dim` 131072.\n\n#### Measurements\n\nOp-level wall time, so this includes the two new kernels, the rowmap fill and\nthe host-side preprocessing -- it is not the kernel-only figure quoted above.\nMinimum of 50-60 iterations on an idle device, two independent passes.\n\n**30 shapes measured, none slower.** The span is 1.03x to 12.55x. The extremes\nare both explained by how much of the op the change can reach: the 1.03x case is\n64 rows over 256 experts at `padding_alignment` 128, where 32544 of the 32768\noutput rows are padding and almost all the time is the padding fill; the 12.55x\ncase is `do_gather=false` at the production shape, which is phase 1 alone, i.e.\nthe chain removal with nothing else in the way.\n\nA representative selection:\n\n| shape | before | after | |\n|---|---|---|---|\n| 153984 rows, token_dim 2048, topk 10, 8 experts, float8 | 1247.5 us | 380.5 us | 3.28x |\n| same, bfloat16 | 1221.8 us | 633.7 us | 1.93x |\n| 16384 rows, topk 1, 8 experts | 165.1 us | 61.7 us | 2.68x |\n| 16384 rows, topk 8, 64 experts, float8 | 237.8 us | 89.2 us | 2.67x |\n| same, bfloat16 | 211.9 us | 120.2 us | 1.76x |\n| 4096 rows | 80.7 us | 46.7 us | 1.73x |\n| 16384 rows, 256 experts | 315.0 us | 194.6 us | 1.62x |\n| 32 rows (single block) | 49.7 us | 37.2 us | 1.34x |\n| 8192 rows, token_dim 8192 | 170.0 us | 134.6 us | 1.26x |\n| 33 / 64 / 256 / 1024 rows | ~51 us | ~43 us | 1.21-1.33x |\n| 153984 rows, 384 experts | 1646.2 us | 1050.8 us | 1.57x |\n| 400000 rows, topk 10 | 3077.6 us | 1613.5 us | 1.91x |\n| 16384 rows, topk 16, 32 experts | 225.9 us | 175.6 us | 1.29x |\n| 16384 rows, `padding_alignment` 1 | 165.7 us | 87.9 us | 1.89x |\n| 16384 rows, `return_expert_indices` | 173.2 us | 93.1 us | 1.86x |\n| 153984 rows, `do_gather=false` | 1089.5 us | 86.8 us | 12.55x |\n| 64 rows, 256 experts (padding-dominated) | 113.1 us | 110.3 us | 1.03x |\n\nBeyond the rows above, the sweep also varies `padding_alignment` (1, 8, 128) and\n`return_expert_indices` at 8 and 256 experts, `topk` 12 and 16, and\n`do_gather=false` at three sizes. Run-to-run spread between the two passes is\n0.3-2.7% on shapes above roughly 150 us and 7-15% below that, where the\nmeasurement is dominated by host-side dispatch rather than by the kernels; every\nratio above is well outside that spread.\n\nThe shapes with an unaligned `token_dim` have no before number, because the old\npath aborts on them. For reference, `token_dim` 2044 costs 194.3 us against\n92.2 us for 2048, so the fallback is about 2.1x the vectorised path -- which is\nthe price of correctness on a shape that used to crash.\n\n#### Note on `moe_unpermute`\n\nAn earlier revision of this PR also rewrote `moe_unpermute` to cover several\nzipped rows per block. It has been reverted, byte for byte, and this PR now\ntouches `moe_permute` only. The reason: measured against the shipped kernel on\nan idle device, the several-rows-per-block mapping wins at the shape it was\ntuned for (1.28x unweighted, 1.57x weighted) but loses elsewhere -- 25% at 64\nlocal experts, 46% at 256, and 21% at `token_dim` 8192. The per-block working\nset is `rows * token_dim`, while the number of rows was chosen from the row\ncount alone. Sizing it correctly is separate work.\n\n### 是否引起精度变化\n否\n\n\n---\n\nCherry-pick of #79756 (authored by @LiYuRio) to `release/3.4`.\n\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79756 <!-- For pass CI -->", "url": "https://github.com/PaddlePaddle/Paddle/pull/79782"}
{"number": 79754, "title": "[Operator Mechanism] Add deterministic path to gather grad cuda kernel", "state": "closed", "created_at": "2026-09-09T05:35:09Z", "updated_at": "2026-09-17T08:36:43Z", "closed_at": "2026-09-17T08:36:43Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T08:36:43Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\nOperator Mechanism\r\n\r\n\r\n### PR Types\r\nOthers\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n背景：`gather_grad` 的 GPU 反向在 `axis != 0` 时走 `funcs::GatherV2GradCUDAFunction`，其 `GatherGradGPUKernel` 对每个元素调用`CudaAtomicAdd`。当 index 含重复值时（gather 反向即 scatter-add），原子加的累加顺序受线程调度影响，导致同一份输入会出现多次梯度不逐位一致。\r\n\r\n在 `FLAGS_cudnn_deterministic`=true时，为 `axis != 0` 增加一条确定性反向路径，保持累加顺序的一致以及结果的稳定可复现，用于在PaddleAPITest中实现和torch的逐位对齐\r\n- 新增`gather_grad_deterministic_cuda_kernel`：每个线程独占一个 `(inner_dim_index, outer_dim_index)` 列，在列内顺序遍历index完成梯度累加，因此同一元素的多次累加顺序固定\r\n- 新增host 侧 `GatherGradDeterministicCUDAFunction`，索引映射与既有`GatherGradGPUKernel` 保持一致，支持 int32/int64 index\r\n\r\np.s 另外修复了gather_grad在处理0-D index时outer_dim计算错误的问题：0-D index下前向将在output中删除axis维，output仅有`input.dims()-1`维，所以gather_grad中计算outer_dim时要从axis维开始而不是axis+1维\r\n\r\n### 是否引起精度变化\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79754"}
{"number": 79753, "title": "[Operator Mechanism] Add deterministic path for take_along_axis_grad cuda kernel", "state": "closed", "created_at": "2026-09-09T01:20:10Z", "updated_at": "2026-09-17T08:36:31Z", "closed_at": "2026-09-17T08:36:31Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T08:36:31Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\nOperator Mechanism\r\n\r\n### PR Types\r\nothers\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n为take_along_axis增加了确定性`TakeAlongAxisGradDeterministicKernel`\r\n\r\n**背景**：paddle.take_along_axis和torch.take_along_dims在反向有误差，原因是双方反向的底层kernel使用先到先得的顺序累加反向梯度，受warp调度影响累加顺序不固定，当同一个索引位置被写超过三次+，受浮点数不满足结合律的影响，相同输入每次运行的结果不一致。torch的反向有deterministic path（`index_put_with_sort_kernel`，但不是专门为take_along_dims的反向设计），通过对全局索引进行排序获取稳定写入顺序，paddle中有对应行为的`IndexPutWithSortKernel`\r\n\r\n本pr中在take_along_axis_grad_kernel中为paddle增加了和torch的逻辑一致的deterministic path，模拟torch的_scatter_via_index_put按`IndexPutWithSortKernel`/`index_put_with_sort_kernel`的参数要求处理index数组\r\n\r\n复现case：\r\n`paddle.take_along_axis(Tensor(paddle.Size([1048576, 256]),\"float32\"), Tensor(paddle.Size([1048576, 6]),\"int64\"), axis=1, )`\r\n测试需要：\r\n```\r\ntorch.use_deterministic_algorithms(True)\r\npaddle.set_flags({'FLAGS_use_accuracy_compatible_kernel': True})\r\n```\r\n\r\n### 是否引起精度变化\r\n否\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79753"}
{"number": 79774, "title": "[Cherry-Pick][Operator Mechanism] Align CPU var and std with PyTorch", "state": "closed", "created_at": "2026-09-15T09:13:37Z", "updated_at": "2026-09-17T06:20:54Z", "closed_at": "2026-09-17T06:20:54Z", "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T06:20:54Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n \r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 对齐 Paddle `var/std` 在 CPU、GPU、动态图、PIR、静态图和 stride 路径上的行为。每类修改分别说明如下。\r\n\r\n#### 1. CPU 非有限值处理\r\n\r\n**修改前的问题**\r\n\r\nCPU kernel 中的 `StoreFloatOrDouble` 将所有非有限 float 统一写成 `+Inf`。例如：\r\n\r\n```python\r\nx = paddle.to_tensor([float(\"nan\")], dtype=\"float32\")\r\npaddle.var(x)\r\n```\r\n\r\nPyTorch 返回 `NaN`，原 Paddle 返回 `+Inf`；`-Inf` 的符号也会丢失。\r\n\r\n**修改内容**\r\n\r\n重写结果存储逻辑，分别处理 NaN、`+Inf`、`-Inf`，并仅将有限值溢出转换成对应符号的 Inf。\r\n\r\n**修改后结果**\r\n\r\nCPU `var/std` 保持 IEEE 非有限值语义，与 PyTorch 的 NaN/Inf 分类和符号一致。\r\n\r\n#### 2. CPU 累加算法和 stride 对齐\r\n\r\n**修改前的问题**\r\n\r\n原 CPU 实现依赖 mean/sum 组合和连续内存访问。对转置等非连续 view，累加访问顺序与 PyTorch 不同，结果可能无法逐位一致；多输出计算也完全串行。\r\n\r\n例如，对转置输入：\r\n\r\n```python\r\nx = paddle.to_tensor(data).transpose([1, 0])\r\npaddle.var(x, axis=0)\r\n```\r\n\r\n原实现和 PyTorch 的最后若干 bit 可能不同。\r\n\r\n**修改内容**\r\n\r\n- float/double 单输出使用 mean + squared-difference；\r\n- 多输出使用 double Welford；\r\n- correction 使用 `max(0, n - correction)`；\r\n- 增加输出维度级 OpenMP 并行；\r\n- \r\n**修改后结果**\r\n\r\n连续和非连续输入的 CPU `var/std` 与 PyTorch 保持逐位一致，并支持更完整的 CPU dtype。\r\n\r\n#### 3. 显式 `axis=[]` 语义\r\n\r\n**修改前的问题**\r\n\r\n原 Python API、native backward 和 composite backward 会把 `axis=[]` 当成完整 reduction。实际上 PyTorch 的行为是：\r\n\r\n- forward 仍然计算整体 variance/std；\r\n- backward divisor 使用 `1`；\r\n- `axis=None` 才使用实际 reduction size。\r\n\r\n例如 `x=[1, 1.7]` 时，`axis=[]` 的 backward 不应与 `axis=None` 共用同一个 divisor。\r\n\r\n**修改内容**\r\n\r\n- Python 层将 `axis=None` 规范化为完整 axis，显式 `axis=[]` 保持为空；\r\n- CPU/GPU `var_grad` 分别处理空 axis；\r\n- 两套 composite backward 规则保留空 axis，并使用独立 divisor。\r\n\r\n**修改后结果**\r\n\r\nforward 结果保持整体 reduction，backward 的 `axis=[]` 与 `axis=None` 按 PyTorch 语义分别计算，native 和 composite 路径一致。\r\n\r\n#### 4. correction 奇异边界及其 `axis=[]` 回归\r\n\r\n**修改前的问题**\r\n\r\n当完整 reduction 满足 `n - correction <= 0` 时，原 backward 直接进行有符号除零。例如：\r\n\r\n```python\r\nx = paddle.to_tensor([1.0, 2.0], stop_gradient=False)\r\ny = paddle.var(x, correction=2)\r\ny.backward()\r\n```\r\n\r\n原 Paddle 可能得到 `[-Inf, +Inf]`，PyTorch 返回 `[+Inf, +Inf]`；恒定输入时 PyTorch 返回 NaN。\r\n\r\n第一次加入 singular branch 时又出现了新的回归：条件仅判断 `denom <= 0`，把 `axis=[]` 也错误纳入 full reduction 处理，破坏了上一节已经修复的空 axis 行为。\r\n\r\n**修改内容**\r\n\r\n- CPU/GPU `var_grad` 在 full reduction 且 `n - correction <= 0` 时使用：\r\n  - `x == mean` -> NaN；\r\n  - `x != mean` -> `+Inf`；\r\n- 将 singular handling 限制为归一化后选中全部维度的 reduction；\r\n- 显式 `axis=[]` 不进入该分支，继续使用其独立 divisor。\r\n\r\n**修改后结果**\r\n\r\n完整 reduction 的 correction 边界与 PyTorch 对齐，同时不再破坏 `axis=[]` 的特殊 backward 语义。\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79747\r\n### 是否引起精度变化\r\n\r\n是。该 PR 有意修正 CPU/native `var/std` 与 PyTorch 不一致的数值行为，可能改变 NaN/Inf、非连续输入、低精度和 correction 边界下的结果；对于正常有限输入，目标是与 PyTorch 保持逐位一致。", "url": "https://github.com/PaddlePaddle/Paddle/pull/79774"}
{"number": 79775, "title": "[Cherry-Pick][Operator Mechanism] Consolidate strided view validation and gradient fixes", "state": "closed", "created_at": "2026-09-15T09:15:01Z", "updated_at": "2026-09-17T06:20:36Z", "closed_at": "2026-09-17T06:20:36Z", "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T06:20:36Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 修复 strided view 的前向安全性和反向梯度正确性，覆盖 `as_strided`、`as_strided_grad`、`tensor_unfold_grad` 以及 ATen compat `as_strided` 路径。主要包含六类修改：\r\n\r\n1. 非连续 Tensor 的 NaN/Inf 检查安全性；\r\n2. strided view 的 shape、offset、存储范围和整数溢出校验；\r\n3. 重叠 view 和重叠 unfold 窗口的梯度累加；\r\n4. 非连续、嵌套 view 的反向坐标映射；\r\n5. ATen compat `as_strided` 的 storage offset 语义对齐；\r\n6. 公共辅助逻辑合并及回归测试补充。\r\n\r\n#### 1. 非连续 Tensor 的 NaN/Inf 检查安全性\r\n\r\n##### 问题\r\n\r\nNaN/Inf 检查直接把 Tensor 当成从首地址开始的连续内存，按 `ptr[i]` 扫描 `numel()` 个元素。这个假设对 strided view 不成立：\r\n\r\n```python\r\nx = paddle.arange(4, dtype='float32')\r\ny = paddle.as_strided(x, shape=[4, 4], stride=[0, 0])\r\n```\r\n\r\n`y` 的 16 个逻辑元素实际只映射到一个存储位置。若仍按连续内存扫描，检查范围会超过底层 allocation；对转置、切片等普通非连续 view，扫描到的内容也不是该 view 的逻辑元素。\r\n\r\n##### 改动\r\n\r\n在 `CheckTensorHasNanOrInf` 中检测到 Tensor 非连续时跳过这次 flat memory scan，并记录 verbose 日志。底层连续 Tensor 仍会在其自身检查路径中被检查。\r\n\r\n##### 修改后结果\r\n\r\n- 不再因为 stride 为 0 或非连续布局读取错误地址；\r\n- 不会把与 view 无关的内存误判为该 Tensor 的内容；\r\n- 连续 Tensor 的原有 NaN/Inf 检查行为不变。\r\n\r\n该行为是本 PR 当前保留的安全策略，未进一步扩大修改范围。\r\n\r\n#### 2. strided view 的 shape、offset、存储范围和溢出校验\r\n\r\n##### 问题\r\n\r\n原实现主要设置 view metadata，不完整验证 `(shape, stride, offset)` 描述的地址范围。非法 view 可能被接受，后续读写越界；短 stride 还会让后续代码访问不存在的 stride 项。\r\n\r\n例如，以下请求都不应创建合法 view：\r\n\r\n```python\r\nx = paddle.zeros([16], dtype='float32')\r\n\r\n# 访问远超过 allocation 的区域\r\npaddle.as_strided(x, shape=[64, 64], stride=[64, 1])\r\n\r\n# offset 超过 allocation\r\npaddle.as_strided(x, shape=[16], stride=[1], offset=4096 * 4)\r\n\r\n# byte offset 没有按元素大小对齐\r\npaddle.as_strided(x, shape=[2], stride=[1], offset=1)\r\n\r\n# shape 和 stride rank 不一致\r\npaddle.as_strided(x, shape=[15, 3], stride=[2])\r\n\r\n# stride * (shape - 1) 的 int64 计算溢出\r\npaddle.as_strided(x, shape=[5], stride=[1 << 62])\r\n```\r\n\r\nATen compat 路径还存在 storage offset 从元素数转换为 byte offset 时的无符号乘法问题：`(1 << 62) * 4` 可能回绕成 0，从而错误地别名到 allocation 开头。\r\n\r\n##### 改动\r\n\r\n- 在已有 strided view 校验中统一要求 `dims.size() == strides.size()`，并把检查放在 zero-size/其他 early return 之前；\r\n- 新增安全的 `int64_t` 加法和乘法，溢出时抛出 `InvalidArgument`，不再依赖回绕后的结果；\r\n- 计算 view 覆盖的最小、最大 storage element，检查是否落在底层 allocation 内；\r\n- 检查 offset 非负、按 dtype element size 对齐；\r\n- 在 ATen compat `as_strided` 和 `as_strided_` 中使用 checked 的 element-to-byte offset 转换，并复用同一套范围校验；\r\n- zero-size view 仍允许不触碰存储的合法形状，非零 view 必须满足完整 rank 和范围约束。\r\n\r\n##### 修改后结果\r\n\r\n- 越界、负 offset、非对齐 offset、rank 不一致和算术溢出均在进入 kernel 前明确报错；\r\n- 不会因为 int64/size_t 回绕而把非法 view 当成合法 view；\r\n- 合法的连续、负 stride、zero-size 和带 offset view 继续保持原有结果。\r\n\r\n#### 3. 重叠 view 和重叠 unfold 窗口的梯度累加\r\n\r\n##### 问题\r\n\r\n当多个逻辑位置映射到同一个 storage slot 时，反向不能使用普通 copy。旧路径会发生写覆盖，而不是把所有 incoming gradient 相加。\r\n\r\n`as_strided` 示例：\r\n\r\n```python\r\nx = paddle.arange(5, dtype='float32')\r\nx.stop_gradient = False\r\ny = paddle.as_strided(x, shape=[3, 3], stride=[1, 1])\r\ny.backward(paddle.ones_like(y))\r\n```\r\n\r\n`x.grad` 的正确结果是：\r\n\r\n```text\r\n[1, 2, 3, 2, 1]\r\n```\r\n\r\nzero-stride view 也必须累加：\r\n\r\n```python\r\nx = paddle.ones([1], dtype='float32')\r\nx.stop_gradient = False\r\ny = paddle.as_strided(x, shape=[4, 4], stride=[0, 0])\r\ny.backward(paddle.ones_like(y))\r\n# x.grad == [16]\r\n```\r\n\r\n`tensor.unfold` 在 `step < size` 时窗口相互重叠，同样存在覆盖问题。例如长度为 5、`size=3`、`step=1` 时，中间元素需要收到 3 个窗口的梯度。\r\n\r\n##### 改动\r\n\r\n- 增加 `MaybeOverlappingStrides`，按 stride 排序判断不同逻辑索引是否可能命中同一 storage slot；\r\n- 增加 `StridedTensorAccumulate`，把 dense 的 `out_grad` 按 view 几何 scatter-add 到目标梯度存储；\r\n- CPU 使用串行 host 累加，避免重复 index 在普通并行写入下丢失更新；\r\n- GPU 在可用 dtype 上使用 `arange + index_put(accumulate=true)` 做 scatter-add；不具备对应 kernel 的 backend 使用 host fallback 后再拷回；\r\n- `as_strided_grad` 和 `tensor_unfold_grad` 在检测到重叠时走累加路径，非重叠场景继续使用更快的 copy 路径；\r\n- 梯度 buffer 在需要累加时先清零，保证未命中位置保持零。\r\n\r\n##### 修改后结果\r\n\r\n- 重复 stride、zero stride、带 offset 的重叠 view 梯度均按出现次数求和；\r\n- overlapping unfold 的窗口贡献不再互相覆盖；\r\n- 非重叠 view 的计算路径和结果保持不变；\r\n- CPU 和 GPU 的梯度结果一致。\r\n\r\n#### 4. 非连续、嵌套 view 的反向坐标映射\r\n\r\n##### 问题\r\n\r\n`input_grad` 是按照输入 Tensor 的逻辑 shape 分配的 dense buffer，而 `as_strided` 的 `offset`、`stride` 描述的是共享 allocation 的 storage 坐标。两者只有在输入连续且 view 完全落在输入范围内时才可以直接用 `offset - input.offset()` 对齐。\r\n\r\n例如：\r\n\r\n```python\r\nbase = paddle.arange(8, dtype='float32')\r\nbase.stop_gradient = False\r\nouter = paddle.as_strided(base, shape=[6], stride=[1], offset=2 * 4)\r\ninner = paddle.as_strided(outer, shape=[3, 3], stride=[1, 1], offset=2 * 4)\r\ninner.backward(paddle.ones_like(inner))\r\n```\r\n\r\n如果直接把 storage index 当成 `outer` 的 row-major index，梯度会写入错误位置；如果 view 跨过输入自身边界，还可能写出 `input_grad`，即使它仍然位于共享 allocation 内。\r\n\r\n##### 改动\r\n\r\n- 对非连续输入或超出输入逻辑范围的 view，先在共享 storage 坐标中建立临时梯度 buffer；\r\n- 将 `out_grad` 按 view 的 shape/stride/offset 累加或复制到该 storage buffer；\r\n- 再按照输入自身的 dims/strides/offset 从 storage buffer gather 回 dense `input_grad`；\r\n- view 与 input 不相交的 storage slot 被自然丢弃，不会写出输入梯度范围；\r\n- 输入自身存在重叠时在入口处明确抛出 `NotImplementedError`，避免对“一个共享 slot 如何分摊到多个输入逻辑位置”做未定义猜测。\r\n\r\n##### 修改后结果\r\n\r\n- 转置、切片、带 offset 的 nested view 反向梯度能回到正确的输入逻辑位置；\r\n- view 起始于输入之前、结束于输入之后或与输入完全不相交时，梯度不会越界；\r\n- 输入自身重叠的情况得到明确诊断，要求调用方先转为 contiguous Tensor。\r\n\r\n#### 5. ATen compat `as_strided` 语义对齐\r\n\r\n##### 问题\r\n\r\nATen compat 的 `storage_offset` 按元素计数，而 Paddle 内部 metadata offset 按 byte 计数。原实现直接进行未经检查的乘法，并且 compat 路径手工构造 view，可能绕过 PHI `as_strided` kernel 的范围检查。\r\n\r\n##### 改动\r\n\r\n- 在 `Tensor::as_strided` 和 `Tensor::as_strided_` 中统一使用 checked element-to-byte offset 转换；\r\n- 复用 PHI 的 storage range、alignment、rank 和 overflow 检查；\r\n- compat 头文件改为包含统一的 `strided_utils.h`，避免两套校验逻辑漂移。\r\n\r\n##### 修改后结果\r\n\r\n- compat `as_strided` 与 Paddle PHI kernel 对非法范围、负 offset、溢出 offset 的行为一致；\r\n- 合法的 contiguous、non-contiguous 和 inplace view 仍保持原有语义；\r\n- 不再因为 offset 回绕而产生错误 alias。\r\n\r\n#### 6. 公共辅助逻辑合并及回归测试\r\n\r\n##### 问题\r\n\r\n早期实现把 view 校验和 backward 辅助逻辑拆到多个新增头文件中，导致公共 compat 入口需要额外 include，逻辑容易重复且不利于维护。部分修复还只覆盖 kernel 路径，没有对应的 forward、backward、CPU/GPU 和 compat 回归测试。\r\n\r\n##### 改动\r\n\r\n- 将 strided view 的范围、溢出、重叠判断、copy 和 accumulate 辅助逻辑合并到已有的 `paddle/phi/kernels/funcs/strided_utils.h`；\r\n- 删除临时拆分的 `strided_view_utils.h` 和 `strided_grad_utils.h`，不新增持久化头文件；\r\n- 补充或强化 `test_as_strided.py`、`test_tensor_unfold.py` 和 `ATen_as_strided_test.cc`。\r\n\r\n测试覆盖：\r\n\r\n- 重复 stride、zero stride、带 offset 的重叠梯度；\r\n- nested/non-contiguous input 的 backward；\r\n- shape/stride rank 不一致；\r\n- storage range、负 offset、非对齐 offset 和 int64 overflow；\r\n- zero-size view 和合法 view 的兼容行为；\r\n- ATen compat forward/inplace `as_strided` 的非法 offset 和越界行为；\r\n- CPU、GPU 以及可用的其他 backend 路径。\r\n\r\n##### 修改后结果\r\n\r\n- 相关逻辑集中在现有公共辅助头文件中，减少重复实现和 include 依赖；\r\n- 非法输入在 metadata/kernel 入口处被拒绝，合法输入保持兼容；\r\n- 关键梯度和边界回归场景有自动化测试覆盖。\r\n\r\n### 修改后的总体行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| 非连续 Tensor 的 NaN/Inf 检查 | 可能按连续地址误读或越界 | 非连续 view 跳过 flat scan，避免错误内存访问 |\r\n| shape/stride rank 不一致 | 可能被接受，后续访问缺失 stride | 入口明确报错 |\r\n| 越界或溢出 view | 可能静默创建并在读写时破坏内存 | 范围、对齐和算术溢出检查提前拒绝 |\r\n| 重叠 `as_strided` backward | 重复 slot 被覆盖 | 所有贡献累加 |\r\n| overlapping `unfold` backward | 窗口贡献互相覆盖 | 按窗口出现次数累加 |\r\n| 非连续/nested input backward | storage 坐标被误当成逻辑坐标 | 经过 storage buffer 正确 gather 回输入梯度 |\r\n| 输入自身重叠 | 可能产生未定义梯度 | 明确抛出 `NotImplementedError` |\r\n| ATen `storage_offset` | 元素到 byte 的转换可能回绕 | checked conversion，与 PHI 路径统一 |\r\n\r\n合法输入的前向结果不变；修改主要影响原本可能越界、静默接受或梯度错误的边界场景。梯度数值的变化是对重叠 view 原本错误的覆盖行为进行正确累加，不是精度策略变化。\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79748\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是", "url": "https://github.com/PaddlePaddle/Paddle/pull/79775"}
{"number": 79776, "title": "[Cherry-Pick][Operator Mechanism] Align CPU elementwise broadcast backward with PyTorch bitwise", "state": "closed", "created_at": "2026-09-15T09:16:52Z", "updated_at": "2026-09-17T06:20:09Z", "closed_at": "2026-09-17T06:20:09Z", "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T06:20:09Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 对齐 Paddle CPU elementwise 广播反向（add / sub / div / remainder）在 `float32` / `float64` 下与 PyTorch 的逐位计算行为。此前广播侧梯度按串行顺序累加，且 div 的 `dy` 公式分组与 PyTorch 不同，导致梯度与 PyTorch CPU 存在数个 ulp 的偏差。主要包含四类修改：\r\n\r\n1. 广播梯度归约顺序对齐（cascade sum）；\r\n2. 除法反向公式分组对齐；\r\n3. 恒等梯度项跳过物化（性能）与 inplace view 别名修复；\r\n4. 空 Tensor 广播反向修复。\r\n\r\n所有算子统一走通用 elementwise grad 路径，不引入按算子的专用实现。\r\n\r\n#### 1. 广播梯度归约顺序对齐\r\n\r\n##### 问题\r\n\r\n广播输入的梯度通过通用路径串行累加，累加顺序与 PyTorch 的 cascade summation 不同。浮点加法不满足结合律，顺序不同则舍入结果不同，广播反向梯度与 PyTorch CPU 不逐位一致：\r\n\r\n```text\r\n串行：((a0 + a1) + a2) + a3 ...\r\ntorch：先按固定宽度分块向量内累加，再逐级归并（cascade）\r\n```\r\n\r\n##### 改动\r\n\r\n`CommonGradBroadcastCPU`、`ElemwiseGradBroadcast1CPU`、`ElemwiseGradBroadcast2CPU` 对需要归约的一侧：\r\n\r\n- 按输出 row-major 顺序物化逐元素梯度项（`dx_op(x[i], y[i], out[i], dout[i])`）；\r\n- 使用已有的 `TorchCompatibleReduceSum`（cascade sum，与 torch `Loop2d` 派发的向量内/向量外归约同序）对物化项归约；\r\n- 不需要归约的一侧直接写入目标梯度，不物化完整输出临时 buffer。\r\n\r\n#### 2. 除法反向公式分组对齐\r\n\r\n##### 问题\r\n\r\n原 `DivGradDY` 使用 `-dout * out / y`，求值顺序为 `(-dout * out) / y`；PyTorch CPU 的 div 反向（`FunctionsManual.cpp` 中 `-grad * ((self/other)/other)`）先计算 `out / y` 再乘 `-dout`。浮点乘除不满足结合律，`float32` 下两种分组可能产生最后几位的差异。\r\n\r\n##### 改动\r\n\r\n- `DivGradDY` 重排为 `-dout * (out / y)`，与 PyTorch 分组一致；\r\n- `dx` 保持 `dout / y`，与 PyTorch 的 `grad / other` 一致；\r\n- 删除早期实现中的 `TorchCompatibleDivideGradCPU` 专用路径，由上述通用方案取代，所有 elementwise 算子共享。\r\n\r\n#### 3. 恒等梯度项跳过物化与 inplace view 别名修复\r\n\r\n##### 问题\r\n\r\n逐项物化保证了归约顺序，但引入了性能回归：`[1048576] x [1]` 的 add 广播反向需要物化 4MB 中间项，耗时从约 1.03ms 升至 3.0ms。PyTorch 对梯度项与原始梯度相同的算子（add/sub 的恒等项）直接对原始梯度做 `sum_to_size`，不物化中间项。\r\n\r\n此外，eager 反向在 `grad_out` 引用计数为 1 时，会把第一个梯度输出（`dx`）作为 `grad_out` 的 view 传入 kernel（inplace 内存复用）。若直接对 `dout` 归约，cascade 入口的 `memset` 会先清零 `dout` 的前缀，再读到的已是被破坏的数据，结果恰好\"丢失首行\"。\r\n\r\n##### 改动\r\n\r\n- 恒等 functor（add 的 `IdentityGrad`、`SubGradDX`、`RemainderGradDx`）声明 `kGradTermIsDout = true` 标签，`elementwise_grad_base.h` 通过 SFINAE trait `GradTermEqualsDout<F>` 泛型检测；\r\n- 当梯度项与 `dout` 逐位相同（恒等 functor 且 `T == Tout`）时，跳过物化，直接对 `dout` 做 cascade 归约，并跳过无剩余工作的逐元素循环，对齐 PyTorch `sum_to_size` 的做法；\r\n- 未标注的 functor（div、mul、pow、`SubGradDY` 等）自动走原物化路径，行为不变；\r\n- 在 `ElemwiseGradComputeWithBroadcast` 中，归约侧输出与 `dout` 共享 buffer 时先 detach（重新分配），再进入归约；`CommonElementwiseBroadcastBackward` 补充对称的 `dy` detach guard（原有 `dx` guard 保持不变）。\r\n\r\n`[1048576] x [1]` add 反向耗时从物化方案的 3.0ms 回落至 0.57ms（stock 串行实现 1.03ms）。\r\n\r\n#### 4. 空 Tensor 广播反向修复\r\n\r\n##### 问题\r\n\r\n当广播结果 `out.numel() == 0` 时（如 `[0, 3]` 与 `[1, 3]`），原路径不会进入实际计算循环，非空一侧的输入梯度可能保持未初始化内存，而不是返回零梯度。\r\n\r\n##### 改动\r\n\r\n在 `ElemwiseGradComputeWithBroadcast` 入口显式处理空输出：无条件 `Alloc` 所需梯度（numel 为 0 也要分配空 holder，否则 autograd 判定该输入无梯度返回 `None`），并将非空梯度 buffer 清零。\r\n\r\n#### 修改后的行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| 广播反向梯度 vs PyTorch | 串行累加顺序不同，数个 ulp 偏差 | 单轴/多轴/标量/`[1M]x[1]` 逐位一致 |\r\n| div `dy` 公式分组 | `(-dout * out) / y` | `-dout * (out / y)`，与 PyTorch 一致 |\r\n| `[1048576]x[1]` add 反向耗时 | 物化方案 3.0ms | 0.57ms（stock 串行 1.03ms） |\r\n| inplace view 输入 + 归约侧 | 直接归约 `dout` 会被 memset 破坏（丢首行） | 归约前 detach，结果正确 |\r\n| 空 Tensor 广播反向 | 非空侧梯度可能未初始化 | 梯度为零，空 holder 正常分配 |\r\n| div double-grad vs PyTorch | 最大 6 ulp、80% 元素不一致 | `g2x` 逐位一致，`g2y` <= 1 ulp（附带改善） |\r\n\r\n合法输入的前向计算不变；梯度数值变化均为向 PyTorch 结果的对齐。\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79750\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79776"}
{"number": 79759, "title": "Cp trunc normal", "state": "closed", "created_at": "2026-09-10T04:00:34Z", "updated_at": "2026-09-17T06:19:55Z", "closed_at": "2026-09-17T06:19:55Z", "labels": ["contributor", "skip-ci: coverage", "skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T06:19:55Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->Operator Mechanism\r\n\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] --> Improvements\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->cherry-pick of #79703 \r\n\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->是\r\n\r\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79703", "url": "https://github.com/PaddlePaddle/Paddle/pull/79759"}
{"number": 79755, "title": "[Operator Mechanism] Add deterministic path for cummax, cummin and cumsum", "state": "open", "created_at": "2026-09-09T06:25:51Z", "updated_at": "2026-09-17T04:14:56Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\nOperator Mechanism\r\n\r\n\r\n### PR Types\r\nOthers\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n为cummax/cummin/cumsum的kernel增加和torch逐位对齐的路径\r\n1. 为cummax/cummin的反向增加deterministic路径`ScatterAddDeterministic`：\r\n- 模拟torch`scatter_add`的deterministic分支实现，在`FLAGS_cudnn_deterministic=true`可以实现反向结果的稳定复现；（反向默认路径的归约顺序受warp调度影响➕浮点数累加不满足结束律 -> 相同数据集每轮结果仍有ulp级的波动）\r\n\r\n2. 为cumsum在inner_dim==1(aixs==dims-1)的compat kernel增加反向路径：\r\n- cumsum的前向和反向走同一路径，区别在于参数中反向时reverse=true，前向时reverse=false；\r\n- 前向在`FLAGS_use_accuracy_compatible_kernel=true`时已有和torch对齐的实现，但是不支持reverse参数，故反向无法对齐；\r\n- 当前pr在对齐kernel中增加reverse参数，并在cuda kernel `InclusiveScanInnerDimSklanskyImpl`中增加对反向索引计算的处理\r\n\r\n3. 为cumsum增加处理1D tensor时和torch对齐的确定性`InclusiveDeterministicScan` ：\r\n- 处理1D tensor时前缀后累加受warp调度影响使归约顺序无法固定，模拟torch的deterministic路径实现了和torch对齐的kernel，在`FLAGS_cudnn_deterministic=true && FLAGS_use_accuracy_compatible_kernel=true`时启用可以和torch在`torch.use_deterministic_algorithms(True)`时实现逐位对齐\r\n\r\n\r\n### 是否引起精度变化\r\n否\r\n（不影响默认路径的精度，仅新增compat路径和deterministic路径在flag下开启）", "url": "https://github.com/PaddlePaddle/Paddle/pull/79755"}
{"number": 68378, "title": "[Inference]Improve Performance in TensorRT Engine Instruction", "state": "closed", "created_at": "2024-09-23T06:39:19Z", "updated_at": "2026-09-16T23:06:55Z", "closed_at": "2024-09-24T11:39:20Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2024-09-24T11:39:20Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nPerformance Optimization\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Deprecations | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\ncard-71500\r\n本PR修复TensorRTEngineInstruction中无效循环太多导致性能降低的问题", "url": "https://github.com/PaddlePaddle/Paddle/pull/68378"}
{"number": 50898, "title": "Release/2.4", "state": "open", "created_at": "2023-02-25T07:41:23Z", "updated_at": "2026-09-16T19:55:08Z", "closed_at": null, "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n### PR types\r\n<!-- One of [ New features | Bug fixes | Function optimization | Performance optimization | Breaking changes | Others ] -->\r\n\r\n### PR changes\r\n### PR changes\r\n<!-- One of [ OPs | APIs | Docs | Others ] -->\r\n\r\n### Describe\r\n<!-- One of [ OPs | APIs | Docs | Others ] -->\r\n\r\n### Describe\r\n<!-- Describe what this PR does -->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/50898"}
{"number": 79761, "title": "[Test] Rename AI-edited tests to coverage tests", "state": "open", "created_at": "2026-09-10T13:47:05Z", "updated_at": "2026-09-16T11:48:36Z", "closed_at": null, "labels": ["skip-ci: api-benchmark", "skip-ci: slice"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\nExecute Infrastructure\n\n### PR Types\nNot User Facing\n\n### Description\n将 develop 分支已合入的 #79738 同步到 `release/3.4` 分支（cherry-pick）。\n\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79738\n\n变更内容：\n- 目录重命名：`test/ai_edited_test` → `test/coverage_test`\n- 111 个 `test_ai_*.py` 文件重命名为 `test_coverage_*.py`\n- 更新 `test/CMakeLists.txt`：`add_subdirectory(ai_edited_test)` → `add_subdirectory(coverage_test)`\n- 新增 `test/coverage_test/README.md`\n\n### 是否引起精度变化\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79761"}
{"number": 79783, "title": "[Cherry-Pick][Performance] Parallelise moe_permute (#79756)", "state": "closed", "created_at": "2026-09-16T08:45:50Z", "updated_at": "2026-09-16T09:06:57Z", "closed_at": "2026-09-16T08:46:28Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\nPerformance Optimization, Operator Mechanism\n\n### PR Types\nPerformance, Bug fixes\n\n### Description\n\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79756\n\nCherry-pick of the `moe_permute` optimisation and bug fix from develop.\n\nTwo rewrites make `moe_permute` 1.9x faster for bfloat16 and 3.3x for float8\nat the production shape, and no shape measured is slower (30 shapes tested,\n1.03x to 12.55x). A third change fixes two pre-existing misaligned 16 B\naccesses that abort the kernel outright.\n\nThe limiter was parallelism, not bandwidth: bfloat16 and float8 move 2x\ndifferent byte counts in 1297.3 vs 1295.8 us (0.1% apart), and turning the\ntoken movement off entirely saves only 18%.\n\n1. Replace the cross-block serial spin chain in phase 1 with two independent\n   kernels (count + scan). Phase 1: 1068.6 -> 61.6 us.\n\n2. Move token rows with one warp per row through registers instead of one row\n   at a time through shared-memory ping/pong buffers via cp_async_bulk. This\n   also removes the TMA store path (unreachable once the gather is\n   unconditional) and returns 2 * token_dim * sizeof(dtype) of shared memory\n   per block. bfloat16: 797.7 -> 246.8 us, float8: 814.2 -> 192.9 us.\n\n3. Fix two places that touch token rows with 16 B vectors without checking the\n   address (phase-2 fallback copy and padding-row fill). Reachable from the\n   public API for any token_dim not a multiple of 16 B.\n\nNeither rewrite performs arithmetic on token data, so the output is\nbit-identical. Verified over 46 shapes against an independently computed\nreference and over every shape the old path can run against the old path itself;\ntest/legacy_test/test_moe_permute_unpermute.py 11/11 passing.\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79783"}
{"number": 79756, "title": "[Performance] Parallelise moe_permute", "state": "closed", "created_at": "2026-09-09T07:11:12Z", "updated_at": "2026-09-16T08:40:40Z", "closed_at": "2026-09-16T08:38:29Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-16T08:38:29Z", "body": "### PR Category\nPerformance Optimization, Operator Mechanism\n\n### PR Types\nPerformance, Bug fixes\n\n### Description\n\n`moe_permute` was one of the two largest data-movement kernels in an MoE\ntraining step for us. Two rewrites make it 1.9x faster for bfloat16 and 3.3x\nfor float8 at our production shape, and no shape we measured is slower. A third\nchange fixes two pre-existing misaligned accesses that abort the kernel\noutright.\n\n**The limiter was parallelism, not bandwidth.** This is worth stating because\nthe usual reading of \"a pure movement kernel at low DRAM utilisation\" is \"the\naccesses are not coalesced\", and acting on that reading here would have been\nwasted work. Two independent measurements say otherwise:\n\n| evidence | reading |\n|---|---|\n| bfloat16 vs float8 | 1297.3 vs 1295.8 us, 0.1% apart -- float8 moves half the bytes in the same time, so the time does not track the byte count |\n| `do_gather=false`, i.e. no token movement at all | saves only 18% |\n\n#### 1. Replace the cross-block serial chain in phase 1\n\nPhase 1b needs, for every (block, expert), how many rows the preceding\nsame-parity blocks assigned to that expert. It was computed with a chain: a\nblock spin-waited on `global_expertwise_block_cumsum` until its predecessor\npublished a running offset with `atomicExch`, then published its own for\n`blockIdx + 2`. The chain is `gridDim.x / 2` deep and every hop is an L2 atomic\nround trip, so the cost scaled with the grid rather than with the data -- at our\nproduction shape, 2406 hops at roughly 419 ns each, which is essentially the\nwhole kernel. Spin-waiting on another block also relies on blocks being\nlaunched roughly in order, which the programming model does not guarantee.\n\nIt is replaced by two kernels with no dependency between them:\n`permute_block_count_kernel` (same block-to-row mapping) writes the per-block\nper-expert row counts, and `permute_chain_scan_kernel` (one block per expert per\nparity) turns them in place into the exclusive prefix sum over preceding\nsame-parity blocks. `chain_offset` becomes an ordinary load. Phase 1 goes from\n1068.6 to 61.6 us.\n\nThe offsets are the same integers, so every row lands in the same output slot.\nDropping the chain also drops the sentinel pre-fill of the cumsum buffer and the\ntwo extra rows it carried for publishing to `blockIdx + 2`, and the launcher\nonly runs the two kernels when there is more than one block -- with a single\nblock there are no preceding blocks, so every offset is zero.\n\n#### 2. Move token rows with one warp per row instead of one row at a time\n\nPhase 2 walked the block's 32 rows strictly serially: per row a\n`cuda::pipeline` wait, two block synchronisations, a single\n`cp_async_bulk_shared_to_global` issued by thread 0, and a\n`cp_async_bulk_wait_group_read<0>` drain. Only one row (2 or 4 KB) was ever in\nflight per block, 255 of the 256 threads waited on thread 0, and every row was\nstaged through shared memory on the way from global to global.\n\nEach warp now owns one source row -- so `warp_num` rows are in flight per block\n-- and moves it with 16 B vector loads and stores straight through registers,\nwith two vectors per lane so the stores of one overlap the load of the next. No\nshared-memory staging, no pipeline, no per-row drain, and one load now feeds all\n`topk` destinations. bfloat16 goes from 797.7 to 246.8 us, float8 from 814.2 to\n192.9 us.\n\nThis makes the TMA store path unreachable: it required the same 16 B row\nalignment as the new gather, so `use_tma && do_gather` now always selects the\ngather, and the ping/pong buffers and `cp_async_bulk` loop are removed. TMA\nstill stages `routemap` and `probs`, which every warp reads repeatedly. Not\nstaging token rows also returns `2 * token_dim * sizeof(dtype)` of shared memory\nper block. The remaining phase-2 fallback, taken when a token row is not a whole\nnumber of 16 B vectors, is the block-cooperative element copy that was already\nthere.\n\nNeither rewrite performs any arithmetic on the token data -- one changes how an\ninteger offset is computed, the other is pure movement -- so the output is\nbit-identical rather than merely close.\n\n#### 3. Fix two misaligned 16 B accesses (pre-existing)\n\n`moe_permute` touched token rows with 16 B vectors in two places that never\nchecked the address: the phase-2 fallback that copies a token row, and the\npadding-row fill. A row starts at `token_dim * sizeof(dtype) * row`, so as soon\nas `token_dim` is not a whole number of 16 B every other row is misaligned and\nthe kernel aborts with `cudaErrorMisalignedAddress`. Reachable from the public\nAPI, for instance bfloat16 with `token_dim = 12` and `padding_alignment = 128`.\n`token_dim = 4` does not reproduce it: the row holds no whole 16 B vector, so\nonly the scalar tail runs.\n\nBoth now go through the alignment-checked helpers -- `try_vectorized_memcpy` for\nthe token row, and a new `try_vectorized_memset` for the padding fill. Where the\nrow happens to be aligned the emitted accesses are unchanged.\n\n#### Verification\n\nNo arithmetic on token data changes: item 1 only changes how an integer offset is\ncomputed and produces the same integers, item 2 is pure movement, and item 3 adds\nan address check ahead of accesses that were already being made. So the bar here\nis bit-identical output, not a tolerance.\n\nTwo independent tracks, over 46 shapes covering: a single block\n(`rows <= 32`) and both parities of the block mapping; `token_dim` aligned and\nunaligned to 16 B; `topk` 1/2/6/10/16; `num_experts` 1/8/40/128/256/384 with\nexperts that receive no token; `padding_alignment` 1/4/8/128; `do_gather` and\n`return_expert_indices` in every combination; bfloat16 and float8, with and\nwithout ue8m0-packed scales.\n\n* Against an independently computed reference for all five outputs\n  (`hidden_states_unzipped`, `zipped_expertwise_rowmap`, `token_prob_unzipped`,\n  `scale_unzipped`, `expert_indices`): **46/46 exactly equal**. Since the op\n  performs no arithmetic, exact equality with the reference *is* bit-exactness.\n* Against a build of the previous revision of this branch, which still had the\n  old path available: **40/40 shapes bit-identical on every output tensor**, 0\n  mismatches. The remaining 6 are the shapes the old path aborts on, i.e. the\n  ones item 3 fixes.\n* `test/legacy_test/test_moe_permute_unpermute.py`: 11/11 passing.\n\nA second sweep covers 19 degenerate and large shapes: zero rows; every token\nunrouted, so the output itself has zero rows; one expert receiving everything;\n`token_dim` 1; a `topk` where most slots are -1; and at the other end 4M rows,\n1M rows over 64 experts, 100k rows over 384 experts, `token_dim` 65536, and\n400k rows at the production `token_dim`. All 19 match the reference --\n`zipped_expertwise_rowmap`, `token_prob_unzipped` and `expert_indices` in full,\nthe gathered rows and the padding rows sampled.\n\nThat sweep turned up one more shape the old path cannot run: at `token_dim`\n65536 in bfloat16 the TMA path asks for `2 * token_dim * sizeof(dtype)` = 256 KB\nof shared memory for its ping/pong buffers, past the per-block limit, so\n`cudaFuncSetAttribute` fails with `cudaErrorInvalidValue`. Measured, the old\npath tops out at `token_dim` 57344 for bfloat16 (224 KB) and fails from 65536\non. The new one has no such ceiling, because it does not stage token rows in\nshared memory at all -- checked up to `token_dim` 131072.\n\n#### Measurements\n\nOp-level wall time, so this includes the two new kernels, the rowmap fill and\nthe host-side preprocessing -- it is not the kernel-only figure quoted above.\nMinimum of 50-60 iterations on an idle device, two independent passes.\n\n**30 shapes measured, none slower.** The span is 1.03x to 12.55x. The extremes\nare both explained by how much of the op the change can reach: the 1.03x case is\n64 rows over 256 experts at `padding_alignment` 128, where 32544 of the 32768\noutput rows are padding and almost all the time is the padding fill; the 12.55x\ncase is `do_gather=false` at the production shape, which is phase 1 alone, i.e.\nthe chain removal with nothing else in the way.\n\nA representative selection:\n\n| shape | before | after | |\n|---|---|---|---|\n| 153984 rows, token_dim 2048, topk 10, 8 experts, float8 | 1247.5 us | 380.5 us | 3.28x |\n| same, bfloat16 | 1221.8 us | 633.7 us | 1.93x |\n| 16384 rows, topk 1, 8 experts | 165.1 us | 61.7 us | 2.68x |\n| 16384 rows, topk 8, 64 experts, float8 | 237.8 us | 89.2 us | 2.67x |\n| same, bfloat16 | 211.9 us | 120.2 us | 1.76x |\n| 4096 rows | 80.7 us | 46.7 us | 1.73x |\n| 16384 rows, 256 experts | 315.0 us | 194.6 us | 1.62x |\n| 32 rows (single block) | 49.7 us | 37.2 us | 1.34x |\n| 8192 rows, token_dim 8192 | 170.0 us | 134.6 us | 1.26x |\n| 33 / 64 / 256 / 1024 rows | ~51 us | ~43 us | 1.21-1.33x |\n| 153984 rows, 384 experts | 1646.2 us | 1050.8 us | 1.57x |\n| 400000 rows, topk 10 | 3077.6 us | 1613.5 us | 1.91x |\n| 16384 rows, topk 16, 32 experts | 225.9 us | 175.6 us | 1.29x |\n| 16384 rows, `padding_alignment` 1 | 165.7 us | 87.9 us | 1.89x |\n| 16384 rows, `return_expert_indices` | 173.2 us | 93.1 us | 1.86x |\n| 153984 rows, `do_gather=false` | 1089.5 us | 86.8 us | 12.55x |\n| 64 rows, 256 experts (padding-dominated) | 113.1 us | 110.3 us | 1.03x |\n\nBeyond the rows above, the sweep also varies `padding_alignment` (1, 8, 128) and\n`return_expert_indices` at 8 and 256 experts, `topk` 12 and 16, and\n`do_gather=false` at three sizes. Run-to-run spread between the two passes is\n0.3-2.7% on shapes above roughly 150 us and 7-15% below that, where the\nmeasurement is dominated by host-side dispatch rather than by the kernels; every\nratio above is well outside that spread.\n\nThe shapes with an unaligned `token_dim` have no before number, because the old\npath aborts on them. For reference, `token_dim` 2044 costs 194.3 us against\n92.2 us for 2048, so the fallback is about 2.1x the vectorised path -- which is\nthe price of correctness on a shape that used to crash.\n\n#### Note on `moe_unpermute`\n\nAn earlier revision of this PR also rewrote `moe_unpermute` to cover several\nzipped rows per block. It has been reverted, byte for byte, and this PR now\ntouches `moe_permute` only. The reason: measured against the shipped kernel on\nan idle device, the several-rows-per-block mapping wins at the shape it was\ntuned for (1.28x unweighted, 1.57x weighted) but loses elsewhere -- 25% at 64\nlocal experts, 46% at 256, and 21% at `token_dim` 8192. The per-block working\nset is `rows * token_dim`, while the number of rows was chosen from the row\ncount alone. Sizing it correctly is separate work.\n\n### 是否引起精度变化\n否\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79756"}
{"number": 79765, "title": "[AP] Add conv2d + epilogue fusion pattern.- #79532", "state": "open", "created_at": "2026-09-11T09:39:48Z", "updated_at": "2026-09-16T02:54:33Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nCINN\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n新增`Conv2dEpilogueFusion`，初步支持 conv2d + trivial_fusion 的子图匹配，打通子图匹配和`ap_variadic` 算子替换流程。\r\n本 PR 提交的单测主要测试 pattern 能否成功匹配和替换成 ap_variadic 算子，后端计算直接调用 cutlass 的 conv2d+relu 接口，自动生成 varidic epilogue 由后续 PR 支持。\r\n\r\n当前生成的代码如下：\r\n```cpp\r\n\r\n// auto generated codes\r\n#include \"kernels.h\"\r\n#include <vector>\r\n\r\nnamespace ap {\r\n\r\ntemplate <typename T>\r\nstruct VariadicEpilogueFunctor {\r\n  struct Arguments {\r\n    // Extents of the output spatial dimensions. The epilogue of an implicit\r\n    // GEMM only knows the GEMM_M index `coord.row`, these are what let the\r\n    // generated statements decompose it back into (n, p, q).\r\n    int P;\r\n    int Q;\r\n    int64_t input0_dim1;\r\n    int64_t input0_dim3;\r\n    const half* in_ptr_0;\r\n  };\r\n\r\n  // Note: need to support vectorized operation\r\n  __forceinline__ __host__ __device__\r\n  T operator()(T x, const Arguments& args, const MatrixCoord& coord) const {\r\n    T out;\r\n    float op2_out0 = static_cast<float>(args.in_ptr_0[((coord.row / (args.P * args.Q)) * args.input0_dim1 * args.input0_dim1 * args.input0_dim3 + ((coord.row / args.Q) % args.P) * args.input0_dim1 * args.input0_dim3 + (coord.row % args.Q) * args.input0_dim3 + coord.column)]);\r\n    float op3_out0 = static_cast<float>(x + op2_out0);\r\n    float op4_out0 = static_cast<float>((op3_out0 > 0 ? op3_out0 : 0) );\r\n    out = op4_out0;\r\n    return out;\r\n  }\r\n};\r\n\r\ntemplate <int TuningConfigId>\r\nstatic void RunConv2dWithVariadicKernel(const Conv2dEpilogueParams &params, const half* input0, const half* input1, half* output, int64_t input0_dim0, int64_t input0_dim1, int64_t input0_dim3, int64_t input1_dim1, const half* in_ptr_0) {\r\n  using ElementT = half;\r\n  using ElementComputeT = float;\r\n\r\n  typename VariadicEpilogueFunctor<ElementComputeT>::Arguments epilogue_args;\r\n  epilogue_args.P = params.P;\r\n  epilogue_args.Q = params.Q;\r\n\r\n  epilogue_args.input0_dim1 = input0_dim1;\r\n  epilogue_args.input0_dim3 = input0_dim3;\r\n  epilogue_args.in_ptr_0 = in_ptr_0;\r\n\r\n  // The activation and the filter are both indexed along the channel dimension,\r\n  // while the output is indexed along the output channel dimension.\r\n  constexpr int AlignA = Alignment<ElementT, 16>::kValue;\r\n  constexpr int AlignB = AlignA;\r\n  constexpr int AlignC = Alignment<ElementT, 16>::kValue;\r\n\r\n  Conv2dVariadicFusion<ElementT, ElementComputeT, VariadicEpilogueFunctor,\r\n                       AlignA, AlignB, AlignC, TuningConfigId>(params,\r\n                                                              epilogue_args);\r\n}\r\n\r\n} // namespace ap\r\n\r\nextern \"C\" {\r\n\r\nvoid Conv2dVariadicKernel(void* stream_ptr, const half* input0, const half* input1, half* output, int64_t input0_dim0, int64_t input0_dim1, int64_t input0_dim3, int64_t input1_dim1, const half* in_ptr_0) {\r\n  // The activation is NHWC (data_format) and the filter is KRSC.\r\n  std::vector<int64_t> input0_shape;\r\n  input0_shape.resize(4);\r\n  input0_shape[0] = input0_dim0;\r\n  input0_shape[1] = input0_dim1;\r\n  input0_shape[2] = input0_dim1;\r\n  input0_shape[3] = input0_dim3;\r\n\r\n  std::vector<int64_t> input1_shape;\r\n  input1_shape.resize(4);\r\n  input1_shape[0] = input0_dim3;\r\n  input1_shape[1] = input1_dim1;\r\n  input1_shape[2] = input1_dim1;\r\n  input1_shape[3] = input0_dim3;\r\n\r\n  ap::Conv2dEpilogueParams params(stream_ptr,\r\n                                  input0,\r\n                                  input1,\r\n                                  /*bias=*/nullptr,\r\n                                  output,\r\n                                  input0_shape,\r\n                                  input1_shape,\r\n                                  std::vector<int64_t>{},\r\n                                  std::vector<int>{1, 1},\r\n                                  std::vector<int>{1, 1},\r\n                                  std::vector<int>{1, 1},\r\n                                  1);\r\n\r\n  ap::RunConv2dWithVariadicKernel<ap::DefaultConfig::kConfigId>(params, input0, input1, output, input0_dim0, input0_dim1, input0_dim3, input1_dim1, in_ptr_0);\r\n}\r\n}\r\n\r\n```\r\n\r\n#### 支持范围\r\n| 维度 | 首版支持 | 拦截位置 |\r\n|---|---|---|\r\n| 算子 | pd_op.conv2d fprop | source_pattern |\r\n| 设备 | CUDA（`-arch=sm_80`）；DCU/HIP 不支持 | `constraint()` → `compile_command_util.supports(\"conv2d\")` |\r\n| dtype | fp16（已验证）；bf16 代码路径存在但未验证 | 无检查 |\r\n| activation 布局 | `data_format == \"NHWC\"` | `constraint()` |\r\n| filter 布局 | `KRSC`，依赖 `FLAGS_manually_trans_conv_filter=1` | 运行期 `Conv2dEpilogueParams` 断言（见已知限制）|\r\n| groups | `== 1` | `constraint()` |\r\n| padding | `padding_algorithm == \"EXPLICIT\"` 且上下/左右对称 | `constraint()` |\r\n| kernel / stride / dilation | 任意（走 `IteratorAlgorithm::kOptimized` + `StrideSupport::kStrided`，`R/S ≤ 32` 时最优） | — |                                            \r\n| epilogue | `ap_trivial_fusion_op`，输入 2 ~ 11、输出；1 ~ 10（`register_conv2d_epilogue_class` 注册），可融性由 `constraint()` 的访存拓扑化简判定 | `constraint()` |\r\n\r\n实现上固定的选择：`split_k_slices = 1`；tile 固定 `DefaultConfig::kConfigId`，autotune 未开启；epilogue functor为标量版本，未向量化；AlignA/AlignB 按输入通道 C 取、AlignC 按输出通道 K 取。\r\n\r\n#### 验证环境\r\n- GPU / 工具链：A100（sm_80）、CUDA 12.8 + nvcc、gcc 12.3.0、Python 3.10；构建需开启 CINN。\r\n- 端到端单测：`test/ap/test_conv2d_epilogue.py`，NHWC fp16、3×3、padding=1、groups=1，子图为 `conv2d + add + relu`。断言融合发生（PIR 中出现`pd_op.ap_variadic`）并与 Eager 对数值（atol=1e-1）。已覆盖 add 的两种索引形态：per-channel bias（沿 N/P/Q 广播，索引化简为coord.column）和与输出同形的逐元素加（coord.row 反解为 (n,p,q) 后线性化）。\r\n- 运行前置：`FLAGS_manually_trans_conv_filter=True`（filter 走 KRSC）、`FLAGS_deny_cinn_ops=\"transpose\"`（transpose 留在 phi kernel）。\r\n\r\n未验证 / 不在本 PR 声明范围：\r\n- DCU/HIP（无环境，已在 pass 层拒绝并有单测）；\r\n- sm_80 以外架构；\r\n- fp32；\r\n- 性能对比与 autotune。\r\n\r\nPcard-90546\r\n\r\n原始PR：https://github.com/PaddlePaddle/Paddle/pull/79532\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否\r\n\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79765"}
{"number": 71749, "title": "2.0.13 patch to 2.0.14", "state": "open", "created_at": "2025-03-18T08:28:14Z", "updated_at": "2026-09-15T12:43:13Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\n\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Deprecations | Others ] -->\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/71749"}
{"number": 79771, "title": "删除icafe建卡检查", "state": "closed", "created_at": "2026-09-15T07:14:47Z", "updated_at": "2026-09-15T09:24:19Z", "closed_at": "2026-09-15T09:06:30Z", "labels": ["skip-ci: all"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-15T09:06:30Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nUser Experience\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nImprovements\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n因icafe建卡检查导致ci卡住，此功能已不需要\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79771"}
{"number": 79703, "title": "align trunc_normal with torch for gpu", "state": "closed", "created_at": "2026-08-26T09:50:50Z", "updated_at": "2026-09-15T08:10:46Z", "closed_at": "2026-09-15T08:10:46Z", "labels": ["contributor", "skip-ci: coverage"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-15T08:10:46Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->Operator Mechanism\r\n\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] --> Improvements\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->align truncated norm initializer with torch on gpu \r\ndetail:\r\n1、upgrade parameter precision to double \r\n2、develop a new trunc_normal gpu kernel uses rejection sampling which is originally applied in python level in torch\r\n3、fix bf16 && fp16 register on xpu according to ci review bot.\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->是\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79703"}
{"number": 79697, "title": "[Operator Mechanism]Add 64-bit strided BatchGEMM dispatch", "state": "closed", "created_at": "2026-08-26T02:27:01Z", "updated_at": "2026-09-15T08:08:21Z", "closed_at": "2026-09-15T08:08:21Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism \r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n当前 `MatMul` 和 `BatchedGEMM` 的部分接口及实现仍会将 `M/N/K/batchCount` 收窄为 32 位整数，导致大于 `INT_MAX` 的维度无法使用 CUDA 12.3 提供的 64 位 cuBLAS 接口，部分路径还可能基于已截断的维度计算 leading dimension。\r\n\r\n本 PR 主要包含以下改动：\r\n\r\n- 将 `MatMul` 和 pointer-array `BatchedGEMM` 的维度参数调整为 `int64_t`，保证 MatMul 广播及 BLAS 调用链能够完整传递 64 位维度。\r\n- 为 CUDA BatchedGEMM、StridedBatchedGEMM 和 TRSM 补充 `_64` 动态加载符号及调用封装。当相关维度超过 `INT_MAX` 时，在 Linux CUDA 12.3 及以上环境分发到对应的 64 位 cuBLAS API；其他情况继续使用原有 32 位接口。\r\n- 在不支持 64 位 BLAS API 的 CPU/HIP 路径增加显式范围检查，避免静默整数截断；同时在分配 MatMul 输出内存前校验 pointer-array batch size。\r\n- 修复 CUDA/HIP 开启 `FLAGS_gemm_use_half_precision_compute_type` 时 FP16 `alpha/beta` 的类型不匹配问题，避免 BLAS 按 FP16 读取 float 标量而产生错误结果。\r\n- 将 FP16 accumulation 限制在 compute capability 7.0 及以上，并修正 FP16 64 位分发中的错误模板特化。\r\n- 移除因 `ldc` 不满足 cuBLAS 要求而无法覆盖目标 shape 的 `N == 1` BatchedGEMM 快速路径，以及重复、不可达的维度检查。\r\n- 新增 C++ 单元测试，覆盖 `MatMul/BatchedGEMM` 的 `int64_t` 接口以及 CPU 对不支持维度的异常处理。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79697"}
{"number": 75247, "title": "[Auto Parallel] Add co_shard spmd_rule for batchnorm and groupnorm.", "state": "closed", "created_at": "2025-09-12T03:11:08Z", "updated_at": "2026-09-15T06:40:05Z", "closed_at": "2026-09-15T06:40:05Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nAuto Parallel\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n为BatchNorm 和 GroupNorm算子支持多刀切机制", "url": "https://github.com/PaddlePaddle/Paddle/pull/75247"}
{"number": 75207, "title": "enhance fleety with fused_transpose_split_quant", "state": "closed", "created_at": "2025-09-10T11:47:18Z", "updated_at": "2026-09-15T06:40:03Z", "closed_at": "2026-09-15T06:40:03Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\n Improvements \r\n\r\n### Description\r\nenhance fleety with fused_transpose_split_quant\r\npcard-91067\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/75207"}
{"number": 75014, "title": "trace system", "state": "closed", "created_at": "2025-09-01T12:39:45Z", "updated_at": "2026-09-15T06:40:02Z", "closed_at": "2026-09-15T06:40:02Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\n Communication Library\r\n\r\n### PR Types\r\n Others\r\n\r\n\r\n### Description\r\ntrace system", "url": "https://github.com/PaddlePaddle/Paddle/pull/75014"}
{"number": 72641, "title": "[CI] clone third party checkout 'Needed a single revision'", "state": "closed", "created_at": "2025-05-09T08:41:18Z", "updated_at": "2026-09-15T06:40:00Z", "closed_at": "2026-09-15T06:40:00Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nEnvironment Adaptation\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nOthers\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\nclone频发子模块checkout失败\r\npcard-67164", "url": "https://github.com/PaddlePaddle/Paddle/pull/72641"}
{"number": 79770, "title": "add dev commit", "state": "closed", "created_at": "2026-09-14T11:26:43Z", "updated_at": "2026-09-15T06:35:54Z", "closed_at": "2026-09-15T06:35:50Z", "labels": ["skip-ci: template", "skip-ci: bot-approval-required"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-15T06:35:50Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nEnvironment Adaptation\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nOthers\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\ndevelop分支编包也带commit\r\nPcard-93406\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79770"}
{"number": 79769, "title": "[release/3.4][Accuracy] Align CUDA bilinear interpolation with deterministic Torch", "state": "closed", "created_at": "2026-09-14T11:16:24Z", "updated_at": "2026-09-15T06:35:15Z", "closed_at": "2026-09-15T06:35:15Z", "labels": ["skip-ci: api-benchmark"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-15T06:35:15Z", "body": "### PR Category\n\nOperator Mechanism\n\n### PR Types\n\nBug fixes\n\n### Description\n\nPcard-67164\n\ndevPR:https://github.com/PaddlePaddle/Paddle/pull/79762\n\n将已合并的 #79762 回移到 `release/3.4`，原始提交为 `b61e01d9f5a37647b4a80ddcb2d8b2a524b0a468`，使用 `git cherry-pick -x` 保留来源。\n\n#### 修复内容与范围\n\n修复 CUDA bilinear 插值在开启 `FLAGS_use_accuracy_compatible_kernel` 后，前向及输入梯度仍无法与 Torch 2.12 确定性路径逐位一致的问题。\n\n兼容分支限定 CUDA、FP32、NCHW、`align_corners=False`、`align_mode=0`，且按输出尺寸计算插值坐标：\n\n| 调用方式 | 开启兼容标志后的路径 |\n|---|---|\n| 显式 `size` | 兼容路径 |\n| `scale_factor` + `recompute_scale_factor=True` | 兼容路径 |\n| `scale_factor` + `recompute_scale_factor=False/None` | 原有路径 |\n\n重算缩放先转换为输出尺寸，与对应显式 `size` 语义一致，无需新增算子来源属性。未开启标志，以及其他 dtype/layout、1D linear、抗锯齿入口保持原实现。\n\n前向保留各步 FP32 舍入，避免 FMA 改变结果；反向复现四个 gather 分支的梯度及固定累加顺序，利用坐标单调性确定贡献区间，避免排序索引和原子累加。\n\n#### 回移处理与验证\n\n- 在反向入口出现一处上下文冲突：release 使用 `phi::dtype::MPTypeTrait`，develop 使用 `MPTypeTrait`。保留 release 原有完整命名空间，仅在其前面插入兼容分支。\n- 已逐行核对：本次 patch 的新增／删除内容与原 PR 合并提交完全一致，仅应用上下文不同，共 4 个文件、699 行新增。\n- `prek` 全部通过，包含 ast-grep、Ruff、clang-format、cpplint；`git diff --check` 通过。\n- 本次未单独完整编译 `release/3.4`；目标分支构建及运行验证由本 PR CI 执行。以下数值与性能结果来自原 PR，不能视为 release 分支已经完成的运行结果。\n\n#### 原 PR 已完成的验证\n\n环境：H800 / sm_90，Paddle CUDA 13.0，Torch 2.12.1+cu129，Torch 开启确定性算法，原生 eager 接口。\n\n- 新增 9 项回归测试全部通过，包括原始 MD5、随机梯度与重复稳定性、边界／大归约、PIR、转置输入、1D 前向、重算缩放和非重算回退。\n- 原始 4 组输出尺寸 `(31,68)`、`(20,35)`、`(19,37)`、`(30,45)` 的前向及输入梯度 MD5 完全一致；原始复现共 17 份数组逐位一致。\n- 100 组随机形状前向／反向、BF16 位置编码完整调用链，以及 5 组标量／分轴重算缩放与 Torch 逐位一致。\n- PaddleAPITest 8 组全部逐位通过；现有 bilinear、linear、interpolate API 共 122 项测试按独立进程运行全部通过。\n- 原 PR 的 Windows GPU 重跑、Linux 构建、静态检查及 Coverage 已通过。\n\n#### 原 PR 性能结论\n\n同一张 H800、相同输入与随机梯度，复用前向图单独测反向；预热 20 次，计时 100 次，5 轮中位数。同步墙钟耗时包含 Python 调用开销，单位 μs：\n\n| 输出尺寸 | Paddle 原路径 | Torch 非确定性 | Paddle 兼容路径 | Torch 确定性 |\n|---|---:|---:|---:|---:|\n| 31×68 | 33.8 | 39.9 | 368.0 | 1844.1 |\n| 20×35 | 26.5 | 39.9 | 307.8 | 981.7 |\n| 19×37 | 28.2 | 39.6 | 306.6 | 987.1 |\n| 30×45 | 28.2 | 39.5 | 326.5 | 1416.0 |\n\n兼容前向约 21 μs，与 Paddle 原路径基本一致。兼容反向约 0.30–0.37 ms，比 Paddle 原路径慢约 11 倍，但比对齐目标 Torch 2.12 确定性路径快约 3.2–5.0 倍。\n\n独立 Torch 进程及 CUDA Event 复核得到相近结果。31×68 的单独 Profiler 采样中，4 次索引梯度 kernel 合计约 1.36 ms、占 GPU kernel 时间约 80%；排序仅约 0.03 ms，主要瓶颈是通用确定性索引梯度归约。这些结论仅适用于该版本、硬件、形状与 eager 路径，不外推到 Torch 整体或 `torch.compile`。\n\n### 是否引起精度变化\n\n是\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79769"}
{"number": 78113, "title": "[API Compatibility No.326] Add paddle.special.digamma -part", "state": "open", "created_at": "2026-03-03T07:16:50Z", "updated_at": "2026-09-15T05:33:30Z", "closed_at": null, "labels": ["contributor", "HappyOpenSource", "API Compatibility"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\r\nUser Experience\r\n\r\n### PR Types\r\nNew features\r\n\r\n### Description\r\n添加 paddle.special.digamma API 实现，兼容 PyTorch。\r\n\r\n主要功能：\r\n- 添加 digamma 函数 API\r\n- 支持动态图和静态图模式\r\n- 支持 out 参数\r\n- 与 PyTorch API 兼容\r\n\r\n### 是否引起精度变化\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/78113"}
{"number": 79764, "title": "[Security] Fix RestrictedUnpickler dotted-name whitelist bypass", "state": "closed", "created_at": "2026-09-11T09:17:36Z", "updated_at": "2026-09-15T03:09:33Z", "closed_at": "2026-09-15T03:09:33Z", "labels": ["skip-ci: template"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-15T03:09:33Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\n\r\nUser Experience\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nSecurity\r\n\r\n### Description\r\n\r\n<!-- Describe what you’ve done -->\r\n\r\n#### 问题背景\r\n\r\n修复 `RestrictedUnpickler` 中由 NumPy wildcard 白名单和 pickle dotted name 属性遍历共同导致的白名单绕过问题。\r\n\r\n此前以下模块使用了 wildcard 白名单：\r\n\r\n* `numpy.core.numeric`\r\n* `numpy._core.numeric`\r\n\r\n对于 protocol 4/5 的 `STACK_GLOBAL`，pickle 的 `name` 可以包含 `.`。原实现只校验 `module` 是否位于白名单中，并在 wildcard 命中后直接调用：\r\n\r\n```python\r\nsuper().find_class(module, name)\r\n```\r\n\r\n当 `name` 为 dotted name 时，Python 可能继续按照属性链进行解析，从受信任的 NumPy 模块访问白名单之外的对象，从而绕过 `RestrictedUnpickler` 的白名单限制。\r\n\r\n#### 修复内容\r\n\r\n* 将 `numpy.core.numeric` 的 wildcard 白名单由 `'*'` 收紧为仅允许 `_frombuffer`\r\n* 将 `numpy._core.numeric` 的 wildcard 白名单由 `'*'` 收紧为仅允许 `_frombuffer`\r\n* 对命中 `_ALLOWED_CLASSES` 的模块，拒绝包含 `.` 的 dotted name，避免通过 `super().find_class()` 继续进行属性链遍历\r\n* 未全局禁止 dotted name，保留安全用户自定义类 fallback 对嵌套 qualified name 的兼容性\r\n* 审查 `_ALLOWED_CLASSES`，确认当前已不存在其他 `'*'` wildcard\r\n\r\n#### 测试覆盖\r\n\r\n新增并完善 RestrictedUnpickler 相关测试，覆盖以下场景：\r\n\r\n* protocol 5 NumPy ndarray 可通过 `safe_loads_pickle()` 正常反序列化\r\n* `numpy.core.numeric._frombuffer` 仍可正常解析\r\n* `numpy._core.numeric._frombuffer` 仍可正常解析\r\n* `numpy.core.numeric.builtins.eval` 被正确拒绝\r\n* `numpy._core.numeric.builtins.eval` 被正确拒绝\r\n* 其他 dotted name，例如 `foo.bar`，被正确拒绝\r\n* 普通 NumPy ndarray、dtype、multiarray reconstruction、字典及文件加载路径保持正常\r\n* 安全用户自定义嵌套类的 dotted qualified name 仍可通过 fallback 正常加载\r\n\r\n#### 验证结果\r\n\r\n* RestrictedUnpickler 完整测试：`48 passed, 1 skipped`\r\n* 跳过项原因：当前测试环境的 NumPy 不提供 `numpy._core.numeric`\r\n* `py_compile` 检查通过\r\n* 相关 pre-commit 检查全部通过\r\n* `git diff --check` 通过\r\n\r\n#### 影响范围\r\n\r\n本次修改仅涉及：\r\n\r\n* `python/paddle/framework/restricted_unpickler.py`\r\n* `test/legacy_test/test_restricted_unpickler.py`\r\n\r\n未修改 CIFAR、Flowers、下载或 tar 解压相关代码。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79764"}
{"number": 79750, "title": "[Operator Mechanism] Align CPU elementwise broadcast backward with PyTorch bitwise", "state": "closed", "created_at": "2026-09-08T07:31:47Z", "updated_at": "2026-09-15T03:02:10Z", "closed_at": "2026-09-15T03:02:10Z", "labels": ["skip-ci: template"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-15T03:02:10Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 对齐 Paddle CPU elementwise 广播反向（add / sub / div / remainder）在 `float32` / `float64` 下与 PyTorch 的逐位计算行为。此前广播侧梯度按串行顺序累加，且 div 的 `dy` 公式分组与 PyTorch 不同，导致梯度与 PyTorch CPU 存在数个 ulp 的偏差。主要包含四类修改：\r\n\r\n1. 广播梯度归约顺序对齐（cascade sum）；\r\n2. 除法反向公式分组对齐；\r\n3. 恒等梯度项跳过物化（性能）与 inplace view 别名修复；\r\n4. 空 Tensor 广播反向修复。\r\n\r\n所有算子统一走通用 elementwise grad 路径，不引入按算子的专用实现。\r\n\r\n#### 1. 广播梯度归约顺序对齐\r\n\r\n##### 问题\r\n\r\n广播输入的梯度通过通用路径串行累加，累加顺序与 PyTorch 的 cascade summation 不同。浮点加法不满足结合律，顺序不同则舍入结果不同，广播反向梯度与 PyTorch CPU 不逐位一致：\r\n\r\n```text\r\n串行：((a0 + a1) + a2) + a3 ...\r\ntorch：先按固定宽度分块向量内累加，再逐级归并（cascade）\r\n```\r\n\r\n##### 改动\r\n\r\n`CommonGradBroadcastCPU`、`ElemwiseGradBroadcast1CPU`、`ElemwiseGradBroadcast2CPU` 对需要归约的一侧：\r\n\r\n- 按输出 row-major 顺序物化逐元素梯度项（`dx_op(x[i], y[i], out[i], dout[i])`）；\r\n- 使用已有的 `TorchCompatibleReduceSum`（cascade sum，与 torch `Loop2d` 派发的向量内/向量外归约同序）对物化项归约；\r\n- 不需要归约的一侧直接写入目标梯度，不物化完整输出临时 buffer。\r\n\r\n#### 2. 除法反向公式分组对齐\r\n\r\n##### 问题\r\n\r\n原 `DivGradDY` 使用 `-dout * out / y`，求值顺序为 `(-dout * out) / y`；PyTorch CPU 的 div 反向（`FunctionsManual.cpp` 中 `-grad * ((self/other)/other)`）先计算 `out / y` 再乘 `-dout`。浮点乘除不满足结合律，`float32` 下两种分组可能产生最后几位的差异。\r\n\r\n##### 改动\r\n\r\n- `DivGradDY` 重排为 `-dout * (out / y)`，与 PyTorch 分组一致；\r\n- `dx` 保持 `dout / y`，与 PyTorch 的 `grad / other` 一致；\r\n- 删除早期实现中的 `TorchCompatibleDivideGradCPU` 专用路径，由上述通用方案取代，所有 elementwise 算子共享。\r\n\r\n#### 3. 恒等梯度项跳过物化与 inplace view 别名修复\r\n\r\n##### 问题\r\n\r\n逐项物化保证了归约顺序，但引入了性能回归：`[1048576] x [1]` 的 add 广播反向需要物化 4MB 中间项，耗时从约 1.03ms 升至 3.0ms。PyTorch 对梯度项与原始梯度相同的算子（add/sub 的恒等项）直接对原始梯度做 `sum_to_size`，不物化中间项。\r\n\r\n此外，eager 反向在 `grad_out` 引用计数为 1 时，会把第一个梯度输出（`dx`）作为 `grad_out` 的 view 传入 kernel（inplace 内存复用）。若直接对 `dout` 归约，cascade 入口的 `memset` 会先清零 `dout` 的前缀，再读到的已是被破坏的数据，结果恰好\"丢失首行\"。\r\n\r\n##### 改动\r\n\r\n- 恒等 functor（add 的 `IdentityGrad`、`SubGradDX`、`RemainderGradDx`）声明 `kGradTermIsDout = true` 标签，`elementwise_grad_base.h` 通过 SFINAE trait `GradTermEqualsDout<F>` 泛型检测；\r\n- 当梯度项与 `dout` 逐位相同（恒等 functor 且 `T == Tout`）时，跳过物化，直接对 `dout` 做 cascade 归约，并跳过无剩余工作的逐元素循环，对齐 PyTorch `sum_to_size` 的做法；\r\n- 未标注的 functor（div、mul、pow、`SubGradDY` 等）自动走原物化路径，行为不变；\r\n- 在 `ElemwiseGradComputeWithBroadcast` 中，归约侧输出与 `dout` 共享 buffer 时先 detach（重新分配），再进入归约；`CommonElementwiseBroadcastBackward` 补充对称的 `dy` detach guard（原有 `dx` guard 保持不变）。\r\n\r\n`[1048576] x [1]` add 反向耗时从物化方案的 3.0ms 回落至 0.57ms（stock 串行实现 1.03ms）。\r\n\r\n#### 4. 空 Tensor 广播反向修复\r\n\r\n##### 问题\r\n\r\n当广播结果 `out.numel() == 0` 时（如 `[0, 3]` 与 `[1, 3]`），原路径不会进入实际计算循环，非空一侧的输入梯度可能保持未初始化内存，而不是返回零梯度。\r\n\r\n##### 改动\r\n\r\n在 `ElemwiseGradComputeWithBroadcast` 入口显式处理空输出：无条件 `Alloc` 所需梯度（numel 为 0 也要分配空 holder，否则 autograd 判定该输入无梯度返回 `None`），并将非空梯度 buffer 清零。\r\n\r\n#### 修改后的行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| 广播反向梯度 vs PyTorch | 串行累加顺序不同，数个 ulp 偏差 | 单轴/多轴/标量/`[1M]x[1]` 逐位一致 |\r\n| div `dy` 公式分组 | `(-dout * out) / y` | `-dout * (out / y)`，与 PyTorch 一致 |\r\n| `[1048576]x[1]` add 反向耗时 | 物化方案 3.0ms | 0.57ms（stock 串行 1.03ms） |\r\n| inplace view 输入 + 归约侧 | 直接归约 `dout` 会被 memset 破坏（丢首行） | 归约前 detach，结果正确 |\r\n| 空 Tensor 广播反向 | 非空侧梯度可能未初始化 | 梯度为零，空 holder 正常分配 |\r\n| div double-grad vs PyTorch | 最大 6 ulp、80% 元素不一致 | `g2x` 逐位一致，`g2y` <= 1 ulp（附带改善） |\r\n\r\n合法输入的前向计算不变；梯度数值变化均为向 PyTorch 结果的对齐。\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79750"}
{"number": 79748, "title": "[Operator Mechanism] Consolidate strided view validation and gradient fixes", "state": "closed", "created_at": "2026-09-08T06:08:18Z", "updated_at": "2026-09-15T03:02:00Z", "closed_at": "2026-09-15T03:02:00Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-15T03:02:00Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 修复 strided view 的前向安全性和反向梯度正确性，覆盖 `as_strided`、`as_strided_grad`、`tensor_unfold_grad` 以及 ATen compat `as_strided` 路径。主要包含六类修改：\r\n\r\n1. 非连续 Tensor 的 NaN/Inf 检查安全性；\r\n2. strided view 的 shape、offset、存储范围和整数溢出校验；\r\n3. 重叠 view 和重叠 unfold 窗口的梯度累加；\r\n4. 非连续、嵌套 view 的反向坐标映射；\r\n5. ATen compat `as_strided` 的 storage offset 语义对齐；\r\n6. 公共辅助逻辑合并及回归测试补充。\r\n\r\n#### 1. 非连续 Tensor 的 NaN/Inf 检查安全性\r\n\r\n##### 问题\r\n\r\nNaN/Inf 检查直接把 Tensor 当成从首地址开始的连续内存，按 `ptr[i]` 扫描 `numel()` 个元素。这个假设对 strided view 不成立：\r\n\r\n```python\r\nx = paddle.arange(4, dtype='float32')\r\ny = paddle.as_strided(x, shape=[4, 4], stride=[0, 0])\r\n```\r\n\r\n`y` 的 16 个逻辑元素实际只映射到一个存储位置。若仍按连续内存扫描，检查范围会超过底层 allocation；对转置、切片等普通非连续 view，扫描到的内容也不是该 view 的逻辑元素。\r\n\r\n##### 改动\r\n\r\n在 `CheckTensorHasNanOrInf` 中检测到 Tensor 非连续时跳过这次 flat memory scan，并记录 verbose 日志。底层连续 Tensor 仍会在其自身检查路径中被检查。\r\n\r\n##### 修改后结果\r\n\r\n- 不再因为 stride 为 0 或非连续布局读取错误地址；\r\n- 不会把与 view 无关的内存误判为该 Tensor 的内容；\r\n- 连续 Tensor 的原有 NaN/Inf 检查行为不变。\r\n\r\n该行为是本 PR 当前保留的安全策略，未进一步扩大修改范围。\r\n\r\n#### 2. strided view 的 shape、offset、存储范围和溢出校验\r\n\r\n##### 问题\r\n\r\n原实现主要设置 view metadata，不完整验证 `(shape, stride, offset)` 描述的地址范围。非法 view 可能被接受，后续读写越界；短 stride 还会让后续代码访问不存在的 stride 项。\r\n\r\n例如，以下请求都不应创建合法 view：\r\n\r\n```python\r\nx = paddle.zeros([16], dtype='float32')\r\n\r\n# 访问远超过 allocation 的区域\r\npaddle.as_strided(x, shape=[64, 64], stride=[64, 1])\r\n\r\n# offset 超过 allocation\r\npaddle.as_strided(x, shape=[16], stride=[1], offset=4096 * 4)\r\n\r\n# byte offset 没有按元素大小对齐\r\npaddle.as_strided(x, shape=[2], stride=[1], offset=1)\r\n\r\n# shape 和 stride rank 不一致\r\npaddle.as_strided(x, shape=[15, 3], stride=[2])\r\n\r\n# stride * (shape - 1) 的 int64 计算溢出\r\npaddle.as_strided(x, shape=[5], stride=[1 << 62])\r\n```\r\n\r\nATen compat 路径还存在 storage offset 从元素数转换为 byte offset 时的无符号乘法问题：`(1 << 62) * 4` 可能回绕成 0，从而错误地别名到 allocation 开头。\r\n\r\n##### 改动\r\n\r\n- 在已有 strided view 校验中统一要求 `dims.size() == strides.size()`，并把检查放在 zero-size/其他 early return 之前；\r\n- 新增安全的 `int64_t` 加法和乘法，溢出时抛出 `InvalidArgument`，不再依赖回绕后的结果；\r\n- 计算 view 覆盖的最小、最大 storage element，检查是否落在底层 allocation 内；\r\n- 检查 offset 非负、按 dtype element size 对齐；\r\n- 在 ATen compat `as_strided` 和 `as_strided_` 中使用 checked 的 element-to-byte offset 转换，并复用同一套范围校验；\r\n- zero-size view 仍允许不触碰存储的合法形状，非零 view 必须满足完整 rank 和范围约束。\r\n\r\n##### 修改后结果\r\n\r\n- 越界、负 offset、非对齐 offset、rank 不一致和算术溢出均在进入 kernel 前明确报错；\r\n- 不会因为 int64/size_t 回绕而把非法 view 当成合法 view；\r\n- 合法的连续、负 stride、zero-size 和带 offset view 继续保持原有结果。\r\n\r\n#### 3. 重叠 view 和重叠 unfold 窗口的梯度累加\r\n\r\n##### 问题\r\n\r\n当多个逻辑位置映射到同一个 storage slot 时，反向不能使用普通 copy。旧路径会发生写覆盖，而不是把所有 incoming gradient 相加。\r\n\r\n`as_strided` 示例：\r\n\r\n```python\r\nx = paddle.arange(5, dtype='float32')\r\nx.stop_gradient = False\r\ny = paddle.as_strided(x, shape=[3, 3], stride=[1, 1])\r\ny.backward(paddle.ones_like(y))\r\n```\r\n\r\n`x.grad` 的正确结果是：\r\n\r\n```text\r\n[1, 2, 3, 2, 1]\r\n```\r\n\r\nzero-stride view 也必须累加：\r\n\r\n```python\r\nx = paddle.ones([1], dtype='float32')\r\nx.stop_gradient = False\r\ny = paddle.as_strided(x, shape=[4, 4], stride=[0, 0])\r\ny.backward(paddle.ones_like(y))\r\n# x.grad == [16]\r\n```\r\n\r\n`tensor.unfold` 在 `step < size` 时窗口相互重叠，同样存在覆盖问题。例如长度为 5、`size=3`、`step=1` 时，中间元素需要收到 3 个窗口的梯度。\r\n\r\n##### 改动\r\n\r\n- 增加 `MaybeOverlappingStrides`，按 stride 排序判断不同逻辑索引是否可能命中同一 storage slot；\r\n- 增加 `StridedTensorAccumulate`，把 dense 的 `out_grad` 按 view 几何 scatter-add 到目标梯度存储；\r\n- CPU 使用串行 host 累加，避免重复 index 在普通并行写入下丢失更新；\r\n- GPU 在可用 dtype 上使用 `arange + index_put(accumulate=true)` 做 scatter-add；不具备对应 kernel 的 backend 使用 host fallback 后再拷回；\r\n- `as_strided_grad` 和 `tensor_unfold_grad` 在检测到重叠时走累加路径，非重叠场景继续使用更快的 copy 路径；\r\n- 梯度 buffer 在需要累加时先清零，保证未命中位置保持零。\r\n\r\n##### 修改后结果\r\n\r\n- 重复 stride、zero stride、带 offset 的重叠 view 梯度均按出现次数求和；\r\n- overlapping unfold 的窗口贡献不再互相覆盖；\r\n- 非重叠 view 的计算路径和结果保持不变；\r\n- CPU 和 GPU 的梯度结果一致。\r\n\r\n#### 4. 非连续、嵌套 view 的反向坐标映射\r\n\r\n##### 问题\r\n\r\n`input_grad` 是按照输入 Tensor 的逻辑 shape 分配的 dense buffer，而 `as_strided` 的 `offset`、`stride` 描述的是共享 allocation 的 storage 坐标。两者只有在输入连续且 view 完全落在输入范围内时才可以直接用 `offset - input.offset()` 对齐。\r\n\r\n例如：\r\n\r\n```python\r\nbase = paddle.arange(8, dtype='float32')\r\nbase.stop_gradient = False\r\nouter = paddle.as_strided(base, shape=[6], stride=[1], offset=2 * 4)\r\ninner = paddle.as_strided(outer, shape=[3, 3], stride=[1, 1], offset=2 * 4)\r\ninner.backward(paddle.ones_like(inner))\r\n```\r\n\r\n如果直接把 storage index 当成 `outer` 的 row-major index，梯度会写入错误位置；如果 view 跨过输入自身边界，还可能写出 `input_grad`，即使它仍然位于共享 allocation 内。\r\n\r\n##### 改动\r\n\r\n- 对非连续输入或超出输入逻辑范围的 view，先在共享 storage 坐标中建立临时梯度 buffer；\r\n- 将 `out_grad` 按 view 的 shape/stride/offset 累加或复制到该 storage buffer；\r\n- 再按照输入自身的 dims/strides/offset 从 storage buffer gather 回 dense `input_grad`；\r\n- view 与 input 不相交的 storage slot 被自然丢弃，不会写出输入梯度范围；\r\n- 输入自身存在重叠时在入口处明确抛出 `NotImplementedError`，避免对“一个共享 slot 如何分摊到多个输入逻辑位置”做未定义猜测。\r\n\r\n##### 修改后结果\r\n\r\n- 转置、切片、带 offset 的 nested view 反向梯度能回到正确的输入逻辑位置；\r\n- view 起始于输入之前、结束于输入之后或与输入完全不相交时，梯度不会越界；\r\n- 输入自身重叠的情况得到明确诊断，要求调用方先转为 contiguous Tensor。\r\n\r\n#### 5. ATen compat `as_strided` 语义对齐\r\n\r\n##### 问题\r\n\r\nATen compat 的 `storage_offset` 按元素计数，而 Paddle 内部 metadata offset 按 byte 计数。原实现直接进行未经检查的乘法，并且 compat 路径手工构造 view，可能绕过 PHI `as_strided` kernel 的范围检查。\r\n\r\n##### 改动\r\n\r\n- 在 `Tensor::as_strided` 和 `Tensor::as_strided_` 中统一使用 checked element-to-byte offset 转换；\r\n- 复用 PHI 的 storage range、alignment、rank 和 overflow 检查；\r\n- compat 头文件改为包含统一的 `strided_utils.h`，避免两套校验逻辑漂移。\r\n\r\n##### 修改后结果\r\n\r\n- compat `as_strided` 与 Paddle PHI kernel 对非法范围、负 offset、溢出 offset 的行为一致；\r\n- 合法的 contiguous、non-contiguous 和 inplace view 仍保持原有语义；\r\n- 不再因为 offset 回绕而产生错误 alias。\r\n\r\n#### 6. 公共辅助逻辑合并及回归测试\r\n\r\n##### 问题\r\n\r\n早期实现把 view 校验和 backward 辅助逻辑拆到多个新增头文件中，导致公共 compat 入口需要额外 include，逻辑容易重复且不利于维护。部分修复还只覆盖 kernel 路径，没有对应的 forward、backward、CPU/GPU 和 compat 回归测试。\r\n\r\n##### 改动\r\n\r\n- 将 strided view 的范围、溢出、重叠判断、copy 和 accumulate 辅助逻辑合并到已有的 `paddle/phi/kernels/funcs/strided_utils.h`；\r\n- 删除临时拆分的 `strided_view_utils.h` 和 `strided_grad_utils.h`，不新增持久化头文件；\r\n- 补充或强化 `test_as_strided.py`、`test_tensor_unfold.py` 和 `ATen_as_strided_test.cc`。\r\n\r\n测试覆盖：\r\n\r\n- 重复 stride、zero stride、带 offset 的重叠梯度；\r\n- nested/non-contiguous input 的 backward；\r\n- shape/stride rank 不一致；\r\n- storage range、负 offset、非对齐 offset 和 int64 overflow；\r\n- zero-size view 和合法 view 的兼容行为；\r\n- ATen compat forward/inplace `as_strided` 的非法 offset 和越界行为；\r\n- CPU、GPU 以及可用的其他 backend 路径。\r\n\r\n##### 修改后结果\r\n\r\n- 相关逻辑集中在现有公共辅助头文件中，减少重复实现和 include 依赖；\r\n- 非法输入在 metadata/kernel 入口处被拒绝，合法输入保持兼容；\r\n- 关键梯度和边界回归场景有自动化测试覆盖。\r\n\r\n### 修改后的总体行为\r\n\r\n| 类别 | 之前 | 之后 |\r\n| --- | --- | --- |\r\n| 非连续 Tensor 的 NaN/Inf 检查 | 可能按连续地址误读或越界 | 非连续 view 跳过 flat scan，避免错误内存访问 |\r\n| shape/stride rank 不一致 | 可能被接受，后续访问缺失 stride | 入口明确报错 |\r\n| 越界或溢出 view | 可能静默创建并在读写时破坏内存 | 范围、对齐和算术溢出检查提前拒绝 |\r\n| 重叠 `as_strided` backward | 重复 slot 被覆盖 | 所有贡献累加 |\r\n| overlapping `unfold` backward | 窗口贡献互相覆盖 | 按窗口出现次数累加 |\r\n| 非连续/nested input backward | storage 坐标被误当成逻辑坐标 | 经过 storage buffer 正确 gather 回输入梯度 |\r\n| 输入自身重叠 | 可能产生未定义梯度 | 明确抛出 `NotImplementedError` |\r\n| ATen `storage_offset` | 元素到 byte 的转换可能回绕 | checked conversion，与 PHI 路径统一 |\r\n\r\n合法输入的前向结果不变；修改主要影响原本可能越界、静默接受或梯度错误的边界场景。梯度数值的变化是对重叠 view 原本错误的覆盖行为进行正确累加，不是精度策略变化。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n是", "url": "https://github.com/PaddlePaddle/Paddle/pull/79748"}
{"number": 79762, "title": "[Accuracy] Align CUDA bilinear interpolation with deterministic Torch", "state": "closed", "created_at": "2026-09-11T02:00:35Z", "updated_at": "2026-09-14T11:12:45Z", "closed_at": "2026-09-14T11:00:40Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-14T11:00:40Z", "body": "### PR Category\r\n\r\nOperator Mechanism\r\n\r\n### PR Types\r\n\r\nBug fixes\r\n\r\n### Description\n\nPcard-67164\r\n\r\n修复 CUDA `F.interpolate(..., mode='bilinear')` 在开启 `FLAGS_use_accuracy_compatible_kernel` 后，仍无法与 Torch 2.12 确定性插值路径逐位对齐的问题，同时覆盖前向与输入梯度。\r\n\r\n#### 问题与生效范围\r\n\r\n位置编码输入 `(1, 768, 52, 52)` 经 BF16 舍入后转换为 FP32，插值到 `(31, 68)`、`(20, 35)`、`(19, 37)`、`(30, 45)` 时，原有实现的前向运算及反向累加顺序与 Torch 不同。仅开启 accuracy-compatible 标志不能消除差异。\r\n\r\n本次新增分支限定为：CUDA、FP32、NCHW、bilinear、按输出尺寸计算插值坐标、`align_corners=False`、`align_mode=0`，且开启 `FLAGS_use_accuracy_compatible_kernel`。这里包含显式 `size`，以及 `scale_factor` 搭配 `recompute_scale_factor=True`：后者先计算输出尺寸，再按输入／输出尺寸计算坐标，与对应的显式 `size` 调用语义一致。Torch 对照开启 `torch.use_deterministic_algorithms(True)`，实际调用 `_upsample_linear_vec` 分解实现。\r\n\r\n```python\r\npaddle.set_flags({'FLAGS_use_accuracy_compatible_kernel': True})\r\ny = paddle.nn.functional.interpolate(\r\n    x, size=[31, 68], mode='bilinear', align_corners=False, align_mode=0\r\n)\r\n```\r\n\r\n未开启标志时继续使用原实现。1D linear、其他 dtype/layout、`recompute_scale_factor=False/None` 的 scale-factor 调用及抗锯齿入口不进入新增分支；等尺寸仍沿用原有复制路径。\r\n\r\n`recompute_scale_factor` 分支约定：\r\n\r\n| 调用方式 | accuracy-compatible 标志开启后的路径 |\r\n|---|---|\r\n| 显式 `size` | 兼容路径 |\r\n| `scale_factor` + `recompute_scale_factor=True` | 兼容路径 |\r\n| `scale_factor` + `recompute_scale_factor=False/None` | 原有路径 |\r\n\r\n该约定修正了初版说明中“所有 scale-factor 调用保持原路径”的范围表述。kernel 收到空 scale 时表示按输出尺寸计算坐标，不代表 Python 调用一定使用了显式 `size`。无需新增来源属性或改变算子接口。\r\n\r\n#### 实现要点\r\n\r\n- 前向依次沿宽、高计算插值，显式保留 FP32 各步舍入，避免 FMA 改变坐标、权重与输出。\r\n- 反向复现四个 gather 分支的梯度计算与 `11 → 10 → 01 → 00` 累加顺序。\r\n- 利用各角坐标映射的单调性，通过二分查找获取每个输入像素的输出贡献区间，按稳定索引顺序归约，无需排序索引或分配临时梯度张量。\r\n- 贡献较多时复现 Torch 确定性 index_put 的 32-lane 归约及尾部累加；贡献数有严格上界的常见尺寸使用单线程路径。新增反向不使用原子累加。\r\n\r\n#### 正确性验证\r\n\r\n已完成本地完整源码编译，使用新构建的原生 `F.interpolate` 验证，未通过 monkey patch 替换接口。环境为 H800 / sm_90、Paddle CUDA 13.0、Torch 2.12.1+cu129、Python 3.12。\r\n\r\n- 本 PR 四个文件的 `prek` 检查全部通过，包括 ast-grep、clang-format、cpplint 与 Ruff。\r\n- 新增回归测试 **9 项全部通过**：原始 MD5、随机梯度及重复稳定性、边界与大归约、PIR 静态图、转置输入、1D 前向、重算缩放与非重算回退、其他模式保持原路径。测试使用独立 NumPy 参考实现，不依赖安装 Torch。\r\n- 重算缩放新增 5 组配置（标量／分轴、下采样／上采样／混合缩放），与独立 NumPy 参考及对应 `size` 的前向、随机梯度逐位一致，每组重复 3 次；另外与 Torch 2.12 确定性路径对拍，5 组前向／反向全部逐位一致。非重算 `False/None` 路径验证标志开关前向逐位一致，反向采用 `rtol=atol=1e-6`，因为原有原子累加路径本身可能有舍入顺序差异。\r\n- 原始复现的 **17 份数组全部逐位一致**，包含四组 2D 前向/输入梯度、等尺寸及 1D 前向；差异元素数均为 0。\r\n- 原生 Paddle 与 Torch 额外 **100 组随机形状前向/反向逐位一致**；BF16 参数 → 转置 → FP32 插值 → BF16 输出的完整位置编码调用链，输出和参数梯度均逐位一致。\r\n- PaddleAPITest：**4 组小规模 + 4 组生产规模全部逐位通过**，无 skip 或 bitwise-known 豁免。\r\n- 现有测试按文件独立进程运行：bilinear **91 项**、linear **19 项**、interpolate API **12 项**，共 **122 项全部通过**。合并模块运行会受到已有静态图全局状态影响，因此采用独立进程验证。\r\n\r\n```bash\r\npython test/legacy_test/test_bilinear_interp_accuracy_compatible.py -v\r\npython test/legacy_test/test_bilinear_interp_v2_op.py\r\npython test/legacy_test/test_linear_interp_v2_op.py\r\npython test/legacy_test/test_nn_functional_interpolate.py\r\n```\r\n\r\n原始输入使用 `np.random.default_rng(0).standard_normal((1, 768, 52, 52)).astype(np.float32)`，再做 BF16 → FP32 转换；反向使用全 1 输出梯度：\r\n\r\n| 输出尺寸 | 前向 MD5 | 输入梯度 MD5 |\r\n|---|---|---|\r\n| 31×68 | `05f31786fbfd2f17bc3587a5872335f2` | `7407cf8a481e119de7e43689d6b634a0` |\r\n| 20×35 | `bfc8e76343b3b98acef27e373ef8f870` | `1161b1d927bce439cdc7589cfecb6ca8` |\r\n| 19×37 | `d116da26f32a919993158d9012a90cb1` | `1327ab280c2d20460d5469cf54224c25` |\r\n| 30×45 | `c8b2eec98469fbd185959fd10fac7cf5` | `d749fc8113211af85d17d1756d9a681e` |\r\n\r\n#### 性能结论与复核\r\n\r\n同一张空闲 H800、相同输入及随机输出梯度，使用 eager `grad(..., retain_graph=True)` 复用前向图，单独测反向；每配置预热 20 次、计时 100 次，随机配置顺序，重复 5 轮取中位数。下表为 CUDA 同步的墙钟耗时，包含 Python 调用开销，单位 μs：\r\n\r\n| 输出尺寸 | Paddle 原路径 | Torch 非确定性原路径 | Paddle 兼容路径 | Torch 确定性路径 |\r\n|---|---:|---:|---:|---:|\r\n| 31×68 | 33.8 | 39.9 | 368.0 | 1844.1 |\r\n| 20×35 | 26.5 | 39.9 | 307.8 | 981.7 |\r\n| 19×37 | 28.2 | 39.6 | 306.6 | 987.1 |\r\n| 30×45 | 28.2 | 39.5 | 326.5 | 1416.0 |\r\n\r\n- 兼容前向约 **21 μs**，与 Paddle 原路径基本一致。\r\n- 兼容反向约 **0.30–0.37 ms**，相对 Paddle 原路径慢约 **11 倍**，但比本次对齐目标——Torch 2.12 确定性路径——快约 **3.2–5.0 倍**。固定且兼容的浮点归约顺序存在额外成本；当前实现仍有优化空间。\r\n- 在**不导入 Paddle 的独立 Torch 进程**中复测，确定性反向为 **0.96–1.82 ms**，CUDA Event 与墙钟计时一致，结果可复现。连续发射的 CUDA Event 也可能包含发射间隙，因此另用 Profiler 检查实际 kernel 开销。\r\n- 对 31×68 的确定性反向做单独 Profiler 采样：4 次索引梯度 kernel 合计约 **1.36 ms，占 GPU kernel 时间约 80%**；4 次排序合计仅约 **0.03 ms**。主要瓶颈是通用确定性索引梯度归约，不能把排序或 Python 调用解释为主要耗时来源。Profiler 与正式计时分开运行。\r\n- 上述结论仅针对当前版本、硬件、形状及 eager 确定性路径，不代表 Torch 整体算子性能，也不包含 `torch.compile` 的性能评价。\r\n\r\n### 是否引起精度变化\r\n是\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79762"}
{"number": 79650, "title": "Avoid duplicate history checks in Python stack capture", "state": "open", "created_at": "2026-08-13T06:47:26Z", "updated_at": "2026-09-14T07:31:38Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nPerformance Optimization\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nPerformance\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n优化 memory history Python stack capture 的运行时开销。\r\n\r\n  当 memory history 关闭时，CaptureCurrentPyStack() 与 MemStackGuard 原本会重复检查 MemHistoryEnabled()。本次修改引入 MemStackCapture 状态快照，由 stack capture 一次性返回 recorder 是否启用及 stack_id，MemStackGuard 复用该状态，避免重复检查。\r\n\r\n  同时统一更新 eager op、legacy eager op 及 PyLayer 前向/反向调用点，并保留以下语义：\r\n\r\n  - recorder 关闭时仅执行一次状态检查并快速返回；\r\n  - recorder 开启但 Python stack capture 关闭时，仍然压入 stack_id = 0，避免错误继承外层 stack；\r\n  - guard 生命周期内 recorder 开关变化时，仍能保证 push/pop 平衡；\r\n  - 补充 MemStackGuard 关闭态、嵌套调用及运行时切换场景测试。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否", "url": "https://github.com/PaddlePaddle/Paddle/pull/79650"}
{"number": 79746, "title": "M100 fp32 only regen", "state": "open", "created_at": "2026-09-07T14:10:22Z", "updated_at": "2026-09-14T07:14:02Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\n\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79746"}
{"number": 8867, "title": "[WIP] Select Operator (do not merge)", "state": "closed", "created_at": "2018-03-08T05:44:23Z", "updated_at": "2026-09-13T10:32:56Z", "closed_at": "2023-01-11T11:39:40Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "This is a work in progress\r\n* Add Python API for select\r\n* Add select operator\r\n* Add unit tests for select (Fibonacci sequence) in both python and c++", "url": "https://github.com/PaddlePaddle/Paddle/pull/8867"}
{"number": 79766, "title": "[XPU] Remove duplicated activation op-test wrappers that shadow sqrt and silu tests", "state": "open", "created_at": "2026-09-12T10:17:55Z", "updated_at": "2026-09-12T18:05:21Z", "closed_at": null, "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\n\nCustom Device\n\n### PR Types\n\nBug fixes\n\n### Description\n\n`test/xpu/test_activation_op_xpu.py` defines `XPUTestFloorOP` and `XPUTestSiluOP` **twice** each (lines 399 / 1025 and 184 / 1202 on `develop`).\n\n`create_test_class()` in `test/xpu/get_test_cover_info.py` registers the generated cases into module globals keyed by the **inner** class name:\n\n```python\nfor test_class in register_classes:\n    ...\n    cls_name = f\"{test_class[0]}_{test_type}\"\n    func_globals[cls_name] = type(cls_name, (class_obj,), {...})\n```\n\nso a later registration that reuses an inner class name silently overwrites the earlier one.\n\n**1. XPU `sqrt` is not being tested at all.**\n\nThe stale `XPUTestFloorOP` block names its inner class `XPUTestSqrt` (a copy‑paste leftover — the op it configures is `floor`):\n\n```python\nclass XPUTestFloorOP(XPUOpTestWrapper):\n    def __init__(self):\n        self.op_name = 'floor'\n    class XPUTestSqrt(TestActivationOPBase):   # <-- should be XPUTestFloor\n        def set_case(self):\n            self.op_type = \"floor\"\n```\n\nIt is registered immediately after `XPUTestSqrtOP`, so `XPUTestSqrt_<dtype>` ends up holding the **floor** case. The test names still read `sqrt`, and `record_op_test('sqrt', ...)` still marks sqrt as covered, so the op-coverage report does not reveal the gap.\n\n**2. The silu base case silently regressed to an older implementation.**\n\nThe stale `XPUTestSiluOP` block re-registers `XPUTestSilu_<dtype>` with an older, minimal `set_case` that drops the bfloat16 `convert_float_to_uint16` path and the `init_shape` / `set_env` / `delete_env` hooks of the current implementation. (The `TestSilu_ZeroDim`, `TestSilu_LUT` and `TestSiluLargeShape1` subclasses keep working, because they captured the current class object at class-creation time — only the base case is affected.)\n\n**Fix**\n\nBoth stale blocks are strict subsets of the surviving definitions, so they are removed together with their `create_test_class` loops. The op each one covers is still registered exactly once by the surviving wrapper.\n\n**Effect (resolved statically from the module, per registered base class)**\n\n| generated base class | before | after |\n|---|---|---|\n| `XPUTestSqrt_<dtype>` | `XPUTestFloorOP`, op=`floor`, methods `set_case`, `test_check_grad` | `XPUTestSqrtOP`, op=`sqrt`, methods `set_case` |\n| `XPUTestSilu_<dtype>` | `XPUTestSiluOP` (old), methods `set_case` | `XPUTestSiluOP` (current), methods `set_case`, `init_shape`, `set_env`, `delete_env`, `test_check_output`, `test_check_grad` |\n\nRegistered base classes: **100 before, 100 after** — no generated test class is removed, two are restored to the op they are named for. `floor` goes from two wrappers to one; `sqrt` goes from zero to one.\n\nDiff is `+0 / −50`, test-only.\n\n### 是否引起精度变化\n\n否\n\n（Test-only change. It does re-enable XPU `sqrt` op tests that have not run for some time, so `PR-CI-Kunlun` may surface a pre-existing sqrt issue — that is the point of the change rather than a regression introduced by it.）\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79766"}
{"number": 79767, "title": "[Test] Un-shadow duplicated test classes in test_lu_op.py and test_tensordot.py", "state": "open", "created_at": "2026-09-12T10:19:40Z", "updated_at": "2026-09-12T18:05:19Z", "closed_at": null, "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### PR Category\n\nExecute Infrastructure\n\n### PR Types\n\nBug fixes\n\n### Description\n\nTwo unit-test files define a test class name twice at module level. Python keeps only the last binding, so `unittest` / `pytest` collect only the later class and the earlier case silently stops running.\n\n**1. `test/legacy_test/test_lu_op.py`**\n\n`TestLUOp` is defined at line 106 (`# m < n`, \"case 1\", `x_shape = [3, 10, 12]`) and again at line 387, where a zero-size case (`x_shape = [2, 0, 12]`) reuses the same name.\n\n`TestLUOp2` … `TestLUOp5` still behave correctly — they subclass the first definition and captured the class object at class-creation time — but the m < n case itself is no longer collected, since the module attribute `TestLUOp` now points at the zero-size class.\n\nRenamed the zero-size class to `TestLUOpZeroSize`, matching the `TestLUAPIZeroSize` naming already used in the same file. Both cases are then collected.\n\n| collected class | before | after |\n|---|---|---|\n| `TestLUOp` | L387 zero-size `[2, 0, 12]` | L106 m < n `[3, 10, 12]` (base of `TestLUOp2`–`TestLUOp5`) |\n| `TestLUOpZeroSize` | *(not collected)* | L387 zero-size `[2, 0, 12]` |\n\nClass count is unchanged (9 before, 9 after); one case is recovered.\n\n**2. `test/legacy_test/test_tensordot.py`**\n\n`TestTensordotAPIZeroSize` is defined at line 365 and again at line 385. The second definition is byte-identical to the first except that it adds `set_dtype` → `np.float64` — which is exactly what `TestTensordotAPIFloat64ZeroSize` (line 380, a subclass of the *first* definition) already provides.\n\nSo the duplicate buys no extra coverage and costs the default `float32` zero-size case: the collected `TestTensordotAPIZeroSize` runs float64, and float64 is then tested twice.\n\nRemoved the second definition. `TestTensordotAPIZeroSize` goes back to the base `set_dtype` (`np.float32`) and `TestTensordotAPIFloat64ZeroSize` continues to cover float64. No class defined after line 400 subclasses `TestTensordotAPIZeroSize`, so nothing else changes binding.\n\n| collected class | before | after |\n|---|---|---|\n| `TestTensordotAPIZeroSize` | float64 (duplicate of `TestTensordotAPIFloat64ZeroSize`) | float32 (base dtype) |\n| `TestTensordotAPIFloat64ZeroSize` | float64 | float64 (unchanged) |\n\nDiff is `+1 / −19`, test-only.\n\n### 是否引起精度变化\n\n否\n\n（Test-only. Two previously-uncollected cases start running again; no framework or kernel code is touched.）\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/79767"}
{"number": 48363, "title": "Merge v2 develop", "state": "open", "created_at": "2022-11-25T02:08:38Z", "updated_at": "2026-09-12T15:25:51Z", "closed_at": null, "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n### PR types\r\n<!-- One of [ New features | Bug fixes | Function optimization | Performance optimization | Breaking changes | Others ] -->\r\n\r\n### PR changes\r\n<!-- One of [ OPs | APIs | Docs | Others ] -->\r\n\r\n### Describe\r\n<!-- Describe what this PR does -->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/48363"}
{"number": 67802, "title": "Fix pnc bug", "state": "open", "created_at": "2024-08-29T01:40:42Z", "updated_at": "2026-09-11T23:58:11Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\n\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Deprecations | Others ] -->\r\n\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/67802"}
{"number": 79532, "title": "[AP] Add conv2d + epilogue fusion pattern.", "state": "closed", "created_at": "2026-07-22T06:36:05Z", "updated_at": "2026-09-11T09:40:04Z", "closed_at": "2026-09-11T09:40:04Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nCINN\r\n\r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nNew features\r\n\r\n### Description\r\n<!-- Describe what you’ve done -->\r\n新增`Conv2dEpilogueFusion`，初步支持 conv2d + trivial_fusion 的子图匹配，打通子图匹配和`ap_variadic` 算子替换流程。\r\n本 PR 提交的单测主要测试 pattern 能否成功匹配和替换成 ap_variadic 算子，后端计算直接调用 cutlass 的 conv2d+relu 接口，自动生成 varidic epilogue 由后续 PR 支持。\r\n\r\n当前生成的代码如下：\r\n```cpp\r\n\r\n// auto generated codes\r\n#include \"kernels.h\"\r\n#include <vector>\r\n\r\nnamespace ap {\r\n\r\ntemplate <typename T>\r\nstruct VariadicEpilogueFunctor {\r\n  struct Arguments {\r\n    // Extents of the output spatial dimensions. The epilogue of an implicit\r\n    // GEMM only knows the GEMM_M index `coord.row`, these are what let the\r\n    // generated statements decompose it back into (n, p, q).\r\n    int P;\r\n    int Q;\r\n    int64_t input0_dim1;\r\n    int64_t input0_dim3;\r\n    const half* in_ptr_0;\r\n  };\r\n\r\n  // Note: need to support vectorized operation\r\n  __forceinline__ __host__ __device__\r\n  T operator()(T x, const Arguments& args, const MatrixCoord& coord) const {\r\n    T out;\r\n    float op2_out0 = static_cast<float>(args.in_ptr_0[((coord.row / (args.P * args.Q)) * args.input0_dim1 * args.input0_dim1 * args.input0_dim3 + ((coord.row / args.Q) % args.P) * args.input0_dim1 * args.input0_dim3 + (coord.row % args.Q) * args.input0_dim3 + coord.column)]);\r\n    float op3_out0 = static_cast<float>(x + op2_out0);\r\n    float op4_out0 = static_cast<float>((op3_out0 > 0 ? op3_out0 : 0) );\r\n    out = op4_out0;\r\n    return out;\r\n  }\r\n};\r\n\r\ntemplate <int TuningConfigId>\r\nstatic void RunConv2dWithVariadicKernel(const Conv2dEpilogueParams &params, const half* input0, const half* input1, half* output, int64_t input0_dim0, int64_t input0_dim1, int64_t input0_dim3, int64_t input1_dim1, const half* in_ptr_0) {\r\n  using ElementT = half;\r\n  using ElementComputeT = float;\r\n\r\n  typename VariadicEpilogueFunctor<ElementComputeT>::Arguments epilogue_args;\r\n  epilogue_args.P = params.P;\r\n  epilogue_args.Q = params.Q;\r\n\r\n  epilogue_args.input0_dim1 = input0_dim1;\r\n  epilogue_args.input0_dim3 = input0_dim3;\r\n  epilogue_args.in_ptr_0 = in_ptr_0;\r\n\r\n  // The activation and the filter are both indexed along the channel dimension,\r\n  // while the output is indexed along the output channel dimension.\r\n  constexpr int AlignA = Alignment<ElementT, 16>::kValue;\r\n  constexpr int AlignB = AlignA;\r\n  constexpr int AlignC = Alignment<ElementT, 16>::kValue;\r\n\r\n  Conv2dVariadicFusion<ElementT, ElementComputeT, VariadicEpilogueFunctor,\r\n                       AlignA, AlignB, AlignC, TuningConfigId>(params,\r\n                                                              epilogue_args);\r\n}\r\n\r\n} // namespace ap\r\n\r\nextern \"C\" {\r\n\r\nvoid Conv2dVariadicKernel(void* stream_ptr, const half* input0, const half* input1, half* output, int64_t input0_dim0, int64_t input0_dim1, int64_t input0_dim3, int64_t input1_dim1, const half* in_ptr_0) {\r\n  // The activation is NHWC (data_format) and the filter is KRSC.\r\n  std::vector<int64_t> input0_shape;\r\n  input0_shape.resize(4);\r\n  input0_shape[0] = input0_dim0;\r\n  input0_shape[1] = input0_dim1;\r\n  input0_shape[2] = input0_dim1;\r\n  input0_shape[3] = input0_dim3;\r\n\r\n  std::vector<int64_t> input1_shape;\r\n  input1_shape.resize(4);\r\n  input1_shape[0] = input0_dim3;\r\n  input1_shape[1] = input1_dim1;\r\n  input1_shape[2] = input1_dim1;\r\n  input1_shape[3] = input0_dim3;\r\n\r\n  ap::Conv2dEpilogueParams params(stream_ptr,\r\n                                  input0,\r\n                                  input1,\r\n                                  /*bias=*/nullptr,\r\n                                  output,\r\n                                  input0_shape,\r\n                                  input1_shape,\r\n                                  std::vector<int64_t>{},\r\n                                  std::vector<int>{1, 1},\r\n                                  std::vector<int>{1, 1},\r\n                                  std::vector<int>{1, 1},\r\n                                  1);\r\n\r\n  ap::RunConv2dWithVariadicKernel<ap::DefaultConfig::kConfigId>(params, input0, input1, output, input0_dim0, input0_dim1, input0_dim3, input1_dim1, in_ptr_0);\r\n}\r\n}\r\n\r\n```\r\n\r\n#### 支持范围\r\n| 维度 | 首版支持 | 拦截位置 |\r\n|---|---|---|\r\n| 算子 | pd_op.conv2d fprop | source_pattern |\r\n| 设备 | CUDA（`-arch=sm_80`）；DCU/HIP 不支持 | `constraint()` → `compile_command_util.supports(\"conv2d\")` |\r\n| dtype | fp16（已验证）；bf16 代码路径存在但未验证 | 无检查 |\r\n| activation 布局 | `data_format == \"NHWC\"` | `constraint()` |\r\n| filter 布局 | `KRSC`，依赖 `FLAGS_manually_trans_conv_filter=1` | 运行期 `Conv2dEpilogueParams` 断言（见已知限制）|\r\n| groups | `== 1` | `constraint()` |\r\n| padding | `padding_algorithm == \"EXPLICIT\"` 且上下/左右对称 | `constraint()` |\r\n| kernel / stride / dilation | 任意（走 `IteratorAlgorithm::kOptimized` + `StrideSupport::kStrided`，`R/S ≤ 32` 时最优） | — |                                            \r\n| epilogue | `ap_trivial_fusion_op`，输入 2 ~ 11、输出；1 ~ 10（`register_conv2d_epilogue_class` 注册），可融性由 `constraint()` 的访存拓扑化简判定 | `constraint()` |\r\n\r\n实现上固定的选择：`split_k_slices = 1`；tile 固定 `DefaultConfig::kConfigId`，autotune 未开启；epilogue functor为标量版本，未向量化；AlignA/AlignB 按输入通道 C 取、AlignC 按输出通道 K 取。\r\n\r\n#### 验证环境\r\n- GPU / 工具链：A100（sm_80）、CUDA 12.8 + nvcc、gcc 12.3.0、Python 3.10；构建需开启 CINN。\r\n- 端到端单测：`test/ap/test_conv2d_epilogue.py`，NHWC fp16、3×3、padding=1、groups=1，子图为 `conv2d + add + relu`。断言融合发生（PIR 中出现`pd_op.ap_variadic`）并与 Eager 对数值（atol=1e-1）。已覆盖 add 的两种索引形态：per-channel bias（沿 N/P/Q 广播，索引化简为coord.column）和与输出同形的逐元素加（coord.row 反解为 (n,p,q) 后线性化）。\r\n- 运行前置：`FLAGS_manually_trans_conv_filter=True`（filter 走 KRSC）、`FLAGS_deny_cinn_ops=\"transpose\"`（transpose 留在 phi kernel）。\r\n\r\n未验证 / 不在本 PR 声明范围：\r\n- DCU/HIP（无环境，已在 pass 层拒绝并有单测）；\r\n- sm_80 以外架构；\r\n- fp32；\r\n- 性能对比与 autotune。\r\n\r\n### 是否引起精度变化\r\n<!-- one of the following [ 是 | 否 ]-->\r\n否\r\n\r\n@Paddle-Bot 请review。", "url": "https://github.com/PaddlePaddle/Paddle/pull/79532"}
{"number": 57795, "title": "【PaddlePaddle Hackathon 5th No.30】为 Paddle 新增 vdot API ", "state": "closed", "created_at": "2023-09-27T03:37:28Z", "updated_at": "2026-09-11T09:17:39Z", "closed_at": "2023-09-27T03:50:46Z", "labels": ["contributor"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n### PR types\r\n<!-- One of [ New features ] -->\r\n为 Paddle 新增 vdot API \r\n\r\n\r\n### PR changes\r\n<!-- One of [ OPs] -->\r\n\r\n### Description\r\n为 Paddle 新增 vdot API \r\n新增vdot 单测\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/57795"}
{"number": 79747, "title": "[Operator Mechanism] Align var and std CPU behavior with PyTorch", "state": "closed", "created_at": "2026-09-08T03:23:46Z", "updated_at": "2026-09-11T07:29:15Z", "closed_at": "2026-09-11T07:29:15Z", "labels": ["skip-ci: template"], "comments_count": null, "is_pr": true, "merged_at": "2026-09-11T07:29:15Z", "body": "<!-- TemplateReference: https://github.com/PaddlePaddle/Paddle/wiki/PULL-REQUEST-TEMPLATE--REFERENCE -->\r\n<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n\r\n### PR Category\r\n<!-- One of [ User Experience | Execute Infrastructure | Operator Mechanism | CINN | Custom Device | Performance Optimization | Distributed Strategy | Parameter Server | Communication Library | Auto Parallel | Inference | Environment Adaptation ] -->\r\nOperator Mechanism\r\n \r\n### PR Types\r\n<!-- One of [ New features | Bug fixes | Improvements | Performance | BC Breaking | Deprecations | Docs | Devs | Not User Facing | Security | Others ] -->\r\nBug fixes\r\n\r\n### Description\r\n\r\n本 PR 对齐 Paddle `var/std` 在 CPU、GPU、动态图、PIR、静态图和 stride 路径上的行为。每类修改分别说明如下。\r\n\r\n#### 1. CPU 非有限值处理\r\n\r\n**修改前的问题**\r\n\r\nCPU kernel 中的 `StoreFloatOrDouble` 将所有非有限 float 统一写成 `+Inf`。例如：\r\n\r\n```python\r\nx = paddle.to_tensor([float(\"nan\")], dtype=\"float32\")\r\npaddle.var(x)\r\n```\r\n\r\nPyTorch 返回 `NaN`，原 Paddle 返回 `+Inf`；`-Inf` 的符号也会丢失。\r\n\r\n**修改内容**\r\n\r\n重写结果存储逻辑，分别处理 NaN、`+Inf`、`-Inf`，并仅将有限值溢出转换成对应符号的 Inf。\r\n\r\n**修改后结果**\r\n\r\nCPU `var/std` 保持 IEEE 非有限值语义，与 PyTorch 的 NaN/Inf 分类和符号一致。\r\n\r\n#### 2. CPU 累加算法和 stride 对齐\r\n\r\n**修改前的问题**\r\n\r\n原 CPU 实现依赖 mean/sum 组合和连续内存访问。对转置等非连续 view，累加访问顺序与 PyTorch 不同，结果可能无法逐位一致；多输出计算也完全串行。\r\n\r\n例如，对转置输入：\r\n\r\n```python\r\nx = paddle.to_tensor(data).transpose([1, 0])\r\npaddle.var(x, axis=0)\r\n```\r\n\r\n原实现和 PyTorch 的最后若干 bit 可能不同。\r\n\r\n**修改内容**\r\n\r\n- float/double 单输出使用 mean + squared-difference；\r\n- 多输出使用 double Welford；\r\n- correction 使用 `max(0, n - correction)`；\r\n- 增加输出维度级 OpenMP 并行；\r\n- 注册 CPU float16/bfloat16 前向 kernel。\r\n\r\n**修改后结果**\r\n\r\n连续和非连续输入的 CPU `var/std` 与 PyTorch 保持逐位一致，并支持更完整的 CPU dtype。\r\n\r\n#### 3. 显式 `axis=[]` 语义\r\n\r\n**修改前的问题**\r\n\r\n原 Python API、native backward 和 composite backward 会把 `axis=[]` 当成完整 reduction。实际上 PyTorch 的行为是：\r\n\r\n- forward 仍然计算整体 variance/std；\r\n- backward divisor 使用 `1`；\r\n- `axis=None` 才使用实际 reduction size。\r\n\r\n例如 `x=[1, 1.7]` 时，`axis=[]` 的 backward 不应与 `axis=None` 共用同一个 divisor。\r\n\r\n**修改内容**\r\n\r\n- Python 层将 `axis=None` 规范化为完整 axis，显式 `axis=[]` 保持为空；\r\n- CPU/GPU `var_grad` 分别处理空 axis；\r\n- 两套 composite backward 规则保留空 axis，并使用独立 divisor。\r\n\r\n**修改后结果**\r\n\r\nforward 结果保持整体 reduction，backward 的 `axis=[]` 与 `axis=None` 按 PyTorch 语义分别计算，native 和 composite 路径一致。\r\n\r\n#### 4. correction 奇异边界及其 `axis=[]` 回归\r\n\r\n**修改前的问题**\r\n\r\n当完整 reduction 满足 `n - correction <= 0` 时，原 backward 直接进行有符号除零。例如：\r\n\r\n```python\r\nx = paddle.to_tensor([1.0, 2.0], stop_gradient=False)\r\ny = paddle.var(x, correction=2)\r\ny.backward()\r\n```\r\n\r\n原 Paddle 可能得到 `[-Inf, +Inf]`，PyTorch 返回 `[+Inf, +Inf]`；恒定输入时 PyTorch 返回 NaN。\r\n\r\n第一次加入 singular branch 时又出现了新的回归：条件仅判断 `denom <= 0`，把 `axis=[]` 也错误纳入 full reduction 处理，破坏了上一节已经修复的空 axis 行为。\r\n\r\n**修改内容**\r\n\r\n- CPU/GPU `var_grad` 在 full reduction 且 `n - correction <= 0` 时使用：\r\n  - `x == mean` -> NaN；\r\n  - `x != mean` -> `+Inf`；\r\n- 将 singular handling 限制为归一化后选中全部维度的 reduction；\r\n- 显式 `axis=[]` 不进入该分支，继续使用其独立 divisor。\r\n\r\n**修改后结果**\r\n\r\n完整 reduction 的 correction 边界与 PyTorch 对齐，同时不再破坏 `axis=[]` 的特殊 backward 语义。\r\n\r\n\r\n### 是否引起精度变化\r\n\r\n是。该 PR 有意修正 CPU/native `var/std` 与 PyTorch 不一致的数值行为，可能改变 NaN/Inf、非连续输入、低精度和 correction 边界下的结果；对于正常有限输入，目标是与 PyTorch 保持逐位一致。", "url": "https://github.com/PaddlePaddle/Paddle/pull/79747"}
{"number": 28396, "title": "Hip", "state": "closed", "created_at": "2020-11-04T02:17:21Z", "updated_at": "2026-09-11T05:04:43Z", "closed_at": "2020-11-04T03:07:41Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "<!-- Demo: https://github.com/PaddlePaddle/Paddle/pull/24810 -->\r\n### PR types\r\nNew features\r\n<!-- One of [ New features | Bug fixes | Function optimization | Performance optimization | Breaking changes | Others ] -->\r\n\r\n### PR changes\r\nOthers\r\n<!-- One of [ OPs | APIs | Docs | Others ] -->\r\n\r\n### Describe\r\n1、修改编译选项WITH_AMD_GPU为 WITH_ROCM_PLATFORM\r\n2、为rocm平台添加相关编译选项\r\n3、添加ROCM 平台底层代码调用：dynload，完善miopen/hiprand/rocblas/rocrand等相关库的调用接口\r\n\r\n扩展paddle框架的硬件适用范围，\r\n编译参数：\r\n\r\ncmake .. -DPYTHON_EXECUTABLE=$PATH_PYTHON/bin/python3 -DPYTHON_INCLUDE_DIR=$PATH_PYTHON/include\r\n-DPYTHON_LIBRARY=$PATH_PYTHON/lib/libpython3.so -DCMAKE_BUILD_TYPE=Release -DWITH_GPU=OFF -DWITH_GLOO=OFF -DWITH_TP_CACHE=ON -DWITH_TESTING=ON -DWITH_MKL=OFF -DWITH_NCCL=OFF -DWITH_ROCM_PLATFORM=ON\r\nos：CentOS Linux release 7.7.1908\r\nGCC：7.3.1\r\ncmake:3.18\r\nrocm:3.5\r\n<!-- Describe what this PR does -->\r\n", "url": "https://github.com/PaddlePaddle/Paddle/pull/28396"}
