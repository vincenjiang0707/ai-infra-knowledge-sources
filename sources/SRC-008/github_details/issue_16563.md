# [Issue #16563] [Bug]: 0.23.0RC1版本800I A2单机拉起deepseek-v4服务容器内使用内存container_memory_working_set_bytes持续增加

source: https://github.com/vllm-project/vllm-ascend/issues/16563
state: closed | updated: 2026-09-22T03:04:48Z
labels: bug, llm-model, deepseek

## 正文

### Your current environment

拉起服务配置如下
export OMP_PROC_BIND=false
export OMP_NUM_THREADS=10
export PYTHON_NPU_ALLOC_CONF=expandable_segments:True
export LD_PRELOAD=/usr/lib64/libjemalloc.so.2:$LD_PRELOAD
export HCCL_BUFFSIZE=1024
export VLLM_ASCEND_APPLY_DSWA_PATCH=1
export VLLM_ASCEND_ENABLE_FLASHCOMM=1
export TASK_QUEUE_ENABLE=1
export HCCL_OP_EXPANSION_MODE="AIV"


vllm serve /data/DeepSeek-V4-Flash \
--max-model-len 133072 \
--max-num-batched-tokens 8192 \
--served-model-name DeepSeek-V4-Flash \
--gpu-memory-utilization 0.9 \
--max-num-seqs 32 \
--data-parallel-size 1 \
--tensor-parallel-size 8 \
--enable-expert-parallel \
--tokenizer-mode deepseek_v4 \
--tool-call-parser deepseek_v4 \
--enable-auto-tool-choice \
--reasoning-parser deepseek_v4 \
--safetensors-load-strategy 'prefetch' \
--enable-prefix-caching \
--model-loader-extra-config '{"enable_multithread_load": "true", "num_threads": 128}' \
--quantization ascend \
--port 22101 \
--block-size 128 \
--speculative-config '{"num_speculative_tokens": 1,"method": "mtp","enforce_eager": true}' \
--compilation-config '{"cudagraph_mode": "FULL_DECODE_ONLY"}' \
--async-scheduling \
--additional-config '{"ascend_compilation_config":{ "enable_npugraph_ex":true, "enable_static_kernel":false }, "enable_cpu_binding": "true", "enable_dsa_cp": true, "multistream_overlap_shared_expert":true}'

### 🐛 Describe the bug

拉起服务后，观察容器内container_memory_working_set_bytes可以发现使用量一直在增加，7天增加了80G，服务访问量大概为每秒20-40个请求，该现象可以稳定复现。

## 评论 (1)

### luluxiu520 · 2026-09-22

问题已定位清楚，与该问题一致，https://github.com/vllm-project/vllm-ascend/issues/12345#issuecomment-5099010239，问题已解决。
