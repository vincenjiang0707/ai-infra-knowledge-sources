source: https://docs.vllm.ai/en/latest/api/vllm/distributed/kv_events/
lastmod: 2026-09-24

#

`vllm.distributed.kv_events`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events)

Classes:

-
–[BlockRemoved](https://docs.vllm.ai#vllm.distributed.kv_events.BlockRemoved) -
–[BlockStored](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored) -
–[EventPublisher](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisher)Lightweight publisher for EventBatch batches with data parallelism

-
–[EventPublisherFactory](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisherFactory) -
–[KVCacheEvent](https://docs.vllm.ai#vllm.distributed.kv_events.KVCacheEvent)Base class for all KV cache-related events.

-
–[KVConnectorKVEvents](https://docs.vllm.ai#vllm.distributed.kv_events.KVConnectorKVEvents)Abstract base class for KV events.

-
–[KVEventAggregator](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator)Aggregates KV events across multiple workers.

-
–[NullEventPublisher](https://docs.vllm.ai#vllm.distributed.kv_events.NullEventPublisher)No-op implementation (default when disabled).

-
–[ZmqEventPublisher](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher)Reliable PUB/ROUTER publisher with an in-memory replay buffer.


##

`BlockRemoved`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockRemoved)

Bases: [KVCacheEvent](https://docs.vllm.ai#vllm.distributed.kv_events.KVCacheEvent)

Attributes:

-
([group_idx](https://docs.vllm.ai#vllm.distributed.kv_events.BlockRemoved.group_idx)

) –[int](https://docs.python.org/3/builtins/functions.html#int)| NoneLOCAL or REMOTE relative to the publisher; None means unspecified.

-
([locality](https://docs.vllm.ai#vllm.distributed.kv_events.BlockRemoved.locality)

) –[str](https://docs.python.org/3/builtins/stdtypes.html#str)| NoneSecondary offloading tier identifier, if generated by one.


## Source code in `vllm/distributed/kv_events.py`


##

`BlockStored`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored)

Bases: [KVCacheEvent](https://docs.vllm.ai#vllm.distributed.kv_events.KVCacheEvent)

Attributes:

-
([block_size](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.block_size)

) –[int](https://docs.python.org/3/builtins/functions.html#int)Deprecated: use

`lora_name`

for KV block key hash. -
([extra_keys](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.extra_keys)

) –[list](https://docs.python.org/3/builtins/stdtypes.html#list)[[tuple](https://docs.python.org/3/builtins/stdtypes.html#tuple)[[Any](https://docs.python.org/3/library/typing.html#typing.Any), ...] | None] | NoneStore events carry cache-spec metadata so consumers can classify and

-
([kv_cache_spec_sliding_window](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.kv_cache_spec_sliding_window)

) –[int](https://docs.python.org/3/builtins/functions.html#int)| NoneLOCAL or REMOTE relative to the publisher; None means unspecified.

-
([locality](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.locality)

) –[str](https://docs.python.org/3/builtins/stdtypes.html#str)| NoneSecondary offloading tier identifier, if generated by one.

-
([lora_name](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.lora_name)

) –[str](https://docs.python.org/3/builtins/stdtypes.html#str)| NoneExtra keys used in block hash computation, one entry per block in

-
([ownership](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.ownership)

) –[str](https://docs.python.org/3/builtins/stdtypes.html#str)| NoneSession that triggered this store or reuse report.


## Source code in `vllm/distributed/kv_events.py`


###

`block_size`

`instance-attribute`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.block_size)

Deprecated: use `lora_name`

for KV block key hash. Retained for backward compatibility.

###

`extra_keys = None`

`class-attribute`

`instance-attribute`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.extra_keys)

Store events carry cache-spec metadata so consumers can classify and filter groups as they are learned. Remove events only need group_idx+hash.

###

`kv_cache_spec_sliding_window = None`

`class-attribute`

`instance-attribute`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.kv_cache_spec_sliding_window)

LOCAL or REMOTE relative to the publisher; None means unspecified.

###

`locality = None`

`class-attribute`

`instance-attribute`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.locality)

Secondary offloading tier identifier, if generated by one.

###

`lora_name`

`instance-attribute`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.lora_name)

Extra keys used in block hash computation, one entry per block in block_hashes. Each entry contains MM identifiers, LoRA name, cache_salt, prompt embedding hashes, etc. for that specific block. Exposed for external KV cache consumers to reconstruct block hashes.

###

`ownership = None`

`class-attribute`

`instance-attribute`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.BlockStored.ownership)

Session that triggered this store or reuse report.

This identifies the request context that emitted the event, not exclusive ownership of the underlying block, which may be shared across sessions.

##

`EventPublisher`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisher)

Bases: [ABC](https://docs.python.org/3/library/abc.html#abc.ABC)

Lightweight publisher for EventBatch batches with data parallelism support.

In data parallel setups, each DP rank runs its own EventPublisher instance to avoid duplicate events and ensure proper event attribution:

- Each DP rank creates a separate publisher
- Publishers automatically annotate events with their data_parallel_rank
- This allows consumers to distinguish events from different DP ranks

The publisher is responsible for adding DP metadata since the scheduler operates independently of DP topology and shouldn't need DP awareness.

Methods:

-
–[get_publisher_config](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisher.get_publisher_config)Return the publisher's resolved runtime configuration.

-
–[publish](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisher.publish)Emit events in order.

-
–[shutdown](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisher.shutdown)Shutdown the publisher.


## Source code in `vllm/distributed/kv_events.py`


##

`EventPublisherFactory`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisherFactory)

Methods:

-
–[create](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisherFactory.create)Create publisher from a config mapping.


## Source code in `vllm/distributed/kv_events.py`


###

`create(config, data_parallel_rank=0)`

`classmethod`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisherFactory.create)

Create publisher from a config mapping.

## Source code in `vllm/distributed/kv_events.py`


##

`KVCacheEvent`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVCacheEvent)

Bases: `Struct`


Base class for all KV cache-related events.

##

`KVConnectorKVEvents`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVConnectorKVEvents)

Bases: [ABC](https://docs.python.org/3/library/abc.html#abc.ABC)

Abstract base class for KV events. Acts as a container for KV events from the connector.

## Source code in `vllm/distributed/kv_events.py`


##

`KVEventAggregator`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator)

Aggregates KV events across multiple workers. Tracks how many times each event appears and returns only those that were emitted by all workers.

Methods:

-
–[add_events](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.add_events)Add events from a worker batch.

-
–[clear_events](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.clear_events)Clear all tracked events.

-
–[get_all_events](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.get_all_events)Return all events for all workers.

-
–[get_common_events](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.get_common_events)Return events that appeared in all workers.

-
–[get_number_of_workers](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.get_number_of_workers)Return the number of workers.

-
–[increment_workers](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.increment_workers)Increment the number of workers contributing events.

-
–[reset_workers](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.reset_workers)Reset the number of workers to 1.


## Source code in `vllm/distributed/kv_events.py`


|
|

###

`add_events(events)`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.add_events)

Add events from a worker batch.

Parameters:

-

(`events`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.add_events(events))

) –[list](https://docs.python.org/3/builtins/stdtypes.html#list)[[KVCacheEvent](https://docs.vllm.ai#vllm.distributed.kv_events.KVCacheEvent)]List of KVCacheEvent objects.


## Source code in `vllm/distributed/kv_events.py`


###

`clear_events()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.clear_events)

###

`get_all_events()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.get_all_events)

###

`get_common_events()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.get_common_events)

Return events that appeared in all workers.

Returns:

-

–[list](https://docs.python.org/3/builtins/stdtypes.html#list)[[KVCacheEvent](https://docs.vllm.ai#vllm.distributed.kv_events.KVCacheEvent)]List of events present in all workers.


## Source code in `vllm/distributed/kv_events.py`


###

`get_number_of_workers()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.get_number_of_workers)

###

`increment_workers(count=1)`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.KVEventAggregator.increment_workers)

Increment the number of workers contributing events.

Parameters:

## Source code in `vllm/distributed/kv_events.py`


##

`NullEventPublisher`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.NullEventPublisher)

Bases: [EventPublisher](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisher)

No-op implementation (default when disabled).

## Source code in `vllm/distributed/kv_events.py`


##

`ZmqEventPublisher`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher)

Bases: [EventPublisher](https://docs.vllm.ai#vllm.distributed.kv_events.EventPublisher)

Reliable PUB/ROUTER publisher with an in-memory replay buffer.

Spawns a separate thread to handle publishing from a queue.

#### Parameters[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher--parameters)

endpoint: PUB address. Use `tcp://*:5557`

to bind or `tcp://host:5557`

to connect. replay_endpoint: Optional ROUTER address for replay requests. When given, subscribers can request missed batches by sending the starting sequence number as an 8-byte big-endian integer. buffer_steps: Number of past batches to keep for replay. hwm: ZeroMQ high-water-mark for PUB socket. max_queue_size: Maximum number of events to buffer in memory. topic: Topic to publish events to.

Methods:

-
–[offset_endpoint_port](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher.offset_endpoint_port)Helper function to offset the port in an endpoint by

-
–[shutdown](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher.shutdown)Stop the publisher thread and clean up resources.


## Source code in `vllm/distributed/kv_events.py`


|
|

###

`_bind(sock, endpoint)`

`staticmethod`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher._bind)

Bind; tcp://*:0 binds this node's IP and returns the bound endpoint.

## Source code in `vllm/distributed/kv_events.py`


###

`_is_bind_endpoint(endpoint)`

`staticmethod`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher._is_bind_endpoint)

Bind wildcard and ipc/inproc endpoints; connect to the rest.

###

`_publisher_thread()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher._publisher_thread)

Background thread that processes the event queue.

## Source code in `vllm/distributed/kv_events.py`


###

`_service_replay()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher._service_replay)

If a replay request is waiting, send buffered batches.

## Source code in `vllm/distributed/kv_events.py`


###

`_socket_setup()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher._socket_setup)

Initialize sockets https://pyzmq.readthedocs.io/en/v19.0.0/morethanbindings.html#thread-safety

## Source code in `vllm/distributed/kv_events.py`


###

`offset_endpoint_port(endpoint, data_parallel_rank)`

`staticmethod`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher.offset_endpoint_port)

Helper function to offset the port in an endpoint by the data parallel rank.

Parameters:

-

(`endpoint`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher.offset_endpoint_port(endpoint))

) –[str](https://docs.python.org/3/builtins/stdtypes.html#str)| NoneThe endpoint string (e.g., "tcp://*:5557" or "inproc://cache")

-

(`data_parallel_rank`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher.offset_endpoint_port(data_parallel_rank))

) –[int](https://docs.python.org/3/builtins/functions.html#int)The data parallel rank to offset by


Returns:

-

–[str](https://docs.python.org/3/builtins/stdtypes.html#str)| NoneThe endpoint with the port offset by data_parallel_rank or suffix appended; a tcp port of 0 is left for the OS.


## Source code in `vllm/distributed/kv_events.py`


###

`shutdown()`

[¶](https://docs.vllm.ai#vllm.distributed.kv_events.ZmqEventPublisher.shutdown)

Stop the publisher thread and clean up resources.