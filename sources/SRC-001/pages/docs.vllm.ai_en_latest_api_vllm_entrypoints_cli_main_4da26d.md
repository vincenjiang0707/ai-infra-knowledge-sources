source: https://docs.vllm.ai/en/latest/api/vllm/entrypoints/cli/main/
lastmod: 2026-09-24

#

`vllm.entrypoints.cli.main`

[¶](https://docs.vllm.ai#vllm.entrypoints.cli.main)

The CLI entrypoints of vLLM.

Note that all future modules must be lazily loaded within main to avoid certain eager import breakage.


`vllm.entrypoints.cli.main`

The CLI entrypoints of vLLM.

Note that all future modules must be lazily loaded within main to avoid certain eager import breakage.