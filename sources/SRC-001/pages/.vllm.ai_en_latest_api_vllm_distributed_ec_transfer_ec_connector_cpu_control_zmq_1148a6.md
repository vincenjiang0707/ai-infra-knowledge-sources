source: https://docs.vllm.ai/en/latest/api/vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq/
lastmod: 2026-09-23

#

`vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq)

ZMQ control-plane implementations for ECCPUConnector.

ZmqClientConnection — one DEALER socket to a single producer peer. ZmqClientTransport — pool of ZmqClientConnection objects (consumer side). ZmqServerTransport — ROUTER socket (producer side).

Classes:

-
–[ZmqClientConnection](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientConnection)One DEALER socket to one producer peer.

-
–[ZmqClientTransport](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport)Pool of ZmqClientConnection objects — one per producer peer.

-
–[ZmqServerTransport](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqServerTransport)ROUTER socket for the producer side. Byte-level only, driven by


##

`ZmqClientConnection`

`dataclass`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientConnection)

Bases: [ControlConnection](https://docs.vllm.ai/base/#vllm.distributed.ec_transfer.ec_connector.cpu.control.base.ControlConnection)

One DEALER socket to one producer peer.

Owns the socket and its optional monitor socket. NIXL state is NOT stored here — it belongs in ECSession above this layer.

## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


##

`ZmqClientTransport`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport)

Pool of ZmqClientConnection objects — one per producer peer.

Creates DEALER sockets lazily on first contact. Does not touch NIXL; NIXL registration is handled by ECCPUConsumer once an XferAck arrives.

Methods:

-
–[connect](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.connect)Return the existing connection for addr, or create a new one.

-
–[poll](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.poll)Non-blocking drain of messages from every peer's DEALER.

-
–[poll_dead](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.poll_dead)Non-blocking sweep of peer monitors; return addresses that died.

-
–[remove](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.remove)Remove and close the connection for addr, returning it (or None).


## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


###

`connect(addr)`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.connect)

Return the existing connection for addr, or create a new one.

## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


###

`poll()`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.poll)

Non-blocking drain of messages from every peer's DEALER.

## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


###

`poll_dead()`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.poll_dead)

Non-blocking sweep of peer monitors; return addresses that died.

## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


###

`remove(addr)`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqClientTransport.remove)

Remove and close the connection for addr, returning it (or None).

## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


##

`ZmqServerTransport`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqServerTransport)

ROUTER socket for the producer side. Byte-level only, driven by ProducerSession.

poll(timeout_ms) waits up to timeout_ms for messages, then drains all that are available; timeout_ms=0 drains whatever has already arrived. send() routes a reply back to a specific peer by ZMQ identity.

Methods:

-
–[poll](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqServerTransport.poll)Wait up to timeout_ms for incoming messages, then drain all pending.

-
–[send](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqServerTransport.send)Send a reply payload to the peer identified by ZMQ identity.


## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


###

`poll(timeout_ms=0)`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqServerTransport.poll)

Wait up to timeout_ms for incoming messages, then drain all pending.

Returns a list of (identity, payload) pairs.

## Source code in `vllm/distributed/ec_transfer/ec_connector/cpu/control/zmq.py`


###

`send(identity, payload)`

[¶](https://docs.vllm.ai#vllm.distributed.ec_transfer.ec_connector.cpu.control.zmq.ZmqServerTransport.send)

Send a reply payload to the peer identified by ZMQ identity.