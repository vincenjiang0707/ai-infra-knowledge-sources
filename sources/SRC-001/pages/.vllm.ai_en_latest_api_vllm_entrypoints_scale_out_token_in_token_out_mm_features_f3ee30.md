source: https://docs.vllm.ai/en/latest/api/vllm/entrypoints/scale_out/token_in_token_out/mm_features/
lastmod: 2026-09-24

#

`vllm.entrypoints.scale_out.token_in_token_out.mm_features`

[¶](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features)

Helpers for render-time multimodal feature extraction and generate input.

Functions:

-
–[extract_mm_features](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.extract_mm_features)Extract multimodal features from a rendered engine prompt.

-
–[merge_mm_kwargs_items](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.merge_mm_kwargs_items)Merge full multimodal data with its metadata-only counterpart.

-
–[mm_kwargs_from_features](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.mm_kwargs_from_features)Deserialize

`features`

into per-modality kwargs for`mm_input`

. -
–[placeholder_ranges_from_engine_input](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.placeholder_ranges_from_engine_input)Return per-modality placeholder ranges, or

`None`

for text-only prompts.

##

`_encode_metadata_items(items, *, declared)`

[¶](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features._encode_metadata_items)

Serialize placeholder-metadata and `keep_on_cpu`

fields per item.

## Source code in `vllm/entrypoints/scale_out/token_in_token_out/mm_features.py`


##

`_encode_mm_kwargs_with_metadata(raw_mm_kwargs, *, metadata_fields_for=None)`

[¶](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features._encode_mm_kwargs_with_metadata)

Serialize full kwargs and their metadata-only subsets per modality.

## Source code in `vllm/entrypoints/scale_out/token_in_token_out/mm_features.py`


##

`extract_mm_features(engine_input, *, metadata_fields_for=None)`

[¶](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.extract_mm_features)

Extract multimodal features from a rendered engine prompt.

Returns `None`

for text-only prompts. `mm_metadata`

keeps the intersection of processed kwargs that prefill needs after EC transfer: fields declared as embedding metadata, plus fields marked `keep_on_cpu`

(for example M-RoPE grid dims).

## Source code in `vllm/entrypoints/scale_out/token_in_token_out/mm_features.py`


##

`merge_mm_kwargs_items(kwargs_item, metadata_item)`

[¶](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.merge_mm_kwargs_items)

Merge full multimodal data with its metadata-only counterpart.

## Source code in `vllm/entrypoints/scale_out/token_in_token_out/mm_features.py`


##

`mm_kwargs_from_features(features)`

[¶](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.mm_kwargs_from_features)

Deserialize `features`

into per-modality kwargs for `mm_input`

.

## Source code in `vllm/entrypoints/scale_out/token_in_token_out/mm_features.py`


##

`placeholder_ranges_from_engine_input(engine_input)`

[¶](https://docs.vllm.ai#vllm.entrypoints.scale_out.token_in_token_out.mm_features.placeholder_ranges_from_engine_input)

Return per-modality placeholder ranges, or `None`

for text-only prompts.