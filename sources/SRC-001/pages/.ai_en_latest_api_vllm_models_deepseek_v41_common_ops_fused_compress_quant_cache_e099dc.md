source: https://docs.vllm.ai/en/latest/api/vllm/models/deepseek_v41/common/ops/fused_compress_quant_cache/
lastmod: 2026-09-24

#

`vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache)

V4.1 state saving/compression and independently schedulable cache insertion.

Functions:

-
–[fused_save_compress_norm](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm)Pool each closed group into a normalized BF16 latent; save FP32 states.

-
–[rope_quant_insert](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.rope_quant_insert)Apply GPT-J RoPE and publish a latent to the compressed KV cache.


##

`_rope_quant_insert_mxfp8_kernel(latent, positions, cos_sin, cache, cache_slots, COS_STRIDE, CACHE_STRIDE, CACHE_BLOCK, COMPRESS_RATIO, SANITIZE_CACHE_NANS)`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache._rope_quant_insert_mxfp8_kernel)

V4.1 record: RoPE first, then MXFP8-quantize all 512 dims.

The RoPE dims are quantized here too, so unlike the V4 kernel the rotation has to happen before the scales are picked.

## Source code in `vllm/models/deepseek_v41/common/ops/fused_compress_quant_cache.py`


##

`_rope_quant_insert_nvfp4_kernel(latent, positions, cos_sin, cache, cache_slots, COS_STRIDE, CACHE_STRIDE, CACHE_BLOCK, COMPRESS_RATIO, SANITIZE_CACHE_NANS)`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache._rope_quant_insert_nvfp4_kernel)

V4.1 NVFP4 record: RoPE, then e2m1 with one e4m3 scale per 16 dims.

The scale is `amax / 6`

(6 is e2m1's largest magnitude) clamped to the e4m3 range, with no per-tensor scale on top.

## Source code in `vllm/models/deepseek_v41/common/ops/fused_compress_quant_cache.py`


##

`fused_save_compress_norm(kv_score, positions, state_cache, slot_mapping, query_start_loc, token_to_req_indices, rms_norm_weight, rms_norm_eps, compress_ratio, latent_out)`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm)

Pool each closed group into a normalized BF16 latent; save FP32 states.

The latent feeds the main-cache insert and the indexer K path, which the attention layer schedules on separate streams.

Ratio 2 keeps one ring block per request holding the open group's rows: position `p`

lives in row `p % capacity`

and `slot_mapping`

encodes `block * capacity + p % capacity`

. The grid has one program per request followed by one per pair of packed tokens. A request program handles the group that the chunk's first token closes with its predecessor's ring row, then stores the chunk's last `capacity`

rows to the ring; because the same program does both, ring reads and writes never race. A pair program handles the group that ends inside its pair, reading both rows from the raw input. Ratio 1 has no ring and one program per token; `slot_mapping`

then only marks valid tokens.

Parameters:

-

(`kv_score`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(kv_score))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)FP32 [tokens, 512] for CR1, [tokens, 1024] for CR2.

-

(`positions`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(positions))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)Absolute positions of the packed request tokens.

-

(`state_cache`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(state_cache))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)| NoneRing FP32 [blocks, capacity, 1024] KV/score states (CR2).

-

(`slot_mapping`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(slot_mapping))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)Ring slots (CR2) or main-cache slots (CR1).

-

(`query_start_loc`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(query_start_loc))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)| None[num_reqs + 1] token offsets of each request's chunk.

-

(`token_to_req_indices`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(token_to_req_indices))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)| NoneRequest indices for the packed token rows.

-

(`rms_norm_weight`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(rms_norm_weight))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)BF16 [512] normalization weight.

-

(`rms_norm_eps`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(rms_norm_eps))

) –[float](https://docs.python.org/3/builtins/functions.html#float)RMSNorm epsilon.

-

(`compress_ratio`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(compress_ratio))

) –[int](https://docs.python.org/3/builtins/functions.html#int)Group size, either 1 or 2.

-

(`latent_out`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.fused_save_compress_norm(latent_out))

) –[Tensor](https://pytorch.org/docs/stable/tensors.html#torch.Tensor)BF16 [tokens, 512], written only at valid group boundaries.


## Source code in `vllm/models/deepseek_v41/common/ops/fused_compress_quant_cache.py`


##

`rope_quant_insert(latent, positions, cos_sin_cache, kv_cache, slot_mapping, compress_ratio, fp8_scale=None)`

[¶](https://docs.vllm.ai#vllm.models.deepseek_v41.common.ops.fused_compress_quant_cache.rope_quant_insert)

Apply GPT-J RoPE and publish a latent to the compressed KV cache.

The BF16 latent supplies both NoPE quantization and RoPE input. It is read only for valid slots at group boundaries. The cache dtype selects the layout: `uint8`

is a paged FlashMLA layout, whose record the per-token byte width names -- 584 B for V4 (576 value bytes and eight segregated UE8M0 scale bytes, including one zero padding scale), 528 B for V4.1 (512 MXFP8 value bytes covering the RoPE dims too, then 16 UE8M0 scales of 32 dims each), or 288 B for V4.1 NVFP4 (256 bytes of e2m1 pairs then 32 e4m3 scales of 16 dims each), which only the compressed cache uses. `bfloat16`

and `float8_e4m3fn`

are the plain [448 NoPE | 64 RoPE] rows read by FlashInfer, the latter scaled by the per-tensor `fp8_scale`

.

## Source code in `vllm/models/deepseek_v41/common/ops/fused_compress_quant_cache.py`


|
|