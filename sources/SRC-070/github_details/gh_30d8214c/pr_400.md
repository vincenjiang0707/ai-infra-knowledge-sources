# [PR #400]     feat(native): add standalone CUDA core

source: https://github.com/deepseek-ai/DeepGEMM/pull/400
state: closed | updated: 2026-08-07T07:26:58Z
labels: 

## 正文

    
    Edge deployment needs to link a runtime library without loading the Python wheel frontend, PyTorch, ATen, or pybind symbols.
    
    - split CMake configuration so the Python extension and native core build independently
    - expose a stable C ABI over opaque CUDA streams and row-major tensor views
    - implement the NN GEMM path with CUDA Runtime and cuBLASLt only
    - package the shared library and public header inside the wheel for build-time discovery and artifact copying
    - retain the existing Python extension path as the default build

## 评论 (1)

### ds-review-bot · 2026-08-07

## 🤖 ds-review-bot Code Review

### v6
半精度路径使用了 cuBLASLt 不支持的计算类型，且共享 workspace 在多流调用时存在异步复用竞态。这会使主要数据类型直接失败，并使并发调用产生错误结果。

### v4f
Review of 'feat(native): add standalone CUDA core' (4 files changed: CMakeLists.txt, csrc/native_api.h, csrc/native_api.cpp, setup.py). The implementation fully satisfies the requirements: (1) CMake is split so the Python/pybind extension (DG_BUILD_PYTHON, default ON) and the standalone native core (DG_BUILD_NATIVE_CORE, default OFF) build independently - pybind11/Torch find_package, Torch include/link dirs, pybind11_add_module(_C) and the deep_gemm_indexing_cuda IDE target are all guarded by DG_BUILD_PYTHON; (2) csrc/native_api.h exposes a stable C ABI over opaque CUDA streams (void*) and row-major tensor views (data/rows/cols/row_stride/dtype, int64_t fields, extern "C"), plus deep_gemm_native_last_error() diagnostics; (3) csrc/native_api.cpp includes only cublasLt.h and cuda_runtime_api.h and links only CUDA::cudart/CUDA::cublas/CUDA::cublasLt - no PyTorch/ATen/pybind/Python symbols; the NN GEMM math was verified: row-major A[M,K], B[N,K], D[M,N] map to column-major layouts (k,n,ldb), (k,m,lda), (n,m,ldd) with transA=OP_T, transB=OP_N, computing B_col^T @ A_col = (N,K)@(K,M) -&gt; (N,M) = A @ B^T, which is exactly D[m,n] = sum_k A[m,k]*B[n,k]; alpha=1/beta=accumulate correctly implements the accumulate flag; errors are converted to a non-zero return plus a thread-local diagnostic string; per-device cuBLASLt handles and a 32 MiB workspace are managed under a mutex with cleanup on destruction; (4) setup.py builds the native core via a separate cmake tree (DG_BUILD_PYTHON=OFF, DG_BUILD_NATIVE_CORE=ON) installed into build_lib/deep_gemm/lib and deep_gemm/lib/include, and package_data entries 'lib/libdeep_gemm_native.so' and 'lib/include/native_api.h' exactly match those cmake install destinations, so the shared library and public header ship inside the wheel; (5) the Python extension remains the default build path and is untouched. The header and implementation signatures match exactly. No blocking issues found; only the non-blocking suggestions below.

### v5
评审结论：请求修改。通过项：1) CMake 拆分正确——DG_BUILD_PYTHON / DG_BUILD_NATIVE_CORE 相互独立，native 目标仅链接 CUDA::cudart / CUDA::cublas / CUDA::cublasLt，无 Torch/ATen/pybind 依赖，默认仍构建 Python 扩展；2) setup.py 在 build_py 阶段以 -DDG_BUILD_PYTHON=OFF -DDG_BUILD_NATIVE_CORE=ON 构建 native core 并安装到 build_lib/deep_gemm/lib，package_data 同时打包 libdeep_gemm_native.so 与 lib/include/native_api.h，符合 wheel 打包要求；3) cuBLASLt 参数（workspace/heuristic/reduction mask/CUBLAS_COMPUTE_32F_FAST_TF32）与仓库参考实现 csrc/jit_kernels/impls/smxx_cublaslt.hpp 一致。阻塞问题：新增的 deep_gemm_native_cublaslt_gemm_nn 名为 NN GEMM（PR 描述亦称 'implement the NN GEMM path'），但头文件注释、形状校验与 cuBLASLt 布局/转置全部按 NT 语义实现（D = A[M,K] @ B[N,K]^T）。按仓库既有约定（csrc/apis/gemm.hpp：cublaslt_gemm_nn(a, b, ...) == cublaslt_gemm_nt(a, b.transpose(0,1), ...)），NN 应计算 D[M,N] = A[M,K] @ B[K,N]。后果：按 NN 契约传入 B[K,N] 时，非方阵（K != N）被形状校验拒绝，方阵（K == N）则静默算出 A @ B^T 的错误结果。

**Files reviewed**: 4
**Issues found**: 🔴 4 critical | 🟡 2 warning | 🔵 5 suggestion
**Inline comments posted**: 11

## Review (1)

### ds-review-bot · 2026-08-07 · COMMENTED

(no text)
