# [PR #432] Public Release 26/09

source: https://github.com/deepseek-ai/DeepGEMM/pull/432
state: closed | updated: 2026-09-10T06:31:04Z
labels: 

## 正文

This update introduces new kernels, features, and performance improvements:

### New kernels
  - Sparse Indexer: hierarchical sparse indexing for V4.1, with throughput close to the Lightning Indexer.
  - Mega Gate: fuses the routing GEMM and multiple MoE routing operations into one kernel, with 30–75% speedups.
  - Mega mHC: fuses multiple Hyper-Connection operations and RMSNorm into one kernel, with 45–85% speedups.

### New features
  - Integration with [DeepJIT](https://github.com/deepseek-ai/DeepJIT), a lightweight, header-only JIT runtime.
  - More epilogue fusion options, including runtime alpha scaling and fused output casting.
  - Broader use of cuBLASLt for simpler GEMM and batched operations.

### Performance improvements
  - Faster MoE backward GEMMs, with ~20% gains.
  - Mega MoE optimizations for V4.1: ~15% faster at low latency, ~4% at high throughput.
  - Improved Lightning Indexer kernels, with a follow-up version (~30% faster) coming soon.

### Breaking changes
  - Previously, all kernels were fully deterministic and batch-invariant.
  - This release adds a `use_deterministic_algorithms()` API, which defaults to `False` so some kernels can run faster.
  - Set it to `True` to restore deterministic, batch-invariant behavior for RL and similar workloads.

### Contributors

- DeepJIT Integration: @LyricZhao 
- Sparse Indexer, Mega mHC: @zheanxu 
- Mega Gate: @huahuaquq
- GEMM and MoE Optimizations, Epilogue Fusion Options: @xyf007
- Mega MoE optimizations for V4.1: @zheanxu 
- Lightning Indexer Improvements: @zheanxu @leavelet


## 评论 (1)

### ds-review-bot · 2026-09-10

## 🤖 ds-review-bot Code Review  
  
### v6
该补丁引入了一个公开配置 API 的兼容性回退，并且新的稀疏注意力元数据接口可能将主机或跨设备指针传入 CUDA kernel。

### v5
Public Release 26/09（v2.6.1 → v2.8.0，115+ 文件，约 +12k/−5k 行）。主要内容：1) 新增 Mega MHC 融合大核（HC prenorm GEMM + Sinkhorn + RMSNorm + FP8 量化输出，csrc/apis/mega_mhc.hpp 及对应 impls/epilogue/layout/scheduler）；2) 新增 Mega Gate 融合 MoE 门控（BF16 gate GEMM + top-k，支持 sigmoid/sqrtsoftplus/identity）；3) 新增 Sparse MQA Logits（含 paged 版本）及其 metadata kernel；4) Mega MoE 新增 FP8×FP8 路由专家权重支持（mma_type 参数）；5) 内置 JIT 模块整体迁移到新的 DeepJIT 子模块（third-party/deep_jit），移除 fmt 依赖并改用 C++20 &lt;format&gt;，环境变量语义相应调整（DG_JIT_* 回退 DJ_JIT_*、多路径缓存、拆分 PTXAS 检查开关）；6) 构建门槛提升（CUDA ≥ 12.9、PyTorch ≥ 2.3、C++20、CMake ≥ 3.18）；7) 新增 469 行 SF 格式规范文档（docs/scaling-factor-format.md，内容详尽并列出 source of truth 文件）与大量新测试（test_mega_gate/test_mega_mhc 等），可验证性明显提升。已核对：README 中环境变量说明与 DeepJIT 实际行为一致（默认缓存目录 $HOME/.dj、多路径查找、DG_JIT_CHECK_NO_SPILLS/NO_LOCAL_MEMORY 拆分）；config.hpp 的 get_pdl 解引用 optional 当前安全（DeepJIT 默认初始化为 false）。需要作者跟进的问题见 comments：use_fp8_dispatch 向后兼容处理与注释不符且会使旧调用断言失败；两处以 static/成员 map 持有 CUDA tensor 的全局状态存在线程安全与进程退出时 CUDA 已卸载的析构风险（与本次为 cuBLASLt workspace 加的 DG_USE_TEMP_CUBLASLT_WORKSPACE 规避的问题同源）；~Runtime() noexcept(false) 可能在静态析构期抛出导致 terminate；SymmBuffer.destroy() 清理不完整；wheel 打包整个 csrc/docs 的体积问题等。均不阻塞发布。

### v4f
本次 Public Release 26/09（约 115 个变更文件，+12k/-5k 行）是一次较大的架构与功能更新。核心改动是把运行时 JIT 完整迁移到新的 DeepJIT 子模块：删除内置的 csrc/jit/*（cache/compiler/device_runtime/handle/include_parser/kernel_runtime）与 csrc/utils/{hash,lazy_init,system} 等，移除 NVRTC 与 third-party/fmt 依赖，改用 std::format，C++ 标准提升到 C++20，最低要求提升到 CUDA 12.9 / PyTorch 2.3，并把 csrc/docs 打进 wheel 以便报错定位。功能层面新增了 Sparse MQA logits（含 metadata 构造）、MegaGate（BF16 gate GEMM 融合 MoE top-k 路由）、MegaMHC（mHC + Sinkhorn + RMSNorm + 可选 FP8 量化）、FP8×FP8 Mega MoE、k_grouped_fp4_gemm_nt_contiguous、cuBLASLt NVFP4 GEMM 与 batched_syrk/symm，新增 use_deterministic_algorithms / psum-layout 支持，并统一了 fp8/fp4 GEMM 的命名别名。配套新增了 docs/scaling-factor-format.md 与 test_mega_gate.py / test_mega_mhc.py。整体架构方向清晰、重构合理，多数新内核只在 SM100 上可用并有较密的 host 端校验；本次审查未在已阅读的范围内发现明确的数值/逻辑错误，但缓冲区生命周期与复用、JIT/运行时资源管理、以及若干断言与文档一致性上存在可改进点，详见各条 comment。  
  
**Files reviewed**: 115  
**Issues found**: 🔴 1 critical | 🟡 9 warning | 🔵 11 suggestion  
**Inline comments posted**: 21

## Review (1)

### ds-review-bot · 2026-09-10 · COMMENTED

(no text)
