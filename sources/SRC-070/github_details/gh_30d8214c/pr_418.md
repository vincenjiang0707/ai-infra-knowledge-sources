# [PR #418] feat: add compact M-grouped GEMM execution

source: https://github.com/deepseek-ai/DeepGEMM/pull/418
state: closed | updated: 2026-08-25T10:21:35Z
labels: 

## 正文

(empty)

## 评论 (1)

### ds-review-bot · 2026-08-25

## 🤖 ds-review-bot Code Review  
  
### v6
新增紧凑 GEMM 在合法的带步长张量上会静默访问错误地址，并且公开的 workspace 参数缺少 TensorMap 必需的地址对齐检查。

### v5
该 MR 为 Hopper FP8 1D2D 内核新增 compact M-grouped GEMM 执行路径：新增 GemmType::MGroupedCompact，API 层新增 m_grouped_fp8_fp4_gemm_nt_compact（grouped_layout 为 [G,2] 的 [start_m, valid_m]），内核侧通过在 shared memory 中用 tensormap.replace PTX 动态改写 A/SFA/D 的 TensorMap（global address 与 M 维大小）并写回每 CTA 私有的 gmem descriptor 缓冲区，实现无 BLOCK_M padding 的分组执行；scheduler 新增按组累计的 block 分配逻辑；附带 benchmark/正确性脚本与 README 说明。整体设计合理，descriptor 的 release/acquire 栅栏与 D 路径的 tma_store_wait&lt;0&gt; 处理了大部分一致性问题，但存在几处需要确认的正确性/健壮性问题（TMA 加载路径改写描述符前未等待在途 load、workspace 对齐假设、arch guard 缺失、magic number 与重复代码等）。

### v4p
该 MR 新增 Hopper 下 compact M-grouped FP8 GEMM 执行路径：通过 grouped_layout [G,2] 描述每个 expert 的 compact M 段，并在 kernel 内动态 rebase A/SFA/D 的 TensorMap，避免 BLOCK_M padding。整体实现思路清晰，调度器和 host 配置基本配套；但 D TensorMap 更新存在多线程 store 竞态，可能静默写坏输出，需修复后合入。  
  
**Files reviewed**: 11  
**Issues found**: 🔴 2 critical | 🟡 5 warning | 🔵 7 suggestion  
**Inline comments posted**: 14

## Review (1)

### ds-review-bot · 2026-08-25 · COMMENTED

(no text)
