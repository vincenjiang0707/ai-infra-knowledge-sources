# [PR #396] feat: add SiTU support to FP8/FP4 Mega MoE

source: https://github.com/deepseek-ai/DeepGEMM/pull/396
state: closed | updated: 2026-08-11T08:21:16Z
labels: 

## 正文

## Summary

- Add SiTU gated activation support to SM100 FP8/FP4 Mega MoE while preserving the default SwiGLU path.
- Thread `activation`, `situ_beta`, and `situ_linear_beta` through symmetric-buffer validation, Python/C++ APIs, JIT specialization, and the routed/shared L1 epilogue.
- Restrict SiTU to FP8xFP4, reject activation clamp and non-positive or non-finite beta values, and keep BF16 Mega MoE SwiGLU-only.

This is a semantic port of [longlee0622/DeepGEMM@cc832217](https://github.com/longlee0622/DeepGEMM/commit/cc832217b65bbe693a9eaa64f1077f65daaee67c) onto the latest `nv_dev`. It uses the author refactor-ready follow-up [39d7cac](https://github.com/longlee0622/DeepGEMM/commit/39d7cac2170b12768a324d5013eeb2a4c81c82fa) and preserves the original authorship and sign-off.

## Validation

- Built the exact PR commit with `./develop.sh`.
- Passed the focused deterministic routed SiTU regression for both `fast_math` modes.
- Verified high-beta SiTU converges to SwiGLU, each beta control changes the result independently, the two controls remain distinct, and every output is finite with a non-degenerate baseline.
- Verified `situ_beta=+inf` is rejected by the API validation.
- PTXAS comparison reported the same 56-byte stack frame and zero spill stores/loads for SiTU and SwiGLU.
- `git diff --check` and Python `py_compile` passed.

## 评论 (2)

### ds-review-bot · 2026-07-30

## 🤖 ds-review-bot Code Review

### v6
未发现会破坏现有功能或导致 SiTU 路径错误的实质性问题。

### v4
⚠️ 未完成评审（no_result_file：模型未产出结果文件）

### v5
本 MR（commit 7f17040，cherry-pick 自 39d7cac，保留原作者署名与 sign-off）为 SM100 FP8xFP4 Mega MoE 增加 SiTU 门控激活支持，同时保留默认 SwiGLU 路径。审查结论：实现完整、正确，建议合并。已逐项核实：(1) `activation`/`situ_beta`/`situ_linear_beta` 完整贯穿对称缓冲区校验（csrc/apis/mega.hpp:45 放宽为 swiglu 或 fp8xfp4+situ）、Python API、C++ API、JIT 特化与 kernel 的统一 L1 epilogue——routed 与 shared 共用同一 epilogue（cuh:999 'Unified L1 epilogue'），单处修改即覆盖两条路径；(2) SiTU 正确限制在 FP8xFP4：SymmBuffer/transform_weights 的 Python 断言与 host 端 DG_HOST_ASSERT 双层防护，BF16 API（mega.hpp:318）保持 SwiGLU-only，bf16_mega_moe 未新增 SiTU 参数；(3) host 端校验完备：SiTU 与 activation_clamp 互斥，`std::isfinite(beta) &amp;&amp; beta &gt; 0` 同时拒绝 +inf/NaN/0/负值（与 PR 中 situ_beta=+inf 被拒绝的验证一致），非 SiTU 时禁止传入 beta，kernel 内 DG_STATIC_ASSERT（cuh:90）提供编译期兜底且 SwiGLU 默认 beta=0 可经短路通过；(4) JIT fmt 模板 26 个 `{}` 占位符与 kernel 26 个非默认模板参数逐一对应、顺序正确，Python 位置参数与 pybind 签名一致，不同 beta 生成不同代码文本因而 JIT 缓存行为正确；(5) 默认 SwiGLU 路径与改动前逐语句等价（仅 raw_gate/gate 重命名与 up 提前声明），无行为回归；(6) 测试脚本正确处理 BF16 断言、SiTU 下 clamp 置 None、beta 仅在非 BF16 分支传入、legacy TileLang baseline 在 SiTU 下跳过；(7) git diff --check 干净，工作区无未提交改动。下述评论均为非阻塞性小建议（suggestion），不影响合并。

**Files reviewed**: 5
**Issues found**: 🔵 4 suggestion
**Inline comments posted**: 4

⚠️ *Parse warning: [v4] no_result_file：模型未产出结果文件*

### ds-review-bot · 2026-08-05

## 🤖 ds-review-bot Code Review

### v6
SiTU 主路径基本完整，但合法的极小正 beta 会因倒数溢出而产生 NaN，违反当前 API 接受所有有限正 beta 的校验语义。

### v4f
The MR adds SiTU (Sigmoid-Tanh-Unit) gated activation support to the SM100 FP8xFP4 Mega MoE while preserving SwiGLU as the default. Verified against HEAD be4faf9 (feat 7f17040 + perf refinement), the combined diff f8e8fb5..be4faf9 touches exactly 5 files, matching the PR description. The change is complete and internally consistent: activation/situ_beta/situ_linear_beta are threaded through symmetric-buffer validation (mega.hpp, __init__.py), the Python/C++ APIs (pybind positional arg order matches the C++ signature 1:1), JIT specialization (26 template placeholders align with 26 fmt args in the correct order activation_clamp -&gt; fast_math -&gt; use_situ -&gt; situ_beta -&gt; situ_linear_beta), and the single unified routed/shared L1 epilogue (if constexpr (kUseSiTU) branch computing sigmoid(gate) * beta*tanh(gate/beta) * beta_lin*tanh(up/beta_lin), which correctly degenerates to SwiGLU as beta -&gt; inf). Restrictions are enforced as described: SiTU is limited to FP8xFP4 (Python asserts in SymmBuffer/get_symm_buffer_for_mega_moe; bf16_mega_moe remains SwiGLU-only), activation clamp is rejected for SiTU, and non-positive/non-finite betas are rejected via std::isfinite &amp;&amp; &gt; 0 (so situ_beta=+inf fails as claimed), with a kernel-level DG_STATIC_ASSERT backstop. All prior discussion-history items are resolved: (1) the constexpr reciprocal optimization (kInvSiTUBeta/kInvSiTULinearBeta with __fmul2_rn replacing per-element divides) landed in the perf-refinement commit be4faf9; (2) the transform_weights_for_mega_moe assertion message now reads '`situ` requires FP8xFP4 `(weight, sf)` tuples for both L1 and L2 weights', making the tuple requirement self-documenting; (3) the float-&gt;JIT-string round trip is lossless because the shared to_string(float) in csrc/jit_kernels/impls/runtime_utils.hpp uses hexfloat fmt::format("{:a}f"), so close beta values cannot collide in the JIT cache, with no new risk beyond the pre-existing kActivationClamp pattern. git diff --check passes and the working tree is clean. Runtime claims (develop.sh build, deterministic GPU regression for both fast_math modes, PTXAS stack-frame parity) could not be re-executed in this sandbox (bash restricted to git, no CUDA device), but the committed tests/test_mega_moe_situ.py implements exactly the described metamorphic regression (seeded 20260730, finiteness, high-beta convergence to SwiGLU, and independence of the two beta controls). Approve; only one non-blocking suggestion remains.

### v5
The MR cleanly adds SiTU gated activation to SM100 FP8xFP4 Mega MoE while preserving the default SwiGLU path; no correctness issues were found. Verified: (1) SiTU is restricted to FP8xFP4 — get_symm_buffer_size_for_mega_moe (csrc/apis/mega.hpp:45) requires mma_type == 'fp8xfp4' for 'situ', bf16_mega_moe (csrc/apis/mega.hpp:318) still asserts activation == 'swiglu', and sm100_bf16_mega_moe.cuh is untouched, so BF16 Mega MoE stays SwiGLU-only. (2) Beta validation (csrc/apis/mega.hpp:187-199) rejects activation_clamp together with situ, requires both betas to be provided, finite (std::isfinite rejects +inf and NaN) and strictly positive, and rejects betas without activation='situ'. (3) JIT specialization is consistent: 26 fmt placeholders match 26 arguments and the tail order (kNumSMs, kNumRanks, kActivationClamp, kFastMath, kUseSiTU, kSiTUBeta, kSiTULinearBeta) exactly matches the kernel template parameter list; float non-type template params follow the existing kActivationClamp precedent. (4) Kernel math (sm100_fp8_fp4_mega_moe.cuh:1062-1092) computes sigmoid(gate) * beta*tanh(gate/beta) * linear_beta*tanh(up/linear_beta), which converges to SwiGLU as beta -&gt; inf; DG_STATIC_ASSERT guards non-positive betas and the SwiGLU branch is unchanged. (5) Python/pybind positional argument order is aligned end-to-end. (6) The new deterministic metamorphic test covers both fast_math modes and skips correctly on non-SM100 devices. Prior review comments: the constexpr-division comment is resolved in be4faf9 (kInvSiTUBeta/kInvSiTULinearBeta + __fmul2_rn); the to_string(float) round-trip concern is confirmed safe (runtime_utils.hpp:65 uses lossless hexfloat '{:a}f', so distinct betas always produce distinct JIT cache entries); the tuple-assertion-message comment is resolved (message now names the FP8xFP4 (weight, sf) tuple requirement). Risk: default-path regression none (SwiGLU/BF16 paths and existing signatures unchanged, new params default to None); PTXAS shows no perf regression (same 56B stack frame, zero spills vs SwiGLU).

**Files reviewed**: 5
**Issues found**: 🟡 1 warning | 🔵 4 suggestion
**Inline comments posted**: 5

## Review (2)

### ds-review-bot · 2026-07-30 · COMMENTED

(no text)

### ds-review-bot · 2026-08-05 · COMMENTED

(no text)
