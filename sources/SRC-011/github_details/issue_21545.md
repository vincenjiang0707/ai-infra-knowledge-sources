# [Issue #21545] server: Add model capabilities information (instruction, embedding etc.)

source: https://github.com/ggml-org/llama.cpp/issues/21545
state: open | updated: 2026-09-22T01:12:46Z
labels: server/webui

## 正文

### Name and Version

version: 8685 (0988accf8)
built with GNU 13.3.0 for Linux aarch64

### Operating systems

_No response_

### Which llama.cpp modules do you know to be affected?

llama-server

### Command line

```shell

```

### Problem description & steps to reproduce

<img width="435" height="676" alt="Image" src="https://github.com/user-attachments/assets/8665a940-6f6d-4695-9ca5-a197629f07ad" />

Embedding models show up in the model selection, in this case it's even the default, but the model does not work for chat. It should be hidden or shown differently.

@ggml-org/llama-webui

### First Bad Commit

_No response_

### Relevant log output

_No response_

## 评论 (12)

### allozaur · 2026-04-07

Okay, so a webui-only quick dirty fix would be to filter out the models that include `embedding` in their names, but i am not sure if it is not a bit too dirty and unreliable... @ggerganov i am not aware of any special metadata for a model to determine if it's an embedding or a completion model, so not sure if there's any quick win that we can do in this situation on the server side..?

cc @ngxson @ServeurpersoCom 

### ServeurpersoCom · 2026-04-07

Or an explicit tag to hide the models of your choice from presets.ini, like "hidden"?

### allozaur · 2026-04-07

Well, for `presets.ini` i guess that it'll be enough to just add the `--embedding` flag to the model and we will be able to read it from `/models` data.

For now i am just pushing the fix that relies on heuristic checks, but i think we should revisit this and properly handle categorizing model types on the server side using metadata etc.

### 0cc4m · 2026-04-07

Usually if you do that the temporary fix sticks and the proper implementation never happens.

### ServeurpersoCom · 2026-04-07

To be fair, this heuristic is pretty lightweight and might actually be good enough long term. The CLI flag check (--embedding) is reliable, and the name regex is just a safety net. Worst case a false negative means an embedding model shows up in chat, which is the current behavior anyway. A full server-side solution reading GGUF general.architecture would be cleaner (there's a TODO for it at line 1015 of server-models.cpp), but this covers the real-world cases with minimal code.

### ggerganov · 2026-04-08

Both the name regex and the CLI checks seems very hacky. Let's fix this on the server side. Though it might take a while as I am not sure what would be the best way to recognize the model capabilities.

### allozaur · 2026-04-08

> Both the name regex and the CLI checks seems very hacky. Let's fix this on the server side. Though it might take a while as I am not sure what would be the best way to recognize the model capabilities.

Gotcha, closed the webui PR and re-assigned this to @ngxson

### ServeurpersoCom · 2026-04-08

Being able to manually hide models from the WebUI selector while keeping plenty of models usable via API, without relying on heuristics would be a great feature too :)

### github-actions[bot] · 2026-05-24

This issue was closed because it has been inactive for 14 days since being marked as stale.

### tonificante · 2026-06-20

> Or an explicit tag to hide the models of your choice from presets.ini, like "hidden"?

I think this solution could be great, so we can manually hide all huggingface models with their "standard" names and use custom entries with fully customised names, without having the model selectbox full of other model names which you don't want to have (or even use, like the embeddings, fast apply models, fim models, etc).

Hopefully this feature will be included some day, I always get the latest llamacpp release just to check if there are news on this.

### github-actions[bot] · 2026-08-04

This issue was closed because it has been inactive for 14 days since being marked as stale.

### github-actions[bot] · 2026-09-19

This issue was closed because it has been inactive for 14 days since being marked as stale.
