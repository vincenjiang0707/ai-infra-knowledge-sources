# [PR #5328] fix(training_engine): prevent 397B every-step checkpointing + Raiden weight-sync OOM (mlperf)

source: https://github.com/AI-Hypercomputer/maxtext/pull/5328
state: closed | updated: 2026-09-23T02:13:53Z
labels: 

## 正文

## Summary
Fixes CPU RAM OOM (`exitCode: 137`) during 397B Orbax/Pathways checkpointing and weight synchronization while keeping checkpoint persistence asynchronous (`async_checkpointing=True`).

### Changes
1. **Pathways Persistence `gs://` Guard (`src/maxtext/training_engine/checkpointing.py`)**:
   - Validate in `CheckpointManager.__init__` (before `_maybe_register_pathways_persistence()`) that `checkpoint_dir` starts with `gs://` whenever `ENABLE_PATHWAYS_PERSISTENCE=1`, so every `CheckpointManager` instantiation validates the path even after `_PATHWAYS_PERSISTENCE_REGISTERED` is set.
2. **Purge Staged Sync Buffers Without Blocking on Async Checkpointing (`src/maxtext/training_engine/maxtext_engine.py`)**:
   - Add `_purge_raiden_buffers()` (`self._raiden_sync.release_buffers()` + `gc.collect()`) called before `convert()` in `prepare_weight_sync()` and after `metrics()` in `release_weight_sync()`, without calling `_checkpoint_manager.wait_until_finished()` so async GCS checkpoint uploads overlap concurrently with weight conversion and transfer.
3. **Unit Tests**:
   - `tests/unit/checkpointing_test.py`
   - `tests/post_training/unit/prepare_weight_sync_test.py`

## 评论 (0)

## Review (2)

### gemini-code-assist[bot] · 2026-09-22 · COMMENTED

## Code Review

This pull request introduces several enhancements to the training engine, including a new configuration option 'save_optimizer_state' to control whether optimizer states are saved in checkpoints. It also adds a safety guard to ensure that 'checkpoint_dir' is a 'gs://' URI when 'ENABLE_PATHWAYS_PERSISTENCE' is enabled. Furthermore, the PR implements a buffer purging mechanism in 'maxtext_engine.py' to release memory by calling 'gc.collect()' after weight synchronization. The reviewer suggests a refinement to the '_purge_raiden_buffers' method to safely handle potential 'None' return values from 'release_buffers', ensuring that garbage collection is triggered as intended and that the function strictly adheres to its integer return type annotation.

### khatwanimohit · 2026-09-23 · APPROVED

(no text)
