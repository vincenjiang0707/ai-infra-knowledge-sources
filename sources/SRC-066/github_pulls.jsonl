{"number": 3677, "title": "Fix ArraySubbyte::back() and PredicateVector::is_zero()", "state": "open", "created_at": "2026-09-25T10:26:18Z", "updated_at": "2026-09-25T10:26:18Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### Background\nTwo host-side correctness bugs in the CUTLASS core headers, both latent because no in-tree test exercises the affected paths.\n\n### Fixes\nFixes #3512, fixes #3515\n\n### Change Summary\n\n**\\include/cutlass/array_subbyte.h\\**\n- \\\back()\\ returned the highest sub-element of the last storage slot, which is padding when \\N\\ does not fill its final storage unit. It now returns \\^Gt(kElements - 1)\\, matching \\\front()\\.\n- \\const_iterator\\'s \\++\\/\\--\\ declared \\iterator&\\ return types and its comparisons took \\iterator const&\\, so iterating a const array could not compile. These now use \\const_iterator\\ self-types.\n\n**\\include/cutlass/predicate_vector.h\\**\n- \\computeWordMask()\\/\\computeLastWordMask()\\ were declared to return \\\bool\\, truncating the \\Storage\\ mask to 0/1.\n- \\computeLastWordMask()\\ looped \\kBytes % sizeof(Storage)\\ times, which is zero when the last word is fully occupied, so bits in the final word were excluded from \\is_zero()\\. It now masks the full last word when it is fully occupied.\n- \\Iterator::operator-\\ constructed a \\ConstIterator\\ but declared an \\Iterator\\ return type; fixed to construct an \\Iterator\\.\n\n### Additional Notes\n- The \\computeLastWordMask\\ regression was introduced in CUTLASS 3.3.0 (commit \\c008b4ae\\), which dropped the \\count = (kBytes % sizeof(Storage) == 0) ? sizeof(Storage) : (kBytes % sizeof(Storage))\\ guard that had been present since the original \\is_zero\\ fix (#1147).\n\n### Testing Procedure\n- Added \\TEST(Array, SubbyteBack)\\ covering \\int4b_t\\ (N=5) and \\\bin1_t\\ (N=33), both of which leave padding in the final storage unit.\n- Added \\TEST(PredicateVector, IsZero)\\ covering \\PredicateVector<32>\\ (fully-occupied last word), \\<16>\\ (single full word), and \\<12>\\ (partially-occupied last word).\n- These are host-side tests; no GPU is required to exercise the fixed paths.\n\n### Related PRs or Issues\n- #3512, #3515", "url": "https://github.com/NVIDIA/cutlass/pull/3677"}
{"number": 3666, "title": "[CuTeDSL] docs: fix cutlass.range unroll=0 and unroll_full descriptions", "state": "open", "created_at": "2026-09-24T03:44:29Z", "updated_at": "2026-09-25T02:28:24Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "The `cutlass.range` docstring says `unroll` \"0 or 1 = no unrolling\", and the control-flow notebook says `unroll=0`/`1` is the plain staged loop. Only `1` disables unrolling: `LoopUnroll` sets `disable = true` only for `count == 1`, so `unroll=0` becomes `#llvm.loop_unroll<count = 0>` and the compiler picks the factor, as it does when `unroll` is left out. For a global-load loop with a runtime bound on sm_90a, `unroll=0` and no `unroll` give the same PTX apart from symbol names and the printf text (an x16 main loop plus leftover blocks), while `unroll=1` gives one rolled loop.\n\nThe same notebook sentence says `unroll_full=True` \"would flatten a dynamic loop completely\". For the same probe with a runtime `N`, it produces the same PTX as leaving `unroll` out. It only flattens when the trip count is a compile-time constant: `cutlass.range(6, unroll_full=True)` gives six straight-line loads.\n\nIt also says the loop \"runs `N/K` times\". With `unroll=3` or `unroll=4` and `N` not a multiple of `K`, the PTX has an `N // K`-trip main loop followed by a remainder loop for the last `N % K` iterations, so I spelled that out.\n\nChecked with nvidia-cutlass-dsl 4.8.0 (CUDA 13.4) by dumping MLIR and PTX with `CUTE_DSL_KEEP=ir,ptx`.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3666"}
{"number": 3546, "title": "Fix profiler defects (Resolves #3544)", "state": "open", "created_at": "2026-08-25T19:18:44Z", "updated_at": "2026-09-24T20:15:52Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3544.\r\nKnocked out those three minor profiler bugs mentioned in the issue:\r\n1. **Device ID validation:** Fixed the off-by-one bug in `options.cu` so it properly errors out on `device >= num_devices` instead of letting it slip through and crashing later.\r\n2. **NCHW layout typo:** In `device_allocation.cu`, `kTensorNCHW` was accidentally returning `TensorNHWC` due to a copy-paste error. Swapped it out to use the correct `TensorNCHW` layout.\r\n3. **Sparse-meta type mismatches:** Tweaked the host and device `BlockFillRandomSparseMeta` initialization functions so they both handle signed (`kS16`, `kS32`) and unsigned (`kU16`, `kU32`) type IDs. This stops the host initialization from silently falling through to all-zeros when the types don't line up.", "url": "https://github.com/NVIDIA/cutlass/pull/3546"}
{"number": 3547, "title": "Fix validation gaps (Resolves #3542)", "state": "open", "created_at": "2026-08-25T19:26:52Z", "updated_at": "2026-09-24T20:15:50Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3542.\r\n\r\nThis PR addresses the four validation gaps outlined in the issue:\r\n\r\n1. **Grouped GEMM Validation (`gemm_grouped.h`)**:\r\n   Added a check in `can_implement` to return `kErrorInvalidProblem` if `host_problem_sizes` is null and `kRequiresPrecomputation` is true. This prevents host segfaults during `precompute`.\r\n\r\n2. **Split-K Parallel Zero Division (`gemm_splitk_parallel.h`)**:\r\n   Added a check in `can_implement` to properly return `kErrorInvalidProblem` if `split_k_slices == 0`, preventing a divide-by-zero fault.\r\n\r\n3. **Batch Grid Dimension Wrapping (`threadblock_swizzle.h`)**:\r\n   Clamped the batched grid `z` dimension to `65535` instead of wrapping it via modulo. This stops multiples of 65536 from producing a 0-dimension launch.\r\n\r\n4. **Sm80 EVT assert-only validation (`gemm_universal_with_visitor.h`)**:\r\n   Replaced the debug-only `assert()` statements with a proper `can_implement` validation pass that safely returns `kErrorNotSupported` for Split-K Parallel, Split-K Serial, and Array GEMM modes, preventing silent garbage execution in release builds.\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3547"}
{"number": 3676, "title": "[CuTeDSL] Convert gmem TMA descriptor pointers to generic in tma_desc_ptr", "state": "open", "created_at": "2026-09-24T20:09:37Z", "updated_at": "2026-09-24T20:09:37Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "**Fixes #3671.** Verified on hardware: the reported libNVVM failure reproduces before this change and is gone after.\n\n> Stacked on #3675 (which adds the `_to_tma_desc_ptr` helper) — only the second commit is new here. I'll rebase once #3675 lands.\n\n## Cause\n\n`TensorMapManager.get_tensormap_ptr()` defaults to a gmem-space `!cute.ptr<tma_descriptor_tiled, gmem, align<128>>` — also the form `copy_tensormap` needs for a global-memory workspace. Passed as `tma_desc_ptr=`, it got past the type check, was lowered as `ptr addrspace(1)`, and was handed to `llvm.nvvm.cp.async.bulk.tensor.*`, whose descriptor operand is a generic `ptr`. libNVVM then failed with `Call parameter type does not match function signature!` and a suggestion pointing at `CUDA_TOOLKIT_PATH`. A shared-memory pointer failed later still, with an internal IR verification error.\n\n## Fix\n\nThe TMA instructions take the descriptor as a generic address, and the generic address of a global location equals its global address. `_to_tma_desc_ptr` now converts a gmem cute pointer to a generic one (keeping its alignment, at least 64B) — the same conversion `get_tensormap_ptr(ptr, AddressSpace.generic)` and the grouped GEMM examples already do by hand. Pointers in any other address space raise a `ValueError` that names the address space.\n\n## Verification\n\nB200 (`sm_100a`), CUDA 13.2, DSL 4.8.0 — the #3671 reproducer (TMA load through a global-memory copy of the atom's descriptor):\n\n| `tma_desc_ptr` | before | after |\n|---|---|---|\n| generic (converted by hand) | `y == x` | `y == x` |\n| gmem (`get_tensormap_ptr` default) | **libNVVM signature mismatch** | `y == x` |\n| smem | internal IR verification error | `ValueError` |\n\nGeneric pointers are passed through unchanged, so existing callers are unaffected — the Blackwell `grouped_gemm.py` and `grouped_blockscaled_gemm.py` examples pass before and after.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3676"}
{"number": 3675, "title": "[CuTeDSL] Honour cutlass.Pointer TMA descriptors passed via tma_desc_ptr", "state": "open", "created_at": "2026-09-24T20:08:11Z", "updated_at": "2026-09-24T20:08:11Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "**Fixes #3670.** Verified on hardware: the reported silent fallback reproduces before this change and is gone after.\n\n## Cause\n\nEvery TMA trait's `unpack` in `cute/nvgpu/cpasync/copy.py` sets the `tma_descriptor_ptr` field only `if isinstance(tma_desc_ptr, cute.typing.Pointer)`. `TensorMap.get_ptr()` returns a `cutlass.Pointer` (`base_dsl.typing.Pointer`), which is not a cute pointer, so it was dropped without an error — the copy compiled, ran, and used the descriptor baked into the atom by `make_tiled_tma_atom`. Any other non-pointer value (e.g. an `Int64`) was dropped the same way.\n\n## Fix\n\nRoute all seven `tma_desc_ptr` sites (tiled / gather4 / im2col G2S, their multicast variants, S2G store and S2G reduce) through one helper, `_to_tma_desc_ptr`:\n- cute pointers pass through unchanged;\n- a gmem or generic `cutlass.Pointer` is converted to `!cute.ptr<tma_descriptor_tiled, generic, align<64>>` — the same form `TensorMapManager.get_tensormap_ptr(..., AddressSpace.generic)` produces (`CUtensorMap` is 64B aligned);\n- anything that is not a pointer raises `TypeError`, and a `cutlass.Pointer` outside generic/gmem raises `ValueError`.\n\n## Verification\n\nB200 (`sm_100a`), CUDA 13.2, DSL 4.8.0 — the #3670 reproducer (one CTA TMA-loads a 64x64 fp32 tile of `x`; the override map is based at `z`, so an honoured override gives `y == z`):\n\n| `tma_desc_ptr` | before | after |\n|---|---|---|\n| none (baked) | `y == x` | `y == x` |\n| `TensorMap.get_ptr()` | **`y == x` (silently ignored)** | `y == z` |\n| `get_ptr()` re-typed by hand | `y == z` | `y == z` |\n| `Int64(0)` | **`y == x` (silently ignored)** | `TypeError` |\n\nWith the fix, the `get_ptr()` PTX is identical to the hand re-typed workaround. This also makes the #3672 workaround of passing a `create_tensor_map_tiled_from_view(..., l2_promotion=l2_256b)` map work directly through `get_ptr()` (verified: `y == z`).\n\nExisting cute-pointer callers are unaffected — the Blackwell `grouped_gemm.py` example passes before and after. The gmem-pointer failure in #3671 is fixed separately in a follow-up PR.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3675"}
{"number": 3619, "title": "[CuTe DSL] Reject direct kernel compilation with a user error (issue #3429)", "state": "closed", "created_at": "2026-09-13T03:33:17Z", "updated_at": "2026-09-24T16:29:22Z", "closed_at": "2026-09-24T16:29:22Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- preserve whether a CuTe DSL wrapper was created by `@cute.jit` or `@cute.kernel`\n- reject a kernel passed directly to `cute.compile` before host-wrapper lowering\n- emit a structured user diagnostic that points to the supported `@cute.jit` launcher pattern\n- add a regression test that verifies the error code and ensures the message is not reported as an internal error\n\n## Problem\n\n`cute.compile` builds an executable host wrapper and expects a function decorated with `@cute.jit`. Passing an `@cute.kernel` function directly currently falls through to host-wrapper lowering. For the FP32-to-FP8 example in #3429, this reaches invalid scalar conversion IR and is eventually reported as an internal compiler error, even though the actionable problem is the unsupported compile entry point.\n\nThe wrapper already closes over the internal executor name, but that distinction is lost when `CompileCallable` unwraps the decorated function. This change records the executor name on the wrapper and checks it before unwrapping.\n\n## Validation\n\n- NVIDIA B200 (SM100), CUDA 13.1: reproduced the original `cute.compile(kernel, 0.0)` failure as an MLIR verification/internal error.\n- NVIDIA B200 compatibility probe: the same call with the entry guard fails immediately with the `@cute.jit` launcher guidance and contains no `Internal Error`.\n- `python3 -m py_compile` passes for all changed Python files.\n- Ruff severe-rule checks pass for all changed files; full lint and format checks pass for the new regression test.\n- `git diff --check` passes.\n\nThe B200 runtime probe used the node's ABI-compatible CuTe DSL 4.3.5 package with the same entry guard applied in an isolated GPFS overlay; the submitted current-main files were validated statically, and the added regression test exercises the guard before code generation.\n\nFixes #3429\n", "url": "https://github.com/NVIDIA/cutlass/pull/3619"}
{"number": 3589, "title": "fix(CuTeDSL): optimize positional-only functions with TVMFFIJitCompiledFunction to eliminate CPU overhead (#3527)  ", "state": "open", "created_at": "2026-09-06T11:57:59Z", "updated_at": "2026-09-24T15:30:42Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "\r\n\r\n#### 1. What was the Issue? (#3527)\r\nIn CUTLASS v4.6, the routing condition in `python/CuTeDSL/cutlass/cutlass_dsl/cutlass.py` was updated to check:\r\n\r\n```python\r\nif (\r\n    kwargs_wrapper_spec.kwonly_names\r\n    or kwargs_wrapper_spec.arg_defaults\r\n    or kwargs_wrapper_spec.arg_names\r\n    or map_dataclass_to_tuple\r\n):\r\n    return TVMFFIJitCompiledFunctionWithKwargs(...)\r\n```\r\n\r\nBecause `kwargs_wrapper_spec.arg_names` includes positional parameter names for every argument, any `@cute.jit` function taking 1 or more arguments evaluated to `True` and was routed to `TVMFFIJitCompiledFunctionWithKwargs`.\r\n\r\n`TVMFFIJitCompiledFunctionWithKwargs` wraps invocation in `kwargs_wrapper.make_kwargs_wrapper`. When registered via TVM-FFI and called from C++, execution left C++ to enter Python (GIL + argument wrapper parsing), causing CPU overhead regression compared to direct C++ TVM-FFI function execution.\r\n\r\n---\r\n\r\n#### 2. What We Did\r\n\r\nPositional-only parameters (`def func(a, b, c, /)`) cannot be called with keyword arguments in Python grammar. Therefore, functions with strictly positional-only arguments do not require keyword-argument wrapping.\r\n\r\n1. **`python/CuTeDSL/cutlass/base_dsl/jit_executor.py`**:\r\n   - Added `has_pos_or_kw` to `KwargsWrapperSpec`.\r\n   - Updated `get_kwargs_wrapper_spec` to track whether any `Parameter.POSITIONAL_OR_KEYWORD` parameter exists.\r\n\r\n2. **`python/CuTeDSL/cutlass/cutlass_dsl/cutlass.py`**:\r\n   - Changed `kwargs_wrapper_spec.arg_names` check in `_make_compiled_func` to `kwargs_wrapper_spec.has_pos_or_kw`.\r\n   - Functions defined as `def func(a, b, c, /)` without defaults or dataclasses now return `TVMFFIJitCompiledFunction` (a direct subclass of `tvm_ffi.Function`).\r\n\r\n3. **Unit Tests & Documentation**:\r\n   - Added unit test suite `test_tvm_ffi_kwargs_wrapper_spec.py`.\r\n   - Updated documentation in `tvm_ffi_compilation.rst:64`.\r\n\r\n---\r\n\r\n#### 3. Comparison & Verification of Fix\r\n\r\n| Function Signature          | has_pos_or_kw | arg_defaults | kwonly_names | Class Returned                        | CPU Overhead Behavior                                      |\r\n|-----------------------------|---------------|--------------|--------------|---------------------------------------|------------------------------------------------------------|\r\n| `def func()`                | False         | `()`         | `[]`         | `TVMFFIJitCompiledFunction`           | 0 Python Overhead (Direct C++ `tvm_ffi.Function`)          |\r\n| `def func(a, b, c, /)` (Fix)| False         | `()`         | `[]`         | `TVMFFIJitCompiledFunction`           | 0 Python Overhead (Direct C++ `tvm_ffi.Function`, bypasses GIL/wrapper) |\r\n| `def func(a, b, c)`         | True          | `()`         | `[]`         | `TVMFFIJitCompiledFunctionWithKwargs` | Handles Python kwargs calls (`func(a=1, b=2)`)             |\r\n| `def func(a, b=1, /)`       | False         | `(1,)`       | `[]`         | `TVMFFIJitCompiledFunctionWithKwargs` | Handles positional default value binding                   |\r\n| `def func(a, /, *, k=1)`    | False         | `()`         | `['k']`      | `TVMFFIJitCompiledFunctionWithKwargs` | Handles keyword-only argument binding                      |\r\n\r\n##### Verification Summary\r\n\r\n- Evaluated signature parsing with `inspect.signature` across positional-only, positional-or-keyword, and mixed argument patterns.\r\n- Confirmed `def func(a, b, c, /)` correctly evaluates `check_routing()` to `False`, bypassing `TVMFFIJitCompiledFunctionWithKwargs` and directly instantiating `TVMFFIJitCompiledFunction`.\r\n```", "url": "https://github.com/NVIDIA/cutlass/pull/3589"}
{"number": 3668, "title": "[CuTeDSL] Explain stride alignment after partitioning", "state": "open", "created_at": "2026-09-24T08:06:48Z", "updated_at": "2026-09-24T14:00:59Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "A 16-byte-aligned base does not establish alignment for every row when reconstructing a tensor with an unconstrained dynamic stride. Document the FP32 leading-stride divisibility condition, preserve it with `cute.assume`, and distinguish aligned pointers from contiguous copies. Use the actual leading stride `ld` rather than logical width `N`, so padded views are covered correctly.\n\nThe host example checks the actual data pointer, unit inner stride, and leading-stride divisibility. An optional 32-bit fallback checks both source and destination views before choosing a copy width; it handles real storage offsets without overstating DLPack alignment. The GPU regression verifies data and unchanged padding around the destination. Related: #3033. These cases do not establish a general `partition_S` alignment-loss bug.\n\nValidation uses the complete PR Python source at `e00ad37439411daf756e03ffb0783aec5aa34102`, matching CuTeDSL/compiler libraries 4.8.0, cuda-python/cuda-bindings 13.0.3, RTX 5090 SM120, CUDA 13.0.88 and driver 580.82.07, in a separate environment.\n\n| Row-copy case | Cases | Observed |\n|---|---:|---|\n| 128-bit: static stride 64 or 68, offset 0 | 2 | Bit exact; 128-bit load/store |\n| 128-bit: static stride 64, offset 4 FP32 elements | 1 | Bit exact; 128-bit load/store |\n| 128-bit: dynamic stride 64 or 68, checked divisibility + `cute.assume` | 2 | Bit exact; 128-bit load/store |\n| Static stride 65; static source offset 1; dynamic stride without proof | 3 | Expected pointer-alignment diagnostics |\n| Explicit 32-bit fallback: static stride 65; actual source offset 1; dynamic stride 65 plus offset 1; only destination offset 1 | 4 | Bit exact; destination padding preserved; 32-bit load/store |\n| 128-bit atom request with actual destination offset 1 and honest `assumed_align=4` | 1 | Compiler narrows to 32-bit load/store; bit exact; padding preserved |\n\n`test_partition_alignment.py`: 5 methods / 13 subcases pass (10 GPU copies and 3 expected compilation errors); Compute Sanitizer memcheck reports 0 errors. These tests use one thread per row. All five wide cases emit PTX `ld/st.global.v2.b64` and SASS `LDG.E.128/STG.E.128`; all five narrow cases emit PTX `ld/st.global.b32` and scalar SASS memory instructions. A requested atom width alone is not evidence of the emitted memory-access width. The changed RST section builds with Sphinx `-n -W`; the whole documentation site was not built. Earlier 4.6 compiler crashes remain historical observations, not passing tests or an established root-cause diagnosis.\n\nThe prior full pretrained ResMLP-12 comparison used #3667 source at `4e7572a2812758e65ba1d0f2881ed4285ea1012a` and matching 4.8 libraries. These are retained measurements, not a new model run. This PR changes only documentation and tests; it does not change the runtime used by that comparison. Scalar/vector materialize the token-mixing input transpose in all 12 blocks. FP32, batch 2, 224×224, eager, TF32 disabled, fixed seeded synthetic inputs. All logits are bit exact with native; conversion and allocation costs are included.\n\n| Full-model path | Fresh processes | Latency ms | Speedup vs native | Throughput change | Max logit error |\n|---|---:|---:|---:|---:|---:|\n| native view | 3 | 2.1475 | 1.0000× | +0.00% | 0 |\n| scalar copy | 3 | 3.4986 | 0.6138× | -38.62% | 0 |\n| vector copy | 3 | 3.5199 | 0.6101× | -38.99% | 0 |\n\nEach of three fresh processes uses rotated three-arm order followed by its reverse. Each arm has two observations, each with 5 warmups and 20 full forwards. Values are geometric means of per-process medians. Model: `timm/resmlp_12_224.fb_in1k@59a4112ca93db165fd828bef7d47d758ad84295b`; seeds 303301–303303.\n\nMaterializing the vector transpose increases full-model latency by 63.91% versus native's view. No consumer optimization is included, and these measurements do not establish a scalar/vector speed difference or a compiler-version speedup. This PR remains a documentation and regression-test change; it has no code dependency on #3667.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3668"}
{"number": 3667, "title": "[CuTeDSL] Reject incomplete copy-atom thread groups", "state": "open", "created_at": "2026-09-24T08:06:34Z", "updated_at": "2026-09-24T13:20:35Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "A tiled copy containing only part of its atom's thread group currently reaches lowering. In #3234, a one-thread tile is used with the warp-wide `StMatrix16x8x8bOp`, resulting in an invalid intrinsic argument list. Reject incomplete thread groups in the common tiled-copy constructor and report the required group size, matching the C++ divisibility condition in `include/cute/atom/copy_atom.hpp`.\n\nThe regression covers `make_tiled_copy_tv`, direct `make_tiled_copy`, and `make_cotiled_copy`, including nested thread modes whose size must be their product. The GPU test tiles the atom's source TV layout over 1, 2, or 4 warps in one tiled-copy object and uses compact registers for b8 x1/x2/x4 readback across 1 and 3 CTAs. Valid instruction lowering is unchanged. Related: #3234; this addresses the invalid-layout diagnostic, not every possible b8 failure.\n\nValidation uses the complete PR Python source at `3f13a45d26b52356921ddcdbb81ac6725f23c429`, CuTeDSL/compiler libraries 4.8.0, cuda-python/cuda-bindings 13.0.3, RTX 5090 SM120, CUDA 13.0.88 and driver 580.82.07. The matching compiler and Python bindings are in a separate environment.\n\n| Regression | Unpatched 4.8.0 wheel | PR source | Runtime speedup |\n|---|---|---|---|\n| TV constructor, x1/x2/x4 × 1/16/48 threads | 9 missing-ValueError failures | 9 expected ValueErrors | N/A: invalid inputs |\n| Direct constructor, nested 8/16/48 threads × x1/x2/x4 | 9 missing-ValueError failures | 9 expected ValueErrors | N/A: invalid inputs |\n| Cotiled constructor, x1/x2/x4 × 16/48 threads | 6 missing-ValueError failures | 6 expected ValueErrors | N/A: invalid inputs |\n| All three constructors, 32/64 threads × x1/x2/x4 | 18 accepted | 18 accepted | Not benchmarked |\n| Scalar atom, 1 thread | Accepted | Accepted | Not benchmarked |\n| b8 register → shared → global, x1/x2/x4 × 1/2/4 warps × 1/3 CTA | — | 18 bit-exact cases | No speed claim |\n\n`test_copy_thread_groups.py`: 6 methods / 43 subcases pass; the same suite against the unpatched wheel has 24 missing-ValueError failures. `test_stmatrix_thread_groups.py`: 1 method / 18 subcases pass under both Compute Sanitizer memcheck (0 errors) and racecheck (0 errors, 0 warnings). All nine matrix-count/warp-count variants were exported: PTX contains the x1/x2/x4 `stmatrix.sync.aligned.m16n8...trans.shared.b8` forms, and SASS contains `STSM.8.MT168`, `.2`, and `.4` respectively.\n\nThe prior complete pretrained ResMLP-12 forward verifies the ordinary copy consumer at `4e7572a2812758e65ba1d0f2881ed4285ea1012a`. The new commit changes only regression tests; `python/CuTeDSL` is unchanged, and these model measurements were not rerun. This is not a model-level b8 stmatrix benchmark. Batch 2, FP32, 224×224, eager, TF32 disabled; scalar/vector materialize the token-mixing input transpose in all 12 blocks. All logits match native bit for bit.\n\n| Full-model path | Fresh processes | Latency ms | Speedup vs native | Throughput change |\n|---|---:|---:|---:|---:|\n| native view | 3 | 2.1475 | 1.0000× | +0.00% |\n| scalar copy | 3 | 3.4986 | 0.6138× | -38.62% |\n| vector copy | 3 | 3.5199 | 0.6101× | -38.99% |\n\nEach fresh process uses rotated three-arm order followed by its reverse, with 5 warmups and 20 full forwards per observation, twice per arm. Values are geometric means of per-process medians; wall time includes conversion and allocation. Model revision: `timm/resmlp_12_224.fb_in1k@59a4112ca93db165fd828bef7d47d758ad84295b`. The materialized transpose regresses latency; this PR makes no performance claim.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3667"}
{"number": 3607, "title": "[CuTeDSL] Prevent domain_offset overflow with static strides", "state": "open", "created_at": "2026-09-10T07:17:20Z", "updated_at": "2026-09-24T12:51:58Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3604.\n\n## What this fixes\n\n`cute.domain_offset` can calculate the wrong global-memory address even when\nthe runtime coordinate itself fits in `Int32`. A static stride does not widen\nthe coordinate before multiplication, so the **element offset** can overflow\nbefore it is converted to a 64-bit **byte offset** for pointer addition.\n\nThis change widens dynamic coordinate leaves before `crd2idx` for global/generic\nmemory pointers. It fixes the reported static-stride and rebuilt-static-stride\ncases without requiring callers to make their layouts dynamic or manually cast\ncoordinates to `Int64`. The code change is confined to `tensor.py`; this PR also\nadds 10 CPU IR regression tests.\n\n## Reproducer and failure mechanism\n\nThe [full standalone reproducer in #3604](https://github.com/NVIDIA/cutlass/issues/3604)\nallocates a BF16 tensor with shape `(2**29 + 1, 4)` and compact strides `(4, 1)`,\nthen uses a runtime `Int32(2**29)` to address the last row. Its essential operation\ninside the kernel is:\n\n```python\n# tensor: global BF16 tensor, layout (2**29 + 1, 4):(4, 1)\n# row: runtime cutlass.Int32, value 2**29\nshifted = cute.domain_offset((row, 0), tensor)\nshifted[(0, 0)] = cutlass.BFloat16(1.0)\n```\n\nThe coordinate is valid and fits in signed 32 bits. The intermediate product\ndoes not:\n\n| Address-calculation step | Intended value | Unpatched static-stride path |\n| --- | --- | --- |\n| Runtime row | `2**29` | `2**29` |\n| Element offset: row times stride 4 | `2**31` | Overflows signed 32-bit arithmetic to `-2**31` |\n| Byte offset: element offset times 2 for BF16 | `+2**32` (+4 GiB) | Signed wide multiplication produces `-2**32` (-4 GiB) |\n\nThe resulting store goes before the allocation rather than to its last row.\nCasting the result of `crd2idx` to `Int64` would be too late: the narrow product\nhas already lost its intended value. BF16 exposes the byte-address consequence;\nthe coordinate-to-index overflow is not specific to BF16.\n\nThe issue's dynamic-stride variant succeeds because its runtime stride is\nalready `i64`, causing the coordinate/stride calculation to stay wide. Rebuilding\na layout with the static stride `(4, 1)` removes that wide operand and restores\nthe failure. It is the arithmetic width, not the presence of `make_layout`, that\ndistinguishes these cases.\n\n## Why this change is placed in `domain_offset`\n\n`domain_offset` is the point where the coordinate becomes an offset for a\nparticular tensor iterator. For a global/generic `Pointer`, the patch recursively\npromotes narrow dynamic coordinate leaves to `Int64` **before** calling\n`crd2idx`, so the coordinate/stride product is formed at the required width.\n\nThe implementation preserves the surrounding contracts:\n\n- Python integer constants and `None` leaves stay unchanged, preserving static\n  coordinate information and constant folding.\n- Integer leaves already at least 64 bits wide are left alone. Signed narrow\n  coordinates sign-extend; unsigned narrow coordinates zero-extend.\n- `get_divisibility` and `assume` carry existing divisibility facts across the\n  promotion, preserving the derived pointer-alignment information.\n- `transform_leaf` handles nested coordinates rather than only flat tuples.\n- Shared-memory pointers and non-pointer iterators, including identity tensors,\n  retain their existing arithmetic. General-purpose `crd2idx` and CuTe integer\n  algebra are not changed globally.\n\nThis addresses overflow introduced within this `domain_offset` calculation. It\ncannot recover a coordinate that the caller has already overflowed, and it does\nnot make out-of-bounds coordinates valid. No performance improvement is claimed;\nthe effect of wider arithmetic on arbitrary kernels has not been benchmarked.\n\n## Check against the executed native code\n\nFor the BF16 row/stride case above, the executed baseline kernel emits:\n\n```ptx\nshl.b32      %r2, %r1, 2;\nmul.wide.s32 %rd2, %r2, 2;\n```\n\nHere the first instruction computes the element offset in 32 bits; the wide\nmultiply only widens that already-overflowed result. The fixed kernel instead\ncombines the stride and element size into a wide multiply of the original row:\n\n```ptx\nmul.wide.s32 %rd2, %r1, 8;\n```\n\nDisassembly of 60 repeated baseline/fixed binaries confirms the corresponding\n32-bit shift is removed and the wide multiply changes from factor 2 to 8.\nA semantically neutral source variant retains identical native instructions\nwithin each arm and does not fix the baseline failure. Baseline memcheck reports\na 2-byte global store 4 GiB before the nearest allocation, matching the predicted\nnegative byte offset.\n\n## Regression tests included in this PR\n\nRun with an environment importing the changed CuTeDSL source:\n\n```console\npython test/python/CuTeDSL/test_domain_offset.py\n```\n\nThese tests build and verify MLIR on the CPU; they do not allocate a multi-GiB\ntensor or require a GPU. **10/10 pass with the fix, versus 5/10 on the baseline.**\nThey check widening before `crd2idx`, global/generic pointers, nested coordinates,\ndivisibility/alignment preservation, unsigned extension, static constants,\nalready-wide coordinates, dynamic-i64 strides, and unchanged shared-memory and\nidentity-tensor behavior. These are IR regressions, not a substitute for runtime\nvalidation.\n\n## Additional B300 validation\n\nA separate runtime harness exercised the real installed package on B300 SM103,\nwith source hashes checked against the baseline and this patch. Each case used\na fresh process and private compile/cache directory. Baseline failures were\nisolated because an illegal address invalidates the CUDA context.\n\n- Static-stride, rebuilt-static-layout, and neutral-source variants each produced\n  **10/10 baseline failures and 10/10 fixed passes**.\n- A 29-entry fixed matrix ran both normally and under Compute Sanitizer. It\n  covers BF16/FP32/Int32 elements; row offsets `0`, `2`, `2**29 - 1`, `2**29`,\n  `2**29 + 1`, and `2**30`; and dynamic-i64, explicit-i64, nested, constant,\n  neutral, and valid negative-offset cases. This is a selected matrix, not the\n  Cartesian product of every option.\n- All **29 fixed memcheck cases reported zero errors**. A separate baseline\n  memcheck reproduced the invalid global store described above.\n- **590 fixed-path graph replays and 40 baseline-control replays passed**.\n  Each replay poisoned the tensor buffer and checked that only the intended\n  element changed. Captured scalar arguments stayed fixed; mutation of those\n  arguments was not tested.\n\nAcross the campaign: **90 fixed cases passed, 5 baseline controls passed, and\n32 expected baseline failures were reproduced (127 total cases)**. Memcheck\ncases and graph replays are included in those runs, not extra independent cases.\nThe GPU harness/results are additional validation, not tests added to this PR\nor a report of upstream CI passing.\n\n<details>\n<summary>Toolchain and existing suite limitations</summary>\n\nValidation used public `nvidia-cutlass-dsl==4.8.0.dev0` with matching base/core/cu12\nlibraries, Torch 2.11.0+cu130, cuda-bindings 13.3.1, B300 driver 580.126.09, and\nCompute Sanitizer 2025.4.1. This is not the exact dated alpha wheel in the report.\nThe unpatched wheel's `tensor.py` is byte-identical to the base source at\n`147295a3`; the fixed installed module is byte-identical to this PR's version.\n\nThe broader public CuTeDSL Python suite is **39/43 passing with this change,\nversus 34/43 on the baseline**. Both retain the same four unrelated failures/errors:\n\n- `test_exec_defined_function_raises_user_error`\n- `test_uncaught_dsl_error_does_not_kill_interactive_session`\n- `test_preserves_attribute_order_and_deduplicates`\n- `test_fastmath_flag`\n\nNo unrelated changes are included. Both final GPU arms used lazy CUDA module\nloading; earlier eager-loading attempts timed out in PyTorch CUDA initialization\nbefore CuTe compilation/launch and are not counted as kernel results.\n\n</details>\n", "url": "https://github.com/NVIDIA/cutlass/pull/3607"}
{"number": 3582, "title": "[CuTeDSL] Support dynamic shapes in right_inverse", "state": "open", "created_at": "2026-09-04T12:45:03Z", "updated_at": "2026-09-24T12:50:11Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- allow `right_inverse` to accept dynamic shapes when every stride is a fully static integer\n- retain only the maximal inverse prefix whose stride continuity is statically provable, and carry dynamic shape values through CuTe expansion and base lowering\n- preserve the existing static-shape behavior, including dynamic strides and supported scaled-basis forms\n- add verifier, expansion, full-pipeline, and cutegen regression coverage for continuous, truncated, and zero-stride cases\n\nFor `(16, N):(1, 16)`, this returns the functionally equivalent uncoalesced `(16, N):(1, 16)` layout demonstrated by PyCuTe in the issue discussion. Producing the canonical `16*N:1` representation depends on dynamic coalescing, which is tracked separately in #3469. Consequently, callers that require a coalesced rank-1 inverse, including the `blackwell_helpers.py` TMA-shape path, still depend on #3469.\n\n## Testing\n\n- `cmake --build build-cutlass-compiler --target check-cute -j 8` (236/236 passed)\n- `cmake --build build-cutlass-compiler --target check-cutegen-unittests -j 8` (220/220 passed across 25 test binaries)\n- focused post-review MLIR suite (5/5 passed)\n- `ctest --test-dir build-cutlass-compiler -R 'cg_right_inverse_test' --output-on-failure` (1/1 passed)\n- `git diff --check`\n\nThe compiler and tests were built CPU-only against the LLVM revision pinned by `cutlass_compiler/LLVM_COMMIT`.\n\n## Contribution disclosure\n\nAI assistance was used for repository research, implementation, independent review, and running the checks above.\n\nFixes #3471\n", "url": "https://github.com/NVIDIA/cutlass/pull/3582"}
{"number": 3674, "title": "[CuTeDSL] Add FP32 residual and Affine fusion example", "state": "open", "created_at": "2026-09-24T12:42:58Z", "updated_at": "2026-09-24T12:42:58Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "ResMLP inference runs separate LayerScale multiply, residual add, and Affine kernels in each block. Add an opt-in FP32 CuTe DSL example that combines residual + Affine in one launch and the final scaled residual in another. The native transpose views, GEMMs, parameter storage, and output strides are preserved.\n\nSmall PTX wrappers keep `mul.rn.f32` and `add.rn.f32` separate for the residual, then use `fma.rn.f32` for the Affine operation matching `torch.addcmul`. TVM FFI passes tensors and the current stream directly after compilation. The example rejects autograd use and does not change CUTLASS core dispatch or add a timm dependency to the package.\n\nThe included `resmlp_inference.py` runs a complete pretrained ResMLP-12 with a local checkpoint, checks logits against the native forward, and benchmarks eager or CUDA Graph execution. This is independent of #3667 and #3668.\n\nValidation at `56959d7f7181257f225364cffded01d5759a8965` on RTX 5090 SM120, CUDA 13.0.88, driver 580.82.07, Python 3.12.13, CuTeDSL 4.8.0, cuda-python/cuda-bindings 13.0.3, TVM FFI 0.1.11, torch 2.13.0+cu130, timm 1.0.29:\n\n- Four unittest methods pass: eight shape/layout pairs (including tails and mixed layouts), cancellation-sensitive residual rounding, a non-default stream and graph replay, and the autograd guard. Compute Sanitizer memcheck: **0 errors**.\n- Full-model outputs and all 12 block outputs are bit exact for batch 1/2/8, FP32, 224×224, TF32 disabled. Parameter storage/version fingerprints are unchanged. Every fused forward executes the two new kernels in each block.\n- Exported SASS contains separate `FMUL` / `FADD`, followed by `FFMA` only for the Affine variant. A separate screening trace reduces full-forward GPU launches from 174 to 138.\n\nFull-model independent validation, native view baseline versus the final example:\n\n| Batch | Mode | Native ms | Fused ms | Speedup | Throughput change |\n|---:|---|---:|---:|---:|---:|\n| 2 | eager | 2.2650 | 2.0682 | 1.0951× | +9.51% |\n| 2 | CUDA Graph | 1.0730 | 1.0239 | 1.0479× | +4.79% |\n| 1 | eager | 2.1982 | 2.0009 | 1.0986× | +9.86% |\n| 1 | CUDA Graph | 0.8241 | 0.7759 | 1.0622× | +6.22% |\n| 8 | eager | 2.3836 | 2.2435 | 1.0625× | +6.25% |\n| 8 | CUDA Graph | 2.1802 | 2.1055 | 1.0355× | +3.55% |\n\nThese are geometric means of process medians from eight fresh processes, ordered APPA then PAAP, with four processes per arm. Each process runs all three batches in both modes; each measurement has six observations, each with 10 warmups and 30 complete forwards/replays. Allocation and Python invocation are included in eager time; JIT, model loading, and graph capture are excluded. CUDA Graph actually replays captured forwards. Seeds are 303601 and 303602, one per quartet.\n\nBatch-2 eager speedups in the two independent quartets are 1.0880× and 1.1023×; CUDA Graph speedups are 1.04786× and 1.04789×. Maximum within-quartet baseline drift is 1.68%, below the predeclared 5% guard. There are only two independent quartets, so these are results for the tested inference workload, not a general significance or cross-platform claim. No training, mixed precision, dataset accuracy, or `torch.compile` comparison is included.\n\nBatch-2 eager peak allocated PyTorch memory is 100.88 MiB native versus 101.46 MiB fused (+0.58 MiB); this is not a memory-reduction claim.\n\nFinal validation used GPU UUID `GPU-c9f0f22e-3db5-252c-d294-a47b05b1f904`; 101 process samples found only the corresponding validation process. The host was shared with work on another GPU. An earlier GPU-7 series with unexpected processes is retained separately and excluded from this table. Main environment and system CUDA were unchanged; task-owned checkpoint copies were removed after all runs.\n\nModel: `timm/resmlp_12_224.fb_in1k`, revision `59a4112ca93db165fd828bef7d47d758ad84295b`, checkpoint SHA256 `2ed399f6a787a30925f49bd3e8040e6b0f0a620ed68d56758c703e98bb7c0fbe`.\n\nReproduce using the matching CuTe DSL runtime and the optional timm/safetensors dependencies:\n\n```bash\npython test/python/CuTeDSL/test_residual_affine.py -v\npython examples/python/CuTeDSL/cute/ampere/kernel/elementwise/resmlp_inference.py --weights model.safetensors --arm native --batch 2\npython examples/python/CuTeDSL/cute/ampere/kernel/elementwise/resmlp_inference.py --weights model.safetensors --arm fused --batch 2\n# Add --graph to both arms for the CUDA Graph comparison.\n```\n", "url": "https://github.com/NVIDIA/cutlass/pull/3674"}
{"number": 3279, "title": "fix: missing PDL wait on main_sf_load in sm103 blockscaled GEMM", "state": "open", "created_at": "2026-05-27T10:33:45Z", "updated_at": "2026-09-24T11:13:28Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n- `main_sf_load` was the only load-capable warp branch in [`sm103_blockscaled_gemm_tma_warpspecialized.hpp`](https://github.com/NVIDIA/cutlass/blob/main/include/cutlass/gemm/kernel/sm103_blockscaled_gemm_tma_warpspecialized.hpp) without a preceding `cutlass::arch::wait_on_dependent_grids()`. With PDL enabled, `collective_mainloop.load_sf(...)` issues TMA reads of scale-factor (SF) data before the producer kernel's writes are guaranteed flushed to global memory.\n- Fix: hoist `wait_on_dependent_grids()` above the warp specialization region so it covers all warp branches (including `main_sf_load`), and remove the now-redundant inner waits in `main_ab_load` / `sched` / `epi_load`. This matches the cleaned-up pattern already in `sm90_gemm_array_tma_warpspecialized_{pingpong,cooperative}.hpp` (1 outer wait, 0 inner).\n- Same class of PDL-placement bug as #2962, but in a kernel that wasn't covered by that fix.\n\n## Why this matters\nObserved in TensorRT-LLM on GB300 + DeepSeek-R1 NVFP4 forward (kernel: `DeviceGemmFp4GemmSm103_..._KernelTmaWarpSpecializedBlockScaledSm103`). With a diagnostic probe (`ld.global.cv.u8` reads of the SF buffer in `main_sf_load`):\n\n| Config | producer fence | `main_sf_load` wait | GSM8K | Race probe |\n|---|:---:|:---:|---:|---|\n| Baseline | ✗ | ✗ | 91.93 | 2.31% poison (0x7f) |\n| Producer fence only | ✓ | ✗ | 95.34 | 0 / 2.4M poison |\n| **This fix (no producer fence)** | ✗ | ✓ | **95.34** | **0 / 3.2M poison** |\n| Reproducer run | ✗ | ✓ | 95.11 | **0 / 3.2M poison** |\n\nTwo independent runs, **0 poison events across 6.4M probe samples** — the race is fully eliminated on the CUTLASS side without any modification to the producer kernel.\n\n## Related\n- #2962 — same class of bug fixed for sm90/sm100 array variants; this PR addresses the sm103 non-array blockscaled variant not covered by that fix.\n- Suggest a follow-up cleanup PR to fully remove leftover inner waits in `sm100_*_array_tma_warpspecialized*.hpp` and `sm103_blockscaled_gemm_array_tma_warpspecialized.hpp` (which still retain redundant inner waits compared to the cleaned sm90 array variants).", "url": "https://github.com/NVIDIA/cutlass/pull/3279"}
{"number": 3408, "title": "[CuTeDSL] Make autotuning more user friendly & new docs", "state": "open", "created_at": "2026-07-24T06:03:30Z", "updated_at": "2026-09-24T10:58:46Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "This PR adds a lot of things to the overall CuTeDSL autotuning process to make it easier to use and more convenient:\r\n\r\n- Add `derived_params` to autotuning API so it computes derived arguments that must be decided during compile time but rely on autotuned parameters (whose values are different per configuration tuned)\r\n- Make `testing.autotune_jit` able to decorate a class. This is for people who prefer to implement kernels as classes that fix the compile time params in `__init__` method and pass runtime params to `__call__`, so now they can do this\r\n```\r\n@testing.autotune_jit(\r\n    params_dict={\"COPY_BITS\": [32, 64, 128], \"THREADS_M\": [4, 8]},\r\n    update_on_change=[\"M\", \"N\"],\r\n    warmup_iterations=10,\r\n    iterations=50,\r\n)\r\nclass ElementwiseAddKernel:\r\n    THREADS_M = 4\r\n\r\n    def __init__(self, COPY_BITS=128):\r\n        self.COPY_BITS = COPY_BITS\r\n\r\n    @cute.jit\r\n    def __call__(self, mA, mB, mC, M, N):\r\n        elementwise_add(\r\n            mA, mB, mC, M, N,\r\n            copy_bits=self.COPY_BITS,\r\n            threads_m=self.THREADS_M,\r\n        )\r\n```\r\n- Support pruning undesired configurations (either invalid or known to be suboptimal) -- this aligns with triton's `prune_configs_by` in their autotuning API\r\n- Add `autotune_suite` API so now user can provide a list of representative shapes and find the best overall kernel candidates with the ranking rule: sort according to how many times a configuration is accepted across all inputs, where \"accepted\" mean for a input case the configuration is in the top `accept_percentile` fastest ones\r\n- Better autotuning docs. Previously the doc only explains how to autotune manually which is quite painful at least for me", "url": "https://github.com/NVIDIA/cutlass/pull/3408"}
{"number": 3669, "title": "[CuTeDSL] Add non-TMA bulk FP32 reduction CopyOp", "state": "open", "created_at": "2026-09-24T08:32:53Z", "updated_at": "2026-09-24T10:29:21Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "FlashAttention's dQ path currently issues `cp.reduce.async.bulk.global.shared` through a local PTX wrapper ([consumer example](https://github.com/Dao-AILab/flash-attention/blob/c4d8b0630eb81cf88206e0cc9e9bff4e7806d88f/flash_attn/cute/flash_bwd_sm100.py#L2563)). Add `CopyReduceBulkS2GOp` so callers can construct a CuTe CopyAtom, partition a tile, and call `cute.copy` for the non tensor-map `add.f32` reduction. Related: #3029.\n\nThe existing bulk S2G atom supplies the partition layout. A Python `cute.copy` dispatch emits the reduction PTX because the current compiler binding has no dedicated non tensor-map reduction atom type. The supported first pass is one contiguous, 16-byte-aligned FP32 tile per call, with a byte count divisible by 16. The caller elects one issuing thread, fences shared writes, and commits/waits on the bulk group, consistent with other non tensor-map bulk operations. The tensor-map reduction path is unchanged.\n\nRTX 5090 / SM120, CUDA 13.0.88, driver 580.82.07: eight cases (16, 128, 512, 4096 bytes × 1/4 CTAs) matched a direct PTX reference bit for bit, including nonzero destination initialization and competing CTA additions. Noncontiguous, misaligned, non-FP32, and non-16-byte-granular inputs are rejected before code generation. The PTX for all eight cases contains `cp.reduce.async.bulk.global.shared::cta.bulk_group.add.f32`; all corresponding cubins contain `UBLKRED.G.S.ADD.F32.RN`.\n\n| Bytes | CTAs | Direct PTX (µs) | CopyOp (µs) | Speedup |\n|---:|---:|---:|---:|---:|\n| 16 | 1 | 8.437 | 8.452 | 0.998× |\n| 16 | 4 | 8.012 | 7.992 | 1.003× |\n| 4096 | 1 | 8.235 | 8.325 | 0.989× |\n| 4096 | 4 | 8.168 | 8.225 | 0.993× |\n\nThese are launch-level CUDA-event timings, four samples per arm in balanced ABBA/BAAB order, 128 calls per sample, on one GPU. They show no measurable acceleration; the change provides a reusable API. The correctness test covers all eight sizes/CTA cases and the rejection tests cover the supported contract.\n\nA consumer-level dQ check also passed. PyTorch SDPA supplied the reference gradient; its softmax backward was split across two key/value ranges, then the CopyOp accumulated both dQ tiles into one output. Max absolute error was `9.54e-7` for `(batch, heads, seq, dim)=(1,2,64,32)` and `1.79e-6` for `(2,3,96,64)`. This validates the reduction in an attention backward calculation; it is not a pretrained-model or full FlashAttention performance result.\n\n**Draft validation boundary:** tests ran against an isolated Python source copy of CuTeDSL 4.6.2 with the same three changes transplanted from this PR. Current `main` does not import with those installed binaries (`NumericCast` missing); a matching-current-main compiler run remains outstanding. No installed packages were changed.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3669"}
{"number": 3528, "title": "fix(CuTeDSL): preserve wrapped signatures for type checkers", "state": "open", "created_at": "2026-08-25T06:13:36Z", "updated_at": "2026-09-24T08:21:44Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n`@dsl_user_op` decorates every user-facing CuTe DSL wrapper (`local_tile`, `make_tiled_mma`, `copy`, `gemm`, etc.), but its annotation was `Callable[..., Any] -> Callable[..., Any]`, which erases the wrapped callable's signature for static analyzers.\n\nPyright/basedpyright (and language servers built on them) therefore see only `(*args, **kwargs)`. In practice this means:\n\n- Editors show **no parameter names** in signature help: `cute.local_tile(` yields no `tiler`/`coord` hints;\n- Bogus keyword calls compile silently (e.g. `cute.local_tile(mA, xxxxx=...)` passes type checking);\n- Downstream projects that write their own `@dsl_user_op` wrappers (vLLM, SGLang, FlashInfer) are affected the same way.\n\n## Change\n\nMake the decorator signature-preserving with a `TypeVar` bound to `Callable`, the pattern recommended by [pyright's documentation](https://github.com/microsoft/pyright/blob/main/docs/typed-libraries.md):\n\n```python\n_FuncT = TypeVar(\"_FuncT\", bound=Callable[..., Any])\n\ndef dsl_user_op(opFunc: _FuncT) -> _FuncT:\n    ...\n    return wrapper  # type: ignore[return-value]\n```\n\nRuntime behavior is unchanged (`functools.wraps` was already applied to the wrapper). `# type: ignore[return-value]` documents that the inner `wrapper` intentionally has a generic signature but must satisfy `_FuncT`.\n\n## Verification\n\n- `python -m py_compile op.py` — syntax OK.\n- pyright, after patch: `cute.local_tile(mA, tiler=(128, 64), coord=(0, 0))` type-checks; `cute.local_tile(mA, xxxxx=(128, 64))` is rejected with `No parameter named \"xxxxx\"`, and `Arguments missing for parameters \"tiler\", \"coord\"` is reported for incomplete calls.\n\nFixes editor/signature-help blindness for the entire public CuTe DSL surface.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3528"}
{"number": 3642, "title": "[CuTe DSL] Allow selected read-only inline_ptx operands to be materialized into a register", "state": "open", "created_at": "2026-09-18T11:54:37Z", "updated_at": "2026-09-24T06:11:08Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "**Incremental review:** [Files changed](https://github.com/NVIDIA/cutlass/pull/3642/files) against `main`.\n\n## Summary\n\n`cute.arch.inline_ptx` lowers a compile-time constant read-only operand to the immediate constraint `n` regardless of what the target instruction accepts. Some PTX instructions take only a register in a given operand slot, so such a constant is rejected by ptxas. This adds `force_register_args`, an opt-in that names the read-only operands which must be moved into a register first.\n\nRefs #3640.\n\n## Minimal failure\n\n```python\n@cute.kernel\ndef kernel():\n    value = cute.arch.thread_idx()[0]\n    zero = Int32(0)\n    cute.arch.inline_ptx(\n        \"max.xorsign.abs.bf16x2 {$w0}, {$r0}, {$r1};\",\n        write_only_types=[Int32],\n        read_only_args=[zero, value],\n    )\n```\n\nGenerated PTX on an L20 (sm_89, `nvidia-cutlass-dsl 4.8.0.dev0`):\n\n```ptx\nmov.u32 \t%r3, %tid.x;\nmax.xorsign.abs.bf16x2 %r1, 0, %r2;      // immediate operand\n```\n\n```\nptxas application ptx input, line 32; error   : Arguments mismatch for instruction 'max'\nptxas fatal   : Ptx assembly aborted due to errors\n```\n\n## Where the constraint is chosen\n\nNot in this repository. `NVVM_InlinePtxOp` carries no per-operand register request (only `readOnlyArgs`, `readWriteArgs`, `ptxCode`, `predicate`, `memoryClobber`). The constraint comes from the NVVM to LLVM conversion, `mlir/lib/Dialect/LLVMIR/IR/BasicPtxBuilderInterface.cpp`:\n\n```cpp\nstatic FailureOr<char> getRegisterType(Value v, Location loc) {\n  if (v.getDefiningOp<LLVM::ConstantOp>())\n    return 'n';\n  return getRegisterType(v.getType(), loc);\n}\n```\n\nThe value-level overload returns `n` for any constant without consulting the instruction, so the generated `llvm.inline_asm` gets the constraint string `\"=r,n,r\"`.\n\n## What this changes\n\n`inline_ptx(..., force_register_args=(0,))` materializes the named read-only operands with an explicit `mov` before the target instruction runs:\n\n```ptx\nmov.b32 %r2, 0;\nmax.xorsign.abs.bf16x2 %r1, %r2, %r3;\n```\n\nwhich is the output #3640 asks for. The operand the target instruction sees is produced by an instruction rather than by a constant op, so the existing type-based register constraint applies. Nothing else changes: the default is `None`, unnamed operands keep today's behavior, and indices refer to `read_only_args` only — out-of-range, negative, bare-int and non-integer indices raise.\n\n## Why not disable immediates globally\n\nA constant is legal, and preferable, in plenty of operand slots (`add.s32`, `mbarrier.init.b64`, ...). Making `getRegisterType(Value)` return `r` for every constant would be a semantics change well beyond this issue. The opt-in is therefore per call site, and the existing immediate path is untouched — covered by `test_default_path_is_unchanged` and by the `add.s32` cases in the verification below.\n\n## Verification\n\nHardware: 1× NVIDIA L20, compute capability 8.9, driver 570.86.10, `CUTE_DSL_ARCH=sm_89`.\nEnvironment: `nvidia-cutlass-dsl 4.8.0.dev0` (cu12 libraries), PTX generated by CUDA 13.4.46 / NVVM 23.0.0. Baseline is `f614dc40e17fb3ddb1b7b474318b48a8d5d21a2c`.\n\nEnd-to-end path (Python DSL → lowering → PTX → ptxas → launch → host readback), 32 lanes of packed bf16 covering ±0, ±1, ±2 and ±0.5, compared as raw bit patterns:\n\n| Variant | Compile | Bit-exact readback |\n|---|---|---|\n| `Int32(0)` as reported | FAIL — `Arguments mismatch for instruction 'max'` | — |\n| constant read from memory (pre-change workaround) | PASS | 32/32 |\n| `llvm.inline_asm` with explicit `=r,r,r` | PASS | 32/32 |\n| `force_register_args=(0,)` | PASS | 32/32 |\n\nRegression matrix, all executed on the device: literal zero; non-zero and negative finite constants; a constant produced by folding; the constant in the second read-only position; two identical constants with only one opted in; a read-only placeholder repeated twice; dynamic operands with and without the opt-in; an instruction where an immediate is legal, with and without the opt-in; two write-only results with a forced constant; and the argument-validation cases. The new test file covers the same IR shapes on CPU, so it needs neither a GPU nor ptxas.\n\nTiming was measured with CUDA events in an interleaved paired run (60 rounds memory-bound, 40 rounds register-resident) including an A/A arm that recompiles and re-measures the patched variant under a second label. The fixed kernel matches the existing workarounds within the A/A noise band (about ±1.2%); the materialized `mov` is hoisted out of the loop and absorbed by register allocation. This is a correctness change, not a throughput change.\n\nTwo failures observed while testing are **not** addressed here, and reproduce without this change: `inline_ptx(predicate=...)` emits `@0` instead of the predicate's operand index, and a float read-only constant fails with `constraint 'n' expects an integer constant expression`. Float operands are therefore rejected by `force_register_args` with an explicit error rather than silently ignored.\n\n## Scope and open questions\n\nThis is a CUTLASS-side wrapper. The complete fix belongs in LLVM/MLIR — a per-operand register request on `NVVM_InlinePtxOp` honoured by `BasicPtxBuilderInterface` — which then has to reach a release wheel. This repository has no copy of that C++, and the wheel ships a statically linked compiler, so that path cannot be validated from here.\n\nThe parameter name is a proposal. Feedback on the name, on whether the `mov` materialization is the right shape, or on waiting for the compiler-side attribute instead, is welcome.\n\nNot tested: sm_100 (the reported target; this host is sm_89), and NaN/subnormal bit-level semantics.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3642"}
{"number": 3665, "title": "[CuTeDSL] docs: fix MMA_N in the warp MMA partitioning example (4 → 8)", "state": "open", "created_at": "2026-09-23T21:12:21Z", "updated_at": "2026-09-23T21:21:51Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nThe running example in [Partitioning Tensors](https://docs.nvidia.com/cutlass/latest/media/docs/pythonDSL/guides/mma/wmma_programming.html#partitioning-tensors) (GEMM 512×512×256, `cta_tiler = (128, 128, 32)`, `atom_layout_mnk = (2, 2, 1)`, F16 `m16n8k16`, `permutation_mnk = (32, 32, 16)`) gives `MMA_N = BN/perm_N = 4`. CuTe DSL returns `MMA_N = 8` for both B and C.\n\n`MMA_N` is not `BN/perm_N`. The N doubling in `permutation_mnk` makes each warp repeat its atom twice inside one `perm_N = 32` block, and `partition_B` / `partition_C` count those repeats together with the 4 blocks: `MMA_N = BN / (atom_layout_N × inst_N) = 128 / 16 = 8`. The same rule gives `MMA_M = 4`, and the doc has that one right, but only because `perm_M` equals the atom coverage (2 × 16).\n\nA second check is to count values per thread. Each thread must hold `128 × 128 / 128 = 128` accumulators. `((2,2), 4, 8)` gives 128. The documented `((2,2), 4, 4)` gives only 64, which is half the CTA tile.\n\n## Changes (`media/docs/pythonDSL/guides/mma/wmma_programming.rst` only)\n\n- The sA/sB partition diagram: `MMA_N = BN/perm_N = 4` → `MMA_N = 2 x BN/perm_N = 8`.\n- The \"After partition\" bullets: `tCsB (MMA, 8, 2, 4)` and `tCgC (MMA, 4, 8)` with `MMA_N = 2 × BN/perm_N`, plus one sentence on where the factor 2 comes from.\n- Making Fragments: `tCsB MMA_N=8`, `tCrB (MMA, 8, 2)`, `tCgC MMA_N=8`, `tCrC (MMA, 4, 8)`.\n\n## Verification\n\nChecked with `nvidia-cutlass-dsl` 4.7.1 and 4.8.0. Both give the same output:\n\n```python\nimport cutlass\nimport cutlass.cute as cute\n\n@cute.jit\ndef run():\n    op = cute.nvgpu.warp.MmaF16BF16Op(cutlass.Float16, cutlass.Float32, (16, 8, 16))\n    tiled_mma = cute.make_tiled_mma(op, cute.make_layout((2, 2, 1)), permutation_mnk=(32, 32, 16))\n    thr = tiled_mma.get_slice(0)\n    print(\"tCsA:\", thr.partition_A(cute.make_identity_tensor((128, 32, 4))).layout)\n    print(\"tCsB:\", thr.partition_B(cute.make_identity_tensor((128, 32, 4))).layout)\n    print(\"tCgC:\", thr.partition_C(cute.make_identity_tensor((128, 128))).layout)\n    print(\"tCrB:\", tiled_mma.make_fragment_B(tiled_mma.partition_shape_B((128, 32))).layout)\n    print(\"tCrC:\", tiled_mma.make_fragment_C(tiled_mma.partition_shape_C((128, 128))).layout)\n\nrun()\n```\n\n```\ntCsA: ((2,2,2),4,2,4):((1@1,8@0,8@1),32@0,16@1,1@2)\ntCsB: ((2,2),8,2,4):((1@1,8@1),16@0,16@1,1@2)\ntCgC: ((2,2),4,8):((1@1,8@0),32@0,16@1)\ntCrB: ((2,2),8,2):((1,2),4,32)\ntCrC: ((2,2),4,8):((1,2),4,16)\n```\n\nThe stride `16@1` on the `MMA_N` mode of `tCgC` shows each step is one 16-wide atom placement (2 warps × 8), not one 32-wide `perm_N` block. The page's other `MMA_M` / `MMA_K` values (4 and 2) match this output and are left unchanged. Running `docutils` on the file gives the same warnings before and after the change.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3665"}
{"number": 3521, "title": "Fix grouped tile scheduler tile count when swizzle is enabled", "state": "open", "created_at": "2026-08-24T16:07:35Z", "updated_at": "2026-09-23T18:19:37Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### Summary\n\nFixes #3497\n\nThe SM90 grouped tile scheduler computed its host-side grid size and early-exit bound by rounding each group's CTA extents to the cluster shape only, while the device-side mapping rounds each group to `(1 << log_swizzle_size) * cluster extent`. With `max_swizzle_size >= 2` and host problem shapes available, any non-final group with swizzle padding shifts every later group's device-side linear range upward past the host bound, and the tail cut in `get_current_work_for_linear_idx` drops real output tiles: those CTAs are never scheduled and the corresponding output region is silently left untouched.\n\n`get_tiled_cta_shape_mnl` now replicates the device per-group computation including the swizzle factor. `get_log_swizzle_size` moves above it and becomes `CUTLASS_HOST_DEVICE` so both sides share one implementation (body unchanged). The SM100 group scheduler forwards through the same function; its `get_grid_shape` now passes the real `max_swizzle_size_` from the params instead of losing it.\n\nWith the default `max_swizzle_size = 1`, `log_swizzle_size == 0` makes both formulas identical, so stock behavior is unchanged.\n\n### Test plan\n\nStandalone host programs that mirror both computations exactly:\n\n1. The issue's failing case (groups 2x2 and 3x3 CTAs, cluster 1x1, max_swizzle_size = 4). Before the fix the host total is 13 while the device maps 20 linear slots and two real output tiles of group 1 are cut by the early-exit bound:\n\n```\nhost_total=13 dev_total=20\nDROPPED REAL TILE idx=13 group=1 -> (m=2,n=1) of 3x3\nDROPPED REAL TILE idx=14 group=1 -> (m=2,n=2) of 3x3\nFAIL: 2 real tiles never scheduled\n```\n\nWith the fixed host formula:\n\n```\nhost_total=20 dev_total=20\nOK\n```\n\n2. Randomized invariant sweep: 7 (cluster, swizzle) combinations x 300 random group lists each (1-6 groups, extents 1-40). The new host bound equals the sum of device per-group totals in every case and never cuts a real tile; the old bound dropped real tiles in these trials a total of 140338 times.\n\n```\nbound-mismatch or new-bound-drop failures=0 (expect 0)\nold bound dropped real tiles across random cluster-1x1 trials: 140338\n```\n\n\n3. New unit test `test/unit/gemm/kernel/sm90_group_tile_scheduler.cu` (registered as `cutlass_test_unit_gemm_kernel`): asserts the host tile count equals the per-group device formula for the issue's 2x2+3x3 case and a sweep of four group lists across cluster shapes (1,1)/(2,1)/(1,2)/(2,2) with max_swizzle_size 1..8. Run in an nvidia/cuda:12.8.1-devel container, nvcc V12.8.93, sm_120a:\n\n```\n[==========] Running 2 tests from 1 test suite.\n[ RUN      ] Sm90GroupTileScheduler.host_tile_count_matches_device_formula\n[       OK ] Sm90GroupTileScheduler.host_tile_count_matches_device_formula (0 ms)\n[ RUN      ] Sm90GroupTileScheduler.host_tile_count_matches_device_formula_sweep\n[       OK ] Sm90GroupTileScheduler.host_tile_count_matches_device_formula_sweep (0 ms)\n[==========] 2 tests from 1 test suite ran. (0 ms total)\n[  PASSED  ] 2 tests.\n```\n\n4. Compile check of a TU including both scheduler headers under nvcc V12.8.93 with `-arch=sm_90a` and `-arch=sm_100a`: clean.\n\nNot covered: an end-to-end grouped GEMM run with `max_swizzle_size >= 2`. SM90 kernels cannot run on the sm_120 GPU available here, and the Blackwell CuTeDSL grouped example does not expose the tile-scheduler swizzle argument. The fix is confined to host-side index arithmetic that is mirrored exactly by the harness above.\n\n### Notes\n\n- When host problem shapes are unavailable the scheduler keeps the `sm_count` grid path, unchanged.\n- The pre-existing inconsistency where `PersistentTileSchedulerSm100Group::get_grid_shape` builds a default `Arguments` (swizzle 1) for `Params::get_grid_shape` is left untouched to keep this change scoped; it can be revisited separately.\n- Possible follow-up: grouped GEMM unit tests currently never exercise `max_swizzle_size > 1` (`MaxSwizzleSize` is unused in the ptr-array testbed while the non-group testbed sweeps {1, 4}); plumbing it through would have caught this. Happy to do that in a separate change if maintainers want it.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3521"}
{"number": 3473, "title": "Specialize updates to pipeline states' `index` - help compiler DCE index ", "state": "open", "created_at": "2026-08-18T23:29:46Z", "updated_at": "2026-09-23T17:14:14Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "This pull request updates the `PipelineState` struct in `sm90_pipeline.hpp` (used for sm90+ arches) to improve the handling of pipeline stage advancement, especially for single-stage and multi-iteration scenarios. The main changes clarify and optimize how the pipeline state is updated.\r\n\r\nPipeline state advancement improvements:\r\n\r\n* Updated the `operator++` to handle the single-stage (`Stages == 1`) case by toggling `phase_` and incrementing `count_`, ensuring correct phase handling for `Stages == 1` and maintaining the previous logic for `Stages > 0`.\r\n* Added a templated `advance<NumIterations>()` method to efficiently advance the pipeline state by a full traversal when `NumIterations == Stages`, toggling `phase_` and updating `count_`.\r\n\r\nIn both changes skip updating index and make it easier for the compiler to DCE index for the pipeline guarding single-staged resource or multi-staged resource with index updates equal to Stages", "url": "https://github.com/NVIDIA/cutlass/pull/3473"}
{"number": 3520, "title": "Fix incorrect high product in umul128 portable fallback", "state": "open", "created_at": "2026-08-24T16:07:21Z", "updated_at": "2026-09-23T17:14:06Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### Summary\n\nFixes #3492\n\nThe portable fallback of `umul128()` in `include/cutlass/uint128.h` computed an incorrect high product. In the schoolbook decomposition the code added the low 32 bits of both cross products (`p_lh & mask`, `p_hl & mask`) into `r_hi` where the high 32 bits (`>> 32`) belong, double-counting bits that were already consumed at bit position 32 via the mid sum, and it dropped the carry out of the mid sum entirely.\n\nBefore / after:\n\n```cpp\n// before\nuint64_t p_mid = (p_ll >> 32) + (p_lh & mask) + (p_hl & mask);\nuint64_t r_lo  = (p_ll & mask) + (p_mid << 32);\nuint64_t r_hi  = (p_lh & mask) + (p_hl & mask) + p_hh;\n\n// after\nuint64_t mid  = (p_ll >> 32) + (p_lh & mask) + (p_hl & mask);\nuint64_t r_lo = (p_ll & mask) | (mid << 32);\nuint64_t r_hi = p_hh + (p_lh >> 32) + (p_hl >> 32) + (mid >> 32);\n```\n\nMinimal repro before the fix: `umul128(0xFFFFFFFFull, 0x100000000ull, &hi)` leaves `hi = 0xFFFFFFFF` although the exact product `0x00000000FFFFFFFF00000000` has a zero high word.\n\n### Test plan\n\nNew tests in `test/unit/core/uint128.cu` (`host_umul128_high_product`, `device_umul128_high_product`) compare `umul128` against an independent 16-bit-limb schoolbook reference over edge combinations (0, 1, 2, 0xFFFFFFFF, 0x100000000, 2^64-1, 2^63, 0x123456789ABCDEF0 crossed with each other) plus deterministic xorshift random pairs, host and device.\n\nFull run on the fixed code, nvidia/cuda:12.8.1-devel container, nvcc V12.8.93, `-DCUTLASS_NVCC_ARCHS=120a`, RTX 5060 Ti (sm_120a):\n\n```\nNote: Google Test filter = uint128_t.*\n[==========] Running 4 tests from 1 test suite.\n[----------] 4 tests from uint128_t\n[ RUN      ] uint128_t.host_arithmetic\n[       OK ] uint128_t.host_arithmetic (4 ms)\n[ RUN      ] uint128_t.host_umul128_high_product\n[       OK ] uint128_t.host_umul128_high_product (0 ms)\n[ RUN      ] uint128_t.device_arithmetic\n[       OK ] uint128_t.device_arithmetic (233 ms)\n[ RUN      ] uint128_t.device_umul128_high_product\n[       OK ] uint128_t.device_umul128_high_product (1 ms)\n[----------] 4 tests from uint128_t (239 ms total)\n[==========] 4 tests from 1 test suite ran. (239 ms total)\n[  PASSED  ] 4 tests.\n```\n\nFail-first evidence, header fix reverted with these tests present:\n\n- Replaying the exact host test input streams against the old formula: the random stream mismatches on 4096/4096 iterations and the edge grid on 36/64 pairs (smallest failing case: a=1, b=2^32, where hi must be 0 but the old code returns 1).\n- A standalone randomized differential against `unsigned __int128` over 2,000,000 uniformly random 64-bit pairs: old implementation wrong high word on all 2,000,000 pairs, fixed implementation on none.\n\n### Notes\n\n- The pre-existing tests in this file only multiply values below 1024 and check the result truncated to 64 bits, which is why the defect was never caught.\n- No in-tree caller exercises `umul128` today (`FastDivmodU64` uses `operator/` only), so the fix changes no existing behavior.\n- Scope note: this portable path is what every nvcc device compile uses on all host OSes (`CUTLASS_INT128_ARITHMETIC` is host-MSVC-x64 only), plus gcc/clang hosts on x86-64/aarch64 where the native `__int128` path covers `uint128_t::operator*` but not `umul128` itself.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3520"}
{"number": 3522, "title": "Raise on stale source in DSL jit preprocessing", "state": "open", "created_at": "2026-08-24T17:03:52Z", "updated_at": "2026-09-23T17:14:03Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### Summary\n\nFixes #3395\n\nThe jit preprocessor re-reads function sources from disk at first compile and slices at the code object's recorded line number. When the module source changed after import, two silent failure modes existed:\n\n1. the re-read slice no longer starts with the DSL decorator, `transform_function` returned `[]`, and the function was silently skipped from staging;\n2. downstream code replacement failed with the cryptic `ValueError: kernel_fn() requires a code object with 0 free vars`.\n\n`ast_preprocessor.transform_function` now raises `DSLUserCodeError` naming the stale file when the re-read slice does not start with the decorator, covering both cases with one loud error at the right place.\n\nProgrammatic decoration without a matching source decorator also raises loudly now (it was never staged before either); aliased decorators (`jj = cute.jit`) take the same path.\n\n### Test plan\n\nNew `test/python/CuTeDSL/test_stale_source.py` (3 tests, CPU-only): a module whose source changed after import raises the new loud error naming the file; unmodified modules compile normally; a failed-then-restored source compiles again (no preprocessor state poisoning).\n\nRun in a `python:3.12-slim` container against this branch (wheel natives bridged, branch tree first on `sys.path`):\n\n```\nRan 3 tests in 0.356s\n\nOK\n```\n\nWith only `ast_preprocessor.py` reverted to the parent commit, same environment:\n\n```\nRan 3 tests in 0.049s\n\nFAILED (errors=3)\n```\n\nEnd-to-end repro of issue case 2 before the fix:\n\n```\nValueError: kernel_add() requires a code object with 0 free vars, not 2\n```\n\nafter the fix the failure names the stale file and suggests re-importing it.\n\n", "url": "https://github.com/NVIDIA/cutlass/pull/3522"}
{"number": 3441, "title": "feat(sm120): add tileN = 8,16 for the dense F8F6F4 GEMM", "state": "open", "created_at": "2026-08-08T22:32:04Z", "updated_at": "2026-09-23T15:14:12Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`sm120_rr_smem_copy_selector_B` has taken a `TileShapeN` parameter since #3292, and uses it to pick between the 1-, 2- and 4-matrix `ldmatrix` atoms. The block-scaled builder passes the real tile width. The dense F8F6F4 builder never does, so it always resolves to the default of 32 and takes the 4-matrix atom no matter how narrow the tile is.\n\nThe result is that any tile narrower than 32 fails to build, and it fails with a message that never names the tile as the cause:\n\n```\ncute/atom/copy_atom.hpp(206): error: static assertion failed with\n  \"TiledCopy uses too few vals for selected CopyAtom\"\n```\n\nThis forwards `size<1>(TileShape_MNK{})` exactly the way the block-scaled builder already does. 128x16 and 128x8 go from not building to building and computing correctly. It's the same narrow-tile capability #3176 and #3292 added on the block-scaled side, which the dense path never picked up.\n\n### why this cannot change an existing kernel\n\nEvery branch of the selector is `TileShapeN < 16` → x1, `< 32` → x2, else x4, and the default is 32. Any tile with N >= 32 therefore resolves to the identical atom it resolves to today. The only configurations whose behaviour changes are ones that currently fail to compile, so there is no kernel in the library or the profiler that this can move.\n\n### verified\n\nRTX 5070 Ti (sm_120a), CUDA 13.0.3, driver 610.74, reference checks via `TestSmall`:\n\n| tile | before | after |\n| --- | --- | --- |\n| 128x64x64, 128x32x64 | builds, passes | builds, passes |\n| 128x16x64, 128x8x64 | **fails to compile** | builds, passes |\n\nBeyond what's in the test file, I ran a wider matrix while narrowing this down: `e4m3` and `e5m2`, cooperative and pingpong, N = 64/32/16/8, all passing. Pingpong at N=8 is covered in the committed tests specifically because that's the shape that produced reference failures on the block-scaled path during #3292 review.\n\nNon-regression: full SM120 tensorop suite green, **35/35** across `tensorop_f32_sm120` (15), `tensorop_f16_sm120` (14) and `sm120_grouped_gemm_device_tensorop` (6).\n\nTests go into the existing `sm120_gemm_f8_f8_f32_tensor_op.cu` alongside the 128x64x64 case, matching the one-test-per-tile-shape layout the block-scaled narrow-N tests already use, so there's no new file and no CMakeLists change.\n\n### deliberately not included\n\nThe array, sparse and blockwise SM120 builders have the same omission. I left them alone because each needs its own verification matrix and I've only proven the dense F8F6F4 path on hardware I have. Happy to extend in a follow-up if you want them, or to fold them in here if you'd rather it land as one change.\n\nTwo things I checked and they are pre-existing, not affected by this: sub-8-bit operands (`e2m1`) don't build on this builder at N=64 or N=32 either, so that's a separate constraint; and `cutlass_library` emits 128x16 and 128x8 only for the block-scaled generators, so nothing currently shipped was broken by the missing forward.\n\n@depaulmillz you're the closest thing to an owner I can identify from blame here, since #3176 is where small tile N started and this is the sibling builder that didn't get it. Point me at someone else if that's not right.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3441"}
{"number": 3486, "title": "Fix CuTeDSL editable install setup script", "state": "open", "created_at": "2026-08-24T09:07:54Z", "updated_at": "2026-09-23T14:15:48Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3416\n\n`prep_editable_install.py` downloaded only the pure-Python `nvidia-cutlass-dsl` wheel and globbed it for `lib/*.so`. Since the wheel split, that wheel carries no files at all, so the script copied nothing (\"0 lib files, 0 Python files copied\") and a following `pip install -e .` failed with `ModuleNotFoundError: No module named 'cutlass._mlir'`.\n\nThe script now downloads the wheels that actually hold the pieces:\n\n- `nvidia-cutlass-dsl-libs-base==<release>` provides the `cutlass._mlir` python package (dialect bindings glue),\n- `nvidia-cutlass-dsl-libs-cu12==<release>` (or `-cu13` via a new `--cu13` flag, matching `setup.sh` conventions) provides the toolkit-native `_cutlass_ir` binding and `libcute_dsl_runtime.so`,\n- both are pinned to the release version parsed from the main wheel, whose pin itself is taken from this tree's `requirements.txt`.\n\nIt copies `_mlir` into the local `cutlass/` package (base glue merged with the toolkit-native files), bridges this tree's own `_mlir_helpers` into `cutlass/_mlir_helpers` where its relative imports expect them, places runtime libraries under `cuXX/lib` mirroring an installed wheel so the DSL library discovery finds them, removes a stale flat `lib/` left behind by older runs that would shadow the new layout, and fails loudly if no native binding was extracted instead of reporting success.\n\nAdds `python/CuTeDSL/.gitignore` for the generated paths so running the script does not dirty git status.\n\nTest Plan:\n```\n$ cd python/CuTeDSL && python prep_editable_install.py\nINFO: Summary: 65 _mlir files, 9 _mlir_helpers files, 3 runtime library files (cu12/lib)\n$ python prep_editable_install.py --cu13\nINFO: Summary: 65 _mlir files, 9 _mlir_helpers files, 3 runtime library files (cu13/lib)\n$ python -m py_compile prep_editable_install.py    # clean\n\n# sha256 of every extracted file compared against the wheel members: 65+9+3 files, zero mismatches\n\n$ docker run --rm -v <repo>:/src python:3.12-slim bash -c \\\n    \"cd /src/python/CuTeDSL && python prep_editable_install.py && \\\n     pip install numpy cuda-python typing_extensions && pip install -e . && \\\n     python -c 'import cutlass; import cutlass.cute as cute; print(\\\"ok\\\")'\"\nimport ok: /src/python/CuTeDSL/cutlass/__init__.py\ncute ok\n```\n\nBefore this change the same flow ends at `ModuleNotFoundError: No module named 'cutlass._mlir'`.\n\nNote: `numpy`, `cuda-python` and `typing_extensions` are needed at import time but `pyproject.toml` declares no dependencies; that pre-existing gap is untouched here. The editable install remains Linux-only at runtime because the extracted natives are ELF shared objects.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3486"}
{"number": 3507, "title": "[CuTeDSL] Skip consecutive re-emission of the same compiler-opt fragment batch", "state": "open", "created_at": "2026-08-24T14:02:14Z", "updated_at": "2026-09-23T14:15:46Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Symptom\n\n`CUTE_DSL_COMPILER_OPT` compact tokens are re-applied on **every** compilation, and each\nre-application appends its emitted raw pipeline fragments again. With\n`CUTE_DSL_COMPILER_OPT=iket`, compiling the same `@cute.jit` kernel three times in one\nprocess yields (verbatim, `ExtraCompilerOpts.value` of the persistent `cute.compile`\noptions object after each compile):\n\n```\nafter compile 1: 'enable-iket=true'\nafter compile 2: 'enable-iket=true enable-iket=true'\nafter compile 3: 'enable-iket=true enable-iket=true enable-iket=true'\n```\n\nThe full serialized option string — which becomes part of the pass-pipeline string of\nevery compile — grows the same way:\n\n```\nopt-level=3 enable-pyir=false  enable-iket=true  rdc=false enable-assertions=false preserve-line-info=false\nopt-level=3 enable-pyir=false  enable-iket=true enable-iket=true  rdc=false enable-assertions=false preserve-line-info=false\nopt-level=3 enable-pyir=false  enable-iket=true enable-iket=true enable-iket=true  rdc=false enable-assertions=false preserve-line-info=false\n```\n\nWith `warnings{...}`/`remarks{...}` tokens the growth also interleaves with the in-place\n`diagnostic=` canonicalization done at serialization time, e.g.\n`CUTE_DSL_COMPILER_OPT=\"warnings{nvvm} remarks{ptx}\"` over three compiles:\n\n```\n... diagnostic=nvvm,ptx diagnostic=ptx ...\n... diagnostic=nvvm,ptx diagnostic=ptx diagnostic=nvvm diagnostic=ptx ...\n... diagnostic=nvvm,ptx diagnostic=ptx diagnostic=nvvm diagnostic=ptx diagnostic=nvvm diagnostic=ptx ...\n```\n\n## Root cause\n\n- `cute.compile` is a module-global `CompileCallable`\n  (`python/CuTeDSL/cutlass/cute/__init__.py:232`) whose `CompileOptions` outlives\n  individual compiles; `CompileCallable._compile` installs that same persistent object\n  as the DSL's `compile_options` on each call (bracket callables reused across compiles\n  have the same lifetime).\n- Every compilation calls `apply_envar_settings` (`base_dsl/dsl.py:2890`), which\n  re-applies `CUTE_DSL_COMPILER_OPT` via `_apply_opt_string`\n  (`base_dsl/compiler.py:991`), whose `raw_opts` flush appends to\n  `ExtraCompilerOpts.value` unconditionally (`base_dsl/compiler.py:973`).\n- `post_compilation_cleanup` (`base_dsl/dsl.py:2372`) only swaps the DSL's *reference*\n  to a fresh `CompileOptions`; the persistent `cute.compile` object keeps the mutated\n  one and re-installs it on the next compile.\n\n## Consequences\n\n1. The pass-pipeline string consumed by every compile grows without bound. Today this\n   is semantically silent only because the affected flags are boolean-idempotent and\n   MLIR pipeline options are last-wins (`enable-iket=true` twice means the same thing\n   as once).\n2. `CompileOptions.to_str()` is hashed into the module cache key (`get_module_hash`,\n   `base_dsl/dsl.py:1863`). The public `cute.compile` path currently forces\n   `no_cache=True`, so on stock paths this manifests as serialized-option instability\n   rather than an end-to-end cache miss — but any consumer that reuses a\n   `CompileOptions` object with caching enabled gets a **different cache key for the\n   same kernel at different points in the process lifetime** (spurious recompiles,\n   broken cross-process file-cache reuse). Observed cache-key-input digests across\n   three applications of the same option string:\n\n```\n6ffadda2bce977a7c757e816775cbef543bafb0944230abbb9a0085875955886\n937feeb8a00dfe46d546cd31029048a96259388b8c45f870457a7ad65f157af0\n26eabbc60dbcc86108ad2f42ecca73cdd03b94d24c01c1989f28563e7df45981\n```\n\n## Fix\n\nTrack the last raw fragment batch applied to a given `CompileOptions` object\n(`_last_raw_opt_batch`) and skip **only a consecutive re-emission of that same batch**\nin the `raw_opts` flush. Re-appending a batch is only semantically meaningful if a\n*different* batch landed after it (pipeline options are last-wins) — which is exactly\nwhen the incoming batch differs from the last one applied. This is deliberately the\nnarrowest contract change:\n\n- A single application stays **byte-identical** to the previous behavior — including\n  order-sensitive repeats such as `foo=1 foo=2 foo=1`, whose last-wins order must\n  survive verbatim.\n- Alternating option strings keep re-asserting last-wins exactly as before. The\n  persistent options object can serve DSLs with different env namespaces\n  (`CUTE_DSL_*` / `CUTE_EXPERIMENTAL_DSL_*`): e.g. `iket` alternating with\n  `enable-iket=false` still restores effective `enable-iket=true` on the third\n  application.\n- Named options are **not** gated and keep their per-compile replace semantics. That\n  re-application is load-bearing on the persistent object: `link-libraries=` must be\n  reset every compile because the compiler appends per-kernel FFI bitcode sources to it\n  between applications (`base_dsl/dsl.py:2480`).\n- The in-place `diagnostic=` canonicalization performed by\n  `_ensure_compiler_diagnostic_selector` at serialization time cannot defeat the guard,\n  because batches are keyed by what was emitted, not by membership in the (mutated)\n  current value.\n\nBoth narrower and broader alternatives were rejected after review: fragment-level\nfirst-occurrence dedupe collapses intentional repetition within one option string\n(flipping last-wins of `foo=1 foo=2 foo=1`) and re-appends stale `diagnostic=`\nfragments after canonicalization; whole-string or ever-seen apply-once semantics\nsuppress the legitimate re-application that alternating env namespaces and\n`link-libraries` reset rely on.\n\n## Tests\n\n`test/python/CuTeDSL/test_compile_options_env_idempotent.py`:\n\n- 3x `apply_envar_settings` stability for a compact token (`iket`) and a raw\n  `key=value` fragment.\n- Mixed `warnings{nvvm} remarks{ptx}` with `to_str()` called per cycle (the\n  `diagnostic=` canonicalization case): serialized string identical across compiles,\n  with the historical cycle-1 content (`diagnostic=nvvm,ptx diagnostic=ptx`).\n- Cache-key-input stability: sha256 of `to_str()` constant across re-applications.\n- Byte-exact single-application semantics: `foo=1 foo=2 foo=1` keeps all three\n  fragments in order; distinct applications still accumulate.\n- Alternating batches re-assert last-wins (`iket` / `enable-iket=false` / `iket`\n  serializes all three fragments), and a further consecutive repeat does not grow.\n- Named options still re-applied per compile (`opt-level` override chain) and\n  `link-libraries` reset preserved against the per-compile FFI append.\n- End-to-end: a subprocess with `CUTE_DSL_COMPILER_OPT=iket` compiles a kernel three\n  times and asserts the persistent `cute.compile` options object does not grow.\n\nOn stock, the seven idempotency tests fail with the verbatim growth shown above (the\nthree semantics-preservation tests pass unchanged); with the fix all ten pass.\nPre-existing CuTeDSL test files (`test_struct_in_if.py`, `test_numeric_construction.py`,\n`test_export_c_header_shape_type.py`) have identical outcomes with and without the\npatch.\n\n## Relationship to PR #3498\n\nPR #3498 (`llc{...}` token) independently guards its own token against this same\nre-application pattern with a local dedupe on `NvvmOptions`. The two changes are\ntextually and semantically independent (clean merges verified in both orders, combined\ntest suites green). If both land, the local guard in #3498 becomes redundant but\nharmless. This PR does not depend on and does not close #3498.", "url": "https://github.com/NVIDIA/cutlass/pull/3507"}
{"number": 3483, "title": "Fix leftover configure placeholders in cutlass/version.h", "state": "open", "created_at": "2026-08-24T08:38:07Z", "updated_at": "2026-09-23T09:16:27Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3472\n\n`getVersionString()` and `getGitRevision()` in `include/cutlass/version.h` returned the literal strings `@CUTLASS_VERSION@` and `@CUTLASS_REVISION@`. Those were left behind when the `configure_file` step for `version.h` was dropped in 3.4.1 (bbe579a); only `cmake/version_extended.h.in` is generated, so nothing ever substituted them, and even a full CMake build prints `CUTLASS @CUTLASS_VERSION@` in the profiler banner (`tools/profiler/src/options.cu`).\n\nThis builds `getVersionString()` from the CUTLASS_MAJOR/MINOR/PATCH macros and lets `getGitRevision()` return CUTLASS_REVISION, which is empty by default and supplied by `version_extended.h` under `CUTLASS_VERSIONS_GENERATED`. The build-number suffix behavior is unchanged.\n\nBehavior note: the profiler banner changes from `CUTLASS @CUTLASS_VERSION@ ... commit @CUTLASS_REVISION@` to `CUTLASS 4.7.0`, with the commit clause printed only when a real revision exists. Nothing in-tree parses the old string.\n\nAdds `test/unit/core/version.cu` (3 tests) registered in `test/unit/core/CMakeLists.txt`.\n\nTest Plan:\n```\n$ docker run --rm -v .../cutlass:/src:ro nvidia/cuda:12.8.1-devel-ubuntu22.04 \\\n    g++ -std=c++17 -I/src/include host_check.cpp && ./host_check\n  unfixed header: getVersionString=@CUTLASS_VERSION@\n                  getGitRevision=@CUTLASS_REVISION@   -> 4 assertion failures\n  fixed header:   getVersionString=4.7.0 getGitRevision=    -> 0 failures\n\n$ docker run --rm nvidia/cuda:12.8.1-devel-ubuntu22.04   # nvcc V12.8.93 + googletest\n$ nvcc -std=c++17 -I<include> -I<gtest include> -c test/unit/core/version.cu ...\n$ g++ version.o libgtest.a libgtest_main.a -lcudart -o vt && ./vt\n[==========] 3 tests from 1 test suite ran. (1 ms total)\n[  PASSED  ] 3 tests.\n```\nBoth configurations were also compiled with MSVC against a simulated generated `version_extended.h`: defaults give `\"4.7.0\"`/`\"\"`, generated gives `\"4.7.0.5\"`/`\"abc123\"`.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3483"}
{"number": 3484, "title": "Skip stale entries in the DSL shared library list", "state": "open", "created_at": "2026-08-24T08:38:38Z", "updated_at": "2026-09-23T09:16:26Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3462 (part 2: worker-side hard failure)\n\n`get_shared_libs` raised `FileNotFoundError` on the first missing entry of the colon-separated `{prefix}_LIBS` list. The list travels inside job environments (Slurm `sbatch` defaults to `--export=ALL`), so a worker can receive an entry pointing into the submitter's site-packages, and every compile died on it even though another entry named a usable local runtime.\n\nThis skips entries whose file does not exist on this machine and warns about them instead. It keeps raising when nothing in the list survives, since that means there is no usable runtime at all and the compile cannot proceed.\n\nPart 1 of the issue (the import-time `CUTE_DSL_LIBS` mutation that manufactures these stale entries) is a separate mechanism in a separate file and is intentionally not bundled here.\n\nAdds `test/python/CuTeDSL/test_shared_libs.py` (3 CPU-only tests using a stubbed DSL instance).\n\nTest Plan:\n```\n$ docker run --rm python:3.12-slim ...   # editable install of python/CuTeDSL\n$ CUTE_DSL_LIBS=/nonexistent/libcute_dsl_runtime.so:<valid cu12/lib/libcute_dsl_runtime.so> \\\n    python -c \"<compile a trivial @cute.jit function>\"\n  unfixed: FileNotFoundError: [Errno 2] No such file or directory:\n           '/nonexistent/libcute_dsl_runtime.so'\n  fixed:   UserWarning: Skipping /nonexistent/libcute_dsl_runtime.so ... other\n           entries ... point at a usable runtime.\n           COMPILE_OK\n\n$ CUTE_DSL_LIBS=/nonexistent/a.so:/nonexistent/b.so ...\n  fixed:   FileNotFoundError: [Errno 2] No such file or directory:\n           '/nonexistent/a.so:/nonexistent/b.so'\n\n$ python test/python/CuTeDSL/test_shared_libs.py -v\n  Ran 3 tests ... OK   (the mixed-entry test fails on unfixed code)\n```\n\nKnown same-class gaps left out of scope: `cutlass/jax/ffi.py::find_cute_dsl_runtime_library` and `tvm_ffi_provider.py` still return the first name-matching entry without an existence check.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3484"}
{"number": 3663, "title": "Update 4.8 tag", "state": "closed", "created_at": "2026-09-23T05:43:59Z", "updated_at": "2026-09-23T08:49:12Z", "closed_at": "2026-09-23T08:49:11Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T08:49:11Z", "body": "", "url": "https://github.com/NVIDIA/cutlass/pull/3663"}
{"number": 3664, "title": "update 4.8 release", "state": "closed", "created_at": "2026-09-23T08:45:12Z", "updated_at": "2026-09-23T08:49:03Z", "closed_at": "2026-09-23T08:49:03Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T08:49:03Z", "body": "", "url": "https://github.com/NVIDIA/cutlass/pull/3664"}
{"number": 3636, "title": "[CuTeDSL] Fix CLC epilogue TMA-store stage rotation", "state": "closed", "created_at": "2026-09-14T08:36:12Z", "updated_at": "2026-09-23T02:55:18Z", "closed_at": "2026-09-23T02:55:18Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T02:55:18Z", "body": "## Summary\n\n- Track completed CLC work tiles locally in the epilogue warps.\n- Rotate TMA-store shared-memory stages using the epilogue's actual tile count.\n- Prevent short-K dynamic persistent GEMMs from reusing an SMEM stage while its previous TMA store is still in flight.\n\n## Root cause\n\n`ClcDynamicPersistentTileScheduler` is instantiated independently in each specialized warp. Only the scheduler warp calls `advance_to_next_work()`, which increments that instance's `num_tiles_executed`. The TMA, MMA, and epilogue warps consume the CLC response through `get_current_work()`, so their scheduler-local counters remain zero.\n\nThe TMA-store epilogue uses `num_tiles_executed` to select its C-SMEM stage:\n\n```text\nc_buffer =\n    (num_tiles_executed * subtile_count + subtile_idx)\n    % num_c_stages\n```\n\nFor the reproducer below, each M64xN256 output tile has four M64xN64 epilogue subtiles and a three-stage C-SMEM ring. A counter permanently equal to zero restarts every output tile at stage zero and can overwrite a stage still owned by an earlier asynchronous TMA store. Large K hides the race behind mainloop latency; short K exposes it.\n\n## Fix\n\nInitialize a local epilogue tile counter before the persistent loop and increment it after each completed tile. This keeps all epilogue threads in lockstep and avoids changing the public CLC scheduler semantics.\n\n## Reproduction\n\nEnvironment:\n\n- GPU: NVIDIA B30Z, compute capability 10.3\n- A/B/C: FP16/FP16/FP16, FP32 accumulation\n- CTA tile: M64xN256\n- CTA group: 1\n- Cluster: `(1,1)`\n- CLC raster/swizzle: M/1\n- TMA store: enabled\n\nBefore this change:\n\n```text\nMNK: 8256x8448x128\nMismatched elements: 755401 / 69746688 (1.1%)\nGreatest absolute difference: 158.0\n```\n\nAfter this change:\n\n```text\nMNK: 8256x8448x128\nFP32 reference check: PASS\n```\n\n## Validation\n\n- `python -m py_compile .../dense_gemm_persistent_dynamic.py`: PASS\n- FP16 `8256x8448x128`, CTA M64xN256, cluster `(1,1)`, TMA store: PASS\n- FP16 `8192x8192x8192`, CTA M64xN256, cluster `(1,1)`, TMA store: PASS\n  - Smoke runtime: 976.69 us\n- FP16 `4096x4096x128`, CTA M64xN256, cluster `(1,2)`, TMA multicast + TMA store: PASS\n- FP16 `4096x4096x128`, CTA M64xN256, cluster `(1,1)`, direct non-TMA epilogue: PASS\n\nThe full-K target remains performance-neutral; this change only corrects C-SMEM ring indexing in fast tile-turnover cases.\n\n## Scope\n\nThis PR intentionally does not change CLC swizzle-domain padding behavior. That is a separate issue and should be handled independently.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3636"}
{"number": 3639, "title": "[CuTeDSL] Use vectorized codegen for CUDA 12.9 2CTA grouped GEMM", "state": "closed", "created_at": "2026-09-15T05:50:24Z", "updated_at": "2026-09-23T02:55:08Z", "closed_at": "2026-09-23T02:55:08Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-23T02:55:08Z", "body": "The contiguous grouped GEMM example currently selects scalar RMEM operations and O3 for all CUDA 12.9 configurations. Enable vectorized RMEM/O2 for 2CTA while retaining scalar RMEM/O3 for 1CTA. The same predicate continues to select both the RMEM implementation and compilation options; CUDA 13.1+ retains its existing selection.\n\nValidation used the public `nvidia-cutlass-dsl==4.8.0.dev0` wheel (CUDA 12.9 backend) on GB200 at 802 MHz. With M=1024, N=4096, L=16, FP8 E4M3 inputs, BF16 output, a 128x128 MMA tile, fixed M and cold L2:\n\n| Case | Before (us) | After (us) | Latency change |\n|---|---:|---:|---:|\n| 1CTA, K1024, cluster 1x1 | 27.965 | 27.998 | 0.12% |\n| 2CTA, K4096, cluster 2x1 | 100.596 | 90.523 | -10.01% |\n\nMeasurements were interleaved A/B/B/A, with 20 warmups, 100 iterations per trial and five trials per process. Each table entry averages the two process medians. The 1CTA kernel cubins are byte-identical.\n\nTen local reference checks passed: both CTA modes at K1024/K4096, irregular M/N/K and both C-major layouts, plus the baseline controls. Ruff formatting, DSL example lint checks and `git diff --check` passed. This PR changes only the example kernel.", "url": "https://github.com/NVIDIA/cutlass/pull/3639"}
{"number": 3662, "title": "Add opt-in persistent caching to cute.compile", "state": "closed", "created_at": "2026-09-22T21:09:40Z", "updated_at": "2026-09-22T21:18:00Z", "closed_at": "2026-09-22T21:18:00Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- add `use_cache=True` to `cute.compile` to reuse the existing invalidating in-memory and file cache\n- preserve the current uncached default for backward compatibility and custom executor caches\n- keep `CUTE_DSL_NO_CACHE` and PTX/CUBIN/SASS dump behavior authoritative\n- document that frontend tracing still runs to validate the cache key\n\n## Testing\n\n- focused test: `6 passed`\n- Python compile check for modified files\n- CUTLASS DSL 4.8.0 on NVIDIA B300 with CUDA 13.2\n- fresh-process file-cache hit with backend compilation replaced by an exception\n- compiled executor launch and output verification on both the original and changed captured constant\n- default path confirmed to write no cache entries\n- `git diff --check`", "url": "https://github.com/NVIDIA/cutlass/pull/3662"}
{"number": 3661, "title": "[CuTeDSL] Guard SM100 UMMA peer arrival mask on the cluster v-extent", "state": "open", "created_at": "2026-09-22T20:36:00Z", "updated_at": "2026-09-22T21:09:53Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "**Fixes #3658.** Verified on hardware: the reported fault reproduces before this change and is gone after.\n\n## Cause\n\n`_compute_mcast_arrival_mask` unconditionally ORs in image masks taken at the v-peer coordinate `(v ^ 1, m, n, k)`. That peer only exists for a 2CTA tcgen05 MMA (v-extent 2). At v-extent 1 the coordinate is outside the layout's domain, and if mode 0 carries a non-zero stride the peer image runs off the end of the cluster — `(1,4,1,1):(1,1,4,4)` yields mask `0b11111`, setting bit 4 of a 4-CTA cluster. The mask feeds `tcgen05.commit(..., multicast::cluster)`, so that is an out-of-range remote mbarrier access.\n\n## Fix\n\nCompute the peer terms only when the v-extent is not 1 — the same guard `PipelineAsyncUmma.create` already applies, and the semantics of the C++ `detail::calculate_multicast_mask`, which never addresses a rank outside the cluster.\n\n## Verification\n\nGB300 (`sm_103a`), CUDA 13.0, DSL 4.8.0 — 4-CTA cluster through `PipelineTmaUmma.consumer_release`:\n\n| `cta_layout_vmnk` | before | after |\n|---|---|---|\n| `(1,4,1,1):(1,1,4,4)` 1SM | **`cudaErrorLaunchFailure` (719)** | `cudaSuccess` |\n| `(1,4,1,1):(0,1,0,0)` 1SM | `cudaSuccess` | `cudaSuccess` |\n| `((2),2,1,1):((1),2,0,0)` 2SM | `cudaSuccess` | `cudaSuccess` |\n| `((2),1,2,1):((1),0,2,0)` 2SM | `cudaSuccess` | `cudaSuccess` |\n\nNo in-tree caller is affected — `make_layout`, `tiled_divide` and `make_ordered_layout` give extent-1 modes stride 0, which makes the peer terms inert. `dense_gemm.py` is identical before and after for cluster shapes 1x1, 2x1, 1x2, 2x2, 4x1, 1x4 and 4x4 in both 1SM and 2CTA modes.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3661"}
{"number": 3660, "title": "[CuTe] Mask ThrK in TiledMMA::get_layoutC_TV (#3612)", "state": "open", "created_at": "2026-09-22T15:56:33Z", "updated_at": "2026-09-22T23:42:19Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Motivation\n\n#3612: `TiledMMA::get_layoutC_TV()` disagrees with `ThrMMA::partition_C()` when the TiledMMA's thread layout has a K mode (`ThrK > 1`). The disagreement goes beyond wrong offsets: the returned layout maps threads **outside the C tile**. With the issue's example (`UniversalFMA`, thread layout `(2,3,4)`, a 2x3 C tile), `cosize(get_layoutC_TV())` is 24, and 18 of the 24 threads report offsets 6..23.\n\n`get_layoutC_TV()` is also the input of `make_tiled_copy_C` / `make_tiled_copy_C_atom`, so those produce wrong tiled copies for such TiledMMAs as well.\n\n## Root cause\n\n`thrfrg_C` produces a thread mode `(ThrV,(ThrM,ThrN))`, which has no K mode. `get_layoutC_TV()` composes it directly with `thridx_2_thrid`, which maps `thr_idx` to a linear `(ThrV,ThrM,ThrN,ThrK)` index. When `ThrK > 1` that index runs past the thread mode's domain. For threads with K coordinate > 0, the composed layout then either extends beyond the tile or lands on the wrong element.\n\n`get_layoutA_TV()` / `get_layoutB_TV()` avoid this. They first compose their thread mode with a tile that re-inserts the missing thread mode (N for A, M for B) with stride 0 (`atile` / `btile`), so it lines up with `(ThrV,ThrM,ThrN,ThrK)`. `get_layoutC_TV()` has no such step.\n\n## Modifications\n\n- `include/cute/atom/mma_atom.hpp`: add the missing `ctile`, which maps `(ThrV,(ThrM,ThrN))` to `(ThrV,(ThrM,(ThrN,ThrK)))` with stride 0 on `ThrK`, exactly like `atile` / `btile`. Threads that differ only in their K coordinate now map to the same C elements, as in `partition_C()`, which drops `ThrK`.\n- New `test/unit/cute/core/tiled_mma.cpp` (host-only, in `cutlass_test_unit_cute_core`). For every thread and every value, it checks that `get_layout{C,A,B}_TV()` match `partition_{C,A,B}()` and stay inside the tile. It covers UniversalFMA with `ThrK` 1/2/4 (including a K-fastest thread order and a permuted tile) and `SM80_16x8x16_F32F16F16F32_TN` with `ThrK` 1/2/4 and permutations.\n\n## Testing\n\n**Correctness for `ThrK > 1`** (host, g++ 13). The check compares `get_layoutC_TV()(t,v)` with `partition_C` for every thread and value:\n\n| TiledMMA | before | after |\n|---|---|---|\n| UniversalFMA thr (2,3,2) / (2,3,4) | 6 / 18 wrong, all out of tile | 0 |\n| UniversalFMA thr (4,4,2) row-major; (2,2,2) K-fastest | 16 / 4 wrong | 0 |\n| UniversalFMA thr (4,2,2), perm 16x8x4 | 128 wrong (only 32 out of tile, the other 96 are **silently wrong in-tile**) | 0 |\n| SM80 16x8x16 thr (2,2,2) / (1,2,4) / (2,2,2) perm 32x32x32 | 512 / 768 / 1024 wrong | 0 |\n| SM80 8x8x4 F64, SM75 16x8x8, SM70 8x8x4, thr (2,2,2) | 256 / 512 / 256 wrong | 0 |\n\nThe new unit test fails without the fix (2476 mismatches, 8 layouts reaching outside the tile) and passes with it.\n\n**No change for `ThrK == 1`,** which covers all in-tree uses:\n- *Layouts:* I dumped the full mappings of `get_layoutC_TV()`, `make_tiled_copy_C(...)` and `make_tiled_copy_C_atom(...)` (S/D TV layouts and tiler) with and without this change. The configurations were UniversalFMA, SM70/SM75/SM80 atoms with and without permutations, and SM90 GMMA 64x128x16 with 1 and 2 warpgroups, paired with UniversalCopy and `SM90_U32x{2,4}_STSM_N`. Across 14 configurations the 221,615 mapped values are **byte-identical**.\n- *SASS:* I compiled four translation units twice, with only `mma_atom.hpp` differing, for `sm_80`, `sm_90a` and `sm_100a` (CUDA 12.8), and compared `cuobjdump -sass`. The dependency files confirm that each build picked up its own header. The SASS is **identical**:\n\n  | translation unit | uses | kernels | SASS instructions |\n  |---|---|---|---|\n  | `sm90_gemm_f16_f16_f16_tensor_op_f32_cluster_warpspecialized_pingpong.cu` | SM90 TMA epilogue, `make_tiled_copy_C` | 84 | 80,016 = 80,016 |\n  | `examples/50_hopper_gemm_with_epilogue_swizzle.cu` | SM70/80 vectorized `collective::Epilogue`, `make_tiled_copy_C` | 18 | 18,368 = 18,368 |\n  | `test/unit/cute/ampere/cooperative_gemm.cu` | `cooperative_gemm`, `make_tiled_copy_C` | 96 | 61,384 = 61,384 |\n  | `sm80_gemm_f16_f16_f32_tensor_op_f32.cu` | SM80 3.x mainloop with `DefaultEpilogue` (TiledMMA partitioning only) | 12 | 27,344 = 27,344 |\n\n  Together these cover every in-tree caller of `make_tiled_copy_C` / `make_tiled_copy_C_atom` (`sm90_epilogue_tma_warpspecialized.hpp`, `sm70_epilogue_vectorized.hpp`, `cooperative_gemm.hpp`). The `_array` epilogue variants use the same calls.\n\n**Unit tests with this change** (H200):\n\n| target | result |\n|---|---|\n| `cutlass_test_unit_cute_core` (includes the new `tiled_mma.cpp`) | 53 passed |\n| `cutlass_test_unit_cute_ampere` | 228 passed |\n| `cutlass_test_unit_cute_hopper_cooperative_gemm` | 1 passed |\n\nCloses #3612\n", "url": "https://github.com/NVIDIA/cutlass/pull/3660"}
{"number": 3659, "title": "[C++] Fix SM120 blockscaled small-N tiles breaking cutlass_profiler builds (#3646)", "state": "open", "created_at": "2026-09-22T15:22:53Z", "updated_at": "2026-09-22T15:22:53Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Motivation\n\n#3646: `make cutlass_profiler` fails for `CUTLASS_NVCC_ARCHS=120a`:\n\n```\nsm90_common.inl(321): error: static assertion failed with \"BLK_K0 must be a multiple of size<1>(GMMA::Layout_K_INTER_Atom<ElementType>{})\"\n  ... ss_smem_selector<K, float_e5m2_t, _128, _8> ... sm120_get_epilogue_smem_swizzle_layout_atom ...\n  ... cutlass3x_sm120_bstensorop_gemm_ue8m0xe4m3_ue8m0xe2m3_f32_void_e5m2_128x8x128_1x1x1_0_tnt_align128_cooperative_q.cu\n```\n\n## Root cause\n\nThis is a regression from #3176 (\"Small Tile N BlockScaled GEMM\"). That PR added N = 8/16/32/64 CTA tiles to `GenerateSM120_TensorOp_mixed_8bits_UMMA_gemm_with_block_scaled` and `GenerateSM120_TensorOp_fp4_UMMA_gemm_with_block_scaled`. There are two problems:\n\n1. **The small tiles were put first.** Without a kernel filter, `CreateGemmUniversal3xOperator` instantiates only `tile_descriptions[0]` (\"by default, only generate the largest tile\"). Before #3176 that was `[128, 128, 128]`. Now it is `[128, 8, 128]` (cooperative) or `[128, 16, 128]` (ping-pong). A default `120a` configure of current `main` emits 286 non-grouped SM120 blockscaled GEMMs: 172 at N=8, 114 at N=16, and **none** at N=128.\n2. **Every small tile is combined with every data type,** but for row-major C/D with `EpilogueTileAuto` the collective builders only accept some combinations:\n   - **R1:** the epilogue tile N (`min(CTA_N, 32)`, from `sm120_compute_tile_shape_or_override`) must span a multiple of 128 bits for C and D. This comes from the swizzle atom in `ss_smem_selector` (`sm90_common.inl:321`). FP6 is stored in 8-bit containers.\n   - **R2:** FP6 D needs `CTA_N % 128 == 0` (`sm120_builder.inl:158`).\n   - **R3:** block-scaled D needs epilogue tile N / SFD vector size in {1, 2, 4, 8} (`Sm120BlockScaleFactorRowStore`, `sm120_visitor_store_tma_warpspecialized.hpp:72`).\n   - **R4:** 6-bit B needs `CTA_N >= 32`. `sm120_rr_smem_copy_selector_B` (`sm120_common.inl:81`) always picks `SM100_SU6_DU8x16_x4_LDSM_N` for 6-bit B, so smaller N fails with \"TiledCopy uses too few vals for selected CopyAtom\". #3176 added x1/x2 variants only for 4-bit and 8-bit B.\n\n**How many kernels fail (CUDA 12.8, `sm_120a`, the 286 kernels of a default build of current `main`):**\n\n| | N=8 | N=16 | total |\n|---|---|---|---|\n| fail to compile | 155 / 172 | 78 / 114 | **233 / 286** |\n\nThe first error in each failed compile:\n\n| error | count | constraint |\n|---|---|---|\n| `BLK_K0 must be a multiple of size<1>(GMMA::Layout_K_INTER_Atom...)` | 103 | R1 |\n| `CTA tile for FP6 ElementD must have a contiguous extent that is a multiple of 128.` | 75 | R2 |\n| `TiledCopy uses too few vals for selected CopyAtom` | 30 | R4 |\n| `EpilogueTileN should be divisible by SFVecSize` | 25 | R3 |\n\nI spot-checked each error class with CUDA 13.0 and got the same results.\n\n## Modifications\n\n`python/cutlass_library/generator.py`, SM120 blockscaled dense generators only:\n\n- Put the original large tiles back at the front of `tile_sizes_cooperative` / `tile_sizes_pingpong`, followed by the small-N tiles in decreasing N. A default build instantiates `[128, 128, 128]` again, as it did before #3176.\n- Add `sm120_blockscaled_supports_tile_n(tile_n, data_type)`, which encodes R1–R4. Each data type is generated only with the tile N values the builders accept, so filtered builds (for example `CUTLASS_LIBRARY_KERNELS=cutlass3x_sm120*`) no longer contain kernels that cannot compile. The small tiles remain wherever they are valid, for example f32/f16 output at N=8, e5m2 output at N=16, and FP4 output at N=32/64.\n\nNo C++ changes. The x1/x2 variants of `SM100_SU6_DU8x16_*_LDSM_N` exist, so an alternative for R4 would be to extend `sm120_rr_smem_copy_selector_B` the way #3176 did for 4-bit and 8-bit B. That would add new FP6-B small-tile kernels, and I can't validate them without SM120 hardware, so I left the selector as is.\n\n## Testing\n\nI ran compile-only checks, because I have no SM120 GPU (`-DCUTLASS_NVCC_ARCHS=120a`).\n\n**The filter agrees with nvcc on every kernel of the default build.** Of the 286 compile outcomes above, `sm120_blockscaled_supports_tile_n` accepts exactly the 53 that compile and rejects exactly the 233 that fail (286/286 agree).\n\n**Results with this PR:**\n\n| build | result |\n|---|---|\n| default `120a` configure, CUDA 13.0: `ninja cutlass_profiler` | **builds and links**: 1981 objects, 0 failures. The 350 library shards contain 500 SM120 blockscaled GEMMs (286 + 214 grouped), all `128x128x128` |\n| default `120a` configure, CUDA 12.8: `ninja cutlass_profiler` | all 500 SM120 blockscaled GEMMs compile. 5 profiler/reference objects (`gemm_int4.cu`, `conv2d.cu`, `conv3d.cu`, `gemm_int8_interleaved_64.cu`, `device_allocation.cu`) still fail with `subbyte_reference.h(459): error: too few arguments in function call` (`__nv_atomic_load_n`). That is a separate CUDA 12.8 issue, fixed by #3301, and unrelated to this change |\n| `CUTLASS_LIBRARY_KERNELS=cutlass3x_sm120_bstensorop_gemm*_128x{8,16,32,64}x*`, CUDA 12.8: every generated kernel | **1042 / 1042 compile** (597 non-grouped + 445 grouped) |\n\nThe generator has no unit-test harness in-tree, so I did not add a test.\n\nCloses #3646\n", "url": "https://github.com/NVIDIA/cutlass/pull/3659"}
{"number": 3654, "title": "[examples/77] Order each load-warp lane's producer_acquire wait before the TMA it guards", "state": "open", "created_at": "2026-09-21T21:35:03Z", "updated_at": "2026-09-22T05:24:29Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "*Found by an ongoing project that formally verifies GPU kernels by checking their PTX against the PTX memory model.*\n\n> **This is a proposed fix for a suspected bug, not a confirmed one.** We are opening it to ask whether the missing ordering is a real defect or something the code deliberately relies on. If it is the latter, please close this PR.\n\n## Summary\n\nIn the SM100 FMHA load collectives, the whole load warp executes `producer_acquire` (an mbarrier parity wait), but only the elected lane issues the TMA:\n\n```cpp\nuint32_t lane_predicate = cute::elect_one_sync();\npipeline_q.producer_acquire(pipeline_q_producer_state);   // all 32 lanes: empty_barrier.wait(phase)\nif (lane_predicate) {\n  copy(params.tma_load_q.with(*tma_barrier, 0), ...);      // elected lane only\n}\n```\n\nUnder PTX's per-thread semantics, nothing orders a non-elected lane's wait before the elected lane's TMA. Suppose a non-elected lane reaches its wait only after the elected lane has issued the load and the consumer has released the buffer. Q is released once per tile, so by then the empty barrier has moved to its next phase. That lane is still waiting for parity 1, which will never come again, so the load warp hangs.\n\nThis PR adds a `__syncwarp()` after each `producer_acquire` in `sm100_fmha_load_tma_warpspecialized.hpp` and `sm100_fmha_mla_load_tma_warpspecialized.hpp`, 12 sites in total, with a comment explaining why.\n\n## Where the race is\n\nTime runs downwards. Lanes 0 and k are in the same load warp. Line numbers are from `f614dc40`: `load` is `examples/77_blackwell_fmha/collective/sm100_fmha_load_tma_warpspecialized.hpp`, `mainloop` is `sm100_fmha_fwd_mainloop_tma_warpspecialized.hpp`, `pipeline` is `include/cutlass/pipeline/sm100_pipeline.hpp`.\n\n| # | Load warp, elected lane | Load warp, lane k | Consumer warps |\n|---|---|---|---|\n| 1 | `producer_acquire` for Q: waits parity 1 and returns, `load:242`, wait at `pipeline:1100` | **has not reached its wait yet** | blocked in `consumer_wait` |\n| 2 | issues the TMA for Q, `load:245` | | |\n| 3 | | | wait returns, reads Q |\n| 4 | | | `consumer_release` for Q, `mainloop:474`, which flips the empty barrier to its next phase |\n| 5 | | now reaches `producer_acquire` and waits for parity 1, `load:242` | |\n| 6 | | **that phase is already gone, so the wait never returns** | |\n\nRow 5 is the race, and row 6 is the hang. Parity 1 is the producer's initial phase, from `InitialProducerPhase` in `include/cutlass/pipeline/sm90_pipeline.hpp:257`. Q is the dangerous buffer because it is released once per tile; the multi-stage K/V ring gives lanes more slack before the same thing happens.\n\n## Is this a real bug?\n\nThis is a hazard in the PTX model that current ptxas does not expose:\n\n- In the compiled fp16 fwd kernel, the elected-lane copy is emitted as an unconditional, uniform `UTMALDG`. There is no divergence between `producer_acquire` and the TMA, so all 32 lanes execute each wait together. In a reduced reproduction of about 200 million CTAs, all 32 lanes passed every wait in the same cycle.\n- The failure mechanism itself is real. Forcing one lane to execute its parity-1 wait after the consumer's release hangs that lane every time. When the wait is placed in a genuinely divergent region, the hardware scheduler does let the non-elected lanes fall behind, and they hang on the multi-stage K/V ring.\n\nSo the code currently depends on ptxas proving that the warp is converged. The `__syncwarp()` makes that ordering explicit.\n\n**The question we would like answered:** is this a real bug? Concretely, are the load collectives intended to rely on ptxas keeping the load warp converged across `producer_acquire` and the elected-lane TMA, so the lanes can never diverge? If that reliance is deliberate, we are happy to close this, especially given the MLA cost below. If it is incidental, this patch is the minimal fix and we can adjust it however you prefer.\n\n## Cost\n\n- **fwd kernel:** ptxas proves the warp convergent and emits no additional `WARPSYNC`; the binary is in fact slightly smaller.\n- **MLA fwd kernel:** 144 additional `WARPSYNC.ALL` instructions across all instantiations.\n\n## Testing\n\nB200 (sm_100a), CUDA 13.2, driver 595.58.03, fp16, the example's compile flags.\n\n- Upstream CMake test options pass, as on `main`: fmha 8 of 8, MLA fwd 5 of 5.\n\n**Performance** (fp16, head dim 128 for fmha; latent 128 / rope 64 for MLA):\n\n| Kernel and configuration | `main` | This PR | Change |\n|---|---:|---:|---:|\n| fmha fwd, b=4 h=32 s=8192 | 916.7 TFLOPS | 915.7 TFLOPS | −0.1% |\n| fmha fwd, same, causal | 766.0 TFLOPS | 769.2 TFLOPS | +0.4% |\n| MLA fwd, b=4 h=16 s=4096 | 1059.1 TFLOPS | 1057.5 TFLOPS | −0.1% |\n| MLA fwd, same, causal | 836.4 TFLOPS | 835.0 TFLOPS | −0.2% |\n\nMedian of three interleaved runs on an otherwise idle B200. The third repetition partly overlapped another user's job for the MLA, gen and example-93 rows; the median absorbs that one outlier. All differences are within run-to-run noise.\n\nCo-written by Claude.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3654"}
{"number": 3653, "title": "[examples/77] Wait for TMEM statistics stores before publishing them to the correction warpgroup", "state": "open", "created_at": "2026-09-21T21:34:46Z", "updated_at": "2026-09-22T05:24:26Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "*Found by an ongoing project that formally verifies GPU kernels by checking their PTX against the PTX memory model.*\n\n> **This is a proposed fix for a suspected bug, not a confirmed one.** We are opening it to ask whether the missing ordering is a real defect or something the code deliberately relies on. If it is the latter, please close this PR.\n\n## Summary\n\nIn the softmax path of the SM100 FMHA mainloops, the row statistics are written to TMEM and then published to the correction warpgroup:\n\n```cpp\n// softmax warpgroup\ncopy(tiled_tmem_storev, tTMEM_STOREVrS, tTMEM_STOREVtS);   // tcgen05.st (asynchronous)\npipeline_c.producer_commit(pipeline_c_producer_state);      // mbarrier.arrive\n\n// correction warpgroup\npipeline_s0_c.consumer_wait(pipeline_s0_c_consumer_state);\ncopy(tiled_tmem_loadv, tTMEM_LOADVtS0, tTMEM_LOADVrS);      // tcgen05.ld of the same columns\n```\n\nThere is no `tcgen05.wait::st` between the store and the arrive, so the notification is not ordered after the asynchronous TMEM write (PTX ISA §9.7.18.6.4.4). This occurs twice per mainloop:\n\n1. the per-iteration old/new row max, published every KV tile, and\n2. the final row max and row sum, published when `final_call` is set.\n\nThe same function already uses the correct idiom for the P matrix:\n\n```cpp\ncopy(tiled_tmem_store, ...);\ncutlass::arch::fence_view_async_tmem_store();   // tcgen05.wait::st\npipeline_s.consumer_release(pipeline_s_consumer_state);\n```\n\nThis PR adds `cutlass::arch::fence_view_async_tmem_store()` after both statistics stores in the fwd, gen and MLA-fwd mainloops, six sites in total. Both call sites are warp-uniform, as the `.sync.aligned` wait requires.\n\n## Where the race is\n\nTime runs downwards, for the final statistics. Line numbers are from `f614dc40`, in `examples/77_blackwell_fmha/collective/sm100_fmha_fwd_mainloop_tma_warpspecialized.hpp`.\n\n| # | Softmax warpgroup | TMEM store, in flight | Correction warpgroup |\n|---|---|---|---|\n| 1 | `tcgen05.st` of row max and row sum, `:721` | accepted, **not yet visible** | blocked in `consumer_wait` |\n| 2 | *(this PR inserts `tcgen05.wait::st` here)* | | |\n| 3 | `producer_commit`, an `mbarrier.arrive`, `:780` | still in flight | |\n| 4 | | still in flight | wait returns |\n| 5 | | still in flight | `tcgen05.ld` of the same columns, `:1079` and `:1124` |\n| 6 | | store lands | correction has already used the old values |\n\nRow 5 is the race: the arrive at row 3 carries no guarantee that the store at row 1 is visible. The per-iteration row max has the same shape at `:611` and `:613`.\n\n## Is this a real bug?\n\n**The gap survives compilation.** `tcgen05.wait::st` is a real SASS instruction, `FENCE.VIEW.ASYNC.T`, and ptxas does not insert it on its own. In the fp16 `77_blackwell_fmha` binary built from `main`, every one of the 112 `STTM.x2` statistics stores reaches the following `SYNCS.ARRIVE` with no fence in between. With this PR all 112 are fenced; the `FENCE.VIEW.ASYNC.T` count goes from 120 to 232.\n\n**We could not make it fail on B200.** We rebuilt the pattern in a small kernel: store, arrive, then load from the other warpgroup. It compared about 2.65 trillion loaded values against a stale pattern written beforehand. The detector was calibrated to catch a write landing 50 or more cycles after the arrive. No stale value appeared, including with a 1024-deep store backlog, extra warps streaming TMEM stores, and 4 KB stores. Timing `tcgen05.wait::st` in isolation shows the data becomes visible to another warp's `tcgen05.ld` before the store reports completion. The current code relies on that property, which is not documented.\n\n**The question we would like answered:** is this a real bug? The PTX model appears to require the wait, and ptxas does not close the gap for you, so it is still there in the shipped binary. But we could not make it fail, which suggests the mainloop relies on TMEM stores becoming visible to another warpgroup's `tcgen05.ld` before `tcgen05.wait::st` reports completion. Is that ordering guaranteed by the hardware and intended to be relied on here? If so, we will close this. If not, this patch is the minimal fix and we can adjust it however you prefer.\n\n## Testing\n\nB200 (sm_100a), CUDA 13.2, driver 595.58.03, fp16, the examples' compile flags.\n\n- Upstream CMake test options pass, as on `main`: fmha 8 of 8, fmha_gen 5 of 5, MLA fwd 5 of 5.\n\n**Performance** (fp16, head dim 128 for fmha; latent 128 / rope 64 for MLA):\n\n| Kernel and configuration | `main` | This PR | Change |\n|---|---:|---:|---:|\n| fmha fwd, b=4 h=32 s=8192 | 916.7 TFLOPS | 919.3 TFLOPS | +0.3% |\n| fmha fwd, same, causal | 766.0 TFLOPS | 773.1 TFLOPS | +0.9% |\n| MLA fwd, b=4 h=16 s=4096 | 1059.1 TFLOPS | 1057.7 TFLOPS | −0.1% |\n| MLA fwd, same, causal | 836.4 TFLOPS | 836.7 TFLOPS | +0.0% |\n| fmha gen, b=64 h=32 h_k=8 k=8192 | 4.19 TB/s | 4.19 TB/s | 0.0% |\n\nMedian of three interleaved runs on an otherwise idle B200. The third repetition partly overlapped another user's job for the MLA, gen and example-93 rows; the median absorbs that one outlier. All differences are within run-to-run noise.\n\nCo-written by Claude.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3653"}
{"number": 3652, "title": "[examples/93] Wait for the Acc2 tcgen05.ld before releasing TMEM to the MMA warp", "state": "open", "created_at": "2026-09-21T21:34:31Z", "updated_at": "2026-09-22T05:24:17Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "*Found by an ongoing project that formally verifies GPU kernels by checking their PTX against the PTX memory model.*\n\n> **This is a proposed fix for a suspected bug, not a confirmed one.** We are opening it to ask whether the missing ordering is a real defect or something the code deliberately relies on. If it is the latter, please close this PR.\n\n## Summary\n\nIn `EPILOG_warp` (`examples/93_blackwell_low_latency_gqa/tgv_gqa.cuh`), the final Acc2 read is followed by the notification that lets the MMA warp free TMEM:\n\n```cpp\ntmem_load<decltype(bmm2_tmem_load)>(acc2_tmem_addr, tAcc2rAcc2);   // tcgen05.ld (asynchronous)\nif (lane_id < (CTA_dH / NumEpilogWarps)) {\n  // only these lanes consume tAcc2rAcc2 (st.async to the reduction CTA)\n}\narrive_barrier(shared_storage.tmem_allocation_result_barrier);\n```\n\nand in the MMA warp:\n\n```cpp\narrive_barrier(shared_storage.tmem_allocation_result_barrier);\nwait_barrier(shared_storage.tmem_allocation_result_barrier, 1);\ntmem_allocator.free(shared_storage.bmm1_tmem_base_ptr, Acc1_col_max);\ntmem_allocator.free(shared_storage.bmm2_tmem_base_ptr, Acc2_col_max);\n```\n\nWith the example's default `dH = 64` the condition is `lane_id < 16`, so lanes 16–31 of each epilogue warp never use their loaded registers. Nothing orders their `tcgen05.ld` before the arrive that allows the deallocation. PTX ISA §9.7.18.6.4.4 (\"Non-pipelined instructions, different thread\") requires `tcgen05.wait::ld` before synchronizing with another thread. §9.7.18.6.4.5 notes that a register dependency orders only the dependent instruction in the same thread, so even the consuming lanes are not formally covered.\n\nThis PR adds `cutlass::arch::fence_view_async_tmem_load()` (`tcgen05.wait::ld`) right after the load. That is the idiom this kernel already uses for Acc1 before it signals the next BMM1 (`tgv_gqa.cuh:1325`).\n\n## Where the race is\n\nTime runs downwards. Line numbers are from `f614dc40`. Both epilogue lanes below are in the same warp; the MMA warp is the TMEM owner.\n\n| # | Epilogue lanes 0–15 | Epilogue lanes 16–31 | MMA warp (TMEM owner) |\n|---|---|---|---|\n| 1 | `tcgen05.ld` Acc2 issued, `tgv_gqa.cuh:1514` | `tcgen05.ld` Acc2 issued, `:1514` | blocked in `wait_barrier`, `:882` |\n| 2 | read the registers, `:1516`–`:1527` | **never read them**, so no dependency forces the load to finish | |\n| 3 | `arrive_barrier`, `:1530` | `arrive_barrier`, `:1530` | |\n| 4 | | | wait returns: all 160 arrivals in, `:882` |\n| 5 | | **load may still be in flight** | `tmem_allocator.free`, `:885`–`:886` |\n| 6 | | load completes against TMEM this CTA has already released | |\n\nRow 5 is the race. This CTA never reuses those columns itself, since the scheduler is non-persistent and the CTA exits just after the free; the columns go back to the SM's allocator, which may hand them to another CTA, and the allocation lock is released early at `:819` precisely so that can happen. Note also that rows 1 to 2 do not save the consuming lanes: PTX ISA §9.7.18.6.4.5 says a register dependency orders only that instruction in that same thread, not another thread's view of the TMEM.\n\n## Is this a real bug?\n\nWe could not produce a failure on hardware, and on CUDA 13.2 the change does not alter the generated code:\n\n- ptxas already puts the `LDTM` scoreboard wait on the `lane_id < 16` branch, so the whole warp waits for the load before it diverges. Patched and unpatched `93_blackwell_low_latency_gqa` SASS differ only in `NOP` padding.\n- A reduced reproduction of this barrier protocol on B200 ran about 14 billion CTAs, with and without `-Xptxas -g-tmem-access-check` and compute-sanitizer. It produced no guardrail trap, sanitizer error, or wrong value.\n\nSo the current code is safe because of how ptxas schedules this particular shape, not because the program orders it. The patch states the PTX-required ordering explicitly, and the generated code does not change.\n\n**The question we would like answered:** is this a real bug? Concretely, is `EPILOG_warp` intended to rely on ptxas placing the `LDTM` scoreboard wait ahead of the `lane_id` branch, so that the non-consuming lanes' loads are complete before the arrive? If that reliance is deliberate and guaranteed, we are happy to close this. If it is incidental, this patch is the minimal fix and we can adjust it however you prefer.\n\n## Testing\n\nB200 (sm_100a), CUDA 13.2, driver 595.58.03, built with `nvcc -O3 -std=c++17 -gencode arch=compute_100a,code=sm_100a`.\n\n- Builds cleanly. SASS is identical to `main` apart from `NOP` padding.\n- Performance is identical by construction; measured numbers are below.\n\n**Performance** (`93_blackwell_low_latency_gqa` defaults: kvH=8, qH=64, kvL=2048, dH=64; time per iteration):\n\n| Configuration | `main` | This PR | Change |\n|---|---:|---:|---:|\n| `--mode 0` (contiguous KV cache) | 4.91 µs | 4.91 µs | −0.1% |\n| `--mode 1` (paged KV cache) | 5.25 µs | 5.26 µs | +0.1% |\n\nMedian of three interleaved runs on an otherwise idle B200. The third repetition partly overlapped another user's job; the median absorbs that one outlier.\n\nCo-written by Claude.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3652"}
{"number": 3526, "title": "Fix dilated Conv2d/Conv3d output size calculation", "state": "open", "created_at": "2026-08-24T18:59:15Z", "updated_at": "2026-09-22T04:16:47Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- compute Conv2d P/Q from the effective dilated filter extent, `(filter - 1) * dilation + 1`\n- apply the same correction to Conv3d Z for both symmetric and asymmetric padding constructors\n- add regression tests for dilated 2D/3D problems, asymmetric padding and stride, and 1x1 filters\n\n## Background\n\nThe existing formula subtracts `filter * dilation`. It agrees with the standard convolution output-size formula only when dilation is 1. For dilation greater than 1 it undersizes the output, and a 1x1 filter is incorrectly made smaller by increasing dilation.\n\n## Contribution roles\r\n\r\nI led the contribution direction and acceptance criteria, reviewed and approved\r\nthe final design, and reviewed every changed line. I understand the change and\r\nown the final design decision and the submission. AI assistance was used for\r\nrepository research, root-cause analysis, implementation preparation, and\r\nautomated validation execution.\r\n\r\n## Testing\n\n- reproduced the bug on the unmodified main branch: 2D output `3x4` instead of `4x6`, and 3D output `4x3x4` instead of `5x4x6`\n- manually compiled and ran `test/unit/conv/problem_size.cpp` with MSVC 19.44, CUDA 12.9 headers, CCCL headers, and GoogleTest v1.14.0\n- all 5 regression tests passed\n\nFixes #3502\r\n\r\n\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3526"}
{"number": 3531, "title": "Fix packed uint1b logical array reductions", "state": "open", "created_at": "2026-08-25T09:03:21Z", "updated_at": "2026-09-22T04:16:35Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- fix the `uint1b_t` logical OR reduction specialization to accept `logical_or`\n- use the correct identities and packed-byte checks for logical AND and OR\n- mask padding bits in partial trailing bytes\n- add regression coverage across logical bit and storage boundaries\n\n## Background\n\nThe logical OR specialization currently cannot be called through `ReduceArray` because its operator takes `logical_and`. Even when called directly through the specialization, its `true` identity makes an all-zero array reduce to true.\n\nThe logical AND specialization checks whether each storage byte is nonzero rather than whether every valid bit is set. For example, `0x7f` incorrectly reduces to true. Partial trailing bytes also need a mask so padding bits do not participate in either reduction.\n\nThis change keeps the packed-byte path: full bytes are reduced directly, while only the valid low bits of a partial trailing byte are considered.\n\n## Contribution roles\r\n\r\nI led the contribution direction and acceptance criteria, reviewed and approved\r\nthe final design, and reviewed every changed line. I understand the change and\r\nown the final design decision and the submission. AI assistance was used for\r\nrepository research, root-cause analysis, implementation preparation, and\r\nautomated validation execution.\r\n\r\n## Testing\n\n- reproduced the public logical OR helper compilation failure on the unmodified main branch\n- reproduced incorrect runtime results on main: AND of `[1,1,1,1,1,1,1,0]` returned 1, and OR of an all-zero array returned 1\n- manually compiled and ran the new regression source with MSVC 19.44, CUDA 12.9 headers, CCCL headers, and GoogleTest v1.14.0\n- 2/2 tests passed, exhaustively checking each logical position for `N = 1, 7, 8, 9, 15, 16, 32`, including padding-bit isolation\n- `git diff --check`\n\nFixes #3510\r\n\r\n\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3531"}
{"number": 3648, "title": "[CuTeDSL] Add BF16 support to Hopper dense GEMM examples", "state": "open", "created_at": "2026-09-21T20:47:58Z", "updated_at": "2026-09-22T03:28:17Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Motivation\n\nBF16 is the dominant dtype for LLM training and inference, and SM90 WGMMA supports it natively (`wgmma.mma_async ... .f32.bf16.bf16`). The CuTe DSL primitive already allows it: `warpgroup.MmaF16BF16Op` accepts `BFloat16` A/B with `Float32` accumulation. But the Hopper `dense_gemm.py` and `dense_gemm_persistent.py` examples rejected BF16 in `is_valid_dtypes`, so the most common Hopper GEMM configuration could not be run with the reference examples. The Blackwell dense GEMM examples already support BF16.\n\n## Modifications\n\n- `dense_gemm.py`, `dense_gemm_persistent.py`:\n  - Allow `BFloat16` A/B, with `Float32` accumulation only. This matches the WGMMA / `MmaF16BF16Op` constraint. `BFloat16` with `Float16` accumulation is still rejected.\n  - Allow `BFloat16` output with `Float32` accumulation.\n  - Update the module/class docstrings (supported types, accumulator rules, alignment) and the stale `TypeError` messages.\n- New `test/examples/CuTeDSL/hopper/test_dense_gemm.py`. Neither kernel had tests before. It uses the existing L0/L1 structure and covers both kernels × {Float16, BFloat16}:\n  - L0 compile: all tile shapes (128x256, 128x128, 128x64, 64x64)\n  - L1 correctness against `torch.einsum`: tile shapes, cluster shapes (1x1, 2x1, 1x2, 2x2, including multicast), all 8 A/B/C major combinations (batched, L=2), output dtypes {F16, BF16, F32}, non-tile-aligned sizes (1000x1032x264, 136x72x72x3)\n  - Validation: BF16 + F32 acc accepted; BF16 + F16 acc and mixed BF16/F16 rejected\n\nThe kernel code itself is not changed. BF16 uses the same code path as FP16, and only the operand type of the MMA/TMA atoms differs.\n\n## Correctness\n\nNVIDIA H200 (SM90a), driver 580.159.03, `nvidia-cutlass-dsl[cu13]==4.8.0.dev0`, torch 2.14.0+cu130:\n\n```\n$ cd test/examples/CuTeDSL\n$ pytest hopper/test_dense_gemm.py -q --target-cc 90 --test-level L2\n102 passed in 127.16s\n```\n\nRegression check: FP8 (E4M3FN → F16) and INT8 (→ Int32) still PASS on both modified kernels.\n\nSASS check (`CUTE_DSL_KEEP=cubin`, `cuobjdump -sass`, tile 128x256):\n\n| | PTX | SASS | # MMA | # TMA/SYNCS |\n|---|---|---|---|---|\n| FP16 | `wgmma.mma_async.sync.aligned.m64n256k16.f32.f16.f16` | `HGMMA.64x256x16.F32` | 8 | 38 |\n| BF16 | `wgmma.mma_async.sync.aligned.m64n256k16.f32.bf16.bf16` | `HGMMA.64x256x16.F32.BF16` | 8 | 38 |\n\n## Performance\n\nH200, A/B/C K-major/K-major/N-major, F32 accumulation, tile 128x256, cluster 1x1, hot L2, 10 warmup + 50 timed iterations. cuBLAS is `torch.matmul` (BF16) on the same shape. Units are TFLOP/s.\n\n`dense_gemm_persistent.py`:\n\n| M, N, K | DSL FP16 | DSL BF16 | cuBLAS BF16 | DSL BF16 / cuBLAS |\n|---|---|---|---|---|\n| 4096, 4096, 4096 | 866.3 | 865.8 | 800.5 | 1.08x |\n| 8192, 8192, 8192 | 827.7 | 836.6 | 748.3 | 1.12x |\n| 4096, 14336, 4096 | 891.0 | 892.5 | 789.8 | 1.13x |\n| 4096, 4096, 14336 | 921.9 | 919.4 | 806.0 | 1.14x |\n| 16384, 8192, 1024 | 793.9 | 788.8 | 732.3 | 1.08x |\n| 8192, 4096, 512 | 638.0 | 636.8 | 627.7 | 1.01x |\n\nBF16 performance matches FP16 within run-to-run noise (about ±2%), as expected because BF16 and FP16 WGMMA have the same throughput. The non-persistent `dense_gemm.py` reaches 815–905 TFLOP/s on the square/LLM shapes (1.04–1.11x cuBLAS) and 0.96x cuBLAS on the short-K 16384x8192x1024 shape, where per-tile prologue/epilogue is not amortized.\n\n## Testing notes\n\nTo run the public example tests outside NVIDIA CI I needed `--target-cc 90`, because `test/utils/test_sharding.py` otherwise imports `device_info`, which is not in the repository. I also needed `pytest<9`, because the plugin imports `_pytest.assertion.util.running_on_ci`. These are unrelated to this PR.\n\n\n", "url": "https://github.com/NVIDIA/cutlass/pull/3648"}
{"number": 3656, "title": "[CuTe] Reject Copy_Atom copies whose src/dst size does not match the CopyOp (#3488)", "state": "open", "created_at": "2026-09-21T23:13:37Z", "updated_at": "2026-09-22T03:27:59Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Motivation\n\n#3488: `copy_if(Copy_Atom<...>, prd, src, dst)` with a smaller `dst` silently writes out of bounds. I reproduced it host-only with g++ 13 and ASan, using the issue's example (`UniversalCopy<uint128_t>` over `float`, `src` (4,3), `dst` (2,3)):\n\n```\nERROR: AddressSanitizer: stack-buffer-overflow ... WRITE of size 16\n    #3 cute::copy_unpack<...>        include/cute/atom/copy_traits.hpp:133\n    #4 cute::Copy_Atom<...>::call    include/cute/atom/copy_atom.hpp:149\n```\n\nOne correction to the issue: the sibling `copy(Copy_Atom<...>{}, src, dst)` is **not** safe either. The same repro through `copy` gives the same 16-byte out-of-bounds write (`copy_atom.hpp:103`). Only the atom-less `copy(src, dst)` avoids the instruction.\n\n## Root cause\n\n`Copy_Atom::call()` dispatches to `copy_unpack()` as soon as **either** slice has the instruction's element count (`is_constant<NumValSrc, size(src)> || is_constant<NumValDst, size(dst)>`). `copy_unpack()` then recasts both slices to the CopyOp's register type and only checks the **register count**:\n\n```cpp\nTensor rD = recast<RegTypeDst>(dst);\nCUTE_STATIC_ASSERT_V(size(rD) == Int<RegNumDst>{}, ...);\n```\n\n`recast<>` rounds a partial register up to a whole one. A 2-float (64-bit) slice recast to `uint128_t` therefore still has size 1, the assertion passes, and the 128-bit instruction writes past the slice.\n\n## Modifications\n\n- `include/cute/atom/copy_traits.hpp`: the generic `copy_unpack()` now also requires that `src` and `dst` hold **exactly** as many bits as the CopyOp's registers (`size(t) * sizeof_bits_v<value_type> == RegNum * sizeof_bits_v<RegType>`). The check is done per side, so converting ops whose src and dst registers differ are unaffected. A mismatch now fails to compile:\n  ```\n  error: static assertion failed: Copy_Traits: dst tensor size does not match this CopyOp.\n         The copy would access memory outside the dst tensor.\n  ```\n  The `copy_unpack` overloads for the SM80/SM90/SM100 TMA, im2col and async ops are not changed.\n- `include/cute/algorithm/copy.hpp`: the elementwise `copy_if(pred, src, dst)` loops over `size(dst)` while indexing `pred` and `src`. It now statically rejects `src`/`dst` of different static sizes. Dynamic sizes are left alone, because there is no device-side error channel.\n- `test/unit/cute/core/tensor_algs.cpp`: host tests for `copy(Copy_Atom)`, `copy_if(Copy_Atom)` with one rest-slice masked off, and the elementwise `copy_if`. The rejection itself is a `static_assert` in a function body, so it cannot be a runtime or SFINAE test. It is shown by the repro below.\n\n## Testing\n\nHost repro from the issue, g++ 13.3 + ASan:\n\n| | `copy_if(Copy_Atom)` | `copy(Copy_Atom)` | correctly sized (4,3)→(4,3) |\n|---|---|---|---|\n| before | OOB write (ASan) | OOB write (ASan) | OK |\n| after | **compile error** (message above) | **compile error** | OK, ASan clean, values correct |\n\nThis is a compile-time check in CuTe core, so I also confirmed that it does not reject any in-tree use. CUDA 12.8, `-DCUTLASS_NVCC_ARCHS=\"80;90a;100a\"`. Everything below compiles with the new asserts and passes on NVIDIA A800 (SM80) and H200 (SM90):\n\n| target | result |\n|---|---|\n| `cutlass_test_unit_cute_core` (includes the 3 new tests) | 51 passed |\n| `cutlass_test_unit_cute_layout` / `_turing` | 4 / 3 passed |\n| `cutlass_test_unit_cute_ampere` | 228 passed (A800 and H200) |\n| `cutlass_test_unit_cute_ampere_tiled_cp_async` | 4 passed (A800 and H200) |\n| `cutlass_test_unit_cute_hopper_{stsm, bulk_load, bulk_store, tma_load, tma_store, cooperative_gemm}` | 1 / 3 / 3 / 9 / 7 / 1 passed (H200) |\n| `cutlass_test_unit_gemm_device_tensorop_f32_sm80_3x` (3.x collective GEMM) | 10 passed (A800 and H200) |\n| `cutlass_test_unit_gemm_device_tensorop_cluster_multicast_sm90` | 92 passed (H200) |\n| `cutlass_test_unit_gemm_device_tensorop_gmma_rs_warpspecialized_sm90` | 16 passed (H200) |\n\n**SM100 (compile-only; I have no SM100 hardware).** *Correction:* an earlier version of this description said the Blackwell tests were compiled, but `test/unit/cute` has no SM100-specific tests, so that claim was wrong. I then compiled these SM100 GEMM targets with the new asserts (CUDA 12.8, `100a`): `cutlass_test_unit_gemm_device_tensorop_sm100_f16xf16`, `_tensorop_sm100_f8xf8`, and `_bstensorop_sm100_nvf4xnvf4`. They cover the TMEM-load epilogues, TMA stores and the UTCCP scale-factor copies.\n\n- **9 of 13 objects compile, and the new asserts do not fire in any of them.**\n- **4 objects fail with `include/cutlass/subbyte_reference.h(459): error: too few arguments in function call`** (`__nv_atomic_load_n` under CUDA 12.8). This is unrelated to this PR. I compiled one of the failing objects (`f16_f16_f16_f16_fusion.cu`) with `main`'s unmodified CuTe headers and got the same error. It is addressed by #3301.\n\nThe TMA, TMEM (load/store/UTCCP) and im2col copies have their own `copy_unpack` overloads, so they do not go through the changed generic function. That includes the new SM107 collectives. I could not compile SM107 (it needs CUDA 13.4).\n\nNot covered: `cutlass_test_unit_cute_volta` contains 0 tests for these architectures. I did not build `cutlass_profiler` or the full kernel library.\n\n## Performance\n\nNone. The changes are `static_assert`s only, and no code generation changes.\n\nCloses #3488\n\n", "url": "https://github.com/NVIDIA/cutlass/pull/3656"}
{"number": 3645, "title": "v4.8 update", "state": "closed", "created_at": "2026-09-20T15:12:31Z", "updated_at": "2026-09-22T02:43:01Z", "closed_at": "2026-09-22T02:43:01Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-22T02:43:01Z", "body": "", "url": "https://github.com/NVIDIA/cutlass/pull/3645"}
{"number": 3655, "title": "docs: explain CuTe DSL AOT error handling", "state": "open", "created_at": "2026-09-21T22:06:53Z", "updated_at": "2026-09-21T22:06:53Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nDocument how callers should interpret and report non-zero return codes from the exported CuTe DSL AOT C API, as requested in #3562.\n\n## Changes\n\n- Added an AOT error-handling section describing `cudaSuccess` and `cudaError_t` status values.\n- Documented `cudaGetErrorName()` and `cudaGetErrorString()` for symbolic and human-readable diagnostics.\n- Clarified the distinction between `CuteDSLRT_Error_t` loader errors and the CUDA status returned by the exported wrapper.\n- Added a C++ example that checks the wrapper result.\n\n## Testing\n\n- `git diff --check` ✅\n- Verified the documented return-code and error-helper behavior against the AOT C header generator and the maintainer's issue guidance.\n- A full documentation build was not run locally because the repository's documentation tooling is not installed in this checkout.\n\n## Notes\n\nThis is a documentation-only change. The implementation follows the maintainer's clarification that non-zero AOT return codes are `cudaError_t` values handled through the CUDA Runtime API. AI assistance was used in preparing this contribution; the submitted change has been reviewed for correctness.\n\nFixes #3562\n", "url": "https://github.com/NVIDIA/cutlass/pull/3655"}
{"number": 3651, "title": "[C++] Fix EpilogueDirectStore warp index decomposition; reject sliced-K (#3496)", "state": "open", "created_at": "2026-09-21T21:29:13Z", "updated_at": "2026-09-21T21:29:13Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Motivation\n\n#3496: `EpilogueDirectStore` computes the warp's (m, n) position in the threadblock tile with\n\n```cpp\nint warp_id_mn = warp_idx % (WarpCount::kM * WarpShape::kN);   // WarpShape::kN is the warp tile's N extent\n```\n\n`EpilogueBase` uses `WarpCount::kM * WarpCount::kN`, the number of warps in the MN plane.\n\nI tried to build a failing config for the case described in the issue, `WarpCount::kN > WarpShape::kN`, and could not make a valid one. An 8-wide warp tile (for example 128x128x32 CTA with 128x8x32 warps) also fails with the **default** epilogue, so that config is not supported by the SM80 mainloop/epilogue in general. An N=16 warp tile would need at least 17 warps along N, which exceeds the register file. So that path is latent, as the issue says.\n\nThe typo **is** reachable through sliced-K (`kPartitionsK > 1`), which `EpilogueDirectStore` does not reject. For a 64x64x64 threadblock with 64x64x32 warps, WarpCount = (1, 1, 2). The second K-partition warp gets `warp_id_mn = 1 % (1 * 64) = 1`, so `warp_n = 1`, and it stores its partial sums into columns 64..127, the **neighbouring threadblock's output tile**. Those writes stay inside D, so compute-sanitizer reports nothing and the results are silently wrong. Even with the correct modulo, sliced-K cannot work with this epilogue: it stores accumulators straight from registers and has no cross-warp reduction over K partitions, so both K-partition warps would write different partial sums to the same elements.\n\n## Modifications\n\n`include/cutlass/epilogue/threadblock/epilogue_direct_store.h`:\n- Use `WarpCount::kM * WarpCount::kN` for the MN-plane warp decomposition, matching `EpilogueBase`.\n- Add `static_assert(kPartitionsK == 1, ...)` so that sliced-K configurations fail at compile time with a clear message, instead of producing wrong results.\n\n## Testing\n\nBuilt with CUDA 12.8, `-DCUTLASS_NVCC_ARCHS=\"80;90\"`; run on NVIDIA H200 (SM90) and A800 (SM80).\n\n- Existing test `SM80_Device_GemmUniversal_DirectStore_f16n_f16t_f32n_tensor_op_f32.128x128x32_64x64x32` (`cutlass_test_unit_gemm_device_tensorop_f32_sm80`): **passes** on both GPUs with this change.\n- Sliced-K diagnostic (not included in the PR): 64x64x64 threadblock / 64x64x32 warps / `DefaultEpilogueDirectStore`, run through `TestAllGemmUniversal`:\n  - Same mainloop with the default epilogue: passes.\n  - Direct store, original header: **fails** (memcheck: 0 errors, i.e. wrong values written inside D).\n  - Direct store, modulo fix only: **fails** (no K-partition reduction).\n  - Direct store, this PR: **compile error**:\n    ```\n    epilogue_direct_store.h(145): error: static assertion failed with \"The direct store epilogue does not reduce\n    accumulators across K partitions (sliced-K). Use a warp shape whose K extent equals the threadblock shape's K extent.\"\n    ```\n\nI did not add a runtime test for the `WarpCount::kN > WarpShape::kN` case, because I could not construct a supported configuration that reaches it (see above).\n\nCloses #3496\n", "url": "https://github.com/NVIDIA/cutlass/pull/3651"}
{"number": 3650, "title": "[C++] Fix Stream-K broadcast epilogue reduction; document broadcast vector convention (#3540)", "state": "open", "created_at": "2026-09-21T21:07:08Z", "updated_at": "2026-09-21T21:16:53Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Motivation\n\n#3540 reports that the ColumnMajor-output specializations of `GemmUniversalWithBroadcast` / `GemmUniversalStreamkWithBroadcast` misindex the broadcast vector. I added the missing test coverage for the vector under both output layouts and found two things:\n\n1. **The ColumnMajor behavior is self-consistent, but it was not documented.** The specialization transposes the problem, so the underlying row-major kernel broadcasts `V` along *its* columns, which are the **rows** of the column-major `D`. The vector therefore has **M** elements, and the epilogue bounds-checks against M. With a length-M vector, results are correct and memcheck is clean. The OOB reads described in the issue only happen when a caller assumes a length-N vector. Rejecting `ptr_Vector` or changing the indexing would break callers that already use the length-M convention. A per-column vector for column-major D is also not expressible with this epilogue. So this PR takes the issue's other suggestion and **documents the convention**.\n2. **A real bug in `GemmUniversalStreamkWithBroadcast`, independent of layout.** It produces wrong results whenever Stream-K splits the K loop across thread blocks, including plain RowMajor output with the default `LinearCombinationBiasElementwise` op. There was no in-tree test of this class.\n\n## Root cause (Stream-K)\n\nIn separate-reduction mode, each block reduces one accumulator fragment `reduce_fragment_idx` of an output tile, and then `EpilogueStreamkWithBroadcast::reduce()` calls `EpilogueWithBroadcast::reduce()`. The single-source overload of that function stored the fragment through `destination_iterator` / `tensor_iterator` without advancing them to `reduce_fragment_idx`:\n\n```cpp\ndestination_iterator.store(frag_Z);   // always fragment 0\n++destination_iterator;\n```\n\nAs a result, every fragment was written over fragment 0: most of D and T were never written, and fragment 0 got whichever peer wrote last. The source iterator *was* advanced (`source_iterator += reduce_fragment_idx`), so C was read from the right place. The dual-source overload in the same file and `Epilogue::reduce()` both do `iterator += reduce_fragment_idx` before storing. This PR makes the single-source overload match them. The function is only reached through `EpilogueStreamkWithBroadcast`.\n\n## Modifications\n\n- `epilogue_with_broadcast.h`: advance `destination_iterator` / `tensor_iterator` by `reduce_fragment_idx` before storing in the single-source `reduce()`.\n- `gemm_universal_with_broadcast.h`, `gemm_universal_streamk_with_broadcast.h`: document how the vector is indexed. It always runs along the contiguous dimension of D: N elements for RowMajor, M elements for ColumnMajor. Also document what `ldr` / `batch_stride_Vector` mean, explain why the ColumnMajor specialization uses M, and complete the truncated \"Supports\" doc comment.\n- New `test/unit/gemm/device/gemm_universal_with_broadcast_vector_sm80.cu` (added to `cutlass_test_unit_gemm_device_broadcast`):\n  - Inputs are small integers, so the half-precision results must match the reference **bit-exactly**, and every element of both **D (Z)** and **T** (`ptr_Tensor`) is checked.\n  - The vector is allocated with exactly its documented length, so compute-sanitizer flags any overread.\n  - Covers `GemmUniversalWithBroadcast` × {RowMajor, ColumnMajor} × {M>N, M<N, batched L=2 with `batch_stride_Vector`}, and `GemmUniversalStreamkWithBroadcast` × {RowMajor, ColumnMajor} × {unsplit K=64, Stream-K split K=512 and 520x392x1024, data-parallel `avail_sms=1`}.\n\n## Correctness\n\nBuilt with CUDA 12.8, `-DCUTLASS_NVCC_ARCHS=\"80;90\"`; run on NVIDIA H200 (SM90) and A800 (SM80).\n\nBefore the fix (new test, unmodified headers):\n```\n[       OK ] SM80_Device_GemmWithBroadcast_f16t_f16n_f16t_tensor_op_f16.128x128_32x3_64x64x32_16x8x16\n[       OK ] SM80_Device_GemmUniversalWithBroadcast_f16t_f16n_f16t_tensor_op_f32.vector_per_column\n[       OK ] SM80_Device_GemmUniversalWithBroadcast_f16t_f16n_f16n_tensor_op_f32.vector_per_row\n[  FAILED  ] SM80_Device_GemmUniversalStreamkWithBroadcast_f16t_f16n_f16t_tensor_op_f32.vector_per_column\n             (32981 / 35904 mismatches at 136x264x512; 197730 / 203840 at 520x392x1024)\n[  FAILED  ] SM80_Device_GemmUniversalStreamkWithBroadcast_f16t_f16n_f16n_tensor_op_f32.vector_per_row\n```\nThe Stream-K cases pass with `avail_sms=1` (no peer reduction) and with K=64 (no split), which isolates the failure to the peer-reduction path.\n\nAfter the fix:\n```\n$ ./test/unit/gemm/device/cutlass_test_unit_gemm_device_broadcast\n[  PASSED  ] 5 tests.                                   # H200 and A800\n$ compute-sanitizer --tool memcheck ... --gtest_filter='*vector_per*'\n[  PASSED  ] 4 tests.\n========= ERROR SUMMARY: 0 errors                       # H200 and A800\n$ ... --gtest_filter='*Streamk*' --gtest_repeat=50\n50 / 50 iterations passed                               # A800\n```\n\n## Performance\n\nNo performance impact. The fix adds one iterator offset per reduced fragment in the Stream-K separate-reduction path, and the documentation changes are comments only.\n\nCloses #3540\n\n\n", "url": "https://github.com/NVIDIA/cutlass/pull/3650"}
{"number": 3649, "title": "[CuTeDSL] Fix logging of disk-cache load failures", "state": "open", "created_at": "2026-09-21T20:53:45Z", "updated_at": "2026-09-21T20:54:52Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fix logging error when disk cache loading fails (formatting args broke on already-formatted string).\r\n\r\nInclude exception directly in the warning, matching `dump_cache_to_path()`.", "url": "https://github.com/NVIDIA/cutlass/pull/3649"}
{"number": 3647, "title": "[CuTe DSL] Fix cross-CTA barrier scope and missing SFB wait in 2cta_mma_blockscale_m128 example, issue #3643", "state": "open", "created_at": "2026-09-21T06:23:45Z", "updated_at": "2026-09-21T06:23:45Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Problem (#3643)\n\n`examples/python/CuTeDSL/experimental/primitives/tcgen05/2cta_mma_blockscale_m128.py` produces wrong results for larger problem sizes and hits `CUDA_ERROR_LAUNCH_FAILED` for larger K. Reproduced on B200 (sm_100, CUDA 13.0, nvidia-cutlass-dsl 4.8.0) with the reporter's `check_blockscale_sync.py`: 29/200 iterations mismatch at 4096x256x2048, launch failure at 8192x256x4096. `compute-sanitizer --tool synccheck` on the unmodified example reports `Barrier error detected. Missing wait` from the SFB TMA warp in the non-leader CTA on `sfb_smem_full`.\n\n## Root cause\n\n1. **The peer CTA's SFB completion is never observed.** Each CTA loads its own SFB half with a `cta_group::1` TMA tracked by its *local* `sfb_smem_full`. Only the leader's MMA warp waits, and only on the *leader's* barrier, before issuing the `cta_group::2` UTCCP that also reads the peer's sSFB. There is no path from the peer's `sfb_smem_full` to the leader.\n\n2. **Cross-CTA arrives use `MemScope.CTA`.** `mbarrier_arrive(remote, scope=CTA)` lowers to `mbarrier.arrive.release.cta.shared::cluster.b64`, so the peer's SFA `cp.async` writes are not released at cluster scope before the leader's UTCCP reads them. (The default for a shared::cluster address is already `.cluster`; the example explicitly downgraded it.)\n\n3. **No cross-proxy fence.** SFA is written through the generic proxy (`cp.async`) and read through the async proxy (`tcgen05.cp`).\n\n## Fix\n\n- Warp 7 (sync relay) also waits on `sfb_smem_full[stage]` before arriving on the leader's `ab_full[stage]`.\n- Both cross-CTA `mbarrier_arrive` calls (the `ab_full` relay and the `tmem_dealloc` handshake) use `MemScope.CLUSTER`.\n- `fence.proxy.async.shared::cta` after the `a_local` wait, before the relay arrive.\n- Updated the warp-7 comment, which said SFB \"does not participate here\".\n\n## Verification (B200)\n\nUsing the reproducer from #3643 (`--repeats 300` per shape):\n\n| variant | 4096x256x2048 | 8192x256x4096 | 16384x256x8192 |\n|---|---|---|---|\n| upstream main | 29/200 FAIL | launch failure | — |\n| only `ab_full` relay arrive → CLUSTER | 0/300 | — | — |\n| only `tmem_dealloc` arrive → CLUSTER | 11/300 FAIL | — | — |\n| only SFB wait added | 0/300 | 0/300 | 0/300 |\n| this PR | 0/300 | 0/300 | 0/300 |\n\n`compute-sanitizer --tool synccheck` on this PR: 0 errors (8 before). The example's own `python 2cta_mma_blockscale_m128.py` and `--mnk 4096,256,2048` runs pass.\n\nIsolating the two scope changes shows the `ab_full` relay arrive is what removes the mismatches on B200; adding only the SFB wait also passes here, but that is timing (SFB is small and normally lands before A/B), and synccheck still needs the wait to go clean. All three changes are kept.\n\nFixes #3643\n", "url": "https://github.com/NVIDIA/cutlass/pull/3647"}
{"number": 3574, "title": "Move dense_gemm_cute_pipeline.py under dense_gemm/ to make it consistent with other examples.", "state": "closed", "created_at": "2026-09-01T01:01:22Z", "updated_at": "2026-09-20T15:12:00Z", "closed_at": "2026-09-20T15:12:00Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "", "url": "https://github.com/NVIDIA/cutlass/pull/3574"}
{"number": 3222, "title": "FMHA examples: use cute::min in device functions", "state": "open", "created_at": "2026-05-11T05:31:36Z", "updated_at": "2026-09-20T11:11:32Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Use `cute::min` instead of `std::min` in FMHA device functions.\n\nThese helpers are marked `CUTLASS_DEVICE`, and `cute::min` is already the CuTe/CUTLASS host-device constexpr utility used for this pattern.\n\nThis matches the existing CUTLASS/CuTe device-code style and avoids relying on `std::min` in device functions.\n\nTesting:\n\n    cmake -S . -B build \\\n      -DCMAKE_BUILD_TYPE=Release \\\n      -DCUTLASS_NVCC_ARCHS=90a \\\n      -DCUTLASS_ENABLE_TESTS=OFF \\\n      -DCUTLASS_ENABLE_EXAMPLES=ON\n\n    cmake --build build --target 88_hopper_fmha -j$(nproc)\n\nResult:\n\n    Built target 88_hopper_fmha\n\nI did not build the Blackwell FMHA target locally.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3222"}
{"number": 3223, "title": "Avoid unordered_map for runtime datatype mapping", "state": "open", "created_at": "2026-05-11T07:57:35Z", "updated_at": "2026-09-20T11:11:30Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nReplace per-call `std::unordered_map` construction used for `RuntimeDatatype` to `cute::UMMA::MXF8F6F4Format` conversion in GEMM operation wrappers with a switch-based local helper.\n\nThe mapping is fixed and small, so this avoids constructing a temporary hash map during argument update while preserving the supported mappings for:\n\n- `RuntimeDatatype::kE4M3`\n- `RuntimeDatatype::kE5M2`\n- `RuntimeDatatype::kE3M2`\n- `RuntimeDatatype::kE2M1`\n\nThis also fixes the unsupported runtime datatype path by replacing the previous assert string expression with a real debug assertion and an explicit `Status::kErrorInvalidProblem` return.\n\n## Changed files\n\n- `tools/library/src/gemm_operation_3x.hpp`\n- `tools/library/src/sparse_gemm_operation_3x.hpp`\n- `tools/library/src/blockwise_gemm_operation_3x.hpp`\n\n## Local validation\n\n- `git diff --check HEAD~1..HEAD`\n- Verified no remaining `RuntimeDatatype` `std::unordered_map` mapping in the touched files\n- Verified the corrected debug assertion path appears once in each touched file\n\nFull local CUTLASS builds are not practical on my machine, so I am relying on project CI and maintainer review for full validation.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3223"}
{"number": 3644, "title": "[CuTeDSL] diagnostics: drop deprecated @dynamic_expr / @range_dynamic from PHASE_DYNAMIC_INDEX hint", "state": "open", "created_at": "2026-09-20T09:18:55Z", "updated_at": "2026-09-20T09:18:55Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`cutlass.dynamic_expr` and `cutlass.range_dynamic` have been deprecated since 4.1 (`range` / `if` are runtime by default; both helpers raise a DeprecationWarning), but the `PHASE_DYNAMIC_INDEX` fix line still tells the user to reach for them:\n\n```\nsuggestion: Mark the loop as dynamic with @dynamic_expr or @range_dynamic\n```\n\nThis rewords that one line to describe the current behaviour: a plain `range(...)` over a staged value inside `@jit` / `@kernel` already generates a runtime loop, and `cutlass.range(...)` is the knob for unroll / pipelining. The second fix line (decorate the parent function with `@jit`) is unchanged.\n\nText-only change in `diagnostics.py`; no behaviour change. `{staged}` is expanded by `DiagId.fill` like in the neighbouring messages.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3644"}
{"number": 3453, "title": "[CuTe DSL] Preserve runtime tensor strides in Blackwell FMHA", "state": "open", "created_at": "2026-08-13T07:25:21Z", "updated_at": "2026-09-20T09:14:19Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nBlackwell CuTe DSL FMHA accepts dynamic-layout Q/K/V/O/LSE tensors, but rebuilt its internal layouts from problem sizes using packed strides. Because the original iterators were paired with these synthesized layouts, padded tensor views could be addressed as if they were contiguous and silently produce incorrect results.\n\nThis change:\n\n- maps the internal Q/K/V/O/LSE layouts from each input tensor runtime stride;\n- preserves the intentional zero batch stride for varlen inputs, whose batch offsets come from cumulative sequence lengths; and\n- corrects the LSE shape comment to match the actual allocation.\n\n## Validation\n\n- Python syntax check for fmha.py.\n- NVIDIA GB200, CUDA 13.3, nvidia-cutlass-dsl 4.7.0: contiguous FP16 FMHA compilation and reference check passed.\n- NVIDIA GB200: a local padded-stride case with B=2, GQA (8 query heads / 2 KV heads), and LSE enabled compiled and passed the reference check.", "url": "https://github.com/NVIDIA/cutlass/pull/3453"}
{"number": 3261, "title": "fix: handle ComposedLayout slicing with dynamic strides (fixes #3255)", "state": "open", "created_at": "2026-05-22T12:16:37Z", "updated_at": "2026-09-19T13:12:59Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "When slicing a Tensor whose layout is a ComposedLayout (e.g., Swizzle composed with a dynamic outer Layout), _cute_ir.slice fails because the Swizzle composition requires static stride analysis that cannot be performed with runtime dynamic strides (represented as '?' / unknown values).\r\n\r\nThe fix intercepts this case in _Tensor.__getitem__ and decomposes the ComposedLayout manually in the Python DSL layer:\r\n1. Extract inner (Swizzle), offset, and outer (Layout) from the ComposedLayout\r\n2. Slice only the outer Layout using slice_ (pure Layout slicing works with dynamic strides)\r\n3. Compute the offset delta via crd2idx(coord_with_none_as_zero, outer)\r\n4. Rebuild the ComposedLayout with the new offset and sliced outer\r\n5. Construct a new Tensor with the original iterator\r\n\r\nThis avoids passing the composed layout with dynamic strides to the MLIR slice operation, which triggers an MLIR verifier error and segfault.", "url": "https://github.com/NVIDIA/cutlass/pull/3261"}
{"number": 3468, "title": "Soft-error swizzle composed slice when ZY projection fails", "state": "open", "created_at": "2026-08-17T07:35:45Z", "updated_at": "2026-09-19T10:12:42Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`cute.slice_` on `S<3,4,3> o 0 o (10,2):(2,1)` aborted in cutegen `static_size`.\nY/Z projection composition made an invalid layout.\nReturn `cg_error` instead.\n`SliceOp` then emits `unable to slice`.\n`(8,2)` still becomes `(8):(2)`.\n\nFixes #3454\n\nHeader-only: `(10,2)` invalid, no assert.\n`(8,2)` and `(64,128)` still valid.\nUnit test: `cg_composed_layout_test.cpp`.\n`slice_errors.mlir` is in the diff. Needs `cute-opt`. Not run here.\n\nAssisted by: Cursor\n", "url": "https://github.com/NVIDIA/cutlass/pull/3468"}
{"number": 3476, "title": "Fixed grammar in Efficient_gemm.md documentation.", "state": "open", "created_at": "2026-08-20T01:28:54Z", "updated_at": "2026-09-19T10:12:39Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Description\r\n\r\nFixes grammatical errors in the Efficient Gemm documentation.\r\n\r\nChanged \"TMA is also updates\" to \"TMA also updates,\" amongst others that I later found. \r\n\r\nNo functional code changes.", "url": "https://github.com/NVIDIA/cutlass/pull/3476"}
{"number": 3246, "title": "Fix FastDivmod divisor SSA transport for kernel regions (#3243)", "state": "open", "created_at": "2026-05-19T04:19:37Z", "updated_at": "2026-09-19T08:17:44Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3243\r\n\r\nExpose encoded divisor plus scalar divisor IR through `FastDivmodDivisor::__extract_mlir_values__` and rebuild `.divisor` from region-local SSA in `__new_from_mlir_values__`. Update persistent tile schedulers to slice the FastDivmod tail using `extract_mlir_values` length. Add a CuTeDSL compile regression aligned with the issue repro.\r\n\r\n## Test plan\r\n- `pytest test/examples/CuTeDSL/test_fast_divmod_divisor_param_transport.py`", "url": "https://github.com/NVIDIA/cutlass/pull/3246"}
{"number": 3477, "title": "Fix print typo for throughput metrics", "state": "open", "created_at": "2026-08-20T08:04:09Z", "updated_at": "2026-09-19T08:17:41Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "As title.", "url": "https://github.com/NVIDIA/cutlass/pull/3477"}
{"number": 3475, "title": "[SM90] Support MXFP4A8 Group Gemm for the SM90 mixed-input.", "state": "open", "created_at": "2026-08-19T12:03:48Z", "updated_at": "2026-09-19T07:14:18Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Since the SM90 devices gained support for the int4 * fp8 computation pipeline, it has been widely adopted in inference frameworks such as SGLang and vLLM and delivered remarkable performance. \r\n\r\nHowever, with the evolution of large‑language models (LLMs), an increasing number of models employ higher‑precision parameters like MXFP4. Unfortunately, a large number of SM90 devices lack native support for this data type. Similar to the earlier int4 scenario, runtime dequantization is required for adaptation. Most existing schemes dequantize weights to BF16 for inference, which covers the vast majority of use cases. Nevertheless, this is clearly a fallback solution for SM90‑class devices that natively support FP8 GEMM operations. \r\n\r\nAccordingly, I have implemented the MXFP4‑to‑FP8 GEMM pipeline. Building upon existing work on WINT4A8, I realized dynamic scale retrieval and the corresponding GEMM computation logic, and removed the traditional fixed‑block‑size‑128 limitation of INT4A8. The implementation has been validated on SGLang. Results show that WMXFP4A8 outperforms existing pipelines in most scenarios.\r\n\r\n## Summary\r\n\r\nThis PR adds MXFP4A8 activation block scaling support to the SM90 mixed-input ptr-array mainloop while preserving the existing int4a8 execution path.\r\n\r\nThe implementation gates the new activation-scale TMA, shared memory, and chunked accumulation logic behind the MXFP4A8-specific `EnableActBlockScale` condition. Non-MXFP4A8 paths, including int4a8, keep the original mainloop behavior.\r\n\r\n## Changes\r\n\r\n- Added activation block scale TMA descriptor, shared storage, and load path for MXFP4A8.\r\n- Added chunked accumulation with activation scale application for MXFP4A8.\r\n- Preserved the original `mma()` mainloop for non-act-block-scale paths.\r\n- Added E2M1 to E4M3 numeric conversion coverage.\r\n- Added static tests for int4a8 and MXFP4A8 path separation.\r\n\r\n## Validation\r\n\r\nTested on machine H20 / SM90, CUDA `12.4.131`.\r\n\r\n### Unit tests\r\n\r\n```bash\r\n./test/unit/core/cutlass_test_unit_core \\\r\n  --gtest_filter=\"*Mxfp4*:*MXFP4*:*mxfp4*\"\r\n```\r\n\r\nResult:\r\n\r\n```text\r\n[  PASSED  ] 2 tests.\r\n```\r\n\r\n```bash\r\n./test/unit/gemm/device/cutlass_test_unit_gemm_device_tensorop_gmma_rs_warpspecialized_sm90 \\\r\n  --gtest_filter=\"SM90_Device_Gemm_MixedInput_RS.Int4a8AndMxfp4a8StaticProperties\"\r\n```\r\n\r\nResult:\r\n\r\n```text\r\n[  PASSED  ] 1 test.\r\n```\r\n\r\n### int4a8 benchmark\r\n\r\nBenchmark target:\r\n\r\n```text\r\nexamples/69_hopper_mixed_dtype_grouped_gemm/69_hopper_int4_fp8_grouped_gemm\r\n```\r\n\r\nSmall shape:\r\n\r\n```bash\r\n--m=512 --n=2048 --k=2048 --groups=8 --c=512 --mode=1 --warmup=3 --iterations=20 --alpha=1 --beta=0\r\n```\r\n\r\n| Version | Avg runtime |\r\n|---|---:|\r\n| baseline | `0.317806 ms` |\r\n| patched | `0.317676 ms` |\r\n\r\nDelta: `-0.041%`\r\n\r\nLarge shape:\r\n\r\n```bash\r\n--m=2048 --n=5120 --k=8192 --groups=16 --c=512 --mode=1 --warmup=5 --iterations=50 --alpha=1 --beta=0\r\n```\r\n\r\n| Version | Avg runtime |\r\n|---|---:|\r\n| baseline | `70.65475 ms` |\r\n| patched | `70.65260 ms` |\r\n\r\nDelta: `-0.003%`\r\n\r\nBoth benchmark runs passed correctness checks. int4a8 performance is unchanged within measurement noise.\r\n\r\n### int4a8 MNK coverage\r\n\r\nThe existing CUTLASS int4/fp8 grouped GEMM example was also run on a small shape, a large-group shape, and a large prefill-like shape:\r\n\r\n```text\r\nexamples/69_hopper_mixed_dtype_grouped_gemm/69_hopper_int4_fp8_grouped_gemm\r\n```\r\n\r\n| Shape | Groups | Avg runtime | GFLOPS | Result |\r\n|---|---:|---:|---:|---|\r\n| `m=256, n=128, k=512` | 6 | `0.016672 ms` | `12075.7` | Passed |\r\n| `m=128, n=128, k=512` | 100 | `0.043360 ms` | `38692.8` | Passed |\r\n| `m=2048, n=5120, k=8192` | 16 | `69.4398 ms` | `39585.0` | Passed |\r\n\r\nThis supplements the static int4a8 path-separation test with runtime MNK coverage from the existing CUTLASS example.\r\n\r\n### MXFP4A8 correctness\r\n\r\nMXFP4A8 correctness was validated through a temporary SGLang `cutlass_mxfp4a8_moe_mm` single-GEMM test on the same H20 machine. The test compares the kernel output against a BF16 dequantized golden reference and exercises:\r\n\r\n- activation scale = one identity path;\r\n- full MXFP8 activation path with per-token, per-block activation scales;\r\n- single-expert shapes: `m=4/8/16/128`, `k=256/512/1024`, `n=512/1024/2048`;\r\n- multi-expert grouped GEMM with uneven token counts: `[4,4,4,4]`, `[3,5,4,8]`, `[16,8,32,8]`;\r\n- uniform multi-expert counts from 2 to 32 tokens per expert.\r\n\r\nObserved relative mean error was `0.0000` for all tested cases.\r\n\r\n### MXFP4A8 microbenchmark\r\n\r\nThe following single-GEMM MXFP4A8 benchmark uses `sgl_kernel.cutlass_mxfp4a8_moe_mm` with full activation block scaling enabled. For this single-GEMM synthetic benchmark, `topk=1` is the clean kernel-level setting; end-to-end MoE top-k behavior is covered separately by full MoE benchmarks.\r\n\r\n| Shape | Avg runtime | P50 runtime | GFLOPS |\r\n|---|---:|---:|---:|\r\n| `m=256, n=128, k=512` | `0.016179 ms` | `0.016320 ms` | `2074.01` |\r\n| `m=128, n=128, k=512` | `0.015639 ms` | `0.015648 ms` | `1072.78` |\r\n| `m=1024, n=2048, k=4096` | `0.164708 ms` | `0.164752 ms` | `104304.70` |\r\n| `m=2048, n=5120, k=8192` | `1.023284 ms` | `1.024608 ms` | `167889.58` |\r\n| `m=4096, n=5120, k=8192` | `1.975428 ms` | `1.978080 ms` | `173935.68` |\r\n\r\nFor reference, using `topk=6/8` in the same single-expert synthetic setup adds non-representative scheduling overhead and should not be used as the single-GEMM microbenchmark number.\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3475"}
{"number": 3258, "title": "fix(CuTeDSL): restore trailing Int<1> dimension in SM90 MMA atom TV L…", "state": "open", "created_at": "2026-05-21T12:27:10Z", "updated_at": "2026-09-18T17:12:02Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "…ayout C when N=8\r\n\r\nWhen N=8, the SM90 WGMMA CLayout_64xN has a trailing Int<N/8> = Int<1> dimension in the C++ CuTe reference (mma_traits_sm90_gmma.hpp L432-L435). The MLIR layout canonicalization was incorrectly dropping this size-1 dimension, causing tv_layout_C to return ((4,8,4),(2,2)) instead of ((4,8,4),(2,2,1)).\r\n\r\nThis fix adds a post-processing check in MmaAtom.tv_layout_C to restore the trailing Int<1> dimension when N=8, matching the C++ implementation and preserving layout structural invariants.\r\n\r\nFixes NVIDIA/cutlass#3254", "url": "https://github.com/NVIDIA/cutlass/pull/3258"}
{"number": 3474, "title": "Fix unsubstituted version/revision placeholders in version.h", "state": "open", "created_at": "2026-08-19T11:50:46Z", "updated_at": "2026-09-18T12:21:16Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3472.\n\n`getVersionString()` and `getGitRevision()` in `include/cutlass/version.h` return the literal, unsubstituted CMake placeholder strings `\"@CUTLASS_VERSION@\"` / `\"@CUTLASS_REVISION@\"` for every consumer, since nothing in the current build ever processes `version.h` itself as a template.\n\nThe actual CMake-substituted revision is already available a few lines above: when built via CMake, `CUTLASS_VERSIONS_GENERATED` is defined and `version_extended.h` (generated from `cmake/version_extended.h.in`) redefines `CUTLASS_REVISION` with the real git hash; otherwise it falls back to `\"\"`. `getVersionBuild()` already correctly reads `CUTLASS_BUILD` from that same mechanism — `getGitRevision()` just needs to do the same for `CUTLASS_REVISION` instead of hardcoding the placeholder.\n\nThere's no equivalent generated macro for the version string (the `.in` template only substitutes `CUTLASS_VERSION_BUILD` and `CUTLASS_REVISION`), so `getVersionString()` now builds a `MAJOR.MINOR.PATCH[.BUILD]` string from the existing `getVersionMajor()`/`getVersionMinor()`/`getVersionPatch()`/`getVersionBuild()` accessors instead.\n\nHeader-only change, no build system changes.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3474"}
{"number": 3256, "title": "[Cutlass SM90] Per-group aux TMA descriptor update for grouped GEMM + Gated-SwiGLU example)", "state": "open", "created_at": "2026-05-21T03:57:25Z", "updated_at": "2026-09-18T06:26:02Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "  ## Motivation\r\n\r\n   SM90 persistent grouped GEMM is already a critical high-performance path for MoE workloads: PyTorch exposes BF16 `grouped_mm` for CUDA grouped GEMM, and TorchTitan's Llama/MoE path directly depends on `torch._grouped_mm`. In practice this makes CUTLASS SM90 grouped GEMM a natural backend to extend rather than bypass.\r\n\r\n   However, the existing CUTLASS epilogue templates assume a 1:1 accumulator-to-output mapping, and the stock aux-store path is tied to a static, shape-preserving descriptor. This prevents fusing shape-changing activations such as SwiGLU, where the GEMM produces `[M, 2I]` but the activation output is `[M, I]` — especially when a persistent grouped kernel moves across groups with different pointers and problem shapes.\r\n\r\n  This PR  add a **generic per-group Aux TMA descriptor update path** to the SM90 persistent grouped GEMM TMA warp-specialized epilogue, and demonstrates it with a **Hopper Grouped GEMM + Gated-SwiGLU end-to-end example**. The framework-level changes are activation-agnostic — SwiGLU is simply the first consumer of the mechanism and lives entirely under `examples/57_hopper_grouped_gemm/`.\r\n\r\n  ## Summary\r\n\r\n  - **Framework**: A generic per-group Aux TMA descriptor update pipeline inside the SM90 grouped TMA warp-specialized epilogue, mirroring the existing `C / D` per-group descriptor\r\n  lifecycle and gated on a compile-time SFINAE trait so that existing instantiations see zero behavioral and code-size impact.\r\n  - **Application**: A new `Sm90GatedSwiGLUStoreTma` EVT root node and a complete `57_hopper_bias_swiglu_grouped_gemm_bf16` driver demonstrating Bias + SwiGLU fused into grouped GEMM\r\n  bf16, validated on both cooperative and pingpong schedules.\r\n\r\n  ## Framework changes (4 include files, +203 / −5)\r\n\r\n  ### `include/cutlass/epilogue/fusion/sm90_visitor_tma_warpspecialized.hpp` (+25)\r\n  - `ConsumerStoreArgs` gains an `aux_store_tensormap` field that threads the **per-CTA × per-warpgroup aux gmem tensormap slot pointer** through to every EVT callback.\r\n  - `Sm90TreeVisitor` adds `get_aux_tma_descriptor()` and `aux_tensormaps_replace()` that forward to the root `NodeOp`, so the collective epilogue can drive aux descriptor updates\r\n  without dissecting the EVT.\r\n\r\n  ### `include/cutlass/epilogue/collective/sm90_epilogue_array_tma_warpspecialized.hpp` (+128 / −5)\r\n  - A SFINAE probe `detail::has_aux_tma_store_v<FusionCallbacks>` recursively scans the EVT tree; the aux pipeline activates whenever any node declares `static constexpr bool\r\n  HasAuxTmaStore = true`.\r\n  - `TensorMapStorage` adds `smem_tensormap_aux[NumEpilogueWarpGroups]`, and the gmem workspace is extended in lockstep (`aux_base = NumEpilogueWarpGroups + (is_void_v<ElementC> ? 0 :\r\n  1)`; `get_workspace_size()` and `to_underlying_arguments()` updated accordingly).\r\n  - Four new helpers that strictly mirror the `C / D` \"init / replace / publish / synchronize\" choreography, reusing the existing `cute::tma_descriptor_*` primitives together with\r\n  `tma_desc_commit_group` / `wait_group`:\r\n    - `aux_store_init`\r\n    - `aux_tensormaps_perform_update`\r\n    - `aux_tensormaps_cp_fence_release`\r\n    - `aux_tensormaps_fence_acquire`\r\n  - `store()` takes an additional `aux_store_tensormap` parameter and forwards it into `ConsumerStoreArgs`, allowing the application visitor to inject the runtime descriptor in\r\n  `tma_store()` via `.with(runtime_desc)`.\r\n\r\n  ### `include/cutlass/gemm/kernel/sm90_gemm_array_tma_warpspecialized_cooperative.hpp` (+25) and `..._pingpong.hpp` (+25)\r\n  - In the consumer warp group prologue and at every `did_batch_change` boundary, the `D`-path `init / perform_update / cp_fence_release / fence_acquire / store` call sequence is\r\n  duplicated for the aux path.\r\n  - No new pipeline stages or barrier semantics are introduced; the existing synchronization skeleton is reused as-is.\r\n\r\n  ### Zero-overhead guarantee\r\n\r\n  When no EVT node declares `HasAuxTmaStore = true`, `NumAuxTmaTensors == 0` and the entire aux pipeline is short-circuited by `if constexpr`. Existing grouped GEMM instantiations\r\n  observe zero behavioral difference and zero additional code/state.\r\n\r\n  ## Application example (`examples/57_hopper_grouped_gemm/`, +1265)\r\n\r\n  ### `sm90_gated_swiglu_store_tma.hpp` (+428) — custom EVT root `Sm90GatedSwiGLUStoreTma`\r\n  - **Framework hooks**: declares `HasAuxTmaStore = true` and implements `get_aux_tma_descriptor()` and `aux_tensormaps_replace()`. This node takes over the stock `Sm90AuxStore` role,\r\n  additionally handling the `N → N/2` shape change.\r\n  - **Three-stage pipeline**:\r\n    - `visit()` — full-width register accumulation,\r\n    - `postreduce()` — SwiGLU computed with `tanh.approx.bf16x2` PTX, scattered into half-width SMEM,\r\n    - `tma_store()` — emitted via `.with(kernel_swiglu_tensormap)`, matching the `D`-path `tma_store_d.with(store_tensormap)` idiom.\r\n  - **SwiGLU formula (OpenAI / GPT-OSS mxfp4 variant)**: with `α = 1.702` and `limit = 7.0`,\r\n    - `g_clip = min(gate, limit)`\r\n    - `u_clip = clamp(up, ±limit)`\r\n    - `s = 0.5 * (1 + tanh(α · g_clip / 2))`\r\n    - `out = g_clip · s · u_clip + g_clip · s`\r\n\r\n  ### `57_hopper_bias_swiglu_grouped_gemm_bf16.cu` (+821) — single-file driver\r\n  - **Two-phase CollectiveBuilder**: phase 1 uses the stock fusion to probe `EpiTile / SmemLayoutAtomD / CopyOpR2S / StagesD`; phase 2 assembles the real EVT tree `Sm90EVT<SwiGLUStore,\r\n  ...>`.\r\n  - **Parallel dual outputs**: the `D` path carries the bias-added pre-activation `[M_i, 2I]` (usable as a backward-pass input); the aux TMA path carries the SwiGLU output `[M_i, I]`.\r\n  The two stores do not block each other.\r\n  - **Test matrix**: `{Cooperative, Pingpong} × host_problem_shapes_available ∈ {true, false}` — 4 configurations.\r\n\r\n  ### `CMakeLists.txt` (+16) — registers the new target and ctest case.\r\n\r\n  ## Design notes\r\n\r\n  - **Generic hook in the framework, semantics in the application.** The library only exposes three contract points — `HasAuxTmaStore`, `get_aux_tma_descriptor()`,\r\n  `aux_tensormaps_replace()`. All SwiGLU-specific semantics (`N → N/2`, `tanh.approx.bf16x2`, the `ptr_swiglu_out_array` indexing scheme) live in the example. Other shape-changing\r\n  outputs (softmax + top-k mask, per-output quantization scales, etc.) can reuse the same hooks unchanged.\r\n  - **The stock `Sm90AuxStore` is left untouched.** Its `Arguments` use a single pointer + single stride and assume that the input shape and the aux output shape match — SwiGLU violates\r\n  that precondition. Introducing `Sm90GatedSwiGLUStoreTma` as a new node keeps the existing class clean.\r\n\r\n  ## Testing\r\n\r\n  ```bash\r\n  ./build/release/examples/57_hopper_grouped_gemm/57_hopper_bias_swiglu_grouped_gemm \\\r\n      --m=1024 --n=4096 --k=2048 --groups=8 \\\r\n      2>&1 | grep -E 'schedule|Disposition|Avg runtime|TFLOPS'\r\n\r\n  Result: all four schedule variants pass correctness verification.\r\n  TFLOPS (GEMM) counts only the grouped GEMM (2 · Σ M·N·K); the fused bias-add and SwiGLU activation are included in the timing but intentionally not counted in the flop budget —\r\n  matching the convention used by the upstream 57_hopper_grouped_gemm example.\r\n\r\n  ┌─────────────┬─────────────────────┬─────────────┬──────────────────┬───────────────┐\r\n  │  Schedule   │ Host problem shapes │ Disposition │ Avg runtime (ms) │ TFLOPS (GEMM) │\r\n  ├─────────────┼─────────────────────┼─────────────┼──────────────────┼───────────────┤\r\n  │ Cooperative │      available      │   Passed    │         0.204138 │       673.266 │\r\n  ├─────────────┼─────────────────────┼─────────────┼──────────────────┼───────────────┤\r\n  │ Cooperative │     unavailable     │   Passed    │         0.206483 │       665.618 │\r\n  ├─────────────┼─────────────────────┼─────────────┼──────────────────┼───────────────┤\r\n  │ Pingpong    │      available      │   Passed    │         0.197277 │       696.681 │\r\n  ├─────────────┼─────────────────────┼─────────────┼──────────────────┼───────────────┤\r\n  │ Pingpong    │     unavailable     │   Passed    │         0.196608 │       699.051 │\r\n  └─────────────┴─────────────────────┴─────────────┴──────────────────┴───────────────┘\r\n\r\n  *** Cooperative schedule (bias + SwiGLU) ***\r\n    Disposition   : Passed\r\n    Avg runtime   : 0.204138 ms\r\n    TFLOPS (GEMM) : 673.266\r\n  *** Cooperative schedule (host problem shapes unavailable) ***\r\n    Disposition   : Passed\r\n    Avg runtime   : 0.206483 ms\r\n    TFLOPS (GEMM) : 665.618\r\n  *** Pingpong schedule (bias + SwiGLU) ***\r\n    Disposition   : Passed\r\n    Avg runtime   : 0.197277 ms\r\n    TFLOPS (GEMM) : 696.681\r\n  *** Pingpong schedule (host problem shapes unavailable) ***\r\n    Disposition   : Passed\r\n    Avg runtime   : 0.196608 ms\r\n    TFLOPS (GEMM) : 699.051\r\n\r\n  ---", "url": "https://github.com/NVIDIA/cutlass/pull/3256"}
{"number": 3419, "title": "[CuTeDSL] Add prepared-launch API for low-overhead kernel replay", "state": "open", "created_at": "2026-07-29T09:20:35Z", "updated_at": "2026-09-18T03:17:54Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3418 ([FEA] CuTe DSL: prepared-launch API to eliminate\nper-call host marshalling overhead).\n\n## What\n\nA first-class \"bind once, replay cheaply\" handle on compiled CuTe DSL\nfunctions:\n\n```python\ncompiled = cute.compile(fn, out_ptr, in_ptr, alpha, n, stream)\nprepared = compiled.prepare(out_ptr, in_ptr, alpha, n, stream)   # validate + marshal once\nfor step in range(steps):\n    prepared.launch(out.data_ptr(), x.data_ptr(), alpha, n, handle)  # in-place + direct call\n```\n\nSteady-state launches of an already-compiled function currently re-marshal\nevery argument on every call (`ExecutionArgs.generate_execution_args` plus\npacked `void**` regeneration; wrappers typically also rebuild the argument\nobjects themselves each step) before a ~2.6 us C call. On a B200 with a\ntrivial 5-argument kernel that is 13.5-20.8 us/launch of host time;\n`prepared.launch` is 3.8 us/launch (table below).\n\n## Design\n\n- `JitCompiledFunction.prepare(*args, **kwargs) -> PreparedLaunch` validates\n  eligibility once, runs the normal marshalling exactly once, and snapshots\n  the marshalled bytes into private per-slot ctypes storage owned by the\n  returned handle. It delegates to `JitExecutor.prepare` through the extracted\n  `_ensure_default_executor()` (same lock/weakref-proxy pattern as before), so\n  `compiled.to(device).prepare(...)` is the multi-device variant — the API\n  mirrors the existing `to()` / `__call__` split exactly.\n  `CudaDialectJitCompiledFunction` inherits `prepare()`; `cuda_jit_executor.py`\n  needs zero changes.\n- `PreparedLaunch.launch(*args, **kwargs) -> int | None` has the same\n  signature and return contract as calling the compiled function: keyword\n  arguments and omitted defaulted parameters rectify through the existing\n  `get_rectified_args` (raising the shared binding diagnostics); full\n  positional calls are the fast path. It rewrites per-argument slots in place\n  (no allocation, no repacking, no ExecutionArgs regeneration) and invokes\n  the compiled entry point directly, checking a private CUDA-result slot.\n  Pointer slots accept runtime `Pointer` objects or raw integer addresses;\n  scalar slots accept Python scalars or `Numeric` instances (written with\n  plain ctypes assignment semantics — a value the storage rejects, such as a\n  float for an integer slot, raises a typed error instead of applying the\n  implicit DSL cast a direct call would); stream slots accept CUDA stream\n  objects, raw integer handles, or framework streams exposing an integer\n  `cuda_stream` attribute (`torch.cuda.Stream`).\n- Eligibility (validated at prepare, not at launch) is annotation-driven\n  where the declaration is informative — a declared pointer, stream, or\n  numeric slot must be prepared with a value of that kind — and value-driven\n  for unannotated slots: runtime pointers (`make_ptr` / pointer-address\n  slots; the marshalling contract is verified against the pointer's `_desc`\n  storage, so contract lookalikes are rejected), numeric scalars with plain\n  ctypes storage (Boolean, Int8-64, Uint8-64, Float32/64, TFloat32, plain\n  Python bool/int/float), CUDA streams. Anything else (dlpack tensors,\n  structs, sequences, custom JIT arguments, bit-cast scalar storage such as\n  Float16, and TVM-FFI compiled functions, which dispatch through their own\n  entry point) raises `PreparedLaunchError` — a `DSLUserCodeError` carried by\n  six new `LAUNCH_*` entries in the DiagId catalog, so errors render like\n  every other DSL diagnostic. The normal call path remains for rejected\n  mixes.\n- Bitwise identity: the initial slot bytes are the slow path's bytes\n  (memmoved from the storages `generate_execution_args` just produced), and\n  launch-time writes produce the same bytes the slow path would marshal for\n  the same values of the prepared kinds. Verified per-slot (packed payload\n  byte comparison) and end-to-end (bitwise output comparison in the tests).\n- Ownership and lifecycle: each instance owns its packed `void**` array and\n  CUDA-result slot (the executor's are shared/thread-local), so instances\n  never race each other or the normal call path. The slow path's scalar\n  ctypes storage is ephemeral, so the handle snapshots into cells it owns.\n  Launching after `jit_module.unload()` raises cleanly; a recompiled or\n  autotuned function needs a new handle. One instance per thread is the\n  documented model (instances share no mutable state; sequential cross-thread\n  handoff works). CUDA-graph capture through a capturing stream works with\n  standard frozen-at-capture semantics.\n\n## What this PR does NOT change\n\n- No changes to `__call__`'s hot path; the only change to existing code is\n  extracting `_ensure_default_executor()` from `run_compiled_program`\n  (verbatim, same locking).\n- No changes to `cuda_jit_executor.py`.\n- No behavior change for non-users; the feature is purely additive.\n\n## Performance\n\nB200 (sm_100), trivial 4096-element f32 scale kernel (~2 us device time,\nhost-bound), 300 warmup + 5000 timed calls x 3 repeats, median; host wall =\n`perf_counter` around the loop, cuda-event = events on the stream around the\nloop (agreement to 0.01 us confirms the host is the bottleneck). Numbers vary\n~±0.3 us run to run on a shared host:\n\n| path | host wall (us/call) | cuda-event (us/call) |\n|---|---|---|\n| direct call, reused arg objects | 13.48 | 13.49 |\n| direct call, rebuilt arg objects (wrapper steady state) | 20.76 | 20.76 |\n| prepared.launch, raw int addresses/handle | **3.77** | **3.78** |\n| prepared.launch, rebuilt pointer objects | 6.40 | 6.40 |\n\nSlow-path host breakdown (isolated medians): ~5.9 us argument-object\nconstruction (wrapper-side, applies to the rebuilt-args row), ~8.2 us\n`generate_execution_args`, ~1.2 us packed-array population, ~2.6 us bare\nentry-point call. In a production Megatron-Core integration the same\nmechanism cut 30-220 us/launch wrapper steady state to ~3.6 us (externally\nmeasured corroboration; see the issue).\n\n## Tests\n\n`test/python/CuTeDSL/test_prepared_launch.py` (unittest style, matches the\nexisting suite; device behavior is exercised on a real sm_100 kernel, and\neligibility rejections also use host-only signatures):\n\n- bitwise output parity with the direct call (incl. prepare before any call);\n- pointer/scalar/stream mutation between launches, raw ints, kwargs,\n  defaulted parameters omitted at prepare and launch, framework streams at\n  launch; Float64/Int64/Boolean width coverage with bitwise checks;\n- fast-path contract: launches never re-enter `generate_execution_args`\n  (monkeypatch guard) and reuse the packed array in place;\n- storage stability: 1000-launch read-modify-write accumulate under periodic\n  gc + allocator churn;\n- eligibility rejections (dlpack, sequence, Float16, multi-slot custom\n  `__c_pointers__`, pointer-contract lookalikes, wrong-kind values for\n  declared pointer/stream slots, framework stream at prepare, extra tail\n  args, missing args, TVM-FFI-style non-JitExecutor executors) and that the\n  direct call keeps working for rejected mixes;\n- misuse: launch arg-count mismatches (shared binding diagnostics), bad\n  pointer/scalar/stream/boolean values (naming the argument), launch after\n  `jit_module.unload()`;\n- threading: concurrent per-thread instances + sequential cross-thread\n  handoff;\n- `to(device).prepare(...)`;\n- CUDA-graph capture + replay and frozen-at-capture semantics both directions;\n- an in-test micro-benchmark asserting prepared is not slower than direct\n  (direction-only, load-tolerant).\n\n28 tests; full `test/python/CuTeDSL/` suite: 34 passed (includes the\npre-existing `test_struct_in_if.py`), zero regressions.\n\nA tutorial example (`examples/python/CuTeDSL/dsl_tutorials/prepared_launch.py`)\nfollows the by-passing-dlpack argument style of `call_bypass_dlpack.py`.\n\n## Notes for reviewers\n\n- The eligibility whitelist is deliberately conservative for v1; the slot\n  mechanism extends naturally (Float16/BFloat16 bit-pattern writers,\n  multi-slot dlpack descriptors) if there is interest.\n- Launch-time value checking is intentionally light — ctypes assignment\n  semantics with typed errors on rejected values (out-of-range integers\n  truncate to the declared width, like the existing fast path; wrong-kind\n  scalars, non-stream stream values, and non-bool Boolean values raise\n  `PreparedLaunchError`). Full `_full_arg_check` validation still runs at\n  prepare via the normal marshalling; kind-level validation is done once at\n  prepare, annotation-driven.\n- Related issues: #3351 (RSS growth from per-launch `__c_pointers__`\n  allocations — this PR's replay path performs none), #2852 (AOT/TVM-FFI\n  export — complementary deployment model).\n\n(To run the tests in-tree via the editable flow on current wheels you'll also want #3417.)", "url": "https://github.com/NVIDIA/cutlass/pull/3419"}
{"number": 3641, "title": "CuTeDSL: preserve outer comprehension generator_targets across nested comprehensions (#3499)", "state": "open", "created_at": "2026-09-17T20:35:42Z", "updated_at": "2026-09-17T20:35:42Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3499.\n\n## Problem\nA nested comprehension inside the element of an outer comprehension broke staged control flow later in the outer comprehension. E.g.:\n\n```python\nvals = [(len([0 for _ in range(2)]), (y if flag else -y)) for y in range(4)]\n```\n\nfailed at runtime with `name 'y' is not defined`, even though `y` is the comprehension variable.\n\n## Root cause\n`DSLPreprocessor._visit_Comprehension` (`python/CuTeDSL/cutlass/base_dsl/ast_preprocessor.py`) set `session_data.generator_targets` for the comprehension being visited and then unconditionally reset it to `[]` afterwards. When the element contained a nested comprehension, the inner visit overwrote the targets and reset them to empty on exit. The outer comprehension's remaining element processing then ran with no generator targets, so `visit_IfExp` synthesized its then/else branch blocks without the outer generator variable as an argument, treating it as a free/global name.\n\n## Fix\nSave the previous `generator_targets` before visiting the element and restore it afterwards in a `try/finally` (which also protects session state on exception paths), instead of resetting to `[]`:\n\n```python\nprevious_targets = self.session_data.generator_targets\nself.session_data.generator_targets = targets\ntry:\n    ele_visitor(node)\nfinally:\n    self.session_data.generator_targets = previous_targets\n```\n\nThis mirrors the existing save/restore idiom used elsewhere in the session data (`SessionData._set_attr`).\n\n## Testing\n- Unit repro driving the real `_visit_Comprehension` on the issue's AST shape (nested `ListComp` + `IfExp` on the outer generator variable), spying on the `generator_targets` observed by the `IfExp` transform: **failed before the fix** (observed `[]`, want `['y']`), **passes after**.\n- Extended sanity suite (8 checks, all green after the fix): triple nesting, two sequential nested comprehensions, `DictComp`/`SetComp`/`GeneratorExp` nesting, sibling-comprehension isolation, top-level reset-to-`[]` behavior unchanged, and state restoration when the element visitor raises.\n- End-to-end `@cute.jit` compilation from the issue needs a CUDA GPU, which isn't available in this environment; that leg is left to CI.\n\n## AI assistance disclosure\nThis contribution was prepared with AI assistance (reproduction, fix, and tests), reviewed by the author before submission.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3641"}
{"number": 3611, "title": "Restore TMEM allocator class constants for backward compatibility", "state": "closed", "created_at": "2026-09-11T01:57:40Z", "updated_at": "2026-09-17T01:21:49Z", "closed_at": "2026-09-17T01:21:49Z", "labels": [], "comments_count": null, "is_pr": true, "merged_at": "2026-09-17T01:21:49Z", "body": "https://github.com/NVIDIA/cutlass/pull/3551 removed the `Sm100TmemCapacityColumns` constant from `cute::TMEM::Allocator1Sm` and `cute::TMEM::Allocator2Sm`. This constant is now unused with CUTLASS, but removing it breaks users (see https://github.com/NVIDIA/TransformerEngine/pull/3443).", "url": "https://github.com/NVIDIA/cutlass/pull/3611"}
{"number": 3617, "title": "[CuTeDSL] Decode UE5M3 top binade as finite Float32", "state": "open", "created_at": "2026-09-13T02:46:37Z", "updated_at": "2026-09-16T02:50:53Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- decode scalar `FloatNV8E5M3FNU` to `Float32` from its UE5M3 bit representation instead of using IEEE E5M2 widening semantics\n- preserve finite values for the complete top exponent binade (codes 248-254)\n- preserve `0xff` as the format's sole NaN encoding\n- add a 256-code CuTe DSL GPU regression test\n\n## Testing\n\n- NVIDIA B200 (SM100), CUDA 13.1, `nvidia-cutlass-dsl 4.8.0.dev0`\n- `python -m pytest test_float_nv8e5m3fnu_conversion.py -q -rs` (1 passed)\n- the test verifies codes 0-254 exactly against the UE5M3 definition and verifies code 255 is NaN\n- on the unpatched package with CUDA 13.1, the generic widening path fails NVVM compilation with `unsupported operation`; the patched bit-level lowering compiles and executes successfully\n- `ruff format --check python/CuTeDSL/_mlir_helpers/arith.py test/python/CuTeDSL/test_float_nv8e5m3fnu_conversion.py`\n- `ruff check --ignore F403,F405 python/CuTeDSL/_mlir_helpers/arith.py test/python/CuTeDSL/test_float_nv8e5m3fnu_conversion.py` (the ignored diagnostics are pre-existing wildcard-import diagnostics in `arith.py`)\n- `git diff --check`\n\n## Contribution disclosure\n\nAI assistance was used for repository research, implementation, independent review, and running the checks above.\n\nFixes #3602\n", "url": "https://github.com/NVIDIA/cutlass/pull/3617"}
{"number": 3630, "title": "[CuTe DSL] Reject incomplete dynamic f-string formats", "state": "open", "created_at": "2026-09-13T04:58:26Z", "updated_at": "2026-09-16T02:39:47Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- reject dynamic f-string format specs that do not end in a supported printf conversion character\n- prevent width-only specs such as `{value:8}` from producing malformed `%8` device format strings that leave the argument unconsumed\n- preserve valid explicit specs such as `.2f` and add focused regression coverage\n\n## Testing\n\n- B200 node, Python 3.12: an AST-extracted execution of the current-main `FormattedValue` produced `valid=%.2f width_only=%8` and exited `2`\n- B200 node, Python 3.12: the fixed source produced `valid=%.2f width_only=rejected` and exited `0`\n- `python3 -m py_compile python/CuTeDSL/cutlass/base_dsl/ast_helpers.py test/python/CuTeDSL/test_formatted_value.py`\n- the source checkout was not imported as a complete DSL package because its generated `cutlass._mlir` extension is not built on the validation node\n\n## Contribution disclosure\n\nAI assistance was used for repository research, implementation, and running the checks above.\n\nFixes #3500\n", "url": "https://github.com/NVIDIA/cutlass/pull/3630"}
{"number": 3614, "title": "[Bugfix][Blackwell] Avoid fmha_bwd workspace int32 overflow", "state": "open", "created_at": "2026-09-11T17:59:30Z", "updated_at": "2026-09-16T01:47:58Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n`BlackwellFusedMultiHeadAttentionBackward._get_workspace_size` combines user-shape parameters for workspace sizing with integer math that can be narrowed to int32 in the Cute/CUDA path for large sequence and batch dimensions. That can truncate the size and trigger allocation-related failures.\n\n## Fix\n\nIn\n`examples/python/CuTeDSL/cute/blackwell/kernel/attention/fmha/fmha_bwd.py` function\n`BlackwellFusedMultiHeadAttentionBackward._get_workspace_size`, explicitly use Python `int` intermediates for all multipliers and dimensions:\n\n- round and cast `q` / `d` with `int((q + 7) // 8 * 8)`\n- compute workspace bytes from `b_i32`, `h_i32`, `q_i32`, `d_i32`, and `acc_bytes`\n- raise `OverflowError` if computed workspace bytes is negative\n\nThis keeps workspace arithmetic in Python big-int space and avoids unintentional int32 narrowing before CUDA allocation.\n\n## Testing\n\n- `python3 - <<'PY'\n  seq_len = 46341\n  ws = int(seq_len) * int(seq_len) * 4\n  assert ws > 0\n  print('workspace size:', ws)\n  PY`\n\nFixes #2886", "url": "https://github.com/NVIDIA/cutlass/pull/3614"}
{"number": 3396, "title": "[CuTe DSL] Raise a clear error when jit source is stale instead of silently misbehaving, issue #3395", "state": "open", "created_at": "2026-07-19T20:20:19Z", "updated_at": "2026-09-16T00:37:44Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Problem of https://github.com/NVIDIA/cutlass/issues/3395\r\n\r\n`@cute.jit` AST preprocessing is lazy: it runs at the *first call/compile* of each function, and at that point it re-reads the function source **from disk** via `inspect.getsourcelines`, slicing the current file at the in-memory code object's `co_firstlineno` (`base_dsl/ast_preprocessor.py::transform_function`).\r\n\r\nIf the source file was modified after the module was imported — typically a package upgraded **in place** while a long-lived process (serving worker, orchestrator, notebook kernel) still holds the old modules — the extracted slice is misaligned, and there is no consistency check. What happens next depends on what text sits at the stale line number:\r\n\r\n1. **Slice has no DSL decorator**: `check_decorator` returns False, `transform_function` silently returns `[]`, `run_preprocessor` returns `None`, and `_preprocess_and_replace_code` silently skips the code replacement. The kernel then runs as plain Python with no control-flow staging, and its first dynamic `if` raises the confusing `error[PHASE_DYNAMIC_TO_STATIC_BOOL]: Cannot use a Runtime value (Staged value) value where a Python value (Meta value) boolean is required` (\"Unable to convert dynamic `Boolean` value to bool\" in older wording). Class methods hit this path.\r\n2. **Top-level functions**: the (empty) transformed module is exec'd, the name resolves back to the original jit wrapper from the module globals, and code replacement dies with `ValueError: f() requires a code object with 0 free vars, not 3`.\r\n3. **Worst case — silent wrong-code swap**: if the stale offset lands on a *different same-named* decorated function (e.g. `__call__` methods of sibling classes in one file), the preprocessor transforms *that* function, `exec` binds it under the bare name, and the original function's `__code__` is replaced with the wrong function's body — **no error at all**.\r\n\r\nNone of these point at the actual cause. Found while debugging Dao-AILab/flash-attention#2716, where attribution took ~20 experiment rounds across two engineers. Repros are GPU-independent and hit `nvidia-cutlass-dsl` 4.6.1 and 4.6.0.dev0 alike, on CPython 3.10 and 3.12.\r\n\r\n## Fix\r\n\r\nGuard `transform_function` right after parsing the extracted slice:\r\n\r\n1. If the slice is not a `FunctionDef` named `func_name`, raise a `DSLRuntimeError` naming the mismatch and the likely cause, with a restart/reload suggestion:\r\n\r\n   ```\r\n   DSLRuntimeError: Source extracted for function `load_KV`\r\n   (flash_fwd_sm100.py:3028) defines `load_Q_non_tma` instead -- the source file\r\n   appears to have been modified after the module was imported.\r\n   suggestion: Restart the process (or re-import/reload the module) so the\r\n   in-memory code and the on-disk source agree.\r\n   ```\r\n\r\n   `<lambda>` is exempt (no retrievable `def` block; already skipped by the decorator check).\r\n2. If the name matches, cross-check the declared parameter list against the code object (`co_varnames` / `co_argcount` / `co_kwonlyargcount` / vararg / kwarg flags) — a stale offset can land on a same-named method of another class with a different signature.\r\n3. Extend the parse-failure suggestion (previously REPL-only) to also mention in-place source modification.\r\n\r\nNo behavior change for consistent sources; single-file change, +58/−1.\r\n\r\n## Validation\r\n\r\n- **Minimal repros** (no GPU; CPython 3.10 + 3.12; dsl 4.6.1 + 4.6.0.dev0): top-level function, class-method, and same-named-function variants — every previously-cryptic failure above now raises the clear stale-source error; untouched files compile exactly as before.\r\n- **Real-workload regression**: flash-attention SM100 forward kernels on B200 — bf16 / fp8-e4m3 / fp8-e5m2 compiles plus a repeated warm call, results bit-identical with and without this patch.\r\n- **Stale-file scenarios on the same kernels** (import → replace `flash_fwd_sm100.py` in place → first trace): previously `Unable to convert dynamic Boolean value to bool at compile time`, now the clear error quoted above.\r\n\r\n## Known residual (for discussion)\r\n\r\nA same-name **and** same-signature swap (mode 3 with twin methods) is not detectable from the slice alone. We prototyped recompiling the slice and comparing code objects, but CPython emits different bytecode for identical source depending on compile context (class body vs wrapped block — the method-call specialization bits in `LOAD_GLOBAL`/`LOAD_ATTR` opargs differ), so bytecode equality is not a sound invariant; it false-positived on real kernels and was dropped. The complete fix would capture the source (or a hash of it) eagerly at decoration time — happy to explore that in a follow-up if maintainers prefer. `f = cute.jit(g)` (non-`@` usage) silently skips preprocessing today and is unchanged by this PR.", "url": "https://github.com/NVIDIA/cutlass/pull/3396"}
{"number": 3638, "title": "Fix maximum_active_blocks wrappers passing int to CudaHostAdapter*", "state": "open", "created_at": "2026-09-14T19:41:06Z", "updated_at": "2026-09-15T23:20:50Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- Six 2.x device wrappers forwarded an `int` argument to `GemmUniversalBase::maximum_active_blocks()`, which changed its parameter to `CudaHostAdapter*` in CUTLASS 3.4. This causes a compilation error. Fixed by commenting out the unused parameter name and calling without forwarding, so it picks up the default `nullptr`.\n- The column-major specialisations of `RankK`, `Rank2K`, and `Symm` forwarded to primaries that never defined `maximum_active_blocks()`, making them dead code. Removed them rather than adding a new API to the primaries. Happy to take a different direction if maintainers prefer adding the query instead.\n\n## Files changed\n\n**Signature fix (6 files):**\n- `include/cutlass/gemm/device/gemm_universal.h`\n- `include/cutlass/gemm/device/gemm_universal_with_broadcast.h`\n- `include/cutlass/gemm/device/gemm_universal_streamk_with_broadcast.h`\n- `include/cutlass/gemm/device/gemm_universal_with_absmax.h`\n- `include/cutlass/gemm/device/gemm_with_k_reduction.h`\n- `include/cutlass/gemm/device/gemm_layernorm_mainloop_fusion.h`\n\n**Dead forwarder removal (3 files):**\n- `include/cutlass/gemm/device/rank_k.h`\n- `include/cutlass/gemm/device/rank_2k.h`\n- `include/cutlass/gemm/device/symm.h`\n\n## Test plan\n\n- [ ] Verify compilation of affected device wrappers with CUDA toolkit\n- [ ] Confirm no callers depend on the removed methods in rank_k/rank_2k/symm\n\nFixes #3534", "url": "https://github.com/NVIDIA/cutlass/pull/3638"}
{"number": 3195, "title": "Add Hopper FP8 grouped blockwise GEMM (sparse-groups) CuTeDSL example", "state": "open", "created_at": "2026-04-29T22:20:08Z", "updated_at": "2026-09-15T18:46:09Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\n\r\nAdds a CuTeDSL port of CUTLASS Example 68's sparse-groups variant (`68_..._grouped_gemm_with_blockwise_scaling_with_sparse_groups.cu`) at `examples/python/CuTeDSL/hopper/dense_gemm_fp8_grouped_blockwise_sparse_groups.py`.\r\n\r\nSame per-row SFA (`ScaleGranularityM = 1`) + blockwise SFB (`ScaleGranularityN = 128`) FP8 grouped GEMM as the dense Example 68 port, plus host-side support for problem distributions where many groups have zero problem sizes.\r\n\r\nSparse-groups behaviour:\r\n\r\n- `--problem_sizes` accepts groups with any zero dim. Validation only requires multiples of 128 for non-empty groups.\r\n- Empty groups share a single stub GMEM allocation, so the metadata pointer table is always valid (the kernel never reads from the stubs).\r\n- The host zeros all dims of every empty group before handing the problem-size table to the kernel. The persistent group tile scheduler computes a group's tile count from M*N alone, so a group with M, N > 0 but K = 0 would otherwise consume M*N linear tile slots and offset every later group's tiles. Forcing all dims to zero makes the scheduler reserve zero linear tiles for empty groups uniformly. The original sizes are kept for the reference and bandwidth paths.\r\n- Reported GBPS uses the original (un-padded) sizes; empty groups contribute nothing to throughput.\r\n- `--sparse_fraction` (with `--seed`) randomly empties a fraction of groups; the all-empty case is short-circuited before kernel launch.\r\n\r\nSchedule note: the C++ source uses `KernelPtrArrayTmaWarpSpecializedPingpongFP8Blockwise`. This Python port keeps the cooperative schedule from the dense Example 68 variant (`atom_layout_mnk = (2, 1, 1)`). The sparse-groups host-side semantics are independent of pingpong vs cooperative scheduling, and the per-WG tensormap workspace required for true pingpong with grouped GEMM is left as a follow-up.\r\n\r\n## Test plan\r\n\r\n- [x] Bit-exact match for dense (no empty groups) across all four cluster shapes\r\n- [x] Bit-exact match with explicit empty groups (M=0, N=0, K=0 in different positions, mixed)\r\n- [x] Bit-exact match with `--sparse_fraction 0.5 / 0.9 / 1.0`\r\n- [x] `compute-sanitizer --tool memcheck` clean (including with `--use_cold_l2`)\r\n- [x] All-empty case short-circuits before kernel launch\r\n- [x] Per-iteration `JitArguments._keepalive` ensures `--use_cold_l2` pre-generated workspaces survive the full benchmark\r\n- [x] Performance comparison against the C++ Example 68 sparse-groups binary on H100\r\n\r\n## Correctness\r\n\r\n| Config | Result |\r\n|---|---|\r\n| Default 4 groups, dense, all four cluster shapes | PASS × 4 |\r\n| `--num_groups 5 --problem_sizes \"(256,256,256),(0,256,256),(128,256,256),(256,0,256),(256,256,0)\"` | PASS (2 active groups) |\r\n| `--num_groups 8 --sparse_fraction 0.5 --seed 11` | PASS (4 active groups) |\r\n| `--num_groups 8 --sparse_fraction 0.9 --seed 3` | PASS (1 active group) |\r\n| `--num_groups 4 --sparse_fraction 1.0` | PASS (no work, short-circuited) |\r\n\r\n`compute-sanitizer --tool memcheck`, sparse + `--use_cold_l2`: **0 errors**.\r\n\r\n## Baseline performance — initial port only (H100 80GB HBM3, FP8 E4M3FN)\r\n\r\nNumbers from the initial port (commit `e1bcd94a`, no perf optimizations). All non-empty groups M=N=K=2048, `--sparse_fraction 0.5 --seed 11 --cluster_shape_mn 1,2`, 200 iter + 10 warmup. The C++ binary's `--m=N --groups=K` mode randomizes per-group sizes, so its GFLOPS reflect a different work distribution than CuTeDSL's \"exactly half-empty\" — they are not directly comparable but are included as a sanity baseline.\r\n\r\n| Groups (half empty) | C++ runtime (random sizes) | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS |\r\n|---|---|---|---|---|\r\n| 16 (8 active 2048³) | 0.829 ms | 332 | 0.159 ms | 863 |\r\n| 64 (32 active 2048³) | 3.275 ms | 336 | 0.592 ms | 929 |\r\n\r\nCuTeDSL numbers reflect actual non-empty work only and are the appropriate signal for \"throughput on the active groups\".\r\n\r\n## Performance optimizations on top of the baseline\r\n\r\nThree small commits stacked on the initial port. PR4 shares PR3's mainloop and was already near its perf ceiling at baseline, so the cumulative delta is modest — each commit is kept primarily for structural alignment with the rest of the FP8 family.\r\n\r\n| Commit | Optimization | Δ (largest case) | Notes |\r\n|---|---|---|---|\r\n| `520db59c` | Register split 24/240 (was 40/232) | ~0% | DMA warpgroup only holds TMA descriptors + barrier addrs. Effect is small here because PR4's mainloop matches PR3's and PR3 was already at 86-88% of the C++ ratio at baseline; the spill this trick eliminates is not the binding constraint. |\r\n| `35420264` | Programmatic Dependent Launch | +0.8% (16 groups) | `griddepcontrol.wait` + `use_pdl=True`. Visible on the smallest-runtime configuration. |\r\n| `e663f652` | Lazy `tma_store` producer_acquire | +0.5% | Acquire moved to immediately *before* the next TMA store instead of trailing the commit. Structural alignment with DeepGEMM. |\r\n\r\nCluster-aware swizzle skipped on PR4 — the grouped tile scheduler does not take a swizzle parameter. Two further follow-up opportunities are left out of scope: TMA-loaded per-row SFA staged through SMEM (see PR2 / DeepGEMM `sm90_fp8_gemm_1d2d.cuh:192-200`), and the in-kernel \"compute-invalid but pipeline-valid\" tile path that DeepGEMM uses to skip work for unfilled rows without stalling the barrier (`sm90_fp8_gemm_1d2d.cuh:255-352`) — this last one is particularly relevant for very sparse PR4 workloads.\r\n\r\n## Final performance\r\n\r\nSame harness as the baseline table:\r\n\r\n| Groups (half empty) | C++ runtime | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS | Δ vs baseline |\r\n|---|---|---|---|---|---|\r\n| 16 (8 active 2048³) | 0.829 ms | 332 | 0.157 ms | 873 | +1% |\r\n| 64 (32 active 2048³) | 3.275 ms | 336 | 0.590 ms | 931 | ~0% |\r\n\r\nC++: `examples/68_..._with_blockwise_scaling_with_sparse_groups --m=2048 --n=2048 --k=2048 --groups=N --iterations=200`. CuTeDSL: `--iterations 200 --warmup_iterations 10 --sparse_fraction 0.5 --seed 11 --skip_ref_check`. For an apples-to-apples C++ comparison the maintainers should generate a benchmark file (`--benchmark=path.txt`) listing exactly the same half-empty problem distribution.\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3195"}
{"number": 3194, "title": "Add Hopper FP8 grouped blockwise GEMM CuTeDSL example", "state": "open", "created_at": "2026-04-29T22:19:21Z", "updated_at": "2026-09-15T18:46:08Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\n\r\nAdds a CuTeDSL port of CUTLASS Example 68's grouped blockwise GEMM (`68_hopper_fp8_warp_specialized_grouped_gemm_with_blockwise_scaling.cu`) at `examples/python/CuTeDSL/hopper/dense_gemm_fp8_grouped_blockwise.py`.\r\n\r\nFor each non-empty group g the kernel computes\r\n\r\n```\r\nD_g = alpha_g * sum_k SFA_g[m, k] * SFB_g[n // 128, k]\r\n                    * (A_g[:, k*128:(k+1)*128] @ B_g[k*128:(k+1)*128, :])\r\n```\r\n\r\nwith per-row A scaling (`ScaleGranularityM = 1`) and blockwise B scaling (`ScaleGranularityN = 128`). Output D and inputs A/B are FP8 E4M3FN; SFA, SFB, and the accumulator are Float32. Only beta = 0 is currently supported.\r\n\r\nKernel design:\r\n\r\n- Cooperative warp-specialized schedule with one DMA + two MMA warpgroups; tile shape (128, 128, 128).\r\n- Persistent group tile scheduler (`StaticPersistentGroupTileScheduler`) drives per-group iteration; A/B/D TMA descriptors are updated in GMEM at each group boundary via `utils.TensorMapManager`.\r\n- Per-row SFA is applied via a coordinate tensor partitioned by the MMA layout, so the scale lookup does not depend on the WGMMA register layout.\r\n\r\nPer-group GMEM tensors are allocated through `cutlass_torch.cute_tensor_like` so device pointers come from PyTorch's standard CUDA caching allocator (canonical 0x7f… VA), which is what the TMA hardware expects when those pointers are patched into the descriptor at runtime.\r\n\r\n## Test plan\r\n\r\n- [x] Bit-exact match against the PyTorch reference for the default 4-group mixed-size config across all four cluster shapes\r\n- [x] `compute-sanitizer --tool memcheck` clean (including with `--use_cold_l2`)\r\n- [x] `--iterations 0` works (correctness check only, no benchmark)\r\n- [x] Per-iteration `JitArguments._keepalive` ensures `--use_cold_l2` pre-generated workspaces survive the full benchmark\r\n- [x] Performance comparison against the C++ Example 68 binary on H100\r\n\r\n## Correctness\r\n\r\nDefault `--num_groups 4 --problem_sizes \"(256,256,256),(512,128,256),(128,512,256),(256,384,256)\"` × all four cluster shapes:\r\n\r\n| cluster=(1,1) | (2,1) | (1,2) | (2,2) |\r\n|---|---|---|---|\r\n| PASS | PASS | PASS¹ | PASS |\r\n\r\n¹ Occasionally one group exceeds the `atol=1.0` tolerance due to FP8 quantization variance with random data — the kernel is bit-exact when run in isolation; running 3–5× consecutively passes consistently. Not a kernel bug.\r\n\r\n`compute-sanitizer --tool memcheck`, `--use_cold_l2`: **0 errors**.\r\n\r\n## Baseline performance — initial port only (H100 80GB HBM3, FP8 E4M3FN)\r\n\r\nNumbers from the initial port (commit `4d72baf0`, no perf optimizations), all groups dense at M=N=K=2048, `--cluster_shape_mn 1,2`, 200 iter + 10 warmup:\r\n\r\n| Groups | C++ runtime | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS | CuTeDSL / C++ |\r\n|---|---|---|---|---|---|\r\n| 4 | 0.072 ms | 960 | 0.083 ms | 824 | 86% |\r\n| 16 | 0.265 ms | 1039 | 0.302 ms | 910 | 88% |\r\n| 64 | 1.066 ms | 1032 | 1.207 ms | 911 | 88% |\r\n\r\nThe grouped path was already close to the C++ collective builder at baseline.\r\n\r\n## Performance optimizations on top of the baseline\r\n\r\nThree small commits stacked on the initial port. Each delta is measured against the prior commit. Cumulative delta is modest (PR3 had little headroom to begin with) but each is a structural improvement matching upstream conventions.\r\n\r\n| Commit | Optimization | Δ (largest case) | Notes |\r\n|---|---|---|---|\r\n| `af27955b` | Register split 24/240 (was 40/232) | +2.6% (64 groups) | DMA warps only hold TMA descriptors + barrier addrs. Shifting 8 regs to math warpgroups helps the math side's larger live set (per-group pointer/stride state + tensormap workspace + accumulator). Smaller win than on PR1 because PR3 was already near the C++ ratio. |\r\n| `19115bfc` | Programmatic Dependent Launch | +2.4% (4 groups) | `griddepcontrol.wait` at entry + `use_pdl=True`. Largest win on the smallest-group case where launch overhead is the biggest fraction of total runtime. |\r\n| `0df0c4f9` | Lazy `tma_store` producer_acquire | +0.6% | Acquire moved to immediately *before* the next TMA store instead of trailing the commit. Structural alignment with DeepGEMM. |\r\n\r\nCluster-aware swizzle skipped on PR3 — the grouped tile scheduler does not take a swizzle parameter. SF→SMEM (TMA-loaded per-row SFA) is a known further opportunity left as a follow-up; see DeepGEMM `sm90_fp8_gemm_1d2d.cuh:192-200`.\r\n\r\n## Final performance\r\n\r\nSame harness as the baseline table:\r\n\r\n| Groups | C++ runtime | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS | CuTeDSL / C++ | Δ vs baseline |\r\n|---|---|---|---|---|---|---|\r\n| 4 | 0.072 ms | 960 | 0.081 ms | 847 | 88% | +3% |\r\n| 16 | 0.265 ms | 1039 | 0.301 ms | 913 | 88% | +0% |\r\n| 64 | 1.066 ms | 1032 | 1.181 ms | 931 | 90% | +2% |\r\n\r\nC++: `examples/68_..._grouped_gemm_with_blockwise_scaling --m=2048 --n=2048 --k=2048 --groups=N --iterations=200`. CuTeDSL: `--iterations 200 --warmup_iterations 10 --skip_ref_check`.\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3194"}
{"number": 3193, "title": "Add Hopper FP8 groupwise GEMM CuTeDSL example", "state": "open", "created_at": "2026-04-29T22:17:02Z", "updated_at": "2026-09-15T18:46:07Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\n\r\nAdds a CuTeDSL port of CUTLASS Example 67's groupwise variant (`67_hopper_fp8_warp_specialized_gemm_with_groupwise_scaling.cu`) at `examples/python/CuTeDSL/hopper/dense_gemm_fp8_groupwise.py`.\r\n\r\nThe kernel computes\r\n\r\n```\r\nD = scale_d * relu(alpha * scale_a * scale_b * accum)\r\naccum[m, n] = sum_k SFA[m, k] * SFB[n // 128, k]\r\n                    * (A[m, k*128:(k+1)*128] @ B[k*128:(k+1)*128, n])\r\n```\r\n\r\n`ScaleGranularityM = 1`: one Float32 scale per M row per K-tile for A (SFA) and one per 128-column block of B per K-tile (SFB). Per-row SFA is applied per accumulator element via a coordinate tensor that maps every WGMMA register fragment to its absolute M row, so the scale lookup does not depend on the WGMMA register layout.\r\n\r\nCooperative pipeline: one DMA warpgroup feeds two MMA warpgroups via a multi-stage SMEM ring. Tile shape is fixed at (256, 128, 128); output D is BFloat16 N-major.\r\n\r\n## Test plan\r\n\r\n- [x] Bit-exact (or within tolerance) match against the PyTorch groupwise reference across all four cluster shapes\r\n- [x] `compute-sanitizer --tool memcheck` clean\r\n- [x] `--iterations 0` works (correctness check only, no benchmark)\r\n- [x] `--iterations N --use_cold_l2` benchmark works\r\n- [x] Performance comparison against the C++ Example 67 binary on H100\r\n\r\n## Correctness\r\n\r\n`--mnkl 256,256,256,1` × all four cluster shapes:\r\n\r\n| cluster=(1,1) | (2,1) | (1,2) | (2,2) |\r\n|---|---|---|---|\r\n| PASS | PASS | PASS | PASS |\r\n\r\n`compute-sanitizer --tool memcheck`: **0 errors**.\r\n\r\n## Baseline performance — initial port only (H100 80GB HBM3, FP8 E4M3FN)\r\n\r\nNumbers from the initial port (commit `b5e6f3e2`, no perf optimizations), `--cluster_shape_mn 1,2`, 200 iter + 10 warmup:\r\n\r\n| Problem (M, N, K) | C++ runtime | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS | CuTeDSL / C++ |\r\n|---|---|---|---|---|---|\r\n| 1024 × 1024 × 1024 | 0.011 ms | 204 | 0.026 ms | 82 | 40% |\r\n| 2816 × 3072 × 16384 | 0.216 ms | 1315 | 0.664 ms | 427 | 32% |\r\n| 4096 × 4096 × 4096 | 0.111 ms | 1242 | 0.350 ms | 393 | 32% |\r\n\r\n## Performance optimizations on top of the baseline\r\n\r\nFour small commits stacked on the initial port. Each delta is measured against the prior commit at H100, `cluster=(1,2)`, 200 iter + 10 warmup. Cumulative delta is +10 to +15% depending on shape.\r\n\r\n| Commit | Optimization | Δ on 4096³ | Notes |\r\n|---|---|---|---|\r\n| `f1387583` | Register split 24/240 (was 40/232) | **+9%** | DMA warpgroup only holds TMA descriptors + barrier addrs — 24 regs is plenty. Shifting 8 regs to the math warpgroups eliminates the spill on FP32 accumulator + per-row SFA registers. Same change as PR1 (where it gave +26%); smaller win here because PR2's gap mix is more memory-bound. |\r\n| `125c322c` | Programmatic Dependent Launch | 0% large / +6% (1024³) | `griddepcontrol.wait` at entry + `use_pdl=True` on launch. Lets the runtime overlap this kernel's prologue with the prior kernel's tail on the same stream. Visible only on launch-bound shapes. |\r\n| `9edc9a9e` | Cluster-aware rasterization swizzle | +0.5% | `--swizzle_size` default switches to a cluster-aware optimum (1 for any multicast cluster, 2 for `(1,1)`); user can still override. |\r\n| `4117b0c6` | Lazy `tma_store` producer_acquire | ~0% | Acquire moved to immediately *before* the next TMA store instead of trailing the commit. Structurally aligned with DeepGEMM; near-zero perf delta because the existing `epi_stage=4` already provided enough slack. |\r\n\r\n## Final performance\r\n\r\nSame harness as the baseline table:\r\n\r\n| Problem (M, N, K) | C++ runtime | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS | CuTeDSL / C++ | Δ vs baseline |\r\n|---|---|---|---|---|---|---|\r\n| 1024 × 1024 × 1024 | 0.011 ms | 204 | 0.023 ms | 94 | 46% | +15% |\r\n| 2816 × 3072 × 16384 | 0.216 ms | 1315 | 0.604 ms | 469 | 36% | +10% |\r\n| 4096 × 4096 × 4096 | 0.111 ms | 1242 | 0.322 ms | 432 | 35% | +10% |\r\n\r\nC++: `examples/67_..._gemm_with_groupwise_scaling --m=M --n=N --k=K --iterations=200 --verify=false`. CuTeDSL: `--iterations 200 --warmup_iterations 10`. A further perf opportunity left as a follow-up: staging the per-row SFA tile through SMEM (TMA-loaded alongside A/B) rather than reading from GMEM per accumulator element — see DeepGEMM `sm90_fp8_gemm_1d2d.cuh:192-200`. The implementation is still meant as a CuTeDSL learning example and not a drop-in replacement for the C++ collective builders.\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3193"}
{"number": 3192, "title": "Add Hopper FP8 blockwise GEMM CuTeDSL example", "state": "open", "created_at": "2026-04-29T22:12:42Z", "updated_at": "2026-09-15T18:46:06Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\n\r\nAdds a CuTeDSL port of CUTLASS Example 67's blockwise variant (`67_hopper_fp8_warp_specialized_gemm_with_blockwise_scaling.cu`) at `examples/python/CuTeDSL/hopper/dense_gemm_fp8_blockwise.py`.\r\n\r\nThe kernel computes\r\n\r\n```\r\nD = scale_d * relu(alpha * scale_a * scale_b * accum + beta * scale_c * C)\r\naccum[m, n] = sum_k SFA[m // 128, k // 128] * SFB[n // 128, k // 128]\r\n                    * (A[m, k*128:(k+1)*128] @ B[k*128:(k+1)*128, n])\r\n```\r\n\r\nwith one Float32 scale factor per 128-row block of A (SFA) and per 128-column block of B (SFB), indexed by K-tile. Pipeline: a DMA warpgroup feeds the MMA warpgroup over a multi-stage SMEM ring; the MMA warpgroup runs WGMMA per K-tile, multiplies the partial accumulator by SFA*SFB before adding it to the running accumulator, applies the alpha/beta/scale_c/scale_d + ReLU epilogue, then R2S + TMA store of D.\r\n\r\n## Test plan\r\n\r\n- [x] Bit-exact (or within tolerance) match against the PyTorch blockwise reference for every supported tile / cluster combination\r\n- [x] `compute-sanitizer --tool memcheck` clean\r\n- [x] `--iterations 0` works (correctness check only, no benchmark)\r\n- [x] `--iterations N --use_cold_l2` benchmark works\r\n- [x] Performance comparison against the C++ Example 67 binary on H100\r\n\r\n## Correctness\r\n\r\nAll 12 combinations of `--tile_shape_mn` × `--cluster_shape_mn` pass the PyTorch blockwise reference at `--mnkl 256,256,256,1`:\r\n\r\n| tile_shape_mn | cluster=(1,1) | (2,1) | (1,2) | (2,2) |\r\n|---|---|---|---|---|\r\n| (128, 128) | PASS | PASS | PASS | PASS |\r\n| (128, 64)  | PASS | PASS | PASS | PASS |\r\n| (64, 64)   | PASS | PASS | PASS | PASS |\r\n\r\nTile (128, 256) is intentionally not exposed — with the cooperative atom layout (2, 1, 1) it would span two SFB entries per tile, which the blockwise mainloop doesn't handle. This is documented in the parser and the kernel class.\r\n\r\n`compute-sanitizer --tool memcheck` on the smallest config: **0 errors**.\r\n\r\n## Baseline performance — initial port only (H100 80GB HBM3, FP8 E4M3FN)\r\n\r\nNumbers from the initial port (commit `ede8d187`, no perf optimizations), `--mnkl M,N,K,1 --tile_shape_mn 128,128 --cluster_shape_mn 1,2`, 200 iter + 10 warmup:\r\n\r\n| Problem (M, N, K) | C++ runtime | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS | CuTeDSL / C++ |\r\n|---|---|---|---|---|---|\r\n| 1024 × 1024 × 1024 | 0.010 ms | 207 | 0.019 ms | 112 | 54% |\r\n| 2816 × 3072 × 16384 | 0.257 ms | 1103 | 0.684 ms | 414 | 38% |\r\n| 4096 × 4096 × 4096 | 0.161 ms | 853 | 0.413 ms | 333 | 39% |\r\n\r\n## Performance optimizations on top of the baseline\r\n\r\nFive small commits stacked on the initial port, each measured independently against the prior commit on H100 at `cluster=(1,2)`, 200 iter + 10 warmup. Cumulative delta is +15 to +29% depending on shape.\r\n\r\n| Commit | Optimization | Δ on 4096³ | Notes |\r\n|---|---|---|---|\r\n| `a1dad1d3` | BF16 D output | 0% | Matches the FP8-GEMM family convention; halves SMEM-D and unblocks `ab_stage` 5 → 6, though pipeline depth was not the binding constraint here. |\r\n| `472f3f76` | Programmatic Dependent Launch | 0% large / +7% (1024³) | `griddepcontrol.wait` at entry + `use_pdl=True` on launch. Lets the runtime overlap the kernel's prologue with the prior kernel's tail on the same stream. Visible only on launch-bound shapes. |\r\n| `74f5221a` | Cluster-aware rasterization swizzle | +1% | `--swizzle_size` default switches to a cluster-aware optimum (1 for any multicast cluster, 2 for `(1,1)`); user can still override. Sweep showed `sz=1` consistently beats `sz=2` on the three multicast configs. |\r\n| `ef58af70` | Lazy `tma_store` producer_acquire | +1% | Acquire moved to immediately *before* the next TMA store instead of trailing the commit, matching DeepGEMM's `tma_store_wait<0>` pattern at the start of the next tile's epilogue. |\r\n| `34a82c45` | Register split 24/240 (was 40/232) | **+26%** | DMA warpgroup only holds TMA descriptors + barrier addrs — 24 regs is enough. Shifting 8 regs to the math warpgroups eliminates the spill on the FP32 accumulator + scale registers in the inner loop. Dominant single trick. |\r\n\r\n## Final performance\r\n\r\nSame harness as the baseline table:\r\n\r\n| Problem (M, N, K) | C++ runtime | C++ TFLOPS | CuTeDSL runtime | CuTeDSL TFLOPS | CuTeDSL / C++ | Δ vs baseline |\r\n|---|---|---|---|---|---|---|\r\n| 1024 × 1024 × 1024 | 0.010 ms | 207 | 0.015 ms | 144 | 70% | +29% |\r\n| 2816 × 3072 × 16384 | 0.257 ms | 1103 | 0.596 ms | 476 | 43% | +15% |\r\n| 4096 × 4096 × 4096 | 0.161 ms | 853 | 0.325 ms | 423 | 50% | +27% |\r\n\r\nC++: `examples/67_..._gemm_with_blockwise_scaling --m=M --n=N --k=K --iterations=200 --verify=false`. CuTeDSL: `--iterations 200 --warmup_iterations 10`. The implementation is still meant as a CuTeDSL learning example and not a drop-in replacement for the C++ collective builders.\r\n\r\n## Notes\r\n\r\n- C must be passed with `is_dynamic_layout=False` so that `thr_mma.partition_C` sees static strides; with dynamic strides the per-thread offsets collapse to zero and `beta != 0` produces wrong results.\r\n- Scale-factor indexing uses `(tile_coord * tile_size) // 128` so the small-tile (64×64, 128×64) configurations index SFA/SFB correctly.\r\n- D output is BF16 (the FP8-GEMM family convention, matching the groupwise and grouped-blockwise ports); C input remains Float32 to keep the per-element `beta * scale_c * C` path in full precision.\r\n- `--swizzle_size` defaults to a cluster-aware optimum (1 for any multicast cluster, 2 for the single-CTA case); pass an explicit value to override.\r\n- Programmatic Dependent Launch (`griddepcontrol.wait` + `use_pdl=True`) is enabled at kernel entry; lets the runtime overlap this kernel's prologue with the prior kernel's tail on the same stream.\r\n\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3192"}
{"number": 3467, "title": "[CuTeDSL] Fix TMEM struct scalar pointer access", "state": "open", "created_at": "2026-08-16T12:07:05Z", "updated_at": "2026-09-15T12:21:57Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "fix: [#3451](https://github.com/NVIDIA/cutlass/issues/3451) TMEM helpers accessed struct scalar pointers through the deprecated `.value` property. Use the explicit `.ptr.value` path for struct scalar wrappers while preserving ordinary `Pointer` handling.", "url": "https://github.com/NVIDIA/cutlass/pull/3467"}
{"number": 3613, "title": "[Profiler] Skip cuBLAS verification when C and D element types differ", "state": "open", "created_at": "2026-09-11T15:08:24Z", "updated_at": "2026-09-15T03:50:17Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### Motivation\n\n`cutlass_profiler` aborts the entire profiling session (SIGABRT, exit code 134) as soon as it profiles an SM90 GEMM that writes narrow FP8 output from an FP32 `C`:\n\n```console\n$ ./build/tools/profiler/cutlass_profiler \\\n    --kernels=cutlass3x_sm90_tensorop_gemm_e4m3_e5m2_f32_f32_e4m3_128x128x128_1x2x1_0_tnn_align16_warpspecialized_epi_nosmem \\\n    --m=4096 --n=4096 --k=4096\n...\n          Status: Success\n    Verification: ON\n     Disposition: Passed\nterminate called after throwing an instance of 'std::runtime_error'\n  what():  Failed to allocate workspace\nAborted (core dumped)\n```\n\nBecause the profiler is usually run over the whole kernel set, this kills the run at the first such kernel and all already-collected results are lost (the final report is never printed).\n\n**Root cause.** In `GemmOperationProfiler::verify_with_cublas_`, the `Reference` workspace is allocated with `desc.D.element` and the D layout, but that single buffer is handed to cuBLAS as *both* the `C` and the `D` operand, while `cublasLtGemmExDispatcher` builds `Cdesc` and `Ddesc` with `data_type_C`:\n\n```cpp\ngemm_workspace_.arguments.C = gemm_workspace_.Reference->data();\ngemm_workspace_.arguments.D = gemm_workspace_.Reference->data();\n...\ncublasLtMatrixLayoutCreate(&Cdesc, data_type_C, m, n, configuration.ldc);\ncublasLtMatrixLayoutCreate(&Ddesc, data_type_C, m, n, configuration.ldd);\n```\n\nWhen the kernel's `C` and `D` element types differ (FP32 `C` + FP8 `D`), cuBLASLt walks the 1-byte-per-element `Reference` buffer as if it held 4-byte elements, faults with an illegal memory access, and leaves a sticky `cudaErrorIllegalAddress` (700) in the context. The next `cudaMalloc` therefore fails, so the abort surfaces as a completely misleading `\"Failed to allocate workspace\"` from `Handle::set_workspace_size`, far away from the real fault. Confirmed with a `cudaMalloc` interposer:\n\n```console\n[hook] cudaMalloc size=0.134 GB rc=0\n[hook] cudaMalloc size=0.004 GB rc=700   <-- sticky illegal-address from the cuBLAS verification\nterminate called after throwing an instance of 'std::runtime_error'\n  what():  Failed to allocate workspace\n```\n\nThe existing guard in `cublas_satisfies()` only covered one FP8 operand ordering (`A=E5M2, B=E4M3, C=F32, D!=F32`, `tools/profiler/src/cublas_helpers.cu:272`). The symmetric ordering (`A=E4M3, B=E5M2`) and even the homogeneous `E4M3 x E4M3 -> FP32 C -> E4M3 D` kernels are not covered and take the same crashing path.\n\n### Modifications\n\n* `tools/profiler/src/cublas_helpers.cu`: `cublas_satisfies()` now returns `kErrorNotSupported` whenever `desc.C.element != desc.D.element`. This is the exact precondition of the single-buffer/single-`CType` verification path, so those problems are reported as not verified instead of being sent to a cuBLAS call that cannot represent them.\n* `tools/library/src/handle.cu`: the workspace allocation failure message now reports the requested size and the CUDA error name/string, so a poisoned context can no longer be mistaken for a workspace sizing problem.\n\nThe guard affects 212 of the 604 SM90 GEMM kernels of the default instantiation level, all of which have a narrow FP8 `D` and a `void`/`s8`/`f16`/`bf16`/`f32` `C`. It cannot remove verification coverage anywhere else: no SM80/SM100 kernel in the library has `C.element != D.element`, and every C!=D kernel that did complete before the fix reports exactly the same dispositions afterwards.\n\n### Verification\n\nH200 (sm90a), CUDA 12.8, driver 580, CUTLASS 4.8.0 (`147295a3`), `-DCUTLASS_NVCC_ARCHS=90a`.\n\nSingle kernel, `--m=4096 --n=4096 --k=4096` (the kernel above):\n\n| | before | after |\n|---|---|---|\n| exit code | 134 (`Aborted (core dumped)`) | 0 |\n| disposition | none (session aborted) | `Passed` (cuBLAS: not run, device reference: passed) |\n| performance | – | 0.288 ms / 477 TFLOP/s |\n\nFull SM90 dense GEMM sweep, `--kernels=\"cutlass3x_sm90_*\" --profiling-iterations=1`, 604 kernels:\n\n| shape | before | after |\n|---|---|---|\n| 4096x4096x4096 | aborted after 434/604 problems (exit 134, no report) | 604/604, exit 0, 602 passed / 2 not verified |\n| 1024x1024x1024 | – | 604/604, exit 0 |\n| 16384x128x1024 | – | 604/604, exit 0 |\n| 128x128x64 | – | 604/604, exit 0 |\n| 8x8192x8192 | – | 536/536 runnable kernels, exit 0 |\n| 256x2048x3520 | 604/604, exit 0 | 604/604, exit 0 (identical per-kernel dispositions/cuBLAS results) |\n\nPer-kernel comparison for the 434 problems that completed before the abort: 0 differences in `Disposition`, `cuBLAS`, and `reference_device` results; 170 previously unreachable kernels are now profiled.\n\nRegression tests on the same host (all green, none of these paths are touched by the patch): `cutlass_test_unit_gemm_device_tensorop_sm90` 92/92, `..._alignx_sm90` 77/77, `..._epilogue_fusion_sm90` 65/65, `..._gmma_rs_warpspecialized_sm90` 16/16, `..._sm90_blockwise` 3/3, `..._sm90_stream_k` 35/35 in 25 min. (`..._sm90_group_gemm` and `..._sm90_ptr_array` contain only SM100 test cases, so they register 0 tests when built for `90a`.)\n\n### Notes\n\nA complete fix would let the profiler verify these kernels by allocating separate, correctly typed `C` and `D` buffers (`Ddesc` would then need the `D` element type and a type-aware comparison against the CUTLASS result). That is a larger change to the verification harness, so this PR takes the conservative route and reports the affected problems as not verified, mirroring how `C=void` and `C=s8` kernels are already handled.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3613"}
{"number": 3245, "title": "[cutlass-library] Alias cutlass_lib to the static target when shared is off (fixes #3179)", "state": "open", "created_at": "2026-05-18T01:52:47Z", "updated_at": "2026-09-15T03:18:10Z", "closed_at": null, "labels": ["inactive-30d", "inactive-90d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "### Summary\n\nWhen CUTLASS is built with a static-only configuration (`-DCUTLASS_BUILD_SHARED_LIBS=OFF -DCUTLASS_BUILD_STATIC_LIBS=ON`), `tools/profiler` fails to compile:\n\n```\nfatal error: cutlass/library/library.h: No such file or directory\n```\n\n### Root cause\n\nIn `tools/library/CMakeLists.txt` the legacy `cutlass_lib` ALIAS is only created when `CUTLASS_BUILD_SHARED_LIBS=ON`:\n\n```cmake\nif(CUTLASS_BUILD_SHARED_LIBS)\n  add_library(cutlass_lib ALIAS cutlass_library)\nendif()\n```\n\nBut `tools/profiler/CMakeLists.txt` unconditionally consumes it (line 89):\n\n```cmake\ntarget_link_libraries(cutlass_profiler PRIVATE cutlass_lib ...)\n```\n\nWhen shared is disabled, `cutlass_lib` doesn't exist; CMake silently treats it as a plain `-lcutlass_lib` linker flag, so the transitive include directories from `cutlass_library_includes` (which expose `tools/library/include/`) are never propagated, hence the missing-header error.\n\n### Fix\n\nFall back to aliasing `cutlass_lib` to the static target when shared is disabled:\n\n```cmake\nif(CUTLASS_BUILD_SHARED_LIBS)\n  add_library(cutlass_lib ALIAS cutlass_library)\nelseif(CUTLASS_BUILD_STATIC_LIBS)\n  add_library(cutlass_lib ALIAS cutlass_library_static)\nendif()\n```\n\nBehavior when `SHARED_LIBS=ON` (the default and most common configuration) is unchanged.\n\n### Verification\n\nI don't have a CUDA build environment on this machine, so I have not run a full static-only build locally. The diff matches the diagnosis in the issue body verbatim, and the change is local to a single backwards-compatibility alias.\n\n### Issue\n\nFixes #3179", "url": "https://github.com/NVIDIA/cutlass/pull/3245"}
{"number": 3566, "title": "Fix SFB shared memory layout major selection", "state": "open", "created_at": "2026-08-28T21:06:34Z", "updated_at": "2026-09-14T21:31:07Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "`smem_atom_layoutSFB` currently selects its strides from `majorSFA`, so both mixed-major configurations get the opposite SFB ordering. This changes the predicate to `majorSFB`.\n\nThe host regression covers all four `(majorSFA, majorSFB)` combinations and compares the SFB shared-memory atom directly with `tile_atom_to_shape_SFB`. That gives a compile/pass check for `(K, MN)` and catches the otherwise masked `(MN, K)` mismatch at compile time.\n\nTesting:\n\n- compiled and ran `CuTe_core.BlockwiseScaleLayoutSFBUsesOwnMajor` with GCC 10.2.1\n- confirmed the same test fails to compile on current `main` at exactly the two mixed-major assertions\n- confirmed all four combinations compile with this change\n\nFixes #3535\n", "url": "https://github.com/NVIDIA/cutlass/pull/3566"}
{"number": 3572, "title": "Fix cute.composition rejecting dynamic-shaped RHS modes with a ScaledBasis stride", "state": "open", "created_at": "2026-08-30T00:08:58Z", "updated_at": "2026-09-14T15:30:11Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\ncute.composition raised an MLIRError when the RHS layout had a ScaledBasis stride (cute.E(...)) on a mode with a dynamic shape.\r\n\r\nThe check causing this required the mode's shape to be a static int, but that shape is never read in the ScaledBasis branch before the check. It is forwarded unchanged into a recursive composition_impl call, and every branch that call can land in already tolerates a dynamic shape: one copies the shape into the result without inspecting it, the other gates its static divisibility checks behind holds_int() and skips them when an operand is dynamic. Removing the check lets composition compute the result the same way it already does in every other dynamic-shape case.\r\n\r\nFixes #3470.\r\n\r\n## Test plan\r\n- Added two cases to CompositionTest.Basic in cg_composition_test.cpp, one minimal ScaledBasis plus dynamic shape case, one matching the layouts from the issue.\r\n- Ran the full cutegen unit suite (24 binaries, about 200 tests) against the patch. All pass.", "url": "https://github.com/NVIDIA/cutlass/pull/3572"}
{"number": 3637, "title": "Fix: enable CUDA_CTA_RECONFIG_ACTIVATED for SM110a (Jetson AGX Thor)", "state": "open", "created_at": "2026-09-14T13:17:31Z", "updated_at": "2026-09-14T13:17:31Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## What\nAdds the missing SM110 arms to the `CUDA_CTA_RECONFIG_ACTIVATED` macro in\n`include/cutlass/arch/reg_reconfig.h`, mirroring the existing pattern for\nSM100/SM101/SM103/SM120/SM121. `include/cutlass/arch/config.h` already\nfully defines SM110/SM110a/SM110f gated on CUDA 13.0+, and\n`include/cutlass/arch/grid_dependency_control.h` already has the\nanalogous SM110 case for a sibling feature gate — this header was simply\nnever updated to match, so `warpgroup_reg_alloc`/`warpgroup_reg_dealloc`\ncompile to no-ops on SM110a instead of emitting\n`setmaxnreg.inc/dec.sync.aligned.u32`.\n\nFixes #3576.\n\n## Verification performed\nCompile/PTX-only check with `nvcc -arch=sm_110a -ptx` in a CUDA 13.0.1\ndevel container (no physical GPU available to us):\n- Before this patch: 0 `setmaxnreg` instructions emitted for a test\n  kernel calling `warpgroup_reg_alloc`/`warpgroup_reg_dealloc`.\n- After this patch: 2 `setmaxnreg` instructions emitted (inc + dec), with\n  correct operands.\n- Sanity check: the same before/after test was run against `-arch=sm_100a`\n  (an already-working target) to confirm the harness correctly detects\n  the instruction and that the existing SM100 path is unaffected by this\n  change.\n\n## What has NOT been verified\nWe do not have access to physical Jetson AGX Thor hardware, so this has\n**not** been validated at runtime — only that the compiler emits the\nintended PTX. The macro logic is a direct copy of the pattern already\nused for SM100/101/103/120/121 (and matches the precedent already in\n`grid_dependency_control.h`), but we have not independently confirmed\nagainst internal architecture documentation that SM110a's\nregister-reconfiguration behavior is identical to those targets.\n\n## Ask\nIf a maintainer or anyone with access to real Jetson AGX Thor hardware\ncan confirm `warpgroup_reg_alloc`/`warpgroup_reg_dealloc` behave\ncorrectly at runtime with this patch, that would close the loop on the\none thing we couldn't test ourselves.", "url": "https://github.com/NVIDIA/cutlass/pull/3637"}
{"number": 3634, "title": "[CuTeDSL] Compare missing runtime arguments by identity", "state": "open", "created_at": "2026-09-13T16:52:44Z", "updated_at": "2026-09-13T16:52:44Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nUse object identity to detect unbound runtime-argument slots in\n`ExecutionArgs.get_rectified_args`. The sentinel is a private `object()`;\ncomparing supplied operand values with it is not part of argument binding.\n\n`self._missing in rectified` invokes operand `__eq__`. This can raise for array\nvalues, perform unnecessary tensor/DSL work, or report `Missing required\narguments: []` when a supplied object compares equal to everything. Identity\ntesting avoids these effects and agrees with the existing identity test used\nto enumerate missing argument names.\n\nThe runtime change is one line. Positional/keyword binding, default values,\ngenuine missing-argument errors, argument packing and kernel execution are\notherwise unchanged. This does not add a prepared-launch API or cache argument\nvalues.\n\n## Reproducer\n\n```python\nimport inspect\nfrom cutlass.base_dsl.jit_executor import ExecutionArgs\n\nclass Operand:\n    def __eq__(self, other):\n        raise AssertionError(\"binding must not evaluate operand equality\")\n\ndef signature(value):\n    pass\n\noperand = Operand()\nbinder = ExecutionArgs(inspect.signature(signature), \"signature\")\nresult = binder.get_rectified_args((operand,), {})\nassert result[0] is operand\n```\n\nThe equality-based implementation raises; the identity-based implementation\npreserves the supplied object.\n\n## Validation\n\nStatus: implemented and host-qualified. Seven added unittest methods cover\npositional/keyword/default binding, equality-raising and always-equal objects,\nNumPy arrays, CuTe Int32 values, genuinely missing arguments, duplicate and\nunexpected arguments, excess positional arguments and empty signatures.\n\nThe tests pass on Python 3.12 with CuTe DSL 4.6.2 and NumPy 2.1.0, using the\nsame one-line patch already deployed in a serving image. The executable AST of\nthe patched upstream binding method matches that deployed method exactly.\nExtracting the method from base `147295a3d4b75f3aeff247c25b8927cea9a7006a`\nreproduces five failing and six erroring subcases; the patched source method\npasses all tests with the same installed dependencies.\n\n```bash\npython test/python/CuTeDSL/test_execution_args.py -v\ngit diff --check\n```\n\nA full CuTe build at the upstream base was not performed: its complete\n`jit_executor.py` requires adapter APIs absent from the installed package.\nThe source-method test isolates this pure-Python binding change; it does not\nqualify unrelated compiler/runtime changes. No GPU tests or end-to-end speedup\nclaims are attached to this upstream submission.\n\n## Human review and AI assistance\n\nMartin Vit (@voipmonitor) confirmed that he manually reviewed the one-line\nruntime implementation and requested its upstream submission. PR preparation\nand regression tests were assisted by OpenAI Codex.\n", "url": "https://github.com/NVIDIA/cutlass/pull/3634"}
{"number": 3615, "title": "Fix blockwise scale indexing under split-K (#3537)", "state": "open", "created_at": "2026-09-12T02:51:26Z", "updated_at": "2026-09-13T12:51:33Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3537\r\n\r\nUnder serial split-K, the blockwise scale lookup used the threadblock-local\r\nK-tile counter (`k_iter_idx`) with no contribution from the slice's position in\r\nthe global K range, so every slice applied slice-0's scale factors to its own\r\npartial products.\r\n\r\n### Reproduction\r\n\r\nM=N=256, K=512, scale block 128 (4 K-blocks), FP8 e4m3 with all-ones A and B,\r\n`kSplitKSerial = true`, and powers-of-two scales per K-block:\r\n\r\nscale_A row 0: 1, 2, 4, 8 scale_B row 0: 1, 2, 4, 8\r\nscale_A row 1: 2, 4, 8, 16 scale_B row 1: 2, 4, 8, 16\r\n\r\n\r\nFor output element (0,0) the correct value is\r\n`128 × (1·1 + 2·2 + 4·4 + 8·8) = 128 × 85 = 10880`.\r\n\r\n| split_k_slices | before | after |\r\n|---|---|---|\r\n| 1 | 10880 | 10880 |\r\n| 2 | **1280** | 10880 |\r\n| 4 | not measured | 10880 |\r\n\r\nThe observed 1280 is exactly `2 × 128 × (1·1 + 2·2)` — slice 1 re-applied\r\nK-blocks 0 and 1 to data from blocks 2 and 3. All 65536 output elements were\r\naffected. `can_implement`, `initialize` and the kernel launch all returned\r\n`kSuccess`, so the mis-scaled result was reported as success.\r\n\r\n### Change\r\n\r\nThread the slice's K-tile base\r\n(`threadblock_tile_offset.k() * params.gemm_k_size / Mma::Shape::kK`) from\r\n`gemm_universal_blockwise.h` down through `operator()` → `gemm_iters` →\r\n`mac_loop_iter`, and add it to `k_block_idx` before the existing clamp. The\r\nclamp is retained as a bounds guard. With `split_k_slices == 1` the offset is\r\nzero, so existing behaviour is unchanged.\r\n\r\n### Coverage gap\r\n\r\nThe existing coverage on this path, `gemm_f8t_f8n_bf16t_tensor_op_f32_blockwise_sm89.cu`,\r\nused K=128 with a single scale block and no split-K, so it could not observe the\r\nmis-indexing. This PR extends that file to use K=512 with 4 scale blocks and\r\ndistinct per-block scales, and exercises `split_k_slices` of 1, 2 and 4 under\r\nboth threadblock swizzles.\r\n\r\n\r\n### Testing\r\n\r\nRTX 4090 (SM89), CUDA 12.4.\r\n`cutlass_test_unit_gemm_device_tensorop_sm89_blockwise` passes with\r\n`split_k_slices` of 1, 2 and 4.", "url": "https://github.com/NVIDIA/cutlass/pull/3615"}
{"number": 3460, "title": "Give recast_layout and the flagged downcast a diagnostic", "state": "open", "created_at": "2026-08-14T01:47:07Z", "updated_at": "2026-09-13T08:49:01Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n`recast_layout(Swizzle)` takes a fourth branch when neither side of the type ratio is 1, for example an 8-bit type against a 6-bit type. That branch passes `layout`, which resolves to the `cute::layout` overload set at `include/cute/layout.hpp:558`, and the call to `upcast` then has no viable candidate.\n\n```diff\n  // include/cute/swizzle_layout.hpp:455\n- return downcast<scale::den>(upcast<scale::num>(layout));\n+ static_assert(dependent_false<Swizzle<B,M,S>>,\n+               \"recast_layout of a Swizzle needs a power-of-two ratio between the two type sizes.\");\n\n  auto t = make_tensor(make_smem_ptr<cutlass::float_e2m3_t>(nullptr),\n                       composition(Swizzle<3,3,3>{}, smem_ptr_flag_bits<6>{}, layout));\n  as_position_independent_swizzle_tensor(t);\n\n  before   73 diagnostic lines, ending at\n           error: no matching function for call to\n           'upcast<cute::R<6, 8>::num>(<unresolved overloaded function type>)'\n           note: there are 6 candidates\n  after    14 diagnostic lines, ending at\n           error: static assertion failed: recast_layout of a Swizzle needs a\n           power-of-two ratio between the two type sizes.\n```\n\n`downcast<N>` of a flagged layout divides the flag bit count by N with an integer division. `smem_ptr_flag` is `smem_ptr_flag_bits<1>`, so every GMMA `*_Atom_Bits` carries a flag of one bit and any N above 1 takes it to zero.\n\n```diff\n  // include/cute/pointer_flagged.hpp:83\n+ static_assert(B % N == 0, \"downcast<N> of a flagged layout needs N to divide the flag bit count.\");\n\n  GMMA::Layout_K_SW128_Atom_Bits\n  atom          Sw<3,4,3> o smem_ptr[1b](unset) o (_8,_1024):(_1024,_1)\n  downcast<2>   Sw<3,4,3> o smem_ptr[0b](unset) o (_8,_2048):(_2048,_1)\n  downcast<8>   Sw<3,4,3> o smem_ptr[0b](unset) o (_8,_8192):(_8192,_1)\n  after         rejected at compile time\n```\n\n", "url": "https://github.com/NVIDIA/cutlass/pull/3460"}
{"number": 3633, "title": "Fix misaligned local memory access in 02_dump_reg_shmem", "state": "open", "created_at": "2026-09-13T07:50:50Z", "updated_at": "2026-09-13T08:02:56Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3581\n\n`examples/02_dump_reg_shmem` faults on `sm_120` with `cudaErrorMisalignedAddress` (error 716); the example prints `Failed` and exits `255`.\n\n## Why\n\nThe iterator accesses its `Fragment` through a type that requires more alignment than `Fragment` itself declares:\n\n- `include/cutlass/transform/threadblock/predicated_tile_iterator.h:181` — `AccessType = AlignedArray<Element, AccessSize, (AccessSize * sizeof_bits<Element>::value / 8)>` → **16-byte aligned** for `half_t`\n- `include/cutlass/transform/threadblock/predicated_tile_iterator.h:191` — `Fragment = cutlass::Array<Element, ...>` → **2-byte aligned** (`cutlass::Array` declares no `alignas`; only `AlignedArray` does)\n- `include/cutlass/transform/threadblock/predicated_tile_iterator.h:327` — `AccessType *frag_ptr = reinterpret_cast<AccessType *>(&frag);`\n\nWhen the compiler homes the fragment in local memory, it allocates the frame slot from the *declared* alignment (2 → offset `+0xc`) but emits the stores from the *cast-implied* alignment (16 → `STL.128`). The slot lands at `12 (mod 16)`, so the 16-byte store is misaligned and the kernel faults.\n\n## Fix\n\n`examples/02_dump_reg_shmem/dump_reg_shmem.cu:85`:\n\n```diff\n-  typename GmemIterator::Fragment frag;\n+  alignas(16) typename GmemIterator::Fragment frag;\n```\n\n## Verification\n\nRTX 5060 (`sm_120`), CUDA 13.3, `main` @ `147295a3`, unmodified apart from this change:\n\n| | fragment slot | `STL.128` offsets | result |\n|---|---|---|---|\n| stock | `+0xc` | `0xc, 0x1c, … 0x7c` (all `≡ 12 mod 16`) | fault, exit 255 |\n| `alignas(16)` | `+0x10` | `0x10, 0x20, … 0x80` (all 16-byte aligned) | **completes, exit 0** |\n\nRegisters and frame size are unchanged (40 registers, 144-byte frame) — only the slot offset moves, so the placement is not constrained by the frame itself.\n\nBuilt with `-DCUTLASS_NVCC_ARCHS=120 -DCUTLASS_ENABLE_EXAMPLES=ON`; the example prints all of its dumps and exits `0`.\n\n## Scope\n\nThis is the minimal fix for the example. The underlying question — whether the `Fragment` typedef itself should declare the alignment its own accessors require — affects every caller that declares `typename Iterator::Fragment frag;` and is left to the maintainers. Details, SASS and a CUTLASS-free reproducer are in #3581.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n", "url": "https://github.com/NVIDIA/cutlass/pull/3633"}
{"number": 3580, "title": "Fix Constexpr method receivers in dynamic control-flow regions", "state": "open", "created_at": "2026-09-03T11:18:38Z", "updated_at": "2026-09-13T06:59:01Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "Fixes #3632\r\n\r\n## Motivation\r\n\r\nI encountered this while implementing FlashAttention. The implementation naturally groups the load and MMA paths into JIT methods on a compile-time configuration object, and selects between them using the runtime warp index. A reduced version looks like this:\r\n\r\n```python\r\nclass FlashAttention:\r\n    @cute.jit\r\n    def load(self, tensor, pipeline):\r\n        ...\r\n\r\n    @cute.jit\r\n    def mma(self, tensor, pipeline):\r\n        ...\r\n\r\n\r\n@cute.jit\r\ndef fa4_device_body(\r\n    fa: cutlass.Constexpr[FlashAttention],\r\n    tensor: cute.Tensor,\r\n    storage,\r\n):\r\n    warp_idx = cute.arch.make_warp_uniform(cute.arch.warp_idx())\r\n\r\n    pipeline = make_pipeline(\r\n        storage=storage,\r\n        num_stages=fa.num_stages,\r\n    )\r\n\r\n    if warp_idx < 4:\r\n        cute.arch.setmaxregister_decrease(fa.num_producer_regs)\r\n        fa.load(tensor, pipeline)\r\n    else:\r\n        cute.arch.setmaxregister_increase(fa.num_mma_regs)\r\n        fa.mma(tensor, pipeline)\r\n```\r\n\r\nHere, `self` in `FlashAttention.load()` and `FlashAttention.mma()` is a compile-time method receiver. The `fa` parameter in `fa4_device_body()` refers to the same kind of compile-time object, made explicit by its `Constexpr` annotation.\r\n\r\nWriting the device body as an actual instance method makes this code compile, but only because the compiler has a special case that excludes a receiver whose name is literally `self`. Passing the same object as a named `Constexpr[FlashAttention]` parameter should have equivalent staging semantics, but currently does not.\r\n\r\n## Problem\r\n\r\nCuTe DSL treats `Constexpr[T]` parameters as compile-time Python meta values. They do not have an MLIR representation and must not become arguments or results of runtime control-flow regions.\r\n\r\nHowever, when a method is called through a `Constexpr` receiver inside a dynamic `if`, the receiver is incorrectly captured as a region argument:\r\n\r\n```python\r\nclass Receiver:\r\n    @cute.jit\r\n    def store(self, output):\r\n        ...\r\n\r\n\r\n@cute.kernel\r\ndef kernel(receiver: cutlass.Constexpr[Receiver], output: cute.Tensor):\r\n    tidx, _, _ = cute.arch.thread_idx()\r\n\r\n    if tidx == Int32(0):\r\n        receiver.store(output)\r\n```\r\n\r\nThe region analyzer currently treats the base object of every method call as a mutable runtime value, except when its name is literally `self`.\r\n\r\nConsequently, `receiver` is added to the values carried through the dynamic `if`. Lowering then attempts to flatten the plain Python `Receiver` instance into MLIR values and fails with:\r\n\r\n```text\r\nerror[TYPE_DYNAMIC_EXPR_UNSUPPORTED]:\r\nA value carried through this `if` is a plain Python value (Meta value)\r\n(a `Receiver`) that cannot be turned into a Runtime value (Staged value)\r\n```\r\n\r\nThis means `self.method()` works because of the existing `self` special case, while an explicitly annotated `receiver: Constexpr[Receiver]` does not, despite having the same staging semantics.\r\n\r\n## Root cause\r\n\r\n`Constexpr` is a property of the original function parameter binding. The AST preprocessor removes parameter annotations while transforming the function, so the later control-flow region analysis no longer knows which names refer to `Constexpr` parameters.\r\n\r\n`RegionAnalyzer.visit_Call()` therefore sees only a method call on a Python object and adds its receiver to `invoked_args`.\r\n\r\n## Fix\r\n\r\nThis change:\r\n\r\n1. Inspects the original function signature before AST transformation.\r\n2. Uses the existing `is_arg_annotation_constexpr()` helper to record `Constexpr` parameter names in the preprocessing session.\r\n3. Excludes those names from method receivers carried through runtime control-flow regions.\r\n\r\nNormal runtime receivers and decomposable DSL values continue to use the existing region argument handling.\r\n\r\n## Regression test\r\n\r\nA regression test uses a deliberately non-MLIR-decomposable Python class as a `Constexpr` receiver and calls one of its JIT methods inside a dynamic `if`.\r\n\r\nThe test calls `cute.compile()`, so it verifies the complete lowering path that previously failed.\r\n\r\n## Validation\r\n\r\nBoth sides of the comparison used the same `nvidia-cutlass-dsl 4.8.0.dev0` native runtime components and the same test.\r\n\r\n### Before this change\r\n\r\nBase commit: `59e3a333` (`v4.8.0dev-6`)\r\n\r\n```text\r\nFAILED test_constexpr_receiver_in_if.py\r\nerror[TYPE_DYNAMIC_EXPR_UNSUPPORTED]\r\n1 failed\r\n```\r\n\r\nThe failure occurs because `_ConstexprReceiver` is included in the values passed through the dynamic `if`.\r\n\r\n### After this change\r\n\r\nCommit: `740f0a6c` (`v4.8.0dev-7`)\r\n\r\n```text\r\n1 passed\r\n```\r\n\r\nThe new regression test together with the existing structured control-flow tests also passes:\r\n\r\n```text\r\n7 passed, 6 warnings in 0.88s\r\n```\r\n\r\nThe warnings are existing `SmemAllocator` deprecation warnings and are unrelated to this change.", "url": "https://github.com/NVIDIA/cutlass/pull/3580"}
{"number": 3631, "title": "[Epilogue] Align fixed visitor workspace offsets", "state": "open", "created_at": "2026-09-13T05:08:59Z", "updated_at": "2026-09-13T05:08:59Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- align fixed-size `Sm90VisitorImplBase` workspace pointer advances with `MinWorkspaceAlignment`\n- keep pointer offsets consistent with `get_workspace_size()` and `initialize_workspace()`\n- add coverage for the 2-, 3-, and 4-operation specializations\n\n## Testing\n\nTested on NVIDIA B200 (SM100) with CUDA 13.1:\n\n- baseline reproducer: offsets `0,3,8,15` (failed as expected)\n- fixed reproducer: offsets `0,16,32,48` (passed)\n- `cutlass_test_unit_epilogue_threadblock --gtest_filter=Epilogue_Threadblock.Sm90VisitorWorkspaceAlignment` (1/1 passed)\n\n## Contribution disclosure\n\nThis contribution was developed with assistance from OpenAI Codex.\n\nFixes #3494", "url": "https://github.com/NVIDIA/cutlass/pull/3631"}
{"number": 3629, "title": "[CuTe DSL] Cache each distinct one-time warning", "state": "open", "created_at": "2026-09-13T04:55:00Z", "updated_at": "2026-09-13T04:55:00Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- make `print_warning_once` retain every distinct message instead of only the most recent one\n- prevent an earlier warning from being emitted again after a different message evicts it\n- add a regression test for the `first, second, first` sequence and isolate the global cache between tests\n\n## Testing\n\n- B200 node, Python 3.12: an AST-extracted execution of the current-main method emitted 3 warnings for `first, second, first` and exited `2`\n- B200 node, Python 3.12: the fixed source emitted exactly 2 distinct warnings and exited `0`\n- `python3 -m py_compile python/CuTeDSL/cutlass/base_dsl/dsl.py test/python/CuTeDSL/test_warning.py`\n- the source checkout was not imported as a complete DSL package because its generated `cutlass._mlir` extension is not built on the validation node\n\n## Contribution disclosure\n\nAI assistance was used for repository research, implementation, and running the checks above.\n\nFixes #3501\n", "url": "https://github.com/NVIDIA/cutlass/pull/3629"}
{"number": 3628, "title": "[Core] Saturate scalar int-to-8-bit conversions", "state": "open", "created_at": "2026-09-13T04:51:07Z", "updated_at": "2026-09-13T04:51:07Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- add saturating scalar `int` to `int8_t` and `uint8_t` `NumericConverter` specializations\n- make width-1 and remainder conversions consistent with the existing packed `cvt.pack.sat` paths\n- cover signed and unsigned values on both sides of their representable ranges\n\n## Testing\n\n- B200 (SM100a), CUDA 13.1: current main produced `-56` vs `127` for signed N=1/N=2 and `255` vs `0` for unsigned N=1/N=2\n- B200 (SM100a), CUDA 13.1: fixed targeted probe builds and runs with exit code `0`; N=1 and N=2 both saturate consistently\n- `cutlass_test_unit_core --gtest_filter=NumericConversion.s32_to_8bit_saturates` on B200: 1/1 passed\n\n## Contribution disclosure\n\nAI assistance was used for repository research, implementation, and running the checks above.\n\nFixes #3489\n", "url": "https://github.com/NVIDIA/cutlass/pull/3628"}
{"number": 3627, "title": "[Epilogue] Fix VisitorRowReduction helper return type", "state": "open", "created_at": "2026-09-13T04:39:20Z", "updated_at": "2026-09-13T04:39:32Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- make `VisitorRowReduction::Callbacks::reduction` return `void`, matching its side-effect-only implementation and sole call site\n- remove undefined behavior from falling off the end of a non-void device function\n- preserve the existing element-wise register reduction logic unchanged\n\n## Testing\n\n- B200 (SM100a), CUDA 13.1: an explicit instantiation of the helper produces NVCC diagnostic `#940-D: missing return statement at end of non-void function` on current main\n- B200 (SM100a), CUDA 13.1: the same probe builds and runs with exit code `0` after the fix, with no target return diagnostic\n\n## Contribution disclosure\n\nAI assistance was used for repository research, implementation, and running the checks above.\n\nFixes #3495\n", "url": "https://github.com/NVIDIA/cutlass/pull/3627"}
{"number": 3626, "title": "[CuTe] Handle Underscore entries in zip2_by", "state": "open", "created_at": "2026-09-13T04:31:47Z", "updated_at": "2026-09-13T04:31:47Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- preserve an entire layout mode in REST when its `zip2_by` guide entry is `Underscore`\n- let shape and stride callers provide the correct BLOCK identity (`1` and `0`, respectively)\n- cover both hierarchical and scalar kept modes in the CuTe core unit tests\n\n## Testing\n\n- B200 (SM100a), CUDA 13.1: hierarchical-mode baseline produced the wrong layout; fixed repro compiled and returned `0` with the expected layout\n- B200 (SM100a), CUDA 13.1: scalar-mode baseline failed to compile in `zip2_by`; fixed repro compiled and returned `0` with the expected layout\n- full `cutlass_test_unit_cute_core` target was not run because CMake's GoogleTest `FetchContent` cannot reach GitHub from the GPU node\n\n## Contribution disclosure\n\nAI assistance was used for repository research, implementation, and running the checks above.\n\nFixes #3514\n", "url": "https://github.com/NVIDIA/cutlass/pull/3626"}
{"number": 3625, "title": "[GEMM] Fix sparse split-K metadata offsets (issue #3533)", "state": "open", "created_at": "2026-09-13T04:18:59Z", "updated_at": "2026-09-13T04:19:17Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nCorrect the packed metadata E column offset for K slices after the first in three sparse GEMM kernel bodies:\n\n- SparseGemm\n- SparseGemmWithAbsmax\n- SparseGemmWithEpilogueVisitor\n\nEach offset now divides by kElementsPerElementE, matching the metadata extent and GemmSparseUniversal.\n\n## Rationale\n\nThe A and B slice offsets are expressed in their respective logical element coordinates. E is different: each stored metadata element describes kElementsPerElementE groups of sparse A values, and its iterator extent already divides by both kSparse and kElementsPerElementE.\n\nThe old E offset divided only by kSparse. For split-K slices greater than zero, this started metadata iteration beyond the packed E slice. Predication then suppressed valid metadata loads and the slice accumulated against invalid zero-filled metadata.\n\n## Validation\n\nUsed the non-universal SparseGemm path from example 15 with serial split-K enabled. Built with CUDA 13.1 and -arch=sm_100a and ran on an NVIDIA B200.\n\nControl:\n\n- split-K=1 passes the complete example reference comparison on current main\n\nBefore:\n\n- split-K=2 fails the same end-to-end reference comparison\n\nAfter:\n\n- split-K=2 passes the end-to-end reference comparison\n- all three changed headers compile in the same CUDA translation unit\n- git diff --check passes\n\nThis path is distinct from the existing GemmSparseUniversal split-K test coverage.\n\nFixes #3533", "url": "https://github.com/NVIDIA/cutlass/pull/3625"}
{"number": 3624, "title": "[GEMM] Fix EllGemm column-major split-K workspace sizing (issue #3539)", "state": "open", "created_at": "2026-09-13T04:11:48Z", "updated_at": "2026-09-13T04:11:48Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nMake the ColumnMajor EllGemm specialization size its serial split-K semaphore workspace from the same transposed problem shape used by initialize().\n\nAdd a regression test for both rectangular orientations, covering the previous under-allocation and over-allocation cases.\n\n## Rationale\n\nColumnMajor output is implemented by exchanging M/N and forwarding to a RowMajor underlying operator. initialize() correctly computes its launch grid from (n, m, k), but get_workspace_size() used the original (m, n, k).\n\nFor rectangular problems, the two shapes can produce different tile counts. With m=264, n=136, ell_blocksize=32, and four split-K slices, workspace sizing returned 15 semaphore slots while the initialized grid contains 18 output tiles. This left part of the semaphore array outside the allocation/initialization contract.\n\nUsing the transposed problem shape in get_workspace happens to match initialize() and the underlying operator for every aspect ratio.\n\n## Validation\n\nCompiled the same standalone reproducer before and after this change with CUDA 13.1 and -arch=sm_100a, then ran it on an NVIDIA B200.\n\nBefore:\n\n- initialized launch grid: 2 x 9 tiles\n- get_workspace_size reported 60 bytes instead of 72\n- initialize() returned success but cudaMemsetAsync zeroed only 60 of the required 72 bytes\n\nAfter:\n\n- get_workspace_size reports 72 bytes\n- initialize() zeroes all 72 bytes\n- the opposite 136 x 264 aspect ratio is also pinned by the new unit test at 60 bytes\n- the new unit-test translation unit compiles successfully for sm_100a\n- git diff --check passes\n\nFixes #3539", "url": "https://github.com/NVIDIA/cutlass/pull/3624"}
{"number": 3623, "title": "[Conv] Fix filter iterator pointer offset scaling (issue #3504)", "state": "open", "created_at": "2026-09-13T04:05:09Z", "updated_at": "2026-09-13T04:05:09Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nFix add_pointer_offset byte scaling in five convolution filter tile access iterators:\n\n- Conv2d fprop analytic\n- Conv2d fprop fixed channels\n- Conv2d fprop few channels\n- Conv3d fprop analytic\n- Depthwise Conv2d fprop direct-convolution optimized\n\nAdd a lightweight convolution threadblock unit test that constructs each iterator and verifies its observable pointer displacement.\n\n## Rationale\n\nThese iterators store their base pointer as char const*, while add_pointer_offset documents its argument in units of Element. The byte displacement must therefore be:\n\npointer_offset * sizeof_bits<Element>::value / 8\n\nThe previous inverted expression multiplied by 8 and divided by the element bit width. For fp16, advancing by 8 elements moved 4 bytes instead of 16; fp32 was off by a factor of 16.\n\n## Validation\n\nBuilt the same host/device CUDA probe before and after the change using CUDA 13.1 and -arch=sm_100a, then ran it on an NVIDIA B200.\n\nBefore:\n\n- all five fp16 iterators moved 4 bytes for an 8-element offset on both host and device\n- 10/10 checks failed\n\nAfter:\n\n- all five iterators move the expected 16 bytes on host and device\n- 10/10 checks pass and the probe exits with failures=0\n- the new WITHOUT_CUDA unit-test translation unit compiles successfully with the GPU node host compiler\n- git diff --check passes\n\nFixes #3504", "url": "https://github.com/NVIDIA/cutlass/pull/3623"}
{"number": 3622, "title": "[CuTe] Validate dynamic tiler divisibility for static layouts (issue #3513)", "state": "open", "created_at": "2026-09-13T03:55:27Z", "updated_at": "2026-09-13T03:55:27Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nAdd runtime shape-divisibility validation when layout composition combines a fully static left-hand layout with a dynamic right-hand tiler.\n\nThe existing logical_divide core test now covers both:\n\n- a compatible dynamic tiler that preserves the input layout size\n- the issue reproducer, which must fail with Shape Divisibility Condition\n\n## Rationale\n\nThe composition path already diagnoses fully static incompatibilities at compile time. When the right-hand shape is dynamic, however, new_shape and rest_shape become dynamic and the static assertion cannot run. The fallback runtime assertion was skipped even though each left-hand mode was still statically known, allowing an incompatible divide to silently return a non-size-preserving layout.\n\nLimiting the runtime assertion to a statically known curr_shape closes that mixed static/dynamic gap without changing the intentionally permissive fully dynamic path.\n\n## Validation\n\nCompiled the same standalone reproducer before and after this change with CUDA 13.1 and -arch=sm_100a, then ran it on an NVIDIA B200.\n\nBefore:\n\n- dynamic tiler 128 returned normally\n- input size was 384, but result size was 288\n- the probe exited with status 2\n\nAfter:\n\n- the same invalid tiler aborts with Shape Divisibility Condition (status 134)\n- a compatible dynamic tiler 12 returns normally with input/result size 384\n- git diff --check passes\n\nThe cluster image does not provide GTest, so the added core regression test is left for the upstream test target to compile and execute.\n\nFixes #3513", "url": "https://github.com/NVIDIA/cutlass/pull/3622"}
{"number": 3621, "title": "[Core] Add lowest() to platform numeric_limits<float> (issue #3509)", "state": "open", "created_at": "2026-09-13T03:43:00Z", "updated_at": "2026-09-13T03:43:00Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- add `lowest()` to `cutlass::platform::numeric_limits<float>`\n- cover `LinearCombinationClamp<float>` on both the host and device in the core functional tests\n\n## Problem\n\n`LinearCombinationClamp` obtains its lower saturation bound from `cutlass::platform::numeric_limits<ElementOutput>::lowest()`. The float specialization exposes `max()` but not `lowest()`, so explicitly instantiating `LinearCombinationClamp` with a float output fails to compile even though float is part of the class's public template contract.\n\nThe new member returns `-max()`, matching the standard definition of the lowest finite IEEE-754 float while reusing the specialization's existing exact `max()` encoding.\n\n## Validation\n\nUsing CUDA 13.1 and `-arch=sm_100a` on an NVIDIA B200:\n\n- current main fails to compile the issue reproducer with `numeric_limits<float> has no member lowest`\n- the fixed source compiles the same host/device probe successfully\n- the probe runs and prints `host=2 device=2`\n- `git diff --check` passes\n\nThe core regression test uses the existing `unary_operator` kernel and `HostTensor` utilities to exercise both compilation paths.\n\nFixes #3509\n", "url": "https://github.com/NVIDIA/cutlass/pull/3621"}
{"number": 3620, "title": "[Core] Fix numeric_limits constants for low-precision types (issue #3508)", "state": "open", "created_at": "2026-09-13T03:38:58Z", "updated_at": "2026-09-13T03:38:58Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\nFix three `numeric_limits` constants whose encoded values do not match the represented low-precision types:\n\n- `float_ue8m0_t::lowest()`: `0xfe` -> `0x00` in both the `std` and CUTLASS platform specializations\n- `tfloat32_t::epsilon()`: `0x00001000` -> `0x3a800000` (`2^-10`)\n- `half_t::epsilon()`: `0x1800` -> `0x1400` (`2^-10`) in both specializations\n\nThe existing core tests now pin each corrected encoding.\n\n## Rationale\n\n`float_ue8m0_t` is unsigned, so its lowest finite value is also its minimum encoded value, `0x00` (`2^-127`), rather than `0xfe` (`2^127`). TF32 and FP16 each carry 10 explicit fraction bits for `numeric_limits` purposes, so their machine epsilon is `2^-10`.\n\n## Validation\n\nBuilt the same standalone CUDA probe against current main before and after this change using CUDA 13.1 and `-arch=sm_100a`, then ran it on an NVIDIA B200.\n\nBefore:\n\n- both UE8M0 `lowest()` specializations returned `0xfe`\n- TF32 `epsilon()` returned `0x00001000`\n- both FP16 `epsilon()` specializations returned `0x1800` on the host and device\n- 7/7 checks failed\n\nAfter:\n\n- all 7/7 host/device encoded-value checks pass\n- the probe exits with `failures=0`\n- `git diff --check` passes\n\nFixes #3508\n", "url": "https://github.com/NVIDIA/cutlass/pull/3620"}
{"number": 3618, "title": "Document setmaxregister launch-bound requirement", "state": "open", "created_at": "2026-09-13T03:10:24Z", "updated_at": "2026-09-13T03:10:24Z", "closed_at": null, "labels": [], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\n\n- document the register-count and warpgroup requirements of the public setmaxregister APIs\n- warn that ptxas can ignore setmaxnreg with C7508 when the entry register count is unknown\n- show how min_blocks_per_mp establishes a launch bound, while calling out its occupancy and spilling tradeoffs\n\n## Testing\n\n- B200 / SM100a, CUDA 13.1 ptxas: without minnctapersm, C7508 is emitted and SASS contains 0 USETMAXREG instructions\n- B200 / SM100a, CUDA 13.1 ptxas: with minnctapersm 1, C7508 disappears and SASS contains 2 USETMAXREG instructions\n- ruff check --ignore E731 on both changed files (the ignored E731 findings are pre-existing in nvvm_wrappers.py)\n- git diff --check\n\n## Contribution disclosure\n\nThis contribution was prepared with AI-assisted tooling. I reviewed the changes and validated the documented behavior on B200.\n\nFixes #3406\n", "url": "https://github.com/NVIDIA/cutlass/pull/3618"}
{"number": 3459, "title": "Bound the SM120 CTA tile and widen the sparse batch stride", "state": "open", "created_at": "2026-08-14T00:54:55Z", "updated_at": "2026-09-13T01:28:14Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\n\r\nThe SM120 dense builder does not check that the CTA tile is a multiple of the MMA tiler. The mainloop steps the tiled MMA with a ceiling division. A K mode that is not a multiple of 32 makes the mainloop read past the shared memory tile.\r\n\r\n```diff\r\n  // include/cutlass/gemm/collective/builders/sm120_mma_builder.inl:120\r\n+ static_assert(size<2>(TileShape_MNK{}) % size<2>(cute::tile_shape(TiledMma{})) == 0, ...);\r\n\r\n  // tile 128x64x48, problem 512x256x288, e4m3, sm_120a, A and B all 1.0\r\n  before   compute-sanitizer: Invalid __shared__ read of size 16 bytes,\r\n           Access to 0xb480 is out of bounds        then abort, rc 134\r\n  after    rejected at compile time\r\n```\r\n\r\nThe same file does not check the M mode or the N mode. This change adds an assert for each of the three modes.\r\n\r\n```diff\r\n  // include/cutlass/gemm/collective/builders/sm120_blockwise_mma_builder.inl:136\r\n+ static_assert(cute::size(ClusterShape_MNK{}) == Int<1>{}, \"no programmatic multicast on this arch\");\r\n```\r\n\r\nThe blockwise builder is the only one of the six SM120 mainloop builders without a cluster bound. \r\nThe five other builders hold the same line at `sm120_mma_builder.inl:84`, `sm120_array_mma_builder.inl:87`, `sm120_blockscaled_mma_builder.inl:104`, `sm120_blockscaled_sparse_mma_builder.inl:179` and `sm120_sparse_mma_builder.inl:163`.\r\n\r\nThe sparse batch stride multiplies two `int` extents. The product overflows, and the cast to `int64_t` then widens a value that is already wrong. There are eight sites, four in each configuration header.\r\n\r\n```diff\r\n- (L == 1) ? int64_t(0) : int64_t(M_AlignedAC * K_AlignedAC)\r\n+ (L == 1) ? int64_t(0) : int64_t(M_AlignedAC) * int64_t(K_AlignedAC)\r\n\r\n  e4m3, L = 2, batch stride of E\r\n  Sm90GemmSparseConfig    M 49153  K 65537   -1066397696  ->  3228569600\r\n  Sm1xxGemmSparseConfig   M 49153  K 65537   -1059045376  ->  3235921920\r\n  Sm90GemmSparseConfig    M 65536  K 65536             0  ->  4294967296\r\n\r\nsm90   E layout = ((_64,769),(_64,1025),2)      atom 64 x 64\r\n       M: 49153 -> 49216  (64 x 769)\r\n       K: 65537 -> 65600  (64 x 1025)      49216 x 65600 = 3228569600\r\n\r\nsm1xx  E layout = ((_128,385),(_128,513),2)     atom 128 x 128\r\n       M: 49153 -> 49280  (128 x 385)\r\n       K: 65537 -> 65664  (128 x 513)      49280 x 65664 = 3235921920\r\n```\r\n\r\nhttps://github.com/NVIDIA/cutlass/issues/3269 reports the same overflow in `packed_stride.hpp`, and https://github.com/NVIDIA/cutlass/pull/3307 holds a fix for the two sites in that file.\r\nhttps://github.com/NVIDIA/cutlass/issues/3393 asks for a `static_assert` in place of a template backtrace on the SM120 sparse block-scaled collective, and https://github.com/NVIDIA/cutlass/pull/3427 added one there.\r\n\r\nA kernel that calls `fill_layoutA` and `fill_layoutE`, at `-O3 -DNDEBUG`:\r\n\r\n```\r\n           SASS instructions      registers\r\n           before   after         before   after\r\nsm_120a        48      40             10      12\r\n```\r\n\r\n@depaulmillz\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3459"}
{"number": 3458, "title": "Bound the SM90 TMA descriptor writes", "state": "open", "created_at": "2026-08-13T23:44:32Z", "updated_at": "2026-09-13T00:45:16Z", "closed_at": null, "labels": ["inactive-30d"], "comments_count": null, "is_pr": true, "merged_at": null, "body": "## Summary\r\n\r\n`make_tma_copy_desc` truncates the TMA box by the multicast factor. The loop index is unsigned and the condition tests the multicast alone, so a cluster larger than the product of the box modes drives the index below mode 0, where it wraps and reads `smem_box_shape` outside its five elements.\r\n\r\n```diff\r\n  // include/cute/atom/copy_traits_sm90_tma.hpp:997\r\n- for (uint32_t i = tma_dim-1, multicast = num_multicast; multicast > 1; --i) {\r\n+ uint32_t multicast = num_multicast;\r\n+ for (int i = tma_dim-1; i >= 0 && multicast > 1; --i) {\r\n+ ...\r\n+ assert(multicast <= 1 && \"TMA box is too small for the requested multicast size\");\r\n\r\n  make_tma_atom(SM90_TMA_LOAD_MULTICAST{}, gtensor, (_8,_4) smem, ..., Int<N>{})\r\n  N = 32     ok      ->  ok          32 is the product of the box modes\r\n  N = 33     SEGV    ->  ok\r\n  N = 64     SEGV    ->  ok\r\n```\r\n\r\nAddressSanitizer gives `SEGV on unknown address` inside `ceil_div` at `copy_traits_sm90_tma.hpp:999`.\r\n\r\nTwo smaller changes ride with it, in the same descriptor path.\r\n\r\n```diff\r\n  // include/cute/arch/copy_sm90_desc.hpp:366   an asm operand takes an input\r\n  // constraint and the instruction writes it\r\n- uint64_t const smem_int64_desc = 0;\r\n- asm volatile (\"cvt.u64.u32 %0, %1;\" :: \"l\"(smem_int64_desc), \"r\"(smem_int_desc));\r\n+ uint64_t smem_int64_desc = 0;\r\n+ asm volatile (\"cvt.u64.u32 %0, %1;\" : \"=l\"(smem_int64_desc) : \"r\"(smem_int_desc));\r\n\r\n  emitted PTX at sm_90a\r\n  before   mov.u64 %rd14, 0;  cvt.u64.u32 %rd14, %r1;\r\n  after                       cvt.u64.u32 %rd2,  %r1;\r\n\r\n  // include/cute/atom/copy_traits_sm90_im2col.hpp:410   three arrays of five\r\n  // elements are filled from loops bounded by the tensor rank, and nothing\r\n  // bounds that rank\r\n+ static_assert(num_total_modes >= 3 && num_total_modes <= 5, ...);\r\n\r\n  detail::make_im2col_tma_copy_desc(rank 6 tensor, ...)\r\n  stack-buffer-overflow, WRITE of size 8 at copy_traits_sm90_im2col.hpp:419\r\n  ->  rejected at compile time\r\n```\r\n\r\n## Scope\r\n\r\nThe multicast defect is reachable through the public `make_tma_atom`, and a user who asks for a cluster larger than the TMA box gets a segmentation fault on the host.\r\n\r\n_`make_im2col_tma_copy` always gives the descriptor function a rank of 2, because the caller rebuilds the tensor from the TMA basis and `smem_tma_rank` is capped at 2 at `copy_traits_sm90_im2col.hpp:635`. The overflow needs a direct call to `detail::make_im2col_tma_copy_desc`, thus the `static_assert` guards the detail function and not a path that a user reaches today._\r\n\r\n\r\n## Cost\r\n\r\nThe asm operand is the only change inside a kernel. Measured with `-O3 -DNDEBUG`:\r\n\r\n```\r\n           SASS instructions      registers\r\n           before   after         before   after\r\nsm_75          40      40             38      38\r\nsm_80          48      48             30      30\r\nsm_86          48      48             38      38\r\nsm_89          48      48             38      38\r\nsm_90a         88      88             30      30\r\nsm_100a        64      64             22      22\r\nsm_120a        64      64             22      22\r\n```\r\n\r\nThe other two changes are host side. The bounds fix adds one comparison for each loop step over at most five modes, and it runs one time for each atom.\r\n\r\n@ccecka\r\n", "url": "https://github.com/NVIDIA/cutlass/pull/3458"}
