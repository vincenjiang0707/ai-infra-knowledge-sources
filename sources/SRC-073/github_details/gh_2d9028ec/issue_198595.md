# [Issue #198595] DISABLED test_rerun_then_pass (__main__.TestJunitOutcomes)

source: https://github.com/pytorch/pytorch/issues/198595
state: open | updated: 2026-09-25T06:59:29Z
labels: module: tests, triaged, module: flaky-tests, skipped, module: unknown, bot-triaged

## 正文

Platforms: rocm, dynamo, inductor

  This test was disabled because it is failing in CI. See [recent examples](https://hud.pytorch.org/flakytest?name=test_rerun_then_pass&suite=TestJunitOutcomes&limit=100) and the most recent trunk [workflow logs](https://github.com/pytorch/pytorch/runs/107907550519).

  Over the past 6 hours, it has been determined flaky in 139 workflow(s) with 278 failures and 139 successes.

  **Debugging instructions (after clicking on the recent samples link):**
  DO NOT ASSUME THINGS ARE OKAY IF THE CI IS GREEN. We now shield flaky tests from developers so CI will thus be green but it will be harder to parse the logs.
  To find relevant log snippets:
  1. Click on the workflow logs linked above
  2. Click on the Test step of the job so that it is expanded. Otherwise, the grepping will not work.
  3. Grep for `test_rerun_then_pass`
  4. There should be several instances run (as flaky tests are rerun in CI) from which you can study the logs.
  
  

<details><summary>Sample error message</summary>

```
Traceback (most recent call last):
ELIDED
```

</details>


  Test file path: `junit_xml_testdata/pytest_suite.py` or `expected`

  For all disabled tests (by GitHub issue), see https://hud.pytorch.org/disabled.

cc @mruberry

## 评论 (1)

### pytorch-bot[bot] · 2026-09-25

<!-- validation-comment-start --><body>Hello there! From the DISABLED prefix in this issue title, it looks like you are attempting to disable a test in PyTorch CI. The information I have parsed is below:

* Test name: `test_rerun_then_pass (__main__.TestJunitOutcomes)`
* Platforms for which to skip the test: dynamo, inductor, rocm
* Disabled by `pytorch-bot[bot]`

Within ~15 minutes, `test_rerun_then_pass (__main__.TestJunitOutcomes)` will be disabled in PyTorch CI for these platforms: dynamo, inductor, rocm. Please verify that your test name looks correct, e.g., `test_cuda_assert_async (__main__.TestCuda)`.

To modify the platforms list, please include a line in the issue body, like below. The default action will disable the test for all platforms if no platforms list is specified. 
```
Platforms: case-insensitive, list, of, platforms
```
We currently support the following platforms: asan, dynamo, inductor, linux, mac, macos, rocm, slow, win, windows, xpu.


### How to re-enable a test
To re-enable the test globally, close the issue. To re-enable a test for only a subset of platforms, remove the platforms from the list in the issue body. This may take some time to propagate. To re-enable a test only for a PR, put `Fixes #198595` in the PR body and rerun the test jobs. Note that if a test is flaky, it maybe be difficult to tell if the test is still flaky on the PR.
</body><!-- validation-comment-end -->
