# [Issue #198172] TorchInductor misses useful NVFP4 inference candidates on Blackwell

source: https://github.com/pytorch/pytorch/issues/198172
state: open | updated: 2026-09-24T15:51:59Z
labels: triaged, bot-triaged

## 正文


Human commentary: I want NVFP4 inference shapes to compare a small,
representative candidate set under the same launch and cache conditions.

AI-assisted content:

> ## Problem
>
> On GB200, `nvMatmulHeuristics` omits useful small- and medium-M NVFP4 tactics,
> while a deeper ranked pool increases compile and autotune time. CUDA graph,
> subprocess, and pipelined paths can also diverge in replay/cache policy after
> failures. A fifteen-shape sweep found that a three-candidate ranked-family cap
> plus bounded shape-scoped candidates retained or improved every winner and
> reduced aggregate candidate time from 189.03 to 164.93 seconds.
>
> Reproduce with the built-in TorchInductor CUDA-graph benchmarker or with vLLM
> `5302d1fe40e6969b854e0ab288d34825653767c6` on Qwen3-14B NVFP4, batch 32,
> 32 input tokens, 128 output tokens, static compilation/CUDA graphs, and `-O2`.
>
> Issue text prepared with assistance from an AI tool.



## 评论 (2)

### github-actions[bot] · 2026-09-22

> Thanks for the report! This describes a hardware-specific scenario (GB200/Blackwell) using a specific vLLM commit and model (Qwen3-14B) with a particular batch size/CUDA graph/compilation configuration.
>
> To help us investigate and route this properly, could you share:
>
> - A minimal, self-contained script that reproduces the candidate-selection behavior using only PyTorch/TorchInductor APIs (e.g., a small NVFP4 matmul benchmark with `torch.compile(mode="max-autotune")`), without requiring the full vLLM stack, if possible
> - If vLLM is required to reproduce, exact setup/run steps (flags, environment) for the vLLM configuration described
> - Output of `python -m torch.utils.collect_env`
>
> Once we have a reproducible script we can properly classify and route this issue.

(AI-assisted triage comment)

### janeyx99 · 2026-09-24

@mlazos i trust you'll label this with the right labels if necessary
