# [Issue #21948] [Bug]: Add tests for all parallel sampling parameter combinations

source: https://github.com/vllm-project/vllm/issues/21948
state: open | updated: 2026-09-22T20:30:20Z
labels: bug, good first issue, keep-open, stale

## 正文

Verify that we have tests that explicitly exercise parallel sampling (`n>1` request sampling parameter) for all of the following combinations:
- Via `AsyncLLM.generate`, via `LLMEngine add_request() / step()`
- For `output_kind` equal to each of `CUMULATIVE`, `DELTA`, `FINAL_ONLY`

Ideally a test for each of `AsyncLLM` and `LLMEngine` in the appropriate files, with `output_kind` parameterized.

Additionally we can still test `LLM.generate()` but this enforces `FINAL_ONLY`. There is already a test for this one [here](https://github.com/vllm-project/vllm/blob/004203e95330ac9a878df8192619570b0770667e/tests/v1/engine/test_llm_engine.py#L120), but I'm not sure about the other cases.

@sethkimmel3 has reported that the `LLMEngine` + `CUMULATIVE` combination is not working properly, this should be exposed and fixed if so.


## 评论 (8)

### gigit0000 · 2025-07-31

Hi @njhill I checked the relevant PR's. Mind if I take this on?

### robertgshaw2-redhat · 2025-07-31

> Hi [@njhill](https://github.com/njhill) I checked the relevant PR's. Mind if I take this on?

Feel free!

### njhill · 2025-07-31

Thanks @gigit0000!

### github-actions[bot] · 2025-10-30

This issue has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days. Leave a comment if you feel this issue should remain open. Thank you!

### ps2181 · 2025-12-25

Hi @njhill ,

Is this issue still open? If so, I’d like to take it on.
Thanks!

### GoyalIshaan · 2026-03-05

Hi @njhill, I'd like to work on this. Is it still open and unassigned? Happy to start with writing the tests and then fixing the LLMEngine + CUMULATIVE bug once it's exposed.

### clumsylad21 · 2026-05-06

Hii @gigit0000 @njhill ! I’m new to contributing to vLLM and have been studying the `LLMEngine.add_request()` / `step()` flow.

I’d like to help with the LLMEngine test coverage for the `n > 1` + `output_kind` combinations if this is still needed. I can start by adding a parameterized test for `CUMULATIVE`, `DELTA`, and `FINAL_ONLY`.

### akshatgit07 · 2026-09-22

I’d like to take a focused piece of this: audit the current `n > 1` coverage for each `RequestOutputKind` in the `LLMEngine.add_request()` / `step()` path, then add only the missing parameterized regression cases. I’ll keep this test-only unless a failing case exposes a separate implementation bug, and I’ll reference this issue in a draft PR.
