# [PR #28734] [AMD] Fix Load and Inference of MLA models with Quark PTPC FP8 attention on ROCm

source: https://github.com/sgl-project/sglang/pull/28734
state: open | updated: 2026-09-23T06:43:37Z
labels: documentation, quant, amd, dependencies, lora, Multi-modal, deepseek, speculative-decoding, hicache, sgl-kernel, blackwell, npu, run-ci, diffusion, model-gateway, mthreads, jit-kernel, bypass-fastfail

## 正文

## Motivation

Loading and running MLA models with quark-quantized MXFP4 weights + per-token-per-channel FP8 attention (like `amd/GLM-5.2-Quark-MXFP4-AttnFP8`) on ROCm/gfx95 fails with multiple errors.

The root cause is that the aiter gfx95 `_gfx95_quant_format="fp8"` path fuses RMSNorm + FP8 quantization into a single kernel (`fused_rms_fp8_group_quant`), passing pre-quantized **tuples** instead of plain bf16 tensors downstream. This pattern was already handled for compressed_tensors attention quantization across three prior PRs:

- **#12181** added the `apply_fp8_ptpc_linear` routing in `compressed_tensors_w8a8_fp8.py` for pre-quantized tuple inputs
- **#21403** added the `isinstance(input, tuple)` branch in `apply_fp8_ptpc_linear` itself
- **#22258** added the 3-tuple `(fp8, scale, bf16)` passthrough from `fused_rms_fp8_group_quant` in `communicator.py`, and the corresponding unwrap in `dsa_indexer._project_and_scale_head_gates`

None of these extended to **quark** attention quantization, and this PR closes that gap.

## Modifications

**`fp8_utils.py`: `channel_quant_to_tensor_quant`**  
Reshape 1D per-channel scale `[N]` -> `[N, 1]` before multiplying against the 2D weight. Quark's `kv_b_proj` has a per-channel FP8 weight with a 1D scale vector that failed to broadcast correctly against the `[N, K]` weight.

**`fp8_utils.py`: `apply_fp8_ptpc_linear`**  
Unpack pre-quantized input tuple as `input[0], input[1]` instead of destructuring, to tolerate both 2-tuples and 3-tuples (the 3-tuple was introduced in #22258 but the unpack assumed exactly 2 elements).

**`quark_w8a8_fp8.py`: `apply_weights`**  
Mirror the handling that #12181 added to `compressed_tensors_w8a8_fp8.apply_weights`, extended for quark's weight layout and both tuple formats
Note: unlike compressed_tensors which stores `shuffle(w)`, quark stores `shuffle(w).t()`, making the weight layout incompatible with `apply_fp8_ptpc_linear`/`gemm_a8w8_bpreshuffle`; So we dequantize to bf16 and use the existing `apply_fp8_linear` path without requiring weight storage changes.

**`dsa_indexer.py`: `_get_q_k_bf16` and `_get_k_bf16`**  
Mirror the unwrap added in #22258 for `_project_and_scale_head_gates`, applied to the two remaining methods that also receive `x` and call `self.wk`. 

## Validation

Accuracy is evaluated using [lm_eval](https://github.com/EleutherAI/lm-evaluation-harness) on MI355X.

Model: `amd/GLM-5.2-Quark-MXFP4-AttnFP8`

| Task  | flexible-extract | strict-match |
|---|---|---|
| GSM8K | 93.93% ± 0.66% | 93.71% ± 0.67% |

## Reproduction

### Launch server

```bash
SGLANG_USE_AITER=1 sglang serve \
    --model-path amd/GLM-5.2-Quark-MXFP4-AttnFP8 \
    --trust-remote-code --tp 8 \
    --json-model-override-args '{"qk_rope_head_dim": 64}' \
    --speculative-algorithm EAGLE \
    --speculative-num-steps 5 \
    --speculative-eagle-topk 1 \
    --speculative-num-draft-tokens 6 \
    --mem-fraction-static 0.8 \
    --chunked-prefill-size 16384 \
    --watchdog-timeout 1200 \
    --model-loader-extra-config '{"enable_multithread_load": true, "num_threads": 8}' \
    --host 127.0.0.1 --port 30000
```

### Accuracy

```bash
lm_eval --model local-completions \
    --model_args model=amd/GLM-5.2-Quark-MXFP4-AttnFP8,base_url=http://localhost:30000/v1/completions,num_concurrent=128,max_retries
  =10,max_gen_toks=2048,timeout=60000 \
    --batch_size auto --tasks gsm8k --num_fewshot 8
```

## Performance

Benchmarked on MI350X, tp=4, `sglang.bench_serving` random workload (1k input / 1k output, 128 prompts, concurrency 64), averaged across 5 runs.

| Checkpoint | Config | Output tput (tok/s) | Median TPOT (ms) | Median ITL (ms) |
|---|---|---|---|---|
| amd/GLM-5.2-MXFP4 | EAGLE spec-decoding | 2524 | 15.74 | 14.02 |
| **amd/GLM-5.2-Quark-MXFP4-AttnFP8** (this PR enables) | EAGLE spec-decoding | 2721 (**+7.8%**) | 14.83 (**-5.8%**) | 13.47 (**-3.9%**) |
| amd/GLM-5.2-MXFP4 | No spec-decoding | 1941 | 32.63 | 32.48 |
| **amd/GLM-5.2-Quark-MXFP4-AttnFP8** (this PR enables) | No spec-decoding | 2187 (**+12.7%**) | 28.92 (**-11.4%**) | 28.76 (**-11.5%**) |



<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:x: [Run #35765770714](https://github.com/sgl-project/sglang/actions/runs/35765770714)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #35765770296](https://github.com/sgl-project/sglang/actions/runs/35765770296)<!-- slot:pr-test-extra:end -->
Latest PR Test (AMD ROCm 10): <!-- slot:pr-test-amd:start -->:x: [Run #35765770584](https://github.com/sgl-project/sglang/actions/runs/35765770584)<!-- slot:pr-test-amd:end -->
<!-- pr-states:end -->

## 评论 (13)

### HaiShaw · 2026-06-30

/tag-and-rerun-ci

### ColinZ22 · 2026-07-01

@amd-bot ci-status

### amd-bot · 2026-07-01

> @ColinZ22

## CI Status for PR #28734

**Merge verdict:** Do **not** treat green/red at face value. **Zero of the failures are caused by this PR** — the AMD failures are perf-threshold flakes + one unrelated `TypeError`, and the NVIDIA/NPU/XPU/build failures are all infra (runner disk-full, missing `pytest`, serving flakes). **But PR CI does not exercise this PR's actual fix**: loading/inferencing MLA models with Quark **PTPC FP8 (W8A8 FP8)** attention on ROCm is covered by **no PR-CI test**, and most changed code is **gfx95 (MI355X)**-gated while the AMD test hardware is largely MI325. A human must verify the fix manually before merging. Also note the NVIDIA "PR Test Base" pipeline is **incomplete** (fast-fail cascade from an infra failure).

> [!CAUTION]
> **This PR's changed code is not verified by PR CI.** The fix targets Quark PTPC FP8 (`QuarkW8A8Fp8`) attention on MLA models (DeepSeek/GLM) on ROCm, and much of the diff is gated behind gfx95/`_use_aiter_gfx95` (MI355X). No PR-CI test loads a Quark W8A8-FP8 MLA checkpoint — the one Quark test that ran ([`test_quark_mxfp4.py`](https://github.com/sgl-project/sglang/actions/runs/28409635744/job/84230874209)) uses **Qwen3-8B + quark_mxfp4** (dense, MXFP4), not MLA + PTPC-FP8. The mi35x GLM-5/gpt-oss Quark tests are all `nightly=True` and MXFP4/W4A8, not PTPC-FP8. **Green does NOT verify this fix.** Before merging, run a Quark PTPC-FP8 MLA model (e.g. a DeepSeek/GLM W8A8-FP8 checkpoint) on MI355X and confirm load + inference correctness.

> [!WARNING]
> **NVIDIA "PR Test Base" CI is incomplete.** `base-b-test-1-gpu-small` shards 0–8 all failed on an infra error (`No space left on device` during `uv` setup), and `wait-for-base-b` fast-failed, cascading to skip downstream `base-b-*`/`base-c-*` jobs. Those downstream jobs are **not tested**. Re-run once the runner is healthy (or add `bypass-fastfail` sparingly for full signal).

Changed files: `dsa/dsa_indexer.py` (+20/-0), `quantization/fp8_utils.py` (+6/-2), `quark/schemes/quark_w8a8_fp8.py` (+19/-2), `deepseek_common/attention_forward_methods/forward_mla.py` (+15/-2), `models/deepseek_v2.py` (+31/-3)

**Executed CI failure attribution:** AMD: 4 failures (0 related) · Others: 5 root-cause failures (0 related) + ~25 fast-fail/aggregator cascade jobs (not independent). All jobs completed; none pending.

### AMD Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [stage-b-1gpu-large (0)](https://github.com/sgl-project/sglang/actions/runs/28409635744/job/84230874239) | `test/registered/perf/test_bench_serving_1gpu_part1.py` | `test_online_lora_latency` | `AssertionError: 2750.87 not less than 2400` | 🟢 | LoRA latency threshold; PR touches no LoRA/serving path — perf flake |
| [stage-b-2gpu-large (0)](https://github.com/sgl-project/sglang/actions/runs/28409635744/job/84230874221) | `test/registered/perf/test_bench_serving_2gpu.py` | `test_pp_offline_throughput_default_decode` | `AssertionError: 4755.4 not greater than 6700` | 🟢 | PP throughput threshold; unrelated to MLA/Quark FP8 changes |
| [stage-b-2gpu-large (1)](https://github.com/sgl-project/sglang/actions/runs/28409635744/job/84230874208) | `test/registered/perf/test_bench_one_batch_2gpu.py` | `test_torch_compile_tp2_bs1` | `AssertionError: 177.0 not greater than 200` | 🟢 | torch.compile throughput threshold; perf flake |
| [stage-b-1gpu-small (6)](https://github.com/sgl-project/sglang/actions/runs/28409635744/job/84230874410) | `test/registered/utils/test_type_based_dispatcher.py` | `test_type_dispatcher_e2e_performance` | `TypeError: Missing required argument 'input_embeds'` | 🟢 | Fails in `TypeBasedDispatcher`/`io_struct` construction; PR touches none of that code — pre-existing on main |

### Other Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [base-b-1gpu-small (0)/(1) + all shards](https://github.com/sgl-project/sglang/actions/runs/28409635748/job/84213808445) | N/A (env setup) | N/A | `No space left on device (os error 28)` during `uv` | 🟢 | Runner disk-full infra failure; root cause of all `base-b-*` + `wait-for-base-b` cascade fails |
| [build-test](https://github.com/sgl-project/sglang/actions/runs/28409635646/job/84212926720) | `test/registered/cpu/test_activation.py` | N/A | `ModuleNotFoundError: No module named 'pytest'` | 🟢 | Missing dep in CPU build image; infra/env, not PR |
| [stage-b-1gpu-xpu](https://github.com/sgl-project/sglang/actions/runs/28409635659/job/84213756379) | `test/registered/xpu/test_triton_attention_backend.py` | N/A (bench) | `ClientPayloadError: Response payload is not completed` | 🟢 | XPU serving-benchmark flake; PR is ROCm-only, doesn't touch XPU |
| [multimodal-gen-test-2-npu-a3](https://github.com/sgl-project/sglang/actions/runs/28409635645/job/84212929365) | `sglang/multimodal_gen/test/server/ascend/test_server_2_npu.py` | `test_diffusion_generation[qwen_image_t2i_2npu]` | `Consistency check failed` | 🟢 | NPU diffusion consistency flake; unrelated |
| [stage-b-test-4-npu-a3](https://github.com/sgl-project/sglang/actions/runs/28409635645/job/84212929341) | `test/registered/ascend/basic_function/quant/test_npu_w4a4_quantization.py` | `test_gsm8k` | `AssertionError: 896.78 not >= 1000` (throughput) | 🟢 | NPU W4A4 throughput threshold; unrelated to ROCm Quark FP8 |

### Details / what to do before merge

- **Coverage (top priority).** Add or run a Quark **PTPC FP8 / W8A8-FP8 MLA** model test on MI355X. The changed code paths — `_get_q_k_bf16`/`_get_k_bf16` 3-tuple handling in `dsa_indexer.py`, `is_quark_w8a8_fp8_layer` gating in `forward_mla.py` + `deepseek_v2._detect_gfx95_quant_format`, and the tuple-unpack in `apply_fp8_ptpc_linear` — are only reachable with such a checkpoint on gfx95. No PR-CI job loads one, so a green run is not evidence the fix works. Manually validate load + a short inference/accuracy run.
- **Minor caveat on shared FP8 code:** `channel_quant_to_tensor_quant` (added scale `unsqueeze` broadcast) and `apply_fp8_ptpc_linear` (`input[0], input[1]` unpack) sit on the shared aiter/ROCm FP8 path. The tuple-unpack change is backward-compatible with 2-tuples, so regression risk to non-Quark ROCm FP8 is low — but the AMD perf jobs above don't exercise PTPC-FP8, so they don't cover it either.
- **NVIDIA base CI:** infra disk-full, not a code issue — re-run `base-b`/`base-c` after the runner is cleaned so you get real NVIDIA signal.
- The `test_type_based_dispatcher.py` `TypeError` and the AMD perf-threshold fails are unrelated to this PR; no action needed from the author, though they indicate current AMD/main CI noise.
---
*Generated by amd-bot using Claude Code CLI*


### ColinZ22 · 2026-07-01

@HaiShaw Changes ready to be merged, all failing tests are unrelated to the changes made in this PR. Could you please review when you get a chance? Thanks!

### ColinZ22 · 2026-07-14

@amd-bot ci-status

### amd-bot · 2026-07-14

> @ColinZ22

## CI Status for PR #28734

**Merge verdict:** ❌ **Not ready — do not merge.** PR CI is **incomplete and not meaningful**: (1) the branch is **diverged from `main`** so `check-changes` blocked the standard pipeline (required base [`cbcbef6`](https://github.com/sgl-project/sglang/commit/cbcbef6)); (2) the AMD pipeline — the *only* CI that can exercise this ROCm/gfx95 fix — never ran its tests (`wait-for-stage-a-amd` timed out waiting for a runner, skipping all stage-b/stage-c AMD jobs). Every executed failure is unrelated to this PR's code, but **none of this PR's changed code was exercised by any test that ran.**

> [!CAUTION]
> **This PR's changed code is not verified by CI.** The fix targets MLA + Quark PTPC FP8 attention on ROCm/gfx95 (`fp8_utils.py`, `quark_w8a8_fp8.py`, `dsa_indexer.py`, `deepseek_v2.py`, `forward_mla.py`) — all AMD-only paths. The AMD run's `wait-for-stage-a-amd` timed out at 01:08 UTC because the MI325 runner didn't free up until 01:29 UTC (>4 h queue), so **all AMD stage-b/stage-c jobs (including the mi35x/gfx95 jobs) were skipped**. Green would prove nothing here, and CI isn't even green. Before merge: **rebase onto latest `main`**, then get a full AMD run (especially the `*-mi35x` jobs) and confirm the Quark-quantized MLA model (`GlmMoeDsaForCausalLM`) actually loads + infers on gfx95.

Changed files: `dsa_indexer.py` (+20/-0), `fp8_utils.py` (+6/-2), `quark/schemes/quark_w8a8_fp8.py` (+19/-2), `deepseek_common/.../forward_mla.py` (+15/-2), `deepseek_v2.py` (+31/-3)

**Executed CI failure attribution:** AMD: 0 executed test failures (stage-b/c **skipped** — not tested) · Others: 5 executed failures (0 related to this PR). Root gate: `check-changes` = **rebase required**.

### AMD CI status (no executed test failures — pipeline did not complete)

| Job | Result | Why |
|-----|--------|-----|
| [wait-for-stage-a-amd](https://github.com/sgl-project/sglang/actions/runs/29284998583/job/86935379249) | ❌ Timeout | Waited 4 h (240/240 attempts), timed out 01:08 UTC. `stage-a-test-1-gpu-small-amd` didn't start until 01:29 UTC (runner queue) and later **passed** — but too late. Infra/capacity, not this PR. |
| stage-b/stage-c AMD (incl. `*-mi35x`) | ⏭️ Skipped | Fast-fail cascade from the wait timeout. **These are the jobs that would exercise the PR — none ran.** |
| [call-gate / pr-gate](https://github.com/sgl-project/sglang/actions/runs/29284998324/job/86935291271) (Extra AMD) | ❌ By design | `Missing required label 'run-ci-extra'` — intentional gate, not a real failure. |

### Other Executed Failures (5 — none related to this PR)

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [stage-b-test-1-gpu-xpu](https://github.com/sgl-project/sglang/actions/runs/29284998315/job/86941308221) | `test/registered/xpu/test_topk.py` | `TestBiasedGroupedTopK.test_fast_biased_grouped_topk` | `ModuleNotFoundError: No module named 'tvm_ffi'` | 🟢 | XPU env/dependency gap; PR touches no XPU/topk/tvm_ffi code |
| [build-test (xeon-gnr, base-b-test-cpu)](https://github.com/sgl-project/sglang/actions/runs/29284998421/job/86935417609) | `test/run_suite.py` | N/A (import) | `AttributeError: type object 'HWBackend' has no attribute 'MLX'` | 🟢 | Stale-branch artifact: merge base predates the `MLX` enum member; PR doesn't touch `run_suite.py`/`ci_register.py`. Fixed by rebase. |
| [build-test (Arm64)](https://github.com/sgl-project/sglang/actions/runs/29284998506/job/86935391999) | `test/run_suite.py` | N/A (import) | `AttributeError: type object 'HWBackend' has no attribute 'MLX'` | 🟢 | Same stale-branch cause as Xeon row. |
| [multimodal-gen-test-1-npu-a3](https://github.com/sgl-project/sglang/actions/runs/29284998372/job/86935391778) | `.../ascend/test_server_1_npu.py` | `TestDiffusionServerOneNpu.test_diffusion_generation[glm_image_t2i_1npu / flux_image_t2i_npu]` | `DecodingStage perf: assert 801.67 <= 740.05` | 🟢 | NPU diffusion perf threshold; unrelated to ROCm MLA/FP8 |
| [stage-b-test-4-npu-a3](https://github.com/sgl-project/sglang/actions/runs/29284998372/job/86935391650) | `test/registered/ascend/basic_function/dllm/test_npu_llada2_mini.py` | LLaDA2.0-mini accuracy check | `AssertionError: accuracy 0.62 < 0.88` | 🟢 | NPU DLLM accuracy; unrelated to this PR |

_Cascade/aggregator jobs collapsed and excluded from the count: `check-changes` (rebase gate, root cause below), `pr-gate`/`*-finish` jobs (roll up upstream results)._

### Details / what to do before merge
1. **Rebase onto latest `main` and force-push.** `check-changes` failed with *"A major update has landed on `main` … Required base commit `cbcbef6`"* ([log](https://github.com/sgl-project/sglang/actions/runs/29284998571/job/86935293758)). Until rebased, the standard pipeline won't run, and the stale checkout is what produces the `HWBackend.MLX` build failures (the enum member exists on current `main`, confirmed in `python/sglang/test/ci/ci_register.py`).
2. **Get a complete AMD run — this is the real coverage gap.** After rebase, re-run AMD CI and confirm the `stage-b/stage-c` AMD jobs (especially the `*-mi35x`/gfx95 ones) actually execute this time. The prior run's skips were caused by an MI325 runner-queue timeout on `wait-for-stage-a-amd`, not by anything in the PR.
3. **Verify the fix's target path is truly exercised.** The PR fixes loading + inference of a Quark-MXFP4/PTPC-FP8 MLA model (`GlmMoeDsaForCausalLM`) on gfx95. Confirm an AMD test actually loads that quantized config on MI35x; if no PR-CI test covers it, the author should run it manually and report results, since a green AMD run otherwise still wouldn't verify the change.
4. The 5 executed failures (XPU `tvm_ffi`, NPU diffusion perf, NPU LLaDA accuracy) are pre-existing/infra and out of scope for this PR — no action needed here beyond noting they don't block on this PR's account.
---
*Generated by amd-bot using Claude Code CLI*


### ColinZ22 · 2026-07-22

@amd-bot ci-status

### amd-bot · 2026-07-22

> @ColinZ22

## CI Status for PR #28734

**Merge verdict:** No executed CI failure is caused by this PR — all 3 are unrelated pre-existing flakes (Intel XPU timeout, Llama NCCL weight-transfer, Qwen3 PD-disaggregation KV transfer). AMD pipeline completed downstream (stage-a/b/c all ran; stage-c green) and the base/NVIDIA CPU suite is green, so **from a red-X standpoint this is mergeable**. **However, PR CI does not verify the PR's actual purpose** — loading/inference of MLA models with Quark PTPC FP8 attention on ROCm/gfx95 is not exercised by any test that ran. Green here does *not* prove the fix works end-to-end.

> [!CAUTION]
> This PR's GPU runtime changes are **not exercised by PR CI**. The new test `test_quark_w8a8_fp8_ptpc.py` is `register_cpu_ci(suite="base-a-test-cpu")` and only covers the two hardware-independent helpers: `channel_quant_to_tensor_quant` broadcast ([`fp8_utils.py`](https://github.com/sgl-project/sglang/pull/28734/files)) and the `is_quark_w8a8_fp8_layer` predicate ([`quark_w8a8_fp8.py`](https://github.com/sgl-project/sglang/pull/28734/files)). The end-to-end path the PR title describes — the changes in `deepseek_v2.py::_detect_gfx95_quant_format`, `forward_mla.py` (fused-kernel gating), `dsa_indexer.py` (3-tuple extraction), and `apply_fp8_ptpc_linear` — requires an AMD **gfx95** GPU + a Quark-quantized MLA checkpoint (e.g. `GlmMoeDsaForCausalLM`), which no PR-CI job runs. **Before merge, the author should run a real gfx95 load + inference of a Quark PTPC-FP8 MLA model** and paste the result.

Changed files: `dsa_indexer.py` (+20), `fp8_utils.py` (+6/-2), `quark_w8a8_fp8.py` (+19/-2), `forward_mla.py` (+15/-2), `deepseek_v2.py` (+10/-3), `test_quark_w8a8_fp8_ptpc.py` (+108, new CPU test — ✅ ran & passed).

**Executed CI failure attribution:** AMD: 2 failures (0 related) · XPU: 1 failure (0 related) · Extra workflows: gate-only, opt-in not enabled (not a failure).

### AMD Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [stage-b-test-2-gpu-large-amd (1)](https://github.com/sgl-project/sglang/actions/runs/29808560329/job/88596668626) | `test/registered/model_loading/test_load_weights_from_remote_instance.py` | `test_load_weights_from_remote_instance` | `AssertionError: sgl_dp_1_dst_params rank 1` (params not close) | 🟢 | Llama-3.1/3.2 weight transfer over NCCL/transfer-engine; no Quark/FP8/MLA/DeepSeek code path touched by the PR |
| [stage-b-test-large-8-gpu-mi35x-disaggregation-amd](https://github.com/sgl-project/sglang/actions/runs/29808560329/job/88596668581) | `test/registered/amd/disaggregation/test_disaggregation_pp.py` | `test_disaggregation_pp` | `KVTransferError: Failed to get kvcache from prefill instance, it might be dead` → `KeyError: 'answer'` | 🟢 | Qwen3-8B PD-disaggregation KV transport flake; not a DeepSeek MLA / Quark path |

### Other Executed Failures

| Job | Test File | Test Function | Error | Related? | Why |
|-----|-----------|---------------|-------|----------|-----|
| [stage-b-test-1-gpu-xpu](https://github.com/sgl-project/sglang/actions/runs/29808560148/job/88594310381) | `test/registered/xpu/test_intel_xpu_backend.py` | `test_attention_backend` | `TIMEOUT after 1200s → exit 255` | 🟢 | Intel XPU backend test; PR only touches ROCm/aiter + generic quant helpers, no XPU path |

*(The `pr-test-amd-finish` / `finish` jobs are aggregators reporting the above; not independent failures. The `PR Test Extra` + `PR Test Extra (AMD)` `pr-gate` failures are the opt-in `run-ci-extra` label being absent — expected gating, not a failure. Consequently the "extra" suites did not run.)*

### Details / what to do before merge
- **Coverage (most important):** The three unrelated red Xs are safe to ignore, but do not read the green base/AMD run as verification of the fix. The reviewer must confirm the author ran the gfx95 + Quark PTPC-FP8 MLA load/inference manually (the scenario in the PR title), since no PR-CI job covers `forward_mla.py`, `dsa_indexer.py`, `deepseek_v2.py`, or `apply_fp8_ptpc_linear` on that hardware.
- **AMD flakes (🟢):** the 2-GPU `test_load_weights_from_remote_instance` and 8-GPU disagg failures are transport/loading flakes in unrelated models — a re-run of AMD stage-b is the cheapest confirmation, but no action is required on this PR.
- **XPU timeout (🟢):** unrelated Intel-backend timeout; re-run or ignore.
---
*Generated by amd-bot using Claude Code CLI*


### ColinZ22 · 2026-08-24

/rerun-failed-ci

### ColinZ22 · 2026-08-27

/rerun-failed-ci

### ColinZ22 · 2026-08-28

/rerun-failed-ci

### ColinZ22 · 2026-08-31

All PR check failures are unrelated to PR chagnes: 
- base-c-test-acc-2/4/8-npu-a3 and multimodal-gen-test-1-npu-a3 are runner/container failures with no test assertion reached, unrelated since this PR contains zero NPU code;
- stage-b-test-1-gpu-small/large: server timed out/failed initialization due to torch compile/dynamo environment fault on a BF16 MoE path, not related to PR changes;
- base-b-test-1-gpu-small (3): flaky exact-equality assert for logprobs of unquantized Llama-3.1-8B on NVIDIA with no FP8/AMD/quark involvement, unrelated to PR changes. 
- stage-c-test-large-8-gpu-amd-mi35x (0): Qwen3.5-FP8 accuracy drop, both the fused and unfused fusion launches failed, unrelated to PR code changes.

@HaiShaw Could you please help take a look and merge when you get a chance, thanks!

### ColinZ22 · 2026-09-03

/rerun-failed-ci

## Review (14)

### gemini-code-assist[bot] · 2026-06-19 · COMMENTED

## Code Review

This pull request introduces support for handling fused RMSNorm and FP8 group quantization outputs (which can be 2-tuples or 3-tuples containing unquantized bf16 tensors) across DSA indexing and linear layers. The review feedback highlights potential `TypeError` crashes in `dsa_indexer.py` when `x` is a 2-tuple, suggesting that it should be explicitly dequantized to `bfloat16` rather than left as a tuple. Additionally, the reviewer recommends a more idiomatic PyTorch approach using `unsqueeze(-1)` in a loop to reshape the scale tensor in `fp8_utils.py` instead of using tuple shape arithmetic.

> [!IMPORTANT]
> The [consumer version of Gemini Code Assist on GitHub](https://developers.google.com/gemini-code-assist/docs/review-repo-code) is being sunset. Starting **June 18, 2026**, new organization installations will be blocked, and all code review activity will officially cease on **July 17, 2026**.
> For more details on the timeline and next steps, please review the [Help Documentation](https://developers.google.com/gemini-code-assist/docs/deprecations/consumer-code-review).

### ColinZ22 · 2026-06-19 · COMMENTED

(no text)

### ColinZ22 · 2026-06-19 · COMMENTED

(no text)

### ColinZ22 · 2026-06-19 · COMMENTED

(no text)

### BowenBao · 2026-06-19 · COMMENTED

(no text)

### BowenBao · 2026-06-19 · COMMENTED

(no text)

### ColinZ22 · 2026-06-19 · COMMENTED

(no text)

### ColinZ22 · 2026-06-19 · COMMENTED

(no text)

### BowenBao · 2026-06-19 · COMMENTED

(no text)

### ColinZ22 · 2026-06-23 · COMMENTED

(no text)

### ColinZ22 · 2026-06-24 · COMMENTED

(no text)

### Arist12 · 2026-09-01 · COMMENTED

Nice fix — we've been deploying MXFP4 GLM checkpoints on gfx950 recently and ended up in these same code paths, so I read through this. The three fixes look correct and minimal to me, and the test file passes on our side (MI355X, `rocm/sgl-dev:v0.5.18-rocm724-mi35x-20260829`). One small note inline.

### ColinZ22 · 2026-09-02 · COMMENTED

(no text)

### BowenBao · 2026-09-10 · APPROVED

(no text)
