# [PR #39064] [ROCm][Bugfix] Keep quantization for mixed Quark Qwen3.5 MTP checkpoints

source: https://github.com/sgl-project/sglang/pull/39064
state: open | updated: 2026-09-23T06:54:42Z
labels: quant, run-ci

## 正文

## Motivation

Speculative decoding is currently not working for amd/Qwen3.8-2.4T-A95B-Quark-MXFP4 (NEXTN, TP8, gfx950), because MTP does not load on mixed Quark MXFP4 Qwen3.5 checkpoints. This PR fixes the issue by addressing two separate problems:

`_mtp_quant_config` drops the draft's quant config as soon as any `mtp.*` layer appears in the Quark exclude list. That is correct for checkpoints whose MTP module is entirely bf16 (#23146), but here it builds the whole draft unquantized, so the MoE loader allocates bf16 experts that the MXFP4 checkpoint shards no longer fit.

`Qwen3_5ForCausalLMMTP` declares no `packed_modules_mapping`, which the loader reads off the model class so the quant config can expand `qkv_proj` into `q_proj`/`k_proj`/`v_proj` before matching the exclude list. Without it an excluded attention projection is allocated MXFP4 - half as wide once packed - against bf16 shards.

## Modifications

Two changes in `python/sglang/srt/models/qwen3_5_mtp.py`:

- `_mtp_quant_config`: the Quark branch now takes the whole-module skip only when the routed experts are excluded as well, rather than on any `mtp.*` entry. They are the bulk of the draft, and the ordinary per-layer exclusions keep attention, the shared expert and `fc` in bf16 on their own.
- `Qwen3_5ForCausalLMMTP`: declare `packed_modules_mapping`, aliasing the target model's as `Qwen3_5ForConditionalGeneration` already does. That is also what the `TODO` above the Quark block in `_get_quantization_config` asks for.

`test/registered/unit/models/test_qwen3_5_mtp_quant_config.py` is new, covered under Accuracy Tests below.

Nothing else changes behaviour. The `mtp.*` exclude lists of the two published AMD checkpoints are identical apart from the routed experts:

| checkpoint | `mtp.*` excludes | of which `mlp.experts` |
| --- | --- | --- |
| amd/Qwen3.5-397B-A17B-MXFP4 | 1546 | 1536 (= 512 x 3) |
| amd/Qwen3.8-2.4T-A95B-Quark-MXFP4 | 10 | 0 |

So the 397B still takes the skip and is unaffected. The only lists whose behaviour changes are Quark lists naming some `mtp.*` layers but not the routed experts, which is exactly the mixed case.

One side effect worth flagging: `_get_quantization_config` mutates the mapping it reads off the class, so a Quark run now adds `fused_qkv_a_proj_with_mqa` to `Qwen3_5ForCausalLM.packed_modules_mapping` process-wide. It is harmless here and `Qwen3_5ForConditionalGeneration` already aliases the same dict, but I can copy instead if you prefer.

## Accuracy Tests

The draft loads and serves on `amd/Qwen3.8-2.4T-A95B-Quark-MXFP4`, NEXTN 3/1/4, TP8, MI350X; without this change it fails at load. New CPU-only unit tests cover both checkpoint shapes, using the real exclude entries, including a regression guard for #23146.

## Speed Tests and Profiling

None; this only changes which weights are allocated quantized at load time.

## Checklist

- [x] Format your code according to the [Format code with pre-commit](https://docs.sglang.io/developer_guide/contribution_guide.html#format-code-with-pre-commit).
- [x] Add unit tests according to the [Run and add unit tests](https://docs.sglang.io/developer_guide/contribution_guide.html#run-and-add-unit-tests).
- [ ] Update documentation according to [Write documentations](https://docs.sglang.io/developer_guide/contribution_guide.html#write-documentations). No user-facing behaviour changes, so there is nothing to document; happy to add a note if you want one.
- [x] Provide accuracy and speed benchmark results according to [Test the accuracy](https://docs.sglang.io/developer_guide/contribution_guide.html#test-the-accuracy) and [Benchmark the speed](https://docs.sglang.io/developer_guide/contribution_guide.html#benchmark-the-speed).
- [x] Follow the SGLang code style [guidance](https://docs.sglang.io/developer_guide/contribution_guide.html#code-style-guidance).


<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:x: [Run #35745236127](https://github.com/sgl-project/sglang/actions/runs/35745236127)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #35745235874](https://github.com/sgl-project/sglang/actions/runs/35745235874)<!-- slot:pr-test-extra:end -->
Latest PR Test (AMD ROCm 10): <!-- slot:pr-test-amd:start -->:x: [Run #35745236230](https://github.com/sgl-project/sglang/actions/runs/35745236230)<!-- slot:pr-test-amd:end -->
<!-- pr-states:end -->

## 评论 (1)

### omirosh · 2026-09-23


Investigated 2026-09-23 against Actions runs:

- CUDA base: [35745236127](https://github.com/sgl-project/sglang/actions/runs/35745236127)
- AMD ROCm 10: [35745236230](https://github.com/sgl-project/sglang/actions/runs/35745236230)
- Intel CPU: [35745235845](https://github.com/sgl-project/sglang/actions/runs/35745235845)

The PR only changes `python/sglang/srt/models/qwen3_5_mtp.py` and adds `test/registered/unit/models/test_qwen3_5_mtp_quant_config.py`. It is load-time Quark exclude-list / `packed_modules_mapping` for Qwen3.5 MTP.

**None of the real test failures hit that path.** CPU `base-a-test-cpu` shards all passed, so the new unit test is almost certainly green. AMD stage-c never ran: `wait-for-stage-b-amd` fail-fasted after one MI300 shard died during MORI clone.

## Why jobs sit queued for so long

This is runner scarcity plus SGLang's stage gates, not this PR.

CUDA is staged: A, then B, then C. `wait-for-base-b` **passed** after **2h 41m** (started 2026-09-23 01:19 UTC, finished 04:00 UTC). That waiter only polls; the wall time is "all base-b GPU shards must finish before any base-c job is even created." Base-c jobs were then created together at 04:00:37 UTC.

Most base-c matrices use `max_parallel: 1` on scarce boxes (`4-gpu-h100`, `4-gpu-b200`, `8-gpu-h20`, `8-gpu-b300`, `8-gpu-h200`). Shards of the same type cannot overlap, so later shards wait behind the one that has the machine.

Once `base-c-test-4-gpu-h100 (0)` actually ran and failed, later jobs that finally got a runner died in **12-42s** on `check-pr-test-health` (fail-fast). They look like "queued forever then instantly red" because the queue was hours and the test body never started.

| Job | Created (UTC) | Started (UTC) | Queue | What happened after start |
| --- | --- | --- | --- | --- |
| `wait-for-base-b` | (CUDA run) | 01:19 | n/a (poller) | Waited **2h 41m** for CUDA base-b to finish; then succeeded and unlocked base-c. |
| `base-c-test-4-gpu-h100 (0)` | 04:00 | 04:30 | ~30 min | Only CUDA base-c job that ran tests; failed (HF 429). |
| `base-c-test-4-gpu-b200 (1-4)` | 04:00 | 04:36-04:56 | 36-56 min | Fail-fast on health check; no tests. |
| `base-c-test-8-gpu-h200 (1)` | 04:00 | 04:48 | ~48 min | Fail-fast. |
| `base-c-test-8-gpu-h20 (0)` | 04:00 | 06:08 | **~2h 8m** | Fail-fast after getting an h20 runner. |
| `base-c-test-4-gpu-h100 (2)` | 04:00 | 06:09 | **~2h 9m** | Fail-fast. |
| `base-c-test-8-gpu-b300 (0)`, `h20 (1)`, `h20 (2)` | 04:00 | still queued | **>2.5h and counting** | No runner assigned. `8-gpu-b300` is a single scarce pool. |
| AMD `stage-b-test-1-gpu-small` shard 11 | 15:33 | 16:48 | **~75 min** | Then died cloning a MORI submodule. Other MI300 shards were already occupying `linux-mi300-1gpu-sglang`. |
| AMD `wait-for-stage-b-amd` | 15:33 | 15:33 | none | Polled 19 jobs for 1h 29m, then fail-fasted when shard 11 failed. That **skipped all AMD stage-c**. |

## Actual test / install failures

| Job | Failed test / step | Reason | Relevant to PR? |
| --- | --- | --- | --- |
| [`stage-b-test-1-gpu-small-amd-mi35x`](https://github.com/sgl-project/sglang/actions/runs/35745236230/job/106815084099) | `test/registered/kernels/ops/kimi_k3/flydsl_ops/test_kimi_k3_kda_decode.py` | Import-time `ModuleNotFoundError: No module named 'aiter.ops.flydsl.utils'`. 23/36 files in that shard passed. | **No.** Kimi K3 flydsl kernel vs aiter pin in the CI image. |
| [`stage-b-test-1-gpu-small-amd` shard 11](https://github.com/sgl-project/sglang/actions/runs/35745236230/job/106815084871) | Install dependencies (never reached tests) | `git clone` of MORI `3rdparty/spdk`: `RPC failed; curl 56 Recv failure: Connection reset by peer`, exit 128. | **No.** Network flake while reinstalling MORI. |
| [`base-c-test-4-gpu-h100 (0)`](https://github.com/sgl-project/sglang/actions/runs/35745236127/job/107039269583) | `TestMTPWithTBO.setUpClass` (`test/registered/ep/test_deepep_small.py`) | Server died loading **DeepSeek** `lmsys/sglang-ci-dsv3-test-NextN` with DeepEP + EAGLE. Hugging Face **429 Too Many Requests** (`3000 api requests / 5 min`). Rank 0 scheduler exit **-3**. | **No.** CI HF rate limit on a DeepSeek NextN fixture, not Qwen3.5 Quark MTP. |
| [Intel SPR shard 3](https://github.com/sgl-project/sglang/actions/runs/35745235845/job/106805199164) | `TestSubBlockSparseAttentionModalities.test_bf16_does_not_require_sparge_attention` | `AttributeError`: mock patch of `minimax_h3.get_global_forced_attn_backend` (attribute missing). Same failure as on #36505. Sibling SPR/GNR shards cancelled. | **No.** CPU multimodal-gen mock; this PR does not touch that module. |

## Infra / gate / cascade

| Check | What happened | Relevant to PR? |
| --- | --- | --- |
| CUDA `base-c-test-4-gpu-b200 (1-4)`, `h100 (1-2)`, `h200 (1)`, `h20 (0)` | `check-pr-test-health` fail-fast: root cause is `base-c-test-4-gpu-h100 (0)`. Tests never ran. | **No.** |
| CUDA `base-c-test-8-gpu-b300 (0)`, `h20 (1)`, `h20 (2)` | Still **queued** with no runner (hours). | **No.** |
| `wait-for-stage-b-amd` | Fail-fast after MI300 shard 11 failed. AMD stage-c jobs skipped. | **No.** |
| `pr-test-amd-finish` | Cascades from AMD stage-b wait failure. | **No.** |
| `call-gate / pr-gate` (extra + amd-extra) | **"Require run-ci-extra label"**. Extra suites skipped. | **No.** |
| `pr-test-extra-finish`, `pr-test-amd-extra-finish` | Extra was gated off. | **No.** |

## What this means for the PR

The only suite that would have exercised the new CPU unit test (`base-a-test-cpu`) is **green**. Nothing that failed loads `Qwen3_5ForCausalLMMTP` or a mixed Quark Qwen3.5 checkpoint.

To get a cleaner CUDA base-c signal you would need a rerun after the HF 429, or `bypass-fail-fast` so later shards still run. AMD stage-c needs shard 11 (or `wait-for-stage-b-amd`) retried after the MORI clone flake. Long queues will remain until H100/B200/H20/B300 runners drain; that is pool size and `max_parallel: 1`, not this diff.

