# [PR #20] Fixed subtle bug in triton_util.py

source: https://github.com/gpu-mode/lectures/pull/20
state: closed | updated: 2024-05-12T14:30:07Z
labels: 

## 正文

The check if a tensor is is_contiguous in `triton_util.py` is subtly wrong. Should be `t.is_contiguous()` instead of `t.is_contiguous`.

## 评论 (0)
