# [Issue #41230] [Docs] Document NIXL KV connector metrics aggregation semantics

source: https://github.com/vllm-project/vllm/issues/41230
state: open | updated: 2026-09-22T12:40:36Z
labels: good first issue

## 正文

## Summary

The NIXL KV connector logs transfer metrics periodically:

```
KV Transfer metrics: Num successful transfers=4, Avg xfer time (ms)=1.381, P90 xfer time (ms)=2.601, Avg post time (ms)=0.672, P90 post time (ms)=0.801, Avg MB per transfer=2.25, Throughput (MB/s)=1629.549, Avg number of descriptors=72.0
```

Currently there is no documentation explaining what these metrics represent, especially in the context of **multi-rank (TP > 1) deployments**. This has already caused confusion among users.

## Current behavior

All metrics are **aggregated across all TP ranks** before summary stats are computed:

```python
1. Each TP rank independently records per-transfer telemetry (`transfer_duration`, `post_duration`, `bytes_transferred`, `num_descriptors`) via `NixlKVConnectorStats.record_transfer()` in [`stats.py`](https://github.com/vllm-project/vllm/blob/main/vllm/distributed/kv_transfer/kv_connector/v1/nixl/stats.py).
2. Stats from all ranks are concatenated via `aggregate()` (`list.extend()`).
3. `reduce()` computes averages, percentiles, and throughput over the **combined** pool of observations from all ranks.
```

This means:
```
- **"Num successful transfers"** is the total count across all ranks, not per-rank.
- **"Avg MB per transfer"** is the average over all individual rank-level transfers, not the total bytes moved for a single KV cache transfer operation.
- **"Throughput (MB/s)"** is `total_MB_all_ranks / total_time_all_ranks` — effectively an average per-rank throughput, not the aggregate system throughput.
- **Percentiles (P90)** are computed over the combined distribution of all ranks' transfer times.
```

This is unintuitive because users may expect metrics to reflect per-engine totals or aggregate system throughput.

## What needs to be documented

1. **Docstrings in `stats.py`**: Add clear documentation to `NixlKVConnectorStats` explaining that stats are aggregated across all TP ranks and what each metric represents in that context.
2. **Inline comments in `reduce()`**: Clarify the semantics of throughput and averages — that they are per-rank averages over the combined observation pool.
3. **Docstrings in `metrics.py`**: Document the `observe()` → `aggregate()` → `reduce()` → `log()` pipeline and the fact that stats arrive pre-aggregated across workers.
4. **(Optional) Docs page**: Add a section to the disaggregated serving documentation explaining how to interpret the KV Transfer metrics log line.

## Relevant files

```
- `vllm/distributed/kv_transfer/kv_connector/v1/nixl/stats.py` — `NixlKVConnectorStats` (recording, aggregation, reduction)
- `vllm/distributed/kv_transfer/kv_connector/v1/metrics.py` — `KVConnectorLogging` (observe/log pipeline), `KVConnectorStats` (base class)
```

## Context

See related discussion: metrics are aggregated across ranks rather than reported per-rank or per-engine. This is a deliberate design choice (fire-and-forget from workers), but it needs to be clearly documented so users can correctly interpret the numbers.

## 评论 (16)

### zeel2104 · 2026-04-29

Open for contribution?

### WorldExplored · 2026-04-29

@zeel2104 Can I work on this with you? I would like to take points 2/3.  @robertgshaw2-redhat  

### zeel2104 · 2026-04-29

Hi @WorldExplored, I actually just finished the implementation and submitted the PR! I ended up covering the scaling logic and the related points while I was in the flow.

You're more than welcome to take a look at the PR and leave a review or test it on your hardware to make sure everything looks good on your end. Thanks for the offer to help!

### kevglynn · 2026-05-04

Opened a PR for this: #41600

I traced the full observe → aggregate → reduce → log pipeline through the worker, executor, and logger to make sure the docstrings accurately describe the data flow and unit conversions. Happy to adjust scope or style based on review feedback.

### sridhar-3009 · 2026-05-30

I'd like to work on this — I'll submit a PR.

### monish-rgb · 2026-06-02

Hi, I am new to vLLM and interested in contributing. I noticed PR #44055 is already working on this issue. If there is any remaining documentation improvement or beginner-friendly follow-up tasks, I would be happy to help.

### sridhar-3009 · 2026-06-03

Hi @monish-rgb — PR #44055 covers the core documentation work (docstrings for `NixlKVConnectorStats`, inline comments in `reduce()`, and the `observe → aggregate → reduce → log` pipeline in `metrics.py`).

The one item from the issue that is still open is the **optional docs page** (point 4): a section in the disaggregated serving documentation explaining how to interpret the KV Transfer metrics log line in multi-rank deployments. That would live in `docs/serving/` and would be a good standalone follow-up contribution if you are interested.

### monish-rgb · 2026-06-03

I would like to contribute to this documentation (point4) thanks!

### markmc · 2026-06-12

This issue appears to be somewhat of a bot magnet ...

#44055 has been merged, covering item 4 (docs on the metrics added to `docs/features/nixl_connector_usage.md`).

I made a fix to #44055 before merging: the descriptions of "post time" were incorrect AIUI. They described it as a post-transfer notification step, but it's actually the time to submit/post the transfer request to the RDMA backend before the async data movement begins.

Items 1-3 (source-level documentation in stats.py and metrics.py) are still open.

The remaining open PRs — #41259, #41900, #43849 — all overlap with the now-merged docs work, and all carry the same incorrect "post-transfer notification" description. I'm going to close them.

@NickLucche - your call on whether #44055 was sufficient (and correct!)

### ammmanism · 2026-07-09

Hi! I'd like to work on the remaining source-level documentation for this issue (items 1–3 in stats.py and metrics.py). I reviewed the latest discussion and understand that the docs page (item 4) has already been completed. If no one is actively working on the remaining tasks, could you please assign this issue to me? Thanks!

### kritikagarg · 2026-07-19

Hello! Could you please assign this to me? I'd love to contribute. From what I understand, items 1–3 (source-level documentation in stats.py and metrics.py) are still open, and I'd be happy to work on them.


### dhariskhan1275 · 2026-07-28

can i work on it?

### n-dlms · 2026-08-03

I'm working on the remaining items 1-3 (source-level documentation in `vllm/distributed/kv_transfer/kv_connector/v1/nixl/stats.py` and `vllm/distributed/kv_transfer/kv_connector/v1/metrics.py`). The docs page (item 4) was completed by #44055. PR is up now.

### code-killer0 · 2026-09-09

Hi, I love to contribute. Can I start?


### arnavsharma1811 · 2026-09-18

I'd like to work on this. I'll add docstrings to NixlKVConnectorStats, inline comments in reduce(), and update metrics.py to clarify the aggregation semantics. Please let me know if there are any specific formatting requirements.

### grigortodorov · 2026-09-22

I think i can help on this.
