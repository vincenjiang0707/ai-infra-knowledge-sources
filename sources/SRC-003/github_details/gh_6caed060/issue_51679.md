# [Issue #51679] [Bug]: qwen3_xml tool parser consumes `</think>`, merging reasoning into `content` with no way to split it

source: https://github.com/vllm-project/vllm/issues/51679
state: open | updated: 2026-09-24T15:42:53Z
labels: tool-calling

## 正文

```bash
> **Edited.** The original body claimed that `--reasoning-parser qwen3` leaves
> `reasoning_content` empty. That was wrong — it works, and it is a usable
> workaround. The section is corrected in place below; the retraction and the
> evidence are in the comments, which is why they refer to text no longer here.
```

### Your current environment

<details>
<summary>Environment</summary>

```
vLLM version : 0.26.0 (from GET /version)
Model        : Qwen/Qwen3.6-27B  (stock, apache-2.0; config model_type=qwen3_5)
GPU          : 8x NVIDIA A100-SXM4-40GB, driver 535.86.10
PyTorch      : 2.11.0+cu129 (CUDA 12.9)
Python       : 3.12.12
OS           : Linux 5.15.0 (x86_64, glibc 2.31)
```

</details>

Server command (note: **no `--reasoning-parser`**):

```bash
vllm serve Qwen/Qwen3.6-27B \
    --enable-auto-tool-choice \
    --tool-call-parser qwen3_xml \
    --port 8020 \
    --max-model-len 262144 \
    --tensor-parallel-size 8 \
    --gpu-memory-utilization 0.85 \
    --enable-prefix-caching \
    --mamba-cache-mode align \
    --max-num-seqs 32 \
    --disable-custom-all-reduce \
    --enforce-eager
```

```bash
> One deviation, in the interest of a repro anyone can run: our server was
> actually started with an additional `--chat-template <custom>.jinja`. For the
> single-user-message prompt used below that file renders **byte-identically** to
> the checkpoint's built-in `chat_template.jinja` (both verified through Jinja
> directly), so the flag is dropped here as it is not part of the mechanism. We
> have not re-run the repro with the built-in template selected explicitly.
```

### 🐛 Describe the bug

With `--tool-call-parser qwen3_xml` and **no reasoning parser configured**, the
`</think>` tag is consumed by the parser and never reaches the client. Because
no reasoning parser is configured there is also no `reasoning_content` field to
receive the reasoning — so the reasoning text is silently concatenated into
`content` ahead of the answer, with the only boundary marker removed. A chat
client is left with no way to tell deliberation from the final answer.

The Qwen3 chat template opens `<think>\n` in the generation prompt, so every
reply begins inside a think block and the model closes it with `</think>` before
answering. That closing tag is the client's only signal, and it is exactly what
goes missing.

**A workaround exists** — attach `--reasoning-parser qwen3` and the split is
correct (see below). The report is that the asymmetric configuration loses
information *silently*, not that there is no way to avoid it.

#### Reproduction

```bash
Send the same prompt two ways — through `/v1/chat/completions` (template renders
the prompt, output passes the tool-call parser) and through `/v1/completions`
(identical prompt string, no parser). The rendered generation prompt is
byte-identical in both cases:
```

```
'<|im_start|>user\nWhat is 17*23?<|im_end|>\n<|im_start|>assistant\n<think>\n'
```

<details>
<summary>repro.py</summary>

```python
import json, urllib.request

BASE, MODEL = "http://127.0.0.1:8020", "Qwen/Qwen3.6-27B"
PROMPT = "What is 17*23?"
# Exactly what the model's own chat template renders for the message list below
# (verified with AutoTokenizer.apply_chat_template(..., add_generation_prompt=True)).
RAW = f"<|im_start|>user\n{PROMPT}<|im_end|>\n<|im_start|>assistant\n<think>\n"

def post(path, payload):
    req = urllib.request.Request(BASE + path, data=json.dumps(payload).encode(),
                                 headers={"Content-Type": "application/json"})
    with urllib.request.urlopen(req, timeout=600) as fh:
        return json.load(fh)

for i in range(5):
    d = post("/v1/chat/completions", {"model": MODEL, "max_tokens": 1500, "temperature": 0.7,
                                      "messages": [{"role": "user", "content": PROMPT}]})
    m = d["choices"][0]["message"]
    t = m.get("content") or ""
    print(f"A chat/completions  close_tag={'</think>' in t!s:<5} len={len(t):<5} "
          f"reasoning_content={m.get('reasoning_content')!r}")

for i in range(5):
    d = post("/v1/completions", {"model": MODEL, "prompt": RAW,
                                 "max_tokens": 1500, "temperature": 0.7})
    t = d["choices"][0]["text"]
    print(f"B completions       close_tag={'</think>' in t!s:<5} len={len(t):<5}")
```

</details>

Output:

```
A chat/completions  close_tag=False len=848   reasoning_content=None
A chat/completions  close_tag=False len=832   reasoning_content=None
A chat/completions  close_tag=False len=905   reasoning_content=None
A chat/completions  close_tag=False len=1156  reasoning_content=None
A chat/completions  close_tag=False len=1347  reasoning_content=None
-> 0/5 contained '</think>'

B completions       close_tag=True  len=849
B completions       close_tag=True  len=887
B completions       close_tag=True  len=1034
B completions       close_tag=True  len=1132
B completions       close_tag=True  len=810
-> 5/5 contained '</think>'
```

`finish_reason` is `stop` in all ten samples, so this is not truncation. The
returned `content` in case A still contains the full deliberation followed by the
answer — nothing is dropped, but the two are now indistinguishable:

```
Here's a thinking process:

1.  **Understand the User's Request**: ...
   [~700 more characters of deliberation]

17 × 23 = **391**
```

#### Where it happens

`qwen3_xml` resolves to the combined engine in `vllm/parser/qwen3.py`, which owns
the reasoning phase as well as tool calls: it starts in `ParserState.REASONING`
and its `THINK_END` transition emits a `REASONING_END` event without re-emitting
the literal tag.

```python
# vllm/parser/qwen3.py
initial_state=ParserState.REASONING if thinking else ParserState.CONTENT,
...
(ParserState.REASONING, "THINK_END"): Transition(
    ParserState.CONTENT,
    (EventType.REASONING_END,),
),
# Absorb duplicate </think> — model may emit it after
# already transitioning to CONTENT; drop it silently.
(ParserState.CONTENT, "THINK_END"): Transition(
    ParserState.CONTENT,
    (),
),
```

That is correct behaviour when a reasoning parser is present to receive the
reasoning segment. When one is *not* configured, the segment has nowhere to go
and is folded back into `content` — minus its delimiter.

Note `Qwen3Parser.__init__` derives `thinking` from
`chat_template_kwargs.get("enable_thinking", True)` only — nothing consults
whether a reasoning parser is actually attached.

#### What the reasoning adapter does (the workaround)

Instantiating the adapters directly — CPU, no server — shows the reasoning path
is healthy, so this is purely about the asymmetric configuration:

```python
from transformers import AutoTokenizer
from vllm.reasoning.qwen3_engine_reasoning_parser import Qwen3ParserReasoningAdapter

tok = AutoTokenizer.from_pretrained("Qwen/Qwen3.6-27B")
gen = "Let me work it out.\n17*20=340, 17*3=51, sum 391.\n</think>\n\n17 x 23 = **391**"
Qwen3ParserReasoningAdapter(tok).extract_reasoning(gen, request)
```

```
reasoning_content = 'Let me work it out.\n17*20=340, 17*3=51, sum 391.\n'
content           = '\n\n17 x 23 = **391**'
```

`extract_tool_calls` on a reply whose tool call follows `</think>` also returns
`tools_called=True`, so attaching the reasoning parser does **not** reintroduce
the failure mode of #39056 on this version — I could not reproduce that here.

Same check on the tool adapter shows the merge from the other side: for a reply
that deliberates and then calls a tool, the leftover content is
`'Thinking about it.'` — the reasoning, tag removed.

#### Expected behaviour

With a tool parser configured and no reasoning parser, one of:

1. leave `</think>` in `content` (i.e. the tool-call parser is transparent with
   respect to the reasoning phase), so clients can split as they could under the
   old split parsers; or
2. populate `reasoning_content` anyway, since the combined parser has in fact
   separated the two; or
3. warn, or refuse the configuration at startup, so the loss is not silent.

Any of these is fine; the current behaviour is the one option that loses
information without saying so.

#### Why this configuration exists at all

Deliberately, in our case. With the reasoning inline in `content`, every OpenAI
-compatible client replays the model's own deliberation as ordinary content;
`reasoning_content` is non-standard and most agent CLIs drop it on the floor. So
"tool parser, no reasoning parser" is not a misconfiguration to be talked out of
— it is the configuration that keeps thinking visible to clients that only speak
the standard schema.

#### Context: this is an edge of the unified-parser migration

The combined engine is the shape proposed in #32713, *[RFC]: Unified Parser for
reasoning, tool calling* — closed as not planned / stale, though the unified
parser landed. By my reading the RFC never discussed the asymmetric
configuration, which is exactly where the reasoning segment ends up with no
consumer and its delimiter removed. Under the old split parsers the tool parser
had no reasoning phase to own and the tag survived by construction.

#### Impact

We drive Qwen3.6-27B through a dozen different agent CLIs and reconstruct
trajectories from the HTTP traffic. Every one of those harnesses reports zero
thinking content on this stack, because they all split on `</think>`. It is a
labelling loss rather than a data loss, but it is unrecoverable downstream and
it is silent — nothing in the response indicates a boundary was removed.

#### Possibly related

These describe the opposite direction (content ending up in `reasoning`, or tool
calls trapped in reasoning) and are on 0.19.x, i.e. before the combined parser:

- #39056 — tool calls lost when XML markup is emitted inside `<think>`
  (`--reasoning-parser qwen3` + `--tool-call-parser qwen3_coder`)
- #35221 — reasoning-only output parsed as `content` (closed, PR #35230)
- #40816, #38894 — answer emitted in `reasoning`, `content` empty

#### Before submitting a new issue...

- [x] I have searched for similar existing issues and read the
  [troubleshooting docs](https://docs.vllm.ai/en/stable/usage/troubleshooting.html).


## 评论 (11)

### tic-top · 2026-08-10

Correction to my own "Note on `--reasoning-parser qwen3`" section above — that
paragraph is wrong, or at least not supported by the evidence I attributed to it,
and I would rather retract it than leave it sitting in a bug report.

I re-checked by instantiating the adapters directly (CPU, no server), which
exercises the same classes the server would use:

```python
from transformers import AutoTokenizer
from vllm.reasoning.qwen3_engine_reasoning_parser import Qwen3ParserReasoningAdapter

tok = AutoTokenizer.from_pretrained("Qwen/Qwen3.6-27B")
gen = "Let me work it out.\n17*20=340, 17*3=51, sum 391.\n</think>\n\n17 x 23 = **391**"
print(Qwen3ParserReasoningAdapter(tok).extract_reasoning(gen, request))
```

```
reasoning_content = 'Let me work it out.\n17*20=340, 17*3=51, sum 391.\n'
content           = '\n\n17 x 23 = **391**'
```

So the reasoning adapter splits correctly. My earlier claim that
`reasoning_content` comes back empty with the flag on came from a separate
experiment on this box that I did not re-run, and I now believe it was mistaken.
Please disregard that section.

Two things this does not change:

* **The report itself stands.** The configuration in question is
  `--tool-call-parser qwen3_xml` with **no reasoning parser**, and there
  `</think>` is still consumed with nothing to receive the reasoning — 0/5 vs
  5/5 as measured above. Attaching a reasoning parser is a workaround, not a
  reason the current behaviour is fine.
* **Tool calls survive either way** on 0.26.0, which I also confirmed with the
  same direct instantiation — `extract_tool_calls` on a reply whose tool call
  follows `</think>` returns `tools_called=True`. Worth stating explicitly since
  #39056 is about tool calls being lost once a reasoning parser is attached; I
  could not reproduce that failure mode here.

Incidentally, the same direct check shows the reasoning text leaking into the
tool parser's leftover `content` (`content = 'Thinking about it.'` for a reply
that opened with deliberation and then called a tool), which is the same merge
described in the report, just visible on the tool-call path.


### tic-top · 2026-08-10

Context that may help triage: the combined engine this report is about
(`vllm/parser/qwen3.py`) is the shape proposed in #32713, *[RFC]: Unified Parser
for reasoning, tool calling*. That RFC is closed as not planned / stale, but the
unified parser landed anyway, and by my reading the RFC never discussed the
asymmetric configuration — a tool parser attached with no reasoning parser —
which is exactly where the reasoning segment ends up with no consumer and its
delimiter removed.

So this is less "a parser bug" than an unhandled edge of a half-migrated design:
under the old split parsers the tool parser had no reasoning phase to own, and
the tag survived by construction. Worth deciding deliberately which of the three
behaviours in the report is intended, rather than leaving it to whichever parser
happens to be configured.


### Shreyas-jk · 2026-08-10

I traced this and can confirm the mechanism, with the exact transition responsible. Static analysis against `main` @ `8fea1d306` — I have not reproduced on a served model, so the runtime symptoms are yours, not mine.

### Mechanism

The engine-based tool adapter always starts the shared engine in `CONTENT`:

```python
- streaming — `vllm/parser/engine/adapters.py:178`, `engine.initialize_streaming(initial_state=ParserState.CONTENT)`
- non-streaming — `vllm/parser/engine/parser_engine.py:556`, `extract_tool_calls_from_content(...)` with `initial_state=ParserState.CONTENT`
```

In `CONTENT`, `</think>` matches this entry:

```python
# vllm/parser/qwen3.py:132
# Absorb duplicate </think> — model may emit it after
# already transitioning to CONTENT; drop it silently.
(ParserState.CONTENT, "THINK_END"): Transition(ParserState.CONTENT, ()),
```

The transition emits no events, so the tag is discarded.

With `--reasoning-parser qwen3` attached this is correct: the reasoning adapter consumes the real `</think>` and hands the tool adapter reasoning-stripped content, so any `</think>` still arriving genuinely *is* a duplicate. With no reasoning parser, `ParserManager.get_parser` composes the `DelegatingParser` with `reasoning_parser_cls=None` (`vllm/parser/parser_manager.py:130-140`), the engine never enters `REASONING`, and the reasoning text is emitted as `CONTENT` with its only delimiter removed. Both the streaming and non-streaming paths are affected identically, since both start in `CONTENT`.

This matches your reading in the comments: the absorb rule is right for the configuration it was written for, and has no way to tell the two configurations apart.

### One thing that makes the fix cheaper than it looks

An unmatched terminal is not dropped — the engine already falls through to emitting its literal text:

```python
# vllm/parser/engine/streaming_parser_engine.py:322
transition = self.config.transitions.get(key)
if transition is None:
    ...
    return self._emit_for_state(value)
```

So simply *not* installing `(CONTENT, THINK_END)` makes `</think>` flow through as a `TEXT_CHUNK` with no new event types or engine plumbing. `qwen3_config()` is already parameterised and `functools.cache`d, so a keyword controlling whether that entry is installed is a small change.

The real question is not how to emit the tag, but how the tool adapter learns it is unpaired — `Parser.__init__` (`vllm/parser/abstract_parser.py:127-128`) constructs the tool parser as `tool_parser_cls(tokenizer, tools)`, with no channel for that fact, and widening that signature would touch every non-engine tool parser.

### Directions

I would rather have a maintainer's call before writing a patch, since these differ in user-visible behaviour:

1. **Pass `</think>` through as content when unpaired.** Restores the pre-unification behaviour (under the split parsers the tag survived by construction), so `content` is exactly the raw model output and clients can split on it as before. Needs a way to plumb "no reasoning consumer attached" — either a keyword threaded from `ParserManager.get_parser`, which already special-cases parser selection, or a post-construction hook on `ParserEngineToolAdapter`. My preference: smallest behavioural delta, and it fixes the silent data loss rather than describing it.
2. **Auto-pair the matching reasoning adapter** when an engine-based tool parser owns reasoning terminals and no reasoning parser was requested. `make_adapters` already builds both from one engine class, so the wiring exists. Produces the properly split output, but silently starts populating `reasoning_content` for users who never asked for it — and changes `finish_reason`/`include_reasoning` behaviour for existing deployments.
3. **Warn at startup and change nothing.** Cheapest and safest, but leaves the data loss in place.

Happy to take this one if a direction is acceptable. I checked and I do not see another PR open against this issue.

*AI assistance was used to trace the mechanism above; I have reviewed the findings and the cited code myself.*


### tic-top · 2026-08-11

@Shreyas-jk thank you — that trace is exactly right, and it corrects my own reading of the mechanism. I had blamed `(REASONING, THINK_END)`; the transition actually responsible is the `(CONTENT, THINK_END)` absorb rule, reached because the tool adapter always starts the engine in `CONTENT`. The "correct for the configuration it was written for, with no way to tell the two configurations apart" framing is the clearest statement of the bug so far.

I confirmed both of your claims on a served install (v0.26.0, `--tool-call-parser qwen3_xml`, no reasoning parser, `Qwen/Qwen3.6-27B`-family weights), rather than only by static analysis:

**Deleting `(CONTENT, THINK_END)` is sufficient, and the fallthrough at `streaming_parser_engine.py:322` does emit the literal tag as you predicted.** CPU-only probe first, stock vs. that one entry removed, on a generation with a tool call following `</think>`:

| | `</think>` in `content` | tool calls |
|---|---|---|
| stock, non-streaming | ✗ | 1 call |
| stock, streaming | ✗ | 4 deltas |
| entry removed, non-streaming | ✓ | 1 call |
| entry removed, streaming | ✓ | 4 deltas |

Then on the live server, patched via a wrapper that swaps `vllm.parser.qwen3.qwen3_config` for one returning `dataclasses.replace(config, transitions=<without that key>)`:

```
* non-streaming: `</think>` present in 2/2 replies that finished with `stop` (the third hit `max_tokens` mid-deliberation, so the model never emitted the tag — not a parser effect)
* streaming, with a tool defined: `</think>` present in the reassembled content **and** 5 `tool_calls` deltas — so restoring the tag costs nothing on the tool path
```

No engine changes, no new event types, exactly as you said.

**For what a user's vote is worth, direction 1.** Direction 2 is the one that would hurt us: we deliberately run without a reasoning parser so the thinking stays inline in `content`, because the agent CLIs we drive replay `content` and drop the non-standard `reasoning_content` — auto-pairing would silently stop the agents from seeing their own prior deliberation, which changes their behaviour, not just our metrics. Direction 1 restores the pre-unification output byte-for-byte and lets clients decide. Direction 3 leaves people to discover the loss the way we did: a whole comparison series with thinking silently unlabelled.

Please do take it — and I would rather leave this issue **open** until a maintainer picks a direction, since what we are running is a local monkeypatch, not a fix. Happy to close it the moment a PR lands, or sooner if the maintainers prefer to track the decision somewhere else.


### Shreyas-jk · 2026-09-02

#47562 (merged 2026-09-01) appears to fix this, though it does not reference this
issue. It was fixing #47137, and the `skip_reasoning_parsing` bypass added in its
review rounds 3 and 4 resolves this as a side effect.

I verified on unpatched `origin/main`.

## Method

Every probe was run twice: once at the default, and once with
`skip_reasoning_parsing` forced to `False`, which reproduces pre-#47562 behavior.
Without that control, a surviving delimiter proves nothing.

```
qwen3_xml  skip_reasoning_parsing=True   'work it out: 17*23</think>the answer is 391'
qwen3_xml  skip_reasoning_parsing=False  'work it out: 17*23the answer is 391'
```

The control absorbs, so the probe can fail.

Parsers were built through the shipped path,
`ParserManager.get_parser(tool_parser_name=..., enable_auto_tools=True)`, and the
flag confirmed to engage only when unpaired:

```
UNPAIRED (tool only)     reasoning_parser=NoneType                     skip_reasoning_parsing=True
PAIRED (tool+reasoning)  reasoning_parser=Qwen3ParserReasoningAdapter  skip_reasoning_parsing=False
```

## Result for this issue

`qwen3_xml`, unpaired, unpatched main. Non-streaming and streaming identical:

```
input:   work it out: 17*23</think>the answer is 391
content: work it out: 17*23</think>the answer is 391
```

The delimiter survives.

Worth noting that `qwen3_xml` and `qwen3_coder` are the same object, not merely
equivalent. Both registry entries in `vllm/tool_parsers/__init__.py:177-184` name
`('qwen3_engine_tool_parser', 'Qwen3EngineToolParser')`, and class identity, config
identity (`qwen3_config` is `functools.cache`d) and transition-table equality all
hold. #47562's tests used `qwen3_coder`, so they were exercising this parser.

## Wider sweep

#47562's think-markup tests feed a balanced `<think>...</think>` block. This issue
reports the unbalanced case, a stray closer in CONTENT with no opener, which takes a
different transition. So I ran that shape against every config carrying a
`(CONTENT, "THINK_END")` transition:

```
delimiter kept:      qwen3_xml, seed_oss, glm47_moe, ling3, deepseek_v4,
                     kimi_k2, minimax_m2, mistral
delimiter absorbed:  inkling
not applicable:      nemotron_v3
```

`inkling` is deliberate rather than an oversight. Its `<|end_message|>` also fires
from `TOOL_ARGS`, so it fails #47562's purity check and the bypass stays inert for
that config by design.

`nemotron_v3` is registered only as a reasoning parser, so there is no unpaired
tool-parser configuration for it and this shape cannot occur.

`gemma4` keeps the delimiter but shows a separate defect in its content that is
unrelated to this issue. I am filing that separately.

I also ran `tests/parser/mistral` (166 passed, `mistral_common` 1.11.7), which
#47562's test plan skipped because of a local version skew, so mistral is covered
here.

From my side this looks resolved and can be closed. My PR #51858 targeted this and
is now redundant; I am closing it in favor of #47562.


### Shreyas-jk · 2026-09-16

Correction: I retracted this on #51858 on a wrong premise and have corrected that there. The gemma4 defect is real and filed as #57231.


### pstjohn · 2026-09-23

I can confirm the same underlying parser transition also causes data loss when a model correctly quotes `</think>` as literal text in its final answer.

My server uses both:

```text
--reasoning-parser mimo
--tool-call-parser mimo
```

(mimo resolves to the shared Qwen3 parser engine.)
Minimal deterministic parser repro:
```python
raw = "<think>private rationale</think> Visible code: `<think>literal</think>`."
reasoning, content = parser.extract_reasoning(raw, None)
```
Expected:
```
reasoning: "private rationale"
content:   " Visible code: `<think>literal</think>`."
```
Actual:
```
reasoning: "private rationale"
content:   " Visible code: `<think>literal`."
```
The closing literal tag is silently consumed by:
```python
(ParserState.CONTENT, "THINK_END"): Transition(
    ParserState.CONTENT,
    (),
)
```
This is not malformed model output: the model deliberately puts both tags inside a Markdown code span. The parser treats every `</think>` encountered after transitioning to `CONTENT` as a duplicate structural terminator, without any way to distinguish a duplicate delimiter from quoted/documented literal text.

The same loss occurs in the MiMo/Qwen3 tool-parser pass, so a reasoning-parser-only fix is insufficient when auto tool choice is enabled.

This affects coding-agent transcripts: documentation or code examples involving literal `<think>...</think>` arrive at the client with broken formatting and unrecoverable missing bytes.

A fix should preserve `THINK_END` once the parser is in `CONTENT`, or otherwise only absorb a duplicate closer when it can be established as structural rather than ordinary content. The current behavior is lossy even with a reasoning parser configured.

### Shreyas-jk · 2026-09-24

@pstjohn I reproduced this, and the absorb is config-dependent in a way worth pinning down before anyone patches it. Line refs @ `84030bbe3d`.

**Reproduced, and it isn't mimo-specific.** Same input through `ParserManager.get_parser` in two configs (mock tokenizer, no GPU):

```
A. --reasoning-parser mimo --tool-call-parser mimo
   content: ' Visible code: `<think>literal`.'          <- closer lost, as you reported
B. --tool-call-parser mimo (no reasoning parser)
   content: '<think>private rationale</think> Visible code: `<think>literal</think>`.'
```

seed_oss behaves identically with its own markers (lost in the reasoning+tool config, preserved in tool-only), so this is not one parser's table: the same `(CONTENT, "THINK_END") -> ()` entry is in qwen3 (`qwen3.py:135`, inherited by seed_oss), `deepseek_v4.py:166`, `glm47_moe.py:114`, `kimi_k2.py:72`, `minimax_m2.py:128`, `mistral.py:180`, and gemma4's marker analogue at `gemma4.py:374`. What differs is whether reasoning parsing is active at all: with a tool parser alone, `DelegatingParser.__init__` sets `skip_reasoning_parsing` and the markers pass through as ordinary content.

**The transition is original to the engine, not a later bug fix.** It arrived with `c4a3f9d137` (#45413, the engine's first commit), so there's no narrower "this broke X" case a fix has to preserve. It is pinned by two tests: `tests/parser/engine/test_qwen3_reasoning.py:157` (`test_duplicate_think_end_absorbed`) and `:519` (streaming).

**The part I think makes this hard.** That pinned test's input is

```
"Reasoning here.</think>Content here.</think>More content."
```

which is the same shape as yours: a closer in `CONTENT`, after content text, with more text after it. The two differ only in intent, not in anything the state machine can see. So "preserve `THINK_END` once in `CONTENT`" and "keep absorbing duplicates" are the same decision with opposite defaults, and your own fallback (absorb only when structural) needs a signal the parser doesn't have at that layer. Token ids don't supply it either, since a model emitting a literal `</think>` inside a code span emits the same special-token id as a structural one; knowing it's inside a Markdown span would require content-aware scanning the engine deliberately doesn't do.

That leaves, as far as I can see: flip the default and update those two tests, or make absorption a per-parser config flag with your case as the reason it exists. Both are maintainer calls about which failure is worse: lossy literals, or stray closers leaking to clients from models that do emit duplicates.

For what it's worth, the tool-parser-only half of this (markers surviving when no reasoning parser is configured) is pinned across 6 parsers in #55255, so whichever way this goes, the other config is covered.

The config matrix script is a single file with no GPU or weights needed; happy to post it if useful.


### Shreyas-jk · 2026-09-24

Correction to my previous comment, and a measurement that changes the options. I said token ids can't distinguish a literal closer from a structural one. That's wrong as stated: **the streaming path already distinguishes them, and the non-streaming path doesn't.**

Real `Qwen/Qwen3-0.6B` tokenizer, `--reasoning-parser qwen3 --tool-call-parser qwen3_xml`, same input as your repro, one token per delta (so `stream_interval=1`):

| literal `</think>` spelled as | non-streaming `extract_reasoning` | streaming |
|---|---|---|
| special id `[151668]` | absorbed | absorbed |
| ordinary ids `[522, 63671, 74, 29]` (`'</'`, `'thin'`, `'k'`, `'>'`) | absorbed | **preserved** |

Both spellings decode to exactly `</think>`; only the ids differ. So for identical bytes on the wire, streaming and non-streaming disagree, which looks like a bug independent of the design question in this issue.

Two caveats I want to be explicit about:

- I built the ordinary-token spelling by hand (`tok.encode("</thin") + tok.encode("k>")`). `tok.encode("</think>")` yields `[151668]` even with `split_special_tokens=True`, so I don't know whether a model ever samples the split spelling in practice. If it doesn't, the common case is still "same id either way", and the default question stands unchanged.
- I haven't pinned the mechanism. `_process_scanner_items` feeds `TextChunk` text to the lexer (`streaming_parser_engine.py:272-282`), and the lexer matches on decoded text (`incremental_lexer.py:211`), so on my reading the streaming case "should" absorb it too. It doesn't, and I'd rather report the measurement than guess why.

**Blast radius, for whoever decides the default.** The `(CONTENT, "THINK_END") -> ()` entry is in 7 parser configs: `qwen3.py:135` (inherited by `seed_oss`), `deepseek_v4.py:166`, `glm47_moe.py:114`, `kimi_k2.py:72`, `minimax_m2.py:128`, `mistral.py:180`, plus the marker analogue at `gemma4.py:374`. Flipping the default changes all of them and requires deliberately changing two tests: `tests/parser/engine/test_qwen3_reasoning.py:157` and `:519`. (For contrast, the tool-parser-only config, where these markers already pass through, is pinned for 6 parsers in #55255.)

@bbrowning you added this in `c4a3f9d137` with the comment "Absorb duplicate `</think>` — model may emit it after already transitioning to CONTENT". Two questions, since you have the context nobody else does: was that absorb written for a model you saw doing it, or as general defensiveness? And is the streaming/non-streaming difference above worth its own issue, separately from the default question here?

Repro script for the table is one file, real tokenizer, no GPU — happy to attach it or open it as a separate issue if the divergence is news.


### pstjohn · 2026-09-24

inlining the test for visibility
https://github.com/vllm-project/vllm/blob/e6dc16cebd57cabc55c1b3503ce16faaf6717d21/tests/parser/engine/test_qwen3_reasoning.py#L157-L162

This makes it quite hard to use vLLM when working with agent traces 😓 

### Shreyas-jk · 2026-09-24

Found the mechanism behind the streaming/non-streaming split I posted above, and it changes what a fix looks like. **The rule you're asking for already exists in the streaming engine.**

`streaming_parser_engine.py:345-355`:

```python
strict = self._token_id_terminal_names if self._ever_had_token_ids else None
for tok in tokens:
    if tok.terminal == CONTENT_TERMINAL or (strict and tok.terminal in strict):
        events.extend(self._on_content(tok.value, tok.token_count))
    else:
        events.extend(self._on_terminal(tok.terminal, tok.value, tok.token_count))
```

`_token_id_terminal_names` is the set of terminals that resolved to a real special-token id (`:148-150`). So once a stream has carried token ids, a marker that the *lexer* matched in decoded text — rather than arriving as its special id — is emitted as **content**, not treated as a terminal. That is exactly "absorb only when structural": if `</think>` has id 151668 and what arrived was `'</'`,`'thin'`,`'k'`,`'>'`, the engine already concludes it's ordinary text.

That's why my table came out asymmetric. It isn't an accident, and the `(CONTENT, THINK_END)` transition never runs for a text-spelled closer in streaming.

Non-streaming can't apply the rule: `extract_reasoning(self, model_output: str, request)` (`abstract_parser.py:415`) receives no ids at all, so `_ever_had_token_ids` is False, `strict` is None, and the text match goes to `_on_terminal` and is absorbed.

**So the decisive question is what your transcript actually contained, @pstjohn:** when the model wrote the literal `</think>` inside the code span, did it emit token id 151668, or ordinary text tokens?

```
- **Ordinary text tokens:** streaming already preserves your bytes, only the non-streaming path loses them, and the fix is to plumb `model_output_token_ids` into `extract_reasoning` and apply the same rule. No default flip, no test changes. (There's precedent for the signature: gemma4's non-streaming `parse()` already takes `model_output_token_ids`.)
- **Id 151668:** nothing at this layer can tell your literal from a genuine duplicate terminator, and it's the binary default call across the 7 configs I listed.
```

If you still have the request, `--return-token-ids` (or logging `delta_token_ids` around the code span) would settle it. If you're seeing the loss in streaming rather than in a non-streamed response, that by itself says it arrived as 151668.

@bbrowning worth knowing either way: the "structural vs literal" distinction is already encoded in the streaming path, so the two paths disagree on identical bytes. My earlier question about whether that deserves its own issue stands.

