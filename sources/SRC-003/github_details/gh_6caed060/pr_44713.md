# [PR #44713] [Rust Frontend] Add Granite tool and reasoning parsers

source: https://github.com/vllm-project/vllm/pull/44713
state: open | updated: 2026-09-25T01:44:56Z
labels: needs-rebase, stale, rust

## 正文

### Summary

Adds Rust-frontend parser support for the Granite model family (tracking: #44280), mirroring the existing Python parsers as three self-contained, additive modules:

```bash
- **Granite reasoning parser** (`granite`, covers 3.x and 4) — plain-text phrase delimiters (`Here is my thought process:` / `Here is my response:`), not special tokens.
- **Granite 3.0/3.1 tool parser** (`granite`) — optional `<|tool_call|>`/`<tool_call>` marker followed by a JSON array of `{"name","arguments"}` calls.
- **Granite 20B FC tool parser** (`granite-20b-fc`) — one `<function_call>` marker per call (repeated, no closing marker), each followed by a `{"name","arguments"}` JSON object.
```


### Notes

- **Reasoning is a custom parser** because Granite's markers are multi-word phrases rather than single vocab tokens, so `DelimitedReasoningParser` doesn't fit; it runs a small before-think → reasoning → content phase machine.
- `granite4` standalone parser split out to #45216

## 评论 (10)

### mergify[bot] · 2026-06-06

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @tahsintunan.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### willamhou · 2026-06-09

Hi @tahsintunan, your `granite4.rs` overlaps with my #43934 (open since 5/29). The other three additions here (`granite.rs` v3, `granite_20b_fc.rs`, reasoning parser) are genuinely new — no overlap.

Two notes for the deciding maintainer:

```
- Mine follows the shared `JsonToolCallParser` core convention used by Hermes/InternLM2/Mistral/Qwen3-XML (config only, no new state machine).
- Yours is functionally more complete: it handles `"arguments"` as a JSON string (the granite behavior test in `tests/tool_parsers/test_granite4_tool_parser.py:25`). Mine inherits the shared core's object-only limitation that `internlm2.rs` documents as a deferred gap. So your local workaround ships more Python behavior, at the cost of bypassing the shared core for granite4 specifically.
```

cc @BugenZhao @njhill — happy to defer to your call. If this version is preferred I'll close #43934. If mine is preferred (with string-args addressed as a follow-up core PR), splitting `granite4.rs` out of this PR would let the other parsers land independently.

### mergify[bot] · 2026-06-10

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @tahsintunan.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### tahsintunan · 2026-06-11

@willamhou you're right, and the overlap is on me. I reviewed tracking issue #44280, where #43934 wasn't linked, and skipped a proper search of open PRs. Sorry about that.

I've split the `granite4` parser into its own PR #45216 so the other three here can land independently, and so @BugenZhao can weigh the two approaches side by side: standalone (#45216) vs config-only (#43934). My read is that the escaped-string arguments are a Granite 4 special case, so I'd rather not let that leak into the shared core, which is why I went with a standalone parser. That's a design call, though, so I'm happy to go either way.


### mergify[bot] · 2026-06-12

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @tahsintunan.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### mergify[bot] · 2026-06-15

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @tahsintunan.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### tahsintunan · 2026-06-16

@BugenZhao PTAL when you get a chance. It keeps picking up conflicts as main moves, so would be good to land before the next drift. Thanks!

### tahsintunan · 2026-06-16

For v3, reusing the shared core isn't really possible. The payload is a JSON array of calls and the core has no array concept (only a delimiter between bare objects), plus the leading marker is optional and can be either form. So it needs its own grammar.

For 20b we could reuse it, except for one thing: it drops the text between and after tool calls (only the prefix is content), and the shared core does the opposite and emits that text. So 20b would come out wrong if we just reuse it as-is.

And it's not just 20b, that drop is the shared Python convention (hermes, internlm2, seed_oss, deepseek all do it). Which means the core already diverges from Python for hermes/internlm2 today, the hermes streaming test even bakes in normal_text "preface  suffix" where Python gives just "preface ".

So if this divergence is not intentional, I'd keep 20b standalone for now since it's the parity-correct one, and fix the core's content handling in a separate PR (which also fixes hermes/internlm2). Then 20b can move onto the shared core on top of that. wdyt @BugenZhao?

### mergify[bot] · 2026-06-25

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @tahsintunan.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork

### github-actions[bot] · 2026-09-25

This pull request has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days. Leave a comment if you feel this pull request should remain open. Thank you!

## Review (2)

### claude[bot] · 2026-06-06 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.

### BugenZhao · 2026-06-16 · COMMENTED

Thanks. Just wondering why don't we reuse the common JSON tool parser for `granite` and `granite_20b_fc`?
