# [PR #53967] [Bugfix][Model] Detect FP8_DYNAMIC quantization from llmcompressor recipe.yaml

source: https://github.com/vllm-project/vllm/pull/53967
state: open | updated: 2026-09-25T00:58:46Z
labels: bug, quantization

## 正文

## Purpose

Fixes #53950.

Granite 4.2 FP8 checkpoints (`ibm-granite/granite-4.2-30b-fp8`, also `granite-4.2-30b-nvfp4`) ship their quantization recipe in `recipe.yaml` (`default_modifiers.QuantizationModifier.scheme: FP8_DYNAMIC`) and carry no `quantization_config` in `config.json` and no `hf_quant_config.json`. The loader therefore never detects quantization, instantiates plain `RowParallelLinear` layers, and dies on the checkpoint's `weight_scale` parameters: `ValueError: There is no module or parameter named 'layers.0.mlp.down_proj.weight_scale' in GraniteModel`.

`vllm/transformers_utils/config.py` now falls back to `recipe.yaml` after the two existing config paths and maps the llmcompressor `FP8_DYNAMIC` scheme onto the compressed-tensors config dict the loader already understands (`naive-quantized`, `channel` weight strategy, `token` dynamic input activations, schema verified against a working `neuralmagic/...-FP8-dynamic` checkpoint). Unknown or shapeless recipes return `None`, so every other checkpoint keeps the exact previous behavior.

Not a duplicate: nothing open references #53950, and the loader had no recipe.yaml path at all (`recipe` only appears under `tools/recipes/`, a different runtime-tuning concept).

## Test Plan

New tests in `tests/transformers_utils/test_config.py`: the FP8_DYNAMIC mapping shape, unknown/shapeless recipes returning `None`, a local repo dir with `config.json` + `recipe.yaml` resolving to the compressed-tensors config, and `config.json`'s own `quantization_config` winning over the recipe fallback.

```
.venv/bin/python -m pytest tests/transformers_utils/test_config.py -q -k recipe
```

## Test Result

4/4 new tests pass. `tests/transformers_utils/` otherwise matches upstream/main (the three failures there are pre-existing environment issues: HF 401 on the llama3 tokenizer download and two dspark config failures, identical on a clean main checkout). `ruff check` and `ruff format` clean on both touched files. Not run: a full model load with the real 30B checkpoint (no GPU here); the config resolution is covered by the new tests.

AI assistance: this PR was prepared with help from Kimi K3 (investigation, patch, tests); I reviewed the diff and ran the tests above.


## 评论 (8)

### queuine · 2026-09-02

I tested the PR by putting its changed files over vLLM v0.28.0 (that I run within a docker container), and it works for me with both fp8 and nvfp4 quantized Granite 4.2 (30b and 8b versions).

### he-yufeng · 2026-09-02

@queuine Appreciate you taking it for a spin on real hardware, fp8 and nvfp4 on both Granite 4.2 sizes is exactly the coverage this needed. Thanks!


### he-yufeng · 2026-09-05

Still applies on current main: there is no `recipe.yaml` path anywhere in `vllm/transformers_utils` (a repo-wide search returns zero), so llmcompressor FP8_DYNAMIC recipes still misdetect. Since opening, this picked up two independent corroborations: LiRunGuo reproduced both failure paths on the issue (v0.24.0 and main@94d96e2), and queuine ran this patch on real hardware across fp8 and nvfp4 Granite 4.2 in both 30b and 8b sizes. The four new tests pass, and unknown or shapeless recipes safely return None. Would appreciate a review when someone has a moment.


### mergify[bot] · 2026-09-09

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @universeplayer.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


### he-yufeng · 2026-09-12

Rebased onto current main (7036aaafa2) to clear the mergify conflict. `tests/transformers_utils/test_config.py` collided with the upstream mrope test additions in two places: the import block (both `json` and `math` kept) and the test body (upstream's mrope cases kept, the recipe-yaml tests re-applied after them). `config.py` applied cleanly.

Verification on the rebased head: the full file runs 24/25 locally (`VLLM_TARGET_DEVICE=cpu`), the single failure being `test_get_llama3_eos_token`, which needs Hugging Face hub access to a gated meta-llama repo and is unrelated to this change. The 12 recipe/mrope tests specifically all pass.


### mergify[bot] · 2026-09-16

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @universeplayer.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


### he-yufeng · 2026-09-24

Re-verified against current main (310f15d354): `recipe.yaml`-only FP8_DYNAMIC checkpoints still get no quantization config there. Fresh runs on that main: the PR's four tests fail 3/4 without the fix (the unknown-recipe control passes), and with the diff applied on top, all four pass. No drift.

(This re-verification pass was run with AI assistance.)


### mergify[bot] · 2026-09-24

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @universeplayer.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


## Review (1)

### claude[bot] · 2026-08-27 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.
