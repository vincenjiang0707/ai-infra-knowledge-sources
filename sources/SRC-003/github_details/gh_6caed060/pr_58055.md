# [PR #58055] [CI] Wire the 17 root-level tests/test_*.py into misc.yaml

source: https://github.com/vllm-project/vllm/pull/58055
state: open | updated: 2026-09-25T01:25:10Z
labels: ready, ci/build

## 正文

## Summary
```
- Follow-up to #54867 (test-tethering check): wires all 17 root-level `tests/test_*.py` files that were sitting on the tethering allowlist into `misc.yaml`'s `cpu-params-env-tokenizers-parser` job, matching its existing by-name pytest pattern.
- Since these tests hadn't run in CI, running them locally surfaced two real, pre-existing bugs, now fixed:
  - `vllm/version.py::_prev_minor_version_was()` asserted `__version_tuple__[0] == 0`, but its return line already generalizes to any major version - the assert just blocked it. Removed; all parametrized test cases (including the `1.x` ones) now pass.
  - `test_zen_cpu_platform_detection.py`'s 3 `caplog`-based tests never saw log output because `vllm/logger.py` configures the `"vllm"` logger with `propagate=False`, so records never reach the root logger `caplog` listens on. Added a `caplog_vllm` fixture that attaches `caplog`'s handler directly to the `"vllm"` logger.
  - Also fixed `test_no_triton_fallback` leaking a fake `HAS_TRITON=False` `vllm.triton_utils` into `sys.modules` process-wide (unlike its sibling helper `_has_triton_for_backends`, it never restored the real modules it popped).
- `test_version_tuple`'s assertion was also widened from a hardcoded `len(...) in (3, 4, 5)` to `>= 3`, since `setuptools_scm` can legitimately produce a 6-element tuple (release + pre-release tag + dev-distance + git-hash all at once).
```

## Test plan
```
- All 17 files run individually and together: `pytest -v -s <files>` - 141 passed.
- Full expanded job's test set (17 new + 5 pre-existing) run together exactly as the CI job invokes them: 247 passed.
- `python tools/pre_commit/check_test_tethering.py --all` - exit 0 (all 17 now tethered, no new gaps).
- `pre-commit run --hook-stage manual --files <changed files>` - ruff, ruff-format, typos, check-buildkite-step-keys, check-test-tethering, mypy (3.10-3.13), check-spdx-header, and the rest all green.
- Not run: the real Docker CPU test image build (the PR's own `docker-build-cpu-image` + this job will exercise it).
```

AI assistance was used (Claude Code) to investigate the allowlisted tests, diagnose the two stale-test root causes, and wire the CI job. All changes reviewed and tested locally per AGENTS.md before opening.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 评论 (9)

### khluu · 2026-09-22

/ci run

### github-actions[bot] · 2026-09-22

❌ This PR is 1 commit behind upstream `main`. Your branch must contain every commit currently on upstream `main`. No new CI build was started. Merge or rebase onto the latest `main`, then rerun `/ci run`. To test this branch at your own risk, use `/ci run --allow-stale`.

<!-- vllm-ci-command:5771698179 -->

### wjabbour · 2026-09-25

/ci run

### github-actions[bot] · 2026-09-25

❌ @wjabbour, A reviewer with write access must run `/ci run`, approve the PR, or add the `ready` label first.

<!-- vllm-ci-command:5824678097 -->

### khluu · 2026-09-25

/ci run

### github-actions[bot] · 2026-09-25

✅ Triggered [Buildkite CI #91116](https://buildkite.com/vllm/ci/builds/91116) for commit `82603d39386a`.

### github-actions[bot] · 2026-09-25

✅ @wjabbour, CI is now available for this PR.

- `/ci run` starts upstream CI; `/amd-ci run` starts AMD CI only.
- Your branch must contain every commit currently on its upstream target branch. Merge or rebase onto the latest target branch, then rerun the command. Append `--allow-stale` to a run command to test an outdated branch at your own risk.
- `/ci retry` retries failed jobs in the CI build for the current PR head. If the current head has no CI build, it starts a new CI build for the current head containing only jobs that failed in the latest earlier CI build for this PR.
- `/amd-ci retry` retries failed jobs in AMD CI for the current PR head. Use `/amd-ci run` when the current head has no AMD CI build.
- `/ci cancel` cancels scheduled or running CI builds for this PR branch; `/amd-ci cancel` does the same for AMD CI only.

<!-- vllm-ci-authorized -->

### wjabbour · 2026-09-25

/ci retry

### github-actions[bot] · 2026-09-25

✅ The previous CI build is still running: https://buildkite.com/vllm/ci/builds/91116

## Review (1)

### claude[bot] · 2026-09-22 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.
