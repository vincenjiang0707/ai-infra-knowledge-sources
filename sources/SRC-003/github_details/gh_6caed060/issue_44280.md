# [Issue #44280] [Roadmap] Rust Frontend Feature Parity

source: https://github.com/vllm-project/vllm/issues/44280
state: open | updated: 2026-09-25T01:10:48Z
labels: rust

## 正文

## Motivation

The Rust frontend has landed in the vLLM repository as a drop-in alternative to the Python API server process. It is enabled through `VLLM_USE_RUST_FRONTEND=1` and uses the existing vLLM engine/core boundary.

The Rust frontend is still experimental and not yet feature-complete relative to the Python frontend. This issue tracks the roadmap for the remaining gaps in feature parity and provides community contributors with a place to find scoped work.

This roadmap prioritizes user value and the Rust frontend's architectural goals over blanket 1:1 parity with the Python frontend. In particular, we may skip:

- Features / endpoints / options that are not commonly used in production
- Options that mainly reflect Python frontend implementation details or Python-side workarounds
- Features whose Python implementation has accumulated enough complexity that redesigning or removing them may be better than porting them directly

**For items marked with 🧭:** Please start with a design discussion before implementation, covering the intended API/architecture, co-design with the engine if needed, and the testing plan.

Related context:

- Original RFC: https://github.com/vllm-project/vllm/issues/40846
- Integration PRs
  * https://github.com/vllm-project/vllm/pull/40848
  * https://github.com/vllm-project/vllm/pull/43283

## Current status

- [x] Rust code lives in the main vLLM repository under `rust/`
- [x] `vllm-rs` integrates with `vllm serve` as a Python-supervised frontend subprocess
- [x] Core `/v1/chat/completions` support (streaming & non-streaming)
- [x] Core `/v1/completions` support (streaming & non-streaming)
- [x] Tool calling and reasoning output support for several key model families
- [x] Commonly used OpenAI-compatible sampling options
- [x] Image-only multimodal chat path for registered image processor/model specs
- [x] Multi-engine internal load balancing with scheduler-stat-aware routing
- [x] Admin and operational routes: health, load, version, metrics, reset-cache, sleep/wake
- [x] Mock-engine and HTTP integration-test infrastructure for Rust frontend development

## Roadmap

### Distributed serving and large-scale control plane

```
- [x] External DP load-balancing mode: support the Python-style shape where each API server connects to one DP rank 🧭
- [x] Hybrid DP load-balancing mode: support per-node internal LB plus cross-node external LB 🧭
- [ ] Elastic EP support: implement `/scale_elastic_ep`, `/is_scaling_elastic_ep`, request draining, and scaling-state rejection behavior 🧭
```

### Additional APIs

- [ ] Anthropic Messages API: `/v1/messages` and `/v1/messages/count_tokens` 🧭
- [ ] Non-stateful Responses API: start with `POST /v1/responses` without requiring durable response storage 🧭
- [ ] Pooling / embedding APIs: `/v1/embeddings`, `/pooling`, `/classify`, `/score`, `/v1/score`, `/rerank`, `/v1/rerank`, `/v2/rerank` 🧭
- [ ] Speech-to-text APIs: `/v1/audio/transcriptions` and `/v1/audio/translations` 🧭
- [ ] Realtime API: WebSocket `/v1/realtime` 🧭
- [ ] Generative scoring API: `/generative_scoring`
- [x] Tokenization APIs: `/tokenize`, `/detokenize`, and `/tokenizer_info`
- [ ] Extend the Rust gRPC interface to cover embeddings and other non-generation outputs 🧭

### Request compatibility and validation

- [ ] Audit request parameter coverage and validation, making unsupported features fail clearly
- [x] `stream_options.continuous_usage_stats`
- [ ] `n > 1` for chat completions and completions 🧭
- [ ] Beam search via the existing `use_beam_search` request parameter, including `length_penalty`
- [ ] `truncate_prompt_tokens`
- [x] `thinking_token_budget`
- [x] `include_reasoning=false`
- [ ] `repetition_detection`
- [x] `echo` behavior for token-ID completions prompts
- [x] `max_tokens=0` prompt-only behavior
- [x] Request ID handling in requests and responses
- [ ] Tracing 🧭

### Production readiness

- [x] TLS / SSL support: `--ssl-keyfile`, `--ssl-certfile`, and related serving arguments
- [x] API key authentication: `--api-key` / `VLLM_API_KEY`
- [x] CORS middleware and CLI argument parity
- [ ] Reverse-proxy `--root-path` support
- [x] `/server_info` route parity
- [ ] Logging argument parity: max log length, output logging, delta logging, access-log exclusions, and log-level controls

### LoRA adapter support

- [x] Startup LoRA adapter loading through `--lora-modules`
- [x] Runtime adapter loading/unloading: `/v1/load_lora_adapter` and `/v1/unload_lora_adapter`
- [x] Multiple-adapter request routing and validation

### Multimodal support

- [x] `image_url` chat content path for the currently registered image model specs
- [ ] Image embedding inputs
- [ ] Audio inputs and audio embedding inputs 🧭
- [ ] Video inputs and video chunk inputs 🧭
- [ ] `mm_processor_kwargs` and `media_io_kwargs`
- [ ] Broader model-family coverage beyond the current image-only processor registry 🧭

### RL / admin / lifecycle APIs

- [x] `/pause`, `/resume`, and `/is_paused`
- [x] `/abort_requests`
- [ ] `/update_weights` and `/init_weight_transfer_engine`
- [x] `/get_world_size`

### Tool and reasoning parser parity

- [x] `parallel_tool_calls`
- [x] Tool-choice constraints: `required` and named function 🧭
- [ ] Tool-choice constraints: `allowed_tools` 🧭
- [ ] Add missing tool and reasoning parsers for currently unsupported Python parser names.
- [ ] Add model-family roundtrip fixtures for newly added parsers

Parser work should prioritize production use and key model families over 100% parity. Additionally, the Rust frontend has a redesigned tool and reasoning parser architecture for more efficient, robust, and maintainable parsing, so prefer adapting the current Rust design over line-by-line Python ports.

<details>
<summary>Full parser parity checklist</summary>

#### Tool parsers

- [ ] `apertus`
- [ ] `cohere_command3`
- [ ] `cohere_command4`
- [ ] `ernie45`
- [ ] `functiongemma`
- [ ] `gigachat3`
- [ ] `granite`
- [ ] `granite-20b-fc`
- [x] `granite4`
- [ ] `hunyuan_a13b`
- [x] `internlm`
- [ ] `jamba`
- [ ] `lfm2`
- [ ] `llama4_pythonic` 🧭
- [ ] `longcat`
- [ ] `mimo`
- [x] `minimax`
- [x] `minimax_m2`
- [x] `minimax_m3`
- [ ] `minicpm5`
- [ ] `olmo3`
- [x] `phi4_mini_json`
- [ ] `poolside_v1`
- [ ] `pythonic` 🧭
- [ ] `seed_oss`
- [ ] `step3`
- [ ] `step3p5`
- [ ] `xlam`

#### Reasoning parsers

- [ ] `cohere_command3`
- [ ] `cohere_command4`
- [ ] `ernie45`
- [ ] `granite`
- [ ] `holo2`
- [ ] `hunyuan_a13b`
- [ ] `hy_v3`
- [ ] `mimo`
- [ ] `minimax_m2_append_think`
- [ ] `mistral`
- [ ] `olmo3`
- [ ] `poolside_v1`
- [x] `seed_oss`
- [x] `step3p5`

</details>

## Testing and compatibility infrastructure

- [ ] Improve E2E coverage through real `vllm serve` with `VLLM_USE_RUST_FRONTEND=1` on CI
- [ ] Documentation for building, testing, and contributing to the Rust frontend


## 评论 (42)

### snailwei · 2026-06-04

not working with 0.22.0

```
(EngineCore pid=739) INFO 06-04 11:58:06 [vllm.py:977] Asynchronous scheduling is enabled.
(EngineCore pid=739) INFO 06-04 11:58:06 [kernel.py:270] Final IR op priority after setting platform defaults: IrOpPriorityConfig(rms_norm=['native'], fused_add_rms_norm=['native'])
ERROR 06-04 11:58:06 [utils.py:546] Exception occurred while running API servers: Process RustFrontend (PID: 740) died with exit code 2
ERROR 06-04 11:58:06 [utils.py:546] Traceback (most recent call last):
ERROR 06-04 11:58:06 [utils.py:546]   File "/usr/local/lib/python3.12/dist-packages/vllm/v1/utils.py", line 533, in wait_for_completion_or_failure
ERROR 06-04 11:58:06 [utils.py:546]     raise RuntimeError(
ERROR 06-04 11:58:06 [utils.py:546] RuntimeError: Process RustFrontend (PID: 740) died with exit code 2
(Worker_TP0 pid=940) INFO 06-04 11:58:07 [multiproc_executor.py:775] Parent process exited, terminating worker queues
(Worker_TP1 pid=941) INFO 06-04 11:58:07 [multiproc_executor.py:872] WorkerProc shutting down.
(Worker_TP0 pid=940) INFO 06-04 11:58:07 [multiproc_executor.py:872] WorkerProc shutting down.
Traceback (most recent call last):
  File "/usr/local/bin/vllm", line 10, in <module>
    sys.exit(main())
             ^^^^^^
  File "/usr/local/lib/python3.12/dist-packages/vllm/entrypoints/cli/main.py", line 92, in main
    args.dispatch_function(args)
  File "/usr/local/lib/python3.12/dist-packages/vllm/entrypoints/cli/serve.py", line 144, in cmd
    run_multi_api_server(args)
  File "/usr/local/lib/python3.12/dist-packages/vllm/entrypoints/cli/serve.py", line 359, in run_multi_api_server
    wait_for_completion_or_failure(
  File "/usr/local/lib/python3.12/dist-packages/vllm/v1/utils.py", line 533, in wait_for_completion_or_failure
    raise RuntimeError(
RuntimeError: Process RustFrontend (PID: 740) died with exit code 2
```

### willamhou · 2026-06-12

I'd like to pick up the `ernie45` **reasoning parser** from the checklist — it pairs with the ERNIE 4.5 tool parser PR #44310 (the tool parser intentionally leaves `</think>` / `<response>` boundaries to the reasoning parser, so this completes end-to-end support for the ERNIE 4.5 thinking models). Will follow the `DelimitedReasoningParser` shared-core pattern where possible.

### xzwgit · 2026-06-17

The Rust frontend doesn’t return max_model_len / context_length in the /v1/models response, unlike the Python frontend. Is there any timeline for adding this metadata field?

```json
curl -s http://127.0.0.1:8123/v1/models   | jq .
{
  "object": "list",
  "data": [
    {
      "id": "Qwen3.6-27B",
      "object": "model",
      "created": 0,
      "owned_by": "vllm-frontend-rs"
    }
  ]
}
```


### tahsintunan · 2026-06-17

> The Rust frontend doesn’t return max_model_len / context_length in the /v1/models response, unlike the Python frontend. Is there any timeline for adding this metadata field?

PR is up here: #45950 

### tahsintunan · 2026-06-17

Put up a design for the Tracing 🧭 item in RFC #44757. Would welcome feedback before implementing.

### willamhou · 2026-06-19

Claiming two unblocked items from this roadmap:

```json
- **`truncate_prompt_tokens` (+ `truncation_side`)** under *Request compatibility and validation*. Currently scaffolded but rejected with "not supported" in `rust/src/server/src/routes/openai/{chat_completions,completions}/validate.rs`. Will add the actual truncation in the prompt-encode path after `text/src/lib.rs:137-140`, wire the request field through, support the Python `-1` sentinel + `truncation_side` left/right semantics from `vllm/renderers/params.py`. Self-contained — no engine-side change.
- **Logging argument parity** (`max_log_len`, output / delta / access-log toggles, log-level controls) under *Production readiness*. Currently rejected as `Unsupported` in `rust/src/cmd/src/cli/unsupported.rs`. Plan to wire the flags through and respect them at the existing `enable_log_requests` log sites.
```

I will submit `truncate_prompt_tokens` first as a focused PR, then the logging-args work as a separate follow-up so each can be reviewed independently.

### jesco-absolut · 2026-06-22

Planning to implement the step3 Rust tool parser (roadmap #44280), following the existing winnow-based tool-parser conventions in rust/src/tool-parser. Will cover the <｜tool_calls_begin｜>…<｜tool_calls_end｜> wrapper with one or more <｜tool_call_begin｜>…<｜tool_sep｜><steptml:invoke …> calls, with expect-test snapshot fixtures for complete, parallel, streaming, text-interleaved, no-parameter, and malformed cases.

### jesco-absolut · 2026-06-22

```bash
Planning to work on Rust frontend `/generative_scoring` route parity from the roadmap.
Scope is frontend-only: request/response types, prompt construction, existing
`logprob_token_ids` generation path, and mock-engine route tests. I’ll avoid sampler,
speculative decoding, and backend changes covered by existing generative-scoring/logprob PRs.
```


### dreamer-89 · 2026-06-23

Claiming the pooling/embedding APIs item for the Rust frontend (`/v1/embeddings`, `/pooling`, `/classify`, `/score`, `/rerank`). I'll post a short design note on the request/response types and how they map to the engine's pooling path, then implement incrementally starting with `/v1/embeddings`. Anyone already working on this?


### yangyang-cs95 · 2026-06-26

I'm currently working on this issue and have already opened a https://github.com/vllm-project/vllm/pull/46684  to add `repetition_detection` support to the sampling params. 
Also,I'd like to work on adding `apertus` tool parser support to the Rust frontend. https://github.com/vllm-project/vllm/pull/46813.
If anyone is already working on this or has relevant context, please let me know so we can avoid duplicate efforts.
Thanks!

### Albab-Hasan · 2026-06-29

claiming phase 1 of #45154. `AtomicBool` scaling flag in `AppState`, rejection middleware for inference routes (same `TRACKED_HANDLERS` pattern as `track_server_load`), `is_scaling_elastic_ep`, and `scale_elastic_ep` with validation. phase 2 orchestration is out of scope.

### aaishwarymishra · 2026-07-01

claiming olmo3 reasoning parser

### yangyang-cs95 · 2026-07-02

claiming gigachat3 tool parse

### rabrooks · 2026-07-07

Claiming the hy_v3 reasoning parser from the parser parity checklist. 

### evacchi · 2026-07-08

claiming the mistral reasoning parser from the parser parity checklist 


### rabrooks · 2026-07-08

Update/Question on Reasoning parsers `hy_v3`:

I took a closer look at the released tencent/Hy3 model and noticed `tokenizer_config.json` sets `token_suffix: ":opensource"`. As a result, it only contains `<think:opensource>` / `</think:opensource>` . There are no plain `<think>` tokens.

That makes the suffix support added to the Python parsers ( #47192) effectively required for this model. Without it, I don't think the parser can resolve the delimiter token IDs.

**Plan:** add a default-None accessor to the vllm-tokenizer `Tokenizer` trait and have the HF backend populate it from `tokenizer_config.json` (which is already loaded for `added_tokens_decoder`). Alternatively, we could thread it through the existing chat-side tokenizer_config loading if that's the preferred approach.  I'll proceed with the trait approach unless there's a preference.

Also, this is my first PR to vLLM, so I appreciate all the feedback and guidance!

### michaelfeil · 2026-07-17

https://github.com/64bit/async-openai/issues/568 @BugenZhao I am curious how you handle divergence from popular crates that implement the openai-api, which is a subset of what vllm uses. e.g. vllm adds image support in tool messages of chat completion (makes sense, tools can be multi-modal). 

### TanNgocDo · 2026-07-20

Claiming **Realtime API: WebSocket `/v1/realtime`** 🧭 from this roadmap.

**Goal:** Rust frontend parity with Python (`vllm/entrypoints/speech_to_text/realtime/`).
Python + `docs/serving/online_serving/speech_to_text.md#realtime-api` are the spec.

**Engine boundary:** Streaming-input sessions already work in EngineCore
via `EngineCoreRequest.resumable` and scheduler `WAITING_FOR_STREAMING_REQ` handling.
The ZMQ schema in `engine-core-client` already has `resumable`. I don't expect new
engine protocol fields for the generic resumable-segment pattern.

**Rust gaps:** `vllm-llm` is one-shot only — always sends `resumable: false`
and has no equivalent of Python `AsyncLLM._add_streaming_input_request()` (multi-segment
loop, shared request id, final sentinel, merged DELTA output stream). Realtime also needs
model-specific `buffer_realtime_audio` segmentation (fixed-window for Qwen3-ASR,
sliding-window + token feedback for Voxtral), currently Python-only. Rust already handles
batch audio content parts for chat (`InputAudio`/`AudioUrl` → multimodal features), but
has no live streaming-buffer/feedback-loop pattern.

cc @BugenZhao 

### alvarobartt · 2026-07-21

Hey @BugenZhao, any ETA on Responses API i.e., `/v1/responses`, support? Thanks 🤗 

### umut-polat · 2026-07-24

Claiming the `poolside_v1` reasoning parser from the parser parity checklist.

Plan: wrap the shared delimited `<think>`/`</think>` state machine, but scope the prompt boundary scan to the current assistant turn by slicing the prompt token IDs at the last `<assistant>` token — mirroring the Python parser's `is_reasoning_end` override, which exists so stray `</think>` in conversation history or few-shot examples doesn't disable reasoning parsing for the whole response. Will include unit tests for the turn-scoping behavior.

### secbear · 2026-08-02

I’d like to claim the holo2 reasoning parser from the Rust frontend parity checklist. I verified that there is no existing claim or open Holo2 Rust PR.

```
Planned scope: add a semantic alias to the existing prompt-first DeepSeek R1 parser, register the holo2 name and Hcompany/Holo2-* model matcher, and add factory plus real Hcompany/Holo2-4B tokenizer/template roundtrip coverage for default, enabled, and disabled thinking. I don’t expect to add a new parsing state machine or make tool-parser changes. 
Please let me know if this overlaps with work already in progress.
```

### santho090 · 2026-08-09

I'd like to take the `tool_choice.allowed_tools` item.

The Rust frontend already parses and validates part of this option, but request conversion still rejects it as unsupported. The current Rust wire type is also flat, while the OpenAI Chat Completions shape nests `mode` and `tools` under `allowed_tools`:

```json
{
  "type": "allowed_tools",
  "allowed_tools": {
    "mode": "required",
    "tools": [
      {"type": "function", "function": {"name": "get_weather"}}
    ]
  }
}
```

Proposed scope:

- Match the nested Chat Completions wire shape and require the exact `allowed_tools` and `function` discriminators.
- Resolve the allowlist while preparing the chat request.
- Filter the declared tools by name while preserving their original order.
- Reuse the existing internal `ChatToolChoice::Auto` and `ChatToolChoice::Required` variants. This keeps the engine, renderer, and parser interfaces unchanged.
- Treat an empty `auto` allowlist as no available tools and reject an empty `required` allowlist because it cannot be satisfied.

I plan to add request-deserialization, validation, conversion, and mock HTTP coverage for both modes, subset filtering, invalid discriminators, missing tools, invalid mode, empty allowlists, unsupported reference types, and the existing tool-choice variants.

I searched open PRs for the roadmap issue number and for `allowed_tools` plus Rust frontend terms. I did not find an implementation for Rust Chat Completions. #45512 covers MCP enforcement in the Python Responses API, which is a separate path.

Please flag any concerns with this lowering before I open the implementation PR.


### 982945902 · 2026-08-17

I'd like to claim the `minimax_m2_append_think` reasoning parser from the Rust frontend parity checklist.

Planned scope:

- Add an exact-name Rust reasoning parser that prepends `<think>` once and preserves the complete model output as content, matching the existing Python parser semantics.
- Keep reasoning output empty, preserve `</think>` verbatim, and cover the intentional difference between empty complete output and an empty streaming response.
- Register the parser without adding an automatic model matcher, because MiniMax M2 supports both the normal reasoning parser and this explicit compatibility mode.
- Add parser lifecycle, chunk-boundary, factory, and MiniMax M2 model-family roundtrip coverage.

I checked the roadmap comments, open PRs referencing #44280, and searches for the parser name and did not find overlapping Rust work. I plan to submit this first as a focused PR.


### 982945902 · 2026-08-17

I'd also like to claim the `olmo3` tool parser after the MiniMax M2 parser PR is submitted.

Planned scope:

- Adapt Olmo3's `<function_calls>...</function_calls>` Python-style call format to the current incremental Rust parser architecture rather than porting the Python implementation line by line.
- Support parallel calls, nested literal values, JSON-style `true` / `false` / `null`, parameterless calls, arbitrary stream chunk boundaries, and ordered surrounding text.
- Fail safely on incomplete or unsupported expressions without panicking.
- Register the parser/model matcher and add model-family roundtrip fixtures alongside focused parser tests.

I checked the roadmap comments and open PR searches for Olmo3 Rust tool-parser work and found no overlap. I will keep this as a separate PR so review remains scoped.


### 982945902 · 2026-08-17

Progress update for the two claimed parser items:

- `minimax_m2_append_think` reasoning parser: #52574
- `olmo3` tool parser plus model-family roundtrip fixtures: #52579

Both PRs are intentionally still Draft pending the submitter's human line-by-line review. Focused Rust parser/chat tests, clippy, changed-file pre-commit hooks, and CPU full-model HTTP serving regressions have passed locally. Both are currently awaiting the first-time-contributor `verified`/`ready` label required for GitHub pre-commit CI to run.


### willamhou · 2026-08-17

I’d like to take a focused subtask under request compatibility: support the existing Python frontend `--enable-force-include-usage` flag in the Rust frontend.

Scope:
- promote the flag from `Unsupported` into the shared runtime/API server options;
- make streaming chat and completions include cumulative usage plus the final usage chunk even when request `stream_options` omit it, matching Python `should_include_usage`;
- add CLI and route regression tests.

I searched the roadmap comments and open PRs for `enable_force_include_usage`, `enable-force-include-usage`, and “force include usage” and found no overlapping work.

### LuisPard011 · 2026-08-17

Claiming the `hunyuan_a13b` reasoning parser from the parser parity checklist.

Plan: wrap the shared delimited state machine with `<think>\n` / `\n</think>\n<answer>\n` as the
delimiters, so the framing newlines are consumed with the boundary, and suppress the trailing
`\n</answer>` from content — no second state machine.

Same snag @rabrooks hit on `hy_v3`: Hunyuan's delimiters aren't vocab tokens (`<think>\n` tokenizes as
`<th` + `ink` + `>Ċ`), which is presumably why the Python parser hardcodes id sequences like
`[14023, 771, 397]`. `DelimitedReasoningParser::new` would fail on `token_to_id` here, so I'd make the
resolved IDs `Option<u32>` and add a constructor that skips resolution, leaving existing callers
untouched. Happy to go another way if you'd rather not touch `delimited.rs`. No automatic model
matcher either — a `hunyuan` substring would also catch HunyuanOCR and the dense 7B.

Checked this thread and the open PRs. #52133 is the Hunyuan A13B *tool* parser and touches only tool
files, so they should be complementary; I found nothing for the reasoning half.

### brianosaurus · 2026-08-17

I'd like to claim **"Audit request parameter coverage and validation, making unsupported features fail clearly"** from the request compatibility section.

Starting point: The chat request struct has no `deny_unknown_fields`, and serde's default is to ignore unknown keys, so parameters the Python frontend accepts are silently dropped rather than rejected. Absent from `rust/src/chat/src/request.rs` today:

`min_tokens`, `logit_bias`, `allowed_token_ids`, `include_stop_str_in_output`,
`truncate_prompt_tokens`, `repetition_detection`, `use_beam_search`,
`length_penalty`

A client sending `min_tokens` or `logit_bias` gets a 200 with the option discarded and no indication it was ignored, which seems like the opposite of the intent here.

Planned scope, deliberately narrow:

1. Enumerate the divergence properly rather than from my grep, comparing the Python request schema against the Rust struct, and record it in the PR.
2. Make unknown and known-but-unimplemented parameters fail with a clear error. 
3. Leave `TODO`s where a parameter should later be implemented rather than rejected.
4. Tests via the existing mock-engine HTTP harness.

Two things I would rather agree on before writing code, since this changes API-visible behaviour even though the item is not marked 🧭:

```
- Reject unknown fields outright, or allow-list and reject only the known unimplemented ones? Strict rejection is cleaner but will break clients that currently send ignored extras.
- Error code preference: 400 for all of them, or 501 for known-unimplemented versus 400 for genuinely unknown?
```

### LuisPard011 · 2026-08-17

```bash
> Claiming the `hunyuan_a13b` reasoning parser from the parser parity checklist.
> 
> Plan: wrap the shared delimited state machine with `<think>\n` / `\n</think>\n<answer>\n` as the delimiters, so the framing newlines are consumed with the boundary, and suppress the trailing `\n</answer>` from content — no second state machine.
> 
> Same snag [@rabrooks](https://github.com/rabrooks) hit on `hy_v3`: Hunyuan's delimiters aren't vocab tokens (`<think>\n` tokenizes as `<th` + `ink` + `>Ċ`), which is presumably why the Python parser hardcodes id sequences like `[14023, 771, 397]`. `DelimitedReasoningParser::new` would fail on `token_to_id` here, so I'd make the resolved IDs `Option<u32>` and add a constructor that skips resolution, leaving existing callers untouched. Happy to go another way if you'd rather not touch `delimited.rs`. No automatic model matcher either — a `hunyuan` substring would also catch HunyuanOCR and the dense 7B.
> 
> Checked this thread and the open PRs. [#52133](https://github.com/vllm-project/vllm/pull/52133) is the Hunyuan A13B _tool_ parser and touches only tool files, so they should be complementary; I found nothing for the reasoning half.
```

Progress update on the `hunyuan_a13b` reasoning parser claim above: #52658.

### JonSnow1807 · 2026-08-18

I'd like to claim the `ernie45` tool parser and `ernie45` reasoning parser from the parser parity checklist. The earlier PRs for both (#44310, #45385) were closed on Aug 17 without landing, and I don't see another open PR or claim covering ERNIE 4.5, so the lane looks free again.

Planned scope (adapting the current Rust design rather than porting line by line):

```json
- **Reasoning parser**: wrap the shared delimited `<think>`/`</think>` state machine with prompt-first initialization (the ERNIE-4.5 thinking template prefills `<think>\n`), drop the framing newlines around `</think>` the way the Step3p5 parser does, and strip the `<response>` / `</response>` answer framing that the `ERNIE-4.5-21B-A3B-Thinking` template wraps around the final content — while keeping whatever follows `</response>` (e.g. tool calls) instead of truncating it.
- **Tool parser**: build on the existing marker-wrapped JSON tool-call core in `rust/src/parser/src/tool/json/` for `<tool_call>{"name": …, "arguments": …}</tool_call>` blocks — repeated calls, incremental argument streaming across arbitrary chunk boundaries, Hermes-style structural tag for strict/required tool choice. The tool parser stays out of the think/response framing so the split between the two parsers is clean.
- Register both under `ernie45` with an ERNIE-4.5 thinking model matcher, add focused parser tests, and add a `baidu/ERNIE-4.5-21B-A3B-Thinking` roundtrip fixture (its tokenizer and template are ungated).
```

Happy to adjust if there's a preference on where the `<response>` framing should live.


### JonSnow1807 · 2026-08-18

Update on the two ERNIE 4.5 items claimed above: PR #52841 adds both the `ernie45` tool parser and the `ernie45` reasoning parser, with a `baidu/ERNIE-4.5-21B-A3B-Thinking` roundtrip fixture.

### 982945902 · 2026-08-19

I would like to claim the `lfm2` tool parser item.

Planned scope:

- Implement the `<|tool_call_start|>[python_style_calls]<|tool_call_end|>` format in the current Rust `ToolParserEvent` architecture, without line-by-line porting of the Python parser.
- Support multiple calls, nested literal arguments, dotted callable names, arbitrary streaming chunk boundaries, surrounding content, and safe fallback for malformed/incomplete calls.
- Preserve the LFM2 behavior of suppressing an immediate echoed tool-call body after the wrapper while allowing legitimate trailing assistant text.
- Register the parser/model matcher and add focused parser tests plus a model-family roundtrip fixture where the public tokenizer/template permits it.

Duplicate-work check: I reviewed the roadmap comments and searched open PRs for `lfm2` / `LFM2 tool parser`; I found no existing claim or PR covering this Rust parser. This will be submitted separately from #52579.

### 982945902 · 2026-08-19

Implementation is now available for review in #52900.

### 982945902 · 2026-08-19

Claiming the `step3p5` Rust frontend tool parser from the parser parity checklist.

I checked the roadmap comments and open/all PRs for `step3p5`; I found the merged reasoning parser and Python-side Step3p5 fixes, but no Rust frontend tool parser implementation or existing claim. PR #46369 is scoped to the separate `step3` tool format, so I will not overlap with it.

Plan:

- adapt the Python `Step3p5ToolParser` protocol to the current Rust streaming parser engine rather than porting its compatibility wrapper line by line;
- cover complete and arbitrarily chunked streaming output, multiple calls, text/tool transitions, typed arguments, malformed/incomplete calls, and tool-call IDs;
- register `step3p5` in the Rust chat tool-parser factory with model-family routing ordered ahead of the broader `step3` pattern;
- add a Step3.5 model-family roundtrip fixture if the tokenizer/template path is available without gated artifacts.


### 982945902 · 2026-08-19

Implementation is now available for review in #52946.

### evacchi · 2026-08-21

https://github.com/vllm-project/vllm/pull/48013 rebased+force-pushed

### ZireaelK · 2026-08-26

I would like to claim the `ling3` tool parser and `ling3` reasoning parser for the Rust frontend.

**Planned scope:**
- Add `Ling3ToolParser` (adapting the GLM-4.7 XML tool calling format and implicit reasoning termination) and `Ling3ReasoningParser` (`<think>` / `</think>` state machine with default thinking enabled) in `rust/src/parser/`.
- Register `ling3` in `ToolParserFactory` and `ReasoningParserFactory` with model pattern matching for `ling-3` / `ling3` model families.
- Add comprehensive Rust unit tests in `vllm-parser` and integration tests in `vllm-chat`.

**Duplicate-work check:**
Checked the roadmap and open PRs for `ling3`; verified there is no existing claim or open PR.

### elizabetht · 2026-08-29

@BugenZhao are there any open issues in this roadmap work that I can help contribute? 

### blueberry808 · 2026-08-30

I'd like to claim the `mimo` tool parser for the Rust frontend.

```
Looked into the Python side first: there's no dedicated MiMo tool-call format ,
`--tool-call-parser mimo` is mapped directly to the existing Qwen3XMLToolParser. The Rust frontend already implements Qwen3XmlToolParser (registered as `qwen3_xml`), so this is primarily a registry change rather than a new parser implementation.
```

Planned scope:
- Register a `mimo` model-id pattern in ToolParserFactory
  (rust/src/chat/src/parser/tool/mod.rs) resolving to `QWEN3_XML`.
- Add roundtrip test fixtures for MiMo model IDs.

Duplicate-work check: no open PR touches the MiMo tool parser. #49578
(MiMo reasoning parser) is a separate, already-claimed item from another
contributor, it's not overlapping with this.

### evacchi · 2026-09-01

I think this was closed errorenously

### blueberry808 · 2026-09-09

I would like to claim `image_embeds` (Image embedding inputs) for the Rust frontend.

**Planned scope:**

```
- Add an `ImageEmbeds` chat content-part variant in `rust/src/chat/src/request.rs`, accepting the OpenAI-style `image_embeds` field as either a single base64-encoded tensor or a dict of named tensors (for models needing extra fields, e.g. Qwen2-VL's `image_grid_thw`, MiniCPM-V's `image_sizes`).
- Wire it through `rust/src/chat/src/multimodal.rs`'s media-extraction path into `MediaContentPart::ImageEmbeds`, sharing the image modality's placeholder token and per-prompt limit budget, matching Python's `modality.replace("_embeds", "")` behavior in `vllm/entrypoints/chat_utils.py`.
- Add a tensor-decode module handling both payload formats the Python client tooling produces: `.npy`-prefixed arrays and pickled `torch.save` tensors, converting into the existing `WireTensor` wire type.
- Add shape/hidden-size validation before the tensor reaches the engine, matching Python's `EmbeddingItems` validation in `vllm/multimodal/parse.py`.
- Add an `--enable-mm-embeds` flag gating this path, matching Python's `mm_config.enable_mm_embeds`.
- Add unit tests in `vllm-chat` for parsing, decoding, and validation, including a fixture-based round-trip test against a real Python-generated `torch.save` blob rather than a hand-constructed one.
```



### 0z5a · 2026-09-15

I’d like to take the MiniCPM5 Rust frontend support slice, including parser parity and regression coverage.
