# [Issue #50690] [Bug]: gpt-oss chat completions return 500 "Unexpected token 200002 while expecting start token 200006" when ignore_eos=true

source: https://github.com/vllm-project/vllm/issues/50690
state: open | updated: 2026-09-24T12:13:51Z
labels: 

## 正文

### Your current environment

```
- vLLM **0.26.0** and **0.18.0** (both reproduce), official `vllm/vllm-openai` images
- `openai-harmony` 0.0.8
- Model: `openai/gpt-oss-120b`, 1x H200, TP=1
- `vllm serve openai/gpt-oss-120b --max-model-len 32768 --tensor-parallel-size 1`
```

### 🐛 Describe the bug

A Chat Completions request with `ignore_eos: true` returns **HTTP 500** whenever the model finishes its Harmony turn before `max_completion_tokens` is reached:

```json
{"error":{"message":"Unexpected token 200002 while expecting start token 200006","type":"InternalServerError","param":null,"code":500}}
```

(200002 = `<|return|>`, 200006 = `<|start|>`)

**Minimal reproducer** — one request, no streaming:

```bash
curl -s http://localhost:8000/v1/chat/completions -H 'Content-Type: application/json' -d '{
  "model": "openai/gpt-oss-120b",
  "messages": [{"role": "user", "content": "Say hi."}],
  "max_completion_tokens": 512,
  "ignore_eos": true
}'
```

The prompt matters: the answer must be **shorter** than `max_completion_tokens`. A prompt whose answer fills the budget never reaches the terminator and succeeds, which makes this look intermittent.

| arm | 0.18.0 | 0.26.0 |
|---|---|---|
| short answer + `ignore_eos` | 500 | 500 |
| answer longer than `max_completion_tokens` + `ignore_eos` | OK | OK |
| short answer, no `ignore_eos` | OK | OK |

### Why

`<|return|>` / `<|call|>` are the Harmony assistant stop tokens. `ignore_eos=true` suppresses stopping on them (`SamplingParams.update_from_generation_config`, the `if not self.ignore_eos:` guards), so generation continues past the end of the turn. Those trailing tokens are then fed to the `StreamableParser`, which is a state machine: after `<|end|>` it expects `<|start|>`, gets `<|return|>`, and raises. The exception is not caught, so the whole request fails.

The parser half reproduces **without a server or GPU**:

```python
from openai_harmony import HarmonyEncodingName, Role, StreamableParser, load_harmony_encoding
enc = load_harmony_encoding(HarmonyEncodingName.HARMONY_GPT_OSS)
e = lambda s: enc.encode(s, allowed_special="all")
stream = e("<|channel|>") + e("final") + e("<|message|>") + e("answer") + e("<|end|>") + e("<|return|>")
p = StreamableParser(enc, role=Role.ASSISTANT)
for t in stream:
    p.process(t)
# HarmonyError: Unexpected token 200002 while expecting start token 200006
```

vLLM is on both sides of this: it permits `ignore_eos` to override its own Harmony stop tokens, then treats the resulting token stream as a parse error.

### Impact

`ignore_eos` is how benchmark tools pin output length so ISL/OSL combinations are controlled — both `vllm bench serve` (`--ignore-eos`) and GuideLLM use it. It is therefore hard to benchmark gpt-oss over Chat Completions at all. Measured with GuideLLM against 0.18.0 at ISL/OSL 1024/1024: **65% of requests failed** (971 errors, all this message). At OSL 128 the error rate was 0%, because the model rarely finishes a turn that early — so the failure rate scales with output length and can easily be mistaken for server saturation.

`/v1/completions` is unaffected (no Harmony parsing on that path).

### Related

```
- #41731 "[Bugfix] Respect ignore_eos for GPT-OSS Harmony stop tokens" — describes this exact cause, closed without merging (went stale on merge conflicts)
- #41950 "[Bugfix] Stop Harmony stream parsing after parser errors" — the parser-side guard, closed without merging
- #44009 "[Frontend] Clean up stop_token_ids override for Harmony" — merged, removed the `default_sampling_params` stop-token injection, but **does not fix this**: 0.26.0 still returns 500 (table above)
- #22519 — same parser error via the tool-call path
```

Either half would fix it: honor the Harmony stop tokens even under `ignore_eos` (#41731), or stop feeding a failed parser and fall back to raw tokens (#41950).

### Before submitting a new issue...

- [x] Made sure I searched the documentation and existing issues.


## 评论 (5)

### rebel-joonhyunglee · 2026-08-01

Client-side counterpart filed at vllm-project/guidellm#979 — GuideLLM sets `ignore_eos: true` unconditionally on chat completions, which is what trips this in practice (65% request failure rate benchmarking gpt-oss-120b at OSL 1024). Fixing either side unblocks gpt-oss benchmarking; fixing this one also stops the server from 500ing on a request it accepted.

### qhaas · 2026-08-13

Still an issue with GPT-OSS:120B and VLLM 0.27.1.  Setting `ignore_eos=false` clears it, but it seems to impact the number of output tokens generated in guidellm runs.

### wasnertobias · 2026-08-17

Seeing this on 0.27.1 with `gpt-oss-120b` without `ignore_eos`, so the client flag doesn't
look like the only trigger. Plain non-streaming chat completions through a gateway that never
sets it: 1 failure in 563 requests over ~90 minutes, same error string, same traceback
(`chat_completion_full_generator` → `HarmonyParser.parse` → `process_chunk`).

```bash
The model closed its final message with `<|end|>` and then sampled `<|return|>` instead of
`<|start|>`
```

```python
from openai_harmony import HarmonyEncodingName, Role, StreamableParser, load_harmony_encoding

enc = load_harmony_encoding(HarmonyEncodingName.HARMONY_GPT_OSS)
e = lambda s: enc.encode(s, allowed_special="all")

p = StreamableParser(enc, role=Role.ASSISTANT)
for t in e("<|channel|>final<|message|>Answer<|end|>") + e("<|return|>"):
    p.process(t)  # raises on the last token
```

What bothers me is that the answer is already fully parsed when it raises: `p.messages` holds
the complete final message, and `p.process_eos()` still works afterwards. So the 500 throws
away a correct response over one redundant token. Same in the other variants I tried (a second
`<|end|>`, plain text after `<|end|>`, a tail after the final `<|return|>`) — the rejected
token always lands after a completed message, so dropping it costs nothing.

`flush()` already catches `HarmonyError` and recovers; only the token loop in `process_chunk()`
doesn't. Happy to put up a small PR for that guard if nobody is on it.

### wuhangxian · 2026-09-10

I opened [#56216](https://github.com/vllm-project/vllm/pull/56216) as a focused fix for this issue.

How this PR differs from the previous related PRs:

- #41731 and #41950 were earlier Harmony-related attempts, but they were closed and did not land as the final fix for this issue.
- #44009 was merged and cleaned up the Harmony `stop_token_ids` override path, but this issue reports that the same 500 still reproduced on vLLM 0.26.0 after that change.
- #56216 does not rely on changing stop-token configuration. Instead, it handles the failure at the parser boundary in `HarmonyParser.process_chunk()`.

What #56216 fixes:

- Preserves a completed answer that was already parsed before the trailing `<|return|>` token.
- Prevents `HarmonyError` from escaping as HTTP 500 for this parser state.
- Stops consuming later chunks after the parser enters the failed state.
- Reuses the existing `flush()` recovery/reset path.

Before the fix:

`HarmonyError: Unexpected token 200002 while expecting start token 200006`

After #56216:

`parse_result: (None, 'Answer', None)`

The PR also adds regression coverage for both non-streaming parsing and streaming chunks after parser failure.


### errmakov · 2026-09-24

There are now five open PRs for this same crash (`<|return|>`/`<|call|>` arriving after `<|end|>`, which puts `StreamableParser` in `EXPECT_START`). Some of them don't link each other or this issue, so here they are in one place for whoever reviews:

| PR | Approach | State |
|---|---|---|
| #47350 | try/except around `process()` in `HarmonyParser.process_chunk`, `continue` past the bad token | reviewed by @yzong-rh and all comments addressed; needs rebase |
| #52055 | try/except in `process_chunk`, emit buffered tokens as raw final text, reset | mergeable, no maintainer review |
| #56216 | try/except in `process_chunk`, stop parsing after the first error | mergeable, no review |
| #43408 | wider tolerance for malformed Harmony streams | needs rebase |
| #37072 | `SafeStreamableParser` wrapper, targets the pre-refactor `harmony_utils.py` | stale, needs rebase |

Related issues: #46464, #51977.

Current `main` still reproduces it, and so does the latest `openai-harmony` (0.0.8), so the library hasn't fixed this:

```python
from openai_harmony import load_harmony_encoding, HarmonyEncodingName, StreamableParser, Role
enc = load_harmony_encoding(HarmonyEncodingName.HARMONY_GPT_OSS)
p = StreamableParser(enc, role=Role.ASSISTANT)
for t in enc.encode("<|channel|>final<|message|>Hi<|end|>", allowed_special="all"):
    p.process(t)
p.process(200002)  # HarmonyError: Unexpected token 200002 while expecting start token 200006
```

The direction @yzong-rh suggested on #46464 (catch `HarmonyError` in `process_chunk` and skip the offending token) is what #47350 implements. @aarnphm @chaunceyjiang @sfeng33 @bbrowning, could one of you pick a PR so the others can be closed?

(I used an AI assistant to put this summary together and ran the repro above myself.)

