# [Issue #42769] [Bug]: DeepSeek V4 load_weights UnboundLocalError: 'name_mapped' when expert mapping has no match

source: https://github.com/vllm-project/vllm/issues/42769
state: closed | updated: 2026-09-23T01:41:34Z
labels: stale

## 正文

### Summary

`vllm/model_executor/models/deepseek_v4.py` has an unbound-local bug in `DeepseekV4ForCausalLM.load_weights()`'s expert branch. The variable `name_mapped` is assigned **inside** a `for mapping in expert_mapping` loop, but referenced **after** the loop. When no entry in `expert_mapping` matches the incoming tensor name (all iterations hit the early `continue`), `name_mapped` is never bound — but the subsequent `loaded_params.add(name_mapped)` still runs:

```
File "vllm/model_executor/models/deepseek_v4.py", line 1557, in load_weights
    loaded_params.add(name_mapped)
UnboundLocalError: cannot access local variable 'name_mapped' where it is not associated with a value
```

### Environment

- vLLM: 0.21.0 (also reproducible on `main` as of 2026-05-15)
- Transformers: 4.57.1
- Hardware: 2× A100 80GB, TP=2
- Trigger: any DSV4 checkpoint whose expert weight tensor names don't match every entry in `get_expert_mapping()` (e.g., custom quantization-aware suffixes like `.tq_packed`).

### Affected code

`vllm/model_executor/models/deepseek_v4.py` around lines 1532-1558 (line numbers from main as of 2026-05-15, may drift slightly):

```python
                if ".experts." in name:
                    # E8M0 scales special handling...
                    if (
                        "weight_scale" in name
                        and loaded_weight.dtype == torch.float8_e8m0fnu
                    ):
                        loaded_weight = loaded_weight.view(torch.uint8)
                    for mapping in expert_mapping:
                        param_name, weight_name, expert_id, shard_id = mapping
                        if weight_name not in name:
                            continue                 # <-- early continue
                        name_mapped = name.replace(weight_name, param_name)
                        # ... weight loader call ...
                        if success:
                            name = name_mapped
                            break
                    loaded_params.add(name_mapped)   # <-- unbound if loop never set it
                    continue
```

If every iteration `continue`s on the `if weight_name not in name` guard, the `for` loop exits without `break` AND without ever assigning `name_mapped`. `loaded_params.add(name_mapped)` then raises `UnboundLocalError`.

### Repro

Easiest path: load any DSV4 checkpoint that includes an expert tensor name not present in `get_expert_mapping()`'s output. We hit it loading a TQ3-native variant where weight names have a `.tq_packed` suffix, but it would also fire on:
- Custom quantization schemes that decorate weight names
- Any checkpoint shape mismatch where `.experts.` is in the name but the suffix doesn't match the expected `gate_proj`/`up_proj`/`down_proj`/`w1`/`w2`/`w3` patterns

### Proposed fix

Initialize `name_mapped = None` before the loop, then short-circuit if it stayed unset:

```python
                if ".experts." in name:
                    if (
                        "weight_scale" in name
                        and loaded_weight.dtype == torch.float8_e8m0fnu
                    ):
                        loaded_weight = loaded_weight.view(torch.uint8)
                    name_mapped = None               # <-- ADDED
                    for mapping in expert_mapping:
                        param_name, weight_name, expert_id, shard_id = mapping
                        if weight_name not in name:
                            continue
                        name_mapped = name.replace(weight_name, param_name)
                        # ... weight loader call ...
                        if success:
                            name = name_mapped
                            break
                    if name_mapped is None:          # <-- ADDED
                        continue                     # <-- ADDED
                    loaded_params.add(name_mapped)
                    continue
```

Three-line change. Skips unrecognized expert tensors cleanly instead of raising. If a maintainer prefers raising explicitly for unrecognized expert names, the `continue` could be `raise ValueError(f"unrecognized expert tensor name: {name}")` — that's a stylistic choice.

### Workaround we're using

Patching the installed `deepseek_v4.py` at runtime via a setup script (`sed`-style replace with the diff above). Three-line surgical patch.

### Context

We've been validating a TQ3-native (3-bit weight quantization) version of DeepSeek-V4-Flash on vLLM. This is the second `deepseek_v4.py` bug we've hit during that work; the first was reported as [#42741](https://github.com/vllm-project/vllm/issues/42741) (`compress_ratios` attribute access vs the new transformers normalization). Happy to submit a PR with both fixes together if a maintainer wants.

---

## 评论 (9)

### varjoranta · 2026-05-16

Opened a suggested fix: #42835.

### konka0812 · 2026-05-17

请问您用a800跑起来了吗，因为我看DeepGEMM 要求 GPU 计算能力不低于 sm_90 (Hopper 架构，a100/a800好像不能跑呀

### varjoranta · 2026-05-17

Thanks for raising this.

You're right that the stock DeepSeek V4 FP4 / DeepGEMM path isn't usable on A100/A800. vLLM's `deep_gemm_mega_moe` path needs a newer GPU architecture than Ampere, so A100/A800 can't run that path.

This issue is separate from that hardware limit, though. The bug here is in the expert loader's name-mapping logic, and it can fail before any MoE kernel is even selected. That's what #42835 fixes.

For the TQ3-native path I'm working on, expert weights are stored TQ3-packed and dequantized into bf16 scratch buffers, then handed to the regular FusedMoE backend. So it should sidestep native MXFP4 DeepGEMM entirely, as long as `moe_backend=deep_gemm_mega_moe` isn't selected.

One honest caveat: so far I've only validated loading, not full generation, and not on A100 specifically. I'm out of GPU credit at the moment, so the real A100 generation smoke test is on hold until I can top up. I'll report back here once I've actually run it rather than claim it works before then.

---

谢谢你提出这个问题。

你说得对，原生的 DeepSeek V4 FP4 / DeepGEMM 路径在 A100/A800 上不可用。vLLM 的 `deep_gemm_mega_moe` 路径需要比 Ampere 更新的 GPU 架构，所以 A100/A800 跑不了那条路径。

不过这个 issue 和那个硬件限制是两回事。这里的 bug 出在 expert loader 的名称映射逻辑上，它可能在任何 MoE kernel 被选择之前就失败。这正是 #42835 要修的。

对于我在做的 TQ3-native 路径：专家权重以 TQ3 packed 形式存储，反量化到 bf16 scratch buffer，再交给普通的 FusedMoE backend。所以只要不选择 `moe_backend=deep_gemm_mega_moe`，它应该可以完全绕开原生 MXFP4 DeepGEMM。

老实说有一点要说明：到目前我只验证了加载，没有验证完整生成，也没有专门在 A100 上验证过。我现在 GPU 额度用完了，所以真正的 A100 generation smoke test 要等我充值之后才能做。等我真正跑过之后会回来这里报告，在那之前不会声称它能用。


### wuxiaohui0 · 2026-05-19

> 请问你用a800跑起来了吗，因为我看DeepGEMM要求GPU计算能力不低于sm_90（Hopper架构，a100/a800好像跑不了呀

Have you found a framework that can run on the A800, such as vllm, sglang, etc.?

### varjoranta · 2026-05-19

It's not really a framework question. The blocker is the native DeepSeek-V4 FP4 / DeepGEMM path, which needs Hopper-class (sm_90+) GPUs. That constraint follows the model's native path, not vLLM specifically, so swapping framework (sglang etc.) while running the same FP4 path hits the same wall on A800.

The way around it on A800 is a weight-only-quantized checkpoint that dequantizes to bf16 and runs through standard kernels, so DeepGEMM is never on the path. The TQ3-native path in my plugin (turboquant-plus-vllm) is built exactly that way and should run on Ampere in principle. Honest status, same as above: I have it loading, but I have not yet verified generation on A100/A800-class hardware, so I can't tell you it's confirmed-working on A800 yet.

If either of you has an A800 free, trying the TQ3-native path and reporting back would genuinely help. You'd be validating the exact case I can't run right now, and it would confirm the A800 route for everyone hitting this. Repo and usage: https://github.com/varjoranta/turboquant-vllm. Happy to help debug if you run into issues, and I'll still run the smoke test myself once I have the hardware.

中文：

这其实不是框架的问题。卡点在 DeepSeek-V4 原生的 FP4 / DeepGEMM 路径，它需要 Hopper 级（sm_90+）的 GPU。这个限制来自模型的原生路径，不是 vLLM 特有的，所以换框架（sglang 等）只要还走同样的 FP4 路径，在 A800 上会遇到同样的墙。

A800 上的绕过办法是用一个 weight-only 量化的 checkpoint，运行时反量化到 bf16，走标准 kernel，这样 DeepGEMM 完全不在路径上。我插件里的 TQ3-native 路径（turboquant-plus-vllm）正是这样设计的，原则上应该能在 Ampere 上跑。如实说明，和上面一致：我这边能加载，但还没有在 A100/A800 级硬件上验证生成，所以还不能说它在 A800 上确认可用。

如果你们谁有空闲的 A800，试一下 TQ3-native 路径并反馈结果会很有帮助。你们验证的正是我现在跑不了的那个场景，也能为所有遇到这个问题的人确认 A800 这条路。仓库和用法：https://github.com/varjoranta/turboquant-vllm 。遇到问题我很乐意帮忙调试；等我有硬件了我也会自己跑 smoke test。


### wuxiaohui0 · 2026-05-20

```bash
> It's not really a framework question. The blocker is the native DeepSeek-V4 FP4 / DeepGEMM path, which needs Hopper-class (sm_90+) GPUs. That constraint follows the model's native path, not vLLM specifically, so swapping framework (sglang etc.) while running the same FP4 path hits the same wall on A800.
> 
> The way around it on A800 is a weight-only-quantized checkpoint that dequantizes to bf16 and runs through standard kernels, so DeepGEMM is never on the path. The TQ3-native path in my plugin (turboquant-plus-vllm) is built exactly that way and should run on Ampere in principle. Honest status, same as above: I have it loading, but I have not yet verified generation on A100/A800-class hardware, so I can't tell you it's confirmed-working on A800 yet.
> 
> If either of you has an A800 free, trying the TQ3-native path and reporting back would genuinely help. You'd be validating the exact case I can't run right now, and it would confirm the A800 route for everyone hitting this. Repo and usage: https://github.com/varjoranta/turboquant-vllm. Happy to help debug if you run into issues, and I'll still run the smoke test myself once I have the hardware.
```

Does this mean I need to first perform offline dequantization of the model weights to BF16, and then start the VLLM server, or should I perform the quantization directly when the VLLM server starts?

### varjoranta · 2026-05-22

Neither, in the form you described. The plugin handles it automatically: you don't need an offline BF16 dequant step, and you don't pass a "quantize live at startup" flag either.

The simplest workflow:

```
1. Install the plugin: `pip install turboquant-plus-vllm`
2. Pull the pre-quantized native TQ3 checkpoint from HuggingFace: `varjosoft/DeepSeek-V4-Flash-TQ3-native` (~131 GB, already TQ3-packed).
3. Serve with vLLM as normal, pointing at that model. vLLM auto-loads the turboquant plugin via the `vllm.general_plugins` entry point. At load the plugin streams TQ3-packed weights into VRAM and dequantizes per-layer (and per-active-expert for MoE) into BF16 scratch buffers; standard FusedMoE BF16 kernels then run. DeepGEMM is never on the path.
```

One honest blocker before you try: current vLLM main still hits the UnboundLocalError from this issue, plus a separate `transformers>=4.57` `compress_ratios` crash. Fixes are pending in #43442 and #43443. Until they merge you'll need to apply them on top of vLLM main, e.g. `gh pr checkout 43442` then cherry-pick #43443 (both are one-file patches in `vllm/models/deepseek_v4/nvidia/model.py`).

After that you should be able to `vllm serve varjosoft/DeepSeek-V4-Flash-TQ3-native --tensor-parallel-size N` on 4× A800. I've validated load through this path but not yet confirmed end-to-end generation on Ampere. Your run would be the missing data point. If you hit issues, paste the trace here and I'll help debug.

中文：

按你描述的两种方式都不是。插件会自动处理：你不需要先离线把权重反量化到 BF16，也不需要在 vLLM 启动时传一个"实时量化"的 flag。

最简单的流程：

1. 安装插件：`pip install turboquant-plus-vllm`
2. 从 HuggingFace 拉取已经预先量化好的 native TQ3 checkpoint：`varjosoft/DeepSeek-V4-Flash-TQ3-native`（约 131 GB，已经是 TQ3 packed 形式）。
3. 用 vLLM 正常起服务，model 指向上面那个。vLLM 会通过 `vllm.general_plugins` 这个 entry point 自动加载 turboquant 插件。加载时插件会把 TQ3 packed 权重流式读入显存，按层（MoE 部分按当前激活的 expert）反量化到 BF16 scratch buffer，之后用标准的 FusedMoE BF16 kernel 跑。整条路径不经过 DeepGEMM。

试之前有一个老实要说明的卡点：当前 vLLM main 仍然会撞到本 issue 的 UnboundLocalError，加上一个 `transformers>=4.57` 的 `compress_ratios` 崩溃。修复正在 #43442 和 #43443 等合并。合并之前你需要先把它们打到 vLLM main 上，例如 `gh pr checkout 43442` 然后 cherry-pick #43443（两个都是 `vllm/models/deepseek_v4/nvidia/model.py` 单文件的小补丁）。

之后应该就能在 4× A800 上 `vllm serve varjosoft/DeepSeek-V4-Flash-TQ3-native --tensor-parallel-size N` 起来。这条路径我验证过加载，但还没有在 Ampere 上确认端到端生成。你这边跑就是缺失的数据点。遇到问题把 trace 贴过来我帮你看。


### github-actions[bot] · 2026-08-21

This issue has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days. Leave a comment if you feel this issue should remain open. Thank you!

### github-actions[bot] · 2026-09-23

This issue has been automatically closed due to inactivity. Please feel free to reopen if you feel it is still relevant. Thank you!
