# [PR #57169] [KV Cache] GLM-5.3-Flash: use the generic packed KV layout; CircularBufferSpec tail with speculative ring slots

source: https://github.com/vllm-project/vllm/pull/57169
state: open | updated: 2026-09-23T07:07:46Z
labels: rocm, speculative-decoding, kv-connector, nvidia, mrv2, dflash, kv-cache-manager, glm

## 正文

## Purpose

Pick #55219 to refactor the GLM-5.3-Flash KV layout.

This PR preserves @ivanium's original commits and adds three follow-ups:

- Propagate physical block strides to sparse-MLA/indexer metadata builders 
  in the model runner, MRv2, and speculative proposer paths, ensuring packed 
  blocks use their physical stride.
- Add regressions for packed kpool, sparse MLA, and DeepGEMM page addressing.
- Use the existing `block_stride_alignment` allocator contract and remove the
  duplicate `kernel_page_rows` plumbing and inference.

## Validation

Evaluated GLM-5.3-Flash on GB200 using the full 5-shot GSM8K dataset (1,319 questions, temperature 0).

- TP4 baseline: 0.9121
- NIXL P/D TP4 prefill + TP4 decode with CPU staging: 0.8916, 0.9121
- Mooncake P/D, TP4 prefill + TP4 decode after the packed-Mamba payload fix: 0.9136,

Co-Authored-By: Yifan Qiao <yifanqiao@inferact.ai>

## 评论 (10)

### mergify[bot] · 2026-09-16

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @andakai.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


### mergify[bot] · 2026-09-18

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @andakai.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


### andakai · 2026-09-22

@JaredforReal Thanks for the careful review! These help improved the changes. I've addressed all your comments. Could you please take another look when you have time?

### simon-veitner-redhat · 2026-09-22

i think we need some extra handling for AMD.

```python
# vllm/v1/attention/backends/mla/rocm_aiter_mla_sparse.py:360-362
def supported_kv_cache_layouts(cls):
    # Global index conversion assumes contiguous pages within each layer.
    return (KVCacheLayout.LBNHC, KVCacheLayout.LBHNC)
```

```python
# vllm/v1/attention/backends/utils.py:228-238
supported_layouts = set.intersection(*map(set, supported_layouts_lists))
...
if not candidates:
    raise ValueError("No KV cache layout satisfies every supported set: ...")
```

which will give empty set with the adjustment.
maybe someone with access to AMD machine can test this, unfortunately i don't have access to one.

to fix this we probably need to add similar handling for `BLHNC` as for FI

### simondanielsson · 2026-09-22

```bash
> i think we need some extra handling for AMD.
> 
> ```python
> # vllm/v1/attention/backends/mla/rocm_aiter_mla_sparse.py:360-362
> def supported_kv_cache_layouts(cls):
>     # Global index conversion assumes contiguous pages within each layer.
>     return (KVCacheLayout.LBNHC, KVCacheLayout.LBHNC)
> ```
> 
> ```python
> # vllm/v1/attention/backends/utils.py:228-238
> supported_layouts = set.intersection(*map(set, supported_layouts_lists))
> ...
> if not candidates:
>     raise ValueError("No KV cache layout satisfies every supported set: ...")
> ```
> 
> which will give empty set with the adjustment. maybe someone with access to AMD machine can test this, unfortunately i don't have access to one.
> 
> to fix this we probably need to add similar handling for `BLHNC` as for FI
```

Happy to test this on MI350 once it's ready

### JaredforReal · 2026-09-22

thanks~ @simon-veitner-redhat @simondanielsson 
I do think the refactor would break some kernel compatibility, amd as well as SM90 kernels introduced recently
We will fix it up

### simon-veitner-redhat · 2026-09-22

> thanks~ @simon-veitner-redhat @simondanielsson I do think the refactor would break some kernel compatibility, amd as well as SM90 kernels introduced recently We will fix it up

sounds good jared

### andakai · 2026-09-23

@JaredforReal @simon-veitner-redhat @simondanielsson  Thanks for your suggestions! I have fixed and tested both on AMD mi350x and H200. I added packed BLHNC stride handling for AMD and fixed packed manager block handling for SM90.

I validated with the full 1319-question, 5-shot GSM8K evaluation at concurrency 32.

- H200: TP4 0.9151, NIXL P/D 0.8893, Mooncake P/D 0.9174
- AMD MI350X: TP4 0.9143, NIXL P/D 0.9022, Mooncake P/D 0.8984

### simon-veitner-redhat · 2026-09-23

do we have e2e for prefill, decode and mixed aswell before/after?

### andakai · 2026-09-23

> do we have e2e for prefill, decode and mixed aswell before/after?

oh not yet. I will run the  before/after e2e tests.

Also thanks for the suggestion from Jared. I will cover all the sparse mla attention backends for SM90/100.

## Review (4)

### claude[bot] · 2026-09-21 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.

### JaredforReal · 2026-09-22 · COMMENTED

Overall LGTM, left some comment, PTAL, thanks~ @andakai 

### andakai · 2026-09-22 · COMMENTED

(no text)

### andakai · 2026-09-22 · COMMENTED

(no text)
