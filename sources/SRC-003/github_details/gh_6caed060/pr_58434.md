# [PR #58434] [Bugfix][MRV2] Treat padded prompt tails as spec-decode rows for hybrid models

source: https://github.com/vllm-project/vllm/pull/58434
state: closed | updated: 2026-09-25T01:29:54Z
labels: bug, ready, mrv2

## 正文

The scheduler pads a one-token prompt tail over prior state (e.g. a P/D decode-node arrival) with K placeholder drafts to keep the K+1 spec-decode shape (#45237). For recurrent-state layers these rows must run the spec-decode kernels, which keep the running state and roll the rejected placeholders back; the prefill kernels store only the state after all K+1 tokens (#55178).

#52988 changed the hybrid model state to classify spec-decode rows by request state (not prefilling) instead of num_scheduled_tokens, which adaptive verification rewrites. That sent every padded tail's draft count to -1, so Mamba's padded-tail handling no longer fires and GDN/KDA build the row as a prefill, folding the placeholder tokens into the request's recurrent state.

Also count prefilling rows with exactly one remaining prompt token and prior state as decodes, still using request state only.

## 评论 (8)

### njhill · 2026-09-23

/ci run

### github-actions[bot] · 2026-09-23

❌ This PR is 2 commits behind upstream `main`. Your branch must contain every commit currently on upstream `main`. No new CI build was started. Merge or rebase onto the latest `main`, then rerun `/ci run`. To test this branch at your own risk, use `/ci run --allow-stale`.

<!-- vllm-ci-command:5802167891 -->

### njhill · 2026-09-23

/ci run

### github-actions[bot] · 2026-09-23

✅ Triggered [Buildkite CI #90767](https://buildkite.com/vllm/ci/builds/90767) for commit `9da16bc5633c`.

### benchislett · 2026-09-24

Would it be cleaner to detect this case earlier and mark it as a decode? Is that feasible?

So that `is_prefilling` can be a source-of-truth here, instead of just a part of the equation

### njhill · 2026-09-24

> Would it be cleaner to detect this case earlier and mark it as a decode? Is that feasible?
> 
> So that `is_prefilling` can be a source-of-truth here, instead of just a part of the equation

@benchislett I think we can do something along those lines but would prefer to get this fix in first and address that as follow-on since it kind of overlaps with https://github.com/vllm-project/vllm/pull/58400.

### njhill · 2026-09-24

/ci run

### github-actions[bot] · 2026-09-24

✅ Triggered [Buildkite CI #91017](https://buildkite.com/vllm/ci/builds/91017) for commit `15753f3039d7`.

## Review (4)

### claude[bot] · 2026-09-23 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.

### TheEpicDolphin · 2026-09-23 · APPROVED

Nice catch! This change makes sense to me

### qiching · 2026-09-24 · APPROVED

@njhill Thanks, and sorry, my oversight. I missed padded prompt tails in #52988. LGTM now.

### ywang96 · 2026-09-25 · APPROVED

(no text)
