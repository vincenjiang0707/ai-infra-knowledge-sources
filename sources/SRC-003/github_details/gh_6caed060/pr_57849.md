# [PR #57849] [Frontend] Consolidate rlhf entrypoints.

source: https://github.com/vllm-project/vllm/pull/57849
state: open | updated: 2026-09-23T03:51:11Z
labels: documentation, frontend, ready, ci/build, rust

## 正文




## Purpose

Consolidate rlhf entrypoints.
cc @aoshen02


examples:
- examples/rl -> examples/rlhf
- examples/features/batch_invariance -> examples/rlhf/batch_invariance
- examples/features/pause_resume-> examples/rlhf/pause_resume

tests:
- tests/entrypoints/serve/dev/test_sleep.py -> tests/entrypoints/rlhf/state_transitions/test_sleep.py
- tests/entrypoints/serve/dev/rlhf/state_transitions/test_pause_resume.py -> tests/entrypoints/rlhf/state_transitions/test_pause_resume.py
- tests/entrypoints/weight_transfer/test_weight_transfer_llm.py -> tests/entrypoints/rlhf/weight_transfer/offline.py
- tests/entrypoints/openai/test_return_routed_experts.py -> tests/entrypoints/rlhf/return_routed_experts/test_openai_api.py
- tests/entrypoints/scale_out/token_in_token_out/test_return_routed_experts.py -> tests/entrypoints/rlhf/return_routed_experts/test_scale_out_api.py

entrypoints:
- vllm/entrypoints/serve/dev/rlhf/api_router.py -> vllm/entrypoints/rlhf/online/api_router.py
- RLHFOfflineMixin


part https://github.com/vllm-project/vllm/issues/55421

| Category | Existing file | touched |
|---|---|---|
| State transitions | `tests/entrypoints/serve/dev/test_sleep.py` | ✅️ |
| State transitions | `tests/entrypoints/serve/dev/rlhf/state_transitions/test_pause_resume.py` |  ✅️ |
| State transitions | `tests/v1/engine/test_async_llm.py` | ❌️ |
| State transitions | `tests/v1/distributed/test_async_llm_dp.py` | ❌️ |
| State transitions | `tests/v1/worker/test_sleep_mode_backend.py` | ❌️ |
| State transitions | `tests/basic_correctness/test_mem.py` | ❌️ |
| State transitions | `tests/models/language/generation/test_gdn_sleep_wake.py` | ❌️ |
| State transitions | `tests/entrypoints/weight_transfer/test_weight_transfer_llm.py` | ✅️ |
| State transitions | `tests/v1/worker/test_gpu_worker_weight_transfer.py` | ❌️ |
| State transitions | `tests/distributed/test_weight_transfer.py` | ❌️ |
| State transitions | `tests/model_executor/model_loader/test_reload.py` | ❌️ |
| Consistency | `tests/entrypoints/openai/test_return_routed_experts.py` | ✅️ |
| Consistency | `tests/entrypoints/scale_out/token_in_token_out/test_return_routed_experts.py` | ✅️ |
| Consistency | `tests/model_executor/test_routed_experts_capture.py` | ❌️ |
| Consistency | `tests/kernels/moe/test_routed_experts_capture_monolithic.py` | ❌️ |
| Consistency | `tests/v1/sample/test_logprobs.py`, `test_logprobs_e2e.py`, and `tests/v1/determinism/` | ❌️ |


## Test Plan

## Test Result

---
<details>
<summary> Essential Elements of an Effective PR Description Checklist </summary>

- [ ] The purpose of the PR, such as "Fix some issue (link existing issues this PR will resolve)".
- [ ] The test plan, such as providing test command.
- [ ] The test results, such as pasting the results comparison before and after, or e2e results
- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.
</details>



## 评论 (14)

### aoshen02 · 2026-09-21

Some agent comments, will take a look at this this week, thank you!
Request changes. Reviewed commit cbfa9e68d1; found three actionable CI regressions.
1. [P1] Duplicate Buildkite step key breaks pipeline setup. The new RLHF job uses entrypoints-unit-tests, already declared in .buildkite/test_areas/entrypoints.yaml. Both files are loaded by the same pipeline configuration, and their AMD mirrors also collide. The generator explicitly rejects duplicate keys during targeted selection. Give the RLHF job a unique key.
2. [P2] All five offline weight-transfer tests disappear from discovery. Renaming test_weight_transfer_llm.py to offline.py means the new directory-based pytest command skips it. There is no custom python_files setting to include this name. I reproduced zero tests collected, exit code 5, for the weight-transfer directory. Rename it to test_offline.py.
3. [P2] Existing hardware CI commands reference the deleted directory. .buildkite/intel_jobs/entrypoints_intel.yaml:29 still runs pytest ... entrypoints/weight_transfer; .buildkite/test-amd.yaml:1208 contains the same stale command. I reproduced “file or directory not found,” exit code 4. Update the commands and source dependencies alongside the move.
The seven extracted offline methods and all online handlers are AST-identical to the original implementations. Router registration retains the dev-mode guard. I found no additional runtime regression in those changes. Validation covered static comparison and isolated pytest discovery/path checks; GPU integration tests were not run.

### mergify[bot] · 2026-09-21

Documentation preview: https://vllm--57849.org.readthedocs.build/en/57849/

### noooop · 2026-09-21

@claude review

### noooop · 2026-09-21

@claude review

### noooop · 2026-09-21

@claude review

### noooop · 2026-09-21

@claude review

### noooop · 2026-09-21

/ci run

### github-actions[bot] · 2026-09-21

✅ Triggered [Buildkite CI #90194](https://buildkite.com/vllm/ci/builds/90194) for commit `eb0d7b8eefec`.

### noooop · 2026-09-21

@claude review

### noooop · 2026-09-21

/ci run

### github-actions[bot] · 2026-09-21

✅ Triggered [Buildkite CI #90206](https://buildkite.com/vllm/ci/builds/90206) for commit `257b4821aa2e`.

### noooop · 2026-09-21

/ci run

### github-actions[bot] · 2026-09-21

✅ Triggered [Buildkite CI #90211](https://buildkite.com/vllm/ci/builds/90211) for commit `740f039812ce`.

### mergify[bot] · 2026-09-23

This pull request has merge conflicts that must be resolved before it can be
merged. Please rebase the PR, @noooop.

https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork


## Review (6)

### claude[bot] · 2026-09-21 · COMMENTED

## Claude Code Review

This pull request is from a fork — automated review is disabled. A repository maintainer can comment `@claude review` to run a one-time review.

### claude[bot] · 2026-09-21 · COMMENTED

Findings marked 🟡 are optional suggestions and need no follow-up push.

**Additional findings** (outside the current diff — GitHub can't attach inline comments there):

- 🔴 `tests/entrypoints/rlhf/weight_transfer/offline.py` — Renaming this test file from `test_weight_transfer_llm.py` to `offline.py` (no `test_`/`_test` prefix) means pytest's default `python_files` pattern no longer collects it, so these weight-transfer offline tests silently stop running in CI. `.buildkite/test_areas/rlhf.yaml` runs `pytest -v -s entrypoints/rlhf`, which will exit successfully without ever executing this file's tests. Fix: rename the file back to `test_*.py` (e.g. `test_offline.py`) so it matches pytest's discovery pattern, or add `python_files` config covering `offline.py`.

  <details><summary>Extended reasoning...</summary>

  pyproject.toml's [tool.pytest.ini_options] has no python_files override, so pytest uses its default discovery pattern test_*.py / *_test.py. The file at tests/entrypoints/rlhf/weight_transfer/offline.py matches neither pattern (unlike its sibling test_openai_api.py, test_scale_out_api.py, test_pause_resume.py, test_sleep.py, which all keep the test_ prefix). .buildkite/test_areas/rlhf.yaml's new 'RLHF Entrypoints' job runs `pytest -v -s entrypoints/rlhf` from /vllm-workspace/tests; pytest recurses the directory but skips offline.py during collection, reporting success with zero tests run for that module. Any future regression in init_weight_transfer_engine/update_weights/finish_weight_update on the LLM class (moved verbatim into RLHFOfflineMixin) would go undetected by CI even though the test file still exists in the repo and looks like it's being run.

  **Verification:** normal (test-coverage regression). The diff renames tests/entrypoints/weight_transfer/test_weight_transfer_llm.py -> tests/entrypoints/rlhf/weight_transfer/offline.py (git diff: ".../test_weight_transfer_llm.py => rlhf/weight_transfer/offline.py"). offline.py still defines 5 real tests (offline.py lines 104,121,174,240,319: def test_get_world_size_tp1,… | normal (test-coverage regression the…

  </details>

<!-- bhrv:3000399324b55fa6 -->

### claude[bot] · 2026-09-21 · COMMENTED

Findings marked 🟡 are optional suggestions and need no follow-up push.

<!-- bhrv:f675f551e307259e -->

### claude[bot] · 2026-09-21 · COMMENTED

This automated review ran again on the current head; of the four issues raised in prior rounds, two appear fixed (the Intel BMG shell-quote break in `.buildkite/intel_jobs/entrypoints_intel.yaml` is now syntactically closed, and the batch-invariance link in `docs/usage/reproducibility.md` now correctly points back to `../features/batch_invariance.md`). Two remain unresolved even though their threads show as resolved: `.buildkite/test_areas/rust_frontend.yaml` and `.buildkite/test-amd.yaml` still drop `tests/entrypoints/openai/test_return_token_ids.py` from `source_file_dependencies` in favor of the unrelated routed-experts test path (the test itself still runs via `commands:`, but CI's file-based trigger for it is gone), and `.github/CODEOWNERS` still has no rule covering the new `vllm/entrypoints/rlhf` source directory (`offline.py`, `online/api_router.py`, `online/factories.py`) even though the old `/vllm/entrypoints/serve/dev/rlhf` rule was removed. A human should confirm these two before merge.


<details>
<summary>Extended reasoning...</summary>

This is a large (50-file) mechanical reorganization PR moving RLHF entrypoint code, tests, examples, docs, and CI config to new paths, with no new runtime logic beyond re-wiring imports and router registration. I re-ran the diff against the base commit and re-checked each of the five issues this bot posted on two earlier pushed versions of this PR, since three more commits (`a81b7c9`, `8fdf861`, `d21f043`, all "refine") landed since the last review and every thread shows "resolved by the PR author."

Verifying each thread against current code: (1) the Intel BMG entrypoints-unit-tests shell command is now correctly quote-terminated — fixed. (2) the reproducibility.md batch-invariance links now point to the correct, still-existing `../features/batch_invariance.md` — fixed. (3) the `clients.py` docstring nit now correctly says `vllm/entrypoints/rlhf/online/api_router.py` — fixed. (4) `rust_frontend.yaml`/`test-amd.yaml` still show the same diff as before: `source_file_dependencies` swaps `tests/entrypoints/openai/test_return_token_ids.py` for `tests/entrypoints/rlhf/return_routed_experts/test_openai_api.py`, even though the `commands:` section still runs `test_return_token_ids.py` directly — unfixed. (5) `.github/CODEOWNERS` still only adds a rule for `/tests/entrypoints/rlhf`, with no equivalent for the new `/vllm/entrypoints/rlhf` source tree — unfixed. `tools/pre_commit/mypy.py` also still drops `tests/entrypoints/weights_transfer` from `SILENT_GROUPS` with no replacement for the new path, unfixed, though that one was flagged as an optional nit originally.

No security-sensitive surface is touched (no auth, crypto, or permission logic); the risk is entirely CI-coverage regression and code-review routing (CODEOWNERS), not a runtime vulnerability. Given two previously-flagged, code-verifiable issues persist despite their threads being marked resolved by the author (self-resolution on one's own PR is not evidence of a fix), I am not approving. This is a partial-progress update focused only on what remains outstanding, per the guidance to avoid repeating fully-addressed points and to avoid staying fully silent when some but not all flagged issues persist unaddressed.

</details>

<!-- bhrv:56ce99cc0a8c3b08 -->

### claude[bot] · 2026-09-21 · COMMENTED

**Code review found no issues**

No high-confidence issues detected in this change.

<!-- bhrv:a189c181df7194d9 -->

### claude[bot] · 2026-09-21 · COMMENTED

Beyond the inline finding, I checked the flagged AMD coverage-loss concern (test-amd.yaml dropping `tests/entrypoints/weight_transfer` from the MI300 "Entrypoints Unit" step) — it's not an actual regression: `.buildkite/test_areas/rlhf.yaml` adds a new `pytest -v -s entrypoints/rlhf` job with a `mirror: amd:` block that runs the equivalent coverage on MI355 hardware via the new test-area generation path.


<details>
<summary>Extended reasoning...</summary>

This is a large, mostly-mechanical file-move/reorg PR (RLHF entrypoints consolidation) that has already been through four prior review rounds from this same bug-hunting system, with several inline findings from earlier runs resolved by the author. This run's inline finding (relative-import depth bug in tests/entrypoints/rlhf/return_routed_experts/test_openai_api.py:10, moved one package level deeper than the old tests/entrypoints/openai/test_return_routed_experts.py without updating the dot-count, so `from ...utils import RemoteOpenAIServer` now resolves to the non-existent `tests.entrypoints.utils` instead of `tests.utils`) is a genuine, verifiable collection-time ImportError, confirmed by manually walking Python's relative-import resolution against the actual package `__init__.py` layout and `tests/utils.py`'s location.

I independently investigated one of the two candidate items the harness listed as "ruled out this run" to see if it deserved separate mention: the claim that AMD MI300 loses all RLHF weight-transfer entrypoint test coverage because `.buildkite/test-amd.yaml`'s "Entrypoints Unit" step drops the `tests/entrypoints/weight_transfer` source dependency and its `pytest -v -s entrypoints/weight_transfer` command with nothing replacing it in that file. Tracing further, `.buildkite/test_areas/rlhf.yaml` (a new file in this diff) defines a dedicated RLHF entrypoints job that runs `pytest -v -s entrypoints/rlhf` (covering the new `tests/entrypoints/rlhf/weight_transfer/test_offline.py`) and includes a `mirror: amd:` block targeting MI355 hardware — so the coverage moved to a different generation mechanism rather than being dropped. This confirms the harness's disposition and is worth surfacing since it wasn't stated in the PR thread.

Given the real, confirmed inline finding (an actual CI-breaking bug) plus the harness's note that an additional verified finding exists but won't be posted, this PR should not be approved by an automated pass — a human should look at the import-path fix and confirm nothing else was missed in the mechanical rename.

</details>

This pull request has been reviewed before and this review found new issues. Where they share a root cause, one fix may close them together.

**Additional findings** (outside the current diff — GitHub can't attach inline comments there):

- 🔴 `tests/entrypoints/rlhf/return_routed_experts/test_openai_api.py:10` — Maintainers running this moved test get a collection-time ImportError instead of the routed-experts test executing. The file moved from tests/entrypoints/openai/ (3 levels deep) to tests/entrypoints/rlhf/return_routed_experts/ (4 levels deep) but kept the unchanged relative import `from ...utils import RemoteOpenAIServer`. Three dots now resolve to tests.entrypoints.utils, which does not exist (only tests/utils.py does), so pytest fails to collect this file. Fix: change the import to `from ....utils import RemoteOpenAIServer` (4 dots) or use the absolute `from tests.utils import RemoteOpenAIServer` form already used correctly in the sibling test_scale_out_api.py.

  <details><summary>Extended reasoning...</summary>

  Old path tests/entrypoints/openai/test_return_routed_experts.py is package tests.entrypoints.openai; 3 dots strip openai, entrypoints, land on tests, so `from ...utils import X` = tests.utils.RemoteOpenAIServer, which exists and works. New path tests/entrypoints/rlhf/return_routed_experts/test_openai_api.py is package tests.entrypoints.rlhf.return_routed_experts (one level deeper). Same 3 dots now strip return_routed_experts, rlhf, entrypoints, landing on tests.entrypoints, so the import becomes tests.entrypoints.utils. Confirmed via glob: tests/entrypoints/utils.py does not exist, only tests/utils.py does. pytest import/collection of this file raises ModuleNotFoundError: No module named 'tests.entrypoints.utils', so every test in this file (test_routed_experts) fails to even collect, silently dropping this coverage in any CI job that runs it (e.g. rlhf.yaml, rust_frontend.yaml, test-amd.yaml) with a collection error rather than a clear diff-review-visible failure.

  **Verification:** normal. The moved test tests/entrypoints/rlhf/return_routed_experts/test_openai_api.py:10 keeps `from ...utils import RemoteOpenAIServer`. All four package levels have __init__.py (tests/, tests/entrypoints/, tests/entrypoints/rlhf/, tests/entrypoints/rlhf/return_routed_experts/ all confirmed present), so the module's __package__ is tests.entrypoints.rlhf.return_routed_experts. Three dots strip…

  </details>

<!-- bhrv:c4332f224c40b6ce -->
