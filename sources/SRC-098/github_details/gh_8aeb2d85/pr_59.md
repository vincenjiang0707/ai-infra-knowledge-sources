# [PR #59] Fix bandwidth reporting in all-to-all benchmark

source: https://github.com/NVIDIA/nccl-tests/pull/59
state: closed | updated: 2020-12-08T18:36:21Z
labels: 

## 正文

Each of `nranks` source GPUs sends `count * typesize` bytes to each of `nranks` target GPUs, i.e. the total amount of data transmitted is `count * nranks * nranks * typesize` bytes.

## 评论 (1)

### sjeaugey · 2020-12-08

Can you elaborate on why we'd want to take into account the total amount of data sent/received (across all nranks) ? That would not be consistent with all the other operations (allreduce, broadcast, ...).
