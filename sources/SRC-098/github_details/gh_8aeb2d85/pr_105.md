# [PR #105] Support setting CUDA_VISIBLE_DEVICES env variable

source: https://github.com/NVIDIA/nccl-tests/pull/105
state: closed | updated: 2022-07-21T17:50:27Z
labels: 

## 正文

When devices are masked out by setting `CUDA_VISIBLE_DEVICES` we cannot assume that a unique GPU index is available per rank.  

While we obviously cannot share GPUs between ranks, there is another use case, exemplified by PyTorch XLA.  It will set up `CUDA_VISIBLE_DEVICES` such that each rank thinks it has a single GPU with index 0, but no ranks share a GPU.  See https://github.com/pytorch/xla/issues/1203#issuecomment-542693562 for details.

This patch takes `CUDA_VISIBLE_DEVICES` into account by fetching the count of available devices from CUDA.  PT XLA masking behavior can be emulated for nccl-tests by creating a wrapper that does `export CUDA_VISIBLE_DEVICES=$OMPI_COMM_WORLD_RANK` and launching it with with OpenMPI's `mpirun`.

## 评论 (4)

### AddyLaddy · 2022-05-26

Looks good to me. I have been using a similar patch myself. Any comments @sjeaugey ?


### sjeaugey · 2022-05-27

I think I've used another approach, with a `NCCL_TESTS_DEVICE` env var that just overrides the CUDA device to set. Using a wrapper script setting `NCCL_TESTS_DEVICE` to `OMPI_COMM_WORLD_RANK` works just as well, but the GPUs can still see each other.

### nzmsv · 2022-05-27

My goal was to test the case where NCCL *can't* see all GPUs though.  We had a regression in the AWS OFI plugin due to this, and masking off GPUs is a real use case (PT-XLA).

### sjeaugey · 2022-05-30

Ah, right, and in fact I've been using that mechanism to test the use of CUDA_VISIBLE_DEVICES as well, setting `CUDA_VISIBLE_DEVICES=X` and `NCCL_TESTS_DEVICE=0`.

But making the NCCL perf tests capable of running inside such an environment even for multiple GPUs per rank would be nice indeed.

## Review (3)

### sjeaugey · 2022-05-30 · COMMENTED

(no text)

### rashikakheria · 2022-07-01 · COMMENTED

(no text)

### sjeaugey · 2022-07-01 · COMMENTED

(no text)
