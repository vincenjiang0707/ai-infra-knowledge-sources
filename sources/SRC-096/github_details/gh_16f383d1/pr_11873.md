# [PR #11873] [DNM] UCT/IB/MLX5: wqe parser for uct am zcopy

source: https://github.com/openucx/ucx/pull/11873
state: open | updated: 2026-09-22T11:01:49Z
labels: 

## 正文

## What?
Add wqe parse support for uct am zcopy.

## Why?
FT need extract info from wqe before replay uct am zcopy.

## How?
Add parser for uct am zcopy.
Need fix conflicts after [PR#11836](https://github.com/openucx/ucx/pull/11836) and [PR#11915](https://github.com/openucx/ucx/pull/11915) merged.

## 评论 (40)

### svc-nvidia-pr-review · 2026-09-01

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 88e5bc5add0067b3deb63cb20b4c56690fe1acb2 -->

### svc-ucx · 2026-09-01

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `88e5bc5a`

**TL;DR:** The "Codestyle / commit title" check failed because one commit in PR #11873 is titled `GTEST/UCT/IB:  test am zcopy parser` with **two spaces** after the colon, which the title regex rejects; amend that commit message to use a single space.

<details><summary>Full analysis</summary>

**Summary:** Azure Pipelines job "UCX PR (Codestyle commit title)" build 133712 exited with code 1 because `codestyle_check_commit_title` flagged one of the PR's four commit titles as malformed.

**Root cause:** The check script iterates over the PR commits and validates each subject line against the regex `^[0-9A-Z/_-]+:\ [[:alnum:]_]` — i.e. an uppercase module prefix, a colon, **exactly one** space, then an alphanumeric/underscore character. Log output:

```
Good commit title: 'UCT/IB/MLX5: log level'
Good commit title: 'UCT/IB/MLX5: cleanup'
Bad  commit title: 'GTEST/UCT/IB:  test am zcopy parser'
Good commit title: 'UCT/IB/MLX5: am zcopy parser'
##[error]Bash exited with code '1'.
```

`GTEST/UCT/IB:` is a valid prefix, but there are two spaces after the colon, so the character following the single required space is another space rather than `[[:alnum:]_]`, and the match fails. This is a legitimate style violation in the PR's commit history, not a CI infrastructure problem — the other three commits from the same branch pass.

**Implicated commit:** The PR commit titled `GTEST/UCT/IB:  test am zcopy parser` (third of four on branch `failover_uct_rcx_am_zcopy`, PR head [REDACTED:Hex High Entropy String]). Its individual SHA is not printed in the log (the script only echoes titles). The check logic itself is unchanged and correct — nearest related infra commit is cfd9cd1f "BUILD/CI: Limit PR commit range checks (#11527)" by Yossi Itigin, which only narrowed the commit range.

**File:** buildlib/tools/codestyle.sh:14 (the validating regex); the defect is in the PR's commit message, not this file.

**Suggested fix:** Rewrite that commit's subject with a single space after the colon, e.g. `GTEST/UCT/IB: test am zcopy parser`. Since it is not the tip commit, use an interactive rebase and force-push:

```
git rebase -i HEAD~4        # mark 'GTEST/UCT/IB:  test am zcopy parser' as "reword"
# change title to: GTEST/UCT/IB: test am zcopy parser
git push --force-with-lease
```

No change to `buildlib/tools/codestyle.sh` is warranted. If maintainers want to be lenient about extra whitespace, the regex could be relaxed to `^[0-9A-Z/_-]+:[[:space:]]+[[:alnum:]_]`, but the current strictness appears intentional to keep titles uniform.

**Related:** PR https://github.com/openucx/ucx/pull/11873; commit-range scoping change https://github.com/openucx/ucx/pull/11527

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `85f770cf-bd82-4cad-bc56-c6f4fc932c78` in the triage console for the audit trail.

<!-- triage-agent request_id: 85f770cf-bd82-4cad-bc56-c6f4fc932c78 -->
<!-- triage-agent gha_run_id: azure:133712 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-04

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 3db1c4885fd7dafcc725fff868a96ccc5343eb6d -->

### svc-ucx · 2026-09-04

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `3db1c488`

**TL;DR:** The "commit title" codestyle check failed because one commit in PR #11873 has a double space after the colon — `GTEST/UCT/IB:  test am zcopy parser` — which fails the title regex; reword/amend that commit title to use a single space.

<details><summary>Full analysis</summary>

**Summary:** Azure Pipelines job "UCX PR (Codestyle commit title)" build 134323 exited with code 1 after reporting `Bad commit title: 'GTEST/UCT/IB:  test am zcopy parser'`.

**Root cause:** The check iterates over the PR's commits and validates each subject line against the regex `^[0-9A-Z/_-]+:\ [[:alnum:]_]` in `codestyle_check_commit_title()`. That pattern allows exactly one space after the `<MODULE>:` prefix and requires the next character to be alphanumeric or `_`. The commit title `GTEST/UCT/IB:  test am zcopy parser` contains **two** spaces after the colon, so the character following the single matched space is another space, the match fails, `err=1` is set, and the Bash task exits non-zero. All other six commits in the PR passed (`Good commit title: ...`), confirming this is a single malformed title rather than a tooling/infra problem. This is a real code-review issue in the PR branch, not a CI flake.

**Implicated commit:** The PR commit titled `GTEST/UCT/IB:  test am zcopy parser` on branch `failover_uct_rcx_am_zcopy` (PR head [REDACTED:Hex High Entropy String], merge commit e52088c1a). Exact SHA of the offending commit is not printed by the script — author is the PR author.

**File:** buildlib/tools/codestyle.sh:14 (the validating regex); the defect itself is in the commit message, not in tracked source.

**Suggested fix:** Rewrite the offending commit title to remove the extra space, e.g. `GTEST/UCT/IB: test am zcopy parser`, then force-push:

```
git rebase -i [REDACTED:Hex High Entropy String]   # mark that commit as 'reword'
# change title to: GTEST/UCT/IB: test am zcopy parser
git push --force-with-lease
```

Optionally, to make this class of failure more obvious, extend the script to print why a title was rejected (e.g. echo the expected format `^[0-9A-Z/_-]+: <alnum>` alongside the "Bad commit title" message) so contributors don't have to reverse-engineer the regex.

**Related:** https://github.com/openucx/ucx/pull/11873

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `ac569af1-88f3-4994-8b01-7c85b632ed04` in the triage console for the audit trail.

<!-- triage-agent request_id: ac569af1-88f3-4994-8b01-7c85b632ed04 -->
<!-- triage-agent gha_run_id: azure:134323 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-04


Test coverage gap: the PR adds `uct_rc_mlx5_op_info_fill` and helpers but includes no caller and no test exercising the AM zcopy op-info path. If a caller/test lands in a follow-up, that's worth stating in the PR description; otherwise the new code is entirely untested/dead in this PR.


### svc-nvidia-pr-review · 2026-09-04

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 85f2e7c286017ad03856bfed5bf49ef73c9b2b13 -->

### svc-ucx · 2026-09-04

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `85f2e7c2`

**TL;DR:** This is not a code/test failure — the "commit title" codestyle check rejected one commit in PR #11873 because its title `GTEST/UCT/IB:  test am zcopy parser` has a double space after the colon; reword that commit to use a single space and force-push.

<details><summary>Full analysis</summary>

**Summary:** Azure Pipelines job "UCX PR (Codestyle commit title)" build 134345 failed with `##[error]Bash exited with code '1'` after the commit-title linter printed `Bad commit title: 'GTEST/UCT/IB:  test am zcopy parser'`.

**Root cause:** The commit-title validation script iterates over every commit in the PR range and matches each subject against the UCX title convention (`<MODULE>[/<SUBMODULE>]: <single space><description>`). Every other commit in the branch passed:

```
Good commit title: 'UCT/IB/MLX5: address comments'
Good commit title: 'UCT/IB/MLX5: address comments'
Good commit title: 'GTEST/UCT/IB: rm overlap gtests'
Good commit title: 'UCT/IB/MLX5: cleanup'
Good commit title: 'UCT/IB/MLX5: log level'
Good commit title: 'UCT/IB/MLX5: cleanup'
Bad commit title: 'GTEST/UCT/IB:  test am zcopy parser'
Good commit title: 'UCT/IB/MLX5: am zcopy parser'
```

The only difference between the passing `'GTEST/UCT/IB: rm overlap gtests'` and the failing `'GTEST/UCT/IB:  test am zcopy parser'` is the extra space after the `:` separator, which breaks the expected `: ` + description pattern. The script exits non-zero on the first bad title, which surfaces as the Bash task failure. There is no build, compile, or test problem here — the checkout and the whole job take under 2 seconds of actual work (12:29:58.63 → 12:29:58.70), so this is a fast, deterministic lint rejection, not a hang or timeout.

**Implicated commit:** The offending commit is the one titled `GTEST/UCT/IB:  test am zcopy parser` in branch `failover_uct_rcx_am_zcopy` (second-oldest commit in the PR series; PR head at the time was `[REDACTED:Hex High Entropy String]`, tested as merge commit `[REDACTED:Hex High Entropy String]`). This is a PR-authored commit, not a regression from upstream.

**File:** No source file is at fault — the failure is in the git commit metadata of the PR branch. The check itself runs as the `commit title` Bash task in the Azure `Codestyle` stage (buildlib commit-title check; exact path not resolvable in this checkout, `read_source_file` returned "not found" for `buildlib/tools/commit_title.sh`, `buildlib/commit-format.sh`, `buildlib/az-check-commit-title.yml`, `buildlib/pr/az-check-commit-title.yml`, and `contrib/check_commit_title.sh`).

**Suggested fix:** Reword the single bad commit title to remove the duplicated space:

```bash
git rebase -i [REDACTED:Hex High Entropy String]
# mark the 'GTEST/UCT/IB:  test am zcopy parser' commit as 'reword'
# change the title to: GTEST/UCT/IB: test am zcopy parser
git push --force-with-lease origin failover_uct_rcx_am_zcopy
```

Alternatively, since this PR is marked `[WIP][DNM]` and has several `cleanup` / `address comments` fixup commits, squashing the series into one or two properly-titled commits will clear this check and tidy the history at the same time. No infrastructure change or retry is warranted — a rerun will fail identically until the title is fixed.

**Related:** [PR #11873 — \[WIP\]\[DNM\] UCT/IB/MLX5: wqe parser for uct am zcopy](https://github.com/openucx/ucx/pull/11873)

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `799f1b51-9dee-46e3-840c-d0f1c9737343` in the triage console for the audit trail.

<!-- triage-agent request_id: 799f1b51-9dee-46e3-840c-d0f1c9737343 -->
<!-- triage-agent gha_run_id: azure:134345 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-04

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-07

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 539e89e7e283ee09622e172b415fc9a811eba34c -->

### svc-nvidia-pr-review · 2026-09-07


Test/scope (already reported, restating for completeness): there is still no caller or test for uct_rc_mlx5_op_info_fill in this PR, so the new AM-zcopy path is untested/dead here. This was raised in the prior review; if a caller/test is coming in a follow-up, that should be stated in the PR description.


### svc-nvidia-pr-review · 2026-09-07

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 119f81b9d6c430a8497dba46a0bf674e6a52b792 -->

### svc-nvidia-pr-review · 2026-09-07

🤖 Review complete — no issues found.

### svc-ucx · 2026-09-07

🤖 **CI Triage Agent** — `UCX PR (Tests roce on worker 0)` · commit `119f81b9`

**TL;DR:** The `roce` gtest job did not fail an assertion first — it **hung**: `rc/test_ucp_wireup_1sided.disconnect_nb_onesided/0 <rc_v/rma>` posted 1000 `ucp_put_nb`s and then its `ucp_ep_flush_nbx` never completed (3-minute dead silence, repeated "did not complete on time"), cascading into the data-validation failure, the un-freed EP close request and finally `Connection timed out - abort testing` (SIGABRT). The stall is in the rc_verbs/RoCE RMA flush path, which this PR does not touch, so it should be re-run and, if reproducible, chased as an rc_v flush/pending stall rather than as a PR regression.

<details><summary>Full analysis</summary>

**Summary:** gtest `make test` aborted after `rc/test_ucp_wireup_1sided.disconnect_nb_onesided/0 <rc_v/rma>` hung waiting for RMA put/flush completions on the RoCE worker (`swx-rain04`), exit via `test_helpers.cc:57 "Connection timed out - abort testing"`.

**Root cause:** A hang, not slowness. Log timestamps show continuous ~400 ms test cadence until `10:08:45.14` (`[ RUN ] ... disconnect_nb_onesided/0 <rc_v/rma>`), then a **3-minute gap with zero output** to `10:11:45.75`, where the first `ucp_test.cc:300 UCX ERROR request 0x...298 did not complete on time` appears. That request is the `ucp_ep_flush_nbx` issued in `test_ucp_wireup::send_nb()` right after the 1000 `ucp_put_nb` calls (`test_ucp_wireup.cc:287-302`). Every subsequent step then failed as a cascade, each burning another 3-minute deadline:
- `ucp_test.cc:897` — "free not completed EP close request" (the `disconnect_nb()` close request never completed).
- `test_ucp_wireup.cc:361` — `recv_data` vs `data: 0`, "invalid data at index 1000 (control)": the control word `m_recv_data[length]` was still 0, i.e. **no RMA data ever landed**.
- Two more `requests_wait()` timeouts (`10:17:56`, `10:20:57`), then the global test deadline aborted the binary (`Caught signal 6`, backtrace through `ucp_test::requests_wait` → `test_ucp_wireup.cc:748`).

Notably there is **no send-completion-with-error, no IB async event, no retry-exceeded error** in the log — the QP produced no completions at all, consistent with the puts sitting un-progressed (pending/wireup) rather than a fabric error being reported.

Relation to the PR: this variant is `rc_v` (rc_verbs + ud_verbs). All seven commits on the branch (`4b8ed2d3` am zcopy parser, `a6e856fd`, `37cf9f65`, `2dc0d149`, `83944d53`, `539e89e7`, `119f81b9`, all by Zihao Zhao) touch only `src/uct/ib/mlx5/rc/rc_mlx5_common.c`, `src/uct/ib/mlx5/ib_mlx5.c` and `src/uct/ib/mlx5/ib_mlx5_log.c` — mlx5-accelerated code (`uct_rc_mlx5_op_info_fill_am_zcopy` / WQE parsing for op-info extraction). Nothing in `src/uct/ib/rc/verbs`, `src/uct/ib/rc/base` or `src/ucp` is modified, and the mlx5 tests earlier in the same run (`dc_mlx5`, `rcx`, `gga_mlx5`) all passed. So the hung path is not exercised by the PR's changes.

**Implicated commit:** unknown — not `119f81b9` (PR commits are mlx5-only; the hang is on the rc_verbs RMA path). Candidate pre-existing suspects on master for an unfinished flush are the failover/flush work `75611772` (UCP/FT: EP flush failover) and `db208ee4` (UCP/FT: probe-gated lane recovery via aux `uct_ep_check`, which also touched this very test file), but the log contains no direct evidence for either.

**File:** `test/gtest/ucp/test_ucp_wireup.cc:300` (the hung `ucp_ep_flush_nbx` wait, reached from `test_ucp_wireup.cc:737`/`748`); timeout reported at `test/gtest/ucp/ucp_test.cc:300`

**Suggested fix:**
1. Re-run the `roce on worker 0` job on the same commit — this is almost certainly not a regression from the mlx5 AM-zcopy parser change, and a clean re-run will unblock the PR.
2. If it reproduces, treat it as an rc_verbs RMA flush stall, not a timeout to be raised: re-run just that test with `UCX_LOG_LEVEL=debug`, `UCX_PROTO_INFO=y` and `--gtest_filter='rc/test_ucp_wireup_1sided.disconnect_nb_onesided/0'`, and on the hang attach gdb / use `ucs_ep_pending` dumps to see whether the 1000 puts are stuck in the UCP pending queue (wireup lane not yet ready / flush state not advanced) or posted with no CQE.
3. Also check `swx-rain04`'s RoCE port for link/PFC problems around 10:08 UTC — the complete absence of any error CQE while 8 MB of puts never complete is equally consistent with a stuck NIC/fabric on that agent.
4. Do **not** raise the gtest deadline: the largest inter-line gap (180 s, the request deadline itself) proves the process was idle, not making progress.

**Related:** PR #11873 ("[WIP][DNM] UCT/IB/MLX5: wqe parser for uct am zcopy"); no existing issue found for this hang (`test_ucp_wireup_1sided.disconnect_nb_onesided` / rc_v flush timeout) — worth opening one if it recurs.

</details>

<!-- triage-agent request_id: 9accbdc4-0826-4e0d-80a1-dfe11ef41bd9 -->
<!-- triage-agent gha_run_id: azure:134643 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 957f00e7b4fdb9e4ec32225c48f2ea423cddcc57 -->

### svc-nvidia-pr-review · 2026-09-11


`test/gtest/uct/test_peer_failure.cc`

blocker: `test_purge_outstanding(am_zcopy, &ctx);` passes `purge_ctx*` to a `purge_ctx&` parameter (the signature was changed to `test_purge_outstanding(const send_func_t&, purge_ctx&)` in the same hunk). This does not compile. Suggested fix: `test_purge_outstanding(am_zcopy, ctx);`


`test/gtest/uct/test_peer_failure.cc`

blocker: `UCT_INSTANTIATE_TEST_CASE(test_uct_purge_outstanding, am_zcopy)` — `UCT_INSTANTIATE_TEST_CASE(_test_case)` (test/gtest/uct/uct_test.h:528) takes exactly one argument; the two-argument form is `_UCT_INSTANTIATE_TEST_CASE(_test_case, _tl_name)` and the second argument is a TL name, not a test name. Preprocessor error → build break. Should be `UCT_INSTANTIATE_TEST_CASE(test_uct_purge_outstanding)`.


`test/gtest/uct/test_peer_failure.cc`

minor: `recvbuf` (and therefore `RECV_SEED`) is never used by the AM zcopy test — AM needs no remote registration, pls remove.


Coverage note: the new gtest only exercises `uct_rc_mlx5_op_info_fill` when the `ib_mlx5_ext` plugin providing `ep_outstanding_purge` is present (rc_mlx5_iface.c:1242); in a plain in-tree build the test skips on the `UCT_IFACE_FLAG_V2_QUERY_TOKEN` check, so upstream CI will not cover the new AM-zcopy parsing path.


### svc-ucx · 2026-09-11

🤖 **CI Triage Agent** — `UCX PR (Build build_aarch64 rhel100_aarch64)` · commit `957f00e7`

**TL;DR:** The aarch64 build failed on a plain C++ compile error in the gtest sources added by this PR: `UCT_INSTANTIATE_TEST_CASE` was invoked with two arguments at `test/gtest/uct/test_peer_failure.cc:656`, which leaves the macro unexpanded and destroys the parse of everything after it (hence the flood of errors at line 772). Change it to `UCT_INSTANTIATE_TEST_CASE(test_uct_purge_outstanding)` and fix the `test_purge_outstanding(am_zcopy, &ctx)` argument type.

<details><summary>Full analysis</summary>

**Summary:** `make` failed building `test/gtest/uct/gtest-test_peer_failure.o` (`make[3]: *** [Makefile:2475: uct/gtest-test_peer_failure.o] Error 1`) in job `build_aarch64 rhel100_aarch64`.

**Root cause:** Compilation error introduced by the PR's new gtest code, not an infrastructure/timeout issue:
1. `test/gtest/uct/test_peer_failure.cc:656` calls `UCT_INSTANTIATE_TEST_CASE(test_uct_purge_outstanding, am_zcopy)`, but the macro is defined with exactly one parameter (`test/gtest/uct/uct_test.h:528`: `#define UCT_INSTANTIATE_TEST_CASE(_test_case)`). GCC reports the arity mismatch and leaves the invocation unexpanded, so `UCT_INSTANTIATE_TEST_CASE(test_uct_purge_outstanding, am_zcopy)` is parsed as a stray declaration at file scope with a missing `;`. That aborts the parse of the immediately following `class test_uct_peer_failure_multiple` (line 658), which is exactly why the log shows, at line 772, `‘test_uct_peer_failure_multiple’ was not declared in this scope; did you mean ‘gen_tcptest_uct_peer_failure_multiple’?`, `parse error in template argument list`, `‘info’ was not declared in this scope`, and `no matching function for call to ... GetTestSuitePatternHolder<<expression error>>`. All the visible errors are downstream fallout of line 656.
2. A second, independent type error in the same new test: line 653 calls `test_purge_outstanding(am_zcopy, &ctx);` but the method is declared as `void test_purge_outstanding(const send_func_t &send_func, purge_ctx &ctx)` (line 576) — a `purge_ctx*` cannot bind to `purge_ctx&`. (Contrast with `purge_outstanding(&ctx)` at line 603, which does take a pointer.)

**Implicated commit:** `30bb3b7e` — "GTEST/UCT/IB: add test for am zcopy", Zihao Zhao (branch tip commit `[REDACTED:Hex High Entropy String]`, PR #11873)

**File:** `test/gtest/uct/test_peer_failure.cc:656` (primary) and `test/gtest/uct/test_peer_failure.cc:653`

**Suggested fix:**
- Line 656: drop the extra argument — `UCT_INSTANTIATE_TEST_CASE(test_uct_purge_outstanding)`. The test name (`am_zcopy`) is already bound by `UCS_TEST_SKIP_COND_P(test_uct_purge_outstanding, am_zcopy, ...)` at line 623; the instantiate macro only takes the test-case class. If the intent was to restrict instantiation to a transport subset, use one of the dedicated macros (`UCT_INSTANTIATE_IB_TEST_CASE`, `UCT_INSTANTIATE_NO_SELF_TEST_CASE`, etc. in `uct_test.h:540-597`), each of which also takes exactly one argument.
- Line 653: pass by reference — `test_purge_outstanding(am_zcopy, ctx);` (or change the signature to `purge_ctx *ctx` for consistency with `purge_outstanding`).
- Build `test/gtest` locally (`make -C test/gtest`) before re-pushing; this error is architecture-independent and will fail every build job, so aarch64 is just the first one reported.

**Related:** PR #11873 (this PR); base infrastructure from #11891 "GTEST/UCT: Add outstanding purge test base" (commit `61104007`, Roie Danino) which added `test_uct_purge_outstanding` and the `test_purge_outstanding(const send_func_t&, purge_ctx&)` signature.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `a761f83a-2a9e-42a8-a213-e72638fe99ab` in the triage console for the audit trail.

<!-- triage-agent request_id: a761f83a-2a9e-42a8-a213-e72638fe99ab -->
<!-- triage-agent gha_run_id: azure:135666 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: c061cf21dfc046acf2cc8cef6ab05dd899dd548f -->

### svc-nvidia-pr-review · 2026-09-11


Residual coverage gap (already raised in earlier reviews, not repeated inline): the new gtest only exercises this code when the out-of-tree plugin providing `ep_outstanding_purge` is loaded, so upstream CI does not cover the AM-zcopy parsing path.


### svc-ucx · 2026-09-11

🤖 **CI Triage Agent** — `UCX PR (Tests new on worker 1)` · commit `c061cf21`

**TL;DR:** The gtest run failed on a single, unrelated test — `ud_mlx5/test_ud.stale_private_ep_reuse/0` — a newly added, timing-sensitive UD private-EP keepalive test that is already known to be flaky (open PR #11920 "Hardening of UD stale private EP tests"). Re-run the job; the real fix is to land/merge the test-hardening in #11920, not to change anything in PR #11873.

<details><summary>Full analysis</summary>

**Summary:** `make test` in `build-test/test/gtest` exited with Error 1 because 1 of 8608 tests failed: `[  FAILED  ] ud_mlx5/test_ud.stale_private_ep_reuse/0, where GetParam() = ud_mlx5/mlx5_0:1`.

**Root cause:** Evidence from the log: 8607 tests passed, exactly one failed, the run completed normally (no SIGTERM/timeout, continuous timestamps up to `Global test environment tear-down` at 06:05:52 → summary at 06:05:57), and `make: *** [Makefile:4713: test] Error 1` is purely the gtest non-zero exit.

The failing test is `test_ud.stale_private_ep_reuse` (`test/gtest/uct/ib/test_ud.cc:935-970`). It is a wall-clock/keepalive-timing dependent test: it runs with `UD_LINGER_TIMEOUT=1s` / `UD_TIMEOUT=1s` and asserts that a *stale* private UD EP on `m_e2` is garbage-collected after its peer EP on `m_e1` disappears. The wait helper `ud_base_test::wait_for_ep_destroyed()` (`test/gtest/uct/ib/ud_base.cc:98-104`) polls with a 60 s budget and, critically, **does not assert on timeout** — it silently returns, so the subsequent `EXPECT_FALSE(ucs_ptr_array_lookup(&iface(m_e2)->eps, private_index, ud_ep))` (line 960) / `EXPECT_TRUE(find_private_ep(...))` (line 963) fail whenever keepalive-driven release hasn't converged yet on a heavily loaded CI worker. This whole test + the keepalive-based release path are brand new (commit `183a66db` "UCT: Keep-alive for private UD EPs", Ilia Yastrebov, 2026-08-24, plus `0ee470a8` generation-ID and `8a38ae21` "Remove from CEP early"), and there is already an open PR acknowledging this exact instability: **#11920 "UCT/GTEST: Hardening of UD stale private EP tests"**.

Crucially, PR #11873 is *"UCT/IB/MLX5: wqe parser for uct am zcopy"* — it touches the RC/mlx5 WQE parsing path and has no interaction with the UD transport's private-EP/keepalive lifecycle. So this is a pre-existing flaky failure, not a regression from the PR under test.

Caveat: the per-test gtest assertion text is not recoverable from this build — the fetched log is only the tail (~last 500 lines) of a ~60-minute shuffled run, and the test itself executed much earlier. The diagnosis above is based on the failure summary line, the test source, and the existing hardening PR.

**Implicated commit:** `183a66db` — "UCT: Keep-alive for private UD EPs", Ilia Yastrebov (test added by `2d550625` / `183a66db`; not `c061cf21` from PR #11873)

**File:** `test/gtest/uct/ib/test_ud.cc:935-963` (assertions at 956-963), with the non-asserting timeout helper at `test/gtest/uct/ib/ud_base.cc:98-104`

**Suggested fix:**
1. **Unblock PR #11873 now:** re-run the "Tests new on worker 1" job. This failure is unrelated to the WQE-parser change; do not modify the PR.
2. **Real fix — harden the test** (align with / merge PR #11920):
   - Make `wait_for_ep_destroyed()` fail loudly instead of silently timing out, e.g. `ASSERT_TRUE(...)` on the wait result, so a timeout is distinguishable from a genuine logic bug and the log names which wait expired.
   - Replace the fixed 1 s `UD_LINGER_TIMEOUT`/`UD_TIMEOUT` with values scaled by `ucs::test_time_multiplier()`, since these timeouts are too tight for a loaded/shared CI worker running 8608 tests.
   - Wait for `m_e1`'s old EP to be destroyed *before* asserting on `m_e2`'s stale private EP, so the keepalive on `m_e2` is guaranteed to observe a dead peer.
3. If the flake persists after hardening, consider gating the test on a non-shared/serialized worker rather than the general gtest sweep.

**Related:** https://github.com/openucx/ucx/pull/11920 (UCT/GTEST: Hardening of UD stale private EP tests — the actual fix); https://github.com/openucx/ucx/pull/11873 (PR under test, unrelated); https://github.com/openucx/ucx/pull/11800 (UCT/UD: generation ID in dest_id, same feature area)

</details>

<!-- triage-agent request_id: 256eb5c0-b27c-4e06-852c-014c92bd2218 -->
<!-- triage-agent gha_run_id: azure:135676 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 08adddc8e2e0c5f3edc6ad48560c4198805911b8 -->

### svc-nvidia-pr-review · 2026-09-11


Residual coverage gap (already raised in earlier reviews): the new gtest only exercises this path when the out-of-tree plugin providing `ep_outstanding_purge` is loaded, so upstream CI does not cover the AM-zcopy parsing.


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 98f448fef04fb796f4103169afb5239ed42c11d4 -->

### svc-ucx · 2026-09-11

🤖 **CI Triage Agent** — `UCX PR` · commit `98bc0952`

**TL;DR:** The "Basic compile" stage failed because a new function declaration in `src/uct/ib/mlx5/ib_mlx5.h` is missing its terminating semicolon, which makes GCC parse everything after it as a K&R-style function definition and produces hundreds of cascading errors in `ib_mlx5dv_md.c`. Add the missing `;` after the `uct_ib_mlx5_txwq_copy_segs()` prototype at line 924.

<details><summary>Full analysis</summary>

**Summary:** `make` failed in `src/uct/ib/mlx5` while compiling `dv/libuct_ib_mlx5_la-ib_mlx5dv_md.lo` with a flood of syntax errors ending in `ib_mlx5dv_md.c:3479: error: expected '{' at end of input`.

**Root cause:** Pure syntax error in a header, not a real code/type problem. In `src/uct/ib/mlx5/ib_mlx5.h` the newly added prototype

```c
void uct_ib_mlx5_txwq_copy_segs(const uct_ib_mlx5_txwq_t *txwq, void *dst,
                                const void *src, size_t length)   /* <-- no ';' */
```

has no trailing semicolon. GCC therefore treats it as the start of a *function definition* and interprets every subsequent declaration in the header (and in all headers/source included afterwards, including `ucs/profile/profile_defs.h` and the whole of `ib_mlx5dv_md.c`) as old-style (K&R) parameter declarations. That is why the log shows nonsense errors like `storage class specified for parameter 'uct_ib_mkey_token'`, `parameter 'uct_ib_mlx5_devx_reg_indirect_key_inner' declared 'inline'`, `type defaults to 'int' in declaration of 'ucs_profile_context_t'`, and finally the tell-tale pair:

```
ib_mlx5.h:923:6: error: old-style parameter declarations in prototyped function definition
ib_mlx5dv_md.c:3479: error: expected '{' at end of input
```

With `-Werror` in the basic-compile job this aborts the build. All the other reported errors are downstream noise from this single missing character.

**Implicated commit:** `98bc0952` — "UCT/IB/MLX5: copy segs", Zihao Zhao (HEAD of PR #11873; the doc-only predecessor `98f448fe` touched the same block)

**File:** `src/uct/ib/mlx5/ib_mlx5.h:924`

**Suggested fix:** Terminate the prototype:

```c
/* Copy length bytes out of the WQ starting at src, wrapping around at qend */
void uct_ib_mlx5_txwq_copy_segs(const uct_ib_mlx5_txwq_t *txwq, void *dst,
                                const void *src, size_t length);
```

Then verify the corresponding definition exists in `src/uct/ib/mlx5/ib_mlx5.c` (otherwise the link stage will fail next with an undefined reference), and rebuild locally with `./contrib/configure-devel && make -j` before re-pushing, since this would have been caught by any local compile.

**Related:** PR #11873 (branch `failover_uct_rcx_am_zcopy`); no pre-existing issue found — this is a new typo introduced by the PR, not a CI infrastructure or flaky-test problem.

</details>

<!-- triage-agent request_id: 921dbc23-e31d-498b-92bb-6102e673b874 -->
<!-- triage-agent gha_run_id: azure:135714 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-11


Residual coverage gap (raised in earlier reviews, not repeating): the new gtest only exercises this code when the out-of-tree plugin providing `ep_outstanding_purge` is loaded (`rc_mlx5_iface.c:1242`), so upstream CI skips on the `UCT_IFACE_FLAG_V2_QUERY_TOKEN` check and does not cover the AM-zcopy parsing path. That also means the findings above will not be caught by CI.


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 98bc095232fa14e407316eb420737ae280b0cb63 -->

### svc-nvidia-pr-review · 2026-09-11


low confidence: `uct_rc_mlx5_op_info_fill()` assumes `av_size == 0` (`inl = ctrl + 1`, `iovcnt = (wqe_size - sizeof(*ctrl) - inline_seg_size)/...`), i.e. RC only, but it lives in `rc_mlx5_common.c`/`.h` which DC also uses. Today DC has `ep_outstanding_purge = unsupported` so there is no live misuse; a short comment or an `av_size` parameter would prevent a future DC caller from misparsing.


Coverage note: the new gtest only exercises this code when the out-of-tree plugin providing `ep_outstanding_purge` is loaded, so upstream CI skips it on the `UCT_IFACE_FLAG_V2_QUERY_TOKEN` check and will not catch the op-info parsing / fatal / am_id findings.


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 1552b020e40e8d3137fad7ff465df58338eab739 -->

### svc-nvidia-pr-review · 2026-09-11


Residual points from the earlier review still apply to this revision: `uct_rc_mlx5_op_info_fill()` has no in-tree caller (the caller is the out-of-tree `ib_mlx5_ext` plugin providing `ep_outstanding_purge`); the parsing/`ucs_fatal`/`am_id` comments; the `av_size == 0` (RC-only) assumption in a file shared with DC; and upstream CI skips the new gtest on the `UCT_IFACE_FLAG_V2_QUERY_TOKEN` check, so none of the new AM-zcopy parsing code is covered by CI.


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 8494be616cbbe851b787b2e7caa979146247da15 -->

### svc-nvidia-pr-review · 2026-09-11


Notes (not posted, already raised in earlier rounds): no in-tree caller for `uct_rc_mlx5_op_info_fill()`, the `ucs_fatal`/`put_short` unsupported-op handling, the RC-only (`av_size == 0`) assumption in a file shared with DC, and the fact that upstream CI skips the new gtest on `UCT_IFACE_FLAG_V2_QUERY_TOKEN`, so none of the new parsing code is covered by CI (including the two new tests added here).


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 5d8236153b86b8201e6ea5f3003f2c6741eb3dd5 -->

### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 691bfa8e344f0b1052c5fe2910fa4e35b96a7340 -->

### svc-nvidia-pr-review · 2026-09-14

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 6d1ee90da7dd4b2edbde406c67082f02e9e92176 -->

### svc-nvidia-pr-review · 2026-09-14


Context from earlier rounds (not newly posted): `uct_rc_mlx5_op_info_fill()` still has no in-tree caller (the purge hook comes from the out-of-tree `ib_mlx5_ext` plugin); the `ucs_fatal`/unsupported-op handling for non-zcopy WQEs (am_short, FC pure grant, bcopy, NOP); the RC-only `av_size == 0` assumption in a file shared with DC; and upstream CI skips the two new gtests on the `UCT_IFACE_FLAG_V2_QUERY_TOKEN` check, so none of the new parsing code — including the fence case — is covered by CI.


### jeynmann · 2026-09-14

Will rebase after [PR#11857](https://github.com/openucx/ucx/pull/11857) and [PR#11874](https://github.com/openucx/ucx/pull/11874) merged.

### svc-nvidia-pr-review · 2026-09-22

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 131b611afd52643ddad46976040fb5780aae1e74 -->

### svc-ucx · 2026-09-22

🤖 **CI Triage Agent** — `UCX PR (Build build_aarch64 sles15sp7_aarch64)` · commit `131b611a`

**TL;DR:** The aarch64/SLES15SP7 debug build fails at link time with `undefined reference to test_uct_purge_outstanding::AM_ZCOPY_ID` because the new in-class `static const uint8_t AM_ZCOPY_ID` is ODR-used by `EXPECT_EQ` but, unlike `AM_SHORT_ID`, has no out-of-line definition; add `const uint8_t test_uct_purge_outstanding::AM_ZCOPY_ID;` next to the existing definitions.

<details><summary>Full analysis</summary>

**Summary:** `make` of `test/gtest` fails while linking the `gtest` binary: `uct/gtest-test_peer_failure.o: undefined reference to 'test_uct_purge_outstanding::AM_ZCOPY_ID'` (2 relocations), `collect2: error: ld returned 1 exit status`, `make[3]: *** [Makefile:1551: gtest] Error 1`.

**Root cause:** `test_uct_purge_outstanding` declares its constants in-class (`test/gtest/uct/test_peer_failure.cc:352-357`). In C++11 mode (`-std=c++11`, which this job uses) a `static const` member that is bound to a reference is ODR-used and therefore needs a namespace-scope definition. The file provides those definitions only for the older constants:

```
699: const uint8_t  test_uct_purge_outstanding::AM_SHORT_ID;
700: const uint64_t test_uct_purge_outstanding::AM_SHORT_HEADER;
```

The newly added `AM_ZCOPY_ID` (line 356) is passed to `EXPECT_EQ(AM_ZCOPY_ID, info->am.am_id)` at line 519 — gtest's `CmpHelperEQ` takes `const T1&`, so the symbol must exist. No definition was added for it, so the reference is unresolved. It only shows up in this job because it is a `-O0 -D_DEBUG` build with gcc 7 (aarch64-suse-linux/7); at higher optimization levels the constant is folded and the reference disappears, which is why other pipelines pass.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — "UCT/IB/MLX5: Add WQE parser for zcopy", Zihao Zhao (the commit that introduced `AM_ZCOPY_ID` and `validate_am_zcopy`)

**File:** `test/gtest/uct/test_peer_failure.cc:356` (declaration), used at `:519`, missing definition after `:700`

**Suggested fix:** Add the out-of-line definition alongside the existing ones:

```c++
const uint8_t  test_uct_purge_outstanding::AM_SHORT_ID;
const uint64_t test_uct_purge_outstanding::AM_SHORT_HEADER;
const uint8_t  test_uct_purge_outstanding::AM_ZCOPY_ID;        /* add */
const uint64_t test_uct_purge_outstanding::AM_ZCOPY_HDR_SEED;  /* add if ODR-used */
```

Alternatives that avoid the footgun entirely: make the comparison not bind a reference (e.g. `EXPECT_EQ(unsigned(AM_ZCOPY_ID), unsigned(info->am.am_id))`), or declare the IDs as an `enum`/`static constexpr` with definitions kept for C++11. Since the class keeps growing constants, adding the definition (or switching to an enum) is the minimal, consistent fix.

**Related:** PR #11873 (branch `failover_uct_rcx_am_zcopy`); no existing issue found for this link error.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `31fe10b7-5828-415f-b0ed-684a155ac615` in the triage console for the audit trail.

<!-- triage-agent request_id: 31fe10b7-5828-415f-b0ed-684a155ac615 -->
<!-- triage-agent gha_run_id: azure:136957 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-22


Residual gaps (already raised in earlier rounds): the `ucs_fatal` handling of WQEs this parser does not support — zero-length RDMA write posted by `uct_rc_mlx5_base_ep_post_check()` (ep_check/keepalive) hits `ucs_fatal("no-payload rdma write is not supported")`, and DM-backed put_short hits `ucs_fatal("unsupported put bcopy op with DM")`; plus am_bcopy/get/atomic WQEs. Also note `uct_ib_mlx5_set_data_seg_iov()` skips zero-length iov entries, so the reported `iovcnt` can be smaller than what the user posted.


### svc-nvidia-pr-review · 2026-09-22

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ed8a58988db9d0ca694b3838b467137c7bb4a249 -->

### svc-ucx · 2026-09-22

🤖 **CI Triage Agent** — `UCX PR (Tests BlueField on worker 1)` · commit `ed8a5898`

**TL;DR:** The BlueField gtest run failed on a single test — `rcx/test_ucp_fault_tolerance.probe_gated_recovery/6 <rc_x/AM>` — i.e. the RC-mlx5 AM failover/recovery path that this PR ("UCT/IB/MLX5: wqe parser for uct am zcopy", head commit `ed8a5898` "fix num packets") rewrites; the AM WQE packet/PSN accounting used by `uct_rc_mlx5_ep_outstanding_purge` is still wrong, so outstanding AM WQEs are mis-classified as delivered/cancelled and recovery doesn't complete as the test expects.

<details><summary>Full analysis</summary>

**Summary:** `make test` in the gtest stage exited with error 1: 8701 tests passed, 1 failed — `rcx/test_ucp_fault_tolerance.probe_gated_recovery/6, where GetParam() = rc_x/AM` (`make: *** [Makefile:4786: test] Error 1`, `##[error]Bash exited with code '2'`).

**Root cause:** The only failing variant is the *pure AM* fault-tolerance test on the *rc_x* transport — exactly the code path this PR touches. `test_ucp_fault_tolerance.probe_gated_recovery` injects a target-side failure on AM BW lanes, then requires that (a) the AM send/flush after injection completes with `UCS_OK` and no error callbacks, and (b) failed lanes are recovered while an aux probe is armed (`test/gtest/ucp/test_ucp_fault_tolerance.cc:913-943`). Lane recovery depends on `uct_ep_outstanding_purge()` correctly deciding which outstanding WQEs the receiver already got, using the PSN walk in `src/uct/ib/mlx5/rc/rc_mlx5_ep.c:1463-1509`. That walk is driven by `uct_ib_mlx5_wqe_num_packets()` → `uct_rc_mlx5_wqe_num_packets_am()` (`rc_mlx5_ep.c:1292-1324`), which is the function the PR head commit `ed8a5898` ("fix num packets", Zihao Zhao, touched `rc_mlx5_ep.c` ~1h before this build) was still fixing. Note the inconsistency versus the PUT variant: `uct_rc_mlx5_wqe_num_packets_put()` explicitly maps a zero-length payload to 1 packet (`rc_mlx5_ep.c:1271-1289`), while the AM variant returns `uct_rc_mlx5_num_packets(txwq, length)` with no zero/rounding guard, and the post-side PSN accounting (`rc_mlx5.inl:1002-1007`) applies the "+1 for zero length" adjustment only for `MLX5_OPCODE_RDMA_WRITE`. Any off-by-one in that count shifts `wqe_first_psn` for every subsequent WQE, so `uct_ib_mlx5_wqe_is_delivered()` flips `delivered` at the wrong boundary: send ops are either completed with `UCS_OK` when they were actually lost (AM never arrives → `wait_for_value(&m_am_received)`/pattern check fails) or cancelled when they were delivered (spurious error callback → `ASSERT_EQ(0, m_total_err_count)`), and the EP never reaches the clean `failed_lanes == 0` state the test waits for. This is a functional regression in the PR's new code, not infrastructure flakiness: all other transports and all RMA/PUT/GET variants of the same suite passed, and there is no existing flakiness report for this test.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — "UCT/IB/MLX5: fix num packets", Zihao Zhao (PR #11873, building on `131b611a` "Add WQE parser for zcopy")

**File:** `src/uct/ib/mlx5/rc/rc_mlx5_ep.c:1292` (`uct_rc_mlx5_wqe_num_packets_am`), consumed at `src/uct/ib/mlx5/rc/rc_mlx5_ep.c:1482-1508`; test expectation at `test/gtest/ucp/test_ucp_fault_tolerance.cc:913-943`

**Suggested fix:**
1. Make the purge-side packet count bit-for-bit identical to the post-side PSN accounting. Factor the "message length → number of PSNs" computation into one shared helper used both by `uct_rc_mlx5_txwq_update_psn()`/`uct_rc_mlx5_txwq_add_psn()` (`rc_mlx5.inl:1002-1007`) and by `uct_rc_mlx5_wqe_num_packets_am/_put`, so the two can't drift.
2. In `uct_rc_mlx5_wqe_num_packets_am()`, handle the degenerate length like the PUT path does (`return (length == 0) ? 1 : uct_rc_mlx5_num_packets(txwq, length)`), and add `ucs_assertv_always(num_packets > 0, ...)` in `uct_ib_mlx5_wqe_num_packets()` for non-NOP opcodes — a 0 for a real SEND silently corrupts the `wqe_first_psn` walk (the loop only special-cases `num_packets != 0` for the `delivered` check but still advances `wqe_first_psn` by 0). Also confirm raw/FC sends (`uct_rc_mlx5_base_ep_fc_ctrl`, `rc_mlx5_ep.c:1518`) are counted as 1 PSN.
3. Add a unit test that posts AM short/AM zcopy (incl. multi-MTU and multi-dseg IOV) plus a PSN-boundary case, then asserts `uct_rc_mlx5_txwq_outstanding_num_packets()` equals the PSN delta observed on the post path.
4. CI log note: the Azure step log only retains the tail, so the gtest assertion text for this failure is truncated. Re-run locally with `--gtest_filter='rcx/test_ucp_fault_tolerance.probe_gated_recovery/6'` and `UCX_LOG_LEVEL=debug` to capture the exact failing `EXPECT`/`ASSERT` line before pushing a fix.

**Related:** PR #11873 (this PR, DNM), closely related purge work in PR #11836 ("UCT/IB/MLX5: support uct put in purge outstanding"); recovery/probe infrastructure from `db208ee4` (#11563) and `9db2e488` (#11562).

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `0c20cbf1-fd00-4b4a-b657-6d44baae7955` in the triage console for the audit trail.

<!-- triage-agent request_id: 0c20cbf1-fd00-4b4a-b657-6d44baae7955 -->
<!-- triage-agent gha_run_id: azure:137003 -->
<!-- triage-agent-bot -->

## Review (29)

### svc-nvidia-pr-review · 2026-09-01 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-04 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-07 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-14 · COMMENTED

(no text)

### jeynmann · 2026-09-15 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-22 · COMMENTED

(no text)

### jeynmann · 2026-09-22 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-22 · COMMENTED

(no text)
