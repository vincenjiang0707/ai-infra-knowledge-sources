# [PR #11906] CI: Add Fedora 44, Ubuntu 26.10, Azure Linux 4.0, UOS and openEuler 22.03 SP4 / 24.03 SP3

source: https://github.com/openucx/ucx/pull/11906
state: open | updated: 2026-09-10T16:25:09Z
labels: 

## 正文

## What?

Add six OSes to the Azure PR pipeline, both arches: Fedora 44, Ubuntu 26.10, Azure Linux 4.0 (preview), openEuler 22.03 SP4 and 24.03 SP3, UOS 20.1060a.

## Retired (second commit)

Agreed with UCX dev management once the new rows were green:

| row | reason |
|---|---|
| `rhel76` | RHEL 7 EOL 2024-06 |
| `debian109` | Debian 10 LTS ended 2024-06 |
| `ubuntu1804` | EOL 2023-05 |
| `ubuntu2004`, `ubuntu2004_rocm` | Ubuntu 20.04 EOL 2025-05 |
| `ubuntu2210` | interim, EOL 2023-07 |
| `fedora41` | EOL 2025-12, replaced by `fedora44` |
| `euleros2sp12` | DOCA 3.5 dropped EulerOS; the openEuler rows cover it |
| `kylin10sp3` | OFED now targets Kylin V11 only |

`debian113` (LTS ended 2026-08-31) drops to `sanity`; `centos7` stays as `sanity`. Containers for the retired rows go too. Two of them were used outside the build matrix: `Build_Static` built on Ubuntu 20.04 and 18.04 and now builds on 22.04 and 24.04, and `rhel76` was the unused default of the `coverity.yml` container parameter, now `coverity_rh7`. Same images removed from the Jenkins matrix.

Net Build stage: 51 -> 42 rows.

## Why?

No CI coverage on any of them. Fedora 41 is long EOL; openEuler is where the OFED trains are going (MLNX_OFED 26.07 dropped `openeuler22.03sp3` for sp4, DOCA 3.5 dropped EulerOS).

## How?

- `buildlib/pr/main.yml`: 12 containers, images on `rdmz-harbor` (all present).
- `buildlib/pr/build_job.yml`: 6 rows in the x86_64 matrix, 6 in aarch64. `sanity` for inbox-only rows, `short` for OFED-backed rows and aarch64, matching the existing split.
- `.ci/pipeline/build_matrix.yaml`: same 12 rows so the Jenkins matrix does not drift.
- openEuler is DOCA on x86_64, MOFED on aarch64 (DOCA has no aarch64 openEuler). UOS is DOCA on x86_64, inbox on aarch64.
- Images from Mellanox-lab/hpcx-build#233; merge that first. The `fedora44` rows need #11908 (GCC 16 vs `omp master`).
- Third commit is a temporary copy of #11908 (both GCC 16 fixes) so the `fedora44` rows can build here; drop it before merge.

## Not fixed here

- Fedora 44 ships GCC 16, which rejects `#pragma omp master` and three set-but-unused gtest counters under `-Werror`. Fixed in #11908; carried here as a temporary test commit until it lands.


## 评论 (28)

### svc-nvidia-pr-review · 2026-09-06

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 9c6b72b210c455ccce7753be2bec7a817e6e5dec -->

### svc-nvidia-pr-review · 2026-09-06


Confirm that the new fedora44-inbox CI job ran green in this PR and that the harbor.mellanox.com/hpcx/x86_64/fedora44/builder:inbox image resolves before merge.


### svc-nvidia-pr-review · 2026-09-06

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 5cf27e01576f17d01aaebd8c9f054921ab3b52c2 -->

### svc-nvidia-pr-review · 2026-09-06


`.ci/pipeline/build_matrix.yaml`

For the three rows whose images are presumably present (fedora44-inbox, openeuler2203sp4-doca3.5, openeuler2403sp3-doca3.5), the prior review already asks to confirm fedora44-inbox runs green and resolves. The same confirmation should be extended to the two new openeuler rows, but since it's the same class of check already raised, this can be a reply/support on the existing comment rather than a new thread.


`.ci/pipeline/build_matrix.yaml`

Residual coverage gap: image existence in harbor cannot be verified from the repo; the confirmations for fedora44-inbox, openeuler2203sp4-doca3.5, and openeuler2403sp3-doca3.5 still need to be checked against actual CI results before merge.


### svc-nvidia-pr-review · 2026-09-06

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 160afcf43cade02aa1ac8a347dfee17a029bfa98 -->

### svc-nvidia-pr-review · 2026-09-06


Image existence in harbor cannot be verified from the repo checkout. The other new rows (ubuntu2610-inbox, fedora44-inbox, azurelinux40-inbox, openeuler2203sp4-doca3.5, openeuler2403sp3-doca3.5) still need their images confirmed resolvable and their jobs confirmed green against actual CI results before merge. For ubuntu2610-inbox and azurelinux40-inbox, this is the same image-existence check already raised for fedora44/openeuler; support the existing thread rather than opening a new one.


### svc-nvidia-pr-review · 2026-09-06

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 7cd4341f2b3eceb9b6bbe642f46ae4cb390ae187 -->

### svc-nvidia-pr-review · 2026-09-06

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-06

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: fd9326fddaea69717318754feca7942d8af5b164 -->

### svc-nvidia-pr-review · 2026-09-06

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-06

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 837b730f1cb746825d438fb303dea540be0dc237 -->

### svc-nvidia-pr-review · 2026-09-06

🤖 Review complete — no issues found.

### svc-ucx · 2026-09-06

🤖 **CI Triage Agent** — `UCX PR (Build build_aarch64 openeuler2203sp4_aarch64)` · commit `837b730f`

**TL;DR:** The `openeuler2203sp4_aarch64` lane never ran any UCX code — the Azure agent's "Initialize containers" step failed because `su -c "echo '%azure_pipelines_sudo ...' >> /etc/sudoers"` returned `su: cannot open session: Permission denied` inside the new openEuler aarch64 builder image; the image's PAM `su` session stack can't open a session with only `--cap-add=SYS_PTRACE`, so either the container options for that lane need the extra capability or the image's `/etc/pam.d/su` must be fixed.

<details><summary>Full analysis</summary>

**Summary:** Job `build_aarch64 / openeuler2203sp4_aarch64` (Azure build 134556) failed during container initialization, before checkout/build, when the agent tried to grant sudo to the container user.

**Root cause:** Evidence from the job log (15:50:18):

```
##[command]/scrap/bin/docker exec b7fd07d4... su -c "echo '%azure_pipelines_sudo ALL=(ALL:ALL) NOPASSWD:ALL' >> /etc/sudoers"
##[error]Docker-exec executed: `su -c ...`; exit code: `1`; command output: `su: cannot open session: Permission denied`
##[section]Finishing: Initialize containers   (then: steps skipped, Stop Containers)
```

The image pulled and started fine (`docker pull ... aarch64/openeuler22.03sp4/builder:mofed-26.07-0.7.7.0`, node 20 startup OK, `groupadd`/`useradd`/`usermod` all succeeded). The only failing operation is the PAM session open in `su`. The container is created with just `--cap-add=SYS_PTRACE` (see the `docker create ... --cap-add=SYS_PTRACE` line, coming from `DOCKER_OPT_ARGS`), and no `--ulimit memlock=-1:-1` (that only comes with `DOCKER_OPT_IB`, which this lane doesn't use). openEuler's `/etc/pam.d/su` session stack (`pam_loginuid.so` needs to write `/proc/self/loginuid` → CAP_AUDIT_CONTROL; `pam_limits.so` needs CAP_SYS_RESOURCE to apply the MOFED `memlock unlimited` limits) therefore fails, and PAM reports it as `cannot open session: Permission denied`. This is purely an image/container-options mismatch introduced by the new lane, not a UCX code problem.

**Implicated commit:** 837b730f (Alexey Rivkin, "CI: Add Fedora 44, Ubuntu 26.10, Azure Linux 4.0, UOS and openEuler 22.03 SP4 / 24.03 SP3") — the commit that adds the `openeuler2203sp4_aarch64` container and matrix entry.

**File:** `buildlib/pr/main.yml:124-126` (container definition, `options: $(DOCKER_OPT_ARGS) $(DOCKER_OPT_VOLUMES)`), matrix entry at `buildlib/pr/build_job.yml:172-174`; `DOCKER_OPT_ARGS` defined at `buildlib/pr/main.yml:8`.

**Suggested fix:** Give the openEuler lanes the capabilities PAM needs, e.g. add a dedicated options variable in `buildlib/pr/main.yml`:

```yaml
variables:
  ...
  # openEuler /etc/pam.d/su uses pam_loginuid + pam_limits; without these caps
  # the AZP container init step ("su -c ... >> /etc/sudoers") fails with
  # "su: cannot open session: Permission denied"
  DOCKER_OPT_PAM: --cap-add=AUDIT_WRITE --cap-add=AUDIT_CONTROL --cap-add=SYS_RESOURCE --ulimit memlock=-1:-1
...
    - container: openeuler2203sp4_aarch64
      image: rdmz-harbor.rdmz.labs.mlnx/hpcx/aarch64/openeuler22.03sp4/builder:mofed-26.07-0.7.7.0
      options: $(DOCKER_OPT_ARGS) $(DOCKER_OPT_PAM) $(DOCKER_OPT_VOLUMES)
```

Apply the same to `openeuler2403sp3_aarch64` (same `mofed-26.07-0.7.7.0` base, so it will hit the identical failure). The cleaner alternative, if the builder images are owned by the same team, is to fix the image: drop/soften `session required pam_loginuid.so` (make it `optional`) in `/etc/pam.d/su` and remove the MOFED `memlock unlimited` entry from `/etc/security/limits.d/` in the openEuler aarch64 builder recipe. Do not retry the build as-is — this is deterministic and will fail identically until the container options or image are changed.

**Related:** PR #11906 ("CI: Add Fedora 44, Ubuntu 26.10, Azure Linux 4.0, UOS and openEuler 22.03 SP4 / 24.03 SP3") — https://github.com/openucx/ucx/pull/11906

</details>

<!-- triage-agent request_id: a5cdbe06-99a4-4c32-a218-13d2e6a4e7bb -->
<!-- triage-agent gha_run_id: azure:134556 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-07

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 1962bb63faff080488adfa885d9f638ff8ea92ea -->

### svc-nvidia-pr-review · 2026-09-07

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-07

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: d31dc4c8984fd071daf7e3bb4b25df90bbc4d547 -->

### svc-nvidia-pr-review · 2026-09-07


Observation (non-blocking): The OpenMP `master`→`masked` change is correct: `omp master` was deprecated in OpenMP 5.1 (`_OPENMP == 202011`) in favor of `omp masked`. This is a sensible warning-fix that aligns with the newer compilers in the added inbox images (fedora44, ubuntu2610, azurelinux40).


`buildlib/pr/build_job.yml`

Observation (non-blocking): `build_job.yml` uses `build_mode: sanity` for the x86 fedora44/ubuntu2610/azurelinux40 rows but `build_mode: short` for their aarch64 counterparts. `sanity` is lighter (compile-only) than `short`. The x86-sanity vs aarch64-short asymmetry looks intentional (capacity/image differences) rather than a defect, so raise only as a soft question if the author has no rationale.


`buildlib/pr/build_job.yml`

Observation (non-blocking): All new container references in `build_job.yml` have matching definitions in `main.yml`, and the modes are valid per `builds.sh`.


Residual coverage gap (already raised in prior reviews — do not repeat): Image existence in harbor/rdmz-harbor cannot be verified from the repo checkout; confirming the new images resolve and their jobs run green is a CI-result check. This was raised repeatedly in earlier passes; support on the existing thread rather than re-opening.


### svc-ucx · 2026-09-07

🤖 **CI Triage Agent** — `UCX PR (Build build_aarch64 fedora44_aarch64)` · commit `d31dc4c8`

**TL;DR:** The `build_aarch64 / fedora44_aarch64` job failed to compile `test/gtest/ucs/test_pgtable.cc` because the newer GCC in the Fedora 44 image flags `num_regions` as `-Werror=unused-but-set-variable`; delete the dead variable (or mark it used).

<details><summary>Full analysis</summary>

**Summary:** `make` failed in `test/gtest` with `ucs/gtest-test_pgtable.o` Error 1 — a `-Werror` compile error, not a test failure.

**Root cause:** In `test_pgtable.multi_search`, `unsigned num_regions` is initialized and incremented but never read. The GCC shipped in the newly-added `fedora44_aarch64` container extends `-Wunused-but-set-variable` to this case, and UCX builds gtest with `-Wall -Werror`, so the warning becomes a hard error:
```
test/gtest/ucs/test_pgtable.cc:192:18: error: variable ‘num_regions’ set but not used [-Werror=unused-but-set-variable=]
  192 |         unsigned num_regions = 0;
cc1plus: all warnings being treated as errors
make[3]: *** [Makefile:3300: ucs/gtest-test_pgtable.o] Error 1
```
This is pre-existing dead code (the variable has been unused since at least 2019); it only becomes fatal now because this PR (`ci-add-new-oses`, PR #11906) introduces the Fedora 44 build image with a stricter compiler. Other build stages using older toolchains pass.

**Implicated commit:** [REDACTED:Hex High Entropy String] (`ci-add-new-oses`, adds the fedora44_aarch64 image) exposes it; the dead code itself predates it — last meaningful touch `c0e8b1b8` "UCS/GTEST/MEMORY/DATASTRUCT: Fix search range in Page Table" (dmitrygx), which stopped using `num_regions`.

**File:** test/gtest/ucs/test_pgtable.cc:192 (and the `++num_regions;` at line 206)

**Suggested fix:** Remove the dead variable in the same PR so the new OS builds green:
```diff
-        /* generate random regions */
-        unsigned num_regions = 0;
+        /* generate random regions */
         for (int i = 0; i < 200 / ucs::test_time_multiplier(); ++i) {
@@
             regions.push_back(make_region(start, end));
-            ++num_regions;
         }
```
If you prefer to keep it for diagnostics, use it (e.g. include it in the `UCS_TEST_MESSAGE` at line 223) rather than adding a blanket `-Wno-unused-but-set-variable`. Worth a quick grep for other `set but not used` hits under the new Fedora 44 image before merging, since more may surface once this one is fixed (the build aborted at the first error).

**Related:** PR #11906 (`ci-add-new-oses`); no existing issue found for this warning.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `00671517-b970-4b7a-9d84-452fe14f751e` in the triage console for the audit trail.

<!-- triage-agent request_id: 00671517-b970-4b7a-9d84-452fe14f751e -->
<!-- triage-agent gha_run_id: azure:134606 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-07

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 2fabe4c79750550dc9d355c433c7d6883e72b6bd -->

### svc-ucx · 2026-09-07

🤖 **CI Triage Agent** — `UCX PR (Tests roce on worker 1)` · commit `2fabe4c7`

**TL;DR:** The `run_ucx_perftest_fault_tolerance` step failed because the external `ucx_drop_tool` could not create an RDMA-TX flow table on `mlx5_0` on the RoCE worker (`syndrome 0x31ed04: Remote I/O error`) and exited immediately, so no failover ever happened and `verify_active_devices` could not find protocol config `cfg#1`. The test script must check that the drop tool actually started (and skip/gate the test where RDMA-TX steering isn't usable) instead of blindly asserting a reconfiguration.

<details><summary>Full analysis</summary>

**Summary:** Azure job "roce on worker 1" → `./contrib/test_jenkins.sh` exited 1 in `run_ucx_perftest_fault_tolerance`: "Error: Failed to find protocol configuration count 1 in standard out".

**Root cause:** Sequence in the log (host `swx-rain03`, agent EXECUTOR_NUMBER→`UCX_IB_SL=11`, `UCX_IB_TRAFFIC_CLASS=41`):
1. Baseline check passed — `ucx_perftest` cfg#0 correctly used `rc_mlx5/mlx5_0:1` + `rc_mlx5/mlx5_1:1`.
2. The script launched `/hpc/local/oss/ucx/fault_tolerance/ucx_drop_tool -d mlx5_0 -s 11 -t 41`, which failed instantly:
   - `ucx_drop_tool.c:409 UCX ERROR failed to create TX RDMA flow table, syndrome 0x31ed04: Remote I/O error`
   - `ucx_drop_tool.c:661 UCX ERROR failed to setup flow table for device 0: Input/output error`
   The tool then exited (confirmed later by `kill: (2695468) - No such process`).
3. Because no traffic was ever dropped, `ucx_perftest` kept running happily on both rails (latency lines continue with ~8-12 GB/s, no error/failover), so no `perftest self cfg#1` block was ever printed.
4. `parse_active_devices`/`verify_active_devices` therefore returned 1 → `run_ucx_perftest_fault_tolerance` returned 1 → `Bash exited with code '1'`.

This is an environment/test-gating problem, not a code regression from PR #11906 (`ci-add-new-oses`): the RDMA-TX flow-table steering domain the drop tool needs is not available/usable on this RoCE (Ethernet link-layer) worker's device/FW/privileges. The test script treats a dead drop tool as "UCX failed to fail over", masking the real error and producing a confusing failure.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — "CI/FT: Adds fault tolerance CI test (#11570)", william-gallagher-nv (introduced `run_ucx_perftest_fault_tolerance`); the failing `ucx_drop_tool` itself lives outside the repo at `/hpc/local/oss/ucx/fault_tolerance/`.

**File:** `contrib/test_jenkins.sh:549` (drop tool launched, exit status ignored) and `contrib/test_jenkins.sh:557` / `contrib/test_jenkins.sh:428-432` (assertion that reports the misleading error)

**Suggested fix:**
1. Validate the drop tool before asserting failover — redirect its output to a file, sleep briefly, and if the process is gone or its log contains `ERROR`, emit a clear warning and `return 0` (skip) rather than `return 1`:
   ```sh
   ${drop_tool} -d ${dev} -s ${sl} -t ${tc} > drop_tool_${dev}.log 2>&1 &
   drop_pid=$!
   sleep 1
   if ! kill -0 ${drop_pid} 2>/dev/null || grep -q ERROR drop_tool_${dev}.log; then
       echo "Warning: ucx_drop_tool failed to install flow table on ${dev}, skipping FT test"
       cat drop_tool_${dev}.log
       kill_background_processes "${background_pids[@]}"
       return 0
   fi
   background_pids+=(${drop_pid})
   ```
2. Gate the test on IB link layer (in addition to the existing `x86_64` and drop-tool-exists checks), since SL/TC-based packet dropping and the RDMA-TX steering domain are IB-oriented — e.g. skip when `ibv_devinfo -d $dev` reports `link_layer: Ethernet`, or exclude the FT task from the RoCE worker in the Azure job definition.
3. Separately, ask the FT tool owner to make `ucx_drop_tool` support RoCE (match on DSCP/PCP) or report a clean "unsupported" status; also verify the tool has the required capabilities/FW support on `swx-rain03`.

**Related:** https://github.com/openucx/ucx/pull/11570 (added this CI test), current PR https://github.com/openucx/ucx/pull/11906 (failure appears unrelated to its changes)

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `c94d3e35-54d9-4695-a389-69ff639d452d` in the triage console for the audit trail.

<!-- triage-agent request_id: c94d3e35-54d9-4695-a389-69ff639d452d -->
<!-- triage-agent gha_run_id: azure:134622 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-08

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 10fd3cc286ff5295874b85cb24019b58ff60598c -->

### svc-nvidia-pr-review · 2026-09-08

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-08

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 2a4314fb2f9e0092264c068341791b43aec574b5 -->

### svc-nvidia-pr-review · 2026-09-08

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-08

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: f18838f87b0e48cf58bde92e0cbce565a64de885 -->

### svc-nvidia-pr-review · 2026-09-08


`buildlib/pr/main.yml`

The PR deletes the ubuntu2004 and ubuntu1804 entries from resources.containers, but the Build_Static stage's build_source job matrix (base lines ~460-470) still references them via CONTAINER: ubuntu2004 and CONTAINER: ubuntu1804 with container: $[ variables['CONTAINER'] ]. With the container definitions gone, these matrix legs resolve to containers that no longer exist in resources.containers, failing the job at container resolution. Fix: also remove (or repoint to a surviving image) the ubuntu2004 and ubuntu1804 rows in the Build_Static build_source matrix, consistent with dropping those containers everywhere else.


### svc-ucx · 2026-09-08

🤖 **CI Triage Agent** — `UCX PR (Tests roce on worker 1)` · commit `f18838f8`

**TL;DR:** The gtest binary aborted on the roce worker because rc_verbs zcopy RDMA operations on `mlx5_1:1` never completed (`put_zcopy` after `uct_ep_fence`, then an rndv `get_zcopy`), leaving pending ops that hold rcache references, so `ucp_cleanup` tripped `ucs_assertv(region->refcount == 0)` in rcache.c:394 and core-dumped. This is unrelated to PR #11906 (CI-only OS additions) — it is a real rc_verbs fence/zcopy completion hang (or a bad RoCE port on the agent), not a timeout to be raised.

<details><summary>Full analysis</summary>

**Summary:** `roce on worker 1` failed with two rc_verbs test failures and then a fatal assertion during UCP context cleanup (`make: *** [Makefile:4713: test] Aborted (core dumped)`, Bash exit code 2).

**Root cause:** Evidence from the log, in order:
1. `rc_verbs/test_rc_iface_flush_remote.put_fence_no_flush_remote/1 <rc_verbs/mlx5_1:1>` — `test_rc.cc:432` expected `comp.count == 0`, got `1` after the 10 s `wait_for_value` (test duration 10119 ms). The posted RDMA-WRITE (issued with the atomic rkey because a fence was pending, `uct_rc_ep_fence_put`) produced no completion at all — no error CQE either ("Got 0 errors and 1 warnings"), followed by `rc_ep.c:468 destroying txqp ... with uncompleted operation ... uct_rc_ep_send_op_completion_handler`.
2. `rc/test_ucp_wireup_1sided.one_sided_wireup_rndv/6 <rc_v/...>` — data mismatch (only 8192 of 16384 bytes received) and three consecutive `ucp_test.cc:300 request ... did not complete on time` timeouts (12:23:44 → 12:26:44 → 12:29:45 → 12:32:45, i.e. ~3 min of complete silence each). This is a hang in rc_verbs `get_zcopy` completion, confirmed by `destroying txqp ... uncompleted operation ... uct_rc_ep_get_zcopy_completion_handler` and the leaked request warning from `mpool.c:40` ("rendezvous zero-copy read from remote 50% on rc_verbs/mlx5 was not returned to mpool").
3. Because those zcopy ops never completed, their memory registrations still held refcounts: `rcache.c:670 ucp_rcache: destroying inuse region ... ref 1 md[0]=mlx5_0 md[1]=mlx5_1` → fatal `Assertion 'region->refcount == 0' failed` at `rcache.c:394` inside `ucs_rcache_purge()` from `ucp_mem_rcache_cleanup()`/`ucp_cleanup()` in the gtest TearDown → abort/core dump, which is what actually failed the job.

Note the device itself is not dead — `dc_mlx5/mlx5_1:1` flush tests passed minutes earlier; only rc_verbs RDMA-WRITE-with-pending-fence (atomic rkey) and RDMA-READ paths hang. So the suspect area is the rc_verbs fence/atomic-rkey zcopy path on this RoCE port rather than a general link failure.

**Implicated commit:** Not the PR head (`f18838f8` is CI-config only). Most plausible code-side suspects touching this exact path/test: `73a6a5c9` "UCT/IB/MLX5: Handle relaxed-only fence ordering (#11721)", Roie Danino (2026-09-02, modified `test/gtest/uct/ib/test_rc.cc` and fence ordering) and `4d0b5b18` "UCT/IB: Consume pending fence on send (#11699)", Roie Danino (touched `rc_verbs_ep.c` and `rc_ep.h` fence logic).

**File:** `test/gtest/uct/ib/test_rc.cc:412-433` (failing assertion at :432); implicated product code `src/uct/ib/rc/verbs/rc_verbs_ep.c:198-217` / `src/uct/ib/rc/base/rc_ep.h:560-567` (`uct_rc_ep_fence_put` → atomic rkey); fatal abort at `src/ucs/memory/rcache.c:394`.

**Suggested fix:**
1. Re-run the roce job to confirm reproducibility, and check the health/config of the RoCE port `mlx5_1:1` on `swx-rain04` (agent-07) — RoCEv2 GID/ECN/PFC state and whether atomic/relaxed-ordering (`IB_PCI_RELAXED_ORDERING=try`) is functional there. If it reproduces only on this agent, quarantine the agent.
2. If it reproduces on healthy hardware, investigate the rc_verbs pending-fence path: `uct_rc_verbs_ep_put_zcopy()` consumes the pending fence via `uct_rc_ep_fence_put()` (rewriting rkey/addr to the atomic MR) but `uct_rc_verbs_ep_post_send()` only applies `IBV_SEND_FENCE` for `IBV_WR_RDMA_READ` — verify the atomic-MR rkey/offset used for RDMA_WRITE on a RoCE device actually yields a CQE, and consider skipping `put_fence_no_flush_remote` for `rc_verbs` on RoCE until fixed (the test was last touched by #11721).
3. Independently, harden test teardown so a stuck UCT operation reports a test failure instead of aborting the whole gtest binary: in `ucp_test::cleanup()`/`entity` destruction path, flush-cancel/force-purge pending ops before `ucp_cleanup()`, so the `rcache.c:394` assert doesn't mask the real failure and kill the remaining suite.

**Related:** PR https://github.com/openucx/ucx/pull/11906 (the triggering CI-only change, not the cause); PR https://github.com/openucx/ucx/pull/11721 and PR https://github.com/openucx/ucx/pull/11699 (fence-ordering changes to this path). No existing issue found for this signature — worth filing one.

</details>

<!-- triage-agent request_id: 4de6c0fe-e3b3-460c-8efe-c6c78c074887 -->
<!-- triage-agent gha_run_id: azure:134928 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-08

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: d340ed6489b3045bce7fe6cc28c8bd99cdfae447 -->

## Review (8)

### svc-nvidia-pr-review · 2026-09-06 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-06 · COMMENTED

(no text)

### Alexey-Rivkin · 2026-09-06 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-07 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-07 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-08 · COMMENTED

(no text)

### Alexey-Rivkin · 2026-09-09 · COMMENTED

(no text)

### dpressle · 2026-09-10 · APPROVED

(no text)
