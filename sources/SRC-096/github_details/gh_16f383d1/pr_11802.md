# [PR #11802] UCP/PROTO: Reload proto_select after nested config allocation

source: https://github.com/openucx/ucx/pull/11802
state: open | updated: 2026-09-18T12:30:24Z
labels: 

## 正文

## What?

Nested protocol selection can reallocate worker config arrays and then use a stale `proto_select` / `rkey_config` pointer (UAF). Reload those pointers from the config indexes after the nested call returns.

## Why?

`worker->rkey_config` and `worker->ep_config` are growable worker arrays (not `ucp_ep_t`). Nested RTS probe can append an rkey config (`ucp_worker_add_rkey_config()`), realloc `worker->rkey_config[]`, and delayed-free the old buffer while `ucp_proto_select_lookup_slow` still holds `&rkey_config->proto_select`. The heap khash survives but the wrapper does not, so `proto_select->hash` can read as `NULL`.

This crash is the rkey array. The outer lookup uses `&ep_config->proto_select` in `worker->ep_config[]`, which is not grown here. The same realloc path exists for `ep_config[]` if a nested call adds an endpoint config.

```
ucp_proto_select_short_init
  ucp_proto_select_lookup_slow              /* outer: &ep_config->proto_select */
    ucp_proto_select_elem_init
      ucp_proto_select_init_protocols
        ucp_proto_rndv_rts_probe
          ucp_proto_rndv_ctrl_probe
            ucp_proto_rndv_ctrl_select_remote_proto
              rkey_config = &ucs_array_elem(...)   /* pointer taken */
              ucp_proto_select_lookup_slow         /* inner: &rkey_config->proto_select */
                kh_get(proto_select->hash)         /* still valid */
                ucp_proto_select_elem_init
                  /* add_rkey_config → worker->rkey_config[] realloc */
                kh_get(proto_select->hash)         /* UAF, hash == NULL */
```

## How?

Re-fetch `proto_select` (and other pointers into those arrays) after functions that can grow/realloc the worker config arrays.


## 评论 (5)

### iyastreb · 2026-08-24

is it related fix: https://github.com/openucx/ucx/pull/11676?

### svc-ucx · 2026-08-24

🤖 **CI Triage Agent** — `UCX PR` · commit `d93dc880`

**Summary:** Unable to fetch logs — cannot diagnose without them.
**Root cause:** Unknown — log access failed.
**Implicated commit:** unknown
**File:** unknown
**Suggested fix:** Provide a direct link to the failed build log in a follow-up comment.
**Log fetch attempts:**
- `fetch_azure_logs({'build_url': 'https://dev.azure.com/ucfconsort/0b36e3f0-8ab9-4a48-b68b-[REDACTED:Hex High Entropy String]/_build/results?buildId=131726'})` → [fetch_azure_logs ERROR: no failed job found in timeline for build 131726]
- `fetch_jenkins_stages({'job': 'UCX PR', 'build': 131726})` → [fetch_jenkins_stages ERROR] build URL names Jenkins controller 'https://dev.azure.com/ucfconsort/0b36e3f0-8ab9-4a48-b68b-[REDACTED:Hex High Entropy String]/_build/results?buildId=131726', but this deployment is configured for 'https://nbuprod.blsm.nvidia.com/nb
- `fetch_jenkins_logs({'job': 'UCX PR', 'build': 131726})` → [fetch_jenkins_logs ERROR] build URL names Jenkins controller 'https://dev.azure.com/ucfconsort/0b36e3f0-8ab9-4a48-b68b-[REDACTED:Hex High Entropy String]/_build/results?buildId=131726', but this deployment is configured for 'https://nbuprod.blsm.nvidia.com/nbu-
- `fetch_azure_logs({'build_url': 'https://dev.azure.com/ucfconsort/ucx/_build/results?buildId=131726'})` → [fetch_azure_logs ERROR: no failed job found in timeline for build 131726]



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `4ba7a276-9e7f-4f22-be9d-d4a83a4a9fa2` in the triage console for the audit trail.

<!-- triage-agent request_id: 4ba7a276-9e7f-4f22-be9d-d4a83a4a9fa2 -->
<!-- triage-agent gha_run_id: azure:131726 -->
<!-- triage-agent-bot -->

### tvegas1 · 2026-08-24

> is it related fix: #11676?

Ah yes seems so, thanks for letting me know. One difference is that here we try to solve by keeping the rkey_config as one array without extra-indirection.

### svc-nvidia-pr-review · 2026-09-18

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: d93dc880a196986e995319cbb178fd9a5d8d0820 -->

### svc-nvidia-pr-review · 2026-09-18


Residual coverage gap: the reallocation path (>32 rkey configs on one worker) is not exercised by any test I found; all changed code paths are otherwise covered only indirectly by existing `test_ucp_proto` / RMA-RNDV tests, which stay under the reserved capacity.


## Review (2)

### brminich · 2026-09-18 · APPROVED

(no text)

### svc-nvidia-pr-review · 2026-09-18 · COMMENTED

(no text)
