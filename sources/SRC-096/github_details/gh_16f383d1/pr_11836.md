# [PR #11836] UCT/IB/MLX5: support uct put in purge outstanding

source: https://github.com/openucx/ucx/pull/11836
state: closed | updated: 2026-09-23T03:26:50Z
labels: 

## 正文

## What?
Add outstanding purge support for uct put short/bcopy.
Depends on https://github.com/openucx/ucx/pull/11874

## Why?
FT need extract info from wqe before replay uct put.

## How?
Add wqe parser for uct put short/bcopy.
Report operation to caller.


## 评论 (96)

### svc-ucx · 2026-08-26

🤖 **CI Triage Agent** — `UCX PR` · commit `026c3c2a`

**TL;DR:** The `Basic compile` stage failed with `-Werror=incompatible-pointer-types` / "too many arguments" because the cleanup commit `026c3c2a` changed `uct_rc_mlx5_op_info_fill_put_short()` to take a `uct_rc_mlx5_op_callback_data_t*` but left the call site passing the old `(callback_data->data, sizeof(callback_data->data))` pair; fix the call site to pass `callback_data`.

<details><summary>Full analysis</summary>

**Summary:** Build of `src/uct/ib/mlx5/rc/rc_mlx5_common.c` failed (`make[5]: *** [Makefile:939: rc/libuct_ib_mlx5_la-rc_mlx5_common.lo] Error 1`) during the "Basic compile" stage.

**Root cause:** Incomplete refactor in the PR branch. The helper was refactored to a 5-parameter form:

```
110: static void uct_rc_mlx5_op_info_fill_put_short(
111:         uct_ep_op_info_t *info, const uct_ib_mlx5_txwq_t *txwq,
112:         const struct mlx5_wqe_inl_data_seg *inl,
113:         const struct mlx5_wqe_raddr_seg *raddr,
114:         uct_rc_mlx5_op_callback_data_t *callback_data)
```

but the only caller inside `uct_rc_mlx5_op_info_fill_put()` still uses the pre-refactor 6-argument buffer+size convention:

```
193:         uct_rc_mlx5_op_info_fill_put_short(info, txwq, inl, raddr,
194:                                            callback_data->data,
195:                                            sizeof(callback_data->data));
```

GCC reports two errors on this: argument 5 is `uint8_t *` where `uct_rc_mlx5_op_callback_data_t *` is expected, and "too many arguments to function". Since UCX builds with `-Wall -Werror`, this aborts the build. This is a pure compile-time inconsistency, not an environment or flaky-test issue.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — "UCT/IB/MLX5: cleanup", Zihao Zhao (follow-up to `95f69187` "UCT/IB/MLX5: Parse PUT WQEs for outstanding purge", same author)

**File:** `src/uct/ib/mlx5/rc/rc_mlx5_common.c:193-195` (declaration at `src/uct/ib/mlx5/rc/rc_mlx5_common.c:110-114`)

**Suggested fix:** Update the call site to match the new signature — pass the struct pointer and drop the redundant size argument, since the callee already derives the bound via `sizeof(callback_data->data)` at line 118:

```c
    if (inl->byte_count & htonl(MLX5_INLINE_SEG)) {
        uct_rc_mlx5_op_info_fill_put_short(info, txwq, inl, raddr,
                                           callback_data);
        return UCS_OK;
    }
```

Then rebuild locally with the same flags the CI uses (`-Wall -Werror`, `-O0 -g`) before re-pushing, and grep for any other callers of `uct_rc_mlx5_op_info_fill_put_short` that may still use the old convention. Consider squashing the "cleanup" commit into `95f69187` so no intermediate commit in the branch is non-compiling.

**Related:** PR #11836 (branch `failover_uct_rcx_put`) — this is the PR under test; no pre-existing issue matches this signature.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `6aff8dff-8b7c-4106-99ee-ed8206422540` in the triage console for the audit trail.

<!-- triage-agent request_id: 6aff8dff-8b7c-4106-99ee-ed8206422540 -->
<!-- triage-agent gha_run_id: azure:132702 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-08-27

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 535cec752887bece8ffca192df30a31d6f7fa683 -->

### svc-nvidia-pr-review · 2026-08-27


Test coverage: the gtest exercises PUT_SHORT / PUT_BCOPY / PUT_ZCOPY reconstruction only when an ext plugin is registered that returns success — otherwise it skips. In upstream CI (no plugin) these three cases are effectively no-ops, so the parsing helpers are not exercised by CI. Worth noting to the author, though may be acceptable given the consumer is out-of-tree.


### svc-nvidia-pr-review · 2026-08-27

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 613a6dec40ab2ebe4e9dd56ccf7386ed91891ba6 -->

### svc-nvidia-pr-review · 2026-08-27


The three `test_rc_purge_outstanding` cases only exercise the new reconstruction paths when an out-of-tree ext plugin returns success; otherwise they `GTEST_SKIP`. In upstream CI (no plugin) `uct_rc_mlx5_op_info_fill` and its helpers are not covered. This is inherent to the out-of-tree consumer design and is acceptable, but the parsing helpers carry non-trivial pointer/wrap logic that will remain untested in-tree.


### svc-nvidia-pr-review · 2026-08-27

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 6654193355b29564151f718001a3b9191745c8e6 -->

### svc-nvidia-pr-review · 2026-08-27

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ab25169dfa9120e0bb85c2d84c8a48203f011590 -->

### svc-nvidia-pr-review · 2026-08-27

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 7827e4b63497f58c0bd3ea65119ba3120c7b3b30 -->

### svc-nvidia-pr-review · 2026-08-27

🤖 Review complete — no issues found.

### svc-ucx · 2026-08-27

🤖 **CI Triage Agent** — `UCX PR (AddressSanitizer roce on worker 2)` · commit `7827e4b6`

**TL;DR:** The ASAN/RoCE gtest job failed on a single test out of 8616 — `rc_mlx5/uct_atomic_key_reg_rdma_mem_type.fadd64/1` (device-memory/MEMIC atomic fetch-add on the RoCE port `mlx5_1:1`) — a test with a long history of MEMIC-allocation flakiness and no relation to this PR's put/purge WQE changes; re-run the job and, if it reproduces, extend the existing MEMIC retry/skip logic in that test.

<details><summary>Full analysis</summary>

**Summary:** `make test` in `build-test/test/gtest` exited with code 1 because exactly one test failed: `rc_mlx5/uct_atomic_key_reg_rdma_mem_type.fadd64/1` (`GetParam() = rc_mlx5/mlx5_1:1`); the other 8615 tests passed.

**Root cause:** Evidence from the log tail: `[ PASSED ] 8615 tests.` / `[ FAILED ] 1 test ... rc_mlx5/uct_atomic_key_reg_rdma_mem_type.fadd64/1` → `make: *** [Makefile:4713: test] Error 1`. The process ran to completion and printed the normal gtest summary and TOP-20 timing table, so this is a plain gtest failure — **not** an AddressSanitizer report (an ASAN abort would have terminated the run) and not a timeout (no gaps; continuous per-test timestamps through tear-down).

The failing test is the only test that allocates `UCS_MEMORY_TYPE_RDMA` (IB device memory / MEMIC) and runs 4 concurrent worker threads doing `uct_ep_atomic64_fetch(ADD)` against it (`test/gtest/uct/test_atomic_key_reg_rdma_mem_type.cc:29-41`, buffer created with `num_retries = 10`). That test is a well-known flaky spot: its entire git history consists of flakiness mitigations — `69718f6d` "Add retries and rand sleep when it cannot allocate MEMIC memory", `[REDACTED:Hex High Entropy String]` "Retry when it cannot allocate MEMIC memory", `d7d57e17` "Limit MEMIC allocation to prevent out-of-memory test failures". MEMIC is a small, per-HCA shared resource, so parallel jobs on the same CI worker ("roce on worker 2") can exhaust it even with 10 retries.

Nothing in this PR plausibly reaches this path: the PR commits are `31ae0aa2` (TX WQE opcode + wrap-around copy helpers), `535cec75`/`[REDACTED:Hex High Entropy String]` (purge gtest + purge stub) and `7827e4b6` (cleanup) — read-only WQE parsing helpers and the outstanding-purge path, whereas the failing test exercises only atomic fetch-add and device-memory key registration and never calls purge.

Caveat: the exact assertion text is not recoverable — the log fetch returns only the final ~30 s of a ~44-minute run, and the failure occurred much earlier. The above is inferred from the summary line plus the test's source and history.

**Implicated commit:** none in this PR — most likely environmental/flaky. Historical context: `69718f6d` (Michal Shalev), `[REDACTED:Hex High Entropy String]` (Michal Shalev), `d7d57e17` (Leonid Genkin), original test `6c7715d0` (Roie Danino).

**File:** `test/gtest/uct/test_atomic_key_reg_rdma_mem_type.cc:29-41` (buffer alloc at line 33-34; retry count plumbed via `entity::mem_alloc(..., unsigned num_retries)` in `test/gtest/uct/uct_test.h:143-146`)

**Suggested fix:**
1. Re-trigger the "roce on worker 2" job; a single-test failure in an unrelated MEMIC atomic test on a 8616-test run is almost certainly infrastructure noise for this PR.
2. To confirm before dismissing it, grab the full step log (not the tail) and search for `rc_mlx5/uct_atomic_key_reg_rdma_mem_type.fadd64` to see whether it was (a) `Failed to allocate memory` / `UCS_ERR_NO_MEMORY` from MEMIC, or (b) `Reply ... appeared N times; expected: M` from `uct_amo_test::validate_replies`. Case (a) = flaky resource exhaustion; case (b) would be a real data-correctness bug and worth bisecting against `31ae0aa2`.
3. If (a) recurs: in `test_atomic_key_reg_rdma_mem_type.cc`, turn a post-retry MEMIC allocation failure into `UCS_TEST_SKIP_R("cannot allocate device memory (MEMIC)")` instead of a hard failure (extending the intent of `69718f6d`), and/or raise the retry count above 10 with backoff.
4. Reproduce locally with `GTEST_FILTER='rc_mlx5/uct_atomic_key_reg_rdma_mem_type.*' make -C build-test/test/gtest test` on a RoCE-capable HCA to verify the PR is not implicated.

**Related:** PR #11836 (this PR, `[WIP][DNM] UCT/IB/MLX5: wqe parser for uct put`); prior flakiness fixes for the same test: #10475 ("Increase retries and add random sleep when MEMIC allocation fails"); PR #11668 (outstanding purge implementation, the feature this PR builds on).

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `5587cd1c-d6c0-4736-a371-a07907c9318b` in the triage console for the audit trail.

<!-- triage-agent request_id: 5587cd1c-d6c0-4736-a371-a07907c9318b -->
<!-- triage-agent gha_run_id: azure:132980 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-08-31

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 07d3d0c3207ed68a8edcd65f2af32537bf19c4f0 -->

### svc-nvidia-pr-review · 2026-08-31

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-08-31

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: f86508563c93bda127928629ce1d4da2d39ccd2e -->

### svc-nvidia-pr-review · 2026-08-31

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-08-31

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: b254a03b6b80797fbcfed1ccf915cc31b6fa8288 -->

### svc-ucx · 2026-08-31

🤖 **CI Triage Agent** — `UCX PR (Coverity coverity release on coverity_rh7)` · commit `b254a03b`

**TL;DR:** The Coverity release gate failed with one DEADCODE defect at `src/uct/ib/mlx5/rc/rc_mlx5_common.c:148` — commit `b254a03b` added an early `return UCS_ERR_UNSUPPORTED` for `is_dm` but left a now-unreachable `is_dm ? ...` ternary below it; replace the ternary with the constant `UCT_EP_OP_PUT_BCOPY`.

<details><summary>Full analysis</summary>

**Summary:** `coverity release on coverity_rh7` failed at the `cov-analyze`/`cov-format-errors` gate: build and analysis succeeded (549 units, 94 defect occurrences), but 1 newly-introduced defect tripped the `nerrors > 0` check → `##[error]Coverity found 1 issues`.

**Root cause:** In `uct_rc_mlx5_op_info_fill_put_bcopy()`, the head commit of this PR added an early bail-out:
```c
137: uct_rc_mlx5_get_dptr_buffer(op, dptr, &buffer, &length, &is_dm);
138: if (is_dm) {
139:     return UCS_ERR_UNSUPPORTED;
140: }
```
Any code after line 140 is only reachable when `is_dm == 0`, so the pre-existing ternary on line 148 (`info->operation = is_dm ? UCT_EP_OP_PUT_SHORT : UCT_EP_OP_PUT_BCOPY;`) has a provably dead true-branch. Coverity reports exactly this chain: `cond_const` at line 138 (false branch → `is_dm == 0`), then `dead_error_condition` / `dead_error_line` at line 148 — "Execution cannot reach the expression `UCT_EP_OP_PUT_SHORT`". This is a genuine leftover from the refactor, not a false positive or infrastructure flake: the DM case now returns early instead of being encoded as `PUT_SHORT`.

**Implicated commit:** `[REDACTED:Hex High Entropy String]` — Zihao Zhao, "UCT/IB/MLX5: return unsupport for DM PUT" (2026-08-31)

**File:** `src/uct/ib/mlx5/rc/rc_mlx5_common.c:148` (dead branch introduced by the guard at `:138-140`)

**Suggested fix:** Drop the now-redundant ternary so the operation is set unconditionally:
```c
    info->operation   = UCT_EP_OP_PUT_BCOPY;
    info->field_mask |= UCT_EP_OP_INFO_FIELD_OPERATION;
```
While there, consider whether `is_dm` still needs to be a separate local — it is only used for the guard at line 138, so the variable can stay but the `UCT_EP_OP_PUT_SHORT` reference in this function should go away entirely (`PUT_SHORT` is still legitimately set in `uct_rc_mlx5_op_info_fill_put_short()` at line 123, so no other change is needed). Re-run the Coverity job after the fix; the remaining 93 occurrences are pre-existing baseline noise and should not re-trigger the gate.

**Related:** PR #11836 (branch `failover_uct_rcx_put`); same-series commits `7827e4b6`, `f75fbbfa`, `1a563800` touch the same file and functions.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `74b4ccf9-96e8-405c-8e6a-e3a43b5778c1` in the triage console for the audit trail.

<!-- triage-agent request_id: 74b4ccf9-96e8-405c-8e6a-e3a43b5778c1 -->
<!-- triage-agent gha_run_id: azure:133377 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-08-31

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 9355f4c729398982c4ed6361e71851be6f79d28f -->

### svc-nvidia-pr-review · 2026-08-31

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-01

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 058bd731996cf8f52b12c728ac547f4ce025ccab -->

### svc-nvidia-pr-review · 2026-09-01

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-01

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 47e98165cf072f371c567142c7a99a23c70fe248 -->

### svc-nvidia-pr-review · 2026-09-01

🤖 Review complete — no issues found.

### svc-ucx · 2026-09-01

🤖 **CI Triage Agent** — `UCX PR (Tests gpu on worker 2)` · commit `47e98165`

**TL;DR:** The GPU gtest job failed because exactly one test — `rc_gda/test_ucp_device_xfer.put/2` (`host` destination memtype / `warp` level / `nodelay_with_req`) — failed, making `make test` exit 1; the failure is in the PUT path this PR is rewriting ("wqe parser for uct put"), so the branch's mlx5 PUT/WQE commits are the prime suspect and the run should be re-done with a full per-test log to capture the assertion text.

<details><summary>Full analysis</summary>

**Summary:** `contrib/test_jenkins.sh` → gtest exited with `make: *** [Makefile:4714: test] Error 1` after `[ FAILED ] rc_gda/test_ucp_device_xfer.put/2, where GetParam() = rc,rc_gda,cuda_copy,rocm_copy/host/warp/nodelay_with_req` (10230 passed, 20 skipped, 1 failed).

**Root cause:** Evidence in the log: the run completed normally (no SIGTERM/timeout, no large timestamp gaps — the tail shows continuous per-test output right up to the tear-down), so this is a genuine functional test failure, not a wall-clock kill. The failing case is a UCP device (GDA) `put` whose *destination* buffer is host memory (`mapped_buffer(size, test.receiver(), 0, test.rx_mem_type())`, test_ucp_device.cc:122-123) at warp granularity with `UCT_DEVICE_FLAG_NODELAY` + request polling; the sibling variants (`cuda/warp`, `host/thread`, and the non-request send modes) all passed. The test either got a non-OK status from `ucp_device_progress_req` (`ASSERT_UCS_OK(result.status)` in `launch_kernel`) or a payload mismatch in `dst_pattern_check`. The exact assertion line is **not** recoverable: the failing test ran ~78 minutes before the end of the job (`4703340 ms total`) and Azure only retains/returns the tail of the task log, which starts at 16:59:43. So the precise failure mode cannot be pinned from the available log. What is attributable: the only code area this branch changes is exactly the mlx5 PUT path (`de072d11` "Parse PUT WQEs for outstanding purge", `31ae0aa2` "Add TX WQE opcode and wrap-around copy helpers", `1a563800` "null payload support", `9355f4c7` "return unsupport for DM PUT", `8282944a` "Track RC send PSN with path MTU"), and the failing test is a PUT test.

**Implicated commit:** [REDACTED:Hex High Entropy String] (Roie Danino, merge of master into `failover_uct_rcx_put`); the substantive PUT-path changes on the branch are de072d11 / 31ae0aa2 / 1a563800 / 9355f4c7 (Zihao Zhao) and 8282944a (Roie Danino). Not proven from the log — the failing assertion text is outside the retained log window.

**File:** test/gtest/ucp/test_ucp_device.cc:730-746 (test body), test/gtest/ucp/cuda/test_kernels.cu:35-77 (kernel put + `ucp_device_progress_req` loop); suspect production code: src/uct/ib/mlx5/rc/rc_mlx5_ep.c and src/uct/ib/mlx5/ib_mlx5.c (new PUT WQE parse/opcode helpers).

**Suggested fix:**
1. Re-run just this case to establish determinism and capture the assertion, e.g. `GTEST_FILTER='rc_gda/test_ucp_device_xfer.put/2' UCX_LOG_LEVEL=debug ./gtest/gtest` (a single reproduction distinguishes "kernel returned error" from "pattern mismatch").
2. If it reproduces: review the new PUT WQE parser for the host-destination RDMA-WRITE case — specifically the `1a563800` "null payload support" and `de072d11` outstanding-purge parsing paths, which must not mis-account BB count/opcode for a WQE whose payload segment layout differs, and `8282944a`'s path-MTU-based PSN accounting for a 32 KB put (`size = 32 * UCS_KBYTE`, i.e. multi-packet), since a wrong PSN/BB stride would corrupt or silently drop the last packets.
3. If it does not reproduce, mark as flaky and open a tracking issue rather than blocking the PR — note this PR is already labelled `[DNM]`.

**Related:** PR #11836 "[DNM] UCT/IB/MLX5: wqe parser for uct put" (this PR); prior work it builds on: #11668 "UCT: outstanding purge implementation", #11821 "UCT/IB/MLX5: Track RC send PSN with path MTU", #11829 "UCT/IB/MLX5: support in progress error handler".

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `8d0e4631-03de-4704-858c-a81d950b25cb` in the triage console for the audit trail.

<!-- triage-agent request_id: 8d0e4631-03de-4704-858c-a81d950b25cb -->
<!-- triage-agent gha_run_id: azure:133728 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-03

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 868b8f22cc75457f24af470de86595dce283c4c5 -->

### svc-nvidia-pr-review · 2026-09-03

🤖 Review complete — no issues found.

### svc-nvidia-pr-review · 2026-09-03

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: c4d596c63169254ab08c1f54266e4006dacd7e34 -->

### svc-nvidia-pr-review · 2026-09-03


Test coverage gap (already raised in prior reviews): the three `test_rc_purge_outstanding` cases only run when an out-of-tree ext plugin advertises `UCT_IFACE_FLAG_V2_QUERY_TOKEN` and provides `uct_ib_mlx5_ext_ep_outstanding_purge`; upstream CI (no plugin) skips them, so `uct_rc_mlx5_op_info_fill` and the wrap/pointer helpers stay untested in-tree. Inherent to the out-of-tree consumer design.


Notes / no action needed: `uct_ib_mlx5_wqe_opcode` and the `ib_mlx5_log.c` refactor are clean and equivalent. `uct_rc_mlx5_txwq_copy_segs` wrap logic is correct: `wrap_any` guarantees `src <= qend`, so `UCS_PTR_BYTE_DIFF(src, qend) >= 0` and the `ucs_min` signed/unsigned mix is safe; the remainder copy from `qstart` is bounded by `length <= UCT_IB_MLX5_MAX_SEND_WQE_SIZE`. The `put_zcopy` change in `rc_mlx5_ep.c` (hoisting `length`) is a harmless refactor that removes a duplicate `uct_iov_total_length` call.


### svc-ucx · 2026-09-03

🤖 **CI Triage Agent** — `UCX PR (Tests gpu on worker 0)` · commit `c4d596c6`

**TL;DR:** The GPU gtest job failed on a single test — `dc_mlx5/uct_atomic_key_reg_rdma_mem_type.fadd64/1` — the only DM/MEMIC ("RDMA" memory type) atomic test, which is a long-standing flaky test that hard-fails (`ASSERT_UCS_OK`) when device-memory allocation returns `UCS_ERR_NO_MEMORY`; nothing in PR #11836 (put/RDMA_WRITE WQE parser) touches the atomic post path, so re-run the job and, if it reproduces, make MEMIC exhaustion a SKIP instead of a failure.

<details><summary>Full analysis</summary>

**Summary:** `Run ./contrib/test_jenkins.sh` (gpu on worker 0) exited 2 because `make test` reported `1 FAILED TEST`: `[ FAILED ] dc_mlx5/uct_atomic_key_reg_rdma_mem_type.fadd64/1, where GetParam() = dc_mlx5/mlx5_1:1` out of 10256 tests (10235 passed, 20 skipped).

**Root cause:** Evidence from the log: exactly one failing test, no crash/abort, no timeout (the suite ran to teardown and printed the TOP-20 list, so this is not a hang or wall-clock kill). The failing test is the only one that allocates `UCS_MEMORY_TYPE_RDMA` (MLX5 device memory / MEMIC) on the receiver and does a 64-bit fetch-add against it (`test/gtest/uct/test_atomic_key_reg_rdma_mem_type.cc:29-41`, `mapped_buffer(..., UCS_MEMORY_TYPE_RDMA, UCT_MD_MEM_ACCESS_ALL, 10)`). That path goes through `uct_test::entity::mem_alloc()`, which retries 10 times on `UCS_ERR_NO_MEMORY` and then hard-fails with `ASSERT_UCS_OK(status)` — producing exactly this signature: one failed test, run continues. This test already has three historical "MEMIC allocation flakiness" fixes (69718f6d, [REDACTED:Hex High Entropy String], d7d57e17), i.e. it is a known-flaky, resource-dependent test on shared GPU/IB workers.
Counter-evidence against the PR being the cause: PR #11836 (`[DNM] UCT/IB/MLX5: wqe parser for uct put`, head `c4d596c6`) only adds/changes the RDMA_WRITE (put) posting and the WQE-parsing helpers (`uct_rc_mlx5_op_info_fill*` in `src/uct/ib/mlx5/rc/rc_mlx5_common.c:19-168`, which is only entered for `MLX5_OPCODE_RDMA_WRITE`); the atomic posting path used by this test (`uct_rc_mlx5_base_ep_atomic_post` / `..._atomic_fop`, `src/uct/ib/mlx5/rc/rc_mlx5_ep.c:531-577`) still resolves the atomic rkey via `uct_ib_resolve_atomic_rkey()` and is unchanged. Caveat: the Azure API only returns the log tail, so the per-test assertion text for this failure was not retrievable — the mechanism above is inferred from the test source plus the single-failure/no-abort pattern, not from the assertion line itself.

**Implicated commit:** none for the failure itself (PR head is `c4d596c6`, Zihao Zhao — put-path only). Flakiness previously patched in `69718f6d` (Michal Shalev), `[REDACTED:Hex High Entropy String]`, `d7d57e17`.

**File:** `test/gtest/uct/test_atomic_key_reg_rdma_mem_type.cc:33` (buffer alloc) → `test/gtest/uct/uct_test.cc:968-998` (`mem_alloc` retry loop ending in `ASSERT_UCS_OK`)

**Suggested fix:** 1) Re-trigger the GPU job; a green re-run confirms the flake. 2) Harden the test: in `uct_test::entity::mem_alloc()` (or in the test's `init()`), treat a persistent `UCS_ERR_NO_MEMORY` for `UCS_MEMORY_TYPE_RDMA` as `UCS_TEST_SKIP_R("no MEMIC memory available")` instead of `ASSERT_UCS_OK`, so MEMIC exhaustion on a busy worker cannot fail unrelated PRs (also note the current code continues to use `m_mem` after the failed assert, which is unsafe). 3) If the failure reproduces on re-run, then investigate for real and post the full gtest output around the `[ RUN ] dc_mlx5/uct_atomic_key_reg_rdma_mem_type.fadd64/1` line — the first suspect would be the fence/rkey-resolution refactor introduced by this PR in `src/uct/ib/mlx5/rc/rc_mlx5.inl:41-62` (`uct_rc_mlx5_ep_fence_put`/`_fence_get` now emit `fm_ce_se` and choose between `uct_ib_resolve_atomic_rkey()` and `uct_ib_md_direct_rkey()`), since a wrong key choice only shows up with indirect atomic MRs like DM memory.

**Related:** PR https://github.com/openucx/ucx/pull/11836 (this PR); prior flakiness fixes for the same test: https://github.com/openucx/ucx/pull/10475 ; sibling PRs on the same branch series: https://github.com/openucx/ucx/pull/11866, https://github.com/openucx/ucx/pull/11874

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `a2aeefee-0c7b-426b-be0d-e6f763f354d9` in the triage console for the audit trail.

<!-- triage-agent request_id: a2aeefee-0c7b-426b-be0d-e6f763f354d9 -->
<!-- triage-agent gha_run_id: azure:134059 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-04

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 91bcd0ee5a07d99ec54eb1bd87c5119230e78d12 -->

### svc-nvidia-pr-review · 2026-09-04


Note (no action, already covered): The test-coverage gap for `uct_rc_mlx5_op_info_fill` and the wrap/pointer helpers (only exercised via the out-of-tree ext plugin; skipped in upstream CI) has already been raised in prior reviews and is inherent to the out-of-tree consumer design.


### svc-nvidia-pr-review · 2026-09-04

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 88dd33cb18bb40459b622b36d68dbc1ffcd1ea4c -->

### svc-ucx · 2026-09-04

🤖 **CI Triage Agent** — `UCX PR (Tests gpu on worker 2)` · commit `88dd33cb`

**TL;DR:** The `gpu on worker 2` job failed because exactly one gtest failed — `dcx/test_ucp_rma_reg.get_nonblocking/1` (dc_x, "nb"/`REG_NONBLOCK_MEM_TYPES=host` variant) — making `make test` exit 1; the failure is in the RMA path on the shared RC/DC MLX5 send WQ that this PR's new put-WQE parser rewrites, so the parser/WQE-size accounting is the prime suspect and needs a targeted re-run to capture the assertion text.

<details><summary>Full analysis</summary>

**Summary:** `./contrib/test_jenkins.sh` → `make test` in `build-test/test/gtest` returned Error 1 because 1 of 10254 gtests failed: `[ FAILED ] dcx/test_ucp_rma_reg.get_nonblocking/1, where GetParam() = dc_x,cuda_copy,rocm_copy/nb`.

**Root cause:** From the log: no timeout, no signal/crash, no `##[error]` other than the non-zero make status, and the run shows continuous progress (largest inter-line gap is ~13 s in `gdr_copy/uct_p2p_rma_test.put_short`) right up to `Global test environment tear-down` — so this is a genuine test assertion failure, not a hang or wall-clock kill. The failing case is the UCP RMA **get** path over **dc_mlx5** with non-blocking (ODP-style) host registration (`test_ucp_rma_reg` variant `nb` sets `REG_NONBLOCK_MEM_TYPES=host`, `MAX_RMA_LANES=2`), which posts get_zcopy/bcopy WQEs on the same MLX5 tx work queue that PR #11836 ("[DNM] UCT/IB/MLX5: wqe parser for uct put", branch `failover_uct_rcx_put`) reworks. A wrong WQE size (`ds`)/tx-index or wrap-around accounting in the new put parser corrupts subsequent operations posted on the same QP, which matches a single, isolated RMA data/status failure on `dcx` while all other transports pass. Caveat: the Azure REST log only exposes the tail of the job, and the gtest assertion text for this test was printed thousands of lines earlier, so the exact assertion message could not be retrieved — the transport/variant attribution above is from the summary lines, the mechanism is inference from the PR's scope.

**Implicated commit:** [REDACTED:Hex High Entropy String] "UCT/IB/MLX5: cleanup" (Zihao Zhao) — PR head; the series also contains `c4d596c6`, `[REDACTED:Hex High Entropy String]`, `d63f220c`, `9355f4c7` (Zihao Zhao) on top of `31ae0aa2` "UCT/IB/MLX5: Add TX WQE opcode and wrap-around copy helpers"

**File:** test/gtest/ucp/test_ucp_rma.cc:760 (failing test) — suspect production code: the new put WQE parser on the MLX5 tx path, `src/uct/ib/mlx5/ib_mlx5.inl` / `src/uct/ib/mlx5/ib_mlx5.h` (WQE opcode + wrap-around helpers used by both put and get)

**Suggested fix:**
1. Reproduce deterministically on the GPU worker: `GTEST_SHUFFLE=0 UCX_LOG_LEVEL=debug ./gtest --gtest_filter='dcx/test_ucp_rma_reg.get_nonblocking/1'` and post the assertion text — that distinguishes a data-verification mismatch (WQE corruption) from a returned error status/timeout.
2. Review the new put WQE parser for: computed `ds`/WQE size vs. actually posted segments, `txwq->sw_pi`/`prev_sw_pi` advancement, and the wrap-around copy path (`31ae0aa2` helpers) — an off-by-one there silently corrupts the *next* WQE, which here is a get.
3. Add an assertion in the parser that the parsed WQE length equals `ntohl(ctrl->qpn_ds) & 0x3f` segments and that the parsed opcode is one of the expected values, so mis-parses fail loudly instead of corrupting later gets.
4. Do not treat this as flaky/infra: rerun the single test 20x before/after reverting the parser commits to confirm attribution.

**Related:** PR #11836 (this build), sibling series PR #11866 ("wqe parser for uct get"), #11874 ("wqe parser for uct put zcopy"), #11844 (closed, "wqe parser for uct flush")

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `4919c8a9-6786-4ffc-a77a-ee075102d0e9` in the triage console for the audit trail.

<!-- triage-agent request_id: 4919c8a9-6786-4ffc-a77a-ee075102d0e9 -->
<!-- triage-agent gha_run_id: azure:134307 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: e42f88f0fbfd4167dd8ecc4488c96c3ee54d20af -->

### svc-nvidia-pr-review · 2026-09-10


Notes (no action): `uct_ib_mlx5_txwq_copy_segs()` wrap logic is correct — `wrap_any()` guarantees `src <= qend`, so the `ucs_min()` signed/unsigned mix is safe, and the remainder copy is bounded by `length <= UCT_IB_MLX5_MAX_SEND_WQE_SIZE`. `uct_ib_mlx5_wqe_opcode()` and the `ib_mlx5_log.c` refactor are equivalent; `put_zcopy` length hoisting only removes a duplicate `uct_iov_total_length()` call. Coverage: the new `put_short` test uses `min(64, max_short)`, so it only exercises the inline path — neither the DM `put_short` path nor the zero-length path is covered in-tree (and the whole purge callback only runs with the out-of-tree ext plugin, already discussed in earlier reviews).


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: a2c24a98ed07048273b76dd6328a926023f5c476 -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gaps: the two new gtest cases only run when an out-of-tree ext plugin provides `ep_outstanding_purge` and the iface advertises `UCT_IFACE_FLAG_V2_QUERY_TOKEN`; upstream CI skips them, so `uct_rc_mlx5_op_info_fill()` and the wrap/copy helpers stay untested in-tree. Within the tests, `put_short` is capped at 64 bytes, so only the inline path is exercised (no DM, no zero-length).


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: b9b2e7a6bccd057de004d680b8d97223b4dcc500 -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gap (already raised in earlier reviews, not repeated inline): the two new gtest cases only run with the out-of-tree ext plugin, and `put_short` is capped at 64 bytes so only the inline path is exercised — the DM and zero-length paths above are not covered in-tree.


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: bdee43adb6936dfe427dd198e75b476db4622fb5 -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gaps (previously reported, noted here for completeness): the two new gtest cases only run with the out-of-tree ext plugin, and `put_short` is capped at 64 bytes so only the inline path is exercised (no DM, no zero-length).


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: b1b1a53731b50e0a16acb8f72850978b4a730487 -->

### svc-nvidia-pr-review · 2026-09-11


`src/uct/ib/mlx5/rc/rc_mlx5_common.c`

A zero-length RDMA_WRITE ends in the `ucs_fatal()` below: `uct_ep_put_short(ep, NULL, 0, ...)` is legal (it is even used by `test_uct_peer_failure.peer_failure`), and `uct_rc_mlx5_txqp_inline_post()` emits `wqe_size = ctrl + raddr` for `length == 0`. The keepalive WQE posted by `uct_rc_mlx5_base_ep_post_check()` is also a zero-length RDMA_WRITE with no send op (comp == NULL), and it stays unsignaled/outstanding. Both land on the `out:` label and abort with "unsupported put op ... handler none". Can we report it as PUT_SHORT with zero length instead of aborting?


`src/uct/ib/mlx5/rc/rc_mlx5_common.c`

`ucs_fatal("unsupported put bcopy op with DM")` is reachable from a plain `put_short`: `uct_rc_mlx5_base_ep_put_short()` switches to `uct_rc_mlx5_common_ep_short_dm()` once `length > UCT_IB_MLX5_PUT_MAX_SHORT(0)` (~220B) and DM is enabled, and that desc handler is also `ucs_mpool_put`, so the classifier routes it to `..._fill_put_bcopy()` and aborts. Can we copy the payload from `op->buffer` (the DM VA) instead? Note the new `put_short` test caps the size at 64B, so this path is never covered.


`src/uct/ib/mlx5/rc/rc_mlx5_common.c`

`default: ucs_fatal("unsupported op %d", opcode)` means any other opcode still outstanding on the QP aborts the process: `MLX5_OPCODE_NOP` from `uct_rc_mlx5_base_ep_flush()` when `unsignaled != 0`, `MLX5_OPCODE_SEND` from `am_short/bcopy/zcopy` and FC pure grant, `MLX5_OPCODE_RDMA_READ` from `get_*`/`flush_remote`, atomics, and `put_zcopy` via the `out:` path above. The function returns `ucs_status_t` but never returns an error — why not return `UCS_ERR_UNSUPPORTED` and let the caller skip the op it cannot describe? If the caller really does pre-filter WQEs by send-op handler, a short comment stating that contract would help.


Coverage gaps: the two new gtest cases only run with the out-of-tree ext plugin, and `put_short` is capped at 64 bytes so only the inline path is exercised (no DM, no zero-length).


### jeynmann · 2026-09-11

Will rebase after [PR#11857](https://github.com/openucx/ucx/pull/11857) and  [PR#11874](https://github.com/openucx/ucx/pull/11874) merged

### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 0f830426c70d48defdc27f4b82f122dab2f3c940 -->

### svc-nvidia-pr-review · 2026-09-16


Residual coverage note: `put_short` is capped at 64 bytes in the new test, so only the inline path runs; the DM `put_short` path and the zero-length path are still uncovered in-tree, and `put_bcopy` only exercises the non-DM branch. (Previously raised; noting for completeness. Note also that the earlier reviews' claim that these tests need an out-of-tree ext plugin no longer holds — `ep_outstanding_purge` is wired to the in-tree `uct_rc_mlx5_ep_outstanding_purge` and `UCT_IFACE_FLAG_V2_QUERY_TOKEN` is set for any DEVX md, so mlx5 CI does run them.)


### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: da0988c3eae8649cfc8040c7213f259a8cec9f5a -->

### svc-nvidia-pr-review · 2026-09-16


Unresolved earlier findings (not re-raised, would +1 the existing threads): the three `ucs_fatal()` paths reported on head `b1b1a53` are still reachable in this revision, only relocated:
- Zero-length RDMA_WRITE (`uct_ep_put_short(ep, NULL, 0, ...)` and the keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`) now returns `UCS_ERR_UNSUPPORTED` from `uct_rc_mlx5_op_info_fill_put()`, and the purge loop turns any status other than `UCS_ERR_NO_ELEM` into `ucs_fatal()`. Note the skip mechanism already exists (`UCS_ERR_NO_ELEM`), so this is now a one-line change.
- DM `put_short` (`length > UCT_IB_MLX5_PUT_MAX_SHORT(0)` with DM enabled) is still classified as put_bcopy and hits `ucs_fatal("unsupported put bcopy op with DM")` in `uct_rc_mlx5_get_dptr_buffer()`.
- `put_zcopy` still ends in the same `ucs_fatal()` via the `UCS_ERR_UNSUPPORTED` return, despite the "not supported yet" comment implying a graceful skip.


Coverage gaps (previously reported): `put_short` is capped at 64 bytes, so only the inline path runs (no DM, no zero-length); `put_bcopy` only exercises the non-DM branch.


### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 879c296b63f31b413728fb6c2345bc105e94348a -->

### svc-nvidia-pr-review · 2026-09-16


Still-present `ucs_fatal()` reachability issues (moved, not fixed): 0-length `RDMA_WRITE` (e.g. `uct_ep_put_short(ep, NULL, 0, ...)` and the keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`) makes `uct_rc_mlx5_op_info_fill_put()` return `UCS_ERR_UNSUPPORTED`, and the purge loop turns any status other than `UCS_ERR_NO_ELEM` into `ucs_fatal()`.


DM `put_short` (`length > UCT_IB_MLX5_PUT_MAX_SHORT(0)` with DM enabled) is still classified as `put_bcopy` (DM descs also use the `ucs_mpool_put` handler) and hits `ucs_fatal("unsupported put bcopy op with DM")` in `uct_rc_mlx5_get_dptr_buffer()`.


`put_zcopy` / `put_sgl_zcopy` (handler is `uct_rc_ep_send_op_completion_handler`, or no op at all when `comp == NULL`) still end in the same `ucs_fatal()` despite the "put_zcopy is not supported yet" comment.


Test coverage gaps: `put_short` is capped at 64 bytes so only the inline path runs (no DM, no 0-length); `put_bcopy` only exercises the non-DM branch; and `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` (tx mpool exhaustion) before the EP failure, so the `put_bcopy` case may post fewer ops after the flush than intended.


### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: a51a19174509587411da9462f758fb2d47db387e -->

### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: bb23d5be4de4261951bab01545ac470c5a9f24c4 -->

### svc-nvidia-pr-review · 2026-09-16


Already-reported and still unresolved: 0-length `RDMA_WRITE` (`uct_ep_put_short(ep, NULL, 0, ...)` and the keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`, which is unsignaled and stays outstanding) still returns `UCS_ERR_UNSUPPORTED` from `uct_rc_mlx5_op_info_fill_put()`, leading to `ucs_fatal()` in the purge loop.


Already-reported and still unresolved: DM `put_short` (`length > UCT_IB_MLX5_PUT_MAX_SHORT(0)` with DM enabled; the DM desc handler is also `ucs_mpool_put`, `rc_mlx5.inl:1762`) is still classified as `put_bcopy` and hits `ucs_fatal("unsupported put bcopy op with DM")`.


Already-reported and still unresolved: undelivered `put_zcopy`/`put_sgl_zcopy` still hits the same `ucs_fatal()` despite the "not supported yet" comment.


Residual test coverage gaps: `put_short` is capped at 64 bytes, so only the inline path runs (no DM, no 0-length); `put_bcopy` only exercises the non-DM branch; `post_until_error()` can still exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion before the EP failure, so `num_ops_posted_after_flush` may be smaller than intended for `put_bcopy`.


### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: fe53700fdd5ed019770f70c52881e675aec21fc2 -->

### svc-nvidia-pr-review · 2026-09-16


Already reported, still present: 0-length RDMA_WRITE (`uct_ep_put_short(ep, NULL, 0, ...)` and the unsignaled keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`) makes `uct_rc_mlx5_op_info_fill_put()` return `UCS_ERR_UNSUPPORTED`, leading to `ucs_fatal()` in the purge loop.


Already reported, still present: DM `put_short` (`length > UCT_IB_MLX5_PUT_MAX_SHORT(0)`, DM desc handler is also `ucs_mpool_put`, rc_mlx5.inl:1762) is classified as put_bcopy and hits `ucs_fatal("unsupported put bcopy op with DM")`.


Already reported, still present: `put_zcopy`/`put_sgl_zcopy` (handler `uct_rc_ep_send_op_completion_handler`, or no op at all when `comp == NULL`) hit the same `ucs_fatal()` despite the "not supported yet" comment.


Already reported, still present: `MLX5_OPCODE_SEND` with a data segment (am_bcopy/am_zcopy), `RDMA_READ`, and atomics reach `uct_rc_mlx5_wqe_unsupported()` fatal.


`test/gtest/uct/test_peer_failure.cc`

Residual coverage gaps (previously reported): `put_short` is capped at 64 bytes so only the inline path runs (no DM, no 0-length); `put_bcopy` only exercises the non-DM branch; `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion, so `num_ops_posted_after_flush` may be smaller than intended for `put_bcopy`.


### svc-ucx · 2026-09-16

🤖 **CI Triage Agent** — `UCX PR (Tests roce on worker 0)` · commit `fe53700f`

**TL;DR:** The `roce on worker 0` job failed in the `test_ucm_hooks` step: `./test/apps/test_hooks -n 58` died with SIGILL (core dump) on iteration 7/30 — the known UCM bistro code-patching race under heavy thread oversubscription, unrelated to PR #11836 (mlx5 PUT WQE parser). Re-run the job; if it recurs, harden the test by raising `UCX_BISTRO_GRACE_DURATION` and/or capping the thread count.

<details><summary>Full analysis</summary>

**Summary:** `contrib/test_jenkins.sh` → `test_ucm_hooks` aborted the build: `./contrib/test_jenkins.sh: line 1077: 2408388 Illegal instruction  timeout 10 ./test/apps/test_hooks -n $threads` / `timeout: the monitored command dumped core`, exit 1.

**Root cause:** All PR-relevant work in the log passed (ucx_info, release gtest `test_obj_size.size` PASSED). The only failure is the UCM bistro hook stress test at iteration 7/30 with **58 threads** (`threads=$(((RANDOM % (2 * NPROC)) + 1))`, i.e. up to 2x oversubscription). `test_hooks` spawns N threads that hammer `mmap()` while the main thread `dlopen()`s libucm to trigger bistro patching of `mmap`. Patching is only *quasi*-atomic (`src/ucm/bistro/bistro.c:70-103`): it writes a 2-byte self-jump lock (`eb fe`, `ucm_bistro_patch_lock`, `bistro_x86_64.c:309`), spins for `ucm_global_opts.bistro_grace_duration` doing `sched_yield()`, then `memcpy`s bytes `2..len` of the jump patch over live code and finally unlocks with an atomic 2-byte swap. A thread that was preempted *inside* the patched prologue (past the first 2 bytes) for longer than the grace window resumes on half-rewritten instruction bytes and executes garbage → SIGILL. With 58 runnable threads on a shared CI agent, `sched_yield()`-based grace is easily exceeded, which is exactly the flakiness the grace window was added to mitigate (commit c77bb48b "UCM/BISTRO: Increase grace time to reduce patch race"). PR #11836 touches only UCT IB/mlx5 PUT WQE code and cannot affect UCM bistro patching.

Secondary (non-fatal, but a real bug in the log): at 13:24:51 the earlier step printed `./test/apps/test_ucp_config: error: .../build-test/test/apps/.libs/test_ucp_config' does not exist` — the libtool wrapper ran without its real binary being built, and the failure was silently swallowed because only `grep` output is checked (`contrib/test_jenkins.sh:1066-1071`).

**Implicated commit:** Not the PR commit (fe53700f). The failing mechanism originates from 676b5792 (Thomas Vegas, "UCM/BISTRO: Add locking step while patching library functions") and c77bb48b (shasson5, "UCM/BISTRO: Increase grace time to reduce patch race", #11112).

**File:** `src/ucm/bistro/bistro.c:86-97` (grace window + non-atomic payload memcpy); test driver `contrib/test_jenkins.sh:1074-1091`

**Suggested fix:**
1. Re-run the failed job — this is an infrastructure/flaky failure, not a regression from #11836; do not block the PR on it.
2. To stop the recurring flake in CI, in `test_ucm_hooks` export a larger grace window and reduce oversubscription, e.g.:
   - `threads=$(((RANDOM % ${NPROC}) + 1))` instead of `2 * ${NPROC}`, and
   - run with `UCX_BISTRO_GRACE_DURATION` bumped (e.g. `env UCX_BISTRO_GRACE_DURATION=100ms timeout 10 ./test/apps/test_hooks -n $threads`).
3. Longer term, make the payload write not observable mid-flight (e.g. force the 5-byte near-jump path so only one instruction is replaced, or restore-then-patch with all racing threads parked), since the grace-period approach is inherently probabilistic.
4. Separately, fix/flag the missing `test/apps/.libs/test_ucp_config` binary and make that step fail loudly instead of relying solely on `grep` output.

**Related:** PR https://github.com/openucx/ucx/pull/11836 (the PR under test); PR https://github.com/openucx/ucx/pull/11112 (UCM/BISTRO grace-time increase for this same race)

</details>

<!-- triage-agent request_id: 01af65c9-2762-4520-b76e-67403d124ff1 -->
<!-- triage-agent gha_run_id: azure:136289 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 1b003ca383efe2f67d2b432821c4ec6cc5d9e2b5 -->

### svc-nvidia-pr-review · 2026-09-16


Previously reported and still unresolved (`ucs_fatal()` reachability): zero-length `RDMA_WRITE` (`uct_ep_put_short(ep, NULL, 0, ...)` and the unsignaled keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`); `put_zcopy`/`put_sgl_zcopy` return `UCS_ERR_UNSUPPORTED` from `uct_rc_mlx5_op_info_fill_put()` and hit the `ucs_fatal()` in the purge loop; DM `put_short` is still classified as put_bcopy and hits `ucs_fatal("unsupported put bcopy op with DM")`; `am_bcopy`/`am_zcopy`, `RDMA_READ` and atomics still reach `uct_rc_mlx5_wqe_unsupported()`.


Residual coverage gaps (also previously reported): `put_short` is capped at 64 bytes so only the inline path runs; `put_bcopy` only exercises the non-DM branch; `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion, so `num_ops_posted_after_flush` may be smaller than intended for `put_bcopy`.


### svc-nvidia-pr-review · 2026-09-16

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 47778b6d69a7a478b67e3b04719c19593365b9c8 -->

### svc-ucx · 2026-09-16

🤖 **CI Triage Agent** — `UCX PR (Tests BlueField on worker 0)` · commit `47778b6d`

**TL;DR:** The BlueField gtest run failed on a single UCP fault-tolerance test, `rcx/test_ucp_fault_tolerance.probe_gated_recovery/6 (rc_x/AM)`, which is unrelated to this PR's UCT/IB/MLX5 PUT WQE-parser change; the test's `EXPECT_TRUE(probe_armed)` check samples a transient state (`recovery_arg->probe[lane].comp.func`) only between progress loops, so it fails whenever the aux probe is armed and reset inside one progress round. Fix by making probe arming observable deterministically (mock/hold the probe, or add a sticky counter) instead of polling a transient field.

<details><summary>Full analysis</summary>

**Summary:** `make test` in the BlueField worker-0 job exited 2 because 1 of 8688 gtests failed: `rcx/test_ucp_fault_tolerance.probe_gated_recovery/6, where GetParam() = rc_x/AM` (all other tests, including `initiator_failure/6` and `target_failure/6` of the same suite, passed).

**Root cause:** Race in the test's verification logic, not in the transport path touched by the PR. `probe_gated_recovery` (test/gtest/ucp/test_ucp_fault_tolerance.cc:913-943) polls `ep->ext->recovery_arg->probe[lane].comp.func != NULL` once per `short_progress_loop()` while simultaneously waiting for `ucp_ep_get_failed_lanes(ep) == 0`. In the library, the aux probe is transient:
- `ucp_ep_recovery_arm_probe()` sets `comp.func` and, if `uct_ep_check()` does not return `UCS_INPROGRESS`, immediately completes it (src/ucp/core/ucp_ep.c:2071-2096);
- on success `ucp_ep_recovery_rebuild_p2p_lane()` clears it again via `ucp_ep_recovery_reset_probe()` in the *same* call (src/ucp/core/ucp_ep.c:2182-2193);
- once all lanes are back, `ucp_ep_recovery_arg_free()` NULLs `recovery_arg` (src/ucp/core/ucp_ep.c:2411-2416).

So if arm+complete+reset (or full recovery) happens between two samples, `probe_armed` stays `false` and the non-fatal `EXPECT_TRUE(probe_armed)` at line 941 fires ("RC p2p lane recovery completed without arming an aux probe") even though recovery succeeded. This is consistent with the fact that the sibling variants exercising the same `test_am_with_injected_failure()` + recovery-wait path all passed, and with PR #11836 ("UCT/IB/MLX5: wqe parser for uct put") not touching UCP recovery at all. Note: the Azure log tool only returns the tail of the step log, so the individual assertion text for this test had already scrolled out; the diagnosis is derived from the failure identity plus the test/library source.

**Implicated commit:** db208ee4 "UCP/FT: probe-gated lane recovery via aux uct_ep_check (#11563)", Evgeny Leksikov (added both the probe logic and this test); not commit 47778b6d of this PR.

**File:** test/gtest/ucp/test_ucp_fault_tolerance.cc:922-943 (check), with library side at src/ucp/core/ucp_ep.c:2071-2096 and 2182-2193

**Suggested fix:** Make the probe observable deterministically instead of polling a transient field: use the existing `ucs::mock` + `recovery_probe_hold` pattern (as in `teardown_with_outstanding_probe`) to guarantee an in-flight probe, count arm events in the mock, then release it with `mock_invoke_completion(UCS_OK)` and assert recovery; or add a sticky counter (e.g. a `probes_armed` field in `ucp_ep_recovery_arg_t`/worker counters incremented in `ucp_ep_recovery_arm_probe()`) and assert on that. Meanwhile, re-run the BlueField job for PR #11836 — this failure should not block it.

**Related:** PR #11836 (this PR, UCT/IB/MLX5 wqe parser for uct put); PR #11563 / commit db208ee4 (introduced probe-gated recovery + this test); prior instability in this suite: #11397 "TEST/GTEST: Disable failing fault-tolerance test", #11405 "GTEST: fix err counting in test_ucp_fault_tolerance".

</details>

<!-- triage-agent request_id: c460c5b6-c554-4eb3-ab3b-3805cd547309 -->
<!-- triage-agent gha_run_id: azure:136351 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ea0df2f8057fb0e0d464b51f92f074c036b45e94 -->

### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ec102e41b8ade6e672c08ead795e338db0bad73d -->

### svc-nvidia-pr-review · 2026-09-17


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

blocker: this assert (`ucs_assert(UCS_CIRCULAR_COMPARE16(op->sn, >=, ci))` in `uct_rc_mlx5_ep_outstanding_get_send_op`) can fire. `uct_rc_mlx5_ep_outstanding_release_send_op()` removes only ops whose handler is `ucs_mpool_put`, so a *delivered* put_zcopy/put_sgl_zcopy op (handler `uct_rc_ep_send_op_completion_handler`, `sn` == its own WQE index, e.g. `sn = ep->tx.wq.sw_pi` in `uct_rc_mlx5_base_ep_zcopy_post()` or `txwq->sig_pi` in `put_sgl_zcopy`) stays at the queue head and the next iteration sees `op->sn < ci`. With asserts disabled it is silent but worse: the stale head makes `get_send_op()` return `NULL` for every later WQE, so a subsequent undelivered put_bcopy is classified as "no op" and hits the `ucs_fatal()` in the purge loop.


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

In `uct_rc_mlx5_op_info_fill_rma_raddr`, the raddr segment does not always hold the user's `remote_addr`/`rkey`: for a fenced put, `uct_rc_mlx5_ep_fence_put()` calls `uct_ib_resolve_atomic_rkey()`, putting the indirect atomic mkey in the WQE and shifting `remote_addr` by `ep->super.atomic_mr_offset`, so `info->rma.remote_addr = be64toh(raddr->raddr)` / `rma.rkey` differ from what the caller passed. This triggers on the first put after any `uct_iface_flush()` when relaxed ordering is on (`uct_rc_iface_flush()` -> `uct_rc_iface_fence_relaxed_order()` bumps the fence beat, `fence_mode` resolving to `WEAK`), and it is not detectable from the WQE since `put_fence_flag` is 0 unless strong ordering is required — how should we handle it?


### svc-ucx · 2026-09-17

🤖 **CI Triage Agent** — `UCX PR (go gpu)` · commit `ec102e41`

**TL;DR:** The "go gpu" job's `make -C build/bindings/go test` step was killed by Go's default 10-minute test timeout while `TestUcpContext` sat blocked in its very first cgo call, `ucp_config_read()` — a hang (600 s of zero output) in UCX config-file reading on a contended/shared agent, not slowness and not related to PR #11836's mlx5 WQE-parser change; re-run and investigate the agent host rather than raising the timeout.

<details><summary>Full analysis</summary>

**Summary:** `Run go tests` failed with `panic: test timed out after 10m0s` — `FAIL github.com/openucx/ucx/bindings/go/tests 600.528s`, `make: *** [Makefile:670: test] Error 1`.

**Root cause:** Timestamp analysis shows one single gap covering the whole runtime: `03:09:41.4 === RUN TestUcpContext` → `03:19:41.5 panic: test timed out`. Nothing ran in between, so the process was hung, not slow. The Go stack pins the hang precisely: goroutine 19 is in `[syscall]` inside `_Cfunc_ucp_config_read` from `bindings/go/src/ucx/context.go:38` (`ReadConfig` → `NewUcpContext` → `tests/context_test.go:17`). That is the *first* UCX API call in the process — `ucp_init()` was never reached, so no UCT component, no IB/mlx5 code and none of the PR's `put` WQE-parser code is loaded or executed at that point. The only blocking operation on this path is `ucs_config_parser_fill_opts()` → `UCS_INIT_ONCE(config_file_parse) { ucs_config_parse_config_files() }` (src/ucs/config/parser.c:2001-2063), which does serial blocking `fopen()` calls on `/etc/ucx/ucx.conf`, `<libdir>/../etc/ucx/ucx.conf`, `$HOME/ucx.conf`, `$UCX_CONFIG_DIR/ucx.conf` and `./ucx.conf` — an unresponsive network mount (`$HOME` / the `/hpc/local/oss` NFS tree used by the loaded `gdrcopy`/`go` modules) blocks there uninterruptibly. Supporting environment evidence in the same log: the `nvidia-smi -q` dump at 03:09:38 shows a *foreign* leftover process `/__w/1/s/build-test/test/gtest/gtest` (PID 2551560, a container-agent workspace path, while this job runs from `[REDACTED:Base64 High Entropy String]`) holding 260–292 MiB on both A40s with compute mode `Exclusive_Process` plus an MPS server — i.e. the host was shared and had stale test processes. This is an infrastructure hang, not a regression from commit ec102e41.

**Implicated commit:** unknown — not attributable to the PR head ec102e41 (`UCT/IB/MLX5: wqe parser for uct put`); the failing code path never touches uct/ib/mlx5.

**File:** bindings/go/src/ucx/context.go:38 (hang site) → src/ucs/config/parser.c:2001-2063 (`ucs_config_parse_config_files`, blocking `fopen`s)

**Suggested fix:** Do **not** raise the Go test timeout — it would only lengthen the hang. Actions: (1) re-run the "go gpu" job; this signature is environmental and should not reproduce on a healthy agent. (2) On agent-06's host, check for stale/hung mounts (`timeout 5 ls $HOME`, `/hpc/local/oss`) and reap the leftover `gtest`/`nvidia-cuda-mps-server` processes; add a pre-test cleanup/health-check step in `buildlib` so a wedged mount or orphaned GPU process fails fast with a clear message instead of hanging. (3) Hardening so the next occurrence is diagnosable: pass an explicit `-timeout` and run with `GOTRACEBACK=crash` (plus `UCX_LOG_LEVEL=debug`, which prints `parsed config file <path>` and identifies the last file opened) in `bindings/go/Makefile.am:45`, and/or wrap the step in `timeout` + `gcore`/`gdb -p` capture of the hung test binary to get the C stack.

**Related:** PR under test: https://github.com/openucx/ucx/pull/11836 (unrelated to the failure). No existing issue found for a `ucp_config_read` hang in the go bindings — worth opening one for CI-agent hardening.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Base64 High Entropy String). See request_id `88586b1f-8329-4d6a-ba80-8a43f29ae514` in the triage console for the audit trail.

<!-- triage-agent request_id: 88586b1f-8329-4d6a-ba80-8a43f29ae514 -->
<!-- triage-agent gha_run_id: azure:136392 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 4025b148ecdb58ca81539e3aebf813ec5608ef08 -->

### svc-nvidia-pr-review · 2026-09-17


Already-reported, still present: `uct_rc_mlx5_ep_outstanding_get_send_op()` — `ucs_assert(UCS_CIRCULAR_COMPARE16(op->sn, >=, ci))` and the "stale head ⇒ `op == NULL` ⇒ `ucs_fatal()`" consequence are unchanged in this revision.


Already-reported, still present: `uct_rc_mlx5_op_info_fill_rma_raddr()` — fenced put still reports the indirect atomic mkey / shifted `remote_addr` (`uct_rc_mlx5_ep_fence_put()` → `uct_ib_resolve_atomic_rkey()`); the new `put_short`/`put_bcopy` tests would fail on a relaxed-ordering MD, since `init()` calls `flush()` which bumps the fence beat.


Already-reported, still present: the `ucs_fatal()` reachability set (zero-length RDMA_WRITE including the `post_check` keepalive WQE, DM `put_short` classified as put_bcopy via the shared `ucs_mpool_put` handler at `rc_mlx5.inl:1762`, `put_zcopy`/`put_sgl_zcopy`, and `SEND`+dptr / `RDMA_READ` / atomics via `uct_rc_mlx5_wqe_unsupported()`).


Note / no action: `uct_ib_mlx5_wqe_opcode()`, the `*_const` wrap helpers and `uct_ib_mlx5_txwq_copy_segs()` are equivalent to what they replace; `src <= qend` holds for every caller (`wrap_any`), so the `ucs_min()` signed/unsigned mix is safe.


Note / no action: the `put_short` inline path cannot exceed one packet (`UCT_IB_MLX5_PUT_MAX_SHORT(0)` = 208 < 256 = minimum path MTU), so `uct_rc_mlx5_num_packets()` on the inline length agrees with the single PSN added by `uct_rc_mlx5_txqp_inline_post()`.


Note / no action: the `memcpy` into `callback_data.data[256]` is bounded only indirectly, by the `ucs_assertv_always(length <= UCT_IB_MLX5_MAX_SEND_WQE_SIZE)` in `uct_rc_mlx5_wqe_put_length()`, which always runs first for the same WQE in the purge loop — correct, but the bound is far from the copy.


Coverage note: `test_uct_sgl` clamps element sizes with `elem_size()` (never 0), so the `ucs_max(1, ...)` PSN fix has no in-tree test; `put_short` is capped at 64 bytes (inline path only, no DM, no zero length) and `put_bcopy` exercises only the non-DM branch. `post_until_error()` can also exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion for `put_bcopy` (previously reported).


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 691b6bf194fa0b458abf9367e0a677d2e4461778 -->

### svc-nvidia-pr-review · 2026-09-17


The already-reported blockers are all still present in this revision: `ucs_fatal()` reachability for 0-length RDMA_WRITE / DM `put_short` / `put_zcopy` / `SEND`+dptr / `RDMA_READ` / atomics, the `op->sn >= ci` assert with a stale zcopy head, and the fenced-put raddr reporting. I would +1 those existing threads rather than open new ones.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ad3a06dc727d848d8c223850a16b0bf5060e5f9b -->

### svc-nvidia-pr-review · 2026-09-17


Residual coverage gaps: `put_short` is still capped at 64 bytes (inline path only, no DM, no zero length); `put_bcopy` exercises only the non-DM branch; and nothing in-tree covers the DC path broken by the finding above — it is only reachable through the existing `dc_mlx5` zcopy tests, which is where the assert will fire.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 151df83d253944dda8ce104ebdc8bec2b2ff4a4d -->

### svc-nvidia-pr-review · 2026-09-17


Previously reported and still present: `ucs_fatal()`/`UCS_ERR_UNSUPPORTED` paths remain reachable - 0-length `RDMA_WRITE` (`uct_ep_put_short(ep, NULL, 0, ...)` and the unsignaled keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`), DM `put_short` (classified as put_bcopy via the shared `ucs_mpool_put` handler, `rc_iface.h:555`), `put_zcopy`/`put_sgl_zcopy`, and `SEND`+dptr / `RDMA_READ` / atomics via `uct_rc_mlx5_wqe_unsupported()`.


Previously reported and still present: in `uct_rc_mlx5_ep_outstanding_get_send_op()`, `ucs_assert(UCS_CIRCULAR_COMPARE16(op->sn, >=, ci))` still fires when a non-put_bcopy op (zcopy `uct_rc_ep_send_op_completion_handler`) stays at the queue head, since `release_send_op()` only pulls `ucs_mpool_put` ops.


Previously reported and still present: `uct_rc_mlx5_op_info_fill_rma_raddr()` - a fenced put still reports the indirect atomic mkey / shifted `remote_addr`.


Coverage gaps (previously reported, for completeness): `put_short` is capped at 64 bytes so only the inline path runs (no DM, no zero-length); `put_bcopy` exercises only the non-DM branch; `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion for `put_bcopy`.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 66808ae02f66d8c437f87e366538ecbf8f4f65ed -->

### svc-nvidia-pr-review · 2026-09-17


Zero-length `RDMA_WRITE` (`uct_ep_put_short(ep, NULL, 0, ...)` and the unsignaled keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`) still returns `UCS_ERR_UNSUPPORTED` from `uct_rc_mlx5_op_info_fill_put()` and lands in the purge-loop `ucs_fatal()`; `UCS_ERR_NO_ELEM` already exists as the "skip" status.


DM `put_short` is still classified as put_bcopy (DM descs share the `ucs_mpool_put` handler) and hits `ucs_fatal("unsupported put bcopy op with DM")` in `uct_rc_mlx5_get_dptr_buffer()`.


`put_zcopy`/`put_sgl_zcopy` still end in the same `ucs_fatal()`; `am_bcopy`/`am_zcopy`, `RDMA_READ` and atomics still reach `uct_rc_mlx5_wqe_unsupported()`.


`uct_rc_mlx5_ep_outstanding_get_send_op()`: `ucs_assert(UCS_CIRCULAR_COMPARE16(op->sn, >=, ci))` still fires when a *delivered* zcopy op (handler `uct_rc_ep_send_op_completion_handler`) stays at the queue head, since `release_send_op()` only pulls `ucs_mpool_put` ops.


`uct_rc_mlx5_op_info_fill_rma_raddr()` still reports the indirect atomic mkey / shifted `remote_addr` for a fenced put.


Residual coverage gaps: `put_short` is capped at 64 bytes so only the inline path runs (no DM, no zero-length); `put_bcopy` exercises only the non-DM branch; `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` (QP/desc exhaustion) before the EP failure, so `num_ops_posted_after_flush` may be smaller than intended.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: aa83d60b3cc766d6714f0dfa5530ecad4a437423 -->

### svc-nvidia-pr-review · 2026-09-17


Residual coverage gaps (previously reported, listed only for completeness): `put_short` is capped at 64 bytes so only the inline path runs (no DM, no zero-length); `put_bcopy` exercises only the non-DM branch; `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion, so `num_ops_posted_after_flush` may be smaller than intended.


Previously reported blockers remain unchanged in this revision and should be addressed: the `ucs_fatal()`/`UCS_ERR_UNSUPPORTED` reachability set, the `op->sn >= ci` assert with a stale non-`ucs_mpool_put` head, and the fenced-put `raddr` reporting.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 930c7ce0500831bfd4245d77c2ebacae194fd004 -->

### svc-nvidia-pr-review · 2026-09-17


`uct_rc_mlx5_op_info_fill_put()` returns `UCS_ERR_UNSUPPORTED` for zero-length `RDMA_WRITE` (e.g. `uct_ep_put_short(ep, NULL, 0, ...)`, and the unsignaled keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`), and the purge loop turns any status other than `UCS_ERR_NO_ELEM` into `ucs_fatal()`.


DM `put_short` is still classified as put_bcopy (DM descs share the `ucs_mpool_put` handler) and hits `ucs_fatal("unsupported put bcopy op with DM")` in `uct_rc_mlx5_get_dptr_buffer()`.


`put_zcopy`/`put_sgl_zcopy` reach the same purge-loop `ucs_fatal()`; `am_bcopy`/`am_zcopy` (`SEND` + dptr), `RDMA_READ` and atomics still reach `uct_rc_mlx5_wqe_unsupported()`.


`uct_rc_mlx5_ep_outstanding_get_send_op()` still assumes the queue head belongs to `ci` or later, which breaks once a non-`ucs_mpool_put` op (zcopy) is left at the head by `release_send_op()`. Additional evidence: the rewritten `uct_rc_mlx5_ep_purge_flushes()` now extracts every op with `sn <= ci` and guards the type with `ucs_assertv_always()`, so the "op left at the queue head" case is now an abort in release builds as well, not only under `ENABLE_ASSERT`.


`uct_rc_mlx5_op_info_fill_rma_raddr()` still reports the indirect atomic mkey / shifted `remote_addr` for a fenced put.


Residual test coverage gaps (previously reported): `put_short` is capped at 64 bytes so only the inline path runs (no DM, no zero length); `put_bcopy` exercises only the non-DM branch; `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion, so `num_ops_posted_after_flush` may be smaller than intended for `put_bcopy`.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ae5fec42c287eea33330a9d69fc6e01d7a7b6586 -->

### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 9e96e01acbe4b1d1bfd3326869a02983e4f74908 -->

### svc-nvidia-pr-review · 2026-09-17


Previously reported and still present in this revision (would +1 the existing threads rather than open new ones): zero-length `RDMA_WRITE` (`uct_ep_put_short(ep, NULL, 0, ...)`, `post_check` keepalive WQE), DM `put_short` classified as put_bcopy, undelivered `put_zcopy`, and `SEND`+dptr / `RDMA_READ` / atomics all still end in `ucs_fatal()`; the `op->sn >= ci` assert and the fenced-put `raddr` reporting are unchanged.


Residual coverage gaps: the new `put_short` test case is capped at 64 bytes (inline path only — no DM, no zero length) and `put_bcopy` covers only the non-DM branch; nothing in-tree covers a `put_zcopy` WQE in the purge range, which is where the blocker above triggers.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 788ae7b95bc19a425090fec7305b840f4b600ef8 -->

### svc-nvidia-pr-review · 2026-09-17


Previously reported, still present: zero-length `RDMA_WRITE` (`uct_ep_put_short(ep, NULL, 0, ...)`, and the unsignaled keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`) hits `ucs_fatal("no-payload rdma write is not supported")` in `uct_rc_mlx5_op_info_fill_put()`.


Previously reported, still present: DM `put_short` (`length > UCT_IB_MLX5_PUT_MAX_SHORT(0)` with DM enabled; DM descs share the `ucs_mpool_put` handler, `rc_mlx5.inl:1762`) is classified as put_bcopy and hits `ucs_fatal("unsupported put bcopy op with DM")`.


Previously reported, still present: undelivered `put_zcopy`/`put_sgl_zcopy` → `ucs_fatal("put zcopy is not supported")`; `am_bcopy`/`am_zcopy` (`SEND`+dptr), `RDMA_READ` and atomics → `uct_rc_mlx5_wqe_unsupported()`.


Previously reported, still present: `uct_rc_mlx5_op_info_fill_rma_raddr()` reports the indirect atomic mkey and shifted `remote_addr` for a fenced put (`uct_rc_mlx5_ep_fence_put()` → `uct_ib_resolve_atomic_rkey()`), which is not detectable from the WQE.


Previously reported, still present: `uct_rc_mlx5_ep_purge_flushes()` now extracts every op with `sn <= ci` and guards the type with `ucs_assertv_always()`, so any op left at the queue head aborts in release builds as well.


Residual coverage gaps (previously reported): `put_short` is capped at 64 bytes, so only the inline path runs (no DM, no zero length); `put_bcopy` covers only the non-DM branch; nothing in-tree puts a `put_zcopy` WQE in the purge range, which is exactly where the blocker above triggers; `post_until_error()` can still exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion, so `num_ops_posted_after_flush` may be smaller than intended for `put_bcopy`.


### svc-nvidia-pr-review · 2026-09-17

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 563a2fe236b7b920ac2213318f6983f8ce4e70b8 -->

### svc-nvidia-pr-review · 2026-09-17


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

minor: the new `ucs_assertv_always(num_packets != 0, ...)` in `uct_rc_mlx5_ep_outstanding_purge()` can never fire — `uct_ib_mlx5_wqe_num_packets()` returns 0 only for `MLX5_OPCODE_NOP`, which is already handled by the `continue` above. Consider dropping it; otherwise please remove the trailing space in the message:

```suggestion
        ucs_assertv_always(num_packets != 0,
                           "ep %p qp 0x%x unexpected 0-packet WQE opcode 0x%x",
                           ep, txwq->super.qp_num,
                           uct_ib_mlx5_wqe_opcode(ctrl));
```


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `ucs_fatal("no-payload rdma write is not supported")` is reachable — from `uct_ep_put_short(ep, NULL, 0, ...)` (as used by `test_uct_peer_failure.peer_failure`) and from the unsignaled keepalive WQE in `uct_rc_mlx5_base_ep_post_check()`.


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `ucs_fatal("unsupported put bcopy op with DM")` is reachable — a DM `put_short` (`length > UCT_IB_MLX5_PUT_MAX_SHORT(0)`) gets classified as put_bcopy because DM descs share the `ucs_mpool_put` handler.


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `ucs_fatal("put zcopy is not supported")` and `uct_rc_mlx5_wqe_unsupported()` for `SEND`+dptr / `RDMA_READ` / atomics.


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `uct_rc_mlx5_op_info_fill_rma_raddr()` reports the indirect atomic mkey and a shifted `remote_addr` for a fenced put; the new `put_short`/`put_bcopy` tests would fail on a relaxed-ordering MD, since `init()` calls `flush()` which bumps the fence beat.


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `uct_rc_mlx5_ep_outstanding_get_send_op()` still assumes the queue head belongs to `ci` or later. New detail on this revision: `release_send_op()` now unconditionally `ucs_mpool_put()`s whatever `get_send_op()` returned instead of calling `op->handler`, so a *delivered* `put_zcopy` op at `sn == ci` trips `ucs_assert(is_put_bcopy)` under `ENABLE_ASSERT` and silently drops the user completion otherwise. Same root cause as the existing thread.


`test/gtest/uct/test_peer_failure.cc`

Residual coverage gaps (previously reported): `put_short` is capped at 64 bytes so only the inline path runs (no DM, no zero length); `put_bcopy` covers only the non-DM branch; nothing in-tree places a `put_zcopy` WQE in the purge range; and `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion, so `num_ops_posted_after_flush` may be smaller than intended for `put_bcopy`.


### svc-nvidia-pr-review · 2026-09-18

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ed48cbb582316496439e50029e57895a2b0ff8aa -->

### svc-nvidia-pr-review · 2026-09-18


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

`uct_rc_mlx5_ep_outstanding_release_send_op()`: the release-build consequence is worse than "silently drops the user completion" — it is memory corruption. `uct_rc_mlx5_ep_outstanding_get_send_op()` returns *any* non-flush op whose `sn == pi`, and zcopy ops are not mpool objects: `uct_rc_iface_get_send_op()` pops them from `iface->tx.free_ops`, which is the `ucs_calloc()`'d `iface->tx.ops_buffer` array (`rc_iface.c:449`, `rc_iface.h:271`), and they are freed with `uct_rc_iface_put_send_op()`. `ucs_mpool_put(op)` on such an op reads `((ucs_mpool_elem_t*)op - 1)->mpool` (`mpool.inl:82`) — bytes belonging to the previous array element — and then writes through that garbage pointer in `ucs_mpool_add_to_freelist()`. Concrete trigger: `uct_rc_mlx5_base_ep_put_sgl_zcopy()` registers one op with `sn = txwq->sig_pi` = the *last* WQE of the batch, so the last SGL WQE in the purge range hands that op to `release_send_op()`. Routing the release through `op->handler` (as `uct_rc_txqp_purge_outstanding()` does) would avoid this class of bug entirely.


`test/gtest/uct/test_peer_failure.cc`

In `validate_put()`: why compare only the low 32 bits?
```suggestion
        EXPECT_EQ(ctx->rkey, info->rma.rkey);
```
`uct_rc_mlx5_op_info_fill_rma_raddr()` packs the reported rkey with `UCT_IB_INVALID_MKEY` as the atomic half, so `info->rma.rkey` is not the value the caller passed. If that is intentional, please document it in the callback contract (and keep the mask with a short comment) rather than hiding it in the test.


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

`uct_rc_mlx5_wqe_put_length()` / `uct_rc_mlx5_op_info_fill_put()`: maybe extract one parse helper? Both functions recompute `header_size`, re-wrap `ctrl + 1` to `raddr` and re-run `uct_rc_mlx5_wqe_inline_seg(raddr + 1, ...)` for the same WQE, and they disagree on the `wqe_size == header_size` case (returns 0 vs `ucs_fatal()`), which makes the no-payload behavior harder to follow.


Residual gaps (already reported, listed for completeness): `put_short` is capped at 64 bytes so only the inline path runs (no DM, no zero length); `put_bcopy` covers only the non-DM branch; `post_until_error()` still exits on `UCS_ERR_NO_RESOURCE` from TX-resource exhaustion, so `num_ops_posted_after_flush` may be smaller than intended; nothing in-tree places a `put_zcopy`/`put_sgl_zcopy` WQE in the purge range, which is exactly where the corruption above triggers.


### svc-ucx · 2026-09-18

🤖 **CI Triage Agent** — `UCX PR (AddressSanitizer roce on worker 1)` · commit `ed48cbb5`

**TL;DR:** `dcx/test_ucp_sockaddr.onesided_disconnect_bidi/8 <dc_x/stream,sa_data_v1>` hung for ~11 minutes waiting for UCP stream data that never arrived, blowing the gtest deadline and then the global "Connection timed out - abort testing" SIGABRT; the DC/mlx5 send path reworked on this branch (txwq producer-index / flush-WQE handling) never delivered or completed the send.

<details><summary>Full analysis</summary>

**Summary:** ASAN/RoCE gtest aborted (`make: *** [Makefile:4690: test] Aborted (core dumped)`) after `dcx/test_ucp_sockaddr.onesided_disconnect_bidi/8` stalled in `ucp_stream_worker_poll()` and hit both the per-test deadline and the global test timeout.

**Root cause:** This is a hang, not a slow test. Timeline from the log:
- `04:12:06.023` – `[ RUN ] dcx/test_ucp_sockaddr.onesided_disconnect_bidi/8 <dc_x/stream,sa_data_v1>`, "server listening on 1.1.1.1:53998"
- **silence for 11m16s** (676 s ≈ the ASAN-scaled `ucs::get_deadline()`)
- `04:23:22.686` – `test_ucp_sockaddr.cc:466: Expected equality of these values: 1 / ep_count Which is: 0`
- another 3m43s gap → `04:27:05.624` – `test_helpers.cc:57: Failed / Connection timed out - abort testing`, signal 6, core dumped.

The 676 s gap is the polling loop at `test_ucp_sockaddr.cc:462-465`: it calls `progress()` + `ucp_stream_worker_poll()` until the deadline and never saw a single ep with received stream data (`ep_count == 0`). So the stream payload sent over the `dc_x` transport was never delivered/completed — the UCT send queue stalled. The two follow-on failures at lines 467-468 (`poll_eps.ep` = `0x60f000133c80`, `poll_eps.user_data` = `0xbe7202441d4b000`) are *noise*: `poll_eps` is an uninitialized stack variable that is only filled when `ep_count > 0`, so those values are garbage and should not be chased.

The branch under test (`failover_uct_rcx_put`, PR #11836) rewrites exactly the common mlx5 TX post path that carries UCP stream/AM traffic for RC **and DC**. HEAD commit `ed48cbb5` renames the txwq producer-index bookkeeping fields (`sw_pi` / `prev_sw_pi` / `sig_pi`, `src/uct/ib/mlx5/ib_mlx5.h:687-708`), and the immediately preceding commits on the same branch touch send/completion accounting: `48c9a903` "fix flush leak", `[REDACTED:Hex High Entropy String]` "identify flush by opcode", `563a2fe2` "0-packet assertion", `9e96e01a` "refactor inline seg", `aa83d60b` "revert post changes" (all `rc_mlx5.inl` / `rc_mlx5_ep.c`). A PI/completion mis-accounting or a WQE mis-classified as a flush in that path produces precisely this symptom: the doorbell/completion accounting desyncs, the send never lands, and worker progress spins until the deadline.

**Implicated commit:** `ed48cbb5` "UCT/IB/MLX5: fix PI naming" (Zihao Zhao) — head of the PR branch; the stall is in the send path reworked by that commit and its immediate predecessors `48c9a903`, `[REDACTED:Hex High Entropy String]`, `9e96e01a`, `aa83d60b` (same author/branch). Not a pre-existing failure signature in this test.

**File:** `src/uct/ib/mlx5/ib_mlx5.h:687-708` (txwq `sw_pi`/`prev_sw_pi`/`sig_pi`/`hw_ci` bookkeeping) and `src/uct/ib/mlx5/rc/rc_mlx5.inl` post/flush helpers; symptom observed at `test/gtest/ucp/test_ucp_sockaddr.cc:462-468`

**Suggested fix:**
1. Audit the `sw_pi` / `prev_sw_pi` / `sig_pi` rename in `ed48cbb5` for a swapped semantic — `prev_sw_pi` (PI where the last WQE *started*) and `sw_pi` (PI for the *next* WQE) are not interchangeable; using the wrong one when computing the doorbell value, `wqe_size`/`ds`, or the signaled-completion window silently stalls the SQ.
2. Re-check `[REDACTED:Hex High Entropy String]` "identify flush by opcode" / `48c9a903` "fix flush leak": if a normal AM-short/RDMA-write WQE is classified as a flush WQE, its completion is consumed as a flush and the upper-layer request never completes. Verify DC (`dc_x`) goes through the same classification as RC — this test only failed on the DC variant.
3. Reproduce locally without the failover changes to confirm bisect:
   `./gtest --gtest_filter='dcx/test_ucp_sockaddr.onesided_disconnect_bidi/8'` with the RoCE + ASAN config, `UCX_LOG_LEVEL=trace_data` to see whether the stream send WQE is posted and whether its CQE is ever reaped.
4. Independently (test hygiene, not the root cause): initialize `ucp_stream_poll_ep_t poll_eps = {};` and make line 466 an `ASSERT_EQ` in `test/gtest/ucp/test_ucp_sockaddr.cc` so the timeout fails fast with a clean message instead of printing garbage pointers and then cascading into the global-timeout abort.
   Do **not** raise the test time limit — the log shows zero activity across the gap, so this is a hang.

**Related:** PR #11836 (this build), sibling PRs on the same rework: #11866, #11873, #11874, #11915; #11857 "UCT/IB/MLX5: Purge outstanding AM short operations" (closed) shows the same `ep_count == 0` stream-poll signature and is the closest prior art for this failure mode.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `532852c4-daef-43db-869e-39106baae562` in the triage console for the audit trail.

<!-- triage-agent request_id: 532852c4-daef-43db-869e-39106baae562 -->
<!-- triage-agent gha_run_id: azure:136614 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-18

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: eb745142919374189c754f1dd68b8b862da89350 -->

### svc-ucx · 2026-09-18

🤖 **CI Triage Agent** — `UCX PR` · commit `0207feef`

**TL;DR:** The build fails to compile because `ucs_assert_always()` was called with a printf-style message and 5 extra arguments, but that macro takes exactly one argument — the formatted variant is `ucs_assertv_always()`. Change the call at `src/uct/ib/mlx5/rc/rc_mlx5_ep.c:1222` to `ucs_assertv_always(...)`.

<details><summary>Full analysis</summary>

**Summary:** "Basic compile" stage of the Azure Pipelines PR build failed with a hard compile error in `rc/libuct_ib_mlx5_la-rc_mlx5_ep.lo` (`make[5]: *** Error 1`), aborting the whole build.

**Root cause:** In `uct_rc_mlx5_ep_outstanding_complete_send_ops()` the code uses:
```c
ucs_assert_always(uct_rc_mlx5_send_op_is_flush(op),
                  "ep %p qp 0x%x unexpected send op sn %u handler %s",
                  ep, ep->tx.wq.super.qp_num, op->sn,
                  ucs_debug_get_symbol_name(op->handler));
```
but `ucs_assert_always` is defined in `src/ucs/debug/assert.h:24` as a single-argument macro (`#define ucs_assert_always(_expression)`). GCC therefore reports `macro "ucs_assert_always" passed 6 arguments, but takes just 1`, the macro expansion is suppressed, and the identifier ends up undeclared:
```
rc_mlx5_ep.c:1225:65: error: macro "ucs_assert_always" passed 6 arguments, but takes just 1
rc_mlx5_ep.c:1222:9: error: 'ucs_assert_always' undeclared (first use in this function)
```
The message-formatting variant is `ucs_assertv_always(_expression, _fmt, ...)` (assert.h:36). This is a plain source bug in the PR branch, not an infrastructure or flaky-test issue.

**Implicated commit:** `0207feef` — "UCT/IB/MLX5: refactor purge", Zihao Zhao (the HEAD commit of branch `failover_uct_rcx_put`, PR #11836)

**File:** `src/uct/ib/mlx5/rc/rc_mlx5_ep.c:1222` (macro defined at `src/ucs/debug/assert.h:24`, correct variant at `src/ucs/debug/assert.h:36`)

**Suggested fix:** Rename the call to the variadic assert:
```c
ucs_assertv_always(uct_rc_mlx5_send_op_is_flush(op),
                   "ep %p qp 0x%x unexpected send op sn %u handler %s",
                   ep, ep->tx.wq.super.qp_num, op->sn,
                   ucs_debug_get_symbol_name(op->handler));
```
Also verify `<ucs/debug/debug_int.h>` (which declares `ucs_debug_get_symbol_name`) is included in `rc_mlx5_ep.c`, since that call was newly introduced and its declaration error would otherwise be masked by the macro failure. Note the nearby `ucs_assert(...)` at line 1208 is fine because it takes a single expression. Recommend building locally with `--enable-debug`/`-Werror` (matching this CI config) before pushing, as this error is unconditional and would break any build.

**Related:** PR #11836 (`failover_uct_rcx_put`); preceding commits on the same branch touching this file: `fe498e91`, `ed48cbb5`, `48c9a903`, `563a2fe2`

</details>

<!-- triage-agent request_id: e97cf10c-0901-4868-8a82-ee9e3fd9642b -->
<!-- triage-agent gha_run_id: azure:136676 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-18

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 0207feefe27ab34c1590061d7121ea69e95cb970 -->

### svc-nvidia-pr-review · 2026-09-18


Not re-raised (already reported, still present): the `ucs_fatal()`/`UCS_ERR_UNSUPPORTED` reachability set (0-length `RDMA_WRITE` including the `post_check` keepalive WQE, DM `put_short` classified as put_bcopy, `put_zcopy`/`put_sgl_zcopy`, `SEND`+dptr / `RDMA_READ` / atomics), the `op->sn >= pi` assumption in `uct_rc_mlx5_ep_outstanding_extract_send_op()` together with `ucs_mpool_put()` on non-mpool zcopy ops, and the fenced-put `raddr`/`rkey` reporting are unchanged in this revision — +1 to the existing threads.


Coverage gaps also unchanged: `put_short` is capped at 64 bytes (inline path only, no DM, no zero length), `put_bcopy` covers only the non-DM branch, and `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion.


### svc-nvidia-pr-review · 2026-09-20

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 8562f7ede00a6a7df554916bcb023c21918646b3 -->

### svc-nvidia-pr-review · 2026-09-20


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `ucs_fatal()` reachability for zero-length `RDMA_WRITE` (`uct_ep_put_short(ep, NULL, 0, ...)` and the unsignaled keepalive WQE from `uct_rc_mlx5_base_ep_post_check()`), DM `put_short` misclassified as put_bcopy (shared `ucs_mpool_put` handler), `put_zcopy`/`put_sgl_zcopy`, and `SEND`+dptr / `RDMA_READ` / atomics via `uct_rc_mlx5_wqe_unsupported()`.


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `uct_rc_mlx5_ep_outstanding_extract_send_op()` assumes the queue head belongs to `pi` or later, and `..._release_send_op()` calls `ucs_mpool_put()` on ops that are not mpool objects (zcopy ops come from `iface->tx.ops_buffer`).


`src/uct/ib/mlx5/rc/rc_mlx5_ep.c`

Previously reported, still present: `uct_rc_mlx5_op_info_fill_rma_raddr()` reports the indirect atomic mkey / shifted `remote_addr` for a fenced put; the test's `uint32_t(...)` rkey masking hides the packed-rkey mismatch.


`test/gtest/uct/test_peer_failure.cc`

Residual coverage gaps (previously reported): `put_short` is capped at 64 bytes so only the inline path runs (no DM, no zero length); `put_bcopy` covers only the non-DM branch; `post_until_error()` can exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion, so `num_ops_posted_after_flush` may be smaller than intended.


### svc-ucx · 2026-09-20

🤖 **CI Triage Agent** — `UCX PR (Tests BlueField on worker 0)` · commit `8562f7ed`

**TL;DR:** `rc_mlx5/test_uct_purge_outstanding.put_short/1` aborted on `ucs_assertv_always(start_pi != end_pi, ... "unexpected empty outstanding queue")` in `uct_rc_mlx5_ep_outstanding_purge()` — the new purge implementation is not idempotent: the first purge sets `txwq->ft_ci = txwq->prev_sw_pi`, so the second purge the test performs computes `start_pi == end_pi` and fatals instead of returning `UCS_OK` with nothing to purge.

<details><summary>Full analysis</summary>

**Summary:** BlueField gtest run crashed (core dumped, `make: *** [Makefile:4786: test] Aborted`) in `rc_mlx5/test_uct_purge_outstanding.put_short/1 <rc_mlx5/mlx5_1:1>` with an assertion failure in `uct_rc_mlx5_ep_outstanding_purge()`.

**Root cause:** Evidence from the log: `rc_mlx5_ep.c:1264 Assertion 'start_pi != end_pi' failed: ep 0xaaab1e9feec0 qp 0x13d5c unexpected empty outstanding queue`, with the backtrace frames pointing at `test_peer_failure.cc:637/638` — i.e. the **second** `purge_outstanding()` call in `test_purge_outstanding()`, not the first (line 623).

In `src/uct/ib/mlx5/rc/rc_mlx5_ep.c`:
- The purge loop walks `start_pi..end_pi` and on completion sets `txwq->ft_ci = txwq->prev_sw_pi` (line 1325) after asserting the outstanding queue is empty (line 1322).
- On the next purge, `start_pi = uct_ib_mlx5_txwq_next_wqe_index(txwq->ft_ci, uct_ib_mlx5_wqe_size(ctrl))` (lines 1260-1262) therefore evaluates to the index right after the last posted WQE, which equals `end_pi = txwq->sw_pi`.
- The unconditional `ucs_assertv_always(start_pi != end_pi, ...)` at line 1264 treats "nothing left to purge" as a fatal programming error, so the process aborts.

The gtest explicitly requires purge to be idempotent (`purge_outstanding(&ctx); EXPECT_EQ(num_ops_purged, ctx.num_ops_purged);` at test_peer_failure.cc:637-639), so the assertion contradicts the API contract the test encodes. This is a bug in the PR branch's purge refactor, not a flaky/infra failure. (Secondary smell: reading `uct_ib_mlx5_txwq_get_wqe(txwq, txwq->ft_ci)` before checking emptiness dereferences a WQE slot that may no longer hold a valid ctrl segment.)

**Implicated commit:** `0207feef` "UCT/IB/MLX5: refactor purge" (Zihao Zhao), finalized by `[REDACTED:Hex High Entropy String]` "UCT/IB/MLX5: cleanup" (Zihao Zhao, HEAD of PR #11836) — both on `src/uct/ib/mlx5/rc/rc_mlx5_ep.c`.

**File:** src/uct/ib/mlx5/rc/rc_mlx5_ep.c:1259-1266 (assert at :1264; `ft_ci` update at :1325)

**Suggested fix:** Make the empty case a normal early return instead of a fatal assert, and check emptiness before reading the WQE:

```c
    end_pi = txwq->sw_pi;
    if (txwq->ft_ci == txwq->prev_sw_pi) {
        /* Nothing outstanding (e.g. repeated purge) - idempotent no-op */
        return UCS_OK;
    }

    ctrl     = uct_ib_mlx5_txwq_get_wqe(txwq, txwq->ft_ci);
    start_pi = uct_ib_mlx5_txwq_next_wqe_index(txwq->ft_ci,
                                               uct_ib_mlx5_wqe_size(ctrl));
    ucs_assert(start_pi != end_pi);
```

(equivalently: compute `start_pi` and `if (start_pi == end_pi) return UCS_OK;`). Keep a debug-only `ucs_assert` if desired, but do not use `ucs_assertv_always` for a state the caller is allowed to reach. Then re-run `rc_mlx5/test_uct_purge_outstanding.*` (both `put_short` and `am_short`) on BlueField, since the second-purge path is exercised by every case in `test_purge_outstanding()`.

**Related:** PR #11836 (this branch), PR #11857 "UCT/IB/MLX5: Purge outstanding AM short operations" (closed) — original purge implementation this refactor builds on.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `a8c101b7-cc69-451b-9803-32a01de76213` in the triage console for the audit trail.

<!-- triage-agent request_id: a8c101b7-cc69-451b-9803-32a01de76213 -->
<!-- triage-agent gha_run_id: azure:136729 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-20

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ca9a58f651580d705774b3cfebbd058482f32470 -->

### svc-nvidia-pr-review · 2026-09-20


Note (not raised inline): The previously reported `ucs_fatal()`/`UCS_ERR_UNSUPPORTED` reachability set (zero-length `RDMA_WRITE` including the `post_check` keepalive WQE, DM `put_short` classified as put_bcopy via the shared `ucs_mpool_put` handler, `put_zcopy`/`put_sgl_zcopy`, `SEND`+dptr / `RDMA_READ` / atomics), and the fenced-put `raddr`/`rkey` reporting are unchanged in this revision — those existing threads should be `+1`'d rather than re-opened.


Note (not raised inline): The earlier "stale queue head" concern is now structurally different: `uct_rc_mlx5_ep_outstanding_complete_send_ops()` extracts every op with `sn <= pi`, so the head is always current, but any op type other than flush/`ucs_mpool_put` now aborts through `ucs_assertv_always()` in release builds too (already reported). The `ucs_mpool_put()` call itself is safe for the two asserted types (`tx.send_op_mp` / `tx.mp`).


Note (not raised inline): Verified as correct in this revision: sticky `delivered` propagation (RC is in-order), `peek`-then-`complete` ordering so the put_bcopy payload is still valid when the callback runs, the `wqe_put_length()` dptr walk (`ds` granularity keeps `remaining` a multiple of `sizeof(*dptr)`), and the `callback_data.data[256]` bound (enforced by the `assertv_always` in `uct_rc_mlx5_wqe_put_length()`, which always runs first for the same WQE).


Note (not raised inline): Coverage: `put_short` is capped at 64 bytes (inline path only, no DM, no zero length), `put_bcopy` only exercises the non-DM branch, and `post_until_error()` can still exit on `UCS_ERR_NO_RESOURCE` from tx-desc exhaustion (all previously reported).


## Review (96)

### svc-nvidia-pr-review · 2026-08-27 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-08-27 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-08-27 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-08-27 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-08-31 · COMMENTED

(no text)

### roiedanino · 2026-09-01 · COMMENTED

(no text)

### roiedanino · 2026-09-01 · COMMENTED

(no text)

### roiedanino · 2026-09-01 · COMMENTED

(no text)

### roiedanino · 2026-09-01 · COMMENTED

(no text)

### roiedanino · 2026-09-01 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-03 · COMMENTED

(no text)

### jeynmann · 2026-09-03 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-03 · COMMENTED

(no text)

### jeynmann · 2026-09-04 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-04 · COMMENTED

(no text)

### jeynmann · 2026-09-04 · COMMENTED

(no text)

### jeynmann · 2026-09-04 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-04 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-04 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### jeynmann · 2026-09-11 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-11 · COMMENTED

(no text)

### roiedanino · 2026-09-14 · COMMENTED

(no text)

### jeynmann · 2026-09-14 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### jeynmann · 2026-09-16 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### jeynmann · 2026-09-16 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### jeynmann · 2026-09-16 · COMMENTED

(no text)

### jeynmann · 2026-09-16 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### jeynmann · 2026-09-16 · COMMENTED

(no text)

### jeynmann · 2026-09-16 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-16 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-16 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-17 · CHANGES_REQUESTED

pls leave only relevant changes, if refactorung required - pls open separate PR

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-17 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-17 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-17 · COMMENTED

(no text)

### jeynmann · 2026-09-18 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-18 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-18 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-18 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-20 · COMMENTED

(no text)

### jeynmann · 2026-09-20 · COMMENTED

(no text)

### jeynmann · 2026-09-20 · COMMENTED

(no text)

### jeynmann · 2026-09-20 · COMMENTED

(no text)

### jeynmann · 2026-09-20 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-20 · COMMENTED

(no text)

### jeynmann · 2026-09-20 · COMMENTED

(no text)

### jeynmann · 2026-09-20 · COMMENTED

(no text)

### evgeny-leksikov · 2026-09-21 · APPROVED

(no text)

### Artemy-Mellanox · 2026-09-21 · COMMENTED

(no text)

### jeynmann · 2026-09-22 · COMMENTED

(no text)

### Artemy-Mellanox · 2026-09-22 · APPROVED

(no text)
