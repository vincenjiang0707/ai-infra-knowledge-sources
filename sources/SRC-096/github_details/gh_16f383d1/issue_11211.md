# [Issue #11211] Reduce initialization cost of usc_init library constructor

source: https://github.com/openucx/ucx/issues/11211
state: closed | updated: 2026-03-25T14:12:38Z
labels: 

## 正文

The library constructor `usc_init` is problematic as it adds a fixed startup cost / delay to all executables that transitively link to `libusc`.

I suggest implementing a lazy initialization strategy with initializing on first use to avoid the cost of the library constructor.

This is observable with version 1.19.0 (Ubuntu 25.10 questing) and to my understanding, this problem is getting even worse with https://github.com/openucx/ucx/pull/11112.

### Motivation:

I have a test executable that transitively depends on libucs.so via OpenMPI, without using any related functionality.
We run our 1k (increasing) tests in isolation using ctest, calling the executable at least once per test as some are run using mpirun.

I measure an overhead of 42ms per invocation, so we are dealing with a lower bound of 42 seconds of `usc_init` for each of our 10+ test configurations.

### MRE

I measure the invocation overhead by linking the library to a do-nothing main as follows:

```console
$ echo 'int main() {}' > nothing.c
$ gcc nothing.c -c -o nothing.o
$ gcc -Wl,--no-as-needed -lucs nothing.o -o nothing
$ ldd nothing
        linux-vdso.so.1 (0x00007cc9738de000)
        libucs.so.0 => /lib/x86_64-linux-gnu/libucs.so.0 (0x00007cc973831000)
        libc.so.6 => /lib/x86_64-linux-gnu/libc.so.6 (0x00007cc973400000)
        libm.so.6 => /lib/x86_64-linux-gnu/libm.so.6 (0x00007cc973724000)
        libucm.so.0 => /lib/x86_64-linux-gnu/libucm.so.0 (0x00007cc973705000)
        /lib64/ld-linux-x86-64.so.2 (0x00007cc9738e0000)
$ hyperfine -w 1 ./nothing
Benchmark 1: ./nothing
  Time (mean ± σ):      42.2 ms ±   0.2 ms    [User: 20.4 ms, System: 21.8 ms]
  Range (min … max):    41.9 ms …  42.9 ms    69 runs
```

Sanity check with `perf record -Fmax -g ./nothing`:

<img width="1746" height="695" alt="Image" src="https://github.com/user-attachments/assets/91aae497-13fa-4f24-b05a-f713b24f7ac3" />

The perf records suggest that the time is spent in:
https://github.com/openucx/ucx/blob/9117b1bc7867d299db5ced29dca5d1a1d9038609/src/ucm/bistro/bistro.c#L86-L89
However, the deadline mentions a timeout of 5ms in 1.19, which doesn't match up with the measured 42ms as `sched_yield()` covers 79% of samples in the perf trace.

## 评论 (4)

### tvegas1 · 2026-03-24

can you please try setting environment variable `UCX_MEM_BISTRO_GRACE_DURATION=0` (UCX master)

### fsimonis · 2026-03-25

@tvegas1 I built the ucx spack package with default settings and the master branch:

```
$ hyperfine -w 1 -L env ',UCX_MEM_BISTRO_GRACE_DURATION=0' '{env} ./nothing'
Benchmark 1:  ./nothing
  Time (mean ± σ):     403.2 ms ±   0.7 ms    [User: 144.8 ms, System: 258.2 ms]
  Range (min … max):   402.2 ms … 404.8 ms    10 runs
 
Benchmark 2: UCX_MEM_BISTRO_GRACE_DURATION=0 ./nothing
  Time (mean ± σ):       2.3 ms ±   0.5 ms    [User: 1.3 ms, System: 1.1 ms]
  Range (min … max):     0.8 ms …   4.2 ms    840 runs
 
Summary
  UCX_MEM_BISTRO_GRACE_DURATION=0 ./nothing ran
  173.45 ± 35.17 times faster than  ./nothing
```

So on master, the default becomes worse (for this case), but it can be overwritten, which is very good!

I see that #11112 didn't make it into 1.20.0 nor the 1.20.1 rc. When do you expect it to be released?

### tvegas1 · 2026-03-25

For older UCX version you could try `UCX_MEM_EVENTS=n`, this could have perf impact if MPI uses UCX provider.

### fsimonis · 2026-03-25

> UCX_MEM_EVENTS=n

This reduced the load time on my system down to approx 3.5ms for 1.19.0.

```
Time (mean ± σ):       3.5 ms ±   2.2 ms    [User: 0.8 ms, System: 1.2 ms]
Range (min … max):     1.3 ms …   9.0 ms    2000 runs
```

The startup time of OpenMPI now converges against the libpsm2 constructor https://github.com/cornelisnetworks/opa-psm2/issues/73, and the MPICH startup is now negligible.
Thanks a lot for the help!
