# [PR #11934] UCT/ROCM: Enable dmabuf by default with peermem fallback

source: https://github.com/openucx/ucx/pull/11934
state: closed | updated: 2026-09-16T12:54:50Z
labels: 

## 正文

## What?
Use dma-buf for GPU-direct when the kernel supports it, and keep peermem as the fallback on older kernels. Also report allocation-base offsets for interior ROCm addresses.

## Why?
The rocm components in UCX have support for the dma-buf mechanism to enable direct data transfer between an RDMA NIC and a GPU. However, the user explicitly needs to request that support by setting UCX_ROCM_COPY_DMABUF=y, dma-buf support is not turned on by default.

## How?
Starting from linux kernel > 6.0, the peermem mechanism that was used previously is not useable in the vast majority of deployments, necessitating using dma-buf instead. The goal of this story is to enable dma-buf by default in the rocm components of UCX, and ensure that if dma-buf support is not available (e.g. linux kernel 5.15 in most cases) we can fall back to the peermem mechanism if that is available.

## 评论 (52)

### svc-nvidia-pr-review · 2026-09-09

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: a5336a72c5d1c1d17cd41f9bcf4f940e774aeb63 -->

### svc-ucx · 2026-09-09

🤖 **CI Triage Agent** — `UCX PR (Codestyle AUTHORS file update check)` · commit `a5336a72`

**TL;DR:** This is not a code/test bug — the "AUTHORS file update check" failed because PR #11934's commit author, David DeBonis <ddebonis@amd.com>, is a new contributor who is not listed in the `AUTHORS` file. The fix is for the PR to add that line to `AUTHORS` (easiest by running `./contrib/authors_update.sh` locally and committing the result).

<details><summary>Full analysis</summary>

**Summary:** The `AUTHORS file check` bash step in the Codestyle job exited with code 1 because `git diff --exit-code` detected an uncommitted change to `AUTHORS` after `contrib/authors_update.sh` ran.

**Root cause:** The pipeline step computes the PR commit range (`8d1fb96..a5336a72`) and runs `./contrib/authors_update.sh` on it, then asserts the working tree is clean. The script reported a new, unknown author:

```
Names:
++ David DeBonis <ddebonis@amd.com>
```

Since neither the email `ddebonis@amd.com` nor the name `David DeBonis` matches any existing entry in `AUTHORS` (the file jumps from `Daniel Pressler` at line 26 straight to `David Wootton` at line 27), the script appended the entry, `git diff --exit-code` saw the modification and returned 1, which the Bash task surfaced as `##[error]Bash exited with code '1'`. This is the check working as designed — it enforces that new contributors add themselves to `AUTHORS` in the same PR. There is no hang, timeout, infrastructure fault, or regression involved; the whole job ran in about 1 second.

**Implicated commit:** [REDACTED:Hex High Entropy String] (David DeBonis <ddebonis@amd.com>) — the PR head commit, which introduces a new author without an `AUTHORS` entry. The check script itself is unchanged and correct.

**File:** `AUTHORS:26` (insertion point, between `Daniel Pressler` and `David Wootton`); check logic at `contrib/authors_update.sh:36`

**Suggested fix:** In PR #11934, add the author to `AUTHORS` and amend/append a commit:

```bash
# from the repo root, on the PR branch
./contrib/authors_update.sh $(git merge-base HEAD origin/develop)..HEAD
git add AUTHORS && git commit -m "AUTHORS: add David DeBonis"
```

Or simply insert `David DeBonis <ddebonis@amd.com>` manually after line 26 (`Daniel Pressler <danielpr@nvidia.com>`), preserving `LC_COLLATE=C` sort order as the script does. No pipeline or script change is warranted. One caveat worth noting to the contributor: the script's dedup check at `contrib/authors_update.sh:36` uses a substring `grep -iq "$name"`, so if the same person previously contributed under a different name/email spelling, the correct action is to normalize via `.mailmap` rather than adding a duplicate entry.

**Related:** PR https://github.com/openucx/ucx/pull/11934 (the failing PR). No pre-existing issue tracks this failure mode; recent `AUTHORS` commits (e.g. `c2567cbe`, `6d75cec7`) show the same pattern of new contributors adding themselves alongside their change.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `305cfe92-def8-4a3b-b19a-4e065a82fa8b` in the triage console for the audit trail.

<!-- triage-agent request_id: 305cfe92-def8-4a3b-b19a-4e065a82fa8b -->
<!-- triage-agent gha_run_id: azure:135335 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-09


Residual coverage gap: the ROCm CI containers (`ubuntu2004_rocm_5_4_0`, `ubuntu2204_rocm_6_0_0`) are build-only, so neither `mem_query_dmabuf` nor the new `disable` test is exercised by the PR pipeline; both need a run on real AMD hardware to validate the default change.


### svc-nvidia-pr-review · 2026-09-09

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 6ce012cd3c4a33119a71a7aed8ced2e1f26d5f21 -->

### svc-ucx · 2026-09-09

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `6ce012cd`

**TL;DR:** The "commit title" codestyle job failed because one commit in PR #11934 is titled `added author and reduced warning to debug`, which doesn't match UCX's required `MODULE: Description` prefix format — the author needs to reword that commit (e.g. `UCT/ROCM: Add author and reduce warning to debug`) and force-push.

<details><summary>Full analysis</summary>

**Summary:** Azure Pipelines job `Codestyle / commit title` (build 135339) exited with code 1 after `codestyle_check_commit_title` rejected a commit title in PR #11934.

**Root cause:** Not a product or infrastructure bug. The log shows:
- `Bad commit title: 'added author and reduced warning to debug'`
- `Good commit title: 'UCT/ROCM: Enable dmabuf by default with peermem fallback ...'`

`codestyle_check_commit_title()` requires every non-merge commit subject in `base..head` to match the regex `^[0-9A-Z/_-]+:\ [[:alnum:]_]` — i.e. an uppercase module prefix followed by `": "` and an alphanumeric first character (e.g. `UCT/ROCM: Add ...`). The commit `added author and reduced warning to debug` has no module prefix and starts lowercase, so `err=1` was returned and the job failed.

Secondary (cosmetic) CI issue visible in the same log: the step runs with `set -eE`, so the non-zero return from `codestyle_check_commit_title` aborts the script immediately. The `if [[ $? -ne 0 ]]` block in `buildlib/pr/codestyle.yml:25-30` is therefore dead code — the helpful `Bad commit title(s), see <wiki url>` message never printed, leaving only a bare `Bash exited with code '1'`, which makes the failure harder to self-diagnose.

**Implicated commit:** [REDACTED:Hex High Entropy String] (PR #11934 head, merged as 63e9a2ad5 for CI) — the offending commit is the one titled `added author and reduced warning to debug`. Author not shown in this job's log.

**File:** buildlib/tools/codestyle.sh:14 (the enforcing regex); buildlib/pr/codestyle.yml:17-30 (the failing step)

**Suggested fix:**
1. Contributor action (fixes this build): reword the commit, e.g.
   ```
   git rebase -i <base>   # or: git commit --amend  if it's the tip commit
   # retitle to: "UCT/ROCM: Add author and reduce warning to debug"
   git push --force-with-lease
   ```
   Also consider trimming the other commit's subject — its body text appears to be on the subject line, making a very long title (it passes the regex but violates the ≤50-char subject convention).
2. Optional CI hardening so future failures are self-explanatory: in `buildlib/pr/codestyle.yml`, stop the `set -e` from swallowing the message, e.g.
   ```bash
   if ! codestyle_check_commit_title "$base_revision..$head_revision"; then
       url="https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines"
       echo "##vso[task.logissue type=error]Bad commit title(s), see $url for more info."
       echo "##vso[task.complete result=Failed;]"
   fi
   ```

**Related:** PR #11934; contributor guidance: https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `e90fcf24-79fe-4ec1-ae9d-8aedfd0649f2` in the triage console for the audit trail.

<!-- triage-agent request_id: e90fcf24-79fe-4ec1-ae9d-8aedfd0649f2 -->
<!-- triage-agent gha_run_id: azure:135339 -->
<!-- triage-agent-bot -->

### svc-ucx · 2026-09-09

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `b7066dfe`

**TL;DR:** The "commit title" codestyle job failed because two commits in PR #11934 have titles that don't match UCX's required `MODULE: Description` format (`Update src/uct/rocm/base/rocm_base.c` and `added author and reduced warning to debug`). Fix by rebasing/squashing the PR into commits with properly prefixed titles, e.g. `UCT/ROCM: ...`.

<details><summary>Full analysis</summary>

**Summary:** Azure job `UCX PR (Codestyle commit title)` build 135343 failed with `Bash exited with code '1'` after `codestyle_check_commit_title` rejected two commit titles in PR #11934.

**Root cause:** Not an infrastructure or product bug. `buildlib/pr/codestyle.yml` runs `codestyle_check_commit_title` over `base..head` of the PR, which requires every non-merge commit title to match the regex `^[0-9A-Z/_-]+:\ [[:alnum:]_]` (an uppercase module prefix, a colon, a space, then alphanumeric text). The log shows:
- `Bad commit title: 'Update src/uct/rocm/base/rocm_base.c'` — GitHub web-editor default message, no module prefix.
- `Bad commit title: 'added author and reduced warning to debug'` — lowercase, no module prefix.
- `Good commit title: 'UCT/ROCM: Enable dmabuf by default with peermem fallback ...'` — passes the regex, but note the entire commit body appears to be crammed onto the subject line, which is itself a style problem.

Both bad titles set `err=1`, the script returns non-zero under `set -eE`, and the task fails immediately (total job runtime ~1s — no hang or timeout involved).

**Implicated commit:** [REDACTED:Hex High Entropy String] (PR #11934 head, merged as 2f6f62388 for testing) — the offending commits are authored by the PR submitter, not by a UCX maintainer change.

**File:** buildlib/tools/codestyle.sh:14 (check logic) — enforced via buildlib/pr/codestyle.yml:24

**Suggested fix:** Rewrite the PR history so every commit title has the required form. Simplest is to squash the three commits into one:
```
git rebase -i <base>        # or: git reset --soft <base> && git commit
# subject line, <= ~50-72 chars, module prefix required:
UCT/ROCM: Enable dmabuf by default with peermem fallback

# body (blank line above), free-form:
Use dma-buf for GPU-direct when the kernel supports it, and keep
peermem as the fallback on older kernels. Also report allocation-base
offsets for interior ROCm addresses.
git push --force-with-lease
```
If the commits must stay separate, amend each title to e.g. `UCT/ROCM: Reduce warning to debug level` and `AUTHORS: Add contributor`. Also move the long description out of the subject into the commit body — the current "good" commit only passes because the regex checks the prefix, not the length.

**Related:** PR #11934; contributor guidance referenced by the job: https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `3e1de382-fe07-40ac-aac3-0b9ed929ca15` in the triage console for the audit trail.

<!-- triage-agent request_id: 3e1de382-fe07-40ac-aac3-0b9ed929ca15 -->
<!-- triage-agent gha_run_id: azure:135343 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-09


Residual gaps: both `mem_query_dmabuf` and the new `disable` test need an AMD GPU run to validate the offset math and the default flip; the `disable` test also gives no coverage that a *failed* `hsa_amd_portable_export_dmabuf()` with the new `try` default degrades gracefully (fd `-1` → IB non-dmabuf path), which is the main behavioral risk of this PR.


### svc-nvidia-pr-review · 2026-09-09

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: b7066dfe8bafce2b4e88257dea95f12218fe8265 -->

### svc-nvidia-pr-review · 2026-09-09


Residual coverage gap: the ROCm CI jobs are build-only, so neither the compile break nor the offset math would be caught by the PR pipeline; an AMD GPU run of `test_md*` is needed.


### svc-nvidia-pr-review · 2026-09-09

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 384eac451da1b756d62e7fd36c002171a4726f79 -->

### svc-ucx · 2026-09-09

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `384eac45`

**TL;DR:** This is not an infrastructure failure — the "commit title" codestyle job correctly rejected PR #11934 because two of its commits have titles that lack the required `MODULE: ` prefix; the author needs to reword those commits (and the CI script should be fixed so it prints the helpful error instead of a bare exit code 1).

<details><summary>Full analysis</summary>

**Summary:** Azure Pipelines job `UCX PR (Codestyle / commit title)` build 135347 failed with `##[error]Bash exited with code '1'` after `codestyle_check_commit_title` flagged two commit titles in PR #11934.

**Root cause:** The job runs `codestyle_check_commit_title "$base..$head"`, which requires every non-merge commit subject to match `^[0-9A-Z/_-]+:\ [[:alnum:]_]` (i.e. an uppercase module prefix like `UCT/ROCM: `). The log shows the checker's verdict per commit:
- Good: `UCX/ROCM: Update src/uct/rocm/base/rocm_base.c`
- **Bad: `Update src/uct/rocm/base/rocm_base.c`** — no module prefix
- **Bad: `added author and reduced warning to debug`** — no module prefix, lowercase
- Good: `UCT/ROCM: Enable dmabuf by default with peermem fallback Use dma-buf for GPU-direct when the kernel supports it, and keep peermem as the fallback on older kernels. Also report allocation-base offsets for interior ROCm addresses.`

Two bad titles set `err=1`, so the function returned 1. Note a secondary CI-script bug: the step begins with `set -eE`, so the non-zero return aborts the script immediately and the `if [[ $? -ne 0 ]]` block (which prints the wiki guidance link) is dead code — hence the log shows only a raw `Bash exited with code '1'` with no explanation. Also visible: the last commit's subject line has swallowed its whole body (no blank line after the subject), which passes the regex but violates the 50-char-subject convention.

**Implicated commit:** No product regression. The offending commits are the intermediate commits of PR #11934 (merge commit `813df758b`, PR head `[REDACTED:Hex High Entropy String]`); the CI-script defect predates this build in `buildlib/pr/codestyle.yml`.

**File:** `buildlib/tools/codestyle.sh:14` (the title regex) and `buildlib/pr/codestyle.yml:18-30` (`set -eE` making the error-reporting branch unreachable)

**Suggested fix:**
1. PR author: interactively rebase and reword/squash the commits so every title has a module prefix and a short subject, e.g.
   - `Update src/uct/rocm/base/rocm_base.c` → `UCT/ROCM: Update rocm_base dmabuf detection`
   - `added author and reduced warning to debug` → `UCT/ROCM: Add author entry, lower warning to debug`
   - Split the long fourth title: keep `UCT/ROCM: Enable dmabuf by default with peermem fallback` as the subject and move the rest into the body after a blank line.
   Simplest path: `git rebase -i` and squash all four into one properly titled commit, then force-push.
2. CI maintainers: make the failure self-explanatory by replacing the `set -eE` + `$?` pattern with `if ! codestyle_check_commit_title "$base_revision..$head_revision"; then ... fi` (or `set +e` around the call) in `buildlib/pr/codestyle.yml`, so the wiki guidance message is actually emitted.

**Related:** PR https://github.com/openucx/ucx/pull/11934; guidelines referenced by the job: https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `5f81c1af-1070-40f7-b044-a9ed75a3dceb` in the triage console for the audit trail.

<!-- triage-agent request_id: 5f81c1af-1070-40f7-b044-a9ed75a3dceb -->
<!-- triage-agent gha_run_id: azure:135347 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: bff3a33a63f9424e7c8e12d293f36f77d1283712 -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gap: ROCm CI jobs are build-only, so neither `mem_query_dmabuf` (now newly active for `rocm_cpy` because of the default flip) nor the new `disable` test is exercised; both need an AMD GPU run.


Residual coverage gap: `mem_query_dmabuf` still asserts `EXPECT_EQ(mem_buf.ptr(), mem_attr.base_address)` and `EXPECT_EQ(offset, mem_attr.dmabuf_offset)`, which additionally assumes HSA returns offset 0 for the allocation base and that HIP does not sub-allocate; only a hardware run can confirm.


Residual coverage gap: nothing covers a *failing* `hsa_amd_portable_export_dmabuf()` under the new `try` default (fd `-1` → IB falls back to the non-dmabuf path).


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 12b62b5730a57cd05e37001ffa0a805c0a67c679 -->

### svc-nvidia-pr-review · 2026-09-10


Residual gap: `mem_type_detect_mds` now asserts `EXPECT_GE(alloc_length, buffer_size)` for ROCm but keeps `EXPECT_EQ(address, base_address)`. If HSA reports a larger `sizeInBytes` because HIP sub-allocated from a slab (rather than page-rounding), `base_address` would also differ and the strict check would fail — only an AMD GPU run distinguishes the two cases.


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 787b84650cd4b3319d097b2b4cb4e25c4bd07e53 -->

### svc-nvidia-pr-review · 2026-09-10


Residual gaps: the two mangled hunks mean the current head almost certainly does not build with ROCm enabled; ROCm CI jobs are build-only and not part of the default pipeline, so neither the compile break nor the fd lifetime bug would be caught. An AMD GPU run of `gtest.uct.test_md*` is required.


Residual gap: nothing exercises a *failing* `hsa_amd_portable_export_dmabuf()` under the new `try` default (fd `-1` → IB must fall back to the non-dmabuf path).


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 50bd20b4cbc3ea47055d04ec3937c35e86eeac25 -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gap: ROCm CI is build-only, so neither the compile break nor the offset math is exercised by the PR pipeline; an AMD GPU run of `gtest.uct.test_md*` is still required.


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 9de3673bf40d2b1fedf516bcd01d8f7b94f1eb5f -->

### svc-nvidia-pr-review · 2026-09-10


Residual gap: ROCm CI jobs are build-only, so neither the offset math (`mem_query_dmabuf`, now active for `rocm_cpy` because of the `try` default) nor the new `disable` test runs in this pipeline; an AMD GPU run of `gtest.uct.test_md*` is still needed.


Residual gap: `EXPECT_GE(alloc_length, buffer_size)` combined with `EXPECT_EQ(address, base_address)` in `mem_type_detect_mds` only holds if HSA page-rounds `sizeInBytes`; if HIP sub-allocates from a slab, `base_address` differs too and the strict check fails. Only hardware distinguishes the two.


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 3a800423f2f19b695ed579fc8906ee4af34ccc01 -->

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ea6623b5250e4db35a0d9a06acdb9991727de8c0 -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gap: ROCm CI jobs are build-only, so neither the compile break nor the offset math is exercised here; an AMD GPU run of `gtest.uct.test_md*` is still required. Also nothing covers a *failing* `hsa_amd_portable_export_dmabuf()` under the new `try` default (fd `-1` → IB non-dmabuf path), which is the main behavioral risk of the default flip.


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 44d1e763fe2356530b25b3c75b68898ef046c6ba -->

### ddebonis-amd · 2026-09-10

I am tempted to close this PR and start fresh as it seems I am out of sync with the reviewers requests.  Please advise.

### svc-ucx · 2026-09-10

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `44d1e763`

**TL;DR:** The "Codestyle / commit title" job failed because PR #11934 contains 8 commits whose titles don't match UCX's required `MODULE/SUB: Description` format (GitHub web-UI commits like `Update src/uct/rocm/base/rocm_base.c`, `Apply suggestion from @svc-nvidia-pr-review`, and `added author and reduced warning to debug`). Fix by squashing/rewording those commits.

<details><summary>Full analysis</summary>

**Summary:** Azure job `commit title` (build 135582) exited with code 1 — `codestyle_check_commit_title` flagged 8 non-conforming commit titles in PR #11934.

**Root cause:** Not a product/infra bug. `buildlib/tools/codestyle.sh:14` requires every non-merge commit title to match the regex `^[0-9A-Z/_-]+:\ [[:alnum:]_]` (i.e. `UPPERCASE/MODULE: text`). The log shows these titles failing:
- `Update test/gtest/uct/test_md.cc` (×3)
- `Update src/uct/rocm/base/rocm_base.c` (×3)
- `Apply suggestion from @svc-nvidia-pr-review` (×2)
- `added author and reduced warning to debug` (lowercase, no module prefix)

These are auto-generated titles from accepting GitHub review suggestions in the web UI, plus one hand-written lowercase title. Note the bad titles come from PR commits, not from `develop`.

Secondary CI-usability issue: `buildlib/pr/codestyle.yml:18` sets `set -eE` before calling `codestyle_check_commit_title`, so the non-zero return aborts the script immediately and the `if [[ $? -ne 0 ]]` block at lines 25–30 never runs. That's why the log shows a bare `Bash exited with code '1'` instead of the intended actionable message with the contributor-guidelines URL.

**Implicated commit:** The PR's own commits (e.g. the `Update src/uct/rocm/base/rocm_base.c` / `Apply suggestion from @svc-nvidia-pr-review` commits in PR #11934, merge head f49780ad4 over base d5b6b278f). No upstream regression.

**File:** buildlib/tools/codestyle.sh:14 (the check); buildlib/pr/codestyle.yml:18-30 (swallowed error message)

**Suggested fix:**
1. In PR #11934, rebase and squash/reword the offending commits so every title matches the format, e.g.:
   ```
   git rebase -i [REDACTED:Hex High Entropy String]
   # squash the "Update <file>" / "Apply suggestion from ..." fixups into their
   # parent commits, and reword "added author and reduced warning to debug"
   # to e.g. "UCT/ROCM: Add author and reduce warning to debug level"
   git push --force-with-lease
   ```
   Simplest path: squash the whole PR into one or a few commits titled `UCT/ROCM: ...`. Also consider shortening the very long multi-paragraph title on the `Enable dmabuf by default with peermem fallback ...` commit into a ≤50-char subject plus body.
2. Optional CI improvement: in `buildlib/pr/codestyle.yml`, change the invocation so the error handler runs, e.g. `codestyle_check_commit_title "$base..$head" || { echo "##vso[task.logissue type=error]Bad commit title(s), see $url"; echo "##vso[task.complete result=Failed;]"; }` — or drop `-e` around that call — so contributors see the guidance link instead of a bare exit code.

**Related:** PR https://github.com/openucx/ucx/pull/11934 ; guidelines: https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `59602a0e-402e-49a8-b8b3-9d15f5efd55a` in the triage console for the audit trail.

<!-- triage-agent request_id: 59602a0e-402e-49a8-b8b3-9d15f5efd55a -->
<!-- triage-agent gha_run_id: azure:135582 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 13326ceeeb0dd959348c99cf317719ea805dddc4 -->

### svc-ucx · 2026-09-10

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `13326cee`

**TL;DR:** This is not a code/test failure — the "commit title" codestyle job rejected PR #11934 because 11 of its commits have titles that don't match the required `MODULE: Description` format (e.g. `Update test/gtest/uct/test_md.cc`, `Apply suggestion from @svc-nvidia-pr-review`); fix by squashing/rewording those commits.

<details><summary>Full analysis</summary>

**Summary:** Azure job `UCX PR (Codestyle / commit title)` build 135590 failed with `Bash exited with code '1'` after printing `Bad commit title: ...` for 11 commits in PR #11934.

**Root cause:** `codestyle_check_commit_title()` validates every non-merge, first-parent commit title against the regex `^[0-9A-Z/_-]+:\ [[:alnum:]_]` (i.e. an uppercase module prefix followed by `: `). PR #11934 contains commits created via the GitHub web UI and review-suggestion applies, whose auto-generated titles lack the module prefix:
- `Update test/gtest/uct/test_md.cc` (×4)
- `Update src/uct/rocm/base/rocm_base.c` (×3)
- `Apply suggestion from @svc-nvidia-pr-review` (×2)
- `added author and reduced warning to debug` (lowercase, no prefix)

Each of these sets `err=1`, so the check returns non-zero and the job fails. The properly-prefixed commits in the same PR (`UCT/ROCM: ...`, `UCX/ROCM: ...`) all passed, confirming the checker itself is working as designed.

Secondary CI-script issue: `buildlib/pr/codestyle.yml:18` uses `set -eE`, so the script aborts the moment `codestyle_check_commit_title` returns 1 — the `if [[ $? -ne 0 ]]` block at lines 25-30 is dead code, which is why the log shows a bare `Bash exited with code '1'` instead of the intended helpful message with the contributor-guidelines link.

**Implicated commit:** No product commit — the offending titles are in PR #11934 (head `[REDACTED:Hex High Entropy String]`, merge commit `[REDACTED:Hex High Entropy String]`). The checker logic itself is unchanged in `buildlib/tools/codestyle.sh` (Copyright 2024 NVIDIA).

**File:** `buildlib/tools/codestyle.sh:14` (validation regex); `buildlib/pr/codestyle.yml:17-31` (job definition)

**Suggested fix:**
1. PR author: rewrite the offending commit titles, e.g. `git rebase -i $(git merge-base HEAD develop)` and squash the web-UI/`Apply suggestion` commits into their parent, or `reword` them to the required form (`UCT/ROCM: fix dmabuf fd query in test_md`, `UCT/ROCM: add author and reduce warning to debug`). Squashing the whole PR into one well-titled commit is the simplest route since several are one-line review fixups.
2. Avoid committing through the GitHub web editor / "Apply suggestion" button on this repo, since those generate non-conforming titles.
3. Optional CI cleanup: in `buildlib/pr/codestyle.yml`, either drop `-e` for that step or call the function as `codestyle_check_commit_title ... || rc=$?` so the intended `##vso[task.logissue]` message with the guidelines URL is actually emitted instead of a bare exit-1.

**Related:** PR https://github.com/openucx/ucx/pull/11934; guidelines referenced by the job: https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `46dbdb92-95a5-4121-bc61-a66cbbce2387` in the triage console for the audit trail.

<!-- triage-agent request_id: 46dbdb92-95a5-4121-bc61-a66cbbce2387 -->
<!-- triage-agent gha_run_id: azure:135590 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-10

🚫 @svc-ucx — not authorized to trigger a review on this repo. Contact a repo admin to be added to the allowed-users list.

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 12506cb5179150591328430f78740906d0dc75da -->

### svc-ucx · 2026-09-10

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `12506cb5`

**TL;DR:** The "commit title" codestyle job failed because 11 commits in PR #11934 have titles that don't match UCX's required `MODULE: Description` format (mostly GitHub web-UI defaults like `Update src/uct/rocm/base/rocm_base.c` and `Apply suggestion from @svc-nvidia-pr-review`); the fix is to squash/reword those commits, not a CI change.

<details><summary>Full analysis</summary>

**Summary:** Azure Pipelines job `UCX PR (Codestyle commit title)` build 135610 exited with code 1 — `codestyle_check_commit_title` rejected 11 of the 22 commits in the PR branch.

**Root cause:** A real (non-flaky) codestyle violation. The check at `buildlib/tools/codestyle.sh:14` requires every non-merge commit subject to match `^[0-9A-Z/_-]+: [[:alnum:]_]` (i.e. an uppercase module prefix, colon, space, then text). The build log lists the offenders:

- `Potential fix for pull request finding`
- `Update test/gtest/uct/test_md.cc` (×4)
- `Update src/uct/rocm/base/rocm_base.c` (×3)
- `Apply suggestion from @svc-nvidia-pr-review` (×2)
- `added author and reduced warning to debug`

These are all GitHub web-editor / "apply suggestion" auto-generated titles plus one lowercase manual title. The correctly formatted commits in the same PR (`UCT/ROCM: ...`, `UCX/ROCM: ...`) pass, confirming the checker itself is working as designed.

Secondary CI-script issue worth noting: `buildlib/pr/codestyle.yml:18` sets `set -eE`, so when `codestyle_check_commit_title` returns non-zero the script aborts at line 24 and the `if [[ $? -ne 0 ]]` block (lines 25–30) never runs. That's why the log shows only a bare `##[error]Bash exited with code '1'` instead of the intended link to the contributor guidance wiki.

**Implicated commit:** No product commit is at fault. The failing head is `[REDACTED:Hex High Entropy String]` (merged as `ef4a6b464` for testing); the bad titles are commits authored via the GitHub web UI on PR #11934.

**File:** `buildlib/tools/codestyle.sh:14` (the rule being enforced); pipeline definition `buildlib/pr/codestyle.yml:17-31`

**Suggested fix:**
1. Primary — rewrite the PR history so every commit title matches the pattern. Simplest is to squash the whole branch into one properly titled commit:
   ```
   git rebase -i $(git merge-base HEAD origin/develop)   # mark the 11 bad commits as 'fixup'/'squash'
   # or: git reset --soft $(git merge-base HEAD origin/develop) && git commit -m "UCT/ROCM: Enable dmabuf by default with peermem fallback"
   git push --force-with-lease
   ```
   Avoid committing via the GitHub web UI / "Apply suggestion" button on this repo, since those generate `Update <path>` titles that always fail this gate.
2. Also note the last passing title is very long (a multi-sentence subject line); consider keeping the subject under ~50 chars and moving detail to the body.
3. Optional CI cleanup — in `buildlib/pr/codestyle.yml`, call the checker as `codestyle_check_commit_title ... || rc=$?` (or drop `-e` around that call) so the helpful "see wiki for more info" error message is actually emitted instead of being swallowed by `set -eE`.

**Related:** PR https://github.com/openucx/ucx/pull/11934 · guidance referenced by the job: https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `d0f96e79-3053-432b-a740-2839e5350c63` in the triage console for the audit trail.

<!-- triage-agent request_id: d0f96e79-3053-432b-a740-2839e5350c63 -->
<!-- triage-agent gha_run_id: azure:135610 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: fb8d2f90b880d6aec6db4834cebdf2d679fd39dc -->

### svc-ucx · 2026-09-10

🤖 **CI Triage Agent** — `UCX PR (Codestyle commit title)` · commit `fb8d2f90`

**TL;DR:** Not a code/infra bug — the "commit title" codestyle job legitimately failed because PR #11934 contains 11 commits with non-conforming titles (GitHub web-editor defaults like `Update src/uct/rocm/base/rocm_base.c` and `Apply suggestion from @svc-nvidia-pr-review`); the contributor must rewrite/squash those commit messages into the `MODULE: Description` form.

<details><summary>Full analysis</summary>

**Summary:** Azure job `UCX PR (Codestyle / commit title)` build 135614 exited with code 1 after `codestyle_check_commit_title` flagged multiple commit titles in PR #11934.

**Root cause:** The check in `buildlib/tools/codestyle.sh` requires every non-merge, first-parent commit title to match the regex `^[0-9A-Z/_-]+:\ [[:alnum:]_]` (i.e. `MODULE: Description`). The log shows 11 failing titles, all auto-generated by the GitHub web UI / review-suggestion bot:
- `Update src/uct/rocm/base/rocm_base.c` (×4)
- `Update test/gtest/uct/test_md.cc` (×4)
- `Apply suggestion from @svc-nvidia-pr-review` (×2)
- `Potential fix for pull request finding`
- `added author and reduced warning to debug` (lowercase, no module prefix)

The remaining commits (`UCT/ROCM: ...`, `UCX/ROCM: ...`) passed, confirming the checker itself works. Secondary observation: `set -eE` at `buildlib/pr/codestyle.yml:18` causes the script to abort the instant the function returns 1, so the intended friendly message (`Bad commit title(s), see <wiki>`) at lines 25-30 is never emitted — the job just reports `Bash exited with code '1'`, making the failure less self-explanatory than intended.

**Implicated commit:** No product commit — failure is caused by the PR branch's own commit history (PR head `[REDACTED:Hex High Entropy String]`, merge commit `08bba45c2`). The checker logic dates to the codestyle refactor in `buildlib/tools/codestyle.sh` (NVIDIA, 2024).

**File:** `buildlib/tools/codestyle.sh:14` (the title regex) and `buildlib/pr/codestyle.yml:18-30` (error-reporting bug)

**Suggested fix:**
1. Author action (resolves this failure): interactive-rebase PR #11934 and squash/reword the 11 offending commits into properly prefixed titles, e.g. `UCT/ROCM: address review comments`, `UCT/ROCM: fix mem type detect mds`, then force-push. See https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines.
2. Optional CI improvement: in `buildlib/pr/codestyle.yml`, stop the `set -eE` early-exit from swallowing the guidance message, e.g.

```bash
set -eE
source ./buildlib/tools/codestyle.sh
base_revision=$(git rev-parse "$(Build.SourceVersion)^1")
head_revision=$(git rev-parse "$(Build.SourceVersion)^2")
if ! codestyle_check_commit_title "$base_revision..$head_revision"; then
    url="https://github.com/openucx/ucx/wiki/Guidance-for-contributors#general-guidelines"
    echo "##vso[task.logissue type=error]Bad commit title(s), see $url for more info."
    echo "##vso[task.complete result=Failed;]"
fi
```

**Related:** https://github.com/openucx/ucx/pull/11934

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `5f435a39-82e3-4cef-b0e7-7f6e000a9207` in the triage console for the audit trail.

<!-- triage-agent request_id: 5f435a39-82e3-4cef-b0e7-7f6e000a9207 -->
<!-- triage-agent gha_run_id: azure:135614 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 595ff18c2afb0f10a64b0d451a10d34e2b08d0bc -->

### svc-ucx · 2026-09-10

🤖 **CI Triage Agent** — `UCX PR (Build build_x86_64 ubuntu2004_rocm)` · commit `595ff18c`

**TL;DR:** The `ubuntu2004_rocm` build fails to compile `src/uct/rocm/base/rocm_base.c` because a bad merge/edit in commit 595ff18c left a duplicated `ucs_close_fd(&dmabuf_fd); }` block (lines 499–500), which closes `uct_rocm_base_mem_query()` prematurely and breaks brace balance. Delete those two stray lines.

<details><summary>Full analysis</summary>

**Summary:** `make` fails with `-Werror` in the ROCm module: `rocm_base.c:508: error: expected identifier or '(' before 'if'` plus `control reaches end of non-void function` in `uct_rocm_base_mem_query`.

**Root cause:** Syntax error from duplicated code, not an environment/timing issue. In `uct_rocm_base_mem_query()` the dmabuf-FD handling block is:

```c
494:  if (mem_attr_p->field_mask & UCT_MD_MEM_ATTR_V2_FIELD_DMABUF_FD) {
495:      mem_attr_p->dmabuf_fd = dmabuf_fd;
496:  } else {
497:      ucs_close_fd(&dmabuf_fd);
498:  }
499:      ucs_close_fd(&dmabuf_fd);   <-- stray duplicate
500:  }                              <-- stray extra closing brace
```

The extra `}` on line 500 closes the enclosing `if (... DMABUF_FD || ... DMABUF_OFFSET)` block early, so the `}` on line 506 closes the *function*. Everything after (lines 508–514) then appears at file scope, producing "expected identifier or '(' before 'if'/'return'/'}'", and GCC also reports `control reaches end of non-void function` for `uct_rocm_base_mem_query` (fatal under `-Werror`). Only the ROCm-enabled build configuration compiles this file, which is why just `build_x86_64 ubuntu2004_rocm` failed.

**Implicated commit:** [REDACTED:Hex High Entropy String] — "UCT/ROCM: Enable dmabuf by default with peermem fallback", David DeBonis (PR #11934)

**File:** src/uct/rocm/base/rocm_base.c:499-500

**Suggested fix:** Delete the two duplicated lines so the block reads:

```c
    if ((mem_attr_p->field_mask & UCT_MD_MEM_ATTR_V2_FIELD_DMABUF_FD) ||
        (mem_attr_p->field_mask & UCT_MD_MEM_ATTR_V2_FIELD_DMABUF_OFFSET)) {
        uct_rocm_base_dmabuf_export(base_addr, base_size, mem_type, &dmabuf_fd,
                                    &dmabuf_offset);
        if (mem_attr_p->field_mask & UCT_MD_MEM_ATTR_V2_FIELD_DMABUF_FD) {
            mem_attr_p->dmabuf_fd = dmabuf_fd;
        } else {
            ucs_close_fd(&dmabuf_fd);
        }

        if (mem_attr_p->field_mask & UCT_MD_MEM_ATTR_V2_FIELD_DMABUF_OFFSET) {
            mem_attr_p->dmabuf_offset = dmabuf_offset +
                                        UCS_PTR_BYTE_DIFF(base_addr, addr);
        }
    }
```

Then re-push; a local `./contrib/configure-devel --with-rocm && make -j` (or any ROCm-enabled build) will catch this before CI. Also worth double-checking the rest of the rebase in this commit for similar duplicated hunks.

**Related:** PR #11934 (the PR under test); prior ROCm dmabuf work in #11541.

</details>



> 🛡️ This comment had 1 potential secret(s) redacted (Hex High Entropy String). See request_id `693c1fb5-7d8d-40cb-9964-8dfa9f3190ca` in the triage console for the audit trail.

<!-- triage-agent request_id: 693c1fb5-7d8d-40cb-9964-8dfa9f3190ca -->
<!-- triage-agent gha_run_id: azure:135626 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gap: ROCm CI jobs (`ubuntu2004_rocm_5_4_0`, `ubuntu2204_rocm_6_0_0`) are build-only, so the compile break in finding 1 and the offset math are not exercised by this pipeline; an AMD GPU run of `gtest.uct.test_md*` is required.


Residual coverage gap: `mem_type_detect_mds` combines `EXPECT_GE(alloc_length, buffer_size)` with `EXPECT_EQ(address, base_address)`. That holds only if HSA page-rounds `sizeInBytes`; if HIP sub-allocates from a slab, `base_address` differs too and the strict check fails. Only hardware distinguishes the two.


Residual coverage gap: nothing covers a *failing* `hsa_amd_portable_export_dmabuf()` under the new `try` default (fd `-1` → IB must fall back to the non-dmabuf/peermem path), which is the main behavioral risk of the default flip.


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 6812eeb95be7ec9068d465246a3955882a10aee6 -->

### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 7e3dddc70228bc57bd8e1a3ab6637a9d719c84d4 -->

### svc-nvidia-pr-review · 2026-09-10


Residual coverage gap: `mem_query_dmabuf` becomes newly active for `rocm_cpy` because of the default flip, and it asserts `EXPECT_EQ(mem_buf.ptr(), mem_attr.base_address)` / `EXPECT_EQ(offset, mem_attr.dmabuf_offset)`. That requires HSA's `agentBaseAddress` to equal the `hipMalloc` pointer (i.e. no ROCclr slab sub-allocation). Same coupling in `mem_type_detect_mds`, where `EXPECT_GE(alloc_length, buffer_size)` was relaxed but `EXPECT_EQ(address, base_address)` was kept. Only an AMD GPU run distinguishes page-rounding from sub-allocation.


Residual coverage gap: the ROCm CI containers (`ubuntu2004_rocm_5_4_0`, `ubuntu2204_rocm_6_0_0`) are build-only, so none of the above runs in this pipeline; a `gtest.uct.test_md*` run on real hardware is needed.


Residual coverage gap: nothing exercises a *failing* `hsa_amd_portable_export_dmabuf()` under the new `try` default (fd `-1` → IB must fall back to `ibv_reg_mr`/peermem), which remains the main behavioral risk of the default change.


### svc-nvidia-pr-review · 2026-09-10

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 4a9f32a432e2e2a8295cf2b1b1fa712802785cf8 -->

### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: ba5cbb68675a7d49eedbd638a0a74e62c0cdca73 -->

### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: cb780f7fa2ffb9a2d97c37aa21c79212ce78c1fd -->

### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 2e8426ab98f64eb8f2bdad04332929fc61e1dfec -->

### svc-nvidia-pr-review · 2026-09-11


Residual coverage gap: ROCm CI containers are build-only, so neither `mem_query_dmabuf` (newly active for `rocm_cpy` after the default flip) nor the new `dmabuf_disable` test runs in this pipeline; an AMD GPU run of `gtest.uct.test_md*` is needed.


Residual coverage gap: nothing exercises a *failing* `hsa_amd_portable_export_dmabuf()` under the new `try` default (fd `-1` → `uct_ib_reg_mr()` falls back to `ibv_reg_mr`/peermem), which remains the main behavioral risk of the default change.


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 6b6bb7be3cf4aeff5084298b7bec11706a1d400f -->

### svc-nvidia-pr-review · 2026-09-11


Residual coverage gap: The ROCm CI containers (`ubuntu2004_rocm_5_4_0`, `ubuntu2204_rocm_6_0_0`) only run `build_rocm`, so neither `mem_query_dmabuf` (newly active for `rocm_cpy` because of the `try` default) nor the new `dmabuf_disable` test executes in this pipeline; an AMD GPU run of `gtest.uct.test_md*` is needed.


Residual coverage gap: `mem_type_detect_mds` keeps `EXPECT_EQ(address, mem_attr.base_address)` while relaxing the length check. That only holds if HSA page-rounds `sizeInBytes`; if HIP sub-allocates from a slab, `base_address` differs too and the strict check fails. Only hardware distinguishes the two.


Residual coverage gap: Nothing exercises a *failing* `hsa_amd_portable_export_dmabuf()` under the new `try` default, nor the `DMABUF=yes` + unsupported `md_open()` error path whose leak this PR fixes.


### svc-nvidia-pr-review · 2026-09-11

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 29cb04cbfdf715772748ef4b812e2f4c932fe571 -->

### svc-nvidia-pr-review · 2026-09-14

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: dcda33602157b318b7b879a1ff597e041272cdc3 -->

### svc-ucx · 2026-09-14

🤖 **CI Triage Agent** — `UCX PR (AddressSanitizer roce on worker 1)` · commit `dcda3360`

**TL;DR:** The "AddressSanitizer roce on worker 1" job was cancelled by Azure mid-step (not a test failure, no ASAN report) while still in the very first ASAN phase (`run_malloc_hook_gtest`); PR #11934 only touches `src/uct/rocm/*`, which this lane doesn't even build, so this is a CI wall-clock/cancellation issue — re-run the lane.

<details><summary>Full analysis</summary>

**Summary:** `Run ./contrib/test_jenkins.sh` in AddressSanitizer → roce → worker 1 ended with `##[error]The Operation will be canceled` / `The operation was canceled`, right after starting the "Running cuda hooks with far jump" gtest step.

**Root cause:** From the log, no test failed and no AddressSanitizer report was produced. The last work shown is `run_malloc_hook_gtest` (`contrib/test_jenkins.sh:1224-1251`): the `cuda_hooks.*` run completed cleanly (`Running 0 tests from 0 test suites` — the roce host has no GPU: `[ INFO ] srd,cuda_copy,rocm_copy is not available`), then the far-jump variant started at 14:44:02 and was cut at 14:44:04. Timestamps in the whole retrievable window are continuous (max gap ≈1.2 s), so nothing was hung at cancellation time — the job was killed from outside while making progress. Notably the cancellation hit several long-running "roce on worker N" jobs, including one from a *different* stage (Tests roce as well as AddressSanitizer roce), within the same second, which points at a build-level cancellation/timeout of the still-in-flight roce lanes rather than a per-test fault.

Contributing structural issue: worker 1 had spent the whole job budget without even reaching the main suite (`run_gtest_make`, `contrib/test_jenkins.sh:1315`). Each gtest phase in `run_malloc_hook_gtest` is wrapped in `TIMEOUT="timeout 200m"` (`contrib/test_jenkins.sh:45`) while the Azure job cap is `timeoutInMinutes: 360` (`buildlib/pr/tests.yml:17`). Two slow ASAN phases therefore exceed the job budget, and the inner `timeout` never gets to fail loudly — you get exactly this uninformative "canceled mid-step" signature instead of an actionable error.

**Implicated commit:** Not the PR commit. `dcda3360` / `29cb04cb` (David DeBonis, "UCT/ROCM: Enable dmabuf by default with peermem fallback") only touches `src/uct/rocm/base/rocm_base.c` and friends; the roce lane runs the `ubuntu2204_ib` container with no ROCm, so that code is not exercised.

**File:** `contrib/test_jenkins.sh:1246` (step that was cut); `contrib/test_jenkins.sh:45` and `buildlib/pr/tests.yml:17` (timeout budgets)

**Suggested fix:**
1. Re-run the AddressSanitizer roce lane — this build carries no evidence of a code defect from PR #11934.
2. If it recurs, make the failure diagnosable instead of a bare cancellation: shrink the per-phase budget so it fits inside the job cap (e.g. `TIMEOUT="timeout 60m"` for `ASAN_CHECK=yes`, since 2×200 m > 360 m), and/or wrap each `$TIMEOUT ... make -C test/gtest test` so a 124 exit prints which `GTEST_FILTER` phase blew up.
3. Add elapsed-time echoes around each phase of `run_malloc_hook_gtest` / `run_specific_tests` so the log shows which ASAN phase consumed the ~6 h before the cut (the current tail-only log cannot attribute it).
4. Check the roce agent host for over-subscription — four ASAN gtest workers plus the non-ASAN Tests roce matrix on the same pool is a plausible reason these lanes are the only ones still running at cancel time.

**Related:** PR #11934 (`UCT/ROCM: Enable dmabuf by default with peermem fallback`) — unrelated to this lane's failure; no existing issue found for the roce ASAN cancellation signature.

</details>

<!-- triage-agent request_id: 072b4c66-5fc0-4a20-baea-a512f97aa1f2 -->
<!-- triage-agent gha_run_id: azure:136001 -->
<!-- triage-agent-bot -->

### svc-nvidia-pr-review · 2026-09-15

🤖 Starting review — findings will be posted here when done.
<!-- pr-agent-review head_sha: 57c450e5e962ef4b460eb05a5c8806352cd5c14b -->

### svc-nvidia-pr-review · 2026-09-15

🤖 Review complete — no issues found.

## Review (36)

### svc-nvidia-pr-review · 2026-09-09 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-09 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-09 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-09 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-09 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### ddebonis-amd · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### copilot-pull-request-reviewer[bot] · 2026-09-10 · COMMENTED

### 🟡 Changes recommended

The newly added `rocm_dmabuf_disable` test is not scoped to the rocm_cpy MD and will fail when run against other MD instantiations (and contains a duplicate assertion).

*Once you've addressed the issues Copilot identified, you can request another Copilot review.*

<details>
<summary>Pull request overview</summary>

Enables ROCm dma-buf support by default (with fallback behavior when dma-buf is unavailable) and improves memory-query reporting for interior ROCm addresses so consumers get allocation-base address/length plus correct dma-buf offsets.

**Changes:**
- Switch ROCm copy MD dma-buf config default from `no` to `try`, and ensure dmabuf fields are suppressed/neutral when dma-buf isn’t active.
- Cache dmabuf-support detection and return allocation base address/length with corrected dma-buf offset for interior ROCm pointers.
- Update and extend gtest coverage and release notes for the new defaults/behavior.
</details>

<details>
<summary>File summaries</summary>

| File | Description |
| ---- | ----------- |
| test/gtest/uct/test_md.cc | Adjusts mem_query expectations for ROCm allocation length and adds a dmabuf-disable test (currently needs fixes). |
| src/uct/rocm/copy/rocm_copy_md.c | Enables dmabuf by default (`try`) and adds an MD-level mem_query wrapper to hide dmabuf fields when disabled/unavailable. |
| src/uct/rocm/base/rocm_base.c | Adds init-once caching for dmabuf support detection and fixes base-address/offset reporting for interior ROCm pointers. |
| NEWS | Documents the new default dma-buf behavior and the interior-pointer dmabuf offset/base fixes. |
| AUTHORS | Adds a new contributor entry. |
</details>

<details>
<summary>Review details</summary>

### Suppressed comments (1)

**test/gtest/uct/test_md.cc:1302**
* Duplicate EXPECT_EQ on dmabuf_offset; one of these lines should be removed.
```
    EXPECT_EQ(0ul, mem_attr.dmabuf_offset);
    EXPECT_EQ(0ul, mem_attr.dmabuf_offset);
```

- **Files reviewed:** 5/5 changed files
- **Comments generated:** 1
- **Review effort level:** Lite
</details>

---

💡 <a href="/openucx/ucx/new/master?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-10 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-11 · COMMENTED

(no text)

### svc-nvidia-pr-review · 2026-09-14 · COMMENTED

(no text)

### edgargabriel · 2026-09-16 · APPROVED

(no text)
