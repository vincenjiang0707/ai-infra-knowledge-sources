# [PR #19618] [None][chore] Setup the code review rule on release/1.3 branch

source: https://github.com/NVIDIA/TensorRT-LLM/pull/19618
state: open | updated: 2026-09-24T09:40:56Z
labels: 

## 正文

## Description

Enable the release-specific CODEOWNERS rule on `release/1.3` by uncommenting the existing `@NVIDIA/trt-llm-release-branch-approval` entry.

This is intentionally kept separate from the version bump so the approval requirement can be enabled at the appropriate release stage.

> [!IMPORTANT]
> Draft only. Merge when `release/1.3` is ready to require release-team approval. After merge, validate the rule with a subsequent PR targeting `release/1.3`; this PR itself cannot exercise the rule before it exists on the base branch.

## Test Coverage

- The change is limited to uncommenting the existing CODEOWNERS entry.
- `git diff --check` passed.
- Relevant pre-commit hooks and DCO check passed.

## 评论 (0)
