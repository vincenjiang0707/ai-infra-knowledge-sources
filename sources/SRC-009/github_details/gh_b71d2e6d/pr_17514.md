# [PR #17514] [Feature][EPLB] Integrate STAIR policy with Model Runner V2

source: https://github.com/vllm-project/vllm-ascend/pull/17514
state: open | updated: 2026-09-25T05:11:03Z
labels: documentation, module:tests, module:ops, module:core, ready-precise

## 正文

## What this PR does / why we need it?

This is PR 6/6 of the STAIR EPLB series proposed in #17506. It integrates the preceding data-plane, statistics, transfer, and planning components into Model Runner V2 without changing the legacy V1 policy path.

Common baseline: `9f1af00c7fcd7c22cf0c791ab9f6c8d98402ec06` (`upstream/main` on 2026-09-24).

Dependencies:

- #17508 — MRV2 routing and load-recording data plane
- #17509 — policy-prepared temporal load statistics
- #17510 — explicit-source asynchronous transfer
- #17511 — STAIR load and replica planner
- #17512 — constrained STAIR placement

The visible diff is cumulative while those dependencies are open. Review this PR after, or on top of, the five dependency PRs.

The integration:

- selects `default` or `stair` exclusively through the upstream `EPLBConfig.policy` field;
- keeps MRV2 `additional_config.eplb_config` limited to `load_collection_phase` and detailed `stair_config` tuning;
- constructs policies through a small factory and keeps the default policy on the upstream behavior;
- uses the existing EPLB CPU group to statically shard PP-stage-local layers and reconstruct the deterministic plan with all-gather, without a rank-0 planning coordinator;
- preserves weighted temporal bins through the common `rebalance_experts` entry point;
- runs STAIR's CPU planning in one persistent child process with bounded BLAS threads and a framed NPZ/JSON local protocol (`allow_pickle=False`);
- supplies stage-local node topology and per-layer ratios from the last placement that actually committed;
- attaches exact source coordinates for constrained asynchronous transfers and advances hysteresis anchors only after the corresponding layer commits;
- logs committed-cycle mean/p95 ratios before and after planning, changed-layer count, cross-rank transfer count, and cross-node transfer count.

The architecture intentionally keeps generic EPLB lifecycle and publication in `AscendEplbState`, policy-specific statistics and planning in `StairEplbPolicy`, and narrow upstream compatibility adapters in `patch_eplb.py`. This allows future policies to reuse the framework without inheriting STAIR's binning, covariance, subprocess, or placement constraints.

RFC: #17506

Upstream extensibility context: https://github.com/vllm-project/vllm/issues/33549

## Does this PR introduce _any_ user-facing change?

Yes. Model Runner V2 can select the STAIR EPLB policy and tune its advanced parameters. Defaults are intended to work without tuning. The upstream `default` policy remains selectable for experiments and compatibility.

## How was this patch tested?

- 263 focused CPU unit tests passed across STAIR planning, process protocol, layer sharding, policy statistics, explicit transfer, state, platform adapters, MRV2 controller, load collection, and configuration.
- Two source-tree-only checks were excluded locally: construction of a real NPU event on a CPU-only host, and a subprocess import check that requires the generated package `_build_info` module.
- `bash format.sh ci` passed, including Ruff, spelling, markdown, secret scan, forbidden-import, and repository-specific checks.
- Real NPU multi-node validation and CI are intentionally pending while this draft stack is reviewed and rebased independently.

- vLLM main: https://github.com/vllm-project/vllm/commit/ced6857afa0ea7b2e3f0846a62e1394e90f15607


## 评论 (4)

### github-actions[bot] · 2026-09-24

👋 Hi! Thank you for contributing to the vLLM Ascend project. The following points will speed up your PR merge:‌‌

- A PR should do only one thing, smaller PRs enable faster reviews.
- Every PR should include unit tests and end-to-end tests ‌to ensure it works and is not broken by other future PRs.
- Write the commit message by fulfilling the PR description to help reviewer and future developers understand.

If CI fails, you can run linting and testing checks locally according [Contributing](https://docs.vllm.ai/projects/ascend/zh-cn/latest/developer_guide/contribution/index.html) and [Testing](https://docs.vllm.ai/projects/ascend/zh-cn/latest/developer_guide/contribution/testing.html).

---

> [!TIP]
> ## :bulb: Consider Linking a Related Issue or RFC
>
> Your PR title contains the **[Feature]** tag, indicating a bug fix or new feature.
>
> Linking a related issue or RFC in the PR description is **strongly encouraged** — it gives reviewers helpful context and speeds up the review. You can use any of these keywords:
>
> - `Fixes #<issue_number>`
> - `Closes #<issue_number>`
> - `Resolves #<issue_number>`
> - `Refs #<rfc_or_issue_number>` (for RFCs)
>
> :pray: Thanks for helping us keep the project well-organized!


### gemini-code-assist[bot] · 2026-09-24

## Summary of Changes

Hello, I'm Gemini Code Assist[^1]! I'm currently reviewing this pull request and will post my feedback shortly. In the meantime, here's a summary to help you and other reviewers quickly get up to speed!

This pull request integrates the STAIR EPLB policy into Model Runner V2, providing a robust framework for expert load statistics and planning. By introducing a dedicated policy architecture and a persistent subprocess for CPU-bound planning, the changes enable more granular control over expert placement. The integration also includes comprehensive validation for configuration parameters and ensures that the new policy can coexist with the existing default policy.

### Highlights

* **STAIR Policy Integration**: Integrated the STAIR (Statistics, Transfer, and Planning) EPLB policy into Model Runner V2, allowing for advanced expert load balancing.
* **Planning Subprocess**: Implemented a persistent subprocess protocol for CPU-based planning, ensuring efficient computation without blocking the main model runner.
* **Explicit Transfer Execution**: Added explicit source coordinates for constrained asynchronous transfers, improving placement precision and control.
* **Configuration Extensibility**: Extended EPLBConfig to support STAIR-specific tuning parameters while maintaining compatibility with the default policy.
* **Deterministic Sharding**: Utilized the existing EPLB CPU group to statically shard PP-stage-local layers, enabling deterministic planning without a central coordinator.

<details>
<summary><b>New Features</b></summary>

🧠 You can now enable **Memory** (public preview) to help **Gemini Code Assist** learn from your team's feedback. This makes future code reviews more consistent and personalized to your project's style. **Click [here](https://codeassist.google/code-review/login) to enable Memory in your admin console.**
</details>

<details>
<summary><b>Using Gemini Code Assist</b></summary>
<br>

The full guide for Gemini Code Assist can be found on our [documentation page](https://developers.google.com/gemini-code-assist/docs/review-github-code), here are some quick tips.

<b>Invoking Gemini</b>

You can request assistance from Gemini at any point by creating a comment using either `/gemini <command>` or `@gemini-code-assist <command>`. Below is a summary of the supported commands on the current page.

Feature | Command | Description
--- | --- | ---
Code Review | `/gemini review` | Performs a code review for the current pull request in its current state.
Pull Request Summary | `/gemini summary` | Provides a summary of the current pull request in its current state.
Comment | @gemini-code-assist | Responds in comments when explicitly tagged, both in pull request comments and review comments.
Help | `/gemini help` | Displays a list of available commands.

<b>Customization</b>

To customize the Gemini Code Assist for GitHub experience, repository maintainers can create a configuration file and/or provide a custom code review style guide (such as PEP-8 for Python) by creating and adding files to a `.gemini/` folder in the base of the repository. Detailed instructions can be found [here](https://developers.google.com/gemini-code-assist/docs/customize-gemini-behavior-github).

<b>Limitations & Feedback</b>

Gemini Code Assist may make mistakes. Please leave feedback on any instances where its feedback is incorrect or counterproductive. You can react with :thumbsup: and :thumbsdown: on @gemini-code-assist comments. If you're interested in giving your feedback about your experience with Gemini Code Assist for GitHub and other Google products, sign up [here](https://google.qualtrics.com/jfe/form/SV_2cyuGuTWsEw84yG).
</details>


[^1]: Review the [Privacy Notices](https://policies.google.com/privacy), [Generative AI Prohibited Use Policy](https://policies.google.com/terms/generative-ai/use-policy), [Terms of Service](https://policies.google.com/terms), and learn how to configure Gemini Code Assist in GitHub [here](https://developers.google.com/gemini-code-assist/docs/customize-gemini-behavior-github). Gemini can make mistakes, so double check it and [use code with caution](https://support.google.com/legal/answer/13505487).


### freyfwt · 2026-09-25

/rerun
[Bot]: rerun completed.

**Failed:**
- E2E: still in progress, retry /rerun after completion

### freyfwt · 2026-09-25

/rerun
[Bot]: rerun completed.

**Rerun (failed jobs only):**
- E2E

## Review (1)

### gemini-code-assist[bot] · 2026-09-24 · COMMENTED

## Code Review

**Suggested PR Title:**

```txt
[Ops][Feature] Implement STAIR EPLB Policy for Model Runner V2 on Ascend
```

**Suggested PR Summary:**

```markdown
### What this PR does / why we need it?
This pull request implements the STAIR (expert parallelism load balancer) policy for Model Runner V2 on Ascend. It introduces advanced covariance-aware risk and hysteresis-based load balancing, custom load statistics preparation, and explicit layer transfers. It also refactors the MoE operators to use operator-provided expert counts for load recording and adds extensive unit and end-to-end tests.

Critical feedback has been provided regarding the asynchronous communicator, where global ranks must be translated to group-local ranks to avoid out-of-bounds errors. Additionally, safety checks should be added to the wait loops in the state manager to detect background thread termination and prevent silent hangs.

### Does this PR introduce _any_ user-facing change?
Yes, it adds the `stair_config` configuration option under `additional_config.eplb_config` for Model Runner V2 and sets STAIR as the default EPLB policy on Ascend.

### How was this patch tested?
Tested via newly added unit tests in `tests/ut/distributed/eplb/` and end-to-end tests in `tests/e2e/`.
```
