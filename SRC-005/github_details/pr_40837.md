# [PR #40837] [Bug][LoRA] Honor PEFT use_rslora when computing adapter scaling

source: https://github.com/sgl-project/sglang/pull/40837
state: open | updated: 2026-09-23T03:48:27Z
labels: lora

## 正文

## Motivation

PEFT adapters saved with `use_rslora=True` store that flag in `adapter_config.json` and scale updates by `lora_alpha / sqrt(r)`. SGLang's `LoRAAdapter` always used `lora_alpha / r`, so rsLoRA adapters load silently but apply updates that are `sqrt(r)` times too weak (e.g. 8× at `r=64`).

Fixes #40835

## Modifications

- Read `use_rslora` from the PEFT adapter config (default `False`).
- Compute scaling as `alpha / sqrt(r)` when rsLoRA is enabled, matching PEFT/vLLM.
- Add a CPU unit test covering standard LoRA, rsLoRA, and the default-off path.

## Accuracy Tests

```bash
PYTHONPATH=python python3 -m pytest test/registered/unit/lora/test_rslora_scaling.py -v
```

3 passed locally (CPU).

## Checklist

- [x] Format code & doc strings
- [x] Add tests

<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:x: [Run #35815742140](https://github.com/sgl-project/sglang/actions/runs/35815742140)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #35815741655](https://github.com/sgl-project/sglang/actions/runs/35815741655)<!-- slot:pr-test-extra:end -->
Latest PR Test (AMD ROCm 10): <!-- slot:pr-test-amd:start -->:x: [Run #35815741811](https://github.com/sgl-project/sglang/actions/runs/35815741811)<!-- slot:pr-test-amd:end -->
<!-- pr-states:end -->

## 评论 (0)

## Review (1)

### copilot-pull-request-reviewer[bot] · 2026-09-23 · COMMENTED

Copilot was unable to review this pull request because the user who requested the review has reached their quota limit.
