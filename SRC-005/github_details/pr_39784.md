# [PR #39784] [NPU][HiCache] Support DeepSeek V4 L3 with MemCache

source: https://github.com/sgl-project/sglang/pull/39784
state: open | updated: 2026-09-23T03:00:34Z
labels: documentation, deepseek, hicache, npu, unified-radix-cache, memory-pool

## 正文

## Motivation

Add DeepSeek V4 NPU HiCache L3 support using the existing upstream `npu_memcache` backend, on top of the DSV4 L2 work in #37382 and the MemCache backend in #38827.

This adapts the downstream implementation from https://github.com/cx22757/sglang/pull/2 to current upstream main (`7d5696b3a`). It does **not** re-import the backend from #26043 or introduce a parallel `ascend_memcache` backend.

## Modifications

Exactly two commits:

1. **Business implementation**
   - Add C128 L3 backup/prefetch descriptors, complete-group alignment, and physical-object coverage relative to FULL logical pages.
   - Follow upstream's query-before-allocation lifecycle: size C128 staging first, resolve endpoint keys from the controller's namespaced hash chain, and allocate only after the storage hit is known.
   - Treat the DSV4 FULL anchor as logical; require successful reads/writes of the physical C4, Indexer/scale, SWA, C128 and state pools before publishing a usable prefix.
   - Preserve progress/pool/terminal ACK sequencing on storage I/O failure and validate result cardinality.
   - Defer DSV4 SDMA/RDMA runtime initialization to the first backup; stage SDMA through ordinary CPU memory and retain registered Host buffers for RDMA. Metadata-only clear remains usable before runtime initialization.
   - Reset request-scoped C128 state on cache flush and propagate storage-clear failures.
   - Keep upstream MLA/DSA FP8, Indexer scale, DRAFT and conv-only MAMBA handling; document DSV4 layout and lifecycle requirements.
2. **Unit tests**
   - DSV4 storage contracts, lazy initialization, metadata clear, byte round-trip through staging, complete C128 groups, partial hits, failed-read ACKs and Host-pool dispatch.
   - Upstream adaptation regressions for deferred staging, namespaced C128 keys and existing MLA/DSA/DRAFT/MAMBA key layouts.
   - Cache-flush state cleanup and server-argument layout regression tests.

No performance harnesses or temporary diagnostic instrumentation are included.

## Accuracy Tests

**Draft: NPU end-to-end accuracy testing on this upstream-adapted branch is pending.**

The previous downstream tree passed 8K (16 requests) and 128K (32 requests) resident-cache versus L3 output-token equality checks on 16 NPUs with CANN 9.0.0 and `device_sdma`. Those historical results are **not** claimed as validation of this new upstream adaptation.

Local CPU validation on the adapted tree: **77 passed, 1 skipped, 16 subtests passed**
for the first command; **4 passed** for the second command.

```bash
PYTHONPATH=python python -m pytest -q \
  test/registered/unit/mem_cache/test_dsv4_memcache_l3_contract.py \
  test/registered/unit/mem_cache/test_storage_prefetch_lifecycle.py \
  test/registered/unit/mem_cache/test_hicache_staged_write_back_dispatch.py \
  test/registered/unit/server_args/test_server_args.py::TestHiCacheArgs

PYTHONPATH=python python -m pytest -q \
  test/registered/unit/npu/attention/test_npu_dsv4_compressor_cache_mode.py
```

The NPU-path unit test uses mocked accelerator dependencies; it is not a hardware/operator test. Full server-argument collection was also attempted: 253 tests passed and one was skipped across the server-argument/staged-dispatch files, while an unrelated gRPC startup test failed because the local environment lacks `uvicorn`. The targeted HiCache tests above pass.

## Speed Tests and Profiling

Not run on this adapted branch. No performance improvement claims; hardware replay correctness and throughput validation remain pending.

## Checklist

- [ ] Full pre-commit suite (isort, Ruff formatting, Ruff F401/F821/UP037, and `git diff --check` checked locally).
- [x] Add unit tests.
- [x] Update backend documentation.
- [ ] NPU end-to-end accuracy and speed validation on this branch.
- [x] Use the existing upstream backend and keep business/test changes in separate commits.

<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:x: [Run #35812555851](https://github.com/sgl-project/sglang/actions/runs/35812555851)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #35812555956](https://github.com/sgl-project/sglang/actions/runs/35812555956)<!-- slot:pr-test-extra:end -->
Latest PR Test (AMD ROCm 10): <!-- slot:pr-test-amd:start -->:x: [Run #35812555968](https://github.com/sgl-project/sglang/actions/runs/35812555968)<!-- slot:pr-test-amd:end -->
<!-- pr-states:end -->

## 评论 (1)

### cocoa-liu · 2026-09-17

## Performance test results / 性能测试结果

Completed measurements: (1) the 128K local/remote L3 matrix, and (2) the 48K L1/L2/L3 cache-tier comparison.

These are previously measured results, not a fresh rerun of the current PR HEAD. The two datasets use different configurations and should be interpreted separately.

### 1. Completed 128K L3 performance tests

Source: Excel sheet “L3指标明细”, 128K rows, cross-checked against the saved test report.

Recorded configuration: DeepSeek-V4-Flash-w8a8-mtp; 16 NPUs, TP16/DP16/EP16, DeepEP normal; `page_first_direct`, `write_through`. Local L3 used `device_sdma` with a 224 GiB Holder; remote L3 used `device_rdma` with five 112 GiB Holders (560 GiB total). Measurement concurrency was 16. Each warm case used 64 requests in four 16-request cohorts.

#### Recorded latency, throughput, and actual storage hits

| Input tokens | Target hit | Cold TTFT (ms) | Local L3 TTFT (ms) | Remote L3 TTFT (ms) | Cold input TPS | Local L3 input TPS | Remote L3 input TPS | Local / remote actual L3 hit |
|---|---|---:|---:|---:|---:|---:|---:|---|
| 128K (131,072) | 100% | 72,876.7 | 14,208.1 | 15,508.3 | 22,337.0 | 147,598.2 | 135,224.4 | 98.4375% / 98.4375% |
| 128K (131,072) | 50% | 72,876.7 | 54,518.5 | 65,966.6 | 22,337.0 | 38,466.5 | 31,790.9 | 50% / 50% |

#### Gains recorded in the workbook, relative to its cold baseline

| Input / target hit | Local TTFT reduction | Remote TTFT reduction | Local input TPS increase | Remote input TPS increase |
|---|---:|---:|---:|---:|
| 128K / 100% | 80.5% | 78.7% | 560.8% | 505.4% |
| 128K / 50% | 25.2% | 9.5% | 72.2% | 42.3% |

- TTFT reduction = `(cold - warm) / cold`; TPS increase = `(warm - cold) / cold`. These are cold/warm comparisons, **not L1-hit versus L3-hit comparisons**. The workbook cites a 24-request cold baseline; each warm case uses 64 requests.
- The “100%” cases reused all but the final 2,048-token tail: `129024/131072 = 98.4375%`.
- Both local and remote 128K cases completed at the 100% and 50% target hit settings. The recorded formal runs had no eviction or service exceptions.

### 2. L3 versus L1 cache-hit performance

Sources: `formal-48k-8dp-5rounds.json` and its benchmark log, with the matching `bench_cache_tiers.py` script.

Measured workload: **48K = 49,152 input tokens, 8 DP ranks, concurrency 8, five rounds, 40 measured requests per tier**. The script requests one generated token with temperature 0. Each measured request reused exactly **47,104 tokens**; the remaining 2,048-token tail was outside the reused prefix.

Tier selection was verified from all 40 response records per tier:

| Tier | `device` cached tokens/request | `host` cached tokens/request | `storage` cached tokens/request |
|---|---:|---:|---:|
| L1 | 47,104 | 0 | 0 |
| L2 | 0 | 47,104 | 0 |
| L3 | 0 | 0 | 47,104 |

| Cache source | Mean client request latency (ms) | P50 (ms) | P90 (ms) | Mean server E2E (ms) | Input TPS |
|---|---:|---:|---:|---:|---:|
| L1 | 2,585.39 | 2,601.72 | 2,636.80 | 2,535.71 | 148,682.98 |
| L2 | 5,966.98 | 5,975.23 | 6,053.86 | 5,917.02 | 65,247.91 |
| L3 | 6,231.52 | 6,226.48 | 6,373.89 | 6,187.59 | 62,198.24 |

**L3 versus L1 in this workload:**

- Mean client latency increased by **3,646.14 ms**, or **141.03%** (L3 latency was **2.4103×** L1).
- Input throughput decreased by **58.17%** (L3 retained **41.83%** of L1 input throughput).
- Mean server E2E latency increased by **144.02%**.

For context, L3 versus L2 increased mean client latency by **4.43%** and decreased input throughput by **4.67%**.

Interpretation: these are **end-to-end request measurements**, including cache restoration and remaining model work, not isolated memory-copy bandwidth or pure L3 get latency. Input TPS is total submitted input tokens divided by measured wave wall time, including cached input tokens; it is not output-token throughput. Do not describe the result as “128K is 58.17% slower”: this comparison was measured at 48K only.

中文结论：128K 本机/跨机 L3 的两种命中场景均已完成测试。另一个 48K 分层对比中，L3 相比 L1 的平均请求延迟增加 141.03%，输入吞吐下降 58.17%；相比 L2，平均请求延迟增加 4.43%。两组测试条件不同，分别解读。

